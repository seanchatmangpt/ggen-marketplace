PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?repo) AS ?fanout) WHERE { ?candidate a reo:ReplicationCandidate ; reo:observedInRepo ?repo . } GROUP BY ?candidate HAVING (?fanout >= 10)
