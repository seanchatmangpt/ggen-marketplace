PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate ?latency WHERE { ?candidate a reo:ReplicationCandidate ; reo:observationLatencySeconds ?latency . } ORDER BY DESC(?latency)
