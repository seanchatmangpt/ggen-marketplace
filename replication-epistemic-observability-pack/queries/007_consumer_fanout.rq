PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?consumer) AS ?fanout) WHERE { ?candidate a reo:ReplicationCandidate . OPTIONAL { ?consumer reo:consumes ?candidate . } } GROUP BY ?candidate ORDER BY DESC(?fanout)
