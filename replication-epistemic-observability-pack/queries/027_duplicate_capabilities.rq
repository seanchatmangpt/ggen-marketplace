PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?a ?b WHERE { ?a a reo:ReplicationCandidate ; reo:semanticDigest ?d . ?b a reo:ReplicationCandidate ; reo:semanticDigest ?d . FILTER(STR(?a) < STR(?b)) }
