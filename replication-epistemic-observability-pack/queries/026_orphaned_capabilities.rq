PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate WHERE { ?candidate a reo:ReplicationCandidate . FILTER NOT EXISTS { ?consumer reo:consumes ?candidate . } FILTER NOT EXISTS { ?candidate reo:relieves ?dependency . } }
