[toolchain]
# Pinned to nightly: this crate's dependency `wasm4pm-compat` (transitively required
# by `star-toml`, this crate's config-validation dependency — see
# `cargo tree -i wasm4pm-compat` from this directory) unconditionally enables
# nightly-only features (`generic_const_exprs`, `adt_const_params`,
# `unsized_const_params`, `const_trait_impl`, `min_specialization`,
# `portable_simd`) in every published version. There is no stable-toolchain
# escape from this dependency graph today. Mirrors ~/ggen's root
# rust-toolchain.toml pin for the same reason.
channel = "nightly-2026-06-22"
