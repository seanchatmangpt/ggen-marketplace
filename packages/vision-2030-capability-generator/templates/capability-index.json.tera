[
{% for c in results %}  {"rank":{{ loop.index }},"name":{{ c.name | json_encode }},"family":{{ c.family | json_encode }},"definition":{{ c.definition | json_encode }},"problem_eliminated":{{ c.problem | json_encode }},"primitives":{{ c.primitives | json_encode }},"ggen_role":{{ c.ggenRole | json_encode }},"leverage_score":{{ c.score }},"required_standing":{{ c.standing | json_encode }},"primary_falsifier":{{ c.falsifier | json_encode }},"consequence_2030":{{ c.consequence | json_encode }}}{% if not loop.last %},{% endif %}
{% endfor %}]
