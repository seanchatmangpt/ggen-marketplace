# GENERATED by ggen-marketplace/castle-paas-pack. Do not hand-edit.
# Semantic mapping is intentionally limited to non-secret identity/standing/digest fields.
{% for row in results %}
defmodule {{ row["module"] }} do
  use Ash.Resource,
    domain: CastlePaaS.Domain,
    data_layer: AshPostgres.DataLayer,
    extensions: [AshR2RML, AshJsonApi.Resource]

  postgres do
    table "{{ row["table"] }}"
    repo CastlePaaS.Repo
  end

  r2rml do
    class_iri("{{ row["class"] }}")
    subject_template("https://castle.chatmangpt.com/{{ row["table"] }}/{id}")
    table_name("{{ row["table"] }}")
    attribute_mappings([
      {:external_id, "http://purl.org/dc/terms/identifier"},
      {:label, "http://www.w3.org/2000/01/rdf-schema#label"},
      {:standing, "http://purl.org/dc/terms/type"},
      {:digest, "http://www.w3.org/ns/prov#value"}
    ])
  end

  json_api do
    type "{{ row["table"] }}"
  end

  actions do
    read :read do
      primary? true
    end

    create :record do
      primary? true
      public? false
      accept [:external_id, :label, :standing, :digest, :metadata]
    end

    update :revise do
      public? false
      accept [:label, :standing, :digest, :metadata]
    end
  end

  attributes do
    uuid_primary_key :id
{% if row["tenantScoped"] %}    attribute :tenant_id, :uuid, allow_nil?: false, public?: false
{% endif %}    attribute :external_id, :string, allow_nil?: false, public?: true
    attribute :label, :string, allow_nil?: true, public?: true
    attribute :standing, :atom,
      allow_nil?: false,
      default: :UNKNOWN,
      public?: true,
      constraints: [one_of: [:UNKNOWN, :PARTIAL_ALIVE, :ALIVE, :BLOCKED, :BUILD_BROKEN, :UNSUPPORTED, :REFUSED]]
    attribute :digest, :string, allow_nil?: true, public?: true
    attribute :metadata, :map, allow_nil?: false, default: %{}, public?: false
    timestamps()
  end

{% if row["tenantScoped"] %}  multitenancy do
    strategy :attribute
    attribute :tenant_id
  end
{% endif %}
end
{% endfor %}
