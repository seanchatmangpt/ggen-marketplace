PREFIX cp: <https://ggen.dev/ontology/castle-paas#>
SELECT ?prefix ?iri ?required WHERE {
  ?ontology a cp:PublicOntology ;
    cp:ontologyPrefix ?prefix ;
    cp:ontologyIri ?iri ;
    cp:required ?required .
}
ORDER BY ?prefix
