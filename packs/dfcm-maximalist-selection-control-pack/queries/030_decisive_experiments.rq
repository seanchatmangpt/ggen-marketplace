PREFIX dms: <https://ggen.dev/ontology/dfcm-maximalist-selection-control#>
SELECT ?c ?u ?r WHERE {
  ?c a dms:Candidate ; dms:uncertainty ?u ; dms:rollbackCost ?r .
  FILTER(?u > 0.20 && ?r <= 2)
}
ORDER BY DESC(?u) ?r ?c
