PREFIX dms: <https://ggen.dev/ontology/dfcm-maximalist-selection-control#>
SELECT ?left ?right WHERE {
  ?left a dms:Candidate ; dms:compatibleWith ?right .
  ?right a dms:Candidate ; dms:compatibleWith ?left .
  FILTER(STR(?left) < STR(?right))
}
ORDER BY ?left ?right
