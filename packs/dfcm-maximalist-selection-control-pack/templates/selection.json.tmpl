{
  "schema": "ggen.dfcm-selection-consequence/1",
  "authority": "SELECT",
  "actuation_performed": false,
  "selected": [
  {% for row in rows %}{"candidate":"{{ row.c }}","evidence":"{{ row.e }}","uncertainty":"{{ row.u }}","rollback":"{{ row.r }}","dependency_relief":"{{ row.d }}","reuse_capital":"{{ row.k }}"}{% if not loop.last %},{% endif %}
  {% endfor %}]
}
