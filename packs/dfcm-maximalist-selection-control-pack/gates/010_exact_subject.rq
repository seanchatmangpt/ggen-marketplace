PREFIX dms: <https://ggen.dev/ontology/dfcm-maximalist-selection-control#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?candidate a dms:Candidate .
    FILTER NOT EXISTS { ?candidate dms:requiresReceipt true }
  }
}
