PREFIX dms: <https://ggen.dev/ontology/dfcm-maximalist-selection-control#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?c a dms:Candidate .
    FILTER NOT EXISTS { ?c dms:evidence ?e ; dms:uncertainty ?u ; dms:rollbackCost ?r }
  }
}
