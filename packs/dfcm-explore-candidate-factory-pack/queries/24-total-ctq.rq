PREFIX dfcm: <https://seanchatmangpt.github.io/ggen-marketplace/dfcm/explore#>
SELECT ?candidate (?n + ?e + ?r AS ?total) WHERE { ?candidate a dfcm:Candidate ; dfcm:ctqNovelty ?n ; dfcm:ctqEvidence ?e ; dfcm:ctqRollback ?r . }
ORDER BY DESC(?total) ?candidate