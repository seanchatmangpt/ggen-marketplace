PREFIX dfcm: <https://seanchatmangpt.github.io/ggen-marketplace/dfcm/explore#>
SELECT ?candidate ?r ?e ?n WHERE { ?candidate a dfcm:Candidate ; dfcm:ctqRollback ?r ; dfcm:ctqEvidence ?e ; dfcm:ctqNovelty ?n . }
ORDER BY DESC(?r) DESC(?e) DESC(?n) ?candidate