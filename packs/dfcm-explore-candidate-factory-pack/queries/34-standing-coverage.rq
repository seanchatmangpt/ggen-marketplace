PREFIX dfcm: <https://seanchatmangpt.github.io/ggen-marketplace/dfcm/explore#>
SELECT ?standing (COUNT(?candidate) AS ?candidateCount) WHERE {
  ?candidate a dfcm:Candidate ; dfcm:standing ?standing .
} GROUP BY ?standing ORDER BY DESC(?candidateCount) ?standing
