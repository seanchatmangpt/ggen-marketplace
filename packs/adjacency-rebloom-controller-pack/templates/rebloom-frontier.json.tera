{
  "schema": "rebloom.frontier.v1",
  "candidates": [
  {% for row in results %}{"candidate":"{{ row.candidate }}","expected_reuse":{{ row.reuse }},"capability_space_delta":{{ row.delta }}}{% if not loop.last %},{% endif %}
  {% endfor %}],
  "selection": "preserve_non_dominated"
}
