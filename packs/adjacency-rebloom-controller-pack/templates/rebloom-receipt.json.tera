{
  "schema": "rebloom.receipt.v1",
  "entries": [
  {% for row in results %}{"candidate":"{{ row.candidate }}","qualification":"{{ row.qualification }}","actuation":"{{ row.actuation }}"}{% if not loop.last %},{% endif %}
  {% endfor %}],
  "standing": "constructed_not_actuated"
}
