PREFIX rebloom: <https://seanchatmangpt.github.io/ggen-marketplace/ontology/adjacency-rebloom#>
SELECT ?candidate (COUNT(?cap) AS ?branchingFactor) WHERE {
  ?candidate a rebloom:CompositionCandidate ; rebloom:uses ?cap .
}
GROUP BY ?candidate
ORDER BY DESC(?branchingFactor) ?candidate
