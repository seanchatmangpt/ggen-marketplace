PREFIX rebloom: <https://seanchatmangpt.github.io/ggen-marketplace/ontology/adjacency-rebloom#>
SELECT ?cap ?delta WHERE {
  ?cap a rebloom:QualifiedCapability ; rebloom:capabilitySpaceDelta ?delta .
}
ORDER BY DESC(?delta) ?cap
