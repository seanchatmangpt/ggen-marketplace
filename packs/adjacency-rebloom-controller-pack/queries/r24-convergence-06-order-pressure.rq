PREFIX rebloom: <https://seanchatmangpt.github.io/ggen-marketplace/ontology/adjacency-rebloom#>
SELECT ?wave ?order (?order / 3.0 AS ?orderPressure) WHERE {
  ?wave a rebloom:RebloomWave ; rebloom:maxOrder ?order .
}
ORDER BY DESC(?orderPressure) ?wave
