PREFIX rebloom: <https://seanchatmangpt.github.io/ggen-marketplace/ontology/adjacency-rebloom#>
SELECT ?candidate ?qualification ?actuation WHERE {
  ?candidate a rebloom:CompositionCandidate ;
    rebloom:qualification ?qualification ;
    rebloom:actuation ?actuation .
}
ORDER BY STR(?candidate)
