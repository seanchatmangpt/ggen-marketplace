PREFIX rebloom: <https://seanchatmangpt.github.io/ggen-marketplace/ontology/adjacency-rebloom#>
SELECT ?candidate ?actuation WHERE {
  ?candidate a rebloom:CompositionCandidate ; rebloom:actuation ?actuation .
  FILTER(?actuation = "construct-only")
}
ORDER BY ?candidate
