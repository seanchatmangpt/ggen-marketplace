PREFIX rebloom: <https://seanchatmangpt.github.io/ggen-marketplace/ontology/adjacency-rebloom#>
SELECT ?wave ?depth (?depth / 3.0 AS ?depthPressure) WHERE {
  ?wave a rebloom:RebloomWave ; rebloom:maxDepth ?depth .
}
ORDER BY DESC(?depthPressure) ?wave
