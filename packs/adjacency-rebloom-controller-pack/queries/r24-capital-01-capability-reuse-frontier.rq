PREFIX rebloom: <https://seanchatmangpt.github.io/ggen-marketplace/ontology/adjacency-rebloom#>
SELECT ?cap ?reuse WHERE {
  ?cap a rebloom:QualifiedCapability ; rebloom:expectedReuse ?reuse .
}
ORDER BY DESC(?reuse) ?cap
