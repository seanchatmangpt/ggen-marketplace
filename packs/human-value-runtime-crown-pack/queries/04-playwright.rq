PREFIX hv: <https://ggen.dev/ontology/human-value-runtime#>
SELECT ?journey ?receipt WHERE {
  ?journey a hv:PlaywrightJourney .
  ?receipt a hv:ValueReceipt ; hv:journey ?journey .
}
ORDER BY STR(?journey) STR(?receipt)
