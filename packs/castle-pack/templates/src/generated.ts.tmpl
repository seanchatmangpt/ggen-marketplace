// GENERATED by ggen-marketplace/castle-pack from ontology.ttl. Do not hand-edit.
export const GENERATED_BINDINGS = [
{% for row in results %}  { kind: "{{ row["kind"] }}", order: {{ row["order"] }}, identifier: "{{ row["identifier"] }}", slug: "{{ row["slug"] }}", role: "{{ row["role"] }}", authority: "{{ row["authority"] }}", predicate: "{{ row["predicate"] }}", consequence: {{ row["consequence"] }} }{% if not loop.last %},{% endif %}
{% endfor %}] as const;

export const GENERATED_COMPONENTS = GENERATED_BINDINGS.filter((binding) => binding.kind === "component");
export const DEFAULT_ADVERSARIAL_GOALS = GENERATED_BINDINGS
  .filter((binding) => binding.kind === "goal")
  .map((binding) => ({
    id: binding.slug,
    predicate: binding.predicate,
    consequence: binding.consequence,
  }));
