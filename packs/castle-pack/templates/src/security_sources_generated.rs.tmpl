// GENERATED by ggen-marketplace/castle-pack from security-universe.ttl. Do not hand-edit.

#[derive(Debug, Clone, Copy, PartialEq, Eq, Hash)]
pub enum SecurityKind {
    GovernanceFramework,
    ControlCatalog,
    Assurance,
    Regulation,
    CloudBaseline,
    ApplicationSecurity,
    AiSecurity,
    IndustrialControl,
    ThreatKnowledge,
    VulnerabilityKnowledge,
    DetectionLanguage,
    TelemetrySchema,
    SupplyChain,
    EvidenceExchange,
    SecurityOntology,
    PolicyLanguage,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum VersionPolicy {
    Pinned,
    Rolling,
}

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct SecuritySource {
    pub id: &'static str,
    pub authority: &'static str,
    pub version: &'static str,
    pub version_policy: VersionPolicy,
    pub kind: SecurityKind,
    pub machine_surface: &'static str,
    pub source_uri: &'static str,
}

pub const SECURITY_SOURCES: &[SecuritySource] = &[
{% for row in results %}    SecuritySource { id: "{{ row["id"] }}", authority: "{{ row["authority"] }}", version: "{{ row["version"] }}", version_policy: VersionPolicy::{{ row["versionPolicy"] }}, kind: SecurityKind::{{ row["kind"] }}, machine_surface: "{{ row["machineSurface"] }}", source_uri: "{{ row["sourceUri"] }}" },
{% endfor %}];
