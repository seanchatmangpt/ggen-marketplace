// GENERATED by ggen-marketplace/castle-pack from ontology.ttl. Do not hand-edit.
export const FORTUNE5_REQUIREMENTS = [
{% for row in results %}  { order: {{ row["order"] }}, controlId: "{{ row["controlId"] }}", category: "{{ row["category"] }}", description: "{{ row["description"] }}", metric: "{{ row["metric"] }}", comparator: "{{ row["comparator"] }}", target: "{{ row["target"] }}", authority: "{{ row["authority"] }}" }{% if not loop.last %},{% endif %}
{% endfor %}] as const;

export type GeneratedFortune5Requirement = (typeof FORTUNE5_REQUIREMENTS)[number];
