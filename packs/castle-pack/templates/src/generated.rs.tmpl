// GENERATED by ggen-marketplace/castle-pack from ontology.ttl. Do not hand-edit.

pub struct GeneratedBinding {
    pub kind: &'static str,
    pub order: u32,
    pub identifier: &'static str,
    pub slug: &'static str,
    pub role: &'static str,
    pub authority: &'static str,
    pub predicate: &'static str,
    pub consequence: i64,
}

pub struct DefaultAdversarialGoal {
    pub id: &'static str,
    pub predicate: &'static str,
    pub consequence: i64,
}

pub const GENERATED_BINDINGS: &[GeneratedBinding] = &[
{% for row in results %}    GeneratedBinding { kind: "{{ row["kind"] }}", order: {{ row["order"] }}, identifier: "{{ row["identifier"] }}", slug: "{{ row["slug"] }}", role: "{{ row["role"] }}", authority: "{{ row["authority"] }}", predicate: "{{ row["predicate"] }}", consequence: {{ row["consequence"] }} },
{% endfor %}];

pub fn generated_components() -> impl Iterator<Item = &'static GeneratedBinding> {
    GENERATED_BINDINGS.iter().filter(|binding| binding.kind == "component")
}

pub fn default_adversarial_goals() -> Vec<DefaultAdversarialGoal> {
    GENERATED_BINDINGS
        .iter()
        .filter(|binding| binding.kind == "goal")
        .map(|binding| DefaultAdversarialGoal {
            id: binding.slug,
            predicate: binding.predicate,
            consequence: binding.consequence,
        })
        .collect()
}
