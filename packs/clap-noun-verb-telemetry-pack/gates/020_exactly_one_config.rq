# MESSAGE: exactly one cnv-telemetry:Config individual must be admitted; any row (count != 1) is a refusal.
PREFIX cnv-telemetry: <https://clap-noun-verb.dev/ontology/telemetry#>
SELECT (COUNT(?subject) AS ?count) WHERE {
  ?subject a cnv-telemetry:Config .
}
HAVING (COUNT(?subject) != 1)
