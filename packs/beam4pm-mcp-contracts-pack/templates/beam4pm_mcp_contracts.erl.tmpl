---
to: src/beam4pm_mcp_contracts.erl
sparql:
  records: |
    PREFIX mcp: <https://ggen.dev/ontology/beam4pm-mcp#>
    SELECT ?record_name ?record_doc WHERE {
      ?rt a mcp:MessageType ; mcp:messageName ?record_name ; mcp:messageDoc ?record_doc .
    } ORDER BY ?record_name
  fields: |
    PREFIX mcp: <https://ggen.dev/ontology/beam4pm-mcp#>
    SELECT ?record_name ?field_name ?field_type ?field_doc ?field_required ?field_order WHERE {
      ?rt a mcp:MessageType ; mcp:messageName ?record_name ; mcp:hasField ?f .
      ?f mcp:fieldName ?field_name ; mcp:fieldType ?field_type ; mcp:fieldDoc ?field_doc ;
         mcp:fieldRequired ?field_required ; mcp:fieldOrder ?field_order .
    } ORDER BY ?record_name ?field_order
---
%% GENERATED by ggen from the admitted mcp:MessageType graph. Do not edit.

-module(beam4pm_mcp_contracts).

-export([
{%- for r in records %}
    new_{{ r.record_name }}/1{% if not loop.last %},{% endif %}
{%- endfor %}
]).

-export_type([
{%- for r in records %}
    {{ r.record_name }}/0{% if not loop.last %},{% endif %}
{%- endfor %}
]).

{% for r in records -%}
{%- set rfields = fields | filter(attribute="record_name", value=r.record_name) -%}
%% {{ r.record_doc }}
-record({{ r.record_name }}, {
{%- for f in rfields %}
{%- set f_optional_str = f.field_required == "false" %}
{%- set f_optional_bool = f.field_required == false %}
    {{ f.field_name }} :: {% if f.field_type == "string" %}binary(){% elif f.field_type == "integer" %}integer(){% elif f.field_type == "float" %}float(){% elif f.field_type == "boolean" %}boolean(){% elif f.field_type == "datetime" %}binary(){% elif f.field_type == "atom" %}atom(){% elif f.field_type == "list_string" %}[binary()]{% elif f.field_type == "map" %}map(){% endif %}{% if f_optional_str or f_optional_bool %} | undefined{% endif %}{% if not loop.last %},{% endif %} %% {{ f.field_name }}: {{ f.field_doc }}
{%- endfor %}
}).

-type {{ r.record_name }}() :: #{{ r.record_name }}{}.

-spec new_{{ r.record_name }}(map()) -> {ok, {{ r.record_name }}()} | {error, {missing_field, atom()}}.
{%- set required_fields_str = rfields | filter(attribute="field_required", value="true") %}
{%- set required_fields_bool = rfields | filter(attribute="field_required", value=true) %}
{%- set required_fields = required_fields_str | concat(with=required_fields_bool) %}
new_{{ r.record_name }}(Map) ->
{%- for f in required_fields %}
    case maps:is_key({{ f.field_name }}, Map) of
        false -> {error, {missing_field, {{ f.field_name }}}};
        true ->
{%- endfor %}
    {ok, #{{ r.record_name }}{
{%- for f in rfields %}
        {{ f.field_name }} = maps:get({{ f.field_name }}, Map, undefined){% if not loop.last %},{% endif %}
{%- endfor %}
    }}
{%- for f in required_fields %}
    end
{%- endfor %}.

{% endfor -%}
