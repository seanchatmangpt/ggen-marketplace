---
to: docs/CONSTITUTION-pack.md
sparql:
  laws: "PREFIX ccn: <http://seanchatmangpt.github.io/packs/ggen-constitution#> SELECT ?num ?label ?statement ?enforcedBy ?mechanized ?note WHERE { ?law a ccn:Law ; ccn:principleNumber ?num ; ccn:principleLabel ?label ; ccn:statement ?statement ; ccn:enforcedBy ?enforcedBy ; ccn:mechanized ?mechanized . OPTIONAL { ?law ccn:note ?note } } ORDER BY ?num"
---
{#- CONSTITUTION-pack.md — frontmatter-schema twin of ../root-templates/constitution.md.tmpl.
    DISCLOSED DUPLICATION: that sibling template is consumed by root ggen.toml's
    declarative rule `docs-constitution-mirror` ([[generation.rules]]), whose schema does
    NOT read a template's own frontmatter `sparql:` block — it calls sparql() inline
    instead. The pack loader requires frontmatter on every templates/*.tmpl (FM-TPL-001),
    so the root-schema template lives in root-templates/ (outside pack composition) and
    this second template carries the SAME query/body in the "frontmatter" schema,
    emitting to docs/CONSTITUTION-pack.md to avoid a duplicate-output collision with the
    root rule's CONSTITUTION.md. Keep the two bodies in lockstep when amending. -#}
# ggen Constitution — Admitted-Semantics Mirror

GENERATED by `ggen sync run` from `packs/ggen-constitution-pack/ontology.ttl`. Do not hand-edit.

This document mirrors `.specify/memory/constitution.md` v2.0.0 (the human-ratified prose
authority, ratified 2025-12-11, last amended 2026-07-19) as admitted graph semantics: one
`ccn:Law` individual per numbered Core Principle, each carrying its enforcement anchors and
proving tests as facts rather than prose claims. Per that document's own Governance section:
**divergence between this generated mirror and the prose constitution is itself a defect to
resolve — neither silently wins.**

| # | Principle | Mechanized | Enforced by |
|---|-----------|------------|-------------|
{% for row in laws %}| {{ row.num }} | {{ row.label }} | {{ row.mechanized }} | {{ row.enforcedBy }} |
{% endfor %}

## Full text

{% for row in laws %}### {{ row.num }}. {{ row.label }}

{{ row.statement }}

- **Mechanized:** {{ row.mechanized }}
- **Enforced by:** {{ row.enforcedBy }}
{% if row.note %}- **Note:** {{ row.note }}
{% endif %}

{% endfor %}
