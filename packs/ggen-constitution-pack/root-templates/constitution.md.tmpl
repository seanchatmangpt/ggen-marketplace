{#- CONSTITUTION.md — generated mirror of packs/ggen-constitution-pack/ontology.ttl.
    Root ggen.toml is the "declarative-rules" schema (has [[generation.rules]]), which does
    NOT read a template's own frontmatter `sparql:` block -- only the "frontmatter" schema
    (ggen_engine::config::GgenConfig) does that. This template instead calls sparql() itself,
    mirroring the working pattern in .specify/templates/docs/{tcps_status,pack_maturity_model}.md.tera. -#}
{%- set laws = sparql(query='PREFIX ccn: <http://seanchatmangpt.github.io/packs/ggen-constitution#> SELECT ?num ?label ?statement ?enforcedBy ?mechanized ?note WHERE { ?law a ccn:Law ; ccn:principleNumber ?num ; ccn:principleLabel ?label ; ccn:statement ?statement ; ccn:enforcedBy ?enforcedBy ; ccn:mechanized ?mechanized . OPTIONAL { ?law ccn:note ?note } } ORDER BY ?num') -%}
# ggen Constitution — Admitted-Semantics Mirror

GENERATED by `ggen sync run` from `packs/ggen-constitution-pack/ontology.ttl`. Do not hand-edit.

This document mirrors `.specify/memory/constitution.md` v2.0.0 (the human-ratified prose
authority, ratified 2025-12-11, last amended 2026-07-19) as admitted graph semantics: one
`ccn:Law` individual per numbered Core Principle, each carrying its enforcement anchors and
proving tests as facts rather than prose claims. Per that document's own Governance section:
**divergence between this generated mirror and the prose constitution is itself a defect to
resolve — neither silently wins.**

| # | Principle | Mechanized | Enforced by |
|---|-----------|------------|-------------|
{% for row in laws %}| {{ row.num }} | {{ row.label }} | {{ row.mechanized }} | {{ row.enforcedBy }} |
{% endfor %}

## Full text

{% for row in laws %}### {{ row.num }}. {{ row.label }}

{{ row.statement }}

- **Mechanized:** {{ row.mechanized }}
- **Enforced by:** {{ row.enforcedBy }}
{% if row.note %}- **Note:** {{ row.note }}
{% endif %}

{% endfor %}
