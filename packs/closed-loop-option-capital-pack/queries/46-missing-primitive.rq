PREFIX cloc: <https://ggen.dev/ontology/closed-loop-option-capital#>
SELECT ?edge ?primitive ?reuse WHERE {
 ?edge a cloc:CompositionEdge ; cloc:reversible true ; cloc:missingPrimitive ?primitive ; cloc:expectedReuse ?reuse .
}
ORDER BY DESC(?reuse) ?edge
