PREFIX cloc: <https://ggen.dev/ontology/closed-loop-option-capital#>
SELECT ?edge ?support ?delta WHERE {
 ?edge a cloc:CompositionEdge ; cloc:reversible true ; cloc:marketplaceSupport ?support ; cloc:expectedDelta ?delta .
}
ORDER BY DESC(?delta) ?edge
