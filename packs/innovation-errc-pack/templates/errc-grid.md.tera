# ERRC value-innovation portfolio

Authority: `SELECT`  
Actuation performed: `false`

This projection is manufactured from admitted RDF. It can select and rank marketplace opportunities; it cannot execute repository or runtime changes.

| ID | ERRC action | Innovation | Target |
|---|---|---|---|
{% for row in results %}| {{ row.id }} | {{ row.action }} | **{{ row.title }}** — {{ row.description }} | `{{ row.target }}` |
{% endfor %}

## Composition law

Feed admitted `Create`, `Raise`, and retained `Reduce` opportunities into marketplace pack composition and the multicell consumer factory. `Eliminate` is only executable after equivalence, replacement standing, and replay evidence are independently verified. Unselected options remain preserved until an authorized downstream DO path acts with a receipt.
