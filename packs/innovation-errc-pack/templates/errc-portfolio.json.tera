{
  "schema": "ggen.innovation.errc/1",
  "authority": "SELECT",
  "actuation_performed": false,
  "framework": "ERRC",
  "proposals": [
  {% for row in results %}{"id":"{{ row.id }}","action":"{{ row.action }}","title":"{{ row.title }}","description":"{{ row.description }}","target":"{{ row.target }}"}{% if not loop.last %},{% endif %}{% endfor %}
  ]
}
