---
to: docs/anti_llm_cheat_reference.md
freeze_policy: checksum
freeze_slots_dir: .ggen/freeze/anti-llm-cheat-lsp-pack
sparql:
  layers: |
    PREFIX alc: <http://seanchatmangpt.github.io/packs/anti-llm-cheat-lsp#>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?order ?label ?doc WHERE {
      ?l a alc:DetectorLayer ; alc:order ?order ; rdfs:label ?label ; rdfs:comment ?doc .
    } ORDER BY ?order
  rules: |
    PREFIX alc: <http://seanchatmangpt.github.io/packs/anti-llm-cheat-lsp#>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?rule_id ?label ?doc WHERE {
      ?r a alc:Rule ; alc:ruleId ?rule_id ; rdfs:label ?label ; rdfs:comment ?doc .
    } ORDER BY ?rule_id
---
# anti-llm-cheat-lsp reference

GENERATED by `ggen sync` from `packs/anti-llm-cheat-lsp-pack/ontology.ttl`.

## Detector stack

| # | Layer | Doc |
|---|---|---|
{% for row in layers %}| {{ row.order }} | {{ row.label }} | {{ row.doc }} |
{% endfor %}
## Rules (tabulated subset; the full rule set is larger per its README)

| Rule ID | Label | Doc |
|---|---|---|
{% for row in rules %}| `{{ row.rule_id }}` | {{ row.label }} | {{ row.doc }} |
{% endfor %}
