PREFIX gla: <https://chatmangpt.com/ggen-legacy#>
SELECT ?program WHERE {
  ?program a gla:AssuranceProgram .
  FILTER NOT EXISTS { ?program gla:signatureRequired true ; gla:transparencyLogRequired true . }
}
