PREFIX gla: <https://chatmangpt.com/ggen-legacy#>
SELECT ?required WHERE {
  VALUES ?required { "customer-vpc" "independent-lab" "recovery-site" }
  FILTER NOT EXISTS { ?environment a gla:ReplayEnvironment ; gla:environmentId ?required . }
}
