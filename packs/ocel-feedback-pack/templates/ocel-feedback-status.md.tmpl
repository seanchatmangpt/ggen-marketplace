---
to: docs/OCEL_FEEDBACK_STATUS.md
force: true
sparql:
  signals: |
    PREFIX cnv-ocel: <https://clap-noun-verb.dev/ontology/ocel#>
    SELECT ?command_id ?invocation_count ?success_rate ?recommendation WHERE {
      ?signal a cnv-ocel:Signal ;
              cnv-ocel:commandId ?command_id ;
              cnv-ocel:invocationCount ?invocation_count ;
              cnv-ocel:successRate ?success_rate ;
              cnv-ocel:recommendation ?recommendation .
    }
    ORDER BY ?recommendation ?command_id
---
# OCEL Feedback Status

Generated from the real fleet-wide OCEL corpus signals composed into this
project's graph (see `~/ggen-marketplace/packs/ocel-feedback-pack`). Any
command listed with recommendation `prune` is refused by
`gates/005_ocel_pruning_advisory.rq` -- remove it from the ontology or
supply fresh corpus evidence before it can be regenerated.

| Command | Invocations | Success Rate | Recommendation |
|---|---|---|---|
{%- for s in signals %}
| `{{ s.command_id }}` | {{ s.invocation_count }} | {{ s.success_rate }} | **{{ s.recommendation }}** |
{%- endfor %}
