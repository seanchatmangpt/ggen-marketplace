---
to: docs/how-to/deploy-test-and-use.md
sparql:
  deploy: |
    PREFIX rdx: <https://seanchatmangpt.github.io/ggen-marketplace/readme-diataxis#>
    SELECT ?position ?label ?command WHERE {
      ?s a rdx:RunStep ; rdx:position ?position ; rdx:kind "deploy" ; rdx:label ?label ; rdx:command ?command .
    } ORDER BY ?position
  test: |
    PREFIX rdx: <https://seanchatmangpt.github.io/ggen-marketplace/readme-diataxis#>
    SELECT ?position ?label ?command WHERE {
      ?s a rdx:RunStep ; rdx:position ?position ; rdx:kind "test" ; rdx:label ?label ; rdx:command ?command .
    } ORDER BY ?position
  examples: |
    PREFIX rdx: <https://seanchatmangpt.github.io/ggen-marketplace/readme-diataxis#>
    PREFIX dcterms: <http://purl.org/dc/terms/>
    SELECT ?position ?language ?code ?description WHERE {
      ?s a rdx:UsageExample ; rdx:position ?position ; rdx:language ?language ; rdx:code ?code ;
         dcterms:description ?description .
    } ORDER BY ?position
---
# Deploy, Test, and Use

<!-- GENERATED by ggen from readme-diataxis-pack. -->
<!-- Edit admitted RDF/template source, not this projection. -->

## Deploying

{% for row in deploy %}
{{ loop.index }}. {{ row.label }}:
   ```
   {{ row.command }}
   ```
{% endfor %}

## Running Tests

{% for row in test %}
{{ loop.index }}. {{ row.label }}:
   ```
   {{ row.command }}
   ```
{% endfor %}

## Usage Examples

{% for row in examples %}
{{ row.description }}

```{{ row.language }}
{{ row.code }}
```
{% endfor %}
