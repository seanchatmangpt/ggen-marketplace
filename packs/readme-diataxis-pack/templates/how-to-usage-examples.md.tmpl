---
to: docs/how-to/usage-examples.md
sparql:
  examples: |
    PREFIX rdx: <https://seanchatmangpt.github.io/ggen-marketplace/readme-diataxis#>
    PREFIX dcterms: <http://purl.org/dc/terms/>
    SELECT ?position ?language ?code ?description WHERE {
      ?u a rdx:UsageExample ; rdx:position ?position ; rdx:language ?language ; rdx:code ?code .
      OPTIONAL { ?u dcterms:description ?description_raw . }
      BIND(COALESCE(?description_raw, "") AS ?description)
    } ORDER BY ?position
---
<!-- GENERATED by ggen from readme-diataxis-pack. -->
<!-- Edit admitted RDF/template source, not this projection. -->

# Usage examples

{% for row in examples -%}
{% if row.description %}{{ row.description }}
{% endif %}
```{{ row.language }}
{{ row.code }}
```

{% endfor -%}
