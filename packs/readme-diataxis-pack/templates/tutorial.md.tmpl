---
to: docs/tutorials/getting-started.md
sparql:
  steps: |
    PREFIX rdx: <https://seanchatmangpt.github.io/ggen-marketplace/readme-diataxis#>
    SELECT ?position ?kind ?label ?command WHERE {
      ?s a rdx:RunStep ; rdx:position ?position ; rdx:kind ?kind ; rdx:label ?label ; rdx:command ?command .
      FILTER(?kind IN ("clone", "install", "start"))
    } ORDER BY ?position
---
<!-- GENERATED by ggen from readme-diataxis-pack. -->
<!-- Edit admitted RDF/template source, not this projection. -->

# Getting started

Run this project locally, in order.

{% for row in steps -%}
## {{ loop.index }}. {{ row.label }}

```bash
{{ row.command }}
```

{% endfor -%}
