---
to: docs/tutorials/getting-started.md
sparql:
  steps: |
    PREFIX rdx: <https://seanchatmangpt.github.io/ggen-marketplace/readme-diataxis#>
    SELECT ?position ?label ?command WHERE {
      ?step a rdx:RunStep ;
            rdx:position ?position ;
            rdx:kind ?kind ;
            rdx:label ?label ;
            rdx:command ?command .
      FILTER(?kind IN ("clone", "install", "start"))
    } ORDER BY ?position
---
# Getting Started

<!-- GENERATED by ggen from readme-diataxis-pack. -->
<!-- Edit admitted RDF/template source, not this projection. -->

Follow these steps in order to run the project locally.

{% for row in steps %}
## Step {{ loop.index }}: {{ row.label }}

Run:

```
{{ row.command }}
```

{% endfor %}
