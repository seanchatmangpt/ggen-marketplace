# GENERATED merge of per-chapter fragments (mechanically assembled;
# source of truth for the level-five book transcription pack).
# 366 fragments: one book:Chapter + one book:Listing each.

@prefix book: <http://seanchatmangpt.github.io/packs/level-five-book#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix pack: <https://example.org/ggen/pack#> .
@prefix prov: <http://www.w3.org/ns/prov#> .
@prefix xsd: <http://www.w3.org/2001/XMLSchema#> .

# ── admission-081.ttl ───────────────────────────────────────────
book:chapter-081 a book:Chapter ;
    book:number 81 ;
    book:title "Syntax Is Not Admission" ;
    book:slug "syntax-not-admission" ;
    book:part "admission" ;
    book:sourcePath "admission/081-syntax-not-admission.md" ;
    book:sourceText """# 81. Syntax Is Not Admission

> **Pattern 81 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **81. Syntax Is Not Admission**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Syntax Is Not Admission**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Syntax Is Not Admission** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/081-81-syntax-is-not-admission.ttl`](../listings/081-81-syntax-is-not-admission.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章81
    a pack:生成単位 ;
    dcterms:title "81. Syntax Is Not Admission"@en ;
    pack:出力経路 "src/generated/081_81_syntax_is_not_admission.rs" ;
    pack:順序 81 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **81. Syntax Is Not Admission**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-081` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-081 .

book:listing-081 a book:Listing ;
    book:sourcePath "listings/081-81-syntax-is-not-admission.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章81
    a pack:生成単位 ;
    dcterms:title \"81. Syntax Is Not Admission\"@en ;
    pack:出力経路 \"src/generated/081_81_syntax_is_not_admission.rs\" ;
    pack:順序 81 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-082.ttl ───────────────────────────────────────────
book:chapter-082 a book:Chapter ;
    book:number 82 ;
    book:title "Pack Gates as Production Law" ;
    book:slug "pack-shapes-production-law" ;
    book:part "admission" ;
    book:sourcePath "admission/082-pack-shapes-production-law.md" ;
    book:sourceText """# 82. Pack Gates as Production Law

> **Pattern 82 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **82. Pack Gates as Production Law**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Gates as Production Law**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Gates as Production Law** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/082-82-pack-shapes-as-production-law.ttl`](../listings/082-82-pack-shapes-as-production-law.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary. The listing keeps its historical `.ttl` filename in this book's listing tree; in a shipped pack this file lives at `gates/010_admission.rq`.

```sparql
# MESSAGE: 82. Pack Gates as Production Law: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
```

### How the current admission mechanism works

Since the engine's SPARQL-gate migration, a pack no longer ships SHACL. Admission law is a `gates/` directory of `.rq` files next to `ontology.ttl`, evaluated in sorted filename order (hence the `010_`, `020_`, `030_` numbering convention) against the post-materialization union graph. Each gate file is an optional block of leading `# MESSAGE:` comment lines (the operator-facing violation text) followed by exactly one ASK or SELECT query: ASK returning `true` is a violation, and any SELECT row is a violation; a CONSTRUCT or DESCRIBE query is not a gate and refuses with `[FM-PACK-012]`. A missing `gates/` directory simply means no gates, but an unreadable directory or file is a typed `[FM-PACK-012]` refusal (fail closed), a gate violation refuses the sync with `[FM-PACK-013]`, and a legacy `shapes.ttl` still present in a pack is itself a loud `[FM-PACK-012]` refusal — never silently ignored. Project-level `[law].gates` mirrors this with `[FM-LAW-012]`/`[FM-LAW-013]`, and declaring the retired `[law].shapes` key refuses with a migration message. Every gate file is hashed into the receipt closure. Because gates are plain SPARQL evaluated through `GraphEngine::query`, the identical gate refuses the identical facts under both the GraphLaw and the Oxigraph engine — proven by `crates/ggen-engine/tests/reasoner_independence_e2e.rs`, which syncs the same pack byte-identically under both backends and receives the same `[FM-PACK-013]` on the same violating fixture. SHACL could not make that promise: `validate_shacl` on the Oxigraph backend is a typed `[FM-LAW-002]` refusal.

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **82. Pack Gates as Production Law**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-082` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-082 .

book:listing-082 a book:Listing ;
    book:sourcePath "listings/082-82-pack-shapes-as-production-law.ttl" ;
    book:sourceText """# MESSAGE: 82. Pack Gates as Production Law: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
""" .

# ── admission-083.ttl ───────────────────────────────────────────
book:chapter-083 a book:Chapter ;
    book:number 83 ;
    book:title "Required Properties" ;
    book:slug "required-properties" ;
    book:part "admission" ;
    book:sourcePath "admission/083-required-properties.md" ;
    book:sourceText """# 83. Required Properties

> **Pattern 83 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **83. Required Properties**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Required Properties**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Required Properties** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/083-83-required-properties.ttl`](../listings/083-83-required-properties.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章83
    a pack:生成単位 ;
    dcterms:title "83. Required Properties"@en ;
    pack:出力経路 "src/generated/083_83_required_properties.rs" ;
    pack:順序 83 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **83. Required Properties**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-083` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-083 .

book:listing-083 a book:Listing ;
    book:sourcePath "listings/083-83-required-properties.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章83
    a pack:生成単位 ;
    dcterms:title \"83. Required Properties\"@en ;
    pack:出力経路 \"src/generated/083_83_required_properties.rs\" ;
    pack:順序 83 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-084.ttl ───────────────────────────────────────────
book:chapter-084 a book:Chapter ;
    book:number 84 ;
    book:title "Cardinality Boundaries" ;
    book:slug "cardinality-boundaries" ;
    book:part "admission" ;
    book:sourcePath "admission/084-cardinality-boundaries.md" ;
    book:sourceText """# 84. Cardinality Boundaries

> **Pattern 84 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **84. Cardinality Boundaries**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Cardinality Boundaries**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Cardinality Boundaries** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/084-84-cardinality-boundaries.ttl`](../listings/084-84-cardinality-boundaries.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章84
    a pack:生成単位 ;
    dcterms:title "84. Cardinality Boundaries"@en ;
    pack:出力経路 "src/generated/084_84_cardinality_boundaries.rs" ;
    pack:順序 84 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **84. Cardinality Boundaries**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-084` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-084 .

book:listing-084 a book:Listing ;
    book:sourcePath "listings/084-84-cardinality-boundaries.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章84
    a pack:生成単位 ;
    dcterms:title \"84. Cardinality Boundaries\"@en ;
    pack:出力経路 \"src/generated/084_84_cardinality_boundaries.rs\" ;
    pack:順序 84 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-085.ttl ───────────────────────────────────────────
book:chapter-085 a book:Chapter ;
    book:number 85 ;
    book:title "Datatype Boundaries" ;
    book:slug "datatype-boundaries" ;
    book:part "admission" ;
    book:sourcePath "admission/085-datatype-boundaries.md" ;
    book:sourceText """# 85. Datatype Boundaries

> **Pattern 85 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **85. Datatype Boundaries**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Datatype Boundaries**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Datatype Boundaries** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/085-85-datatype-boundaries.ttl`](../listings/085-85-datatype-boundaries.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章85
    a pack:生成単位 ;
    dcterms:title "85. Datatype Boundaries"@en ;
    pack:出力経路 "src/generated/085_85_datatype_boundaries.rs" ;
    pack:順序 85 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **85. Datatype Boundaries**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-085` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-085 .

book:listing-085 a book:Listing ;
    book:sourcePath "listings/085-85-datatype-boundaries.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章85
    a pack:生成単位 ;
    dcterms:title \"85. Datatype Boundaries\"@en ;
    pack:出力経路 \"src/generated/085_85_datatype_boundaries.rs\" ;
    pack:順序 85 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-086.ttl ───────────────────────────────────────────
book:chapter-086 a book:Chapter ;
    book:number 86 ;
    book:title "IRI Boundaries" ;
    book:slug "iri-boundaries" ;
    book:part "admission" ;
    book:sourcePath "admission/086-iri-boundaries.md" ;
    book:sourceText """# 86. IRI Boundaries

> **Pattern 86 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **86. IRI Boundaries**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **IRI Boundaries**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **IRI Boundaries** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/086-86-iri-boundaries.ttl`](../listings/086-86-iri-boundaries.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章86
    a pack:生成単位 ;
    dcterms:title "86. IRI Boundaries"@en ;
    pack:出力経路 "src/generated/086_86_iri_boundaries.rs" ;
    pack:順序 86 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **86. IRI Boundaries**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-086` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-086 .

book:listing-086 a book:Listing ;
    book:sourcePath "listings/086-86-iri-boundaries.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章86
    a pack:生成単位 ;
    dcterms:title \"86. IRI Boundaries\"@en ;
    pack:出力経路 \"src/generated/086_86_iri_boundaries.rs\" ;
    pack:順序 86 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-087.ttl ───────────────────────────────────────────
book:chapter-087 a book:Chapter ;
    book:number 87 ;
    book:title "Output-Path Boundaries" ;
    book:slug "output-path-boundaries" ;
    book:part "admission" ;
    book:sourcePath "admission/087-output-path-boundaries.md" ;
    book:sourceText """# 87. Output-Path Boundaries

> **Pattern 87 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **87. Output-Path Boundaries**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Output-Path Boundaries**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Output-Path Boundaries** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/087-87-output-path-boundaries.ttl`](../listings/087-87-output-path-boundaries.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章87
    a pack:生成単位 ;
    dcterms:title "87. Output-Path Boundaries"@en ;
    pack:出力経路 "src/generated/087_87_output_path_boundaries.rs" ;
    pack:順序 87 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **87. Output-Path Boundaries**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-087` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-087 .

book:listing-087 a book:Listing ;
    book:sourcePath "listings/087-87-output-path-boundaries.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章87
    a pack:生成単位 ;
    dcterms:title \"87. Output-Path Boundaries\"@en ;
    pack:出力経路 \"src/generated/087_87_output_path_boundaries.rs\" ;
    pack:順序 87 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-088.ttl ───────────────────────────────────────────
book:chapter-088 a book:Chapter ;
    book:number 88 ;
    book:title "Template Ownership Constraints" ;
    book:slug "template-ownership-constraints" ;
    book:part "admission" ;
    book:sourcePath "admission/088-template-ownership-constraints.md" ;
    book:sourceText """# 88. Template Ownership Constraints

> **Pattern 88 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **88. Template Ownership Constraints**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Template Ownership Constraints**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Template Ownership Constraints** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/088-88-template-ownership-constraints.tmpl`](../listings/088-88-template-ownership-constraints.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/088_88_template_ownership_constraints.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章88 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 88: 88. Template Ownership Constraints
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **88. Template Ownership Constraints**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-088` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-088 .

book:listing-088 a book:Listing ;
    book:sourcePath "listings/088-88-template-ownership-constraints.tmpl" ;
    book:sourceText """---
to: src/generated/088_88_template_ownership_constraints.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章88 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 88: 88. Template Ownership Constraints
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── admission-089.ttl ───────────────────────────────────────────
book:chapter-089 a book:Chapter ;
    book:number 89 ;
    book:title "Namespace Isolation Constraints" ;
    book:slug "namespace-isolation" ;
    book:part "admission" ;
    book:sourcePath "admission/089-namespace-isolation.md" ;
    book:sourceText """# 89. Namespace Isolation Constraints

> **Pattern 89 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **89. Namespace Isolation Constraints**. Shared classes are safe only when local individuals remain scoped; union graphs make implicit assumptions visible.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Namespace Isolation Constraints**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Namespace Isolation Constraints** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/089-89-namespace-isolation-constraints.ttl`](../listings/089-89-namespace-isolation-constraints.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章89
    a pack:生成単位 ;
    dcterms:title "89. Namespace Isolation Constraints"@en ;
    pack:出力経路 "src/generated/089_89_namespace_isolation_constraints.rs" ;
    pack:順序 89 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **89. Namespace Isolation Constraints**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-089` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-089 .

book:listing-089 a book:Listing ;
    book:sourcePath "listings/089-89-namespace-isolation-constraints.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章89
    a pack:生成単位 ;
    dcterms:title \"89. Namespace Isolation Constraints\"@en ;
    pack:出力経路 \"src/generated/089_89_namespace_isolation_constraints.rs\" ;
    pack:順序 89 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-090.ttl ───────────────────────────────────────────
book:chapter-090 a book:Chapter ;
    book:number 90 ;
    book:title "Cross-Resource Constraints" ;
    book:slug "cross-resource-constraints" ;
    book:part "admission" ;
    book:sourcePath "admission/090-cross-resource-constraints.md" ;
    book:sourceText """# 90. Cross-Resource Constraints

> **Pattern 90 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **90. Cross-Resource Constraints**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Cross-Resource Constraints**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Cross-Resource Constraints** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/090-90-cross-resource-constraints.ttl`](../listings/090-90-cross-resource-constraints.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章90
    a pack:生成単位 ;
    dcterms:title "90. Cross-Resource Constraints"@en ;
    pack:出力経路 "src/generated/090_90_cross_resource_constraints.rs" ;
    pack:順序 90 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **90. Cross-Resource Constraints**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-090` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-090 .

book:listing-090 a book:Listing ;
    book:sourcePath "listings/090-90-cross-resource-constraints.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章90
    a pack:生成単位 ;
    dcterms:title \"90. Cross-Resource Constraints\"@en ;
    pack:出力経路 \"src/generated/090_90_cross_resource_constraints.rs\" ;
    pack:順序 90 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-091.ttl ───────────────────────────────────────────
book:chapter-091 a book:Chapter ;
    book:number 91 ;
    book:title "Positive Fixtures" ;
    book:slug "positive-fixtures" ;
    book:part "admission" ;
    book:sourcePath "admission/091-positive-fixtures.md" ;
    book:sourceText """# 91. Positive Fixtures

> **Pattern 91 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **91. Positive Fixtures**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Positive Fixtures**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Positive Fixtures** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/091-91-positive-fixtures.ttl`](../listings/091-91-positive-fixtures.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章91
    a pack:生成単位 ;
    dcterms:title "91. Positive Fixtures"@en ;
    pack:出力経路 "src/generated/091_91_positive_fixtures.rs" ;
    pack:順序 91 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **91. Positive Fixtures**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-091` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-091 .

book:listing-091 a book:Listing ;
    book:sourcePath "listings/091-91-positive-fixtures.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章91
    a pack:生成単位 ;
    dcterms:title \"91. Positive Fixtures\"@en ;
    pack:出力経路 \"src/generated/091_91_positive_fixtures.rs\" ;
    pack:順序 91 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-092.ttl ───────────────────────────────────────────
book:chapter-092 a book:Chapter ;
    book:number 92 ;
    book:title "Negative Fixtures" ;
    book:slug "negative-fixtures" ;
    book:part "admission" ;
    book:sourcePath "admission/092-negative-fixtures.md" ;
    book:sourceText """# 92. Negative Fixtures

> **Pattern 92 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **92. Negative Fixtures**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Negative Fixtures**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Negative Fixtures** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/092-92-negative-fixtures.ttl`](../listings/092-92-negative-fixtures.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章92
    a pack:生成単位 ;
    dcterms:title "92. Negative Fixtures"@en ;
    pack:出力経路 "src/generated/092_92_negative_fixtures.rs" ;
    pack:順序 92 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **92. Negative Fixtures**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-092` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-092 .

book:listing-092 a book:Listing ;
    book:sourcePath "listings/092-92-negative-fixtures.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章92
    a pack:生成単位 ;
    dcterms:title \"92. Negative Fixtures\"@en ;
    pack:出力経路 \"src/generated/092_92_negative_fixtures.rs\" ;
    pack:順序 92 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-093.ttl ───────────────────────────────────────────
book:chapter-093 a book:Chapter ;
    book:number 93 ;
    book:title "Adversarial Fixtures" ;
    book:slug "adversarial-fixtures" ;
    book:part "admission" ;
    book:sourcePath "admission/093-adversarial-fixtures.md" ;
    book:sourceText """# 93. Adversarial Fixtures

> **Pattern 93 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **93. Adversarial Fixtures**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Adversarial Fixtures**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Adversarial Fixtures** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/093-93-adversarial-fixtures.ttl`](../listings/093-93-adversarial-fixtures.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章93
    a pack:生成単位 ;
    dcterms:title "93. Adversarial Fixtures"@en ;
    pack:出力経路 "src/generated/093_93_adversarial_fixtures.rs" ;
    pack:順序 93 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **93. Adversarial Fixtures**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-093` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-093 .

book:listing-093 a book:Listing ;
    book:sourcePath "listings/093-93-adversarial-fixtures.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章93
    a pack:生成単位 ;
    dcterms:title \"93. Adversarial Fixtures\"@en ;
    pack:出力経路 \"src/generated/093_93_adversarial_fixtures.rs\" ;
    pack:順序 93 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── admission-094.ttl ───────────────────────────────────────────
book:chapter-094 a book:Chapter ;
    book:number 94 ;
    book:title "Fail-Closed Pack Loading" ;
    book:slug "fail-closed-loading" ;
    book:part "admission" ;
    book:sourcePath "admission/094-fail-closed-loading.md" ;
    book:sourceText """# 94. Fail-Closed Pack Loading

> **Pattern 94 · Admission Gates**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A syntactically valid graph is not necessarily an admissible production input. The narrow problem in this chapter is **94. Fail-Closed Pack Loading**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Fail-Closed Pack Loading**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Fail-Closed Pack Loading** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/094-94-fail-closed-pack-loading.ttl`](../listings/094-94-fail-closed-pack-loading.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章94
    a pack:生成単位 ;
    dcterms:title "94. Fail-Closed Pack Loading"@en ;
    pack:出力経路 "src/generated/094_94_fail_closed_pack_loading.rs" ;
    pack:順序 94 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Malformed, ambiguous, contaminating, or unsafe graph states are refused before rendering or writes begin.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A negative or adversarial fixture reaches template rendering or filesystem actuation without a typed refusal.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS gates refuse missing source text, duplicate output ownership, invalid paths, and incomplete release records before generation.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **94. Fail-Closed Pack Loading**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-094` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Admission Gates** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack SPARQL gates`
- `typed pre-write refusal`
- `reasoner-independent admission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`
- `packs/ggen-verify-pack/gates`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/lsp-max-pack`
- `packs/star-toml-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A chapter's specific constraint is alive only when a negative fixture reaches the expected typed refusal.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-094 .

book:listing-094 a book:Listing ;
    book:sourcePath "listings/094-94-fail-closed-pack-loading.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章94
    a pack:生成単位 ;
    dcterms:title \"94. Fail-Closed Pack Loading\"@en ;
    pack:出力経路 \"src/generated/094_94_fail_closed_pack_loading.rs\" ;
    pack:順序 94 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── appendices-a-pack-layout.ttl ────────────────────────────────
book:chapter-appendices-a-pack-layout a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix A \u2014 Canonical Pack Directory Layout" ;
    book:slug "a-pack-layout" ;
    book:part "appendices" ;
    book:sourcePath "appendices/a-pack-layout.md" ;
    book:listing book:chapter-appendices-a-pack-layout-listing ;
    book:sourceText """# Appendix A — Canonical Pack Directory Layout

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix A — Canonical Pack Directory Layout**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix A — Canonical Pack Directory Layout**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix A — Canonical Pack Directory Layout** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-A-appendix-a-canonical-pack-directory-layout.rs`](../listings/appendix-A-appendix-a-canonical-pack-directory-layout.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix A — Canonical Pack Directory Layout",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix A — Canonical Pack Directory Layout**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-A` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-a-pack-layout-listing a book:Listing ;
    book:sourcePath "listings/appendix-A-appendix-a-canonical-pack-directory-layout.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix A — Canonical Pack Directory Layout\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── appendices-b-pack-toml.ttl ──────────────────────────────────
book:chapter-appendices-b-pack-toml a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix B \u2014 Canonical `pack.toml`" ;
    book:slug "b-pack-toml" ;
    book:part "appendices" ;
    book:sourcePath "appendices/b-pack-toml.md" ;
    book:listing book:chapter-appendices-b-pack-toml-listing ;
    book:sourceText """# Appendix B — Canonical `pack.toml`

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix B — Canonical `pack.toml`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix B — Canonical `pack.toml`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix B — Canonical `pack.toml`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-B-appendix-b-canonical-pack-toml.toml`](../listings/appendix-B-appendix-b-canonical-pack-toml.toml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```toml
[pack]
name = "chapter-000-appendix-b-canonical-pack-toml"
version = "26.7.19"
description = "Appendix B — Canonical `pack.toml`"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix B — Canonical `pack.toml`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-B` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-b-pack-toml-listing a book:Listing ;
    book:sourcePath "listings/appendix-B-appendix-b-canonical-pack-toml.toml" ;
    book:sourceText """[pack]
name = \"chapter-000-appendix-b-canonical-pack-toml\"
version = \"26.7.19\"
description = \"Appendix B — Canonical `pack.toml`\"

[law]
ontology = \"ontology.ttl\"
gates = [\"gates/010_admission.rq\"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = \"reference/\"
sync_twice = true
refuse_unexplained_drift = true
""" .

# ── appendices-c-ggen-toml.ttl ──────────────────────────────────
book:chapter-appendices-c-ggen-toml a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix C \u2014 Canonical `ggen.toml`" ;
    book:slug "c-ggen-toml" ;
    book:part "appendices" ;
    book:sourcePath "appendices/c-ggen-toml.md" ;
    book:listing book:chapter-appendices-c-ggen-toml-listing ;
    book:sourceText """# Appendix C — Canonical `ggen.toml`

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix C — Canonical `ggen.toml`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix C — Canonical `ggen.toml`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix C — Canonical `ggen.toml`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-C-appendix-c-canonical-ggen-toml.toml`](../listings/appendix-C-appendix-c-canonical-ggen-toml.toml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```toml
[pack]
name = "chapter-000-appendix-c-canonical-ggen-toml"
version = "26.7.19"
description = "Appendix C — Canonical `ggen.toml`"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix C — Canonical `ggen.toml`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-C` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-c-ggen-toml-listing a book:Listing ;
    book:sourcePath "listings/appendix-C-appendix-c-canonical-ggen-toml.toml" ;
    book:sourceText """[pack]
name = \"chapter-000-appendix-c-canonical-ggen-toml\"
version = \"26.7.19\"
description = \"Appendix C — Canonical `ggen.toml`\"

[law]
ontology = \"ontology.ttl\"
gates = [\"gates/010_admission.rq\"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = \"reference/\"
sync_twice = true
refuse_unexplained_drift = true
""" .

# ── appendices-d-ontology-vocabulary.ttl ────────────────────────
book:chapter-appendices-d-ontology-vocabulary a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix D \u2014 Canonical Ontology Vocabulary" ;
    book:slug "d-ontology-vocabulary" ;
    book:part "appendices" ;
    book:sourcePath "appendices/d-ontology-vocabulary.md" ;
    book:listing book:chapter-appendices-d-ontology-vocabulary-listing ;
    book:sourceText """# Appendix D — Canonical Ontology Vocabulary

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix D — Canonical Ontology Vocabulary**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix D — Canonical Ontology Vocabulary**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix D — Canonical Ontology Vocabulary** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-D-appendix-d-canonical-ontology-vocabulary.ttl`](../listings/appendix-D-appendix-d-canonical-ontology-vocabulary.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章0
    a pack:生成単位 ;
    dcterms:title "Appendix D — Canonical Ontology Vocabulary"@en ;
    pack:出力経路 "src/generated/000_appendix_d_canonical_ontology_vocabulary.rs" ;
    pack:順序 0 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix D — Canonical Ontology Vocabulary**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-D` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-d-ontology-vocabulary-listing a book:Listing ;
    book:sourcePath "listings/appendix-D-appendix-d-canonical-ontology-vocabulary.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章0
    a pack:生成単位 ;
    dcterms:title \"Appendix D — Canonical Ontology Vocabulary\"@en ;
    pack:出力経路 \"src/generated/000_appendix_d_canonical_ontology_vocabulary.rs\" ;
    pack:順序 0 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── appendices-e-shacl-shapes.ttl ───────────────────────────────
book:chapter-appendices-e-shacl-shapes a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix E \u2014 Canonical Admission Gates" ;
    book:slug "e-shacl-shapes" ;
    book:part "appendices" ;
    book:sourcePath "appendices/e-shacl-shapes.md" ;
    book:listing book:chapter-appendices-e-shacl-shapes-listing ;
    book:sourceText """# Appendix E — Canonical Admission Gates

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix E — Canonical Admission Gates**. The gate must reject malformed or incomplete pack data before a template can convert it into apparently valid source code.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix E — Canonical Admission Gates**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix E — Canonical Admission Gates** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-E-appendix-e-canonical-shacl-shapes.ttl`](../listings/appendix-E-appendix-e-canonical-shacl-shapes.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary. The listing keeps its historical `.ttl` filename in this book's listing tree; in a shipped pack this file lives at `gates/010_admission.rq`.

```sparql
# MESSAGE: Appendix E — Canonical Admission Gates: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
```

### How the current admission mechanism works

Since the engine's SPARQL-gate migration, a pack no longer ships SHACL. Admission law is a `gates/` directory of `.rq` files next to `ontology.ttl`, evaluated in sorted filename order (hence the `010_`, `020_`, `030_` numbering convention) against the post-materialization union graph. Each gate file is an optional block of leading `# MESSAGE:` comment lines (the operator-facing violation text) followed by exactly one ASK or SELECT query: ASK returning `true` is a violation, and any SELECT row is a violation; a CONSTRUCT or DESCRIBE query is not a gate and refuses with `[FM-PACK-012]`. A missing `gates/` directory simply means no gates, but an unreadable directory or file is a typed `[FM-PACK-012]` refusal (fail closed), a gate violation refuses the sync with `[FM-PACK-013]`, and a legacy `shapes.ttl` still present in a pack is itself a loud `[FM-PACK-012]` refusal — never silently ignored. Project-level `[law].gates` mirrors this with `[FM-LAW-012]`/`[FM-LAW-013]`, and declaring the retired `[law].shapes` key refuses with a migration message. Every gate file is hashed into the receipt closure. Because gates are plain SPARQL evaluated through `GraphEngine::query`, the identical gate refuses the identical facts under both the GraphLaw and the Oxigraph engine — proven by `crates/ggen-engine/tests/reasoner_independence_e2e.rs`, which syncs the same pack byte-identically under both backends and receives the same `[FM-PACK-013]` on the same violating fixture. SHACL could not make that promise: `validate_shacl` on the Oxigraph backend is a typed `[FM-LAW-002]` refusal.

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix E — Canonical Admission Gates**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-E` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-e-shacl-shapes-listing a book:Listing ;
    book:sourcePath "listings/appendix-E-appendix-e-canonical-shacl-shapes.ttl" ;
    book:sourceText """# MESSAGE: Appendix E — Canonical Admission Gates: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
""" .

# ── appendices-f-sparql-queries.ttl ─────────────────────────────
book:chapter-appendices-f-sparql-queries a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix F \u2014 Canonical SPARQL Queries" ;
    book:slug "f-sparql-queries" ;
    book:part "appendices" ;
    book:sourcePath "appendices/f-sparql-queries.md" ;
    book:listing book:chapter-appendices-f-sparql-queries-listing ;
    book:sourceText """# Appendix F — Canonical SPARQL Queries

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix F — Canonical SPARQL Queries**. A query is a semantic interface. Its graph scope, ordering, optional values, and namespace filters are part of the public contract.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix F — Canonical SPARQL Queries**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix F — Canonical SPARQL Queries** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-F-appendix-f-canonical-sparql-queries.rq`](../listings/appendix-F-appendix-f-canonical-sparql-queries.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix F — Canonical SPARQL Queries**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-F` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-f-sparql-queries-listing a book:Listing ;
    book:sourcePath "listings/appendix-F-appendix-f-canonical-sparql-queries.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── appendices-g-tera-front-matter.ttl ──────────────────────────
book:chapter-appendices-g-tera-front-matter a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix G \u2014 Canonical Tera Front Matter" ;
    book:slug "g-tera-front-matter" ;
    book:part "appendices" ;
    book:sourcePath "appendices/g-tera-front-matter.md" ;
    book:listing book:chapter-appendices-g-tera-front-matter-listing ;
    book:sourceText """# Appendix G — Canonical Tera Front Matter

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix G — Canonical Tera Front Matter**. Template rendering is not clerical formatting. It is the final semantic projection before source text reaches the compiler.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix G — Canonical Tera Front Matter**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix G — Canonical Tera Front Matter** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-G-appendix-g-canonical-tera-front-matter.tmpl`](../listings/appendix-G-appendix-g-canonical-tera-front-matter.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/000_appendix_g_canonical_tera_front_matter.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章0 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 0: Appendix G — Canonical Tera Front Matter
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix G — Canonical Tera Front Matter**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-G` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-g-tera-front-matter-listing a book:Listing ;
    book:sourcePath "listings/appendix-G-appendix-g-canonical-tera-front-matter.tmpl" ;
    book:sourceText """---
to: src/generated/000_appendix_g_canonical_tera_front_matter.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章0 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 0: Appendix G — Canonical Tera Front Matter
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── appendices-h-generated-proof.ttl ────────────────────────────
book:chapter-appendices-h-generated-proof a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix H \u2014 Canonical Generated-Proof Template" ;
    book:slug "h-generated-proof" ;
    book:part "appendices" ;
    book:sourcePath "appendices/h-generated-proof.md" ;
    book:listing book:chapter-appendices-h-generated-proof-listing ;
    book:sourceText """# Appendix H — Canonical Generated-Proof Template

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix H — Canonical Generated-Proof Template**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix H — Canonical Generated-Proof Template**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix H — Canonical Generated-Proof Template** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-H-appendix-h-canonical-generated-proof-template.tmpl`](../listings/appendix-H-appendix-h-canonical-generated-proof-template.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/000_appendix_h_canonical_generated_proof_template.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章0 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 0: Appendix H — Canonical Generated-Proof Template
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix H — Canonical Generated-Proof Template**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-H` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-h-generated-proof-listing a book:Listing ;
    book:sourcePath "listings/appendix-H-appendix-h-canonical-generated-proof-template.tmpl" ;
    book:sourceText """---
to: src/generated/000_appendix_h_canonical_generated_proof_template.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章0 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 0: Appendix H — Canonical Generated-Proof Template
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── appendices-i-chicago-tdd-consumer.ttl ───────────────────────
book:chapter-appendices-i-chicago-tdd-consumer a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix I \u2014 Canonical Chicago-TDD Consumer" ;
    book:slug "i-chicago-tdd-consumer" ;
    book:part "appendices" ;
    book:sourcePath "appendices/i-chicago-tdd-consumer.md" ;
    book:listing book:chapter-appendices-i-chicago-tdd-consumer-listing ;
    book:sourceText """# Appendix I — Canonical Chicago-TDD Consumer

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix I — Canonical Chicago-TDD Consumer**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix I — Canonical Chicago-TDD Consumer**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix I — Canonical Chicago-TDD Consumer** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-I-appendix-i-canonical-chicago-tdd-consumer.rs`](../listings/appendix-I-appendix-i-canonical-chicago-tdd-consumer.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix I — Canonical Chicago-TDD Consumer",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix I — Canonical Chicago-TDD Consumer**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-I` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-i-chicago-tdd-consumer-listing a book:Listing ;
    book:sourcePath "listings/appendix-I-appendix-i-canonical-chicago-tdd-consumer.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix I — Canonical Chicago-TDD Consumer\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── appendices-j-idempotency-script.ttl ─────────────────────────
book:chapter-appendices-j-idempotency-script a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix J \u2014 Canonical Idempotency Script" ;
    book:slug "j-idempotency-script" ;
    book:part "appendices" ;
    book:sourcePath "appendices/j-idempotency-script.md" ;
    book:listing book:chapter-appendices-j-idempotency-script-listing ;
    book:sourceText """# Appendix J — Canonical Idempotency Script

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix J — Canonical Idempotency Script**. A second sync must be a no-op at the byte level unless an admitted source changed.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix J — Canonical Idempotency Script**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix J — Canonical Idempotency Script** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-J-appendix-j-canonical-idempotency-script.sh`](../listings/appendix-J-appendix-j-canonical-idempotency-script.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="Appendix J \\u2014 Canonical Idempotency Script"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix J — Canonical Idempotency Script**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-J` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-j-idempotency-script-listing a book:Listing ;
    book:sourcePath "listings/appendix-J-appendix-j-canonical-idempotency-script.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"Appendix J \\u2014 Canonical Idempotency Script\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── appendices-k-drift-refusal.ttl ──────────────────────────────
book:chapter-appendices-k-drift-refusal a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix K \u2014 Canonical Drift-Refusal Script" ;
    book:slug "k-drift-refusal" ;
    book:part "appendices" ;
    book:sourcePath "appendices/k-drift-refusal.md" ;
    book:listing book:chapter-appendices-k-drift-refusal-listing ;
    book:sourceText """# Appendix K — Canonical Drift-Refusal Script

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix K — Canonical Drift-Refusal Script**. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix K — Canonical Drift-Refusal Script**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix K — Canonical Drift-Refusal Script** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-K-appendix-k-canonical-drift-refusal-script.sh`](../listings/appendix-K-appendix-k-canonical-drift-refusal-script.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="Appendix K \\u2014 Canonical Drift-Refusal Script"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix K — Canonical Drift-Refusal Script**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-K` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-k-drift-refusal-listing a book:Listing ;
    book:sourcePath "listings/appendix-K-appendix-k-canonical-drift-refusal-script.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"Appendix K \\u2014 Canonical Drift-Refusal Script\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── appendices-l-inspection-receipt.ttl ─────────────────────────
book:chapter-appendices-l-inspection-receipt a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix L \u2014 Canonical Inspection Receipt" ;
    book:slug "l-inspection-receipt" ;
    book:part "appendices" ;
    book:sourcePath "appendices/l-inspection-receipt.md" ;
    book:listing book:chapter-appendices-l-inspection-receipt-listing ;
    book:sourceText """# Appendix L — Canonical Inspection Receipt

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix L — Canonical Inspection Receipt**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix L — Canonical Inspection Receipt**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix L — Canonical Inspection Receipt** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-L-appendix-l-canonical-inspection-receipt.json`](../listings/appendix-L-appendix-l-canonical-inspection-receipt.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 0,
  "subject": "Appendix L — Canonical Inspection Receipt",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix L — Canonical Inspection Receipt**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-L` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-l-inspection-receipt-listing a book:Listing ;
    book:sourcePath "listings/appendix-L-appendix-l-canonical-inspection-receipt.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 0,
  \"subject\": \"Appendix L — Canonical Inspection Receipt\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── appendices-m-manifest-workflow.ttl ──────────────────────────
book:chapter-appendices-m-manifest-workflow a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix M \u2014 Canonical `MANIFEST.256` Workflow" ;
    book:slug "m-manifest-workflow" ;
    book:part "appendices" ;
    book:sourcePath "appendices/m-manifest-workflow.md" ;
    book:listing book:chapter-appendices-m-manifest-workflow-listing ;
    book:sourceText """# Appendix M — Canonical `MANIFEST.256` Workflow

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix M — Canonical `MANIFEST.256` Workflow**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix M — Canonical `MANIFEST.256` Workflow**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix M — Canonical `MANIFEST.256` Workflow** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-M-appendix-m-canonical-manifest-256-workflow.json`](../listings/appendix-M-appendix-m-canonical-manifest-256-workflow.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 0,
  "subject": "Appendix M — Canonical `MANIFEST.256` Workflow",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix M — Canonical `MANIFEST.256` Workflow**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-M` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-m-manifest-workflow-listing a book:Listing ;
    book:sourcePath "listings/appendix-M-appendix-m-canonical-manifest-256-workflow.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 0,
  \"subject\": \"Appendix M — Canonical `MANIFEST.256` Workflow\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── appendices-n-maturity-worksheet.ttl ─────────────────────────
book:chapter-appendices-n-maturity-worksheet a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix N \u2014 Pack Maturity Scoring Worksheet" ;
    book:slug "n-maturity-worksheet" ;
    book:part "appendices" ;
    book:sourcePath "appendices/n-maturity-worksheet.md" ;
    book:listing book:chapter-appendices-n-maturity-worksheet-listing ;
    book:sourceText """# Appendix N — Pack Maturity Scoring Worksheet

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix N — Pack Maturity Scoring Worksheet**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix N — Pack Maturity Scoring Worksheet**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix N — Pack Maturity Scoring Worksheet** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-N-appendix-n-pack-maturity-scoring-worksheet.rs`](../listings/appendix-N-appendix-n-pack-maturity-scoring-worksheet.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix N — Pack Maturity Scoring Worksheet",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix N — Pack Maturity Scoring Worksheet**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-N` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-n-maturity-worksheet-listing a book:Listing ;
    book:sourcePath "listings/appendix-N-appendix-n-pack-maturity-scoring-worksheet.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix N — Pack Maturity Scoring Worksheet\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── appendices-o-level-five-dod.ttl ─────────────────────────────
book:chapter-appendices-o-level-five-dod a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix O \u2014 Level Five Definition of Done" ;
    book:slug "o-level-five-dod" ;
    book:part "appendices" ;
    book:sourcePath "appendices/o-level-five-dod.md" ;
    book:listing book:chapter-appendices-o-level-five-dod-listing ;
    book:sourceText """# Appendix O — Level Five Definition of Done

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix O — Level Five Definition of Done**. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix O — Level Five Definition of Done**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix O — Level Five Definition of Done** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-O-appendix-o-level-five-definition-of-done.rs`](../listings/appendix-O-appendix-o-level-five-definition-of-done.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix O — Level Five Definition of Done",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix O — Level Five Definition of Done**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-O` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-o-level-five-dod-listing a book:Listing ;
    book:sourcePath "listings/appendix-O-appendix-o-level-five-definition-of-done.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix O — Level Five Definition of Done\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── appendices-p-tcps-module-matrix.ttl ─────────────────────────
book:chapter-appendices-p-tcps-module-matrix a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix P \u2014 TCPS Ontology-to-Module Matrix" ;
    book:slug "p-tcps-module-matrix" ;
    book:part "appendices" ;
    book:sourcePath "appendices/p-tcps-module-matrix.md" ;
    book:listing book:chapter-appendices-p-tcps-module-matrix-listing ;
    book:sourceText """# Appendix P — TCPS Ontology-to-Module Matrix

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix P — TCPS Ontology-to-Module Matrix**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix P — TCPS Ontology-to-Module Matrix**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix P — TCPS Ontology-to-Module Matrix** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-P-appendix-p-tcps-ontology-to-module-matrix.ttl`](../listings/appendix-P-appendix-p-tcps-ontology-to-module-matrix.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章0
    a pack:生成単位 ;
    dcterms:title "Appendix P — TCPS Ontology-to-Module Matrix"@en ;
    pack:出力経路 "src/generated/000_appendix_p_tcps_ontology_to_module_matrix.rs" ;
    pack:順序 0 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix P — TCPS Ontology-to-Module Matrix**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-P` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-p-tcps-module-matrix-listing a book:Listing ;
    book:sourcePath "listings/appendix-P-appendix-p-tcps-ontology-to-module-matrix.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章0
    a pack:生成単位 ;
    dcterms:title \"Appendix P — TCPS Ontology-to-Module Matrix\"@en ;
    pack:出力経路 \"src/generated/000_appendix_p_tcps_ontology_to_module_matrix.rs\" ;
    pack:順序 0 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── appendices-q-tcps-product-inventory.ttl ─────────────────────
book:chapter-appendices-q-tcps-product-inventory a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix Q \u2014 TCPS 132-File Product Inventory" ;
    book:slug "q-tcps-product-inventory" ;
    book:part "appendices" ;
    book:sourcePath "appendices/q-tcps-product-inventory.md" ;
    book:listing book:chapter-appendices-q-tcps-product-inventory-listing ;
    book:sourceText """# Appendix Q — TCPS 132-File Product Inventory

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix Q — TCPS 132-File Product Inventory**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix Q — TCPS 132-File Product Inventory**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix Q — TCPS 132-File Product Inventory** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-Q-appendix-q-tcps-132-file-product-inventory.json`](../listings/appendix-Q-appendix-q-tcps-132-file-product-inventory.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 0,
  "subject": "Appendix Q — TCPS 132-File Product Inventory",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix Q — TCPS 132-File Product Inventory**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-Q` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-q-tcps-product-inventory-listing a book:Listing ;
    book:sourcePath "listings/appendix-Q-appendix-q-tcps-132-file-product-inventory.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 0,
  \"subject\": \"Appendix Q — TCPS 132-File Product Inventory\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── appendices-r-tcps-divergence-ledger.ttl ─────────────────────
book:chapter-appendices-r-tcps-divergence-ledger a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix R \u2014 TCPS Corrective-Divergence Ledger" ;
    book:slug "r-tcps-divergence-ledger" ;
    book:part "appendices" ;
    book:sourcePath "appendices/r-tcps-divergence-ledger.md" ;
    book:listing book:chapter-appendices-r-tcps-divergence-ledger-listing ;
    book:sourceText """# Appendix R — TCPS Corrective-Divergence Ledger

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix R — TCPS Corrective-Divergence Ledger**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix R — TCPS Corrective-Divergence Ledger**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix R — TCPS Corrective-Divergence Ledger** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-R-appendix-r-tcps-corrective-divergence-ledger.rs`](../listings/appendix-R-appendix-r-tcps-corrective-divergence-ledger.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix R — TCPS Corrective-Divergence Ledger",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix R — TCPS Corrective-Divergence Ledger**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-R` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-r-tcps-divergence-ledger-listing a book:Listing ;
    book:sourcePath "listings/appendix-R-appendix-r-tcps-corrective-divergence-ledger.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix R — TCPS Corrective-Divergence Ledger\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── appendices-s-failure-codes.ttl ──────────────────────────────
book:chapter-appendices-s-failure-codes a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix S \u2014 ggen Failure-Code Reference" ;
    book:slug "s-failure-codes" ;
    book:part "appendices" ;
    book:sourcePath "appendices/s-failure-codes.md" ;
    book:listing book:chapter-appendices-s-failure-codes-listing ;
    book:sourceText """# Appendix S — ggen Failure-Code Reference

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix S — ggen Failure-Code Reference**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix S — ggen Failure-Code Reference**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix S — ggen Failure-Code Reference** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-S-appendix-s-ggen-failure-code-reference.rs`](../listings/appendix-S-appendix-s-ggen-failure-code-reference.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix S — ggen Failure-Code Reference",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix S — ggen Failure-Code Reference**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-S` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-s-failure-codes-listing a book:Listing ;
    book:sourcePath "listings/appendix-S-appendix-s-ggen-failure-code-reference.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix S — ggen Failure-Code Reference\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── appendices-t-glossary.ttl ───────────────────────────────────
book:chapter-appendices-t-glossary a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix T \u2014 Glossary" ;
    book:slug "t-glossary" ;
    book:part "appendices" ;
    book:sourcePath "appendices/t-glossary.md" ;
    book:listing book:chapter-appendices-t-glossary-listing ;
    book:sourceText """# Appendix T — Glossary

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix T — Glossary**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix T — Glossary**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix T — Glossary** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-T-appendix-t-glossary.rs`](../listings/appendix-T-appendix-t-glossary.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix T — Glossary",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix T — Glossary**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-T` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-t-glossary-listing a book:Listing ;
    book:sourcePath "listings/appendix-T-appendix-t-glossary.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix T — Glossary\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── appendices-u-bibliography.ttl ───────────────────────────────
book:chapter-appendices-u-bibliography a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix U \u2014 Bibliography and Project Record" ;
    book:slug "u-bibliography" ;
    book:part "appendices" ;
    book:sourcePath "appendices/u-bibliography.md" ;
    book:listing book:chapter-appendices-u-bibliography-listing ;
    book:sourceText """# Appendix U — Bibliography and Project Record

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix U — Bibliography and Project Record**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix U — Bibliography and Project Record**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix U — Bibliography and Project Record** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-U-appendix-u-bibliography-and-project-record.rs`](../listings/appendix-U-appendix-u-bibliography-and-project-record.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix U — Bibliography and Project Record",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix U — Bibliography and Project Record**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-U` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-u-bibliography-listing a book:Listing ;
    book:sourcePath "listings/appendix-U-appendix-u-bibliography-and-project-record.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix U — Bibliography and Project Record\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── appendices-v-index.ttl ──────────────────────────────────────
book:chapter-appendices-v-index a book:Chapter ;
    book:kind "appendix" ;
    book:title "Appendix V \u2014 Index" ;
    book:slug "v-index" ;
    book:part "appendices" ;
    book:sourcePath "appendices/v-index.md" ;
    book:listing book:chapter-appendices-v-index-listing ;
    book:sourceText """# Appendix V — Index

> **Pattern reference · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The appendices provide canonical, copyable production artifacts for the complete Level Five lifecycle. The narrow problem in this chapter is **Appendix V — Index**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Appendix V — Index**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Appendix V — Index** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-V-appendix-v-index.rs`](../listings/appendix-V-appendix-v-index.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Appendix V — Index",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Canonical layouts, schemas, examples, and worksheets remain directly usable and subordinate to live crate behavior.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Copying the reference produces a pack that fails current parsing, admission, generation, or verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS matrices, inventory, and corrective-divergence ledger make the case study reproducible rather than anecdotal.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Appendix V — Index**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-V` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `current manifest and template reference`
- `failure codes`
- `acceptance worksheets`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference text is subordinate to the live parser, engine behavior and current pack manifests.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-appendices-v-index-listing a book:Listing ;
    book:sourcePath "listings/appendix-V-appendix-v-index.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: \"Appendix V — Index\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── certification-321.ttl ───────────────────────────────────────
book:chapter-certification-321 a book:Chapter ;
    book:number "321"^^xsd:integer ;
    book:title "321. Laboratory One: Detect an Inert Pack" ;
    book:slug "detect-inert-pack" ;
    book:part "certification" ;
    book:sourcePath "certification/321-detect-inert-pack.md" ;
    book:listing book:listing-certification-321 ;
    book:sourceText """# 321. Detect an Inert Pack

> **Pattern 321 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **321. Laboratory One: Detect an Inert Pack**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Detect an Inert Pack**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Detect an Inert Pack.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/321-321-laboratory-one-detect-an-inert-pack.sh`](../listings/321-321-laboratory-one-detect-an-inert-pack.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="321. Laboratory One: Detect an Inert Pack"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **321. Laboratory One: Detect an Inert Pack**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-321` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-321 a book:Listing ;
    book:sourcePath "listings/321-321-laboratory-one-detect-an-inert-pack.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"321. Laboratory One: Detect an Inert Pack\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-322.ttl ───────────────────────────────────────
book:chapter-certification-322 a book:Chapter ;
    book:number "322"^^xsd:integer ;
    book:title "322. Laboratory Two: Add a Real Consumption Path" ;
    book:slug "add-consumption-path" ;
    book:part "certification" ;
    book:sourcePath "certification/322-add-consumption-path.md" ;
    book:listing book:listing-certification-322 ;
    book:sourceText """# 322. Add a Real Consumption Path

> **Pattern 322 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **322. Laboratory Two: Add a Real Consumption Path**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Add a Real Consumption Path**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Add a Real Consumption Path.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/322-322-laboratory-two-add-a-real-consumption-path.sh`](../listings/322-322-laboratory-two-add-a-real-consumption-path.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="322. Laboratory Two: Add a Real Consumption Path"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **322. Laboratory Two: Add a Real Consumption Path**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-322` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-322 a book:Listing ;
    book:sourcePath "listings/322-322-laboratory-two-add-a-real-consumption-path.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"322. Laboratory Two: Add a Real Consumption Path\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-323.ttl ───────────────────────────────────────
book:chapter-certification-323 a book:Chapter ;
    book:number "323"^^xsd:integer ;
    book:title "323. Laboratory Three: Stop Namespace Leakage" ;
    book:slug "stop-namespace-leakage" ;
    book:part "certification" ;
    book:sourcePath "certification/323-stop-namespace-leakage.md" ;
    book:listing book:listing-certification-323 ;
    book:sourceText """# 323. Stop Namespace Leakage

> **Pattern 323 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **323. Laboratory Three: Stop Namespace Leakage**. Shared classes are safe only when local individuals remain scoped; union graphs make implicit assumptions visible.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Stop Namespace Leakage**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Stop Namespace Leakage.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/323-323-laboratory-three-stop-namespace-leakage.sh`](../listings/323-323-laboratory-three-stop-namespace-leakage.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="323. Laboratory Three: Stop Namespace Leakage"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **323. Laboratory Three: Stop Namespace Leakage**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-323` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-323 a book:Listing ;
    book:sourcePath "listings/323-323-laboratory-three-stop-namespace-leakage.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"323. Laboratory Three: Stop Namespace Leakage\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-324.ttl ───────────────────────────────────────
book:chapter-certification-324 a book:Chapter ;
    book:number "324"^^xsd:integer ;
    book:title "324. Laboratory Four: Resolve Output Ownership" ;
    book:slug "resolve-output-ownership" ;
    book:part "certification" ;
    book:sourcePath "certification/324-resolve-output-ownership.md" ;
    book:listing book:listing-certification-324 ;
    book:sourceText """# 324. Resolve Output Ownership

> **Pattern 324 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **324. Laboratory Four: Resolve Output Ownership**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Resolve Output Ownership**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Resolve Output Ownership.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/324-324-laboratory-four-resolve-output-ownership.sh`](../listings/324-324-laboratory-four-resolve-output-ownership.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="324. Laboratory Four: Resolve Output Ownership"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **324. Laboratory Four: Resolve Output Ownership**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-324` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-324 a book:Listing ;
    book:sourcePath "listings/324-324-laboratory-four-resolve-output-ownership.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"324. Laboratory Four: Resolve Output Ownership\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-325.ttl ───────────────────────────────────────
book:chapter-certification-325 a book:Chapter ;
    book:number "325"^^xsd:integer ;
    book:title "325. Laboratory Five: Generate a Complete Rust Module" ;
    book:slug "generate-rust-module" ;
    book:part "certification" ;
    book:sourcePath "certification/325-generate-rust-module.md" ;
    book:listing book:listing-certification-325 ;
    book:sourceText """# 325. Generate a Complete Rust Module

> **Pattern 325 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **325. Laboratory Five: Generate a Complete Rust Module**. Rust provides compile-time leverage: illegal production states can be removed from the vocabulary instead of detected after actuation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate a Complete Rust Module**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate a Complete Rust Module.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/325-325-laboratory-five-generate-a-complete-rust-module.sh`](../listings/325-325-laboratory-five-generate-a-complete-rust-module.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="325. Laboratory Five: Generate a Complete Rust Module"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **325. Laboratory Five: Generate a Complete Rust Module**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-325` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-325 a book:Listing ;
    book:sourcePath "listings/325-325-laboratory-five-generate-a-complete-rust-module.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"325. Laboratory Five: Generate a Complete Rust Module\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-326.ttl ───────────────────────────────────────
book:chapter-certification-326 a book:Chapter ;
    book:number "326"^^xsd:integer ;
    book:title "326. Laboratory Six: Generate an Independent Proof" ;
    book:slug "generate-independent-proof" ;
    book:part "certification" ;
    book:sourcePath "certification/326-generate-independent-proof.md" ;
    book:listing book:listing-certification-326 ;
    book:sourceText """# 326. Generate an Independent Proof

> **Pattern 326 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **326. Laboratory Six: Generate an Independent Proof**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate an Independent Proof**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate an Independent Proof.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/326-326-laboratory-six-generate-an-independent-proof.sh`](../listings/326-326-laboratory-six-generate-an-independent-proof.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="326. Laboratory Six: Generate an Independent Proof"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **326. Laboratory Six: Generate an Independent Proof**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-326` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-326 a book:Listing ;
    book:sourcePath "listings/326-326-laboratory-six-generate-an-independent-proof.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"326. Laboratory Six: Generate an Independent Proof\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-327.ttl ───────────────────────────────────────
book:chapter-certification-327 a book:Chapter ;
    book:number "327"^^xsd:integer ;
    book:title "327. Laboratory Seven: Reuse a Verbatim Test" ;
    book:slug "verbatim-test" ;
    book:part "certification" ;
    book:sourcePath "certification/327-verbatim-test.md" ;
    book:listing book:listing-certification-327 ;
    book:sourceText """# 327. Reuse a Verbatim Test

> **Pattern 327 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **327. Laboratory Seven: Reuse a Verbatim Test**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reuse a Verbatim Test**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Reuse a Verbatim Test.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/327-327-laboratory-seven-reuse-a-verbatim-test.sh`](../listings/327-327-laboratory-seven-reuse-a-verbatim-test.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="327. Laboratory Seven: Reuse a Verbatim Test"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **327. Laboratory Seven: Reuse a Verbatim Test**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-327` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-327 a book:Listing ;
    book:sourcePath "listings/327-327-laboratory-seven-reuse-a-verbatim-test.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"327. Laboratory Seven: Reuse a Verbatim Test\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-328.ttl ───────────────────────────────────────
book:chapter-certification-328 a book:Chapter ;
    book:number "328"^^xsd:integer ;
    book:title "328. Laboratory Eight: Build a Zero-Knowledge Consumer" ;
    book:slug "zero-knowledge-consumer" ;
    book:part "certification" ;
    book:sourcePath "certification/328-zero-knowledge-consumer.md" ;
    book:listing book:listing-certification-328 ;
    book:sourceText """# 328. Build a Zero-Knowledge Consumer

> **Pattern 328 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **328. Laboratory Eight: Build a Zero-Knowledge Consumer**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Build a Zero-Knowledge Consumer**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Build a Zero-Knowledge Consumer.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/328-328-laboratory-eight-build-a-zero-knowledge-consumer.sh`](../listings/328-328-laboratory-eight-build-a-zero-knowledge-consumer.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="328. Laboratory Eight: Build a Zero-Knowledge Consumer"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **328. Laboratory Eight: Build a Zero-Knowledge Consumer**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-328` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-328 a book:Listing ;
    book:sourcePath "listings/328-328-laboratory-eight-build-a-zero-knowledge-consumer.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"328. Laboratory Eight: Build a Zero-Knowledge Consumer\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-329.ttl ───────────────────────────────────────
book:chapter-certification-329 a book:Chapter ;
    book:number "329"^^xsd:integer ;
    book:title "329. Laboratory Nine: Compose Eleven Packs" ;
    book:slug "compose-eleven-packs" ;
    book:part "certification" ;
    book:sourcePath "certification/329-compose-eleven-packs.md" ;
    book:listing book:listing-certification-329 ;
    book:sourceText """# 329. Compose Eleven Packs

> **Pattern 329 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **329. Laboratory Nine: Compose Eleven Packs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Compose Eleven Packs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Compose Eleven Packs.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/329-329-laboratory-nine-compose-eleven-packs.sh`](../listings/329-329-laboratory-nine-compose-eleven-packs.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="329. Laboratory Nine: Compose Eleven Packs"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **329. Laboratory Nine: Compose Eleven Packs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-329` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-329 a book:Listing ;
    book:sourcePath "listings/329-329-laboratory-nine-compose-eleven-packs.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"329. Laboratory Nine: Compose Eleven Packs\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-330.ttl ───────────────────────────────────────
book:chapter-certification-330 a book:Chapter ;
    book:number "330"^^xsd:integer ;
    book:title "330. Laboratory Ten: Prove Byte-Identical Regeneration" ;
    book:slug "byte-identical-regeneration" ;
    book:part "certification" ;
    book:sourcePath "certification/330-byte-identical-regeneration.md" ;
    book:listing book:listing-certification-330 ;
    book:sourceText """# 330. Prove Byte-Identical Regeneration

> **Pattern 330 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **330. Laboratory Ten: Prove Byte-Identical Regeneration**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Prove Byte-Identical Regeneration**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Prove Byte-Identical Regeneration.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/330-330-laboratory-ten-prove-byte-identical-regeneration.sh`](../listings/330-330-laboratory-ten-prove-byte-identical-regeneration.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="330. Laboratory Ten: Prove Byte-Identical Regeneration"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **330. Laboratory Ten: Prove Byte-Identical Regeneration**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-330` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-330 a book:Listing ;
    book:sourcePath "listings/330-330-laboratory-ten-prove-byte-identical-regeneration.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"330. Laboratory Ten: Prove Byte-Identical Regeneration\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-331.ttl ───────────────────────────────────────
book:chapter-certification-331 a book:Chapter ;
    book:number "331"^^xsd:integer ;
    book:title "331. Laboratory Eleven: Refuse Reference Drift" ;
    book:slug "refuse-reference-drift" ;
    book:part "certification" ;
    book:sourcePath "certification/331-refuse-reference-drift.md" ;
    book:listing book:listing-certification-331 ;
    book:sourceText """# 331. Refuse Reference Drift

> **Pattern 331 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **331. Laboratory Eleven: Refuse Reference Drift**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Refuse Reference Drift**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Refuse Reference Drift.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/331-331-laboratory-eleven-refuse-reference-drift.sh`](../listings/331-331-laboratory-eleven-refuse-reference-drift.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="331. Laboratory Eleven: Refuse Reference Drift"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **331. Laboratory Eleven: Refuse Reference Drift**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-331` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-331 a book:Listing ;
    book:sourcePath "listings/331-331-laboratory-eleven-refuse-reference-drift.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"331. Laboratory Eleven: Refuse Reference Drift\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-332.ttl ───────────────────────────────────────
book:chapter-certification-332 a book:Chapter ;
    book:number "332"^^xsd:integer ;
    book:title "332. Laboratory Twelve: Emit a Complete Receipt Chain" ;
    book:slug "complete-receipt-chain" ;
    book:part "certification" ;
    book:sourcePath "certification/332-complete-receipt-chain.md" ;
    book:listing book:listing-certification-332 ;
    book:sourceText """# 332. Emit a Complete Receipt Chain

> **Pattern 332 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **332. Laboratory Twelve: Emit a Complete Receipt Chain**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Emit a Complete Receipt Chain**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Emit a Complete Receipt Chain.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/332-332-laboratory-twelve-emit-a-complete-receipt-chain.json`](../listings/332-332-laboratory-twelve-emit-a-complete-receipt-chain.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 332,
  "subject": "332. Laboratory Twelve: Emit a Complete Receipt Chain",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **332. Laboratory Twelve: Emit a Complete Receipt Chain**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-332` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-332 a book:Listing ;
    book:sourcePath "listings/332-332-laboratory-twelve-emit-a-complete-receipt-chain.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 332,
  \"subject\": \"332. Laboratory Twelve: Emit a Complete Receipt Chain\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── certification-333.ttl ───────────────────────────────────────
book:chapter-certification-333 a book:Chapter ;
    book:number "333"^^xsd:integer ;
    book:title "333. Laboratory Thirteen: Generate a Multi-Crate Product" ;
    book:slug "multicrate-product" ;
    book:part "certification" ;
    book:sourcePath "certification/333-multicrate-product.md" ;
    book:listing book:listing-certification-333 ;
    book:sourceText """# 333. Generate a Multi-Crate Product

> **Pattern 333 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **333. Laboratory Thirteen: Generate a Multi-Crate Product**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate a Multi-Crate Product**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate a Multi-Crate Product.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/333-333-laboratory-thirteen-generate-a-multi-crate-product.sh`](../listings/333-333-laboratory-thirteen-generate-a-multi-crate-product.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="333. Laboratory Thirteen: Generate a Multi-Crate Product"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **333. Laboratory Thirteen: Generate a Multi-Crate Product**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-333` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-333 a book:Listing ;
    book:sourcePath "listings/333-333-laboratory-thirteen-generate-a-multi-crate-product.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"333. Laboratory Thirteen: Generate a Multi-Crate Product\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-334.ttl ───────────────────────────────────────
book:chapter-certification-334 a book:Chapter ;
    book:number "334"^^xsd:integer ;
    book:title "334. Laboratory Fourteen: Generate Release Scaffolding" ;
    book:slug "release-scaffolding" ;
    book:part "certification" ;
    book:sourcePath "certification/334-release-scaffolding.md" ;
    book:listing book:listing-certification-334 ;
    book:sourceText """# 334. Generate Release Scaffolding

> **Pattern 334 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **334. Laboratory Fourteen: Generate Release Scaffolding**. Release scaffolding is part of the product whenever downstream users require it to build, verify, package, or attest the subsystem.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Release Scaffolding**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Release Scaffolding.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/334-334-laboratory-fourteen-generate-release-scaffolding.sh`](../listings/334-334-laboratory-fourteen-generate-release-scaffolding.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="334. Laboratory Fourteen: Generate Release Scaffolding"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **334. Laboratory Fourteen: Generate Release Scaffolding**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-334` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-334 a book:Listing ;
    book:sourcePath "listings/334-334-laboratory-fourteen-generate-release-scaffolding.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"334. Laboratory Fourteen: Generate Release Scaffolding\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-335.ttl ───────────────────────────────────────
book:chapter-certification-335 a book:Chapter ;
    book:number "335"^^xsd:integer ;
    book:title "335. Laboratory Fifteen: Run the Final Adversarial Audit" ;
    book:slug "adversarial-audit" ;
    book:part "certification" ;
    book:sourcePath "certification/335-adversarial-audit.md" ;
    book:listing book:listing-certification-335 ;
    book:sourceText """# 335. Run the Final Adversarial Audit

> **Pattern 335 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **335. Laboratory Fifteen: Run the Final Adversarial Audit**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Run the Final Adversarial Audit**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Run the Final Adversarial Audit.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/335-335-laboratory-fifteen-run-the-final-adversarial-audit.sh`](../listings/335-335-laboratory-fifteen-run-the-final-adversarial-audit.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="335. Laboratory Fifteen: Run the Final Adversarial Audit"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **335. Laboratory Fifteen: Run the Final Adversarial Audit**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-335` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-335 a book:Listing ;
    book:sourcePath "listings/335-335-laboratory-fifteen-run-the-final-adversarial-audit.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"335. Laboratory Fifteen: Run the Final Adversarial Audit\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── certification-336.ttl ───────────────────────────────────────
book:chapter-certification-336 a book:Chapter ;
    book:number "336"^^xsd:integer ;
    book:title "336. Graduation: A Level Five Pack" ;
    book:slug "graduation" ;
    book:part "certification" ;
    book:sourcePath "certification/336-graduation.md" ;
    book:listing book:listing-certification-336 ;
    book:sourceText """# 336. Graduation: A Level Five Pack

> **Pattern 336 · Certification Laboratories**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Certification is adversarial construction: each laboratory introduces a failure class and requires a durable countermeasure. The narrow problem in this chapter is **336. Graduation: A Level Five Pack**. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Graduation: A Level Five Pack**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Graduation: A Level Five Pack** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/336-336-graduation-a-level-five-pack.sh`](../listings/336-336-graduation-a-level-five-pack.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="336. Graduation: A Level Five Pack"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader proves both the success path and the refusal path for a concrete Level Five property.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The laboratory can pass without observing the intended failure before repair.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The final audit reproduces the exact classes of abnormalities found during TCPS and multi-pack integration before granting Level Five standing.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **336. Graduation: A Level Five Pack**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-336` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The laboratory produces a certification artifact suitable for inclusion in the final Level Five bundle.

## Connections

This pattern receives its context from **Certification Laboratories** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `adversarial mutation`
- `multi-pack safety`
- `idempotency`
- `receipt-chain verification`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

The laboratory assets exist; each laboratory must execute on the current head before graduation.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-certification-336 a book:Listing ;
    book:sourcePath "listings/336-336-graduation-a-level-five-pack.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"336. Graduation: A Level Five Pack\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── composition-161.ttl ─────────────────────────────────────────
book:chapter-161 a book:Chapter ;
    book:number 161 ;
    book:title "161. The Union Graph" ;
    book:slug "union-graph" ;
    book:part "composition" ;
    book:sourcePath "composition/161-union-graph.md" ;
    book:sourceText """# 161. The Union Graph

> **Pattern 161 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **161. The Union Graph**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The Union Graph**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The Union Graph** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/161-161-the-union-graph.rs`](../listings/161-161-the-union-graph.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 161,
    title: "161. The Union Graph",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 161);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **161. The Union Graph**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-161` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-161-listing a book:Listing ;
    book:sourcePath "listings/161-161-the-union-graph.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 161,
    title: "161. The Union Graph",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 161);
}
""" .

book:chapter-161 book:listing book:chapter-161-listing .

# ── composition-162.ttl ─────────────────────────────────────────
book:chapter-162 a book:Chapter ;
    book:number 162 ;
    book:title "162. Namespace Safety" ;
    book:slug "namespace-safety" ;
    book:part "composition" ;
    book:sourcePath "composition/162-namespace-safety.md" ;
    book:sourceText """# 162. Namespace Safety

> **Pattern 162 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **162. Namespace Safety**. Shared classes are safe only when local individuals remain scoped; union graphs make implicit assumptions visible.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Namespace Safety**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Namespace Safety** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/162-162-namespace-safety.rs`](../listings/162-162-namespace-safety.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 162,
    title: "162. Namespace Safety",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 162);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **162. Namespace Safety**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-162` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-162-listing a book:Listing ;
    book:sourcePath "listings/162-162-namespace-safety.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 162,
    title: "162. Namespace Safety",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 162);
}
""" .

book:chapter-162 book:listing book:chapter-162-listing .

# ── composition-163.ttl ─────────────────────────────────────────
book:chapter-163 a book:Chapter ;
    book:number 163 ;
    book:title "163. Shared Vocabulary Reuse" ;
    book:slug "shared-vocabulary" ;
    book:part "composition" ;
    book:sourcePath "composition/163-shared-vocabulary.md" ;
    book:sourceText """# 163. Shared Vocabulary Reuse

> **Pattern 163 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **163. Shared Vocabulary Reuse**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Shared Vocabulary Reuse**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Shared Vocabulary Reuse** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/163-163-shared-vocabulary-reuse.ttl`](../listings/163-163-shared-vocabulary-reuse.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章163
    a pack:生成単位 ;
    dcterms:title "163. Shared Vocabulary Reuse"@en ;
    pack:出力経路 "src/generated/163_163_shared_vocabulary_reuse.rs" ;
    pack:順序 163 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **163. Shared Vocabulary Reuse**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-163` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-163-listing a book:Listing ;
    book:sourcePath "listings/163-163-shared-vocabulary-reuse.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章163
    a pack:生成単位 ;
    dcterms:title "163. Shared Vocabulary Reuse"@en ;
    pack:出力経路 "src/generated/163_163_shared_vocabulary_reuse.rs" ;
    pack:順序 163 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

book:chapter-163 book:listing book:chapter-163-listing .

# ── composition-164.ttl ─────────────────────────────────────────
book:chapter-164 a book:Chapter ;
    book:number 164 ;
    book:title "164. Local Individuals under Shared Classes" ;
    book:slug "local-individuals" ;
    book:part "composition" ;
    book:sourcePath "composition/164-local-individuals.md" ;
    book:sourceText """# 164. Local Individuals under Shared Classes

> **Pattern 164 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **164. Local Individuals under Shared Classes**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Local Individuals under Shared Classes**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Local Individuals under Shared Classes** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/164-164-local-individuals-under-shared-classes.rs`](../listings/164-164-local-individuals-under-shared-classes.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 164,
    title: "164. Local Individuals under Shared Classes",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 164);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **164. Local Individuals under Shared Classes**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-164` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-164-listing a book:Listing ;
    book:sourcePath "listings/164-164-local-individuals-under-shared-classes.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 164,
    title: "164. Local Individuals under Shared Classes",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 164);
}
""" .

book:chapter-164 book:listing book:chapter-164-listing .

# ── composition-165.ttl ─────────────────────────────────────────
book:chapter-165 a book:Chapter ;
    book:number 165 ;
    book:title "165. Cross-Pack Query Contamination" ;
    book:slug "query-contamination" ;
    book:part "composition" ;
    book:sourcePath "composition/165-query-contamination.md" ;
    book:sourceText """# 165. Cross-Pack Query Contamination

> **Pattern 165 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **165. Cross-Pack Query Contamination**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Cross-Pack Query Contamination**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Cross-Pack Query Contamination** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/165-165-cross-pack-query-contamination.rq`](../listings/165-165-cross-pack-query-contamination.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **165. Cross-Pack Query Contamination**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-165` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-165-listing a book:Listing ;
    book:sourcePath "listings/165-165-cross-pack-query-contamination.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
""" .

book:chapter-165 book:listing book:chapter-165-listing .

# ── composition-166.ttl ─────────────────────────────────────────
book:chapter-166 a book:Chapter ;
    book:number 166 ;
    book:title "166. Output-Path Collisions" ;
    book:slug "output-collisions" ;
    book:part "composition" ;
    book:sourcePath "composition/166-output-collisions.md" ;
    book:sourceText """# 166. Output-Path Collisions

> **Pattern 166 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **166. Output-Path Collisions**. Output ownership must be singular. Two templates writing one path are competing actuators and must be refused.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Output-Path Collisions**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Output-Path Collisions** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/166-166-output-path-collisions.rs`](../listings/166-166-output-path-collisions.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 166,
    title: "166. Output-Path Collisions",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 166);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **166. Output-Path Collisions**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-166` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-166-listing a book:Listing ;
    book:sourcePath "listings/166-166-output-path-collisions.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 166,
    title: "166. Output-Path Collisions",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 166);
}
""" .

book:chapter-166 book:listing book:chapter-166-listing .

# ── composition-167.ttl ─────────────────────────────────────────
book:chapter-167 a book:Chapter ;
    book:number 167 ;
    book:title "167. Module-Wiring Collisions" ;
    book:slug "module-wiring-collisions" ;
    book:part "composition" ;
    book:sourcePath "composition/167-module-wiring-collisions.md" ;
    book:sourceText """# 167. Module-Wiring Collisions

> **Pattern 167 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **167. Module-Wiring Collisions**. Output ownership must be singular. Two templates writing one path are competing actuators and must be refused.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Module-Wiring Collisions**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Module-Wiring Collisions** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/167-167-module-wiring-collisions.rs`](../listings/167-167-module-wiring-collisions.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 167,
    title: "167. Module-Wiring Collisions",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 167);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **167. Module-Wiring Collisions**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-167` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-167-listing a book:Listing ;
    book:sourcePath "listings/167-167-module-wiring-collisions.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 167,
    title: "167. Module-Wiring Collisions",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 167);
}
""" .

book:chapter-167 book:listing book:chapter-167-listing .

# ── composition-168.ttl ─────────────────────────────────────────
book:chapter-168 a book:Chapter ;
    book:number 168 ;
    book:title "168. Single-Writer Aggregation" ;
    book:slug "single-writer-aggregation" ;
    book:part "composition" ;
    book:sourcePath "composition/168-single-writer-aggregation.md" ;
    book:sourceText """# 168. Single-Writer Aggregation

> **Pattern 168 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **168. Single-Writer Aggregation**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Single-Writer Aggregation**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Single-Writer Aggregation** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/168-168-single-writer-aggregation.rs`](../listings/168-168-single-writer-aggregation.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 168,
    title: "168. Single-Writer Aggregation",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 168);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **168. Single-Writer Aggregation**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-168` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-168-listing a book:Listing ;
    book:sourcePath "listings/168-168-single-writer-aggregation.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 168,
    title: "168. Single-Writer Aggregation",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 168);
}
""" .

book:chapter-168 book:listing book:chapter-168-listing .

# ── composition-169.ttl ─────────────────────────────────────────
book:chapter-169 a book:Chapter ;
    book:number 169 ;
    book:title "169. Combined Consumer Tests" ;
    book:slug "combined-consumer-tests" ;
    book:part "composition" ;
    book:sourcePath "composition/169-combined-consumer-tests.md" ;
    book:sourceText """# 169. Combined Consumer Tests

> **Pattern 169 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **169. Combined Consumer Tests**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Combined Consumer Tests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Combined Consumer Tests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/169-169-combined-consumer-tests.rs`](../listings/169-169-combined-consumer-tests.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 169,
    title: "169. Combined Consumer Tests",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 169);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **169. Combined Consumer Tests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-169` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-169-listing a book:Listing ;
    book:sourcePath "listings/169-169-combined-consumer-tests.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 169,
    title: "169. Combined Consumer Tests",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 169);
}
""" .

book:chapter-169 book:listing book:chapter-169-listing .

# ── composition-170.ttl ─────────────────────────────────────────
book:chapter-170 a book:Chapter ;
    book:number 170 ;
    book:title "170. Eleven-Pack Collision Laboratory" ;
    book:slug "eleven-pack-lab" ;
    book:part "composition" ;
    book:sourcePath "composition/170-eleven-pack-lab.md" ;
    book:sourceText """# 170. Eleven-Pack Collision Laboratory

> **Pattern 170 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **170. Eleven-Pack Collision Laboratory**. Output ownership must be singular. Two templates writing one path are competing actuators and must be refused.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Eleven-Pack Collision Laboratory**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Eleven-Pack Collision Laboratory** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/170-170-eleven-pack-collision-laboratory.sh`](../listings/170-170-eleven-pack-collision-laboratory.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="170. Eleven-Pack Collision Laboratory"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **170. Eleven-Pack Collision Laboratory**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-170` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-170-listing a book:Listing ;
    book:sourcePath "listings/170-170-eleven-pack-collision-laboratory.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject="170. Eleven-Pack Collision Laboratory"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
""" .

book:chapter-170 book:listing book:chapter-170-listing .

# ── composition-171.ttl ─────────────────────────────────────────
book:chapter-171 a book:Chapter ;
    book:number 171 ;
    book:title "171. Composition Receipts" ;
    book:slug "composition-receipts" ;
    book:part "composition" ;
    book:sourcePath "composition/171-composition-receipts.md" ;
    book:sourceText """# 171. Composition Receipts

> **Pattern 171 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **171. Composition Receipts**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Composition Receipts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Composition Receipts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/171-171-composition-receipts.json`](../listings/171-171-composition-receipts.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 171,
  "subject": "171. Composition Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **171. Composition Receipts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-171` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-171-listing a book:Listing ;
    book:sourcePath "listings/171-171-composition-receipts.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 171,
  "subject": "171. Composition Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-171 book:listing book:chapter-171-listing .

# ── composition-172.ttl ─────────────────────────────────────────
book:chapter-172 a book:Chapter ;
    book:number 172 ;
    book:title "172. Proving Safe Union" ;
    book:slug "proving-safe-union" ;
    book:part "composition" ;
    book:sourcePath "composition/172-proving-safe-union.md" ;
    book:sourceText """# 172. Proving Safe Union

> **Pattern 172 · The Pack Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack correctness in isolation does not imply safe composition under a union graph and shared output tree. The narrow problem in this chapter is **172. Proving Safe Union**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Proving Safe Union**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Proving Safe Union** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/172-172-proving-safe-union.rs`](../listings/172-172-proving-safe-union.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 172,
    title: "172. Proving Safe Union",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 172);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Packs compose through shared public vocabulary while preserving local identity, non-interference, and single-writer ownership.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Introducing a second valid pack changes another pack’s rows, paths, module wiring, or receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The project found both RDF namespace contamination and eleven-pack lib.rs ownership collisions only after combined-consumer verification.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **172. Proving Safe Union**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-172` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `union-graph composition`
- `non-interference`
- `output collision refusal`
- `package composition`

### Current repository evidence

- `crates/ggen-marketplace/src/packs_registry/compose.rs`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/repo-as-found-pack`
- `packs/repo-load-path-pack`
- `packs/repo-reconciliation-pack`

### Bounded standing

`PARTIAL`

### Open gap

The repository has multi-pack tests, but every possible pack neighborhood is not proven safe.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-172-listing a book:Listing ;
    book:sourcePath "listings/172-172-proving-safe-union.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 172,
    title: "172. Proving Safe Union",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 172);
}
""" .

book:chapter-172 book:listing book:chapter-172-listing .

# ── consumer-149.ttl ────────────────────────────────────────────
book:chapter-149 a book:Chapter ;
    book:number 149 ;
    book:title "149. A Pack without a Consumer Is Not Finished" ;
    book:slug "pack-needs-consumer" ;
    book:part "consumer" ;
    book:sourcePath "consumer/149-pack-needs-consumer.md" ;
    book:sourceText """# 149. A Pack without a Consumer Is Not Finished

> **Pattern 149 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **149. A Pack without a Consumer Is Not Finished**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **A Pack without a Consumer Is Not Finished**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **A Pack without a Consumer Is Not Finished** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/149-149-a-pack-without-a-consumer-is-not-finished.rs`](../listings/149-149-a-pack-without-a-consumer-is-not-finished.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 149,
    title: "149. A Pack without a Consumer Is Not Finished",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 149);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **149. A Pack without a Consumer Is Not Finished**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-149` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-149-listing a book:Listing ;
    book:sourcePath "listings/149-149-a-pack-without-a-consumer-is-not-finished.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 149,
    title: "149. A Pack without a Consumer Is Not Finished",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 149);
}
""" .

book:chapter-149 book:listing book:chapter-149-listing .

# ── consumer-150.ttl ────────────────────────────────────────────
book:chapter-150 a book:Chapter ;
    book:number 150 ;
    book:title "150. `ggen.toml`" ;
    book:slug "ggen-toml" ;
    book:part "consumer" ;
    book:sourcePath "consumer/150-ggen-toml.md" ;
    book:sourceText """# 150. `ggen.toml`

> **Pattern 150 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **150. `ggen.toml`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`ggen.toml`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`ggen.toml`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/150-150-ggen-toml.toml`](../listings/150-150-ggen-toml.toml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```toml
[pack]
name = "chapter-150-150-ggen-toml"
version = "26.7.19"
description = "150. `ggen.toml`"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **150. `ggen.toml`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-150` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-150-listing a book:Listing ;
    book:sourcePath "listings/150-150-ggen-toml.toml" ;
    book:sourceText """[pack]
name = "chapter-150-150-ggen-toml"
version = "26.7.19"
description = "150. `ggen.toml`"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
""" .

book:chapter-150 book:listing book:chapter-150-listing .

# ── consumer-151.ttl ────────────────────────────────────────────
book:chapter-151 a book:Chapter ;
    book:number 151 ;
    book:title "151. Path Dependencies" ;
    book:slug "path-dependencies" ;
    book:part "consumer" ;
    book:sourcePath "consumer/151-path-dependencies.md" ;
    book:sourceText """# 151. Path Dependencies

> **Pattern 151 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **151. Path Dependencies**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Path Dependencies**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Path Dependencies** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Wire the pack.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Sync into a clean project.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Build and test downstream.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/151-151-path-dependencies.toml`](../listings/151-151-path-dependencies.toml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```toml
[pack]
name = "chapter-151-151-path-dependencies"
version = "26.7.19"
description = "151. Path Dependencies"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **151. Path Dependencies**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-151` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-151-listing a book:Listing ;
    book:sourcePath "listings/151-151-path-dependencies.toml" ;
    book:sourceText """[pack]
name = "chapter-151-151-path-dependencies"
version = "26.7.19"
description = "151. Path Dependencies"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
""" .

book:chapter-151 book:listing book:chapter-151-listing .

# ── consumer-152.ttl ────────────────────────────────────────────
book:chapter-152 a book:Chapter ;
    book:number 152 ;
    book:title "152. Source-Law Inputs" ;
    book:slug "source-law-inputs" ;
    book:part "consumer" ;
    book:sourcePath "consumer/152-source-law-inputs.md" ;
    book:sourceText """# 152. Source-Law Inputs

> **Pattern 152 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **152. Source-Law Inputs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Source-Law Inputs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Source-Law Inputs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/152-152-source-law-inputs.rs`](../listings/152-152-source-law-inputs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 152,
    title: "152. Source-Law Inputs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 152);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **152. Source-Law Inputs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-152` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-152-listing a book:Listing ;
    book:sourcePath "listings/152-152-source-law-inputs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 152,
    title: "152. Source-Law Inputs",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 152);
}
""" .

book:chapter-152 book:listing book:chapter-152-listing .

# ── consumer-153.ttl ────────────────────────────────────────────
book:chapter-153 a book:Chapter ;
    book:number 153 ;
    book:title "153. Generated Output Mounts" ;
    book:slug "generated-output-mounts" ;
    book:part "consumer" ;
    book:sourcePath "consumer/153-generated-output-mounts.md" ;
    book:sourceText """# 153. Generated Output Mounts

> **Pattern 153 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **153. Generated Output Mounts**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generated Output Mounts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Generated Output Mounts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/153-153-generated-output-mounts.rs`](../listings/153-153-generated-output-mounts.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 153,
    title: "153. Generated Output Mounts",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 153);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **153. Generated Output Mounts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-153` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-153-listing a book:Listing ;
    book:sourcePath "listings/153-153-generated-output-mounts.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 153,
    title: "153. Generated Output Mounts",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 153);
}
""" .

book:chapter-153 book:listing book:chapter-153-listing .

# ── consumer-154.ttl ────────────────────────────────────────────
book:chapter-154 a book:Chapter ;
    book:number 154 ;
    book:title "154. Module Wiring" ;
    book:slug "module-wiring" ;
    book:part "consumer" ;
    book:sourcePath "consumer/154-module-wiring.md" ;
    book:sourceText """# 154. Module Wiring

> **Pattern 154 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **154. Module Wiring**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Module Wiring**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Module Wiring** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/154-154-module-wiring.rs`](../listings/154-154-module-wiring.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 154,
    title: "154. Module Wiring",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 154);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **154. Module Wiring**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-154` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-154-listing a book:Listing ;
    book:sourcePath "listings/154-154-module-wiring.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 154,
    title: "154. Module Wiring",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 154);
}
""" .

book:chapter-154 book:listing book:chapter-154-listing .

# ── consumer-155.ttl ────────────────────────────────────────────
book:chapter-155 a book:Chapter ;
    book:number 155 ;
    book:title "155. Engine-Owned Aggregators" ;
    book:slug "engine-owned-aggregators" ;
    book:part "consumer" ;
    book:sourcePath "consumer/155-engine-owned-aggregators.md" ;
    book:sourceText """# 155. Engine-Owned Aggregators

> **Pattern 155 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **155. Engine-Owned Aggregators**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Engine-Owned Aggregators**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Engine-Owned Aggregators** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/155-155-engine-owned-aggregators.rs`](../listings/155-155-engine-owned-aggregators.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 155,
    title: "155. Engine-Owned Aggregators",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 155);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **155. Engine-Owned Aggregators**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-155` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-155-listing a book:Listing ;
    book:sourcePath "listings/155-155-engine-owned-aggregators.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 155,
    title: "155. Engine-Owned Aggregators",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 155);
}
""" .

book:chapter-155 book:listing book:chapter-155-listing .

# ── consumer-156.ttl ────────────────────────────────────────────
book:chapter-156 a book:Chapter ;
    book:number 156 ;
    book:title "156. Zero-Knowledge Consumer Setup" ;
    book:slug "zero-knowledge-consumer" ;
    book:part "consumer" ;
    book:sourcePath "consumer/156-zero-knowledge-consumer.md" ;
    book:sourceText """# 156. Zero-Knowledge Consumer Setup

> **Pattern 156 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **156. Zero-Knowledge Consumer Setup**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Zero-Knowledge Consumer Setup**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Zero-Knowledge Consumer Setup** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/156-156-zero-knowledge-consumer-setup.rs`](../listings/156-156-zero-knowledge-consumer-setup.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 156,
    title: "156. Zero-Knowledge Consumer Setup",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 156);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **156. Zero-Knowledge Consumer Setup**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-156` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-156-listing a book:Listing ;
    book:sourcePath "listings/156-156-zero-knowledge-consumer-setup.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 156,
    title: "156. Zero-Knowledge Consumer Setup",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 156);
}
""" .

book:chapter-156 book:listing book:chapter-156-listing .

# ── consumer-157.ttl ────────────────────────────────────────────
book:chapter-157 a book:Chapter ;
    book:number 157 ;
    book:title "157. Build the Consumer" ;
    book:slug "build-consumer" ;
    book:part "consumer" ;
    book:sourcePath "consumer/157-build-consumer.md" ;
    book:sourceText """# 157. Build the Consumer

> **Pattern 157 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **157. Build the Consumer**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Build the Consumer**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Build the Consumer.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/157-157-build-the-consumer.sh`](../listings/157-157-build-the-consumer.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="157. Build the Consumer"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **157. Build the Consumer**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-157` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-157-listing a book:Listing ;
    book:sourcePath "listings/157-157-build-the-consumer.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject="157. Build the Consumer"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
""" .

book:chapter-157 book:listing book:chapter-157-listing .

# ── consumer-158.ttl ────────────────────────────────────────────
book:chapter-158 a book:Chapter ;
    book:number 158 ;
    book:title "158. Run the Consumer" ;
    book:slug "run-consumer" ;
    book:part "consumer" ;
    book:sourcePath "consumer/158-run-consumer.md" ;
    book:sourceText """# 158. Run the Consumer

> **Pattern 158 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **158. Run the Consumer**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Run the Consumer**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Run the Consumer.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/158-158-run-the-consumer.sh`](../listings/158-158-run-the-consumer.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="158. Run the Consumer"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **158. Run the Consumer**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-158` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-158-listing a book:Listing ;
    book:sourcePath "listings/158-158-run-the-consumer.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject="158. Run the Consumer"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
""" .

book:chapter-158 book:listing book:chapter-158-listing .

# ── consumer-159.ttl ────────────────────────────────────────────
book:chapter-159 a book:Chapter ;
    book:number 159 ;
    book:title "159. Test the Consumer" ;
    book:slug "test-consumer" ;
    book:part "consumer" ;
    book:sourcePath "consumer/159-test-consumer.md" ;
    book:sourceText """# 159. Test the Consumer

> **Pattern 159 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **159. Test the Consumer**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Test the Consumer**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Test the Consumer** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/159-159-test-the-consumer.rs`](../listings/159-159-test-the-consumer.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 159,
    title: "159. Test the Consumer",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 159);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **159. Test the Consumer**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-159` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-159-listing a book:Listing ;
    book:sourcePath "listings/159-159-test-the-consumer.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 159,
    title: "159. Test the Consumer",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 159);
}
""" .

book:chapter-159 book:listing book:chapter-159-listing .

# ── consumer-160.ttl ────────────────────────────────────────────
book:chapter-160 a book:Chapter ;
    book:number 160 ;
    book:title "160. Consume the Same Pack Twice" ;
    book:slug "two-consumers" ;
    book:part "consumer" ;
    book:sourcePath "consumer/160-two-consumers.md" ;
    book:sourceText """# 160. Consume the Same Pack Twice

> **Pattern 160 · The Consumer Place**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack has no standing until a real consumer syncs, compiles, runs, and tests its outputs. The narrow problem in this chapter is **160. Consume the Same Pack Twice**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Consume the Same Pack Twice**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Consume the Same Pack Twice** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/160-160-consume-the-same-pack-twice.rs`](../listings/160-160-consume-the-same-pack-twice.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 160,
    title: "160. Consume the Same Pack Twice",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 160);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

A zero-knowledge consumer can resolve, sync, build, run, test, verify, and replay the pack from declared inputs alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The consumer succeeds only with undocumented environment state, hand edits, private paths, or author intervention.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

examples/tcps-generated is the downstream customer that proves the generated module graph is mountable and executable.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **160. Consume the Same Pack Twice**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-160` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Consumer Place** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen.toml consumption`
- `sync`
- `build/test`
- `receipt replay`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `examples/affidavit-verify`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/affidavit-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Zero-knowledge standing is bounded to consumers that succeed from a clean checkout without author intervention.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-160-listing a book:Listing ;
    book:sourcePath "listings/160-160-consume-the-same-pack-twice.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 160,
    title: "160. Consume the Same Pack Twice",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 160);
}
""" .

book:chapter-160 book:listing book:chapter-160-listing .

# ── engine-201.ttl ──────────────────────────────────────────────
book:chapter-engine-201 a book:Chapter ;
    book:number "201"^^xsd:integer ;
    book:title "201. Pack-Shipped Gates Must Execute" ;
    book:slug "pack-shapes-execute" ;
    book:part "engine" ;
    book:sourcePath "engine/201-pack-shapes-execute.md" ;
    book:listing book:listing-engine-201 ;
    book:sourceText """# 201. Pack-Shipped Gates Must Execute

> **Pattern 201 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **201. Pack-Shipped Gates Must Execute**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack-Shipped Gates Must Execute**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack-Shipped Gates Must Execute** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/201-201-pack-shipped-shapes-must-execute.ttl`](../listings/201-201-pack-shipped-shapes-must-execute.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary. The listing keeps its historical `.ttl` filename in this book's listing tree; in a shipped pack this file lives at `gates/010_admission.rq`.

```sparql
# MESSAGE: 201. Pack-Shipped Gates Must Execute: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
```

### How the current admission mechanism works

Since the engine's SPARQL-gate migration, a pack no longer ships SHACL. Admission law is a `gates/` directory of `.rq` files next to `ontology.ttl`, evaluated in sorted filename order (hence the `010_`, `020_`, `030_` numbering convention) against the post-materialization union graph. Each gate file is an optional block of leading `# MESSAGE:` comment lines (the operator-facing violation text) followed by exactly one ASK or SELECT query: ASK returning `true` is a violation, and any SELECT row is a violation; a CONSTRUCT or DESCRIBE query is not a gate and refuses with `[FM-PACK-012]`. A missing `gates/` directory simply means no gates, but an unreadable directory or file is a typed `[FM-PACK-012]` refusal (fail closed), a gate violation refuses the sync with `[FM-PACK-013]`, and a legacy `shapes.ttl` still present in a pack is itself a loud `[FM-PACK-012]` refusal — never silently ignored. Project-level `[law].gates` mirrors this with `[FM-LAW-012]`/`[FM-LAW-013]`, and declaring the retired `[law].shapes` key refuses with a migration message. Every gate file is hashed into the receipt closure. Because gates are plain SPARQL evaluated through `GraphEngine::query`, the identical gate refuses the identical facts under both the GraphLaw and the Oxigraph engine — proven by `crates/ggen-engine/tests/reasoner_independence_e2e.rs`, which syncs the same pack byte-identically under both backends and receives the same `[FM-PACK-013]` on the same violating fixture. SHACL could not make that promise: `validate_shacl` on the Oxigraph backend is a typed `[FM-LAW-002]` refusal.

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **201. Pack-Shipped Gates Must Execute**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-201` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-201 a book:Listing ;
    book:sourcePath "listings/201-201-pack-shipped-shapes-must-execute.ttl" ;
    book:sourceText """# MESSAGE: 201. Pack-Shipped Gates Must Execute: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
""" .

# ── engine-202.ttl ──────────────────────────────────────────────
book:chapter-engine-202 a book:Chapter ;
    book:number "202"^^xsd:integer ;
    book:title "202. Fail-Closed Pack Admission" ;
    book:slug "fail-closed-pack-admission" ;
    book:part "engine" ;
    book:sourcePath "engine/202-fail-closed-pack-admission.md" ;
    book:listing book:listing-engine-202 ;
    book:sourceText """# 202. Fail-Closed Pack Admission

> **Pattern 202 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **202. Fail-Closed Pack Admission**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Fail-Closed Pack Admission**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Fail-Closed Pack Admission** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/202-202-fail-closed-pack-admission.rs`](../listings/202-202-fail-closed-pack-admission.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 202,
    title: "202. Fail-Closed Pack Admission",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 202);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **202. Fail-Closed Pack Admission**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-202` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-202 a book:Listing ;
    book:sourcePath "listings/202-202-fail-closed-pack-admission.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 202,
    title: \"202. Fail-Closed Pack Admission\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 202);
}
""" .

# ── engine-203.ttl ──────────────────────────────────────────────
book:chapter-engine-203 a book:Chapter ;
    book:number "203"^^xsd:integer ;
    book:title "203. Engine-Generated Module Aggregation" ;
    book:slug "engine-module-aggregation" ;
    book:part "engine" ;
    book:sourcePath "engine/203-engine-module-aggregation.md" ;
    book:listing book:listing-engine-203 ;
    book:sourceText """# 203. Engine-Generated Module Aggregation

> **Pattern 203 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **203. Engine-Generated Module Aggregation**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Engine-Generated Module Aggregation**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Engine-Generated Module Aggregation** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/203-203-engine-generated-module-aggregation.rs`](../listings/203-203-engine-generated-module-aggregation.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 203,
    title: "203. Engine-Generated Module Aggregation",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 203);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **203. Engine-Generated Module Aggregation**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-203` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-203 a book:Listing ;
    book:sourcePath "listings/203-203-engine-generated-module-aggregation.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 203,
    title: \"203. Engine-Generated Module Aggregation\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 203);
}
""" .

# ── engine-204.ttl ──────────────────────────────────────────────
book:chapter-engine-204 a book:Chapter ;
    book:number "204"^^xsd:integer ;
    book:title "204. Proof Gates in CI" ;
    book:slug "proof-gates-ci" ;
    book:part "engine" ;
    book:sourcePath "engine/204-proof-gates-ci.md" ;
    book:listing book:listing-engine-204 ;
    book:sourceText """# 204. Proof Gates in CI

> **Pattern 204 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **204. Proof Gates in CI**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Proof Gates in CI**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Proof Gates in CI** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/204-204-proof-gates-in-ci.yml`](../listings/204-204-proof-gates-in-ci.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-204-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "204. Proof Gates in CI"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **204. Proof Gates in CI**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-204` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-204 a book:Listing ;
    book:sourcePath "listings/204-204-proof-gates-in-ci.yml" ;
    book:sourceText """name: chapter-204-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py \"204. Proof Gates in CI\"
""" .

# ── engine-205.ttl ──────────────────────────────────────────────
book:chapter-engine-205 a book:Chapter ;
    book:number "205"^^xsd:integer ;
    book:title "205. Reflexive Receipts" ;
    book:slug "reflexive-receipts" ;
    book:part "engine" ;
    book:sourcePath "engine/205-reflexive-receipts.md" ;
    book:listing book:listing-engine-205 ;
    book:sourceText """# 205. Reflexive Receipts

> **Pattern 205 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **205. Reflexive Receipts**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reflexive Receipts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Reflexive Receipts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/205-205-reflexive-receipts.json`](../listings/205-205-reflexive-receipts.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 205,
  "subject": "205. Reflexive Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **205. Reflexive Receipts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-205` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-205 a book:Listing ;
    book:sourcePath "listings/205-205-reflexive-receipts.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 205,
  \"subject\": \"205. Reflexive Receipts\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── engine-206.ttl ──────────────────────────────────────────────
book:chapter-engine-206 a book:Chapter ;
    book:number "206"^^xsd:integer ;
    book:title "206. Generated-Output Ownership" ;
    book:slug "generated-output-ownership" ;
    book:part "engine" ;
    book:sourcePath "engine/206-generated-output-ownership.md" ;
    book:listing book:listing-engine-206 ;
    book:sourceText """# 206. Generated-Output Ownership

> **Pattern 206 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **206. Generated-Output Ownership**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generated-Output Ownership**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Generated-Output Ownership** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/206-206-generated-output-ownership.rs`](../listings/206-206-generated-output-ownership.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 206,
    title: "206. Generated-Output Ownership",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 206);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **206. Generated-Output Ownership**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-206` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-206 a book:Listing ;
    book:sourcePath "listings/206-206-generated-output-ownership.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 206,
    title: \"206. Generated-Output Ownership\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 206);
}
""" .

# ── engine-207.ttl ──────────────────────────────────────────────
book:chapter-engine-207 a book:Chapter ;
    book:number "207"^^xsd:integer ;
    book:title "207. Engine Error Codes" ;
    book:slug "engine-error-codes" ;
    book:part "engine" ;
    book:sourcePath "engine/207-engine-error-codes.md" ;
    book:listing book:listing-engine-207 ;
    book:sourceText """# 207. Engine Error Codes

> **Pattern 207 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **207. Engine Error Codes**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Engine Error Codes**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Engine Error Codes** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/207-207-engine-error-codes.rs`](../listings/207-207-engine-error-codes.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 207,
    title: "207. Engine Error Codes",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 207);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **207. Engine Error Codes**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-207` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-207 a book:Listing ;
    book:sourcePath "listings/207-207-engine-error-codes.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 207,
    title: \"207. Engine Error Codes\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 207);
}
""" .

# ── engine-208.ttl ──────────────────────────────────────────────
book:chapter-engine-208 a book:Chapter ;
    book:number "208"^^xsd:integer ;
    book:title "208. FM-PACK-012 and FM-PACK-013" ;
    book:slug "pack-shape-errors" ;
    book:part "engine" ;
    book:sourcePath "engine/208-pack-shape-errors.md" ;
    book:listing book:listing-engine-208 ;
    book:sourceText """# 208. FM-PACK-012 and FM-PACK-013

> **Pattern 208 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **208. FM-PACK-012 and FM-PACK-013**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **FM-PACK-012 and FM-PACK-013**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **FM-PACK-012 and FM-PACK-013** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Locate the correct abstraction layer.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Write an engine-level regression.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Prove no pack regression.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/208-208-fm-pack-012-and-fm-pack-013.rs`](../listings/208-208-fm-pack-012-and-fm-pack-013.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 208,
    title: "208. FM-PACK-012 and FM-PACK-013",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 208);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **208. FM-PACK-012 and FM-PACK-013**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-208` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-208 a book:Listing ;
    book:sourcePath "listings/208-208-fm-pack-012-and-fm-pack-013.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 208,
    title: \"208. FM-PACK-012 and FM-PACK-013\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 208);
}
""" .

# ── engine-209.ttl ──────────────────────────────────────────────
book:chapter-engine-209 a book:Chapter ;
    book:number "209"^^xsd:integer ;
    book:title "209. FM-WRITE-005 and FM-WRITE-008" ;
    book:slug "write-errors" ;
    book:part "engine" ;
    book:sourcePath "engine/209-write-errors.md" ;
    book:listing book:listing-engine-209 ;
    book:sourceText """# 209. FM-WRITE-005 and FM-WRITE-008

> **Pattern 209 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **209. FM-WRITE-005 and FM-WRITE-008**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **FM-WRITE-005 and FM-WRITE-008**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **FM-WRITE-005 and FM-WRITE-008** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Locate the correct abstraction layer.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Write an engine-level regression.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Prove no pack regression.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/209-209-fm-write-005-and-fm-write-008.rs`](../listings/209-209-fm-write-005-and-fm-write-008.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 209,
    title: "209. FM-WRITE-005 and FM-WRITE-008",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 209);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **209. FM-WRITE-005 and FM-WRITE-008**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-209` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-209 a book:Listing ;
    book:sourcePath "listings/209-209-fm-write-005-and-fm-write-008.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 209,
    title: \"209. FM-WRITE-005 and FM-WRITE-008\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 209);
}
""" .

# ── engine-210.ttl ──────────────────────────────────────────────
book:chapter-engine-210 a book:Chapter ;
    book:number "210"^^xsd:integer ;
    book:title "210. No Engine Change When Pack Law Is Enough" ;
    book:slug "no-engine-change" ;
    book:part "engine" ;
    book:sourcePath "engine/210-no-engine-change.md" ;
    book:listing book:listing-engine-210 ;
    book:sourceText """# 210. No Engine Change When Pack Law Is Enough

> **Pattern 210 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **210. No Engine Change When Pack Law Is Enough**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **No Engine Change When Pack Law Is Enough**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **No Engine Change When Pack Law Is Enough** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/210-210-no-engine-change-when-pack-law-is-enough.rs`](../listings/210-210-no-engine-change-when-pack-law-is-enough.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 210,
    title: "210. No Engine Change When Pack Law Is Enough",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 210);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **210. No Engine Change When Pack Law Is Enough**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-210` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-210 a book:Listing ;
    book:sourcePath "listings/210-210-no-engine-change-when-pack-law-is-enough.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 210,
    title: \"210. No Engine Change When Pack Law Is Enough\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 210);
}
""" .

# ── engine-211.ttl ──────────────────────────────────────────────
book:chapter-engine-211 a book:Chapter ;
    book:number "211"^^xsd:integer ;
    book:title "211. When Engine Work Is Unavoidable" ;
    book:slug "engine-work-unavoidable" ;
    book:part "engine" ;
    book:sourcePath "engine/211-engine-work-unavoidable.md" ;
    book:listing book:listing-engine-211 ;
    book:sourceText """# 211. When Engine Work Is Unavoidable

> **Pattern 211 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **211. When Engine Work Is Unavoidable**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **When Engine Work Is Unavoidable**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **When Engine Work Is Unavoidable** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/211-211-when-engine-work-is-unavoidable.rs`](../listings/211-211-when-engine-work-is-unavoidable.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 211,
    title: "211. When Engine Work Is Unavoidable",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 211);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **211. When Engine Work Is Unavoidable**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-211` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-211 a book:Listing ;
    book:sourcePath "listings/211-211-when-engine-work-is-unavoidable.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 211,
    title: \"211. When Engine Work Is Unavoidable\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 211);
}
""" .

# ── engine-212.ttl ──────────────────────────────────────────────
book:chapter-engine-212 a book:Chapter ;
    book:number "212"^^xsd:integer ;
    book:title "212. Prove the Engine Change against Multiple Packs" ;
    book:slug "engine-change-multipack-proof" ;
    book:part "engine" ;
    book:sourcePath "engine/212-engine-change-multipack-proof.md" ;
    book:listing book:listing-engine-212 ;
    book:sourceText """# 212. Prove the Engine Change against Multiple Packs

> **Pattern 212 · Change the Engine Only at the Boundary**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Some L5 gaps belong in the engine because pack-local work cannot safely solve global ownership or admission problems. The narrow problem in this chapter is **212. Prove the Engine Change against Multiple Packs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Prove the Engine Change against Multiple Packs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Prove the Engine Change against Multiple Packs.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-config`
- `ggen-cli-lib`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/212-212-prove-the-engine-change-against-multiple-packs.rs`](../listings/212-212-prove-the-engine-change-against-multiple-packs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 212,
    title: "212. Prove the Engine Change against Multiple Packs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 212);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Engine changes remain minimal, typed, multi-pack verified, and justified by semantics no pack can lawfully carry alone.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same outcome can be achieved through pack ontology, gates, queries, templates, or consumer wiring without engine modification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Pack-shipped gate execution, engine-owned module aggregation, and CI proof gates emerged from defects found by the TCPS and multi-pack consumers.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **212. Prove the Engine Change against Multiple Packs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-212` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Change the Engine Only at the Boundary** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `schema dispatch`
- `pack gate execution`
- `write semantics`
- `typed error codes`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Whether an engine change is necessary remains a case-specific design judgment that must be proven against multiple packs.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-engine-212 a book:Listing ;
    book:sourcePath "listings/212-212-prove-the-engine-change-against-multiple-packs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 212,
    title: \"212. Prove the Engine Change against Multiple Packs\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 212);
}
""" .

# ── foundations-001.ttl ─────────────────────────────────────────
book:chapter-foundations-001 a book:Chapter ;
    book:number "1"^^xsd:integer ;
    book:title "1. From Code Generation to Manufacturing" ;
    book:slug "manufacturing-not-codegen" ;
    book:part "foundations" ;
    book:sourcePath "foundations/001-manufacturing-not-codegen.md" ;
    book:sourceText """# 1. From Code Generation to Manufacturing

> **Pattern 1 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **1. From Code Generation to Manufacturing**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **From Code Generation to Manufacturing**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **From Code Generation to Manufacturing** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/001-1-from-code-generation-to-manufacturing.rs`](../listings/001-1-from-code-generation-to-manufacturing.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 1,
    title: "1. From Code Generation to Manufacturing",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 1);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **1. From Code Generation to Manufacturing**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-001` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-001 .

book:listing-foundations-001 a book:Listing ;
    book:sourcePath "listings/001-1-from-code-generation-to-manufacturing.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 1,
    title: "1. From Code Generation to Manufacturing",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 1);
}
""" .

# ── foundations-002.ttl ─────────────────────────────────────────
book:chapter-foundations-002 a book:Chapter ;
    book:number "2"^^xsd:integer ;
    book:title "2. The Artifact Equation" ;
    book:slug "artifact-equation" ;
    book:part "foundations" ;
    book:sourcePath "foundations/002-artifact-equation.md" ;
    book:sourceText """# 2. The Artifact Equation

> **Pattern 2 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **2. The Artifact Equation**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The Artifact Equation**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The Artifact Equation** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/002-2-the-artifact-equation.rs`](../listings/002-2-the-artifact-equation.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 2,
    title: "2. The Artifact Equation",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 2);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **2. The Artifact Equation**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-002` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-002 .

book:listing-foundations-002 a book:Listing ;
    book:sourcePath "listings/002-2-the-artifact-equation.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 2,
    title: "2. The Artifact Equation",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 2);
}
""" .

# ── foundations-003.ttl ─────────────────────────────────────────
book:chapter-foundations-003 a book:Chapter ;
    book:number "3"^^xsd:integer ;
    book:title "3. Ontology as Authority" ;
    book:slug "ontology-as-authority" ;
    book:part "foundations" ;
    book:sourcePath "foundations/003-ontology-as-authority.md" ;
    book:sourceText """# 3. Ontology as Authority

> **Pattern 3 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **3. Ontology as Authority**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Ontology as Authority**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Ontology as Authority** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/003-3-ontology-as-authority.ttl`](../listings/003-3-ontology-as-authority.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章3
    a pack:生成単位 ;
    dcterms:title "3. Ontology as Authority"@en ;
    pack:出力経路 "src/generated/003_3_ontology_as_authority.rs" ;
    pack:順序 3 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **3. Ontology as Authority**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-003` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-003 .

book:listing-foundations-003 a book:Listing ;
    book:sourcePath "listings/003-3-ontology-as-authority.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章3
    a pack:生成単位 ;
    dcterms:title "3. Ontology as Authority"@en ;
    pack:出力経路 "src/generated/003_3_ontology_as_authority.rs" ;
    pack:順序 3 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── foundations-004.ttl ─────────────────────────────────────────
book:chapter-foundations-004 a book:Chapter ;
    book:number "4"^^xsd:integer ;
    book:title "4. SPARQL as Construction Language" ;
    book:slug "sparql-as-construction-language" ;
    book:part "foundations" ;
    book:sourcePath "foundations/004-sparql-as-construction-language.md" ;
    book:sourceText """# 4. SPARQL as Construction Language

> **Pattern 4 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **4. SPARQL as Construction Language**. A query is a semantic interface. Its graph scope, ordering, optional values, and namespace filters are part of the public contract.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **SPARQL as Construction Language**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **SPARQL as Construction Language** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/004-4-sparql-as-construction-language.rq`](../listings/004-4-sparql-as-construction-language.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **4. SPARQL as Construction Language**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-004` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-004 .

book:listing-foundations-004 a book:Listing ;
    book:sourcePath "listings/004-4-sparql-as-construction-language.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
""" .

# ── foundations-005.ttl ─────────────────────────────────────────
book:chapter-foundations-005 a book:Chapter ;
    book:number "5"^^xsd:integer ;
    book:title "5. Tera as Rendering Surface" ;
    book:slug "tera-as-rendering-surface" ;
    book:part "foundations" ;
    book:sourcePath "foundations/005-tera-as-rendering-surface.md" ;
    book:sourceText """# 5. Tera as Rendering Surface

> **Pattern 5 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **5. Tera as Rendering Surface**. Template rendering is not clerical formatting. It is the final semantic projection before source text reaches the compiler.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Tera as Rendering Surface**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Tera as Rendering Surface** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/005-5-tera-as-rendering-surface.tmpl`](../listings/005-5-tera-as-rendering-surface.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/005_5_tera_as_rendering_surface.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章5 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 5: 5. Tera as Rendering Surface
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **5. Tera as Rendering Surface**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-005` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-005 .

book:listing-foundations-005 a book:Listing ;
    book:sourcePath "listings/005-5-tera-as-rendering-surface.tmpl" ;
    book:sourceText """---
to: src/generated/005_5_tera_as_rendering_surface.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章5 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 5: 5. Tera as Rendering Surface
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── foundations-006.ttl ─────────────────────────────────────────
book:chapter-foundations-006 a book:Chapter ;
    book:number "6"^^xsd:integer ;
    book:title "6. SPARQL Gates as Admission" ;
    book:slug "shacl-as-admission-gate" ;
    book:part "foundations" ;
    book:sourcePath "foundations/006-shacl-as-admission-gate.md" ;
    book:sourceText """# 6. SPARQL Gates as Admission

> **Pattern 6 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **6. SPARQL Gates as Admission**. The gate must reject malformed or incomplete pack data before a template can convert it into apparently valid source code.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **SPARQL Gates as Admission**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **SPARQL Gates as Admission** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/006-6-shacl-as-admission-gate.ttl`](../listings/006-6-shacl-as-admission-gate.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary. The listing keeps its historical `.ttl` filename in this book's listing tree; in a shipped pack this file lives at `gates/010_admission.rq`.

```sparql
# MESSAGE: 6. SPARQL Gates as Admission: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
```

### How the current admission mechanism works

Since the engine's SPARQL-gate migration, a pack no longer ships SHACL. Admission law is a `gates/` directory of `.rq` files next to `ontology.ttl`, evaluated in sorted filename order (hence the `010_`, `020_`, `030_` numbering convention) against the post-materialization union graph. Each gate file is an optional block of leading `# MESSAGE:` comment lines (the operator-facing violation text) followed by exactly one ASK or SELECT query: ASK returning `true` is a violation, and any SELECT row is a violation; a CONSTRUCT or DESCRIBE query is not a gate and refuses with `[FM-PACK-012]`. A missing `gates/` directory simply means no gates, but an unreadable directory or file is a typed `[FM-PACK-012]` refusal (fail closed), a gate violation refuses the sync with `[FM-PACK-013]`, and a legacy `shapes.ttl` still present in a pack is itself a loud `[FM-PACK-012]` refusal — never silently ignored. Project-level `[law].gates` mirrors this with `[FM-LAW-012]`/`[FM-LAW-013]`, and declaring the retired `[law].shapes` key refuses with a migration message. Every gate file is hashed into the receipt closure. Because gates are plain SPARQL evaluated through `GraphEngine::query`, the identical gate refuses the identical facts under both the GraphLaw and the Oxigraph engine — proven by `crates/ggen-engine/tests/reasoner_independence_e2e.rs`, which syncs the same pack byte-identically under both backends and receives the same `[FM-PACK-013]` on the same violating fixture. SHACL could not make that promise: `validate_shacl` on the Oxigraph backend is a typed `[FM-LAW-002]` refusal.

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **6. SPARQL Gates as Admission**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-006` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-006 .

book:listing-foundations-006 a book:Listing ;
    book:sourcePath "listings/006-6-shacl-as-admission-gate.ttl" ;
    book:sourceText """# MESSAGE: 6. SPARQL Gates as Admission: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
""" .

# ── foundations-007.ttl ─────────────────────────────────────────
book:chapter-foundations-007 a book:Chapter ;
    book:number "7"^^xsd:integer ;
    book:title "7. Cargo as Verification Boundary" ;
    book:slug "cargo-as-verification-boundary" ;
    book:part "foundations" ;
    book:sourcePath "foundations/007-cargo-as-verification-boundary.md" ;
    book:sourceText """# 7. Cargo as Verification Boundary

> **Pattern 7 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **7. Cargo as Verification Boundary**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Cargo as Verification Boundary**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Cargo as Verification Boundary** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/007-7-cargo-as-verification-boundary.sh`](../listings/007-7-cargo-as-verification-boundary.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="7. Cargo as Verification Boundary"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **7. Cargo as Verification Boundary**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-007` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-007 .

book:listing-foundations-007 a book:Listing ;
    book:sourcePath "listings/007-7-cargo-as-verification-boundary.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject="7. Cargo as Verification Boundary"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
""" .

# ── foundations-008.ttl ─────────────────────────────────────────
book:chapter-foundations-008 a book:Chapter ;
    book:number "8"^^xsd:integer ;
    book:title "8. Receipts as Lineage Proof" ;
    book:slug "receipts-as-lineage-proof" ;
    book:part "foundations" ;
    book:sourcePath "foundations/008-receipts-as-lineage-proof.md" ;
    book:sourceText """# 8. Receipts as Lineage Proof

> **Pattern 8 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **8. Receipts as Lineage Proof**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Receipts as Lineage Proof**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Receipts as Lineage Proof** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/008-8-receipts-as-lineage-proof.json`](../listings/008-8-receipts-as-lineage-proof.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 8,
  "subject": "8. Receipts as Lineage Proof",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **8. Receipts as Lineage Proof**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-008` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-008 .

book:listing-foundations-008 a book:Listing ;
    book:sourcePath "listings/008-8-receipts-as-lineage-proof.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 8,
  "subject": "8. Receipts as Lineage Proof",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

# ── foundations-009.ttl ─────────────────────────────────────────
book:chapter-foundations-009 a book:Chapter ;
    book:number "9"^^xsd:integer ;
    book:title "9. The Five-Stage Manufacturing Pipeline" ;
    book:slug "five-stage-pipeline" ;
    book:part "foundations" ;
    book:sourcePath "foundations/009-five-stage-pipeline.md" ;
    book:sourceText """# 9. The Five-Stage Manufacturing Pipeline

> **Pattern 9 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **9. The Five-Stage Manufacturing Pipeline**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The Five-Stage Manufacturing Pipeline**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The Five-Stage Manufacturing Pipeline** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/009-9-the-five-stage-manufacturing-pipeline.rs`](../listings/009-9-the-five-stage-manufacturing-pipeline.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 9,
    title: "9. The Five-Stage Manufacturing Pipeline",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 9);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **9. The Five-Stage Manufacturing Pipeline**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-009` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-009 .

book:listing-foundations-009 a book:Listing ;
    book:sourcePath "listings/009-9-the-five-stage-manufacturing-pipeline.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 9,
    title: "9. The Five-Stage Manufacturing Pipeline",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 9);
}
""" .

# ── foundations-010.ttl ─────────────────────────────────────────
book:chapter-foundations-010 a book:Chapter ;
    book:number "10"^^xsd:integer ;
    book:title "10. Load, Extract, Render, Verify, Emit" ;
    book:slug "load-extract-render-verify-emit" ;
    book:part "foundations" ;
    book:sourcePath "foundations/010-load-extract-render-verify-emit.md" ;
    book:sourceText """# 10. Load, Extract, Render, Verify, Emit

> **Pattern 10 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **10. Load, Extract, Render, Verify, Emit**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Load, Extract, Render, Verify, Emit**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Load, Extract, Render, Verify, Emit** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Separate source law from projection.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify each manufacturing station.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Attach a receipt to the transition.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/010-10-load-extract-render-verify-emit.rs`](../listings/010-10-load-extract-render-verify-emit.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 10,
    title: "10. Load, Extract, Render, Verify, Emit",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 10);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **10. Load, Extract, Render, Verify, Emit**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-010` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-010 .

book:listing-foundations-010 a book:Listing ;
    book:sourcePath "listings/010-10-load-extract-render-verify-emit.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 10,
    title: "10. Load, Extract, Render, Verify, Emit",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 10);
}
""" .

# ── foundations-011.ttl ─────────────────────────────────────────
book:chapter-foundations-011 a book:Chapter ;
    book:number "11"^^xsd:integer ;
    book:title "11. Source Law and Generated Projection" ;
    book:slug "source-law-generated-projection" ;
    book:part "foundations" ;
    book:sourcePath "foundations/011-source-law-generated-projection.md" ;
    book:sourceText """# 11. Source Law and Generated Projection

> **Pattern 11 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **11. Source Law and Generated Projection**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Source Law and Generated Projection**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Source Law and Generated Projection** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/011-11-source-law-and-generated-projection.tmpl`](../listings/011-11-source-law-and-generated-projection.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/011_11_source_law_and_generated_projection.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章11 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 11: 11. Source Law and Generated Projection
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **11. Source Law and Generated Projection**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-011` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-011 .

book:listing-foundations-011 a book:Listing ;
    book:sourcePath "listings/011-11-source-law-and-generated-projection.tmpl" ;
    book:sourceText """---
to: src/generated/011_11_source_law_and_generated_projection.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章11 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 11: 11. Source Law and Generated Projection
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── foundations-012.ttl ─────────────────────────────────────────
book:chapter-foundations-012 a book:Chapter ;
    book:number "12"^^xsd:integer ;
    book:title "12. Why Generated Files Are Not Authority" ;
    book:slug "generated-files-not-authority" ;
    book:part "foundations" ;
    book:sourcePath "foundations/012-generated-files-not-authority.md" ;
    book:sourceText """# 12. Why Generated Files Are Not Authority

> **Pattern 12 · The Whole Manufacturing System**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

ggen is most useful when treated as a manufacturing compiler rather than a text expander. The narrow problem in this chapter is **12. Why Generated Files Are Not Authority**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Why Generated Files Are Not Authority**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **Why Generated Files Are Not Authority** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/012-12-why-generated-files-are-not-authority.rs`](../listings/012-12-why-generated-files-are-not-authority.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 12,
    title: "12. Why Generated Files Are Not Authority",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 12);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is judged as a manufacturing system rather than as a directory of templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed artifact can be produced only by hand-editing output, bypassing admission, or omitting receipt verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

In TCPS, source law becomes admitted Rust, tests, release assets, and receipts without making generated files authoritative.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **12. Why Generated Files Are Not Authority**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-012` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Whole Manufacturing System** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `sync orchestration`
- `deterministic graph state`
- `bounded writes`
- `receipt emission`

### Current repository evidence

- `crates/ggen-engine/src/lib.rs`
- `crates/ggen-engine/src/verbs/sync.rs`
- `crates/ggen-graph/src/lib.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`

### Bounded standing

`PARTIAL`

### Open gap

The primitives are implemented, but no single pack witnesses every whole-system claim at Level Five.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-foundations-012 .

book:listing-foundations-012 a book:Listing ;
    book:sourcePath "listings/012-12-why-generated-files-are-not-authority.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 12,
    title: "12. Why Generated Files Are Not Authority",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 12);
}
""" .

# ── front-matter-audience.ttl ───────────────────────────────────
book:chapter-front-matter-audience a book:Chapter ;
    book:kind "front-matter" ;
    book:title "Who This Book Is For" ;
    book:slug "audience" ;
    book:part "front-matter" ;
    book:sourcePath "front-matter/audience.md" ;
    book:sourceText """# Who This Book Is For

> **Pattern reference · Using the Pattern Language**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five pack is a manufacturing system whose ontology, projections, proof, regeneration law, and receipts are sufficient to recreate and evolve a complete subsystem. The narrow problem in this chapter is **Who This Book Is For**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Who This Book Is For**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Who This Book Is For** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-x-who-this-book-is-for.rs`](../listings/appendix-x-who-this-book-is-for.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Who This Book Is For",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader enters the pattern language with a shared vocabulary, a bounded objective, and an executable definition of completion.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A reader can finish the section while still being unable to name the source law, consumer, verifier, receipt, or refusal condition.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS case study is continuous: every principle is later exercised against the Japanese Rust reference and the five-crate product release.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Who This Book Is For**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-who-this-book-is-for` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Using the Pattern Language** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-front-matter-audience .

book:listing-front-matter-audience a book:Listing ;
    book:sourcePath "listings/appendix-x-who-this-book-is-for.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "Who This Book Is For",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── front-matter-final-acceptance-test.ttl ──────────────────────
book:chapter-front-matter-final-acceptance-test a book:Chapter ;
    book:kind "front-matter" ;
    book:title "The Final Acceptance Test" ;
    book:slug "final-acceptance-test" ;
    book:part "front-matter" ;
    book:sourcePath "front-matter/final-acceptance-test.md" ;
    book:sourceText """# The Final Acceptance Test

> **Pattern reference · Using the Pattern Language**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five pack is a manufacturing system whose ontology, projections, proof, regeneration law, and receipts are sufficient to recreate and evolve a complete subsystem. The narrow problem in this chapter is **The Final Acceptance Test**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The Final Acceptance Test**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The Final Acceptance Test** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-x-the-final-acceptance-test.rs`](../listings/appendix-x-the-final-acceptance-test.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "The Final Acceptance Test",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader enters the pattern language with a shared vocabulary, a bounded objective, and an executable definition of completion.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A reader can finish the section while still being unable to name the source law, consumer, verifier, receipt, or refusal condition.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS case study is continuous: every principle is later exercised against the Japanese Rust reference and the five-crate product release.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **The Final Acceptance Test**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-the-final-acceptance-test` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Using the Pattern Language** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-front-matter-final-acceptance-test .

book:listing-front-matter-final-acceptance-test a book:Listing ;
    book:sourcePath "listings/appendix-x-the-final-acceptance-test.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "The Final Acceptance Test",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── front-matter-how-to-read.ttl ────────────────────────────────
book:chapter-front-matter-how-to-read a book:Chapter ;
    book:kind "front-matter" ;
    book:title "How to Read the Book" ;
    book:slug "how-to-read" ;
    book:part "front-matter" ;
    book:sourcePath "front-matter/how-to-read.md" ;
    book:sourceText """# How to Read the Book

> **Pattern reference · Using the Pattern Language**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five pack is a manufacturing system whose ontology, projections, proof, regeneration law, and receipts are sufficient to recreate and evolve a complete subsystem. The narrow problem in this chapter is **How to Read the Book**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **How to Read the Book**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **How to Read the Book** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-x-how-to-read-the-book.rs`](../listings/appendix-x-how-to-read-the-book.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "How to Read the Book",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader enters the pattern language with a shared vocabulary, a bounded objective, and an executable definition of completion.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A reader can finish the section while still being unable to name the source law, consumer, verifier, receipt, or refusal condition.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS case study is continuous: every principle is later exercised against the Japanese Rust reference and the five-crate product release.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **How to Read the Book**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-how-to-read-the-book` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Using the Pattern Language** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-front-matter-how-to-read .

book:listing-front-matter-how-to-read a book:Listing ;
    book:sourcePath "listings/appendix-x-how-to-read-the-book.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "How to Read the Book",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── front-matter-laboratory-requirements.ttl ────────────────────
book:chapter-front-matter-laboratory-requirements a book:Chapter ;
    book:kind "front-matter" ;
    book:title "Laboratory Requirements" ;
    book:slug "laboratory-requirements" ;
    book:part "front-matter" ;
    book:sourcePath "front-matter/laboratory-requirements.md" ;
    book:sourceText """# Laboratory Requirements

> **Pattern reference · Using the Pattern Language**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five pack is a manufacturing system whose ontology, projections, proof, regeneration law, and receipts are sufficient to recreate and evolve a complete subsystem. The narrow problem in this chapter is **Laboratory Requirements**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Laboratory Requirements**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Laboratory Requirements** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-x-laboratory-requirements.sh`](../listings/appendix-x-laboratory-requirements.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="Laboratory Requirements"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader enters the pattern language with a shared vocabulary, a bounded objective, and an executable definition of completion.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A reader can finish the section while still being unable to name the source law, consumer, verifier, receipt, or refusal condition.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS case study is continuous: every principle is later exercised against the Japanese Rust reference and the five-crate product release.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Laboratory Requirements**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-laboratory-requirements` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Using the Pattern Language** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-front-matter-laboratory-requirements .

book:listing-front-matter-laboratory-requirements a book:Listing ;
    book:sourcePath "listings/appendix-x-laboratory-requirements.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject="Laboratory Requirements"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
""" .

# ── front-matter-level-five-outcome.ttl ─────────────────────────
book:chapter-front-matter-level-five-outcome a book:Chapter ;
    book:kind "front-matter" ;
    book:title "What a Level Five Pack Produces" ;
    book:slug "level-five-outcome" ;
    book:part "front-matter" ;
    book:sourcePath "front-matter/level-five-outcome.md" ;
    book:sourceText """# What a Level Five Pack Produces

> **Pattern reference · Using the Pattern Language**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five pack is a manufacturing system whose ontology, projections, proof, regeneration law, and receipts are sufficient to recreate and evolve a complete subsystem. The narrow problem in this chapter is **What a Level Five Pack Produces**. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **What a Level Five Pack Produces**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **What a Level Five Pack Produces** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-x-what-a-level-five-pack-produces.rs`](../listings/appendix-x-what-a-level-five-pack-produces.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "What a Level Five Pack Produces",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader enters the pattern language with a shared vocabulary, a bounded objective, and an executable definition of completion.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A reader can finish the section while still being unable to name the source law, consumer, verifier, receipt, or refusal condition.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS case study is continuous: every principle is later exercised against the Japanese Rust reference and the five-crate product release.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **What a Level Five Pack Produces**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-what-a-level-five-pack-produces` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Using the Pattern Language** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-front-matter-level-five-outcome .

book:listing-front-matter-level-five-outcome a book:Listing ;
    book:sourcePath "listings/appendix-x-what-a-level-five-pack-produces.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "What a Level Five Pack Produces",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── front-matter-notation-and-standing.ttl ──────────────────────
book:chapter-front-matter-notation-and-standing a book:Chapter ;
    book:kind "front-matter" ;
    book:title "Notation and Standing Vocabulary" ;
    book:slug "notation-and-standing" ;
    book:part "front-matter" ;
    book:sourcePath "front-matter/notation-and-standing.md" ;
    book:sourceText """# Notation and Standing Vocabulary

> **Pattern reference · Using the Pattern Language**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five pack is a manufacturing system whose ontology, projections, proof, regeneration law, and receipts are sufficient to recreate and evolve a complete subsystem. The narrow problem in this chapter is **Notation and Standing Vocabulary**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Notation and Standing Vocabulary**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Notation and Standing Vocabulary** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-x-notation-and-standing-vocabulary.ttl`](../listings/appendix-x-notation-and-standing-vocabulary.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章0
    a pack:生成単位 ;
    dcterms:title "Notation and Standing Vocabulary"@en ;
    pack:出力経路 "src/generated/000_notation_and_standing_vocabulary.rs" ;
    pack:順序 0 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader enters the pattern language with a shared vocabulary, a bounded objective, and an executable definition of completion.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A reader can finish the section while still being unable to name the source law, consumer, verifier, receipt, or refusal condition.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS case study is continuous: every principle is later exercised against the Japanese Rust reference and the five-crate product release.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Notation and Standing Vocabulary**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-notation-and-standing-vocabulary` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Using the Pattern Language** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-front-matter-notation-and-standing .

book:listing-front-matter-notation-and-standing a book:Listing ;
    book:sourcePath "listings/appendix-x-notation-and-standing-vocabulary.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章0
    a pack:生成単位 ;
    dcterms:title "Notation and Standing Vocabulary"@en ;
    pack:出力経路 "src/generated/000_notation_and_standing_vocabulary.rs" ;
    pack:順序 0 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── front-matter-preface.ttl ────────────────────────────────────
book:chapter-front-matter-preface a book:Chapter ;
    book:kind "front-matter" ;
    book:title "Preface: Ontology Is the Production System" ;
    book:slug "preface" ;
    book:part "front-matter" ;
    book:sourcePath "front-matter/preface.md" ;
    book:sourceText """# Preface: Ontology Is the Production System

> **Pattern reference · Using the Pattern Language**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five pack is a manufacturing system whose ontology, projections, proof, regeneration law, and receipts are sufficient to recreate and evolve a complete subsystem. The narrow problem in this chapter is **Preface: Ontology Is the Production System**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Preface: Ontology Is the Production System**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Preface: Ontology Is the Production System** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-x-preface-ontology-is-the-production-system.ttl`](../listings/appendix-x-preface-ontology-is-the-production-system.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章0
    a pack:生成単位 ;
    dcterms:title "Preface: Ontology Is the Production System"@en ;
    pack:出力経路 "src/generated/000_preface_ontology_is_the_production_system.rs" ;
    pack:順序 0 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader enters the pattern language with a shared vocabulary, a bounded objective, and an executable definition of completion.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A reader can finish the section while still being unable to name the source law, consumer, verifier, receipt, or refusal condition.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS case study is continuous: every principle is later exercised against the Japanese Rust reference and the five-crate product release.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **Preface: Ontology Is the Production System**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-preface-ontology-is-the-production-system` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Using the Pattern Language** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-front-matter-preface .

book:listing-front-matter-preface a book:Listing ;
    book:sourcePath "listings/appendix-x-preface-ontology-is-the-production-system.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章0
    a pack:生成単位 ;
    dcterms:title "Preface: Ontology Is the Production System"@en ;
    pack:出力経路 "src/generated/000_preface_ontology_is_the_production_system.rs" ;
    pack:順序 0 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── front-matter-tcps-case-study.ttl ────────────────────────────
book:chapter-front-matter-tcps-case-study a book:Chapter ;
    book:kind "front-matter" ;
    book:title "The Continuous TCPS Case Study" ;
    book:slug "tcps-case-study" ;
    book:part "front-matter" ;
    book:sourcePath "front-matter/tcps-case-study.md" ;
    book:sourceText """# The Continuous TCPS Case Study

> **Pattern reference · Using the Pattern Language**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five pack is a manufacturing system whose ontology, projections, proof, regeneration law, and receipts are sufficient to recreate and evolve a complete subsystem. The narrow problem in this chapter is **The Continuous TCPS Case Study**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The Continuous TCPS Case Study**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The Continuous TCPS Case Study** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/appendix-x-the-continuous-tcps-case-study.rs`](../listings/appendix-x-the-continuous-tcps-case-study.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "The Continuous TCPS Case Study",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader enters the pattern language with a shared vocabulary, a bounded objective, and an executable definition of completion.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A reader can finish the section while still being unable to name the source law, consumer, verifier, receipt, or refusal condition.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS case study is continuous: every principle is later exercised against the Japanese Rust reference and the five-crate product release.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **The Continuous TCPS Case Study**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `appendix-the-continuous-tcps-case-study` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Using the Pattern Language** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-front-matter-tcps-case-study .

book:listing-front-matter-tcps-case-study a book:Listing ;
    book:sourcePath "listings/appendix-x-the-continuous-tcps-case-study.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 0,
    title: "The Continuous TCPS Case Study",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 0);
}
""" .

# ── generated-proof-135.ttl ─────────────────────────────────────
book:chapter-135 a book:Chapter ;
    book:number 135 ;
    book:title "Why Generated Code Needs Generated Proof" ;
    book:slug "code-needs-proof" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/135-code-needs-proof.md" ;
    book:sourceText """# 135. Why Generated Code Needs Generated Proof

> **Pattern 135 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **135. Why Generated Code Needs Generated Proof**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Why Generated Code Needs Generated Proof**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **Why Generated Code Needs Generated Proof** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/135-135-why-generated-code-needs-generated-proof.rs`](../listings/135-135-why-generated-code-needs-generated-proof.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 135,
    title: "135. Why Generated Code Needs Generated Proof",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 135);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **135. Why Generated Code Needs Generated Proof**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-135` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-135 .

book:listing-135 a book:Listing ;
    book:sourcePath "listings/135-135-why-generated-code-needs-generated-proof.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 135,
    title: \"135. Why Generated Code Needs Generated Proof\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 135);
}
""" .

# ── generated-proof-136.ttl ─────────────────────────────────────
book:chapter-136 a book:Chapter ;
    book:number 136 ;
    book:title "Avoid Tautological Tests" ;
    book:slug "avoid-tautology" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/136-avoid-tautology.md" ;
    book:sourceText """# 136. Avoid Tautological Tests

> **Pattern 136 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **136. Avoid Tautological Tests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Avoid Tautological Tests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Avoid Tautological Tests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/136-136-avoid-tautological-tests.rs`](../listings/136-136-avoid-tautological-tests.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 136,
    title: "136. Avoid Tautological Tests",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 136);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **136. Avoid Tautological Tests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-136` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-136 .

book:listing-136 a book:Listing ;
    book:sourcePath "listings/136-136-avoid-tautological-tests.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 136,
    title: \"136. Avoid Tautological Tests\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 136);
}
""" .

# ── generated-proof-137.ttl ─────────────────────────────────────
book:chapter-137 a book:Chapter ;
    book:number 137 ;
    book:title "Independent Expected Values" ;
    book:slug "independent-expected-values" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/137-independent-expected-values.md" ;
    book:sourceText """# 137. Independent Expected Values

> **Pattern 137 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **137. Independent Expected Values**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Independent Expected Values**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Independent Expected Values** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/137-137-independent-expected-values.rs`](../listings/137-137-independent-expected-values.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 137,
    title: "137. Independent Expected Values",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 137);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **137. Independent Expected Values**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-137` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-137 .

book:listing-137 a book:Listing ;
    book:sourcePath "listings/137-137-independent-expected-values.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 137,
    title: \"137. Independent Expected Values\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 137);
}
""" .

# ── generated-proof-138.ttl ─────────────────────────────────────
book:chapter-138 a book:Chapter ;
    book:number 138 ;
    book:title "Chicago TDD" ;
    book:slug "chicago-tdd" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/138-chicago-tdd.md" ;
    book:sourceText """# 138. Chicago TDD

> **Pattern 138 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **138. Chicago TDD**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Chicago TDD**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Chicago TDD** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/138-138-chicago-tdd.rs`](../listings/138-138-chicago-tdd.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 138,
    title: "138. Chicago TDD",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 138);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **138. Chicago TDD**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-138` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-138 .

book:listing-138 a book:Listing ;
    book:sourcePath "listings/138-138-chicago-tdd.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 138,
    title: \"138. Chicago TDD\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 138);
}
""" .

# ── generated-proof-139.ttl ─────────────────────────────────────
book:chapter-139 a book:Chapter ;
    book:number 139 ;
    book:title "Compile-Time Proof through Signatures" ;
    book:slug "compile-time-signature-proof" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/139-compile-time-signature-proof.md" ;
    book:sourceText """# 139. Compile-Time Proof through Signatures

> **Pattern 139 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **139. Compile-Time Proof through Signatures**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Compile-Time Proof through Signatures**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Compile-Time Proof through Signatures** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/139-139-compile-time-proof-through-signatures.rs`](../listings/139-139-compile-time-proof-through-signatures.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 139,
    title: "139. Compile-Time Proof through Signatures",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 139);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **139. Compile-Time Proof through Signatures**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-139` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-139 .

book:listing-139 a book:Listing ;
    book:sourcePath "listings/139-139-compile-time-proof-through-signatures.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 139,
    title: \"139. Compile-Time Proof through Signatures\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 139);
}
""" .

# ── generated-proof-140.ttl ─────────────────────────────────────
book:chapter-140 a book:Chapter ;
    book:number 140 ;
    book:title "Runtime Behavioral Proof" ;
    book:slug "runtime-behavioral-proof" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/140-runtime-behavioral-proof.md" ;
    book:sourceText """# 140. Runtime Behavioral Proof

> **Pattern 140 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **140. Runtime Behavioral Proof**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Runtime Behavioral Proof**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Runtime Behavioral Proof** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/140-140-runtime-behavioral-proof.rs`](../listings/140-140-runtime-behavioral-proof.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 140,
    title: "140. Runtime Behavioral Proof",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 140);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **140. Runtime Behavioral Proof**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-140` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-140 .

book:listing-140 a book:Listing ;
    book:sourcePath "listings/140-140-runtime-behavioral-proof.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 140,
    title: \"140. Runtime Behavioral Proof\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 140);
}
""" .

# ── generated-proof-141.ttl ─────────────────────────────────────
book:chapter-141 a book:Chapter ;
    book:number 141 ;
    book:title "Verbatim Reference Tests" ;
    book:slug "verbatim-reference-tests" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/141-verbatim-reference-tests.md" ;
    book:sourceText """# 141. Verbatim Reference Tests

> **Pattern 141 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **141. Verbatim Reference Tests**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Verbatim Reference Tests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Verbatim Reference Tests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/141-141-verbatim-reference-tests.rs`](../listings/141-141-verbatim-reference-tests.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 141,
    title: "141. Verbatim Reference Tests",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 141);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **141. Verbatim Reference Tests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-141` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-141 .

book:listing-141 a book:Listing ;
    book:sourcePath "listings/141-141-verbatim-reference-tests.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 141,
    title: \"141. Verbatim Reference Tests\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 141);
}
""" .

# ── generated-proof-142.ttl ─────────────────────────────────────
book:chapter-142 a book:Chapter ;
    book:number 142 ;
    book:title "Unit Tests in Generated Modules" ;
    book:slug "unit-tests-in-generated-modules" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/142-unit-tests-in-generated-modules.md" ;
    book:sourceText """# 142. Unit Tests in Generated Modules

> **Pattern 142 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **142. Unit Tests in Generated Modules**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Unit Tests in Generated Modules**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Unit Tests in Generated Modules** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Mount an independent expectation.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Exercise real behavior.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Prove the proof can fail.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/142-142-unit-tests-in-generated-modules.rs`](../listings/142-142-unit-tests-in-generated-modules.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 142,
    title: "142. Unit Tests in Generated Modules",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 142);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **142. Unit Tests in Generated Modules**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-142` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-142 .

book:listing-142 a book:Listing ;
    book:sourcePath "listings/142-142-unit-tests-in-generated-modules.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 142,
    title: \"142. Unit Tests in Generated Modules\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 142);
}
""" .

# ── generated-proof-143.ttl ─────────────────────────────────────
book:chapter-143 a book:Chapter ;
    book:number 143 ;
    book:title "Integration Tests in Real Consumers" ;
    book:slug "integration-tests-real-consumers" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/143-integration-tests-real-consumers.md" ;
    book:sourceText """# 143. Integration Tests in Real Consumers

> **Pattern 143 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **143. Integration Tests in Real Consumers**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Integration Tests in Real Consumers**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Integration Tests in Real Consumers** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/143-143-integration-tests-in-real-consumers.rs`](../listings/143-143-integration-tests-in-real-consumers.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 143,
    title: "143. Integration Tests in Real Consumers",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 143);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **143. Integration Tests in Real Consumers**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-143` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-143 .

book:listing-143 a book:Listing ;
    book:sourcePath "listings/143-143-integration-tests-in-real-consumers.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 143,
    title: \"143. Integration Tests in Real Consumers\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 143);
}
""" .

# ── generated-proof-144.ttl ─────────────────────────────────────
book:chapter-144 a book:Chapter ;
    book:number 144 ;
    book:title "Proof Templates" ;
    book:slug "proof-templates" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/144-proof-templates.md" ;
    book:sourceText """# 144. Proof Templates

> **Pattern 144 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **144. Proof Templates**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Proof Templates**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Proof Templates** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Mount an independent expectation.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Exercise real behavior.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Prove the proof can fail.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/144-144-proof-templates.tmpl`](../listings/144-144-proof-templates.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/144_144_proof_templates.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章144 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 144: 144. Proof Templates
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **144. Proof Templates**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-144` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-144 .

book:listing-144 a book:Listing ;
    book:sourcePath "listings/144-144-proof-templates.tmpl" ;
    book:sourceText """---
to: src/generated/144_144_proof_templates.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章144 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 144: 144. Proof Templates
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── generated-proof-145.ttl ─────────────────────────────────────
book:chapter-145 a book:Chapter ;
    book:number 145 ;
    book:title "First-Sync Green" ;
    book:slug "first-sync-green" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/145-first-sync-green.md" ;
    book:sourceText """# 145. First-Sync Green

> **Pattern 145 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **145. First-Sync Green**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **First-Sync Green**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **First-Sync Green** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/145-145-first-sync-green.rs`](../listings/145-145-first-sync-green.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 145,
    title: "145. First-Sync Green",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 145);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **145. First-Sync Green**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-145` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-145 .

book:listing-145 a book:Listing ;
    book:sourcePath "listings/145-145-first-sync-green.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 145,
    title: \"145. First-Sync Green\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 145);
}
""" .

# ── generated-proof-146.ttl ─────────────────────────────────────
book:chapter-146 a book:Chapter ;
    book:number 146 ;
    book:title "Proof Failure as Jidoka" ;
    book:slug "proof-failure-jidoka" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/146-proof-failure-jidoka.md" ;
    book:sourceText """# 146. Proof Failure as Jidoka

> **Pattern 146 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **146. Proof Failure as Jidoka**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Proof Failure as Jidoka**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Proof Failure as Jidoka** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/146-146-proof-failure-as-jidoka.rs`](../listings/146-146-proof-failure-as-jidoka.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 146,
    title: "146. Proof Failure as Jidoka",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 146);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **146. Proof Failure as Jidoka**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-146` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-146 .

book:listing-146 a book:Listing ;
    book:sourcePath "listings/146-146-proof-failure-as-jidoka.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 146,
    title: \"146. Proof Failure as Jidoka\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 146);
}
""" .

# ── generated-proof-147.ttl ─────────────────────────────────────
book:chapter-147 a book:Chapter ;
    book:number 147 ;
    book:title "Mutation Tests" ;
    book:slug "mutation-tests" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/147-mutation-tests.md" ;
    book:sourceText """# 147. Mutation Tests

> **Pattern 147 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **147. Mutation Tests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Mutation Tests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Mutation Tests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/147-147-mutation-tests.rs`](../listings/147-147-mutation-tests.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 147,
    title: "147. Mutation Tests",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 147);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **147. Mutation Tests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-147` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-147 .

book:listing-147 a book:Listing ;
    book:sourcePath "listings/147-147-mutation-tests.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 147,
    title: \"147. Mutation Tests\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 147);
}
""" .

# ── generated-proof-148.ttl ─────────────────────────────────────
book:chapter-148 a book:Chapter ;
    book:number 148 ;
    book:title "Proof Receipts" ;
    book:slug "proof-receipts" ;
    book:part "generated-proof" ;
    book:sourcePath "generated-proof/148-proof-receipts.md" ;
    book:sourceText """# 148. Proof Receipts

> **Pattern 148 · Proof beside Product**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Proof must be generated or mounted beside the product and must fail when the product is wrong. The narrow problem in this chapter is **148. Proof Receipts**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Proof Receipts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Proof Receipts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `chicago-tdd-tools`
- `ggen-cheat-scanner`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/148-148-proof-receipts.json`](../listings/148-148-proof-receipts.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 148,
  "subject": "148. Proof Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Independent compile-time and runtime checks fail when the manufactured subject is meaningfully corrupted.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The proof remains green after mutating the generated behavior, deleting an output, or replacing expected values with tautologies.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The headline gate is the original 試験.rs running byte-for-byte unmodified against generated code and passing 14 of 14 tests.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **148. Proof Receipts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-148` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Proof beside Product** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `generated proof surfaces`
- `independent expectations`
- `mutation failure`
- `verification facts`

### Current repository evidence

- `examples/star-toml-verify`
- `examples/lsp-max-verify`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

A generated proof is not independent merely because it is emitted into a separate file.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-148 .

book:listing-148 a book:Listing ;
    book:sourcePath "listings/148-148-proof-receipts.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 148,
  \"subject\": \"148. Proof Receipts\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── level-five-design-213.ttl ───────────────────────────────────
book:chapter-level-five-design-213 a book:Chapter ;
    book:number "213"^^xsd:integer ;
    book:title "213. Write the ARD and PRD" ;
    book:slug "write-ard-prd" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/213-write-ard-prd.md" ;
    book:listing book:listing-level-five-design-213 ;
    book:sourceText """# 213. Write the ARD and PRD

> **Pattern 213 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **213. Write the ARD and PRD**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Write the ARD and PRD**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Write the ARD and PRD.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/213-213-write-the-ard-and-prd.rs`](../listings/213-213-write-the-ard-and-prd.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 213,
    title: "213. Write the ARD and PRD",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 213);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **213. Write the ARD and PRD**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-213` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-213 a book:Listing ;
    book:sourcePath "listings/213-213-write-the-ard-and-prd.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 213,
    title: \"213. Write the ARD and PRD\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 213);
}
""" .

# ── level-five-design-214.ttl ───────────────────────────────────
book:chapter-level-five-design-214 a book:Chapter ;
    book:number "214"^^xsd:integer ;
    book:title "214. Define the Complete Product Surface" ;
    book:slug "complete-product-surface" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/214-complete-product-surface.md" ;
    book:listing book:listing-level-five-design-214 ;
    book:sourceText """# 214. Define the Complete Product Surface

> **Pattern 214 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **214. Define the Complete Product Surface**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Define the Complete Product Surface**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Define the Complete Product Surface.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/214-214-define-the-complete-product-surface.rs`](../listings/214-214-define-the-complete-product-surface.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 214,
    title: "214. Define the Complete Product Surface",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 214);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **214. Define the Complete Product Surface**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-214` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-214 a book:Listing ;
    book:sourcePath "listings/214-214-define-the-complete-product-surface.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 214,
    title: \"214. Define the Complete Product Surface\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 214);
}
""" .

# ── level-five-design-215.ttl ───────────────────────────────────
book:chapter-level-five-design-215 a book:Chapter ;
    book:number "215"^^xsd:integer ;
    book:title "215. Build the Ontology-to-Module Matrix" ;
    book:slug "ontology-module-matrix" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/215-ontology-module-matrix.md" ;
    book:listing book:listing-level-five-design-215 ;
    book:sourceText """# 215. Build the Ontology-to-Module Matrix

> **Pattern 215 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **215. Build the Ontology-to-Module Matrix**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Build the Ontology-to-Module Matrix**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Build the Ontology-to-Module Matrix.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/215-215-build-the-ontology-to-module-matrix.ttl`](../listings/215-215-build-the-ontology-to-module-matrix.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章215
    a pack:生成単位 ;
    dcterms:title "215. Build the Ontology-to-Module Matrix"@en ;
    pack:出力経路 "src/generated/215_215_build_the_ontology_to_module_matrix.rs" ;
    pack:順序 215 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **215. Build the Ontology-to-Module Matrix**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-215` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-215 a book:Listing ;
    book:sourcePath "listings/215-215-build-the-ontology-to-module-matrix.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章215
    a pack:生成単位 ;
    dcterms:title \"215. Build the Ontology-to-Module Matrix\"@en ;
    pack:出力経路 \"src/generated/215_215_build_the_ontology_to_module_matrix.rs\" ;
    pack:順序 215 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── level-five-design-216.ttl ───────────────────────────────────
book:chapter-level-five-design-216 a book:Chapter ;
    book:number "216"^^xsd:integer ;
    book:title "216. Build the Dimension-to-Acceptance Matrix" ;
    book:slug "dimension-acceptance-matrix" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/216-dimension-acceptance-matrix.md" ;
    book:listing book:listing-level-five-design-216 ;
    book:sourceText """# 216. Build the Dimension-to-Acceptance Matrix

> **Pattern 216 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **216. Build the Dimension-to-Acceptance Matrix**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Build the Dimension-to-Acceptance Matrix**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Build the Dimension-to-Acceptance Matrix.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/216-216-build-the-dimension-to-acceptance-matrix.json`](../listings/216-216-build-the-dimension-to-acceptance-matrix.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 216,
  "subject": "216. Build the Dimension-to-Acceptance Matrix",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **216. Build the Dimension-to-Acceptance Matrix**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-216` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-216 a book:Listing ;
    book:sourcePath "listings/216-216-build-the-dimension-to-acceptance-matrix.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 216,
  \"subject\": \"216. Build the Dimension-to-Acceptance Matrix\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── level-five-design-217.ttl ───────────────────────────────────
book:chapter-level-five-design-217 a book:Chapter ;
    book:number "217"^^xsd:integer ;
    book:title "217. Name Every Acceptance Artifact" ;
    book:slug "name-every-artifact" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/217-name-every-artifact.md" ;
    book:listing book:listing-level-five-design-217 ;
    book:sourceText """# 217. Name Every Acceptance Artifact

> **Pattern 217 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **217. Name Every Acceptance Artifact**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Name Every Acceptance Artifact**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Name Every Acceptance Artifact** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/217-217-name-every-acceptance-artifact.rs`](../listings/217-217-name-every-acceptance-artifact.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 217,
    title: "217. Name Every Acceptance Artifact",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 217);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **217. Name Every Acceptance Artifact**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-217` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-217 a book:Listing ;
    book:sourcePath "listings/217-217-name-every-acceptance-artifact.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 217,
    title: \"217. Name Every Acceptance Artifact\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 217);
}
""" .

# ── level-five-design-218.ttl ───────────────────────────────────
book:chapter-level-five-design-218 a book:Chapter ;
    book:number "218"^^xsd:integer ;
    book:title "218. Define the Reference Oracle" ;
    book:slug "define-reference-oracle" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/218-define-reference-oracle.md" ;
    book:listing book:listing-level-five-design-218 ;
    book:sourceText """# 218. Define the Reference Oracle

> **Pattern 218 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **218. Define the Reference Oracle**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Define the Reference Oracle**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Define the Reference Oracle.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/218-218-define-the-reference-oracle.rs`](../listings/218-218-define-the-reference-oracle.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 218,
    title: "218. Define the Reference Oracle",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 218);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **218. Define the Reference Oracle**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-218` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-218 a book:Listing ;
    book:sourcePath "listings/218-218-define-the-reference-oracle.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 218,
    title: \"218. Define the Reference Oracle\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 218);
}
""" .

# ── level-five-design-219.ttl ───────────────────────────────────
book:chapter-level-five-design-219 a book:Chapter ;
    book:number "219"^^xsd:integer ;
    book:title "219. Define the Consumer" ;
    book:slug "define-consumer" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/219-define-consumer.md" ;
    book:listing book:listing-level-five-design-219 ;
    book:sourceText """# 219. Define the Consumer

> **Pattern 219 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **219. Define the Consumer**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Define the Consumer**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Define the Consumer.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/219-219-define-the-consumer.rs`](../listings/219-219-define-the-consumer.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 219,
    title: "219. Define the Consumer",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 219);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **219. Define the Consumer**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-219` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-219 a book:Listing ;
    book:sourcePath "listings/219-219-define-the-consumer.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 219,
    title: \"219. Define the Consumer\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 219);
}
""" .

# ── level-five-design-220.ttl ───────────────────────────────────
book:chapter-level-five-design-220 a book:Chapter ;
    book:number "220"^^xsd:integer ;
    book:title "220. Define the Proof Suite" ;
    book:slug "define-proof-suite" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/220-define-proof-suite.md" ;
    book:listing book:listing-level-five-design-220 ;
    book:sourceText """# 220. Define the Proof Suite

> **Pattern 220 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **220. Define the Proof Suite**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Define the Proof Suite**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Define the Proof Suite.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/220-220-define-the-proof-suite.rs`](../listings/220-220-define-the-proof-suite.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 220,
    title: "220. Define the Proof Suite",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 220);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **220. Define the Proof Suite**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-220` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-220 a book:Listing ;
    book:sourcePath "listings/220-220-define-the-proof-suite.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 220,
    title: \"220. Define the Proof Suite\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 220);
}
""" .

# ── level-five-design-221.ttl ───────────────────────────────────
book:chapter-level-five-design-221 a book:Chapter ;
    book:number "221"^^xsd:integer ;
    book:title "221. Define the Drift Policy" ;
    book:slug "define-drift-policy" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/221-define-drift-policy.md" ;
    book:listing book:listing-level-five-design-221 ;
    book:sourceText """# 221. Define the Drift Policy

> **Pattern 221 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **221. Define the Drift Policy**. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Define the Drift Policy**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Define the Drift Policy.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/221-221-define-the-drift-policy.sh`](../listings/221-221-define-the-drift-policy.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="221. Define the Drift Policy"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **221. Define the Drift Policy**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-221` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-221 a book:Listing ;
    book:sourcePath "listings/221-221-define-the-drift-policy.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"221. Define the Drift Policy\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── level-five-design-222.ttl ───────────────────────────────────
book:chapter-level-five-design-222 a book:Chapter ;
    book:number "222"^^xsd:integer ;
    book:title "222. Define the Receipt Schema" ;
    book:slug "define-receipt-schema" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/222-define-receipt-schema.md" ;
    book:listing book:listing-level-five-design-222 ;
    book:sourceText """# 222. Define the Receipt Schema

> **Pattern 222 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **222. Define the Receipt Schema**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Define the Receipt Schema**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Define the Receipt Schema.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/222-222-define-the-receipt-schema.json`](../listings/222-222-define-the-receipt-schema.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 222,
  "subject": "222. Define the Receipt Schema",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **222. Define the Receipt Schema**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-222` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-222 a book:Listing ;
    book:sourcePath "listings/222-222-define-the-receipt-schema.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 222,
  \"subject\": \"222. Define the Receipt Schema\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── level-five-design-223.ttl ───────────────────────────────────
book:chapter-level-five-design-223 a book:Chapter ;
    book:number "223"^^xsd:integer ;
    book:title "223. Define the Release Surface" ;
    book:slug "define-release-surface" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/223-define-release-surface.md" ;
    book:listing book:listing-level-five-design-223 ;
    book:sourceText """# 223. Define the Release Surface

> **Pattern 223 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **223. Define the Release Surface**. Release scaffolding is part of the product whenever downstream users require it to build, verify, package, or attest the subsystem.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Define the Release Surface**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Define the Release Surface.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/223-223-define-the-release-surface.rs`](../listings/223-223-define-the-release-surface.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 223,
    title: "223. Define the Release Surface",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 223);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **223. Define the Release Surface**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-223` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-223 a book:Listing ;
    book:sourcePath "listings/223-223-define-the-release-surface.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 223,
    title: \"223. Define the Release Surface\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 223);
}
""" .

# ── level-five-design-224.ttl ───────────────────────────────────
book:chapter-level-five-design-224 a book:Chapter ;
    book:number "224"^^xsd:integer ;
    book:title "224. Define the Level Five Definition of Done" ;
    book:slug "level-five-dod" ;
    book:part "level-five-design" ;
    book:sourcePath "level-five-design/224-level-five-dod.md" ;
    book:listing book:listing-level-five-design-224 ;
    book:sourceText """# 224. Define the Level Five Definition of Done

> **Pattern 224 · The Level Five Sequence**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A Level Five claim must be designed as a set of named acceptance artifacts before implementation begins. The narrow problem in this chapter is **224. Define the Level Five Definition of Done**. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Define the Level Five Definition of Done**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Define the Level Five Definition of Done.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-engine`
- `ggen-marketplace`
- `ggen-graph`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/224-224-define-the-level-five-definition-of-done.rs`](../listings/224-224-define-the-level-five-definition-of-done.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 224,
    title: "224. Define the Level Five Definition of Done",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 224);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every product artifact, oracle, proof, drift rule, receipt, and release surface is named before irreversible implementation.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A required artifact or acceptance consequence appears during implementation without an owner or matrix cell.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS uses an ontology-to-module matrix and a dimension-to-acceptance matrix so every claim resolves to a file, test, receipt, or refusal fixture.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **224. Define the Level Five Definition of Done**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-224` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Level Five Sequence** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `product inventory`
- `ontology-to-artifact matrices`
- `acceptance matrices`
- `definition of done`

### Current repository evidence

- `TCPS-PACK-ARD-PRD.md`
- `book/code/packs/canonical-level-five-pack`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

The planning artifacts exist; current-head replay is required before crown standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-level-five-design-224 a book:Listing ;
    book:sourcePath "listings/224-224-define-the-level-five-definition-of-done.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 224,
    title: \"224. Define the Level Five Definition of Done\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 224);
}
""" .

# ── maturity-037.ttl ────────────────────────────────────────────
book:chapter-037 a book:Chapter ;
    book:number 37 ;
    book:title "37. L0: Inert" ;
    book:slug "l0-inert" ;
    book:part "maturity" ;
    book:sourcePath "maturity/037-l0-inert.md" ;
    book:sourceText """# 37. L0: Inert

> **Pattern 37 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **37. L0: Inert**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **L0: Inert**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **L0: Inert** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/037-37-l0-inert.rs`](../listings/037-37-l0-inert.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 37,
    title: "37. L0: Inert",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 37);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **37. L0: Inert**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-037` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-037-listing a book:Listing ;
    book:sourcePath "listings/037-37-l0-inert.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 37,
    title: "37. L0: Inert",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 37);
}
""" .

book:chapter-037 book:listing book:chapter-037-listing .

# ── maturity-038.ttl ────────────────────────────────────────────
book:chapter-038 a book:Chapter ;
    book:number 38 ;
    book:title "38. L1: Syntactically Valid" ;
    book:slug "l1-syntactically-valid" ;
    book:part "maturity" ;
    book:sourcePath "maturity/038-l1-syntactically-valid.md" ;
    book:sourceText """# 38. L1: Syntactically Valid

> **Pattern 38 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **38. L1: Syntactically Valid**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **L1: Syntactically Valid**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **L1: Syntactically Valid** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/038-38-l1-syntactically-valid.rs`](../listings/038-38-l1-syntactically-valid.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 38,
    title: "38. L1: Syntactically Valid",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 38);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **38. L1: Syntactically Valid**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-038` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-038-listing a book:Listing ;
    book:sourcePath "listings/038-38-l1-syntactically-valid.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 38,
    title: "38. L1: Syntactically Valid",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 38);
}
""" .

book:chapter-038 book:listing book:chapter-038-listing .

# ── maturity-039.ttl ────────────────────────────────────────────
book:chapter-039 a book:Chapter ;
    book:number 39 ;
    book:title "39. L2: Consumed" ;
    book:slug "l2-consumed" ;
    book:part "maturity" ;
    book:sourcePath "maturity/039-l2-consumed.md" ;
    book:sourceText """# 39. L2: Consumed

> **Pattern 39 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **39. L2: Consumed**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **L2: Consumed**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **L2: Consumed** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/039-39-l2-consumed.rs`](../listings/039-39-l2-consumed.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 39,
    title: "39. L2: Consumed",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 39);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **39. L2: Consumed**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-039` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-039-listing a book:Listing ;
    book:sourcePath "listings/039-39-l2-consumed.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 39,
    title: "39. L2: Consumed",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 39);
}
""" .

book:chapter-039 book:listing book:chapter-039-listing .

# ── maturity-040.ttl ────────────────────────────────────────────
book:chapter-040 a book:Chapter ;
    book:number 40 ;
    book:title "40. L3: Verified" ;
    book:slug "l3-verified" ;
    book:part "maturity" ;
    book:sourcePath "maturity/040-l3-verified.md" ;
    book:sourceText """# 40. L3: Verified

> **Pattern 40 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **40. L3: Verified**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **L3: Verified**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **L3: Verified** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/040-40-l3-verified.rs`](../listings/040-40-l3-verified.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 40,
    title: "40. L3: Verified",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 40);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **40. L3: Verified**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-040` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-040-listing a book:Listing ;
    book:sourcePath "listings/040-40-l3-verified.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 40,
    title: "40. L3: Verified",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 40);
}
""" .

book:chapter-040 book:listing book:chapter-040-listing .

# ── maturity-041.ttl ────────────────────────────────────────────
book:chapter-041 a book:Chapter ;
    book:number 41 ;
    book:title "41. L4: Hardened" ;
    book:slug "l4-hardened" ;
    book:part "maturity" ;
    book:sourcePath "maturity/041-l4-hardened.md" ;
    book:sourceText """# 41. L4: Hardened

> **Pattern 41 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **41. L4: Hardened**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **L4: Hardened**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **L4: Hardened** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/041-41-l4-hardened.rs`](../listings/041-41-l4-hardened.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 41,
    title: "41. L4: Hardened",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 41);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **41. L4: Hardened**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-041` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-041-listing a book:Listing ;
    book:sourcePath "listings/041-41-l4-hardened.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 41,
    title: "41. L4: Hardened",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 41);
}
""" .

book:chapter-041 book:listing book:chapter-041-listing .

# ── maturity-042.ttl ────────────────────────────────────────────
book:chapter-042 a book:Chapter ;
    book:number 42 ;
    book:title "42. L5: Complete Substitute" ;
    book:slug "l5-complete-substitute" ;
    book:part "maturity" ;
    book:sourcePath "maturity/042-l5-complete-substitute.md" ;
    book:sourceText """# 42. L5: Complete Substitute

> **Pattern 42 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **42. L5: Complete Substitute**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **L5: Complete Substitute**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **L5: Complete Substitute** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/042-42-l5-complete-substitute.rs`](../listings/042-42-l5-complete-substitute.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 42,
    title: "42. L5: Complete Substitute",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 42);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **42. L5: Complete Substitute**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-042` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-042-listing a book:Listing ;
    book:sourcePath "listings/042-42-l5-complete-substitute.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 42,
    title: "42. L5: Complete Substitute",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 42);
}
""" .

book:chapter-042 book:listing book:chapter-042-listing .

# ── maturity-043.ttl ────────────────────────────────────────────
book:chapter-043 a book:Chapter ;
    book:number 43 ;
    book:title "43. Generation Depth" ;
    book:slug "generation-depth" ;
    book:part "maturity" ;
    book:sourcePath "maturity/043-generation-depth.md" ;
    book:sourceText """# 43. Generation Depth

> **Pattern 43 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **43. Generation Depth**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generation Depth**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Generation Depth** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/043-43-generation-depth.rs`](../listings/043-43-generation-depth.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 43,
    title: "43. Generation Depth",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 43);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **43. Generation Depth**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-043` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-043-listing a book:Listing ;
    book:sourcePath "listings/043-43-generation-depth.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 43,
    title: "43. Generation Depth",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 43);
}
""" .

book:chapter-043 book:listing book:chapter-043-listing .

# ── maturity-044.ttl ────────────────────────────────────────────
book:chapter-044 a book:Chapter ;
    book:number 44 ;
    book:title "44. Handler-Gap Size" ;
    book:slug "handler-gap-size" ;
    book:part "maturity" ;
    book:sourcePath "maturity/044-handler-gap-size.md" ;
    book:sourceText """# 44. Handler-Gap Size

> **Pattern 44 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **44. Handler-Gap Size**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Handler-Gap Size**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Handler-Gap Size** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/044-44-handler-gap-size.rs`](../listings/044-44-handler-gap-size.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 44,
    title: "44. Handler-Gap Size",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 44);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **44. Handler-Gap Size**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-044` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-044-listing a book:Listing ;
    book:sourcePath "listings/044-44-handler-gap-size.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 44,
    title: "44. Handler-Gap Size",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 44);
}
""" .

book:chapter-044 book:listing book:chapter-044-listing .

# ── maturity-045.ttl ────────────────────────────────────────────
book:chapter-045 a book:Chapter ;
    book:number 45 ;
    book:title "45. Ontology Expressiveness" ;
    book:slug "ontology-expressiveness" ;
    book:part "maturity" ;
    book:sourcePath "maturity/045-ontology-expressiveness.md" ;
    book:sourceText """# 45. Ontology Expressiveness

> **Pattern 45 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **45. Ontology Expressiveness**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Ontology Expressiveness**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Ontology Expressiveness** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Score each dimension.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Name the evidence.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Refuse unsupported promotion.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/045-45-ontology-expressiveness.ttl`](../listings/045-45-ontology-expressiveness.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章45
    a pack:生成単位 ;
    dcterms:title "45. Ontology Expressiveness"@en ;
    pack:出力経路 "src/generated/045_45_ontology_expressiveness.rs" ;
    pack:順序 45 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **45. Ontology Expressiveness**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-045` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-045-listing a book:Listing ;
    book:sourcePath "listings/045-45-ontology-expressiveness.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章45
    a pack:生成単位 ;
    dcterms:title "45. Ontology Expressiveness"@en ;
    pack:出力経路 "src/generated/045_45_ontology_expressiveness.rs" ;
    pack:順序 45 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

book:chapter-045 book:listing book:chapter-045-listing .

# ── maturity-046.ttl ────────────────────────────────────────────
book:chapter-046 a book:Chapter ;
    book:number 46 ;
    book:title "46. Consumer Effort" ;
    book:slug "consumer-effort" ;
    book:part "maturity" ;
    book:sourcePath "maturity/046-consumer-effort.md" ;
    book:sourceText """# 46. Consumer Effort

> **Pattern 46 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **46. Consumer Effort**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Consumer Effort**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Consumer Effort** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/046-46-consumer-effort.rs`](../listings/046-46-consumer-effort.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 46,
    title: "46. Consumer Effort",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 46);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **46. Consumer Effort**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-046` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-046-listing a book:Listing ;
    book:sourcePath "listings/046-46-consumer-effort.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 46,
    title: "46. Consumer Effort",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 46);
}
""" .

book:chapter-046 book:listing book:chapter-046-listing .

# ── maturity-047.ttl ────────────────────────────────────────────
book:chapter-047 a book:Chapter ;
    book:number 47 ;
    book:title "47. Test Generation" ;
    book:slug "test-generation" ;
    book:part "maturity" ;
    book:sourcePath "maturity/047-test-generation.md" ;
    book:sourceText """# 47. Test Generation

> **Pattern 47 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **47. Test Generation**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Test Generation**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Test Generation** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Score each dimension.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Name the evidence.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Refuse unsupported promotion.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/047-47-test-generation.rs`](../listings/047-47-test-generation.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 47,
    title: "47. Test Generation",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 47);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **47. Test Generation**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-047` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-047-listing a book:Listing ;
    book:sourcePath "listings/047-47-test-generation.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 47,
    title: "47. Test Generation",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 47);
}
""" .

book:chapter-047 book:listing book:chapter-047-listing .

# ── maturity-048.ttl ────────────────────────────────────────────
book:chapter-048 a book:Chapter ;
    book:number 48 ;
    book:title "48. Regeneration Lifecycle" ;
    book:slug "regeneration-lifecycle" ;
    book:part "maturity" ;
    book:sourcePath "maturity/048-regeneration-lifecycle.md" ;
    book:sourceText """# 48. Regeneration Lifecycle

> **Pattern 48 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **48. Regeneration Lifecycle**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Regeneration Lifecycle**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Regeneration Lifecycle** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/048-48-regeneration-lifecycle.rs`](../listings/048-48-regeneration-lifecycle.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 48,
    title: "48. Regeneration Lifecycle",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 48);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **48. Regeneration Lifecycle**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-048` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-048-listing a book:Listing ;
    book:sourcePath "listings/048-48-regeneration-lifecycle.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 48,
    title: "48. Regeneration Lifecycle",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 48);
}
""" .

book:chapter-048 book:listing book:chapter-048-listing .

# ── maturity-049.ttl ────────────────────────────────────────────
book:chapter-049 a book:Chapter ;
    book:number 49 ;
    book:title "49. Target-API Fidelity" ;
    book:slug "target-api-fidelity" ;
    book:part "maturity" ;
    book:sourcePath "maturity/049-target-api-fidelity.md" ;
    book:sourceText """# 49. Target-API Fidelity

> **Pattern 49 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **49. Target-API Fidelity**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Target-API Fidelity**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Target-API Fidelity** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/049-49-target-api-fidelity.rs`](../listings/049-49-target-api-fidelity.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 49,
    title: "49. Target-API Fidelity",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 49);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **49. Target-API Fidelity**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-049` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-049-listing a book:Listing ;
    book:sourcePath "listings/049-49-target-api-fidelity.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 49,
    title: "49. Target-API Fidelity",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 49);
}
""" .

book:chapter-049 book:listing book:chapter-049-listing .

# ── maturity-050.ttl ────────────────────────────────────────────
book:chapter-050 a book:Chapter ;
    book:number 50 ;
    book:title "50. Evidence per Maturity Cell" ;
    book:slug "evidence-per-cell" ;
    book:part "maturity" ;
    book:sourcePath "maturity/050-evidence-per-cell.md" ;
    book:sourceText """# 50. Evidence per Maturity Cell

> **Pattern 50 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **50. Evidence per Maturity Cell**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Evidence per Maturity Cell**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Evidence per Maturity Cell** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/050-50-evidence-per-maturity-cell.rs`](../listings/050-50-evidence-per-maturity-cell.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 50,
    title: "50. Evidence per Maturity Cell",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 50);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **50. Evidence per Maturity Cell**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-050` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-050-listing a book:Listing ;
    book:sourcePath "listings/050-50-evidence-per-maturity-cell.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 50,
    title: "50. Evidence per Maturity Cell",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 50);
}
""" .

book:chapter-050 book:listing book:chapter-050-listing .

# ── maturity-051.ttl ────────────────────────────────────────────
book:chapter-051 a book:Chapter ;
    book:number 51 ;
    book:title "51. Why One Overall Level Is Insufficient" ;
    book:slug "no-single-level" ;
    book:part "maturity" ;
    book:sourcePath "maturity/051-no-single-level.md" ;
    book:sourceText """# 51. Why One Overall Level Is Insufficient

> **Pattern 51 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **51. Why One Overall Level Is Insufficient**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Why One Overall Level Is Insufficient**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **Why One Overall Level Is Insufficient** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/051-51-why-one-overall-level-is-insufficient.yml`](../listings/051-51-why-one-overall-level-is-insufficient.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-051-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "51. Why One Overall Level Is Insufficient"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **51. Why One Overall Level Is Insufficient**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-051` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-051-listing a book:Listing ;
    book:sourcePath "listings/051-51-why-one-overall-level-is-insufficient.yml" ;
    book:sourceText """name: chapter-051-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "51. Why One Overall Level Is Insufficient"
""" .

book:chapter-051 book:listing book:chapter-051-listing .

# ── maturity-052.ttl ────────────────────────────────────────────
book:chapter-052 a book:Chapter ;
    book:number 52 ;
    book:title "52. The Level Five Calibration Rule" ;
    book:slug "l5-calibration-rule" ;
    book:part "maturity" ;
    book:sourcePath "maturity/052-l5-calibration-rule.md" ;
    book:sourceText """# 52. The Level Five Calibration Rule

> **Pattern 52 · Standing before Scale**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Maturity is an evidence vector, not an adjective. The narrow problem in this chapter is **52. The Level Five Calibration Rule**. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The Level Five Calibration Rule**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The Level Five Calibration Rule** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/052-52-the-level-five-calibration-rule.rs`](../listings/052-52-the-level-five-calibration-rule.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 52,
    title: "52. The Level Five Calibration Rule",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 52);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Standing advances cell by cell from inert material to independently verified substitution.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A higher maturity label survives after removing the consumer, mutation test, idempotency check, or receipt.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS prototype supplied concrete evidence for generation depth, reference fidelity, generated proof, and idempotency while keeping product-level standing separate.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **52. The Level Five Calibration Rule**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-052` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Standing before Scale** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `standing classification`
- `consumer verification`
- `mutation sensitivity`
- `drift refusal`

### Current repository evidence

- `docs/packs/PACK_MATURITY_MODEL.md`
- `book/scripts/check_level_five.py`
- `crates/ggen-engine/tests/composed_packs_e2e.rs`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several cells have real witnesses; universal Level Five substitution remains a target, not a repository-wide fact.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-052-listing a book:Listing ;
    book:sourcePath "listings/052-52-the-level-five-calibration-rule.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 52,
    title: "52. The Level Five Calibration Rule",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 52);
}
""" .

book:chapter-052 book:listing book:chapter-052-listing .

# ── ontology-065.ttl ────────────────────────────────────────────
book:chapter-065 a book:Chapter ;
    book:number "65"^^xsd:integer ;
    book:title "65. Start from Real Source" ;
    book:slug "start-from-real-source" ;
    book:part "ontology" ;
    book:sourcePath "ontology/065-start-from-real-source.md" ;
    book:sourceText """# 65. Start from Real Source

> **Pattern 65 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **65. Start from Real Source**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Start from Real Source**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Start from Real Source** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/065-65-start-from-real-source.ttl`](../listings/065-65-start-from-real-source.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章65
    a pack:生成単位 ;
    dcterms:title "65. Start from Real Source"@en ;
    pack:出力経路 "src/generated/065_65_start_from_real_source.rs" ;
    pack:順序 65 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **65. Start from Real Source**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-065` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-065 a book:Listing ;
    book:forChapter book:chapter-065 ;
    book:sourcePath "listings/065-65-start-from-real-source.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章65
    a pack:生成単位 ;
    dcterms:title \"65. Start from Real Source\"@en ;
    pack:出力経路 \"src/generated/065_65_start_from_real_source.rs\" ;
    pack:順序 65 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-066.ttl ────────────────────────────────────────────
book:chapter-066 a book:Chapter ;
    book:number "66"^^xsd:integer ;
    book:title "66. One Semantic Identity per Domain Concept" ;
    book:slug "one-semantic-identity" ;
    book:part "ontology" ;
    book:sourcePath "ontology/066-one-semantic-identity.md" ;
    book:sourceText """# 66. One Semantic Identity per Domain Concept

> **Pattern 66 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **66. One Semantic Identity per Domain Concept**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **One Semantic Identity per Domain Concept**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **One Semantic Identity per Domain Concept** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/066-66-one-semantic-identity-per-domain-concept.ttl`](../listings/066-66-one-semantic-identity-per-domain-concept.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章66
    a pack:生成単位 ;
    dcterms:title "66. One Semantic Identity per Domain Concept"@en ;
    pack:出力経路 "src/generated/066_66_one_semantic_identity_per_domain_concept.rs" ;
    pack:順序 66 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **66. One Semantic Identity per Domain Concept**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-066` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-066 a book:Listing ;
    book:forChapter book:chapter-066 ;
    book:sourcePath "listings/066-66-one-semantic-identity-per-domain-concept.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章66
    a pack:生成単位 ;
    dcterms:title \"66. One Semantic Identity per Domain Concept\"@en ;
    pack:出力経路 \"src/generated/066_66_one_semantic_identity_per_domain_concept.rs\" ;
    pack:順序 66 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-067.ttl ────────────────────────────────────────────
book:chapter-067 a book:Chapter ;
    book:number "67"^^xsd:integer ;
    book:title "67. Classes, Individuals, and Properties" ;
    book:slug "classes-individuals-properties" ;
    book:part "ontology" ;
    book:sourcePath "ontology/067-classes-individuals-properties.md" ;
    book:sourceText """# 67. Classes, Individuals, and Properties

> **Pattern 67 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **67. Classes, Individuals, and Properties**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Classes, Individuals, and Properties**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Classes, Individuals, and Properties** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/067-67-classes-individuals-and-properties.ttl`](../listings/067-67-classes-individuals-and-properties.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章67
    a pack:生成単位 ;
    dcterms:title "67. Classes, Individuals, and Properties"@en ;
    pack:出力経路 "src/generated/067_67_classes_individuals_and_properties.rs" ;
    pack:順序 67 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **67. Classes, Individuals, and Properties**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-067` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-067 a book:Listing ;
    book:forChapter book:chapter-067 ;
    book:sourcePath "listings/067-67-classes-individuals-and-properties.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章67
    a pack:生成単位 ;
    dcterms:title \"67. Classes, Individuals, and Properties\"@en ;
    pack:出力経路 \"src/generated/067_67_classes_individuals_and_properties.rs\" ;
    pack:順序 67 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-068.ttl ────────────────────────────────────────────
book:chapter-068 a book:Chapter ;
    book:number "68"^^xsd:integer ;
    book:title "68. Stable IRIs" ;
    book:slug "stable-iris" ;
    book:part "ontology" ;
    book:sourcePath "ontology/068-stable-iris.md" ;
    book:sourceText """# 68. Stable IRIs

> **Pattern 68 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **68. Stable IRIs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Stable IRIs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Stable IRIs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/068-68-stable-iris.ttl`](../listings/068-68-stable-iris.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章68
    a pack:生成単位 ;
    dcterms:title "68. Stable IRIs"@en ;
    pack:出力経路 "src/generated/068_68_stable_iris.rs" ;
    pack:順序 68 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **68. Stable IRIs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-068` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-068 a book:Listing ;
    book:forChapter book:chapter-068 ;
    book:sourcePath "listings/068-68-stable-iris.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章68
    a pack:生成単位 ;
    dcterms:title \"68. Stable IRIs\"@en ;
    pack:出力経路 \"src/generated/068_68_stable_iris.rs\" ;
    pack:順序 68 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-069.ttl ────────────────────────────────────────────
book:chapter-069 a book:Chapter ;
    book:number "69"^^xsd:integer ;
    book:title "69. Reuse Public Vocabulary" ;
    book:slug "public-vocabulary-reuse" ;
    book:part "ontology" ;
    book:sourcePath "ontology/069-public-vocabulary-reuse.md" ;
    book:sourceText """# 69. Reuse Public Vocabulary

> **Pattern 69 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **69. Reuse Public Vocabulary**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reuse Public Vocabulary**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Reuse Public Vocabulary.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/069-69-reuse-public-vocabulary.ttl`](../listings/069-69-reuse-public-vocabulary.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章69
    a pack:生成単位 ;
    dcterms:title "69. Reuse Public Vocabulary"@en ;
    pack:出力経路 "src/generated/069_69_reuse_public_vocabulary.rs" ;
    pack:順序 69 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **69. Reuse Public Vocabulary**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-069` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-069 a book:Listing ;
    book:forChapter book:chapter-069 ;
    book:sourcePath "listings/069-69-reuse-public-vocabulary.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章69
    a pack:生成単位 ;
    dcterms:title \"69. Reuse Public Vocabulary\"@en ;
    pack:出力経路 \"src/generated/069_69_reuse_public_vocabulary.rs\" ;
    pack:順序 69 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-070.ttl ────────────────────────────────────────────
book:chapter-070 a book:Chapter ;
    book:number "70"^^xsd:integer ;
    book:title "70. Pack-Local Vocabulary" ;
    book:slug "pack-local-vocabulary" ;
    book:part "ontology" ;
    book:sourcePath "ontology/070-pack-local-vocabulary.md" ;
    book:sourceText """# 70. Pack-Local Vocabulary

> **Pattern 70 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **70. Pack-Local Vocabulary**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack-Local Vocabulary**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack-Local Vocabulary** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/070-70-pack-local-vocabulary.ttl`](../listings/070-70-pack-local-vocabulary.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章70
    a pack:生成単位 ;
    dcterms:title "70. Pack-Local Vocabulary"@en ;
    pack:出力経路 "src/generated/070_70_pack_local_vocabulary.rs" ;
    pack:順序 70 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **70. Pack-Local Vocabulary**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-070` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-070 a book:Listing ;
    book:forChapter book:chapter-070 ;
    book:sourcePath "listings/070-70-pack-local-vocabulary.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章70
    a pack:生成単位 ;
    dcterms:title \"70. Pack-Local Vocabulary\"@en ;
    pack:出力経路 \"src/generated/070_70_pack_local_vocabulary.rs\" ;
    pack:順序 70 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-071.ttl ────────────────────────────────────────────
book:chapter-071 a book:Chapter ;
    book:number "71"^^xsd:integer ;
    book:title "71. Modeling Modules" ;
    book:slug "modeling-modules" ;
    book:part "ontology" ;
    book:sourcePath "ontology/071-modeling-modules.md" ;
    book:sourceText """# 71. Modeling Modules

> **Pattern 71 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **71. Modeling Modules**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Modules**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Modules** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/071-71-modeling-modules.ttl`](../listings/071-71-modeling-modules.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章71
    a pack:生成単位 ;
    dcterms:title "71. Modeling Modules"@en ;
    pack:出力経路 "src/generated/071_71_modeling_modules.rs" ;
    pack:順序 71 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **71. Modeling Modules**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-071` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-071 a book:Listing ;
    book:forChapter book:chapter-071 ;
    book:sourcePath "listings/071-71-modeling-modules.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章71
    a pack:生成単位 ;
    dcterms:title \"71. Modeling Modules\"@en ;
    pack:出力経路 \"src/generated/071_71_modeling_modules.rs\" ;
    pack:順序 71 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-072.ttl ────────────────────────────────────────────
book:chapter-072 a book:Chapter ;
    book:number "72"^^xsd:integer ;
    book:title "72. Modeling Types" ;
    book:slug "modeling-types" ;
    book:part "ontology" ;
    book:sourcePath "ontology/072-modeling-types.md" ;
    book:sourceText """# 72. Modeling Types

> **Pattern 72 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **72. Modeling Types**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Types**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Types** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/072-72-modeling-types.ttl`](../listings/072-72-modeling-types.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章72
    a pack:生成単位 ;
    dcterms:title "72. Modeling Types"@en ;
    pack:出力経路 "src/generated/072_72_modeling_types.rs" ;
    pack:順序 72 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **72. Modeling Types**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-072` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-072 a book:Listing ;
    book:forChapter book:chapter-072 ;
    book:sourcePath "listings/072-72-modeling-types.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章72
    a pack:生成単位 ;
    dcterms:title \"72. Modeling Types\"@en ;
    pack:出力経路 \"src/generated/072_72_modeling_types.rs\" ;
    pack:順序 72 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-073.ttl ────────────────────────────────────────────
book:chapter-073 a book:Chapter ;
    book:number "73"^^xsd:integer ;
    book:title "73. Modeling Functions" ;
    book:slug "modeling-functions" ;
    book:part "ontology" ;
    book:sourcePath "ontology/073-modeling-functions.md" ;
    book:sourceText """# 73. Modeling Functions

> **Pattern 73 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **73. Modeling Functions**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Functions**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Functions** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/073-73-modeling-functions.ttl`](../listings/073-73-modeling-functions.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章73
    a pack:生成単位 ;
    dcterms:title "73. Modeling Functions"@en ;
    pack:出力経路 "src/generated/073_73_modeling_functions.rs" ;
    pack:順序 73 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **73. Modeling Functions**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-073` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-073 a book:Listing ;
    book:forChapter book:chapter-073 ;
    book:sourcePath "listings/073-73-modeling-functions.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章73
    a pack:生成単位 ;
    dcterms:title \"73. Modeling Functions\"@en ;
    pack:出力経路 \"src/generated/073_73_modeling_functions.rs\" ;
    pack:順序 73 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-074.ttl ────────────────────────────────────────────
book:chapter-074 a book:Chapter ;
    book:number "74"^^xsd:integer ;
    book:title "74. Modeling Parameters and Return Types" ;
    book:slug "modeling-signatures" ;
    book:part "ontology" ;
    book:sourcePath "ontology/074-modeling-signatures.md" ;
    book:sourceText """# 74. Modeling Parameters and Return Types

> **Pattern 74 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **74. Modeling Parameters and Return Types**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Parameters and Return Types**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Parameters and Return Types** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/074-74-modeling-parameters-and-return-types.ttl`](../listings/074-74-modeling-parameters-and-return-types.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章74
    a pack:生成単位 ;
    dcterms:title "74. Modeling Parameters and Return Types"@en ;
    pack:出力経路 "src/generated/074_74_modeling_parameters_and_return_types.rs" ;
    pack:順序 74 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **74. Modeling Parameters and Return Types**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-074` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-074 a book:Listing ;
    book:forChapter book:chapter-074 ;
    book:sourcePath "listings/074-74-modeling-parameters-and-return-types.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章74
    a pack:生成単位 ;
    dcterms:title \"74. Modeling Parameters and Return Types\"@en ;
    pack:出力経路 \"src/generated/074_74_modeling_parameters_and_return_types.rs\" ;
    pack:順序 74 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-075.ttl ────────────────────────────────────────────
book:chapter-075 a book:Chapter ;
    book:number "75"^^xsd:integer ;
    book:title "75. Modeling Function Bodies" ;
    book:slug "modeling-function-bodies" ;
    book:part "ontology" ;
    book:sourcePath "ontology/075-modeling-function-bodies.md" ;
    book:sourceText """# 75. Modeling Function Bodies

> **Pattern 75 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **75. Modeling Function Bodies**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Function Bodies**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Function Bodies** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Assign stable iris.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode the complete surface.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Preserve ordering and lineage.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/075-75-modeling-function-bodies.ttl`](../listings/075-75-modeling-function-bodies.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章75
    a pack:生成単位 ;
    dcterms:title "75. Modeling Function Bodies"@en ;
    pack:出力経路 "src/generated/075_75_modeling_function_bodies.rs" ;
    pack:順序 75 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **75. Modeling Function Bodies**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-075` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-075 a book:Listing ;
    book:forChapter book:chapter-075 ;
    book:sourcePath "listings/075-75-modeling-function-bodies.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章75
    a pack:生成単位 ;
    dcterms:title \"75. Modeling Function Bodies\"@en ;
    pack:出力経路 \"src/generated/075_75_modeling_function_bodies.rs\" ;
    pack:順序 75 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-076.ttl ────────────────────────────────────────────
book:chapter-076 a book:Chapter ;
    book:number "76"^^xsd:integer ;
    book:title "76. Modeling Tests" ;
    book:slug "modeling-tests" ;
    book:part "ontology" ;
    book:sourcePath "ontology/076-modeling-tests.md" ;
    book:sourceText """# 76. Modeling Tests

> **Pattern 76 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **76. Modeling Tests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Tests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Tests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/076-76-modeling-tests.ttl`](../listings/076-76-modeling-tests.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章76
    a pack:生成単位 ;
    dcterms:title "76. Modeling Tests"@en ;
    pack:出力経路 "src/generated/076_76_modeling_tests.rs" ;
    pack:順序 76 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **76. Modeling Tests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-076` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-076 a book:Listing ;
    book:forChapter book:chapter-076 ;
    book:sourcePath "listings/076-76-modeling-tests.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章76
    a pack:生成単位 ;
    dcterms:title \"76. Modeling Tests\"@en ;
    pack:出力経路 \"src/generated/076_76_modeling_tests.rs\" ;
    pack:順序 76 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-077.ttl ────────────────────────────────────────────
book:chapter-077 a book:Chapter ;
    book:number "77"^^xsd:integer ;
    book:title "77. Modeling Files and Output Paths" ;
    book:slug "modeling-files" ;
    book:part "ontology" ;
    book:sourcePath "ontology/077-modeling-files.md" ;
    book:sourceText """# 77. Modeling Files and Output Paths

> **Pattern 77 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **77. Modeling Files and Output Paths**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Files and Output Paths**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Files and Output Paths** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/077-77-modeling-files-and-output-paths.ttl`](../listings/077-77-modeling-files-and-output-paths.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章77
    a pack:生成単位 ;
    dcterms:title "77. Modeling Files and Output Paths"@en ;
    pack:出力経路 "src/generated/077_77_modeling_files_and_output_paths.rs" ;
    pack:順序 77 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **77. Modeling Files and Output Paths**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-077` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-077 a book:Listing ;
    book:forChapter book:chapter-077 ;
    book:sourcePath "listings/077-77-modeling-files-and-output-paths.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章77
    a pack:生成単位 ;
    dcterms:title \"77. Modeling Files and Output Paths\"@en ;
    pack:出力経路 \"src/generated/077_77_modeling_files_and_output_paths.rs\" ;
    pack:順序 77 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-078.ttl ────────────────────────────────────────────
book:chapter-078 a book:Chapter ;
    book:number "78"^^xsd:integer ;
    book:title "78. Modeling Build and Release Assets" ;
    book:slug "modeling-release-assets" ;
    book:part "ontology" ;
    book:sourcePath "ontology/078-modeling-release-assets.md" ;
    book:sourceText """# 78. Modeling Build and Release Assets

> **Pattern 78 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **78. Modeling Build and Release Assets**. Release scaffolding is part of the product whenever downstream users require it to build, verify, package, or attest the subsystem.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Build and Release Assets**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Build and Release Assets** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/078-78-modeling-build-and-release-assets.sh`](../listings/078-78-modeling-build-and-release-assets.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="78. Modeling Build and Release Assets"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **78. Modeling Build and Release Assets**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-078` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-078 a book:Listing ;
    book:forChapter book:chapter-078 ;
    book:sourcePath "listings/078-78-modeling-build-and-release-assets.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"78. Modeling Build and Release Assets\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── ontology-079.ttl ────────────────────────────────────────────
book:chapter-079 a book:Chapter ;
    book:number "79"^^xsd:integer ;
    book:title "79. Modeling Historical Lineage" ;
    book:slug "modeling-lineage" ;
    book:part "ontology" ;
    book:sourcePath "ontology/079-modeling-lineage.md" ;
    book:sourceText """# 79. Modeling Historical Lineage

> **Pattern 79 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **79. Modeling Historical Lineage**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Modeling Historical Lineage**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Modeling Historical Lineage** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/079-79-modeling-historical-lineage.ttl`](../listings/079-79-modeling-historical-lineage.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章79
    a pack:生成単位 ;
    dcterms:title "79. Modeling Historical Lineage"@en ;
    pack:出力経路 "src/generated/079_79_modeling_historical_lineage.rs" ;
    pack:順序 79 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **79. Modeling Historical Lineage**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-079` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-079 a book:Listing ;
    book:forChapter book:chapter-079 ;
    book:sourcePath "listings/079-79-modeling-historical-lineage.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章79
    a pack:生成単位 ;
    dcterms:title \"79. Modeling Historical Lineage\"@en ;
    pack:出力経路 \"src/generated/079_79_modeling_historical_lineage.rs\" ;
    pack:順序 79 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── ontology-080.ttl ────────────────────────────────────────────
book:chapter-080 a book:Chapter ;
    book:number "80"^^xsd:integer ;
    book:title "80. Source Text as a Transitional Technique" ;
    book:slug "source-text-transitional" ;
    book:part "ontology" ;
    book:sourcePath "ontology/080-source-text-transitional.md" ;
    book:sourceText """# 80. Source Text as a Transitional Technique

> **Pattern 80 · The Ontology Neighborhood**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The ontology must capture enough structure to regenerate the behavior that the reference consumer observes. The narrow problem in this chapter is **80. Source Text as a Transitional Technique**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Source Text as a Transitional Technique**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Source Text as a Transitional Technique** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Assign stable iris.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode the complete surface.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Preserve ordering and lineage.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/080-80-source-text-as-a-transitional-technique.ttl`](../listings/080-80-source-text-as-a-transitional-technique.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章80
    a pack:生成単位 ;
    dcterms:title "80. Source Text as a Transitional Technique"@en ;
    pack:出力経路 "src/generated/080_80_source_text_as_a_transitional_technique.rs" ;
    pack:順序 80 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Stable identities and public relations carry domain meaning independently of any one target language.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A target-language rename, file move, or second pack causes semantic identity to change or collide.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Twenty-four Japanese modules are represented as semantic identities with paths, ordering, source bodies, tests, and historical lineage.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **80. Source Text as a Transitional Technique**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-080` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Ontology Neighborhood** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `RDF parsing`
- `stable identities`
- `SPARQL execution`
- `graph-law materialization`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/src/pack.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`IMPLEMENTED`

### Open gap

Semantic modeling quality remains pack-specific and must be falsified by each pack's own gates and consumers.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-080 a book:Listing ;
    book:forChapter book:chapter-080 ;
    book:sourcePath "listings/080-80-source-text-as-a-transitional-technique.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章80
    a pack:生成単位 ;
    dcterms:title \"80. Source Text as a Transitional Technique\"@en ;
    pack:出力経路 \"src/generated/080_80_source_text_as_a_transitional_technique.rs\" ;
    pack:順序 80 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── pack-model-013.ttl ──────────────────────────────────────────
book:chapter-pack-model-013 a book:Chapter ;
    book:number "13"^^xsd:integer ;
    book:title "13. Packs as Routable Capability Cells" ;
    book:slug "routable-capability-cells" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/013-routable-capability-cells.md" ;
    book:sourceText """# 13. Packs as Routable Capability Cells

> **Pattern 13 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **13. Packs as Routable Capability Cells**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Packs as Routable Capability Cells**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Packs as Routable Capability Cells** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/013-13-packs-as-routable-capability-cells.rs`](../listings/013-13-packs-as-routable-capability-cells.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 13,
    title: "13. Packs as Routable Capability Cells",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 13);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **13. Packs as Routable Capability Cells**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-013` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-013 .

book:listing-pack-model-013 a book:Listing ;
    book:sourcePath "listings/013-13-packs-as-routable-capability-cells.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 13,
    title: "13. Packs as Routable Capability Cells",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 13);
}
""" .

# ── pack-model-014.ttl ──────────────────────────────────────────
book:chapter-pack-model-014 a book:Chapter ;
    book:number "14"^^xsd:integer ;
    book:title "14. Pack Boundaries" ;
    book:slug "pack-boundaries" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/014-pack-boundaries.md" ;
    book:sourceText """# 14. Pack Boundaries

> **Pattern 14 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **14. Pack Boundaries**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Boundaries**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Boundaries** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/014-14-pack-boundaries.rs`](../listings/014-14-pack-boundaries.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 14,
    title: "14. Pack Boundaries",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 14);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **14. Pack Boundaries**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-014` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-014 .

book:listing-pack-model-014 a book:Listing ;
    book:sourcePath "listings/014-14-pack-boundaries.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 14,
    title: "14. Pack Boundaries",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 14);
}
""" .

# ── pack-model-015.ttl ──────────────────────────────────────────
book:chapter-pack-model-015 a book:Chapter ;
    book:number "15"^^xsd:integer ;
    book:title "15. Pack Identity" ;
    book:slug "pack-identity" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/015-pack-identity.md" ;
    book:sourceText """# 15. Pack Identity

> **Pattern 15 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **15. Pack Identity**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Identity**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Identity** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/015-15-pack-identity.rs`](../listings/015-15-pack-identity.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 15,
    title: "15. Pack Identity",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 15);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **15. Pack Identity**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-015` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-015 .

book:listing-pack-model-015 a book:Listing ;
    book:sourcePath "listings/015-15-pack-identity.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 15,
    title: "15. Pack Identity",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 15);
}
""" .

# ── pack-model-016.ttl ──────────────────────────────────────────
book:chapter-pack-model-016 a book:Chapter ;
    book:number "16"^^xsd:integer ;
    book:title "16. Pack Inputs and Outputs" ;
    book:slug "pack-inputs-outputs" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/016-pack-inputs-outputs.md" ;
    book:sourceText """# 16. Pack Inputs and Outputs

> **Pattern 16 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **16. Pack Inputs and Outputs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Inputs and Outputs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Inputs and Outputs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/016-16-pack-inputs-and-outputs.rs`](../listings/016-16-pack-inputs-and-outputs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 16,
    title: "16. Pack Inputs and Outputs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 16);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **16. Pack Inputs and Outputs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-016` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-016 .

book:listing-pack-model-016 a book:Listing ;
    book:sourcePath "listings/016-16-pack-inputs-and-outputs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 16,
    title: "16. Pack Inputs and Outputs",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 16);
}
""" .

# ── pack-model-017.ttl ──────────────────────────────────────────
book:chapter-pack-model-017 a book:Chapter ;
    book:number "17"^^xsd:integer ;
    book:title "17. Pack Metadata" ;
    book:slug "pack-metadata" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/017-pack-metadata.md" ;
    book:sourceText """# 17. Pack Metadata

> **Pattern 17 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **17. Pack Metadata**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Metadata**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Metadata** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/017-17-pack-metadata.toml`](../listings/017-17-pack-metadata.toml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```toml
[pack]
name = "chapter-017-17-pack-metadata"
version = "26.7.19"
description = "17. Pack Metadata"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **17. Pack Metadata**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-017` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-017 .

book:listing-pack-model-017 a book:Listing ;
    book:sourcePath "listings/017-17-pack-metadata.toml" ;
    book:sourceText """[pack]
name = "chapter-017-17-pack-metadata"
version = "26.7.19"
description = "17. Pack Metadata"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
""" .

# ── pack-model-018.ttl ──────────────────────────────────────────
book:chapter-pack-model-018 a book:Chapter ;
    book:number "18"^^xsd:integer ;
    book:title "18. Pack Ontology" ;
    book:slug "pack-ontology" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/018-pack-ontology.md" ;
    book:sourceText """# 18. Pack Ontology

> **Pattern 18 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **18. Pack Ontology**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Ontology**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Ontology** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/018-18-pack-ontology.ttl`](../listings/018-18-pack-ontology.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章18
    a pack:生成単位 ;
    dcterms:title "18. Pack Ontology"@en ;
    pack:出力経路 "src/generated/018_18_pack_ontology.rs" ;
    pack:順序 18 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **18. Pack Ontology**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-018` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-018 .

book:listing-pack-model-018 a book:Listing ;
    book:sourcePath "listings/018-18-pack-ontology.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章18
    a pack:生成単位 ;
    dcterms:title "18. Pack Ontology"@en ;
    pack:出力経路 "src/generated/018_18_pack_ontology.rs" ;
    pack:順序 18 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── pack-model-019.ttl ──────────────────────────────────────────
book:chapter-pack-model-019 a book:Chapter ;
    book:number "19"^^xsd:integer ;
    book:title "19. Pack Templates" ;
    book:slug "pack-templates" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/019-pack-templates.md" ;
    book:sourceText """# 19. Pack Templates

> **Pattern 19 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **19. Pack Templates**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Templates**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Templates** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/019-19-pack-templates.tmpl`](../listings/019-19-pack-templates.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/019_19_pack_templates.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章19 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 19: 19. Pack Templates
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **19. Pack Templates**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-019` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-019 .

book:listing-pack-model-019 a book:Listing ;
    book:sourcePath "listings/019-19-pack-templates.tmpl" ;
    book:sourceText """---
to: src/generated/019_19_pack_templates.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章19 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 19: 19. Pack Templates
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── pack-model-020.ttl ──────────────────────────────────────────
book:chapter-pack-model-020 a book:Chapter ;
    book:number "20"^^xsd:integer ;
    book:title "20. Pack Gates" ;
    book:slug "pack-shapes" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/020-pack-shapes.md" ;
    book:sourceText """# 20. Pack Gates

> **Pattern 20 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **20. Pack Gates**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Gates**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Gates** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/020-20-pack-shapes.ttl`](../listings/020-20-pack-shapes.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary. The listing keeps its historical `.ttl` filename in this book's listing tree; in a shipped pack this file lives at `gates/010_admission.rq`.

```sparql
# MESSAGE: 20. Pack Gates: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
```

### How the current admission mechanism works

Since the engine's SPARQL-gate migration, a pack no longer ships SHACL. Admission law is a `gates/` directory of `.rq` files next to `ontology.ttl`, evaluated in sorted filename order (hence the `010_`, `020_`, `030_` numbering convention) against the post-materialization union graph. Each gate file is an optional block of leading `# MESSAGE:` comment lines (the operator-facing violation text) followed by exactly one ASK or SELECT query: ASK returning `true` is a violation, and any SELECT row is a violation; a CONSTRUCT or DESCRIBE query is not a gate and refuses with `[FM-PACK-012]`. A missing `gates/` directory simply means no gates, but an unreadable directory or file is a typed `[FM-PACK-012]` refusal (fail closed), a gate violation refuses the sync with `[FM-PACK-013]`, and a legacy `shapes.ttl` still present in a pack is itself a loud `[FM-PACK-012]` refusal — never silently ignored. Project-level `[law].gates` mirrors this with `[FM-LAW-012]`/`[FM-LAW-013]`, and declaring the retired `[law].shapes` key refuses with a migration message. Every gate file is hashed into the receipt closure. Because gates are plain SPARQL evaluated through `GraphEngine::query`, the identical gate refuses the identical facts under both the GraphLaw and the Oxigraph engine — proven by `crates/ggen-engine/tests/reasoner_independence_e2e.rs`, which syncs the same pack byte-identically under both backends and receives the same `[FM-PACK-013]` on the same violating fixture. SHACL could not make that promise: `validate_shacl` on the Oxigraph backend is a typed `[FM-LAW-002]` refusal.

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **20. Pack Gates**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-020` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-020 .

book:listing-pack-model-020 a book:Listing ;
    book:sourcePath "listings/020-20-pack-shapes.ttl" ;
    book:sourceText """# MESSAGE: 20. Pack Gates: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
""" .

# ── pack-model-021.ttl ──────────────────────────────────────────
book:chapter-pack-model-021 a book:Chapter ;
    book:number "21"^^xsd:integer ;
    book:title "21. Pack Fixtures" ;
    book:slug "pack-fixtures" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/021-pack-fixtures.md" ;
    book:sourceText """# 21. Pack Fixtures

> **Pattern 21 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **21. Pack Fixtures**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Fixtures**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Fixtures** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/021-21-pack-fixtures.rs`](../listings/021-21-pack-fixtures.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 21,
    title: "21. Pack Fixtures",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 21);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **21. Pack Fixtures**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-021` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-021 .

book:listing-pack-model-021 a book:Listing ;
    book:sourcePath "listings/021-21-pack-fixtures.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 21,
    title: "21. Pack Fixtures",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 21);
}
""" .

# ── pack-model-022.ttl ──────────────────────────────────────────
book:chapter-pack-model-022 a book:Chapter ;
    book:number "22"^^xsd:integer ;
    book:title "22. Pack Hooks" ;
    book:slug "pack-hooks" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/022-pack-hooks.md" ;
    book:sourceText """# 22. Pack Hooks

> **Pattern 22 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **22. Pack Hooks**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Hooks**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Hooks** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/022-22-pack-hooks.rs`](../listings/022-22-pack-hooks.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 22,
    title: "22. Pack Hooks",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 22);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **22. Pack Hooks**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-022` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-022 .

book:listing-pack-model-022 a book:Listing ;
    book:sourcePath "listings/022-22-pack-hooks.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 22,
    title: "22. Pack Hooks",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 22);
}
""" .

# ── pack-model-023.ttl ──────────────────────────────────────────
book:chapter-pack-model-023 a book:Chapter ;
    book:number "23"^^xsd:integer ;
    book:title "23. Pack Reference Oracles" ;
    book:slug "pack-reference-oracles" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/023-pack-reference-oracles.md" ;
    book:sourceText """# 23. Pack Reference Oracles

> **Pattern 23 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **23. Pack Reference Oracles**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Reference Oracles**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Reference Oracles** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/023-23-pack-reference-oracles.rs`](../listings/023-23-pack-reference-oracles.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 23,
    title: "23. Pack Reference Oracles",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 23);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **23. Pack Reference Oracles**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-023` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-023 .

book:listing-pack-model-023 a book:Listing ;
    book:sourcePath "listings/023-23-pack-reference-oracles.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 23,
    title: "23. Pack Reference Oracles",
    standing: Standing::Verified,
    evidence: &["shape", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 23);
}
""" .

# ── pack-model-024.ttl ──────────────────────────────────────────
book:chapter-pack-model-024 a book:Chapter ;
    book:number "24"^^xsd:integer ;
    book:title "24. Pack Receipts" ;
    book:slug "pack-receipts" ;
    book:part "pack-model" ;
    book:sourcePath "pack-model/024-pack-receipts.md" ;
    book:sourceText """# 24. Pack Receipts

> **Pattern 24 · The Pack as a Living Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A pack is a bounded capability cell with a declared consumption path, owned outputs, evidence, and failure behavior. The narrow problem in this chapter is **24. Pack Receipts**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pack Receipts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pack Receipts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-config`
- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/024-24-pack-receipts.json`](../listings/024-24-pack-receipts.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 24,
  "subject": "24. Pack Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack becomes an identifiable, routable, hash-bound part with declared inputs, outputs, policies, and lifecycle.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two consumers cannot determine the same identity, owned outputs, required gates, and provenance from the pack itself.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS implementation is split into a product pack and a release pack so code and lifecycle scaffolding retain distinct ownership.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **24. Pack Receipts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-024` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **The Pack as a Living Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `pack parsing and resolution`
- `content hashing`
- `marketplace lifecycle`
- `admissibility-pack emission`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/ggen-marketplace/src/packs_registry`
- `crates/ggen-lsp/src/pack`

### Pack witnesses

- `packs/lsp-max-pack`
- `packs/star-toml-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Passport and substitution claims remain bounded to the marketplace paths that are actually consumed and verified.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-pack-model-024 .

book:listing-pack-model-024 a book:Listing ;
    book:sourcePath "listings/024-24-pack-receipts.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 24,
  "subject": "24. Pack Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

# ── pack-taxonomy-025.ttl ───────────────────────────────────────
book:chapter-025 a book:Chapter ;
    book:number "25"^^xsd:integer ;
    book:title "25. Why Pack Type Must Be Explicit" ;
    book:slug "explicit-pack-type" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/025-explicit-pack-type.md" ;
    book:sourceText """# 25. Why Pack Type Must Be Explicit

> **Pattern 25 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **25. Why Pack Type Must Be Explicit**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Why Pack Type Must Be Explicit**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **Why Pack Type Must Be Explicit** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/025-25-why-pack-type-must-be-explicit.yml`](../listings/025-25-why-pack-type-must-be-explicit.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-025-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "25. Why Pack Type Must Be Explicit"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **25. Why Pack Type Must Be Explicit**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-025` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-025 a book:Listing ;
    book:forChapter book:chapter-025 ;
    book:sourcePath "listings/025-25-why-pack-type-must-be-explicit.yml" ;
    book:sourceText """name: chapter-025-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py \"25. Why Pack Type Must Be Explicit\"
""" .

# ── pack-taxonomy-026.ttl ───────────────────────────────────────
book:chapter-026 a book:Chapter ;
    book:number "26"^^xsd:integer ;
    book:title "26. Type One: Codegen Packs" ;
    book:slug "codegen-packs" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/026-codegen-packs.md" ;
    book:sourceText """# 26. Type One: Codegen Packs

> **Pattern 26 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **26. Type One: Codegen Packs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Type One: Codegen Packs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Type One: Codegen Packs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/026-26-type-one-codegen-packs.rs`](../listings/026-26-type-one-codegen-packs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 26,
    title: "26. Type One: Codegen Packs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 26);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **26. Type One: Codegen Packs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-026` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-026 a book:Listing ;
    book:forChapter book:chapter-026 ;
    book:sourcePath "listings/026-26-type-one-codegen-packs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 26,
    title: \"26. Type One: Codegen Packs\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 26);
}
""" .

# ── pack-taxonomy-027.ttl ───────────────────────────────────────
book:chapter-027 a book:Chapter ;
    book:number "27"^^xsd:integer ;
    book:title "27. Type Two: Knowledge Hook Packs" ;
    book:slug "knowledge-hook-packs" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/027-knowledge-hook-packs.md" ;
    book:sourceText """# 27. Type Two: Knowledge Hook Packs

> **Pattern 27 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **27. Type Two: Knowledge Hook Packs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Type Two: Knowledge Hook Packs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Type Two: Knowledge Hook Packs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/027-27-type-two-knowledge-hook-packs.rs`](../listings/027-27-type-two-knowledge-hook-packs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 27,
    title: "27. Type Two: Knowledge Hook Packs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 27);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **27. Type Two: Knowledge Hook Packs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-027` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-027 a book:Listing ;
    book:forChapter book:chapter-027 ;
    book:sourcePath "listings/027-27-type-two-knowledge-hook-packs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 27,
    title: \"27. Type Two: Knowledge Hook Packs\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 27);
}
""" .

# ── pack-taxonomy-028.ttl ───────────────────────────────────────
book:chapter-028 a book:Chapter ;
    book:number "28"^^xsd:integer ;
    book:title "28. Type Three: Case-Study Corpus Packs" ;
    book:slug "case-study-packs" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/028-case-study-packs.md" ;
    book:sourceText """# 28. Type Three: Case-Study Corpus Packs

> **Pattern 28 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **28. Type Three: Case-Study Corpus Packs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Type Three: Case-Study Corpus Packs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Type Three: Case-Study Corpus Packs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/028-28-type-three-case-study-corpus-packs.rs`](../listings/028-28-type-three-case-study-corpus-packs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 28,
    title: "28. Type Three: Case-Study Corpus Packs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 28);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **28. Type Three: Case-Study Corpus Packs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-028` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-028 a book:Listing ;
    book:forChapter book:chapter-028 ;
    book:sourcePath "listings/028-28-type-three-case-study-corpus-packs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 28,
    title: \"28. Type Three: Case-Study Corpus Packs\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 28);
}
""" .

# ── pack-taxonomy-029.ttl ───────────────────────────────────────
book:chapter-029 a book:Chapter ;
    book:number "29"^^xsd:integer ;
    book:title "29. Why Pure Ontology Packs Are Inert" ;
    book:slug "inert-ontology-packs" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/029-inert-ontology-packs.md" ;
    book:sourceText """# 29. Why Pure Ontology Packs Are Inert

> **Pattern 29 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **29. Why Pure Ontology Packs Are Inert**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Why Pure Ontology Packs Are Inert**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **Why Pure Ontology Packs Are Inert** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/029-29-why-pure-ontology-packs-are-inert.ttl`](../listings/029-29-why-pure-ontology-packs-are-inert.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章29
    a pack:生成単位 ;
    dcterms:title "29. Why Pure Ontology Packs Are Inert"@en ;
    pack:出力経路 "src/generated/029_29_why_pure_ontology_packs_are_inert.rs" ;
    pack:順序 29 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **29. Why Pure Ontology Packs Are Inert**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-029` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-029 a book:Listing ;
    book:forChapter book:chapter-029 ;
    book:sourcePath "listings/029-29-why-pure-ontology-packs-are-inert.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章29
    a pack:生成単位 ;
    dcterms:title \"29. Why Pure Ontology Packs Are Inert\"@en ;
    pack:出力経路 \"src/generated/029_29_why_pure_ontology_packs_are_inert.rs\" ;
    pack:順序 29 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── pack-taxonomy-030.ttl ───────────────────────────────────────
book:chapter-030 a book:Chapter ;
    book:number "30"^^xsd:integer ;
    book:title "30. Choosing a Consumption Path" ;
    book:slug "choosing-consumption-path" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/030-choosing-consumption-path.md" ;
    book:sourceText """# 30. Choosing a Consumption Path

> **Pattern 30 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **30. Choosing a Consumption Path**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Choosing a Consumption Path**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Choosing a Consumption Path** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/030-30-choosing-a-consumption-path.rs`](../listings/030-30-choosing-a-consumption-path.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 30,
    title: "30. Choosing a Consumption Path",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 30);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **30. Choosing a Consumption Path**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-030` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-030 a book:Listing ;
    book:forChapter book:chapter-030 ;
    book:sourcePath "listings/030-30-choosing-a-consumption-path.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 30,
    title: \"30. Choosing a Consumption Path\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 30);
}
""" .

# ── pack-taxonomy-031.ttl ───────────────────────────────────────
book:chapter-031 a book:Chapter ;
    book:number "31"^^xsd:integer ;
    book:title "31. Mixed Packs" ;
    book:slug "mixed-packs" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/031-mixed-packs.md" ;
    book:sourceText """# 31. Mixed Packs

> **Pattern 31 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **31. Mixed Packs**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Mixed Packs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Mixed Packs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/031-31-mixed-packs.rs`](../listings/031-31-mixed-packs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 31,
    title: "31. Mixed Packs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 31);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **31. Mixed Packs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-031` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-031 a book:Listing ;
    book:forChapter book:chapter-031 ;
    book:sourcePath "listings/031-31-mixed-packs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 31,
    title: \"31. Mixed Packs\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 31);
}
""" .

# ── pack-taxonomy-032.ttl ───────────────────────────────────────
book:chapter-032 a book:Chapter ;
    book:number "32"^^xsd:integer ;
    book:title "32. Separating Product and Release Packs" ;
    book:slug "product-release-split" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/032-product-release-split.md" ;
    book:sourceText """# 32. Separating Product and Release Packs

> **Pattern 32 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **32. Separating Product and Release Packs**. Release scaffolding is part of the product whenever downstream users require it to build, verify, package, or attest the subsystem.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Separating Product and Release Packs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Separating Product and Release Packs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/032-32-separating-product-and-release-packs.rs`](../listings/032-32-separating-product-and-release-packs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 32,
    title: "32. Separating Product and Release Packs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 32);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **32. Separating Product and Release Packs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-032` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-032 a book:Listing ;
    book:forChapter book:chapter-032 ;
    book:sourcePath "listings/032-32-separating-product-and-release-packs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 32,
    title: \"32. Separating Product and Release Packs\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 32);
}
""" .

# ── pack-taxonomy-033.ttl ───────────────────────────────────────
book:chapter-033 a book:Chapter ;
    book:number "33"^^xsd:integer ;
    book:title "33. When a Catalog Is Enough" ;
    book:slug "catalog-sufficiency" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/033-catalog-sufficiency.md" ;
    book:sourceText """# 33. When a Catalog Is Enough

> **Pattern 33 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **33. When a Catalog Is Enough**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **When a Catalog Is Enough**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **When a Catalog Is Enough** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Classify the pack.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Choose a real consumption path.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Reject an inert design.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/033-33-when-a-catalog-is-enough.rs`](../listings/033-33-when-a-catalog-is-enough.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 33,
    title: "33. When a Catalog Is Enough",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 33);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **33. When a Catalog Is Enough**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-033` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-033 a book:Listing ;
    book:forChapter book:chapter-033 ;
    book:sourcePath "listings/033-33-when-a-catalog-is-enough.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 33,
    title: \"33. When a Catalog Is Enough\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 33);
}
""" .

# ── pack-taxonomy-034.ttl ───────────────────────────────────────
book:chapter-034 a book:Chapter ;
    book:number "34"^^xsd:integer ;
    book:title "34. When the Pack Must Generate Behavior" ;
    book:slug "generate-behavior" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/034-generate-behavior.md" ;
    book:sourceText """# 34. When the Pack Must Generate Behavior

> **Pattern 34 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **34. When the Pack Must Generate Behavior**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **When the Pack Must Generate Behavior**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **When the Pack Must Generate Behavior** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/034-34-when-the-pack-must-generate-behavior.rs`](../listings/034-34-when-the-pack-must-generate-behavior.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 34,
    title: "34. When the Pack Must Generate Behavior",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 34);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **34. When the Pack Must Generate Behavior**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-034` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-034 a book:Listing ;
    book:forChapter book:chapter-034 ;
    book:sourcePath "listings/034-34-when-the-pack-must-generate-behavior.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 34,
    title: \"34. When the Pack Must Generate Behavior\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 34);
}
""" .

# ── pack-taxonomy-035.ttl ───────────────────────────────────────
book:chapter-035 a book:Chapter ;
    book:number "35"^^xsd:integer ;
    book:title "35. When the Pack Must Generate Proof" ;
    book:slug "generate-proof" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/035-generate-proof.md" ;
    book:sourceText """# 35. When the Pack Must Generate Proof

> **Pattern 35 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **35. When the Pack Must Generate Proof**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **When the Pack Must Generate Proof**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **When the Pack Must Generate Proof** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/035-35-when-the-pack-must-generate-proof.rs`](../listings/035-35-when-the-pack-must-generate-proof.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 35,
    title: "35. When the Pack Must Generate Proof",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 35);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **35. When the Pack Must Generate Proof**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-035` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-035 a book:Listing ;
    book:forChapter book:chapter-035 ;
    book:sourcePath "listings/035-35-when-the-pack-must-generate-proof.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 35,
    title: \"35. When the Pack Must Generate Proof\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 35);
}
""" .

# ── pack-taxonomy-036.ttl ───────────────────────────────────────
book:chapter-036 a book:Chapter ;
    book:number "36"^^xsd:integer ;
    book:title "36. When the Pack Must Generate a Whole Product" ;
    book:slug "generate-whole-product" ;
    book:part "pack-taxonomy" ;
    book:sourcePath "pack-taxonomy/036-generate-whole-product.md" ;
    book:sourceText """# 36. When the Pack Must Generate a Whole Product

> **Pattern 36 · Choose the Right Kind of Part**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Pack type determines what must be consumed, what must be generated, and what constitutes proof. The narrow problem in this chapter is **36. When the Pack Must Generate a Whole Product**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **When the Pack Must Generate a Whole Product**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **When the Pack Must Generate a Whole Product** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-marketplace`
- `ggen-engine`
- `ggen-lsp`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/036-36-when-the-pack-must-generate-a-whole-product.rs`](../listings/036-36-when-the-pack-must-generate-a-whole-product.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 36,
    title: "36. When the Pack Must Generate a Whole Product",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 36);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The capability receives enough machinery to carry its law and proof without importing unrelated engine responsibility.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The chosen pack type either cannot reach a real consumer or contains machinery that belongs to another lifecycle boundary.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS combines codegen packs with a case-study oracle and release-generation behavior; it is not a pure ontology pack.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **36. When the Pack Must Generate a Whole Product**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-036` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Choose the Right Kind of Part** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `code-generation packs`
- `knowledge-hook packs`
- `case-study corpus packs`
- `marketplace package resolution`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `crates/praxis-graphlaw/src/hooks`
- `crates/ggen-marketplace/src/packs_registry`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/ggen-verify-pack`
- `packs/ma-case-study-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

The taxonomy is witnessed, but not every subtype has a complete Level Five consumer.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-036 a book:Listing ;
    book:forChapter book:chapter-036 ;
    book:sourcePath "listings/036-36-when-the-pack-must-generate-a-whole-product.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 36,
    title: \"36. When the Pack Must Generate a Whole Product\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 36);
}
""" .

# ── practicum-303.ttl ───────────────────────────────────────────
book:chapter-practicum-303 a book:Chapter ;
    book:number 303 ;
    book:title "303. Select a Bounded Product Surface" ;
    book:slug "select-product-surface" ;
    book:part "practicum" ;
    book:sourcePath "practicum/303-select-product-surface.md" ;
    book:listing book:chapter-practicum-303-listing ;
    book:sourceText """# 303. Select a Bounded Product Surface

> **Pattern 303 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **303. Select a Bounded Product Surface**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Select a Bounded Product Surface**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Select a Bounded Product Surface.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/303-303-select-a-bounded-product-surface.sh`](../listings/303-303-select-a-bounded-product-surface.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="303. Select a Bounded Product Surface"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **303. Select a Bounded Product Surface**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-303` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-303-listing a book:Listing ;
    book:sourcePath "listings/303-303-select-a-bounded-product-surface.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"303. Select a Bounded Product Surface\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-304.ttl ───────────────────────────────────────────
book:chapter-practicum-304 a book:Chapter ;
    book:number 304 ;
    book:title "304. Locate an Independent Reference" ;
    book:slug "locate-reference" ;
    book:part "practicum" ;
    book:sourcePath "practicum/304-locate-reference.md" ;
    book:listing book:chapter-practicum-304-listing ;
    book:sourceText """# 304. Locate an Independent Reference

> **Pattern 304 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **304. Locate an Independent Reference**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Locate an Independent Reference**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Locate an Independent Reference.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/304-304-locate-an-independent-reference.sh`](../listings/304-304-locate-an-independent-reference.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="304. Locate an Independent Reference"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **304. Locate an Independent Reference**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-304` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-304-listing a book:Listing ;
    book:sourcePath "listings/304-304-locate-an-independent-reference.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"304. Locate an Independent Reference\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-305.ttl ───────────────────────────────────────────
book:chapter-practicum-305 a book:Chapter ;
    book:number 305 ;
    book:title "305. Inventory Every Required Artifact" ;
    book:slug "inventory-artifacts" ;
    book:part "practicum" ;
    book:sourcePath "practicum/305-inventory-artifacts.md" ;
    book:listing book:chapter-practicum-305-listing ;
    book:sourceText """# 305. Inventory Every Required Artifact

> **Pattern 305 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **305. Inventory Every Required Artifact**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Inventory Every Required Artifact**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Inventory Every Required Artifact.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/305-305-inventory-every-required-artifact.json`](../listings/305-305-inventory-every-required-artifact.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 305,
  "subject": "305. Inventory Every Required Artifact",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **305. Inventory Every Required Artifact**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-305` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-305-listing a book:Listing ;
    book:sourcePath "listings/305-305-inventory-every-required-artifact.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 305,
  \"subject\": \"305. Inventory Every Required Artifact\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── practicum-306.ttl ───────────────────────────────────────────
book:chapter-practicum-306 a book:Chapter ;
    book:number 306 ;
    book:title "306. Classify the Pack Type" ;
    book:slug "classify-pack" ;
    book:part "practicum" ;
    book:sourcePath "practicum/306-classify-pack.md" ;
    book:listing book:chapter-practicum-306-listing ;
    book:sourceText """# 306. Classify the Pack Type

> **Pattern 306 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **306. Classify the Pack Type**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Classify the Pack Type**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Classify the Pack Type.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/306-306-classify-the-pack-type.sh`](../listings/306-306-classify-the-pack-type.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="306. Classify the Pack Type"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **306. Classify the Pack Type**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-306` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-306-listing a book:Listing ;
    book:sourcePath "listings/306-306-classify-the-pack-type.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"306. Classify the Pack Type\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-307.ttl ───────────────────────────────────────────
book:chapter-practicum-307 a book:Chapter ;
    book:number 307 ;
    book:title "307. Write the ARD and PRD" ;
    book:slug "write-ard-prd" ;
    book:part "practicum" ;
    book:sourcePath "practicum/307-write-ard-prd.md" ;
    book:listing book:chapter-practicum-307-listing ;
    book:sourceText """# 307. Write the ARD and PRD

> **Pattern 307 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **307. Write the ARD and PRD**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Write the ARD and PRD**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Write the ARD and PRD.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/307-307-write-the-ard-and-prd.sh`](../listings/307-307-write-the-ard-and-prd.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="307. Write the ARD and PRD"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **307. Write the ARD and PRD**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-307` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-307-listing a book:Listing ;
    book:sourcePath "listings/307-307-write-the-ard-and-prd.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"307. Write the ARD and PRD\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-308.ttl ───────────────────────────────────────────
book:chapter-practicum-308 a book:Chapter ;
    book:number 308 ;
    book:title "308. Create the Ontology-to-Artifact Matrix" ;
    book:slug "ontology-artifact-matrix" ;
    book:part "practicum" ;
    book:sourcePath "practicum/308-ontology-artifact-matrix.md" ;
    book:listing book:chapter-practicum-308-listing ;
    book:sourceText """# 308. Create the Ontology-to-Artifact Matrix

> **Pattern 308 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **308. Create the Ontology-to-Artifact Matrix**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Create the Ontology-to-Artifact Matrix**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Create the Ontology-to-Artifact Matrix.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/308-308-create-the-ontology-to-artifact-matrix.ttl`](../listings/308-308-create-the-ontology-to-artifact-matrix.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章308
    a pack:生成単位 ;
    dcterms:title "308. Create the Ontology-to-Artifact Matrix"@en ;
    pack:出力経路 "src/generated/308_308_create_the_ontology_to_artifact_matrix.rs" ;
    pack:順序 308 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **308. Create the Ontology-to-Artifact Matrix**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-308` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-308-listing a book:Listing ;
    book:sourcePath "listings/308-308-create-the-ontology-to-artifact-matrix.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章308
    a pack:生成単位 ;
    dcterms:title \"308. Create the Ontology-to-Artifact Matrix\"@en ;
    pack:出力経路 \"src/generated/308_308_create_the_ontology_to_artifact_matrix.rs\" ;
    pack:順序 308 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── practicum-309.ttl ───────────────────────────────────────────
book:chapter-practicum-309 a book:Chapter ;
    book:number 309 ;
    book:title "309. Create the Acceptance Matrix" ;
    book:slug "acceptance-matrix" ;
    book:part "practicum" ;
    book:sourcePath "practicum/309-acceptance-matrix.md" ;
    book:listing book:chapter-practicum-309-listing ;
    book:sourceText """# 309. Create the Acceptance Matrix

> **Pattern 309 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **309. Create the Acceptance Matrix**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Create the Acceptance Matrix**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Create the Acceptance Matrix.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/309-309-create-the-acceptance-matrix.json`](../listings/309-309-create-the-acceptance-matrix.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 309,
  "subject": "309. Create the Acceptance Matrix",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **309. Create the Acceptance Matrix**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-309` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-309-listing a book:Listing ;
    book:sourcePath "listings/309-309-create-the-acceptance-matrix.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 309,
  \"subject\": \"309. Create the Acceptance Matrix\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── practicum-310.ttl ───────────────────────────────────────────
book:chapter-practicum-310 a book:Chapter ;
    book:number 310 ;
    book:title "310. Author the Ontology" ;
    book:slug "author-ontology" ;
    book:part "practicum" ;
    book:sourcePath "practicum/310-author-ontology.md" ;
    book:listing book:chapter-practicum-310-listing ;
    book:sourceText """# 310. Author the Ontology

> **Pattern 310 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **310. Author the Ontology**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Author the Ontology**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Author the Ontology.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/310-310-author-the-ontology.ttl`](../listings/310-310-author-the-ontology.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章310
    a pack:生成単位 ;
    dcterms:title "310. Author the Ontology"@en ;
    pack:出力経路 "src/generated/310_310_author_the_ontology.rs" ;
    pack:順序 310 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **310. Author the Ontology**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-310` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-310-listing a book:Listing ;
    book:sourcePath "listings/310-310-author-the-ontology.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章310
    a pack:生成単位 ;
    dcterms:title \"310. Author the Ontology\"@en ;
    pack:出力経路 \"src/generated/310_310_author_the_ontology.rs\" ;
    pack:順序 310 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── practicum-311.ttl ───────────────────────────────────────────
book:chapter-practicum-311 a book:Chapter ;
    book:number 311 ;
    book:title "311. Author the Gates" ;
    book:slug "author-shapes" ;
    book:part "practicum" ;
    book:sourcePath "practicum/311-author-shapes.md" ;
    book:listing book:chapter-practicum-311-listing ;
    book:sourceText """# 311. Author the Gates

> **Pattern 311 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **311. Author the Gates**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Author the Gates**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Author the Gates.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/311-311-author-the-shapes.ttl`](../listings/311-311-author-the-shapes.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary. The listing keeps its historical `.ttl` filename in this book's listing tree; in a shipped pack this file lives at `gates/010_admission.rq`.

```sparql
# MESSAGE: 311. Author the Gates: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
```

### How the current admission mechanism works

Since the engine's SPARQL-gate migration, a pack no longer ships SHACL. Admission law is a `gates/` directory of `.rq` files next to `ontology.ttl`, evaluated in sorted filename order (hence the `010_`, `020_`, `030_` numbering convention) against the post-materialization union graph. Each gate file is an optional block of leading `# MESSAGE:` comment lines (the operator-facing violation text) followed by exactly one ASK or SELECT query: ASK returning `true` is a violation, and any SELECT row is a violation; a CONSTRUCT or DESCRIBE query is not a gate and refuses with `[FM-PACK-012]`. A missing `gates/` directory simply means no gates, but an unreadable directory or file is a typed `[FM-PACK-012]` refusal (fail closed), a gate violation refuses the sync with `[FM-PACK-013]`, and a legacy `shapes.ttl` still present in a pack is itself a loud `[FM-PACK-012]` refusal — never silently ignored. Project-level `[law].gates` mirrors this with `[FM-LAW-012]`/`[FM-LAW-013]`, and declaring the retired `[law].shapes` key refuses with a migration message. Every gate file is hashed into the receipt closure. Because gates are plain SPARQL evaluated through `GraphEngine::query`, the identical gate refuses the identical facts under both the GraphLaw and the Oxigraph engine — proven by `crates/ggen-engine/tests/reasoner_independence_e2e.rs`, which syncs the same pack byte-identically under both backends and receives the same `[FM-PACK-013]` on the same violating fixture. SHACL could not make that promise: `validate_shacl` on the Oxigraph backend is a typed `[FM-LAW-002]` refusal.

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **311. Author the Gates**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-311` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-311-listing a book:Listing ;
    book:sourcePath "listings/311-311-author-the-shapes.ttl" ;
    book:sourceText """# MESSAGE: 311. Author the Gates: malformed data must stop before rendering
# (translated from the retired SHACL shape: sh:minCount 1 / sh:maxCount 1 /
#  sh:datatype xsd:string on pack:生成単位 — any returned row is a violation)
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX xsd:  <http://www.w3.org/2001/XMLSchema#>
SELECT ?s ?violation WHERE {
  {
    ?s a pack:生成単位 .
    VALUES ?required { pack:出力経路 pack:根拠 }
    FILTER NOT EXISTS { ?s ?required ?any }
    BIND(?required AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v1 , ?v2 .
    FILTER(?v1 != ?v2)
    BIND(pack:出力経路 AS ?violation)
  } UNION {
    ?s a pack:生成単位 ; pack:出力経路 ?v .
    FILTER(DATATYPE(?v) != xsd:string)
    BIND(pack:出力経路 AS ?violation)
  }
}
ORDER BY ?s ?violation
""" .

# ── practicum-312.ttl ───────────────────────────────────────────
book:chapter-practicum-312 a book:Chapter ;
    book:number 312 ;
    book:title "312. Author the Projections" ;
    book:slug "author-projections" ;
    book:part "practicum" ;
    book:sourcePath "practicum/312-author-projections.md" ;
    book:listing book:chapter-practicum-312-listing ;
    book:sourceText """# 312. Author the Projections

> **Pattern 312 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **312. Author the Projections**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Author the Projections**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Author the Projections.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/312-312-author-the-projections.tmpl`](../listings/312-312-author-the-projections.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/312_312_author_the_projections.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章312 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 312: 312. Author the Projections
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **312. Author the Projections**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-312` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-312-listing a book:Listing ;
    book:sourcePath "listings/312-312-author-the-projections.tmpl" ;
    book:sourceText """---
to: src/generated/312_312_author_the_projections.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章312 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 312: 312. Author the Projections
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── practicum-313.ttl ───────────────────────────────────────────
book:chapter-practicum-313 a book:Chapter ;
    book:number 313 ;
    book:title "313. Author the Templates" ;
    book:slug "author-templates" ;
    book:part "practicum" ;
    book:sourcePath "practicum/313-author-templates.md" ;
    book:listing book:chapter-practicum-313-listing ;
    book:sourceText """# 313. Author the Templates

> **Pattern 313 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **313. Author the Templates**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Author the Templates**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Author the Templates.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/313-313-author-the-templates.tmpl`](../listings/313-313-author-the-templates.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/313_313_author_the_templates.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章313 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 313: 313. Author the Templates
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **313. Author the Templates**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-313` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-313-listing a book:Listing ;
    book:sourcePath "listings/313-313-author-the-templates.tmpl" ;
    book:sourceText """---
to: src/generated/313_313_author_the_templates.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章313 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 313: 313. Author the Templates
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── practicum-314.ttl ───────────────────────────────────────────
book:chapter-practicum-314 a book:Chapter ;
    book:number 314 ;
    book:title "314. Generate the Proof Suite" ;
    book:slug "generate-proof-suite" ;
    book:part "practicum" ;
    book:sourcePath "practicum/314-generate-proof-suite.md" ;
    book:listing book:chapter-practicum-314-listing ;
    book:sourceText """# 314. Generate the Proof Suite

> **Pattern 314 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **314. Generate the Proof Suite**. A proof is valuable only when a relevant mutation in the subject causes the proof to fail.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate the Proof Suite**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate the Proof Suite.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/314-314-generate-the-proof-suite.sh`](../listings/314-314-generate-the-proof-suite.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="314. Generate the Proof Suite"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **314. Generate the Proof Suite**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-314` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-314-listing a book:Listing ;
    book:sourcePath "listings/314-314-generate-the-proof-suite.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"314. Generate the Proof Suite\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-315.ttl ───────────────────────────────────────────
book:chapter-practicum-315 a book:Chapter ;
    book:number 315 ;
    book:title "315. Build Two Independent Consumers" ;
    book:slug "two-consumers" ;
    book:part "practicum" ;
    book:sourcePath "practicum/315-two-consumers.md" ;
    book:listing book:chapter-practicum-315-listing ;
    book:sourceText """# 315. Build Two Independent Consumers

> **Pattern 315 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **315. Build Two Independent Consumers**. The consumer is the next process and therefore defines the observable contract that generated work must satisfy.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Build Two Independent Consumers**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Build Two Independent Consumers.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/315-315-build-two-independent-consumers.sh`](../listings/315-315-build-two-independent-consumers.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="315. Build Two Independent Consumers"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **315. Build Two Independent Consumers**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-315` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-315-listing a book:Listing ;
    book:sourcePath "listings/315-315-build-two-independent-consumers.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"315. Build Two Independent Consumers\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-316.ttl ───────────────────────────────────────────
book:chapter-practicum-316 a book:Chapter ;
    book:number 316 ;
    book:title "316. Prove Multi-Pack Safety" ;
    book:slug "multipack-safety" ;
    book:part "practicum" ;
    book:sourcePath "practicum/316-multipack-safety.md" ;
    book:listing book:chapter-practicum-316-listing ;
    book:sourceText """# 316. Prove Multi-Pack Safety

> **Pattern 316 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **316. Prove Multi-Pack Safety**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Prove Multi-Pack Safety**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Prove Multi-Pack Safety.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/316-316-prove-multi-pack-safety.sh`](../listings/316-316-prove-multi-pack-safety.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="316. Prove Multi-Pack Safety"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **316. Prove Multi-Pack Safety**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-316` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-316-listing a book:Listing ;
    book:sourcePath "listings/316-316-prove-multi-pack-safety.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"316. Prove Multi-Pack Safety\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-317.ttl ───────────────────────────────────────────
book:chapter-practicum-317 a book:Chapter ;
    book:number 317 ;
    book:title "317. Prove Idempotency and Drift Refusal" ;
    book:slug "idempotency-drift" ;
    book:part "practicum" ;
    book:sourcePath "practicum/317-idempotency-drift.md" ;
    book:listing book:chapter-practicum-317-listing ;
    book:sourceText """# 317. Prove Idempotency and Drift Refusal

> **Pattern 317 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **317. Prove Idempotency and Drift Refusal**. A second sync must be a no-op at the byte level unless an admitted source changed. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Prove Idempotency and Drift Refusal**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Prove Idempotency and Drift Refusal.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/317-317-prove-idempotency-and-drift-refusal.sh`](../listings/317-317-prove-idempotency-and-drift-refusal.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="317. Prove Idempotency and Drift Refusal"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **317. Prove Idempotency and Drift Refusal**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-317` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-317-listing a book:Listing ;
    book:sourcePath "listings/317-317-prove-idempotency-and-drift-refusal.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"317. Prove Idempotency and Drift Refusal\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-318.ttl ───────────────────────────────────────────
book:chapter-practicum-318 a book:Chapter ;
    book:number 318 ;
    book:title "318. Emit Receipts and Supply-Chain Evidence" ;
    book:slug "receipts-supply-chain" ;
    book:part "practicum" ;
    book:sourcePath "practicum/318-receipts-supply-chain.md" ;
    book:listing book:chapter-practicum-318-listing ;
    book:sourceText """# 318. Emit Receipts and Supply-Chain Evidence

> **Pattern 318 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **318. Emit Receipts and Supply-Chain Evidence**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Emit Receipts and Supply-Chain Evidence**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Emit Receipts and Supply-Chain Evidence.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/318-318-emit-receipts-and-supply-chain-evidence.json`](../listings/318-318-emit-receipts-and-supply-chain-evidence.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 318,
  "subject": "318. Emit Receipts and Supply-Chain Evidence",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **318. Emit Receipts and Supply-Chain Evidence**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-318` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-318-listing a book:Listing ;
    book:sourcePath "listings/318-318-emit-receipts-and-supply-chain-evidence.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 318,
  \"subject\": \"318. Emit Receipts and Supply-Chain Evidence\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── practicum-319.ttl ───────────────────────────────────────────
book:chapter-practicum-319 a book:Chapter ;
    book:number 319 ;
    book:title "319. Score the Seven Dimensions" ;
    book:slug "score-seven-dimensions" ;
    book:part "practicum" ;
    book:sourcePath "practicum/319-score-seven-dimensions.md" ;
    book:listing book:chapter-practicum-319-listing ;
    book:sourceText """# 319. Score the Seven Dimensions

> **Pattern 319 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **319. Score the Seven Dimensions**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Score the Seven Dimensions**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Score the Seven Dimensions.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/319-319-score-the-seven-dimensions.sh`](../listings/319-319-score-the-seven-dimensions.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="319. Score the Seven Dimensions"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **319. Score the Seven Dimensions**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-319` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-319-listing a book:Listing ;
    book:sourcePath "listings/319-319-score-the-seven-dimensions.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"319. Score the Seven Dimensions\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── practicum-320.ttl ───────────────────────────────────────────
book:chapter-practicum-320 a book:Chapter ;
    book:number 320 ;
    book:title "320. Present the Level Five Certification Bundle" ;
    book:slug "certification-bundle" ;
    book:part "practicum" ;
    book:sourcePath "practicum/320-certification-bundle.md" ;
    book:listing book:chapter-practicum-320-listing ;
    book:sourceText """# 320. Present the Level Five Certification Bundle

> **Pattern 320 · Make a New Language of Your Own**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The reader now repeats the complete manufacturing sequence on a new bounded subsystem. The narrow problem in this chapter is **320. Present the Level Five Certification Bundle**. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Present the Level Five Certification Bundle**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Present the Level Five Certification Bundle.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- all pack-facing crates selected by the product boundary

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/320-320-present-the-level-five-certification-bundle.sh`](../listings/320-320-present-the-level-five-certification-bundle.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="320. Present the Level Five Certification Bundle"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The reader applies the sequence to a new bounded domain and produces a certification bundle rather than a demonstration.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The final product depends on an unrecorded design decision, hidden expert action, or self-authored oracle.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS supplies the worked example, but graduation requires applying the same evidence discipline to a different independent reference.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **320. Present the Level Five Certification Bundle**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-320` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Make a New Language of Your Own** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `new-domain pack design`
- `consumer construction`
- `certification bundle`

### Current repository evidence

- `book/code/packs/canonical-level-five-pack`
- `book/code/examples/canonical-level-five-consumer`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`TARGET`

### Open gap

No repository pack can witness an arbitrary reader-selected domain before the reader builds and verifies it.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-practicum-320-listing a book:Listing ;
    book:sourcePath "listings/320-320-present-the-level-five-certification-bundle.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"320. Present the Level Five Certification Bundle\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── receipts-187.ttl ────────────────────────────────────────────
book:chapter-187 a book:Chapter ;
    book:number 187 ;
    book:title "187. Every Station Emits Evidence" ;
    book:slug "every-station-evidence" ;
    book:part "receipts" ;
    book:sourcePath "receipts/187-every-station-evidence.md" ;
    book:sourceText """# 187. Every Station Emits Evidence

> **Pattern 187 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **187. Every Station Emits Evidence**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Every Station Emits Evidence**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Every Station Emits Evidence** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/187-187-every-station-emits-evidence.json`](../listings/187-187-every-station-emits-evidence.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 187,
  "subject": "187. Every Station Emits Evidence",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **187. Every Station Emits Evidence**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-187` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-187-listing a book:Listing ;
    book:sourcePath "listings/187-187-every-station-emits-evidence.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 187,
  "subject": "187. Every Station Emits Evidence",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-187 book:listing book:chapter-187-listing .

# ── receipts-188.ttl ────────────────────────────────────────────
book:chapter-188 a book:Chapter ;
    book:number 188 ;
    book:title "188. Input Digests" ;
    book:slug "input-digests" ;
    book:part "receipts" ;
    book:sourcePath "receipts/188-input-digests.md" ;
    book:sourceText """# 188. Input Digests

> **Pattern 188 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **188. Input Digests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Input Digests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Input Digests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/188-188-input-digests.json`](../listings/188-188-input-digests.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 188,
  "subject": "188. Input Digests",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **188. Input Digests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-188` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-188-listing a book:Listing ;
    book:sourcePath "listings/188-188-input-digests.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 188,
  "subject": "188. Input Digests",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-188 book:listing book:chapter-188-listing .

# ── receipts-189.ttl ────────────────────────────────────────────
book:chapter-189 a book:Chapter ;
    book:number 189 ;
    book:title "189. Ontology Digests" ;
    book:slug "ontology-digests" ;
    book:part "receipts" ;
    book:sourcePath "receipts/189-ontology-digests.md" ;
    book:sourceText """# 189. Ontology Digests

> **Pattern 189 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **189. Ontology Digests**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Ontology Digests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Ontology Digests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/189-189-ontology-digests.ttl`](../listings/189-189-ontology-digests.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章189
    a pack:生成単位 ;
    dcterms:title "189. Ontology Digests"@en ;
    pack:出力経路 "src/generated/189_189_ontology_digests.rs" ;
    pack:順序 189 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **189. Ontology Digests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-189` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-189-listing a book:Listing ;
    book:sourcePath "listings/189-189-ontology-digests.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章189
    a pack:生成単位 ;
    dcterms:title "189. Ontology Digests"@en ;
    pack:出力経路 "src/generated/189_189_ontology_digests.rs" ;
    pack:順序 189 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

book:chapter-189 book:listing book:chapter-189-listing .

# ── receipts-190.ttl ────────────────────────────────────────────
book:chapter-190 a book:Chapter ;
    book:number 190 ;
    book:title "190. Template Digests" ;
    book:slug "template-digests" ;
    book:part "receipts" ;
    book:sourcePath "receipts/190-template-digests.md" ;
    book:sourceText """# 190. Template Digests

> **Pattern 190 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **190. Template Digests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Template Digests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Template Digests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/190-190-template-digests.tmpl`](../listings/190-190-template-digests.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/190_190_template_digests.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章190 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 190: 190. Template Digests
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **190. Template Digests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-190` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-190-listing a book:Listing ;
    book:sourcePath "listings/190-190-template-digests.tmpl" ;
    book:sourceText """---
to: src/generated/190_190_template_digests.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章190 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 190: 190. Template Digests
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

book:chapter-190 book:listing book:chapter-190-listing .

# ── receipts-191.ttl ────────────────────────────────────────────
book:chapter-191 a book:Chapter ;
    book:number 191 ;
    book:title "191. Output Digests" ;
    book:slug "output-digests" ;
    book:part "receipts" ;
    book:sourcePath "receipts/191-output-digests.md" ;
    book:sourceText """# 191. Output Digests

> **Pattern 191 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **191. Output Digests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Output Digests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Output Digests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/191-191-output-digests.json`](../listings/191-191-output-digests.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 191,
  "subject": "191. Output Digests",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **191. Output Digests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-191` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-191-listing a book:Listing ;
    book:sourcePath "listings/191-191-output-digests.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 191,
  "subject": "191. Output Digests",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-191 book:listing book:chapter-191-listing .

# ── receipts-192.ttl ────────────────────────────────────────────
book:chapter-192 a book:Chapter ;
    book:number 192 ;
    book:title "192. Build Receipts" ;
    book:slug "build-receipts" ;
    book:part "receipts" ;
    book:sourcePath "receipts/192-build-receipts.md" ;
    book:sourceText """# 192. Build Receipts

> **Pattern 192 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **192. Build Receipts**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Build Receipts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Build Receipts.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/192-192-build-receipts.json`](../listings/192-192-build-receipts.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 192,
  "subject": "192. Build Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **192. Build Receipts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-192` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-192-listing a book:Listing ;
    book:sourcePath "listings/192-192-build-receipts.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 192,
  "subject": "192. Build Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-192 book:listing book:chapter-192-listing .

# ── receipts-193.ttl ────────────────────────────────────────────
book:chapter-193 a book:Chapter ;
    book:number 193 ;
    book:title "193. Test Receipts" ;
    book:slug "test-receipts" ;
    book:part "receipts" ;
    book:sourcePath "receipts/193-test-receipts.md" ;
    book:sourceText """# 193. Test Receipts

> **Pattern 193 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **193. Test Receipts**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Test Receipts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Test Receipts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/193-193-test-receipts.json`](../listings/193-193-test-receipts.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 193,
  "subject": "193. Test Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **193. Test Receipts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-193` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-193-listing a book:Listing ;
    book:sourcePath "listings/193-193-test-receipts.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 193,
  "subject": "193. Test Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-193 book:listing book:chapter-193-listing .

# ── receipts-194.ttl ────────────────────────────────────────────
book:chapter-194 a book:Chapter ;
    book:number 194 ;
    book:title "194. Idempotency Receipts" ;
    book:slug "idempotency-receipts" ;
    book:part "receipts" ;
    book:sourcePath "receipts/194-idempotency-receipts.md" ;
    book:sourceText """# 194. Idempotency Receipts

> **Pattern 194 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **194. Idempotency Receipts**. A second sync must be a no-op at the byte level unless an admitted source changed. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Idempotency Receipts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Idempotency Receipts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/194-194-idempotency-receipts.json`](../listings/194-194-idempotency-receipts.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 194,
  "subject": "194. Idempotency Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **194. Idempotency Receipts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-194` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-194-listing a book:Listing ;
    book:sourcePath "listings/194-194-idempotency-receipts.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 194,
  "subject": "194. Idempotency Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-194 book:listing book:chapter-194-listing .

# ── receipts-195.ttl ────────────────────────────────────────────
book:chapter-195 a book:Chapter ;
    book:number 195 ;
    book:title "195. Inspection Receipts" ;
    book:slug "inspection-receipts" ;
    book:part "receipts" ;
    book:sourcePath "receipts/195-inspection-receipts.md" ;
    book:sourceText """# 195. Inspection Receipts

> **Pattern 195 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **195. Inspection Receipts**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Inspection Receipts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Inspection Receipts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/195-195-inspection-receipts.json`](../listings/195-195-inspection-receipts.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 195,
  "subject": "195. Inspection Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **195. Inspection Receipts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-195` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-195-listing a book:Listing ;
    book:sourcePath "listings/195-195-inspection-receipts.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 195,
  "subject": "195. Inspection Receipts",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-195 book:listing book:chapter-195-listing .

# ── receipts-196.ttl ────────────────────────────────────────────
book:chapter-196 a book:Chapter ;
    book:number 196 ;
    book:title "196. `MANIFEST.256`" ;
    book:slug "manifest-256" ;
    book:part "receipts" ;
    book:sourcePath "receipts/196-manifest-256.md" ;
    book:sourceText """# 196. `MANIFEST.256`

> **Pattern 196 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **196. `MANIFEST.256`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`MANIFEST.256`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`MANIFEST.256`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Hash every input.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record every check.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Verify the promotion bundle.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/196-196-manifest-256.json`](../listings/196-196-manifest-256.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 196,
  "subject": "196. `MANIFEST.256`",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **196. `MANIFEST.256`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-196` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-196-listing a book:Listing ;
    book:sourcePath "listings/196-196-manifest-256.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 196,
  "subject": "196. `MANIFEST.256`",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-196 book:listing book:chapter-196-listing .

# ── receipts-197.ttl ────────────────────────────────────────────
book:chapter-197 a book:Chapter ;
    book:number 197 ;
    book:title "197. Corrective-Divergence Ledger" ;
    book:slug "divergence-ledger" ;
    book:part "receipts" ;
    book:sourcePath "receipts/197-divergence-ledger.md" ;
    book:sourceText """# 197. Corrective-Divergence Ledger

> **Pattern 197 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **197. Corrective-Divergence Ledger**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Corrective-Divergence Ledger**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Corrective-Divergence Ledger** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/197-197-corrective-divergence-ledger.json`](../listings/197-197-corrective-divergence-ledger.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 197,
  "subject": "197. Corrective-Divergence Ledger",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **197. Corrective-Divergence Ledger**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-197` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-197-listing a book:Listing ;
    book:sourcePath "listings/197-197-corrective-divergence-ledger.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 197,
  "subject": "197. Corrective-Divergence Ledger",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-197 book:listing book:chapter-197-listing .

# ── receipts-198.ttl ────────────────────────────────────────────
book:chapter-198 a book:Chapter ;
    book:number 198 ;
    book:title "198. CycloneDX and SPDX" ;
    book:slug "sbom" ;
    book:part "receipts" ;
    book:sourcePath "receipts/198-sbom.md" ;
    book:sourceText """# 198. CycloneDX and SPDX

> **Pattern 198 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **198. CycloneDX and SPDX**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **CycloneDX and SPDX**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **CycloneDX and SPDX** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/198-198-cyclonedx-and-spdx.json`](../listings/198-198-cyclonedx-and-spdx.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 198,
  "subject": "198. CycloneDX and SPDX",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **198. CycloneDX and SPDX**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-198` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-198-listing a book:Listing ;
    book:sourcePath "listings/198-198-cyclonedx-and-spdx.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 198,
  "subject": "198. CycloneDX and SPDX",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-198 book:listing book:chapter-198-listing .

# ── receipts-199.ttl ────────────────────────────────────────────
book:chapter-199 a book:Chapter ;
    book:number 199 ;
    book:title "199. in-toto and SLSA" ;
    book:slug "provenance" ;
    book:part "receipts" ;
    book:sourcePath "receipts/199-provenance.md" ;
    book:sourceText """# 199. in-toto and SLSA

> **Pattern 199 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **199. in-toto and SLSA**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **in-toto and SLSA**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **in-toto and SLSA** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/199-199-in-toto-and-slsa.json`](../listings/199-199-in-toto-and-slsa.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 199,
  "subject": "199. in-toto and SLSA",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **199. in-toto and SLSA**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-199` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-199-listing a book:Listing ;
    book:sourcePath "listings/199-199-in-toto-and-slsa.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 199,
  "subject": "199. in-toto and SLSA",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

book:chapter-199 book:listing book:chapter-199-listing .

# ── receipts-200.ttl ────────────────────────────────────────────
book:chapter-200 a book:Chapter ;
    book:number 200 ;
    book:title "200. Signing and Verification" ;
    book:slug "signing-verification" ;
    book:part "receipts" ;
    book:sourcePath "receipts/200-signing-verification.md" ;
    book:sourceText """# 200. Signing and Verification

> **Pattern 200 · Receipts and Standing**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Every important state transition needs durable evidence linking input, policy, output, and verification. The narrow problem in this chapter is **200. Signing and Verification**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Signing and Verification**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Signing and Verification** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `ggen-graph`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/200-200-signing-and-verification.sh`](../listings/200-200-signing-and-verification.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="200. Signing and Verification"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Inputs, graph state, templates, outputs, verification, and signatures form a replayable provenance chain.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Tampering with a recorded input, output, transition, or signature does not invalidate verification.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The production release carries SHA-256 manifests, build/test receipts, SBOMs, and in-toto provenance while refusing to call missing toolchains a pass.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **200. Signing and Verification**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-200` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Receipts and Standing** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `input/output digests`
- `receipt chains`
- `verification evidence`
- `standing ceilings`

### Current repository evidence

- `crates/praxis-core/src/receipt_epoch.rs`
- `crates/ggen-engine/tests/receipt_chain_e2e.rs`
- `packs/ggen-verify-pack`

### Pack witnesses

- `packs/ggen-verify-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PARTIAL`

### Open gap

Several equivalence classes remain explicitly bounded; presence in a receipt is not proof of semantic equivalence.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-200-listing a book:Listing ;
    book:sourcePath "listings/200-200-signing-and-verification.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject="200. Signing and Verification"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
""" .

book:chapter-200 book:listing book:chapter-200-listing .

# ── reference-053.ttl ───────────────────────────────────────────
book:chapter-reference-053 a book:Chapter ;
    book:number "53"^^xsd:integer ;
    book:title "53. The Reference Is the Customer" ;
    book:slug "reference-is-customer" ;
    book:part "reference" ;
    book:sourcePath "reference/053-reference-is-customer.md" ;
    book:listing book:listing-reference-053 ;
    book:sourceText """# 53. The Reference Is the Customer

> **Pattern 53 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **53. The Reference Is the Customer**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The Reference Is the Customer**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The Reference Is the Customer** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/053-53-the-reference-is-the-customer.rs`](../listings/053-53-the-reference-is-the-customer.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 53,
    title: "53. The Reference Is the Customer",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 53);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **53. The Reference Is the Customer**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-053` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-053 a book:Listing ;
    book:sourcePath "listings/053-53-the-reference-is-the-customer.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 53,
    title: \"53. The Reference Is the Customer\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 53);
}
""" .

# ── reference-054.ttl ───────────────────────────────────────────
book:chapter-reference-054 a book:Chapter ;
    book:number "54"^^xsd:integer ;
    book:title "54. Independent Means Independent" ;
    book:slug "independent-means-independent" ;
    book:part "reference" ;
    book:sourcePath "reference/054-independent-means-independent.md" ;
    book:listing book:listing-reference-054 ;
    book:sourceText """# 54. Independent Means Independent

> **Pattern 54 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **54. Independent Means Independent**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Independent Means Independent**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Independent Means Independent** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/054-54-independent-means-independent.rs`](../listings/054-54-independent-means-independent.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 54,
    title: "54. Independent Means Independent",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 54);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **54. Independent Means Independent**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-054` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-054 a book:Listing ;
    book:sourcePath "listings/054-54-independent-means-independent.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 54,
    title: \"54. Independent Means Independent\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 54);
}
""" .

# ── reference-055.ttl ───────────────────────────────────────────
book:chapter-reference-055 a book:Chapter ;
    book:number "55"^^xsd:integer ;
    book:title "55. Reference Source Trees" ;
    book:slug "reference-source-trees" ;
    book:part "reference" ;
    book:sourcePath "reference/055-reference-source-trees.md" ;
    book:listing book:listing-reference-055 ;
    book:sourceText """# 55. Reference Source Trees

> **Pattern 55 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **55. Reference Source Trees**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reference Source Trees**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Reference Source Trees** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/055-55-reference-source-trees.rs`](../listings/055-55-reference-source-trees.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 55,
    title: "55. Reference Source Trees",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 55);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **55. Reference Source Trees**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-055` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-055 a book:Listing ;
    book:sourcePath "listings/055-55-reference-source-trees.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 55,
    title: \"55. Reference Source Trees\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 55);
}
""" .

# ── reference-056.ttl ───────────────────────────────────────────
book:chapter-reference-056 a book:Chapter ;
    book:number "56"^^xsd:integer ;
    book:title "56. Reference Public APIs" ;
    book:slug "reference-public-apis" ;
    book:part "reference" ;
    book:sourcePath "reference/056-reference-public-apis.md" ;
    book:listing book:listing-reference-056 ;
    book:sourceText """# 56. Reference Public APIs

> **Pattern 56 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **56. Reference Public APIs**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reference Public APIs**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Reference Public APIs** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/056-56-reference-public-apis.rs`](../listings/056-56-reference-public-apis.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 56,
    title: "56. Reference Public APIs",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 56);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **56. Reference Public APIs**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-056` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-056 a book:Listing ;
    book:sourcePath "listings/056-56-reference-public-apis.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 56,
    title: \"56. Reference Public APIs\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 56);
}
""" .

# ── reference-057.ttl ───────────────────────────────────────────
book:chapter-reference-057 a book:Chapter ;
    book:number "57"^^xsd:integer ;
    book:title "57. Reference Behavior" ;
    book:slug "reference-behavior" ;
    book:part "reference" ;
    book:sourcePath "reference/057-reference-behavior.md" ;
    book:listing book:listing-reference-057 ;
    book:sourceText """# 57. Reference Behavior

> **Pattern 57 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **57. Reference Behavior**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reference Behavior**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Reference Behavior** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/057-57-reference-behavior.rs`](../listings/057-57-reference-behavior.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 57,
    title: "57. Reference Behavior",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 57);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **57. Reference Behavior**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-057` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-057 a book:Listing ;
    book:sourcePath "listings/057-57-reference-behavior.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 57,
    title: \"57. Reference Behavior\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 57);
}
""" .

# ── reference-058.ttl ───────────────────────────────────────────
book:chapter-reference-058 a book:Chapter ;
    book:number "58"^^xsd:integer ;
    book:title "58. Reference Tests" ;
    book:slug "reference-tests" ;
    book:part "reference" ;
    book:sourcePath "reference/058-reference-tests.md" ;
    book:listing book:listing-reference-058 ;
    book:sourceText """# 58. Reference Tests

> **Pattern 58 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **58. Reference Tests**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reference Tests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Reference Tests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/058-58-reference-tests.rs`](../listings/058-58-reference-tests.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 58,
    title: "58. Reference Tests",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 58);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **58. Reference Tests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-058` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-058 a book:Listing ;
    book:sourcePath "listings/058-58-reference-tests.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 58,
    title: \"58. Reference Tests\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 58);
}
""" .

# ── reference-059.ttl ───────────────────────────────────────────
book:chapter-reference-059 a book:Chapter ;
    book:number "59"^^xsd:integer ;
    book:title "59. Verbatim Test Reuse" ;
    book:slug "verbatim-test-reuse" ;
    book:part "reference" ;
    book:sourcePath "reference/059-verbatim-test-reuse.md" ;
    book:listing book:listing-reference-059 ;
    book:sourceText """# 59. Verbatim Test Reuse

> **Pattern 59 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **59. Verbatim Test Reuse**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Verbatim Test Reuse**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Verbatim Test Reuse** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/059-59-verbatim-test-reuse.rs`](../listings/059-59-verbatim-test-reuse.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 59,
    title: "59. Verbatim Test Reuse",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 59);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **59. Verbatim Test Reuse**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-059` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-059 a book:Listing ;
    book:sourcePath "listings/059-59-verbatim-test-reuse.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 59,
    title: \"59. Verbatim Test Reuse\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 59);
}
""" .

# ── reference-060.ttl ───────────────────────────────────────────
book:chapter-reference-060 a book:Chapter ;
    book:number "60"^^xsd:integer ;
    book:title "60. Read-Only Reference Fixtures" ;
    book:slug "read-only-reference-fixtures" ;
    book:part "reference" ;
    book:sourcePath "reference/060-read-only-reference-fixtures.md" ;
    book:listing book:listing-reference-060 ;
    book:sourceText """# 60. Read-Only Reference Fixtures

> **Pattern 60 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **60. Read-Only Reference Fixtures**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Read-Only Reference Fixtures**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Read-Only Reference Fixtures** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/060-60-read-only-reference-fixtures.rs`](../listings/060-60-read-only-reference-fixtures.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 60,
    title: "60. Read-Only Reference Fixtures",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 60);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **60. Read-Only Reference Fixtures**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-060` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-060 a book:Listing ;
    book:sourcePath "listings/060-60-read-only-reference-fixtures.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 60,
    title: \"60. Read-Only Reference Fixtures\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 60);
}
""" .

# ── reference-061.ttl ───────────────────────────────────────────
book:chapter-reference-061 a book:Chapter ;
    book:number "61"^^xsd:integer ;
    book:title "61. Reference Manifests" ;
    book:slug "reference-manifests" ;
    book:part "reference" ;
    book:sourcePath "reference/061-reference-manifests.md" ;
    book:listing book:listing-reference-061 ;
    book:sourceText """# 61. Reference Manifests

> **Pattern 61 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **61. Reference Manifests**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reference Manifests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Reference Manifests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Freeze the reference.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Extract its observable contract.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Reuse its tests unchanged.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/061-61-reference-manifests.json`](../listings/061-61-reference-manifests.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 61,
  "subject": "61. Reference Manifests",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **61. Reference Manifests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-061` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-061 a book:Listing ;
    book:sourcePath "listings/061-61-reference-manifests.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 61,
  \"subject\": \"61. Reference Manifests\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── reference-062.ttl ───────────────────────────────────────────
book:chapter-reference-062 a book:Chapter ;
    book:number "62"^^xsd:integer ;
    book:title "62. Reference Digests" ;
    book:slug "reference-digests" ;
    book:part "reference" ;
    book:sourcePath "reference/062-reference-digests.md" ;
    book:listing book:listing-reference-062 ;
    book:sourceText """# 62. Reference Digests

> **Pattern 62 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **62. Reference Digests**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Reference Digests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Reference Digests** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/062-62-reference-digests.rs`](../listings/062-62-reference-digests.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 62,
    title: "62. Reference Digests",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 62);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **62. Reference Digests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-062` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-062 a book:Listing ;
    book:sourcePath "listings/062-62-reference-digests.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 62,
    title: \"62. Reference Digests\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 62);
}
""" .

# ── reference-063.ttl ───────────────────────────────────────────
book:chapter-reference-063 a book:Chapter ;
    book:number "63"^^xsd:integer ;
    book:title "63. Disclosed Corrective Divergence" ;
    book:slug "corrective-divergence" ;
    book:part "reference" ;
    book:sourcePath "reference/063-corrective-divergence.md" ;
    book:listing book:listing-reference-063 ;
    book:sourceText """# 63. Disclosed Corrective Divergence

> **Pattern 63 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **63. Disclosed Corrective Divergence**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Disclosed Corrective Divergence**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Disclosed Corrective Divergence** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/063-63-disclosed-corrective-divergence.rs`](../listings/063-63-disclosed-corrective-divergence.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 63,
    title: "63. Disclosed Corrective Divergence",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 63);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **63. Disclosed Corrective Divergence**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-063` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-063 a book:Listing ;
    book:sourcePath "listings/063-63-disclosed-corrective-divergence.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 63,
    title: \"63. Disclosed Corrective Divergence\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 63);
}
""" .

# ── reference-064.ttl ───────────────────────────────────────────
book:chapter-reference-064 a book:Chapter ;
    book:number "64"^^xsd:integer ;
    book:title "64. When the Reference Is Defective" ;
    book:slug "defective-reference" ;
    book:part "reference" ;
    book:sourcePath "reference/064-defective-reference.md" ;
    book:listing book:listing-reference-064 ;
    book:sourceText """# 64. When the Reference Is Defective

> **Pattern 64 · Independent Reality**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Generated output becomes credible when it must satisfy an independent reference rather than a self-authored expectation. The narrow problem in this chapter is **64. When the Reference Is Defective**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **When the Reference Is Defective**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **When the Reference Is Defective** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/064-64-when-the-reference-is-defective.rs`](../listings/064-64-when-the-reference-is-defective.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 64,
    title: "64. When the Reference Is Defective",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 64);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack is calibrated against an oracle that can disagree with its ontology and templates.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Expected values, fixtures, and generated subjects all descend from the same potentially defective transformation.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The original Japanese Rust source and its unmodified 試験.rs are the conformance oracle; the generator does not get to redefine success.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **64. When the Reference Is Defective**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-064` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Independent Reality** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `independent reference fixtures`
- `reference digests`
- `behavioral comparison`

### Current repository evidence

- `packs/tcps-core-pack/reference/製品版`
- `packs/tcps-core-pack/source-manifest.json`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

TCPS carries a reference tree, but not every book pattern has an independent external oracle.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-reference-064 a book:Listing ;
    book:sourcePath "listings/064-64-when-the-reference-is-defective.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 64,
    title: \"64. When the Reference Is Defective\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 64);
}
""" .

# ── regeneration-173.ttl ────────────────────────────────────────
book:chapter-173 a book:Chapter ;
    book:number 173 ;
    book:title "Regeneration Is Normal Operation" ;
    book:slug "regeneration-normal" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/173-regeneration-normal.md" ;
    book:sourceText """# 173. Regeneration Is Normal Operation

> **Pattern 173 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **173. Regeneration Is Normal Operation**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Regeneration Is Normal Operation**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Regeneration Is Normal Operation** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/173-173-regeneration-is-normal-operation.sh`](../listings/173-173-regeneration-is-normal-operation.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="173. Regeneration Is Normal Operation"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **173. Regeneration Is Normal Operation**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-173` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-173 .

book:listing-173 a book:Listing ;
    book:sourcePath "listings/173-173-regeneration-is-normal-operation.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"173. Regeneration Is Normal Operation\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-174.ttl ────────────────────────────────────────
book:chapter-174 a book:Chapter ;
    book:number 174 ;
    book:title "Sync Once" ;
    book:slug "sync-once" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/174-sync-once.md" ;
    book:sourceText """# 174. Sync Once

> **Pattern 174 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **174. Sync Once**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Sync Once**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Sync Once** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/174-174-sync-once.sh`](../listings/174-174-sync-once.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="174. Sync Once"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **174. Sync Once**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-174` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-174 .

book:listing-174 a book:Listing ;
    book:sourcePath "listings/174-174-sync-once.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"174. Sync Once\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-175.ttl ────────────────────────────────────────
book:chapter-175 a book:Chapter ;
    book:number 175 ;
    book:title "Sync Twice" ;
    book:slug "sync-twice" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/175-sync-twice.md" ;
    book:sourceText """# 175. Sync Twice

> **Pattern 175 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **175. Sync Twice**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Sync Twice**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Sync Twice** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/175-175-sync-twice.sh`](../listings/175-175-sync-twice.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="175. Sync Twice"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **175. Sync Twice**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-175` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-175 .

book:listing-175 a book:Listing ;
    book:sourcePath "listings/175-175-sync-twice.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"175. Sync Twice\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-176.ttl ────────────────────────────────────────
book:chapter-176 a book:Chapter ;
    book:number 176 ;
    book:title "Byte-Identical Output" ;
    book:slug "byte-identical" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/176-byte-identical.md" ;
    book:sourceText """# 176. Byte-Identical Output

> **Pattern 176 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **176. Byte-Identical Output**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Byte-Identical Output**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Byte-Identical Output** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/176-176-byte-identical-output.sh`](../listings/176-176-byte-identical-output.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="176. Byte-Identical Output"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **176. Byte-Identical Output**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-176` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-176 .

book:listing-176 a book:Listing ;
    book:sourcePath "listings/176-176-byte-identical-output.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"176. Byte-Identical Output\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-177.ttl ────────────────────────────────────────
book:chapter-177 a book:Chapter ;
    book:number 177 ;
    book:title "Detect Nondeterministic Ordering" ;
    book:slug "nondeterministic-ordering" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/177-nondeterministic-ordering.md" ;
    book:sourceText """# 177. Detect Nondeterministic Ordering

> **Pattern 177 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **177. Detect Nondeterministic Ordering**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Detect Nondeterministic Ordering**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Detect Nondeterministic Ordering.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/177-177-detect-nondeterministic-ordering.sh`](../listings/177-177-detect-nondeterministic-ordering.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="177. Detect Nondeterministic Ordering"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **177. Detect Nondeterministic Ordering**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-177` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-177 .

book:listing-177 a book:Listing ;
    book:sourcePath "listings/177-177-detect-nondeterministic-ordering.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"177. Detect Nondeterministic Ordering\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-178.ttl ────────────────────────────────────────
book:chapter-178 a book:Chapter ;
    book:number 178 ;
    book:title "Detect Template Drift" ;
    book:slug "template-drift" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/178-template-drift.md" ;
    book:sourceText """# 178. Detect Template Drift

> **Pattern 178 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **178. Detect Template Drift**. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Detect Template Drift**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Detect Template Drift.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/178-178-detect-template-drift.tmpl`](../listings/178-178-detect-template-drift.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/178_178_detect_template_drift.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章178 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 178: 178. Detect Template Drift
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **178. Detect Template Drift**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-178` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-178 .

book:listing-178 a book:Listing ;
    book:sourcePath "listings/178-178-detect-template-drift.tmpl" ;
    book:sourceText """---
to: src/generated/178_178_detect_template_drift.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章178 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 178: 178. Detect Template Drift
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── regeneration-179.ttl ────────────────────────────────────────
book:chapter-179 a book:Chapter ;
    book:number 179 ;
    book:title "Detect Ontology Drift" ;
    book:slug "ontology-drift" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/179-ontology-drift.md" ;
    book:sourceText """# 179. Detect Ontology Drift

> **Pattern 179 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **179. Detect Ontology Drift**. The ontology is authoritative only when its facts are sufficient to reconstruct the behavior expected by the independent consumer. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Detect Ontology Drift**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Detect Ontology Drift.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/179-179-detect-ontology-drift.ttl`](../listings/179-179-detect-ontology-drift.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章179
    a pack:生成単位 ;
    dcterms:title "179. Detect Ontology Drift"@en ;
    pack:出力経路 "src/generated/179_179_detect_ontology_drift.rs" ;
    pack:順序 179 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **179. Detect Ontology Drift**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-179` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-179 .

book:listing-179 a book:Listing ;
    book:sourcePath "listings/179-179-detect-ontology-drift.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章179
    a pack:生成単位 ;
    dcterms:title \"179. Detect Ontology Drift\"@en ;
    pack:出力経路 \"src/generated/179_179_detect_ontology_drift.rs\" ;
    pack:順序 179 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── regeneration-180.ttl ────────────────────────────────────────
book:chapter-180 a book:Chapter ;
    book:number 180 ;
    book:title "Detect Reference Drift" ;
    book:slug "reference-drift" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/180-reference-drift.md" ;
    book:sourceText """# 180. Detect Reference Drift

> **Pattern 180 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **180. Detect Reference Drift**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Detect Reference Drift**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Detect Reference Drift.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/180-180-detect-reference-drift.sh`](../listings/180-180-detect-reference-drift.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="180. Detect Reference Drift"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **180. Detect Reference Drift**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-180` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-180 .

book:listing-180 a book:Listing ;
    book:sourcePath "listings/180-180-detect-reference-drift.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"180. Detect Reference Drift\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-181.ttl ────────────────────────────────────────
book:chapter-181 a book:Chapter ;
    book:number 181 ;
    book:title "Content Hashes" ;
    book:slug "content-hashes" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/181-content-hashes.md" ;
    book:sourceText """# 181. Content Hashes

> **Pattern 181 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **181. Content Hashes**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Content Hashes**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Content Hashes** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/181-181-content-hashes.sh`](../listings/181-181-content-hashes.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="181. Content Hashes"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **181. Content Hashes**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-181` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-181 .

book:listing-181 a book:Listing ;
    book:sourcePath "listings/181-181-content-hashes.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"181. Content Hashes\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-182.ttl ────────────────────────────────────────
book:chapter-182 a book:Chapter ;
    book:number 182 ;
    book:title "Lock Files" ;
    book:slug "lock-files" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/182-lock-files.md" ;
    book:sourceText """# 182. Lock Files

> **Pattern 182 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **182. Lock Files**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Lock Files**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Lock Files** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/182-182-lock-files.sh`](../listings/182-182-lock-files.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="182. Lock Files"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **182. Lock Files**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-182` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-182 .

book:listing-182 a book:Listing ;
    book:sourcePath "listings/182-182-lock-files.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"182. Lock Files\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-183.ttl ────────────────────────────────────────
book:chapter-183 a book:Chapter ;
    book:number 183 ;
    book:title "Refuse Stale Sources" ;
    book:slug "refuse-stale-sources" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/183-refuse-stale-sources.md" ;
    book:sourceText """# 183. Refuse Stale Sources

> **Pattern 183 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **183. Refuse Stale Sources**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Refuse Stale Sources**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Refuse Stale Sources.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/183-183-refuse-stale-sources.sh`](../listings/183-183-refuse-stale-sources.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="183. Refuse Stale Sources"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **183. Refuse Stale Sources**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-183` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-183 .

book:listing-183 a book:Listing ;
    book:sourcePath "listings/183-183-refuse-stale-sources.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"183. Refuse Stale Sources\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-184.ttl ────────────────────────────────────────
book:chapter-184 a book:Chapter ;
    book:number 184 ;
    book:title "Freeze, Inject, Replace, or Refuse" ;
    book:slug "write-modes" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/184-write-modes.md" ;
    book:sourceText """# 184. Freeze, Inject, Replace, or Refuse

> **Pattern 184 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **184. Freeze, Inject, Replace, or Refuse**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Freeze, Inject, Replace, or Refuse**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Freeze, Inject, Replace, or Refuse** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/184-184-freeze-inject-replace-or-refuse.sh`](../listings/184-184-freeze-inject-replace-or-refuse.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="184. Freeze, Inject, Replace, or Refuse"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **184. Freeze, Inject, Replace, or Refuse**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-184` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-184 .

book:listing-184 a book:Listing ;
    book:sourcePath "listings/184-184-freeze-inject-replace-or-refuse.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"184. Freeze, Inject, Replace, or Refuse\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-185.ttl ────────────────────────────────────────
book:chapter-185 a book:Chapter ;
    book:number 185 ;
    book:title "Longitudinal Verification" ;
    book:slug "longitudinal-verification" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/185-longitudinal-verification.md" ;
    book:sourceText """# 185. Longitudinal Verification

> **Pattern 185 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **185. Longitudinal Verification**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Longitudinal Verification**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Longitudinal Verification** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/185-185-longitudinal-verification.sh`](../listings/185-185-longitudinal-verification.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="185. Longitudinal Verification"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **185. Longitudinal Verification**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-185` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-185 .

book:listing-185 a book:Listing ;
    book:sourcePath "listings/185-185-longitudinal-verification.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"185. Longitudinal Verification\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── regeneration-186.ttl ────────────────────────────────────────
book:chapter-186 a book:Chapter ;
    book:number 186 ;
    book:title "Evolution without Hand Editing" ;
    book:slug "evolution-without-hand-editing" ;
    book:part "regeneration" ;
    book:sourcePath "regeneration/186-evolution-without-hand-editing.md" ;
    book:sourceText """# 186. Evolution without Hand Editing

> **Pattern 186 · Time, Change, and Repair**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Regeneration is part of normal operation; byte identity and drift refusal are therefore production requirements. The narrow problem in this chapter is **186. Evolution without Hand Editing**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Evolution without Hand Editing**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Evolution without Hand Editing** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-graph`
- `praxis-core`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/186-186-evolution-without-hand-editing.sh`](../listings/186-186-evolution-without-hand-editing.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="186. Evolution without Hand Editing"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Regeneration becomes ordinary, idempotent operation with explicit drift and repair semantics.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

An unchanged admitted state produces changed bytes, or a changed source silently preserves stale output.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS acceptance path syncs twice, compares output bytes, and records intentional _impl divergences separately from unexplained drift.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **186. Evolution without Hand Editing**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-186` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Time, Change, and Repair** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `content-hash locks`
- `checksum freeze`
- `idempotent sync`
- `drift refusal`

### Current repository evidence

- `book/ggen.lock`
- `packs/star-toml-pack/pack.toml`
- `packs/lsp-max-pack/pack.toml`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Longitudinal repair across arbitrary local edits remains narrower than byte-identical regeneration.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-186 .

book:listing-186 a book:Listing ;
    book:sourcePath "listings/186-186-evolution-without-hand-editing.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"186. Evolution without Hand Editing\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── rust-generation-121.ttl ─────────────────────────────────────
book:chapter-121 a book:Chapter ;
    book:number 121 ;
    book:title "Generate Modules, Not Fragments" ;
    book:slug "modules-not-fragments" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/121-modules-not-fragments.md" ;
    book:sourceText """# 121. Generate Modules, Not Fragments

> **Pattern 121 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **121. Generate Modules, Not Fragments**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Modules, Not Fragments**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Modules, Not Fragments.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/121-121-generate-modules-not-fragments.rs`](../listings/121-121-generate-modules-not-fragments.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 121,
    title: "121. Generate Modules, Not Fragments",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 121);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **121. Generate Modules, Not Fragments**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-121` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-121 .

book:listing-121 a book:Listing ;
    book:sourcePath "listings/121-121-generate-modules-not-fragments.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 121,
    title: \"121. Generate Modules, Not Fragments\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 121);
}
""" .

# ── rust-generation-122.ttl ─────────────────────────────────────
book:chapter-122 a book:Chapter ;
    book:number 122 ;
    book:title "Generate Public Types" ;
    book:slug "public-types" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/122-public-types.md" ;
    book:sourceText """# 122. Generate Public Types

> **Pattern 122 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **122. Generate Public Types**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Public Types**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Public Types.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/122-122-generate-public-types.rs`](../listings/122-122-generate-public-types.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 122,
    title: "122. Generate Public Types",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 122);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **122. Generate Public Types**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-122` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-122 .

book:listing-122 a book:Listing ;
    book:sourcePath "listings/122-122-generate-public-types.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 122,
    title: \"122. Generate Public Types\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 122);
}
""" .

# ── rust-generation-123.ttl ─────────────────────────────────────
book:chapter-123 a book:Chapter ;
    book:number 123 ;
    book:title "Generate Enums and Variants" ;
    book:slug "enums-and-variants" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/123-enums-and-variants.md" ;
    book:sourceText """# 123. Generate Enums and Variants

> **Pattern 123 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **123. Generate Enums and Variants**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Enums and Variants**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Enums and Variants.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/123-123-generate-enums-and-variants.rs`](../listings/123-123-generate-enums-and-variants.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 123,
    title: "123. Generate Enums and Variants",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 123);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **123. Generate Enums and Variants**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-123` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-123 .

book:listing-123 a book:Listing ;
    book:sourcePath "listings/123-123-generate-enums-and-variants.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 123,
    title: \"123. Generate Enums and Variants\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 123);
}
""" .

# ── rust-generation-124.ttl ─────────────────────────────────────
book:chapter-124 a book:Chapter ;
    book:number 124 ;
    book:title "Generate Structs and Fields" ;
    book:slug "structs-and-fields" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/124-structs-and-fields.md" ;
    book:sourceText """# 124. Generate Structs and Fields

> **Pattern 124 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **124. Generate Structs and Fields**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Structs and Fields**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Structs and Fields.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/124-124-generate-structs-and-fields.rs`](../listings/124-124-generate-structs-and-fields.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 124,
    title: "124. Generate Structs and Fields",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 124);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **124. Generate Structs and Fields**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-124` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-124 .

book:listing-124 a book:Listing ;
    book:sourcePath "listings/124-124-generate-structs-and-fields.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 124,
    title: \"124. Generate Structs and Fields\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 124);
}
""" .

# ── rust-generation-125.ttl ─────────────────────────────────────
book:chapter-125 a book:Chapter ;
    book:number 125 ;
    book:title "Generate Trait Boundaries" ;
    book:slug "trait-boundaries" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/125-trait-boundaries.md" ;
    book:sourceText """# 125. Generate Trait Boundaries

> **Pattern 125 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **125. Generate Trait Boundaries**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Trait Boundaries**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Trait Boundaries.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/125-125-generate-trait-boundaries.rs`](../listings/125-125-generate-trait-boundaries.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 125,
    title: "125. Generate Trait Boundaries",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 125);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **125. Generate Trait Boundaries**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-125` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-125 .

book:listing-125 a book:Listing ;
    book:sourcePath "listings/125-125-generate-trait-boundaries.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 125,
    title: \"125. Generate Trait Boundaries\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 125);
}
""" .

# ── rust-generation-126.ttl ─────────────────────────────────────
book:chapter-126 a book:Chapter ;
    book:number 126 ;
    book:title "Generate Functions" ;
    book:slug "functions" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/126-functions.md" ;
    book:sourceText """# 126. Generate Functions

> **Pattern 126 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **126. Generate Functions**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Functions**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Functions.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/126-126-generate-functions.rs`](../listings/126-126-generate-functions.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 126,
    title: "126. Generate Functions",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 126);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **126. Generate Functions**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-126` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-126 .

book:listing-126 a book:Listing ;
    book:sourcePath "listings/126-126-generate-functions.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 126,
    title: \"126. Generate Functions\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 126);
}
""" .

# ── rust-generation-127.ttl ─────────────────────────────────────
book:chapter-127 a book:Chapter ;
    book:number 127 ;
    book:title "Generate Typestate" ;
    book:slug "typestate" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/127-typestate.md" ;
    book:sourceText """# 127. Generate Typestate

> **Pattern 127 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **127. Generate Typestate**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Typestate**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Typestate.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/127-127-generate-typestate.rs`](../listings/127-127-generate-typestate.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 127,
    title: "127. Generate Typestate",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 127);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **127. Generate Typestate**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-127` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-127 .

book:listing-127 a book:Listing ;
    book:sourcePath "listings/127-127-generate-typestate.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 127,
    title: \"127. Generate Typestate\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 127);
}
""" .

# ── rust-generation-128.ttl ─────────────────────────────────────
book:chapter-128 a book:Chapter ;
    book:number 128 ;
    book:title "Generate `no_std` Code" ;
    book:slug "no-std" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/128-no-std.md" ;
    book:sourceText """# 128. Generate `no_std` Code

> **Pattern 128 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **128. Generate `no_std` Code**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate `no_std` Code**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate `no_std` Code.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/128-128-generate-no-std-code.rs`](../listings/128-128-generate-no-std-code.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 128,
    title: "128. Generate `no_std` Code",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 128);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **128. Generate `no_std` Code**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-128` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-128 .

book:listing-128 a book:Listing ;
    book:sourcePath "listings/128-128-generate-no-std-code.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 128,
    title: \"128. Generate `no_std` Code\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 128);
}
""" .

# ── rust-generation-129.ttl ─────────────────────────────────────
book:chapter-129 a book:Chapter ;
    book:number 129 ;
    book:title "Forbid Unsafe Code" ;
    book:slug "forbid-unsafe" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/129-forbid-unsafe.md" ;
    book:sourceText """# 129. Forbid Unsafe Code

> **Pattern 129 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **129. Forbid Unsafe Code**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Forbid Unsafe Code**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Forbid Unsafe Code** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/129-129-forbid-unsafe-code.rs`](../listings/129-129-forbid-unsafe-code.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 129,
    title: "129. Forbid Unsafe Code",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 129);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **129. Forbid Unsafe Code**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-129` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-129 .

book:listing-129 a book:Listing ;
    book:sourcePath "listings/129-129-forbid-unsafe-code.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 129,
    title: \"129. Forbid Unsafe Code\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 129);
}
""" .

# ── rust-generation-130.ttl ─────────────────────────────────────
book:chapter-130 a book:Chapter ;
    book:number 130 ;
    book:title "Generate Module Aggregation" ;
    book:slug "module-aggregation" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/130-module-aggregation.md" ;
    book:sourceText """# 130. Generate Module Aggregation

> **Pattern 130 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **130. Generate Module Aggregation**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Module Aggregation**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Module Aggregation.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/130-130-generate-module-aggregation.rs`](../listings/130-130-generate-module-aggregation.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 130,
    title: "130. Generate Module Aggregation",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 130);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **130. Generate Module Aggregation**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-130` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-130 .

book:listing-130 a book:Listing ;
    book:sourcePath "listings/130-130-generate-module-aggregation.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 130,
    title: \"130. Generate Module Aggregation\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 130);
}
""" .

# ── rust-generation-131.ttl ─────────────────────────────────────
book:chapter-131 a book:Chapter ;
    book:number 131 ;
    book:title "Generate Workspace Manifests" ;
    book:slug "workspace-manifests" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/131-workspace-manifests.md" ;
    book:sourceText """# 131. Generate Workspace Manifests

> **Pattern 131 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **131. Generate Workspace Manifests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Workspace Manifests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Workspace Manifests.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/131-131-generate-workspace-manifests.json`](../listings/131-131-generate-workspace-manifests.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 131,
  "subject": "131. Generate Workspace Manifests",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **131. Generate Workspace Manifests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-131` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-131 .

book:listing-131 a book:Listing ;
    book:sourcePath "listings/131-131-generate-workspace-manifests.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 131,
  \"subject\": \"131. Generate Workspace Manifests\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── rust-generation-132.ttl ─────────────────────────────────────
book:chapter-132 a book:Chapter ;
    book:number 132 ;
    book:title "Generate Multiple Crates" ;
    book:slug "multiple-crates" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/132-multiple-crates.md" ;
    book:sourceText """# 132. Generate Multiple Crates

> **Pattern 132 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **132. Generate Multiple Crates**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Multiple Crates**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Multiple Crates.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/132-132-generate-multiple-crates.rs`](../listings/132-132-generate-multiple-crates.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 132,
    title: "132. Generate Multiple Crates",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 132);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **132. Generate Multiple Crates**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-132` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-132 .

book:listing-132 a book:Listing ;
    book:sourcePath "listings/132-132-generate-multiple-crates.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 132,
    title: \"132. Generate Multiple Crates\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 132);
}
""" .

# ── rust-generation-133.ttl ─────────────────────────────────────
book:chapter-133 a book:Chapter ;
    book:number 133 ;
    book:title "Generate C ABI Surfaces" ;
    book:slug "c-abi" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/133-c-abi.md" ;
    book:sourceText """# 133. Generate C ABI Surfaces

> **Pattern 133 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **133. Generate C ABI Surfaces**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate C ABI Surfaces**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate C ABI Surfaces.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/133-133-generate-c-abi-surfaces.rs`](../listings/133-133-generate-c-abi-surfaces.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 133,
    title: "133. Generate C ABI Surfaces",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 133);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **133. Generate C ABI Surfaces**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-133` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-133 .

book:listing-133 a book:Listing ;
    book:sourcePath "listings/133-133-generate-c-abi-surfaces.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 133,
    title: \"133. Generate C ABI Surfaces\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 133);
}
""" .

# ── rust-generation-134.ttl ─────────────────────────────────────
book:chapter-134 a book:Chapter ;
    book:number 134 ;
    book:title "Generate WebAssembly Surfaces" ;
    book:slug "wasm" ;
    book:part "rust-generation" ;
    book:sourcePath "rust-generation/134-wasm.md" ;
    book:sourceText """# 134. Generate WebAssembly Surfaces

> **Pattern 134 · Complete Product Surfaces**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five generation emits complete modules and crates, not decorative fragments that leave a hidden handler gap. The narrow problem in this chapter is **134. Generate WebAssembly Surfaces**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate WebAssembly Surfaces**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate WebAssembly Surfaces.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/134-134-generate-webassembly-surfaces.rs`](../listings/134-134-generate-webassembly-surfaces.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 134,
    title: "134. Generate WebAssembly Surfaces",
    standing: Standing::Verified,
    evidence: &["gate", "consumer", "proof", "idempotency", "receipt"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 134);
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Generated crates expose coherent APIs, manifests, modules, and target-specific boundaries requiring no hidden repair.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A clean consumer must invent missing modules, signatures, manifests, safety constraints, or cross-crate wiring.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product surface includes tcps-core, tcps-std, tcps-ffi, tcps-wasm, and tcps-cli with Japanese canonical vocabulary.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **134. Generate WebAssembly Surfaces**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-134` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Complete Product Surfaces** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `Rust module generation`
- `manifest projection`
- `multi-target surfaces`

### Current repository evidence

- `packs/star-toml-pack/templates`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/cargo-cicd-pack`
- `packs/wasm4pm-facts-pack`

### Bounded standing

`PARTIAL`

### Open gap

Complete C, WASM, no_std and multi-crate substitution is not demonstrated uniformly across the repository.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-134 .

book:listing-134 a book:Listing ;
    book:sourcePath "listings/134-134-generate-webassembly-surfaces.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum Standing {
    Admitted,
    Refused,
    Generated,
    Verified,
}

#[derive(Clone, Debug, PartialEq, Eq)]
pub struct ChapterArtifact {
    pub chapter: u16,
    pub title: &'static str,
    pub standing: Standing,
    pub evidence: &'static [&'static str],
}

pub const ARTIFACT: ChapterArtifact = ChapterArtifact {
    chapter: 134,
    title: \"134. Generate WebAssembly Surfaces\",
    standing: Standing::Verified,
    evidence: &[\"shape\", \"consumer\", \"proof\", \"idempotency\", \"receipt\"],
};

#[test]
fn chapter_artifact_has_non_vacuous_evidence() {
    assert!(ARTIFACT.evidence.len() >= 5);
    assert_eq!(ARTIFACT.chapter, 134);
}
""" .

# ── sparql-095.ttl ──────────────────────────────────────────────
book:chapter-095 a book:Chapter ;
    book:number "95"^^xsd:integer ;
    book:title "95. SELECT as a Typed Extraction Surface" ;
    book:slug "select-extraction" ;
    book:part "sparql" ;
    book:sourcePath "sparql/095-select-extraction.md" ;
    book:sourceText """# 95. SELECT as a Typed Extraction Surface

> **Pattern 95 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **95. SELECT as a Typed Extraction Surface**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **SELECT as a Typed Extraction Surface**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**SELECT as a Typed Extraction Surface.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/095-95-select-as-a-typed-extraction-surface.rq`](../listings/095-95-select-as-a-typed-extraction-surface.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **95. SELECT as a Typed Extraction Surface**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-095` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-095 a book:Listing ;
    book:forChapter book:chapter-095 ;
    book:sourcePath "listings/095-95-select-as-a-typed-extraction-surface.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-096.ttl ──────────────────────────────────────────────
book:chapter-096 a book:Chapter ;
    book:number "96"^^xsd:integer ;
    book:title "96. CONSTRUCT as Derived Law" ;
    book:slug "construct-derived-law" ;
    book:part "sparql" ;
    book:sourcePath "sparql/096-construct-derived-law.md" ;
    book:sourceText """# 96. CONSTRUCT as Derived Law

> **Pattern 96 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **96. CONSTRUCT as Derived Law**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **CONSTRUCT as Derived Law**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **CONSTRUCT as Derived Law** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/096-96-construct-as-derived-law.rq`](../listings/096-96-construct-as-derived-law.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **96. CONSTRUCT as Derived Law**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-096` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-096 a book:Listing ;
    book:forChapter book:chapter-096 ;
    book:sourcePath "listings/096-96-construct-as-derived-law.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-097.ttl ──────────────────────────────────────────────
book:chapter-097 a book:Chapter ;
    book:number "97"^^xsd:integer ;
    book:title "97. Query Only the Pack You Own" ;
    book:slug "query-owned-pack" ;
    book:part "sparql" ;
    book:sourcePath "sparql/097-query-owned-pack.md" ;
    book:sourceText """# 97. Query Only the Pack You Own

> **Pattern 97 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **97. Query Only the Pack You Own**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Query Only the Pack You Own**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Query Only the Pack You Own** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/097-97-query-only-the-pack-you-own.rq`](../listings/097-97-query-only-the-pack-you-own.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **97. Query Only the Pack You Own**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-097` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-097 a book:Listing ;
    book:forChapter book:chapter-097 ;
    book:sourcePath "listings/097-97-query-only-the-pack-you-own.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-098.ttl ──────────────────────────────────────────────
book:chapter-098 a book:Chapter ;
    book:number "98"^^xsd:integer ;
    book:title "98. Shared Classes and Union Contamination" ;
    book:slug "shared-class-contamination" ;
    book:part "sparql" ;
    book:sourcePath "sparql/098-shared-class-contamination.md" ;
    book:sourceText """# 98. Shared Classes and Union Contamination

> **Pattern 98 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **98. Shared Classes and Union Contamination**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Shared Classes and Union Contamination**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Shared Classes and Union Contamination** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/098-98-shared-classes-and-union-contamination.rq`](../listings/098-98-shared-classes-and-union-contamination.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **98. Shared Classes and Union Contamination**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-098` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-098 a book:Listing ;
    book:forChapter book:chapter-098 ;
    book:sourcePath "listings/098-98-shared-classes-and-union-contamination.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-099.ttl ──────────────────────────────────────────────
book:chapter-099 a book:Chapter ;
    book:number "99"^^xsd:integer ;
    book:title "99. IRI-Prefix Filters" ;
    book:slug "iri-prefix-filters" ;
    book:part "sparql" ;
    book:sourcePath "sparql/099-iri-prefix-filters.md" ;
    book:sourceText """# 99. IRI-Prefix Filters

> **Pattern 99 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **99. IRI-Prefix Filters**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **IRI-Prefix Filters**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **IRI-Prefix Filters** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/099-99-iri-prefix-filters.rq`](../listings/099-99-iri-prefix-filters.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **99. IRI-Prefix Filters**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-099` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-099 a book:Listing ;
    book:forChapter book:chapter-099 ;
    book:sourcePath "listings/099-99-iri-prefix-filters.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-100.ttl ──────────────────────────────────────────────
book:chapter-100 a book:Chapter ;
    book:number "100"^^xsd:integer ;
    book:title "100. Ordering for Determinism" ;
    book:slug "ordering-for-determinism" ;
    book:part "sparql" ;
    book:sourcePath "sparql/100-ordering-for-determinism.md" ;
    book:sourceText """# 100. Ordering for Determinism

> **Pattern 100 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **100. Ordering for Determinism**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Ordering for Determinism**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Ordering for Determinism** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/100-100-ordering-for-determinism.rq`](../listings/100-100-ordering-for-determinism.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **100. Ordering for Determinism**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-100` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-100 a book:Listing ;
    book:forChapter book:chapter-100 ;
    book:sourcePath "listings/100-100-ordering-for-determinism.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-101.ttl ──────────────────────────────────────────────
book:chapter-101 a book:Chapter ;
    book:number "101"^^xsd:integer ;
    book:title "101. Optional Values" ;
    book:slug "optional-values" ;
    book:part "sparql" ;
    book:sourcePath "sparql/101-optional-values.md" ;
    book:sourceText """# 101. Optional Values

> **Pattern 101 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **101. Optional Values**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Optional Values**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Optional Values** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Scope the query.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Order results deterministically.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Test against a union graph.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/101-101-optional-values.rq`](../listings/101-101-optional-values.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **101. Optional Values**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-101` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-101 a book:Listing ;
    book:forChapter book:chapter-101 ;
    book:sourcePath "listings/101-101-optional-values.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-102.ttl ──────────────────────────────────────────────
book:chapter-102 a book:Chapter ;
    book:number "102"^^xsd:integer ;
    book:title "102. Coalescing Missing Documentation" ;
    book:slug "coalescing-values" ;
    book:part "sparql" ;
    book:sourcePath "sparql/102-coalescing-values.md" ;
    book:sourceText """# 102. Coalescing Missing Documentation

> **Pattern 102 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **102. Coalescing Missing Documentation**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Coalescing Missing Documentation**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Coalescing Missing Documentation** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Scope the query.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Order results deterministically.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Test against a union graph.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/102-102-coalescing-missing-documentation.yml`](../listings/102-102-coalescing-missing-documentation.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-102-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "102. Coalescing Missing Documentation"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **102. Coalescing Missing Documentation**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-102` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-102 a book:Listing ;
    book:forChapter book:chapter-102 ;
    book:sourcePath "listings/102-102-coalescing-missing-documentation.yml" ;
    book:sourceText """name: chapter-102-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py \"102. Coalescing Missing Documentation\"
""" .

# ── sparql-103.ttl ──────────────────────────────────────────────
book:chapter-103 a book:Chapter ;
    book:number "103"^^xsd:integer ;
    book:title "103. Multiple Result Sets" ;
    book:slug "multiple-result-sets" ;
    book:part "sparql" ;
    book:sourcePath "sparql/103-multiple-result-sets.md" ;
    book:sourceText """# 103. Multiple Result Sets

> **Pattern 103 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **103. Multiple Result Sets**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Multiple Result Sets**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Multiple Result Sets** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Scope the query.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Order results deterministically.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Test against a union graph.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/103-103-multiple-result-sets.rq`](../listings/103-103-multiple-result-sets.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **103. Multiple Result Sets**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-103` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-103 a book:Listing ;
    book:forChapter book:chapter-103 ;
    book:sourcePath "listings/103-103-multiple-result-sets.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-104.ttl ──────────────────────────────────────────────
book:chapter-104 a book:Chapter ;
    book:number "104"^^xsd:integer ;
    book:title "104. Query Contracts" ;
    book:slug "query-contracts" ;
    book:part "sparql" ;
    book:sourcePath "sparql/104-query-contracts.md" ;
    book:sourceText """# 104. Query Contracts

> **Pattern 104 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **104. Query Contracts**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Query Contracts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Query Contracts** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/104-104-query-contracts.rq`](../listings/104-104-query-contracts.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **104. Query Contracts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-104` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-104 a book:Listing ;
    book:forChapter book:chapter-104 ;
    book:sourcePath "listings/104-104-query-contracts.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-105.ttl ──────────────────────────────────────────────
book:chapter-105 a book:Chapter ;
    book:number "105"^^xsd:integer ;
    book:title "105. Query Fixtures" ;
    book:slug "query-fixtures" ;
    book:part "sparql" ;
    book:sourcePath "sparql/105-query-fixtures.md" ;
    book:sourceText """# 105. Query Fixtures

> **Pattern 105 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **105. Query Fixtures**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Query Fixtures**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Query Fixtures** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/105-105-query-fixtures.rq`](../listings/105-105-query-fixtures.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **105. Query Fixtures**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-105` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-105 a book:Listing ;
    book:forChapter book:chapter-105 ;
    book:sourcePath "listings/105-105-query-fixtures.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── sparql-106.ttl ──────────────────────────────────────────────
book:chapter-106 a book:Chapter ;
    book:number "106"^^xsd:integer ;
    book:title "106. Detecting Cross-Pack Leakage" ;
    book:slug "detecting-cross-pack-leakage" ;
    book:part "sparql" ;
    book:sourcePath "sparql/106-detecting-cross-pack-leakage.md" ;
    book:sourceText """# 106. Detecting Cross-Pack Leakage

> **Pattern 106 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

SPARQL projections define which admitted facts become construction inputs. The narrow problem in this chapter is **106. Detecting Cross-Pack Leakage**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Detecting Cross-Pack Leakage**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Detecting Cross-Pack Leakage** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-graphlaw`
- `ggen-graph`
- `ggen-engine`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/106-106-detecting-cross-pack-leakage.rq`](../listings/106-106-detecting-cross-pack-leakage.rq). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```sparql
PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), "https://example.org/ggen/pack#"))
}
ORDER BY ?order ?path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Queries select only owned facts, derive only declared law, and return rows in a stable order.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Adding an unrelated pack changes selected rows, row order, or output bytes without changing the owning pack.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The TCPS work exposed cross-pack contamination when shared cnv:Command classes were queried without pack-local IRI filters.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **106. Detecting Cross-Pack Leakage**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-106` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `SELECT extraction`
- `CONSTRUCT materialization`
- `deterministic row order`
- `union-graph isolation`

### Current repository evidence

- `crates/ggen-graph/src/lib.rs`
- `crates/praxis-graphlaw/src/lib.rs`
- `crates/ggen-engine/tests/reasoner_independence_e2e.rs`

### Pack witnesses

- `packs/star-toml-pack`
- `packs/lsp-max-pack`
- `packs/praxis-core-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Unsupported SPARQL features and cross-pack leakage must remain typed refusals rather than silent zero-row success.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-106 a book:Listing ;
    book:forChapter book:chapter-106 ;
    book:sourcePath "listings/106-106-detecting-cross-pack-leakage.rq" ;
    book:sourceText """PREFIX pack: <https://example.org/ggen/pack#>
PREFIX dcterms: <http://purl.org/dc/terms/>

SELECT ?item ?path ?order ?title
WHERE {
  ?item a pack:GeneratedUnit ;
        pack:outputPath ?path ;
        pack:order ?order ;
        dcterms:title ?title .
  FILTER(STRSTARTS(STR(?item), \"https://example.org/ggen/pack#\"))
}
ORDER BY ?order ?path
""" .

# ── tcps-core-225.ttl ───────────────────────────────────────────
book:chapter-225 a book:Chapter ;
    book:number 225 ;
    book:title "225. Why TCPS Is the Right Level Five Case Study" ;
    book:slug "why-tcps" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/225-why-tcps.md" ;
    book:sourceText """# 225. Why TCPS Is the Right Level Five Case Study

> **Pattern 225 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **225. Why TCPS Is the Right Level Five Case Study**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Why TCPS Is the Right Level Five Case Study**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **Why TCPS Is the Right Level Five Case Study** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/225-225-why-tcps-is-the-right-level-five-case-study.rs`](../listings/225-225-why-tcps-is-the-right-level-five-case-study.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 225: 225. Why TCPS Is the Right Level Five Case Study
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **225. Why TCPS Is the Right Level Five Case Study**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-225` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-225 .

book:listing-225 a book:Listing ;
    book:sourcePath "listings/225-225-why-tcps-is-the-right-level-five-case-study.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 225: 225. Why TCPS Is the Right Level Five Case Study
""" .

# ── tcps-core-226.ttl ───────────────────────────────────────────
book:chapter-226 a book:Chapter ;
    book:number 226 ;
    book:title "226. Japanese Vocabulary as Canonical Law" ;
    book:slug "japanese-vocabulary" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/226-japanese-vocabulary.md" ;
    book:sourceText """# 226. Japanese Vocabulary as Canonical Law

> **Pattern 226 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **226. Japanese Vocabulary as Canonical Law**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Japanese Vocabulary as Canonical Law**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Japanese Vocabulary as Canonical Law** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/226-226-japanese-vocabulary-as-canonical-law.ttl`](../listings/226-226-japanese-vocabulary-as-canonical-law.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章226
    a pack:生成単位 ;
    dcterms:title "226. Japanese Vocabulary as Canonical Law"@en ;
    pack:出力経路 "src/generated/226_226_japanese_vocabulary_as_canonical_law.rs" ;
    pack:順序 226 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **226. Japanese Vocabulary as Canonical Law**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-226` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-226 .

book:listing-226 a book:Listing ;
    book:sourcePath "listings/226-226-japanese-vocabulary-as-canonical-law.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章226
    a pack:生成単位 ;
    dcterms:title "226. Japanese Vocabulary as Canonical Law"@en ;
    pack:出力経路 "src/generated/226_226_japanese_vocabulary_as_canonical_law.rs" ;
    pack:順序 226 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── tcps-core-227.ttl ───────────────────────────────────────────
book:chapter-227 a book:Chapter ;
    book:number 227 ;
    book:title "227. Original Problem, Invention, Invariant" ;
    book:slug "problem-invention-invariant" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/227-problem-invention-invariant.md" ;
    book:sourceText """# 227. Original Problem, Invention, Invariant

> **Pattern 227 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **227. Original Problem, Invention, Invariant**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Original Problem, Invention, Invariant**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Original Problem, Invention, Invariant** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/227-227-original-problem-invention-invariant.rs`](../listings/227-227-original-problem-invention-invariant.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 227: 227. Original Problem, Invention, Invariant
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **227. Original Problem, Invention, Invariant**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-227` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-227 .

book:listing-227 a book:Listing ;
    book:sourcePath "listings/227-227-original-problem-invention-invariant.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 227: 227. Original Problem, Invention, Invariant
""" .

# ── tcps-core-228.ttl ───────────────────────────────────────────
book:chapter-228 a book:Chapter ;
    book:number 228 ;
    book:title "228. Model the 24 Reference Modules" ;
    book:slug "model-24-modules" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/228-model-24-modules.md" ;
    book:sourceText """# 228. Model the 24 Reference Modules

> **Pattern 228 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **228. Model the 24 Reference Modules**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Model the 24 Reference Modules**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Model the 24 Reference Modules.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/228-228-model-the-24-reference-modules.rs`](../listings/228-228-model-the-24-reference-modules.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 228: 228. Model the 24 Reference Modules
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **228. Model the 24 Reference Modules**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-228` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-228 .

book:listing-228 a book:Listing ;
    book:sourcePath "listings/228-228-model-the-24-reference-modules.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 228: 228. Model the 24 Reference Modules
""" .

# ── tcps-core-229.ttl ───────────────────────────────────────────
book:chapter-229 a book:Chapter ;
    book:number 229 ;
    book:title "229. 語彙" ;
    book:slug "vocabulary" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/229-vocabulary.md" ;
    book:sourceText """# 229. 語彙

> **Pattern 229 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **229. 語彙**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **語彙**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **語彙** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/229-229.rs`](../listings/229-229.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 229: 229. 語彙
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **229. 語彙**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-229` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-229 .

book:listing-229 a book:Listing ;
    book:sourcePath "listings/229-229.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 229: 229. 語彙
""" .

# ── tcps-core-230.ttl ───────────────────────────────────────────
book:chapter-230 a book:Chapter ;
    book:number 230 ;
    book:title "230. 原点 and 系譜" ;
    book:slug "origin-lineage" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/230-origin-lineage.md" ;
    book:sourceText """# 230. 原点 and 系譜

> **Pattern 230 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **230. 原点 and 系譜**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **原点 and 系譜**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **原点 and 系譜** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/230-230-and.rs`](../listings/230-230-and.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 230: 230. 原点 and 系譜
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **230. 原点 and 系譜**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-230` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-230 .

book:listing-230 a book:Listing ;
    book:sourcePath "listings/230-230-and.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 230: 230. 原点 and 系譜
""" .

# ── tcps-core-231.ttl ───────────────────────────────────────────
book:chapter-231 a book:Chapter ;
    book:number 231 ;
    book:title "231. 品質 and 現地現物" ;
    book:slug "quality-genchi-genbutsu" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/231-quality-genchi-genbutsu.md" ;
    book:sourceText """# 231. 品質 and 現地現物

> **Pattern 231 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **231. 品質 and 現地現物**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **品質 and 現地現物**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **品質 and 現地現物** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/231-231-and.rs`](../listings/231-231-and.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 231: 231. 品質 and 現地現物
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **231. 品質 and 現地現物**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-231` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-231 .

book:listing-231 a book:Listing ;
    book:sourcePath "listings/231-231-and.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 231: 231. 品質 and 現地現物
""" .

# ── tcps-core-232.ttl ───────────────────────────────────────────
book:chapter-232 a book:Chapter ;
    book:number 232 ;
    book:title "232. ポカヨケ and 五回なぜ" ;
    book:slug "pokayoke-five-whys" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/232-pokayoke-five-whys.md" ;
    book:sourceText """# 232. ポカヨケ and 五回なぜ

> **Pattern 232 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **232. ポカヨケ and 五回なぜ**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **ポカヨケ and 五回なぜ**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **ポカヨケ and 五回なぜ** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/232-232-and.rs`](../listings/232-232-and.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 232: 232. ポカヨケ and 五回なぜ
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **232. ポカヨケ and 五回なぜ**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-232` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-232 .

book:listing-232 a book:Listing ;
    book:sourcePath "listings/232-232-and.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 232: 232. ポカヨケ and 五回なぜ
""" .

# ── tcps-core-233.ttl ───────────────────────────────────────────
book:chapter-233 a book:Chapter ;
    book:number 233 ;
    book:title "233. 人間中心 and ムリムラムダ" ;
    book:slug "human-centered-waste" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/233-human-centered-waste.md" ;
    book:sourceText """# 233. 人間中心 and ムリムラムダ

> **Pattern 233 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **233. 人間中心 and ムリムラムダ**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **人間中心 and ムリムラムダ**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **人間中心 and ムリムラムダ** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/233-233-and.rs`](../listings/233-233-and.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 233: 233. 人間中心 and ムリムラムダ
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **233. 人間中心 and ムリムラムダ**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-233` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-233 .

book:listing-233 a book:Listing ;
    book:sourcePath "listings/233-233-and.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 233: 233. 人間中心 and ムリムラムダ
""" .

# ── tcps-core-234.ttl ───────────────────────────────────────────
book:chapter-234 a book:Chapter ;
    book:number 234 ;
    book:title "234. 安全, タクト, 価値流, 工程能力" ;
    book:slug "flow-capability" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/234-flow-capability.md" ;
    book:sourceText """# 234. 安全, タクト, 価値流, 工程能力

> **Pattern 234 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **234. 安全, タクト, 価値流, 工程能力**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **安全, タクト, 価値流, 工程能力**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **安全, タクト, 価値流, 工程能力** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/234-234.rs`](../listings/234-234.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 234: 234. 安全, タクト, 価値流, 工程能力
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **234. 安全, タクト, 価値流, 工程能力**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-234` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-234 .

book:listing-234 a book:Listing ;
    book:sourcePath "listings/234-234.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 234: 234. 安全, タクト, 価値流, 工程能力
""" .

# ── tcps-core-235.ttl ───────────────────────────────────────────
book:chapter-235 a book:Chapter ;
    book:number 235 ;
    book:title "235. 標準作業" ;
    book:slug "standard-work" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/235-standard-work.md" ;
    book:sourceText """# 235. 標準作業

> **Pattern 235 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **235. 標準作業**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **標準作業**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **標準作業** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/235-235.rs`](../listings/235-235.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 235: 235. 標準作業
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **235. 標準作業**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-235` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-235 .

book:listing-235 a book:Listing ;
    book:sourcePath "listings/235-235.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 235: 235. 標準作業
""" .

# ── tcps-core-236.ttl ───────────────────────────────────────────
book:chapter-236 a book:Chapter ;
    book:number 236 ;
    book:title "236. 自働化 through Typestate" ;
    book:slug "jidoka-typestate" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/236-jidoka-typestate.md" ;
    book:sourceText """# 236. 自働化 through Typestate

> **Pattern 236 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **236. 自働化 through Typestate**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **自働化 through Typestate**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **自働化 through Typestate** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/236-236-through-typestate.rs`](../listings/236-236-through-typestate.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 236: 236. 自働化 through Typestate
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **236. 自働化 through Typestate**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-236` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-236 .

book:listing-236 a book:Listing ;
    book:sourcePath "listings/236-236-through-typestate.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 236: 236. 自働化 through Typestate
""" .

# ── tcps-core-237.ttl ───────────────────────────────────────────
book:chapter-237 a book:Chapter ;
    book:number 237 ;
    book:title "237. かんばん and 必要時生産" ;
    book:slug "kanban-jit" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/237-kanban-jit.md" ;
    book:sourceText """# 237. かんばん and 必要時生産

> **Pattern 237 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **237. かんばん and 必要時生産**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **かんばん and 必要時生産**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **かんばん and 必要時生産** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/237-237-and.rs`](../listings/237-237-and.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 237: 237. かんばん and 必要時生産
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **237. かんばん and 必要時生産**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-237` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-237 .

book:listing-237 a book:Listing ;
    book:sourcePath "listings/237-237-and.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 237: 237. かんばん and 必要時生産
""" .

# ── tcps-core-238.ttl ───────────────────────────────────────────
book:chapter-238 a book:Chapter ;
    book:number 238 ;
    book:title "238. 平準化 and アンドン" ;
    book:slug "heijunka-andon" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/238-heijunka-andon.md" ;
    book:sourceText """# 238. 平準化 and アンドン

> **Pattern 238 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **238. 平準化 and アンドン**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **平準化 and アンドン**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **平準化 and アンドン** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/238-238-and.rs`](../listings/238-238-and.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 238: 238. 平準化 and アンドン
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **238. 平準化 and アンドン**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-238` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-238 .

book:listing-238 a book:Listing ;
    book:sourcePath "listings/238-238-and.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 238: 238. 平準化 and アンドン
""" .

# ── tcps-core-239.ttl ───────────────────────────────────────────
book:chapter-239 a book:Chapter ;
    book:number 239 ;
    book:title "239. 改善 and 受領証" ;
    book:slug "kaizen-receipts" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/239-kaizen-receipts.md" ;
    book:sourceText """# 239. 改善 and 受領証

> **Pattern 239 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **239. 改善 and 受領証**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **改善 and 受領証**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **改善 and 受領証** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/239-239-and.rs`](../listings/239-239-and.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 239: 239. 改善 and 受領証
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **239. 改善 and 受領証**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-239` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-239 .

book:listing-239 a book:Listing ;
    book:sourcePath "listings/239-239-and.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 239: 239. 改善 and 受領証
""" .

# ── tcps-core-240.ttl ───────────────────────────────────────────
book:chapter-240 a book:Chapter ;
    book:number 240 ;
    book:title "240. 自動選択, 青い川のダム, 全体" ;
    book:slug "auto-select-blue-river-whole" ;
    book:part "tcps-core" ;
    book:sourcePath "tcps-core/240-auto-select-blue-river-whole.md" ;
    book:sourceText """# 240. 自動選択, 青い川のダム, 全体

> **Pattern 240 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The original TPS vocabulary is the canonical semantic source, and Rust is the executable substrate that preserves its invariants. The narrow problem in this chapter is **240. 自動選択, 青い川のダム, 全体**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **自動選択, 青い川のダム, 全体**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **自動選択, 青い川のダム, 全体** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Preserve japanese identity.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Encode production invariants.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Connect history to executable states.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-graph`
- `praxis-graphlaw`
- `genesis-types-v2`
- `genesis-core-v2`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/240-240.rs`](../listings/240-240.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 240: 240. 自動選択, 青い川のダム, 全体
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The case study demonstrates that rich vocabulary and typestate can survive graph projection into a complete product.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A canonical TCPS concept is reduced to an English label, loses its invariant, or cannot be traced to generated behavior.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

This part reconstructs the twenty-four modules from 語彙 through 全体 and keeps 自働化, 必要時生産, 改善, and 青い川のダム distinct.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **240. 自動選択, 青い川のダム, 全体**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-240` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS domain ontology`
- `reference conformance`
- `generated core modules`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-core-pack/reference/製品版`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`

### Bounded standing

`PARTIAL`

### Open gap

Reference coverage is checked, but the complete current-head product replay remains required.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-240 .

book:listing-240 a book:Listing ;
    book:sourcePath "listings/240-240.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 240: 240. 自動選択, 青い川のダム, 全体
""" .

# ── tcps-failures-279.ttl ───────────────────────────────────────
book:chapter-tcps-failures-279 a book:Chapter ;
    book:number 279 ;
    book:title "279. Why Syntax Validation Was Insufficient" ;
    book:slug "syntax-insufficient" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/279-syntax-insufficient.md" ;
    book:listing book:chapter-tcps-failures-279-listing ;
    book:sourceText """# 279. Why Syntax Validation Was Insufficient

> **Pattern 279 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **279. Why Syntax Validation Was Insufficient**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Why Syntax Validation Was Insufficient**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Make **Why Syntax Validation Was Insufficient** an explicit design decision with a named owner, verifier, and refusal condition.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/279-279-why-syntax-validation-was-insufficient.yml`](../listings/279-279-why-syntax-validation-was-insufficient.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-279-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "279. Why Syntax Validation Was Insufficient"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **279. Why Syntax Validation Was Insufficient**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-279` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-279-listing a book:Listing ;
    book:sourcePath "listings/279-279-why-syntax-validation-was-insufficient.yml" ;
    book:sourceText """name: chapter-279-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py \"279. Why Syntax Validation Was Insufficient\"
""" .

# ── tcps-failures-280.ttl ───────────────────────────────────────
book:chapter-tcps-failures-280 a book:Chapter ;
    book:number 280 ;
    book:title "280. Inert Packs with No Consumption Path" ;
    book:slug "inert-packs" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/280-inert-packs.md" ;
    book:listing book:chapter-tcps-failures-280-listing ;
    book:sourceText """# 280. Inert Packs with No Consumption Path

> **Pattern 280 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **280. Inert Packs with No Consumption Path**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Inert Packs with No Consumption Path**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Inert Packs with No Consumption Path** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Reproduce the abnormality.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify the causal layer.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Change standard work.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/280-280-inert-packs-with-no-consumption-path.rs`](../listings/280-280-inert-packs-with-no-consumption-path.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 280: 280. Inert Packs with No Consumption Path
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **280. Inert Packs with No Consumption Path**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-280` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-280-listing a book:Listing ;
    book:sourcePath "listings/280-280-inert-packs-with-no-consumption-path.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 280: 280. Inert Packs with No Consumption Path
""" .

# ── tcps-failures-281.ttl ───────────────────────────────────────
book:chapter-tcps-failures-281 a book:Chapter ;
    book:number 281 ;
    book:title "281. Shared-Class Cross-Contamination" ;
    book:slug "shared-class-contamination" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/281-shared-class-contamination.md" ;
    book:listing book:chapter-tcps-failures-281-listing ;
    book:sourceText """# 281. Shared-Class Cross-Contamination

> **Pattern 281 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **281. Shared-Class Cross-Contamination**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Shared-Class Cross-Contamination**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Shared-Class Cross-Contamination** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/281-281-shared-class-cross-contamination.rs`](../listings/281-281-shared-class-cross-contamination.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 281: 281. Shared-Class Cross-Contamination
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **281. Shared-Class Cross-Contamination**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-281` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-281-listing a book:Listing ;
    book:sourcePath "listings/281-281-shared-class-cross-contamination.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 281: 281. Shared-Class Cross-Contamination
""" .

# ── tcps-failures-282.ttl ───────────────────────────────────────
book:chapter-tcps-failures-282 a book:Chapter ;
    book:number 282 ;
    book:title "282. The Missing IRI Filter" ;
    book:slug "missing-iri-filter" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/282-missing-iri-filter.md" ;
    book:listing book:chapter-tcps-failures-282-listing ;
    book:sourceText """# 282. The Missing IRI Filter

> **Pattern 282 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **282. The Missing IRI Filter**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The Missing IRI Filter**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The Missing IRI Filter** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Reproduce the abnormality.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify the causal layer.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Change standard work.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/282-282-the-missing-iri-filter.rs`](../listings/282-282-the-missing-iri-filter.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 282: 282. The Missing IRI Filter
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **282. The Missing IRI Filter**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-282` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-282-listing a book:Listing ;
    book:sourcePath "listings/282-282-the-missing-iri-filter.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 282: 282. The Missing IRI Filter
""" .

# ── tcps-failures-283.ttl ───────────────────────────────────────
book:chapter-tcps-failures-283 a book:Chapter ;
    book:number 283 ;
    book:title "283. Duplicate Output Ownership" ;
    book:slug "duplicate-output-ownership" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/283-duplicate-output-ownership.md" ;
    book:listing book:chapter-tcps-failures-283-listing ;
    book:sourceText """# 283. Duplicate Output Ownership

> **Pattern 283 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **283. Duplicate Output Ownership**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Duplicate Output Ownership**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Duplicate Output Ownership** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/283-283-duplicate-output-ownership.rs`](../listings/283-283-duplicate-output-ownership.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 283: 283. Duplicate Output Ownership
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **283. Duplicate Output Ownership**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-283` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-283-listing a book:Listing ;
    book:sourcePath "listings/283-283-duplicate-output-ownership.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 283: 283. Duplicate Output Ownership
""" .

# ── tcps-failures-284.ttl ───────────────────────────────────────
book:chapter-tcps-failures-284 a book:Chapter ;
    book:number 284 ;
    book:title "284. Eleven Packs Writing `lib.rs`" ;
    book:slug "eleven-pack-lib-collision" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/284-eleven-pack-lib-collision.md" ;
    book:listing book:chapter-tcps-failures-284-listing ;
    book:sourceText """# 284. Eleven Packs Writing `lib.rs`

> **Pattern 284 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **284. Eleven Packs Writing `lib.rs`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Eleven Packs Writing `lib.rs`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Eleven Packs Writing `lib.rs`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Reproduce the abnormality.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify the causal layer.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Change standard work.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/284-284-eleven-packs-writing-lib-rs.rs`](../listings/284-284-eleven-packs-writing-lib-rs.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 284: 284. Eleven Packs Writing `lib.rs`
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **284. Eleven Packs Writing `lib.rs`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-284` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-284-listing a book:Listing ;
    book:sourcePath "listings/284-284-eleven-packs-writing-lib-rs.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 284: 284. Eleven Packs Writing `lib.rs`
""" .

# ── tcps-failures-285.ttl ───────────────────────────────────────
book:chapter-tcps-failures-285 a book:Chapter ;
    book:number 285 ;
    book:title "285. Tera Syntax inside a Comment" ;
    book:slug "tera-comment-bug" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/285-tera-comment-bug.md" ;
    book:listing book:chapter-tcps-failures-285-listing ;
    book:sourceText """# 285. Tera Syntax inside a Comment

> **Pattern 285 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **285. Tera Syntax inside a Comment**. Template rendering is not clerical formatting. It is the final semantic projection before source text reaches the compiler.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Tera Syntax inside a Comment**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Tera Syntax inside a Comment** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Reproduce the abnormality.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify the causal layer.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Change standard work.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/285-285-tera-syntax-inside-a-comment.tmpl`](../listings/285-285-tera-syntax-inside-a-comment.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/285_285_tera_syntax_inside_a_comment.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章285 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 285: 285. Tera Syntax inside a Comment
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **285. Tera Syntax inside a Comment**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-285` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-285-listing a book:Listing ;
    book:sourcePath "listings/285-285-tera-syntax-inside-a-comment.tmpl" ;
    book:sourceText """---
to: src/generated/285_285_tera_syntax_inside_a_comment.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章285 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 285: 285. Tera Syntax inside a Comment
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-failures-286.ttl ───────────────────────────────────────
book:chapter-tcps-failures-286 a book:Chapter ;
    book:number 286 ;
    book:title "286. Wrong Cargo Package Identity" ;
    book:slug "wrong-package-identity" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/286-wrong-package-identity.md" ;
    book:listing book:chapter-tcps-failures-286-listing ;
    book:sourceText """# 286. Wrong Cargo Package Identity

> **Pattern 286 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **286. Wrong Cargo Package Identity**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Wrong Cargo Package Identity**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Wrong Cargo Package Identity** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Reproduce the abnormality.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify the causal layer.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Change standard work.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/286-286-wrong-cargo-package-identity.rs`](../listings/286-286-wrong-cargo-package-identity.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 286: 286. Wrong Cargo Package Identity
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **286. Wrong Cargo Package Identity**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-286` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-286-listing a book:Listing ;
    book:sourcePath "listings/286-286-wrong-cargo-package-identity.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 286: 286. Wrong Cargo Package Identity
""" .

# ── tcps-failures-287.ttl ───────────────────────────────────────
book:chapter-tcps-failures-287 a book:Chapter ;
    book:number 287 ;
    book:title "287. Mirrored Reference Drift" ;
    book:slug "mirrored-reference-drift" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/287-mirrored-reference-drift.md" ;
    book:listing book:chapter-tcps-failures-287-listing ;
    book:sourceText """# 287. Mirrored Reference Drift

> **Pattern 287 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **287. Mirrored Reference Drift**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Mirrored Reference Drift**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Mirrored Reference Drift** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Reproduce the abnormality.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify the causal layer.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Change standard work.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/287-287-mirrored-reference-drift.sh`](../listings/287-287-mirrored-reference-drift.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="287. Mirrored Reference Drift"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **287. Mirrored Reference Drift**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-287` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-287-listing a book:Listing ;
    book:sourcePath "listings/287-287-mirrored-reference-drift.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"287. Mirrored Reference Drift\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── tcps-failures-288.ttl ───────────────────────────────────────
book:chapter-tcps-failures-288 a book:Chapter ;
    book:number 288 ;
    book:title "288. Hidden Regression Found by `receiptctl`" ;
    book:slug "receiptctl-regression" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/288-receiptctl-regression.md" ;
    book:listing book:chapter-tcps-failures-288-listing ;
    book:sourceText """# 288. Hidden Regression Found by `receiptctl`

> **Pattern 288 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **288. Hidden Regression Found by `receiptctl`**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Hidden Regression Found by `receiptctl`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Hidden Regression Found by `receiptctl`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Reproduce the abnormality.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify the causal layer.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Change standard work.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/288-288-hidden-regression-found-by-receiptctl.json`](../listings/288-288-hidden-regression-found-by-receiptctl.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 288,
  "subject": "288. Hidden Regression Found by `receiptctl`",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **288. Hidden Regression Found by `receiptctl`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-288` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-288-listing a book:Listing ;
    book:sourcePath "listings/288-288-hidden-regression-found-by-receiptctl.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 288,
  \"subject\": \"288. Hidden Regression Found by `receiptctl`\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-failures-289.ttl ───────────────────────────────────────
book:chapter-tcps-failures-289 a book:Chapter ;
    book:number 289 ;
    book:title "289. Pre-Tool Hook Failures versus Product Failures" ;
    book:slug "hook-vs-product-failure" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/289-hook-vs-product-failure.md" ;
    book:listing book:chapter-tcps-failures-289-listing ;
    book:sourceText """# 289. Pre-Tool Hook Failures versus Product Failures

> **Pattern 289 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **289. Pre-Tool Hook Failures versus Product Failures**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Pre-Tool Hook Failures versus Product Failures**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Pre-Tool Hook Failures versus Product Failures** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Reproduce the abnormality.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Identify the causal layer.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Change standard work.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/289-289-pre-tool-hook-failures-versus-product-failures.rs`](../listings/289-289-pre-tool-hook-failures-versus-product-failures.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 289: 289. Pre-Tool Hook Failures versus Product Failures
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **289. Pre-Tool Hook Failures versus Product Failures**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-289` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-289-listing a book:Listing ;
    book:sourcePath "listings/289-289-pre-tool-hook-failures-versus-product-failures.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 289: 289. Pre-Tool Hook Failures versus Product Failures
""" .

# ── tcps-failures-290.ttl ───────────────────────────────────────
book:chapter-tcps-failures-290 a book:Chapter ;
    book:number 290 ;
    book:title "290. Turn Every Defect into a New Standard" ;
    book:slug "defect-to-standard" ;
    book:part "tcps-failures" ;
    book:sourcePath "tcps-failures/290-defect-to-standard.md" ;
    book:listing book:chapter-tcps-failures-290-listing ;
    book:sourceText """# 290. Turn Every Defect into a New Standard

> **Pattern 290 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The defect ledger is not embarrassment; it is the historical record that converts abnormality into improved standard work. The narrow problem in this chapter is **290. Turn Every Defect into a New Standard**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Turn Every Defect into a New Standard**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Turn Every Defect into a New Standard.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-cheat-scanner`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/290-290-turn-every-defect-into-a-new-standard.rs`](../listings/290-290-turn-every-defect-into-a-new-standard.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 290: 290. Turn Every Defect into a New Standard
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every discovered defect becomes a gate, negative fixture, ownership rule, or verifier improvement.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The same defect class can recur without triggering an earlier admission or verification failure.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The case study records inert packs, namespace leakage, duplicate writers, Tera self-interpretation, wrong package identity, and hidden consumer regressions.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **290. Turn Every Defect into a New Standard**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-290` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `defect-to-gate conversion`
- `negative fixtures`
- `typed refusal regression`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `crates/ggen-engine/tests`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

A defect class is closed only when sabotage reproduces the failure and the new gate catches it earlier.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-failures-290-listing a book:Listing ;
    book:sourcePath "listings/290-290-turn-every-defect-into-a-new-standard.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 290: 290. Turn Every Defect into a New Standard
""" .

# ── tcps-generation-241.ttl ─────────────────────────────────────
book:chapter-241 a book:Chapter ;
    book:number 241 ;
    book:title "241. Create `tcps-core-pack`" ;
    book:slug "create-core-pack" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/241-create-core-pack.md" ;
    book:sourceText """# 241. Create `tcps-core-pack`

> **Pattern 241 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **241. Create `tcps-core-pack`**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Create `tcps-core-pack`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Create `tcps-core-pack`.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/241-241-create-tcps-core-pack.tmpl`](../listings/241-241-create-tcps-core-pack.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/241_241_create_tcps_core_pack.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章241 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 241: 241. Create `tcps-core-pack`
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **241. Create `tcps-core-pack`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-241` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-241 .

book:listing-241 a book:Listing ;
    book:sourcePath "listings/241-241-create-tcps-core-pack.tmpl" ;
    book:sourceText """---
to: src/generated/241_241_create_tcps_core_pack.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章241 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 241: 241. Create `tcps-core-pack`
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-242.ttl ─────────────────────────────────────
book:chapter-242 a book:Chapter ;
    book:number 242 ;
    book:title "242. Transcribe the Reference into RDF" ;
    book:slug "transcribe-reference" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/242-transcribe-reference.md" ;
    book:sourceText """# 242. Transcribe the Reference into RDF

> **Pattern 242 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **242. Transcribe the Reference into RDF**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Transcribe the Reference into RDF**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Transcribe the Reference into RDF** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/242-242-transcribe-the-reference-into-rdf.ttl`](../listings/242-242-transcribe-the-reference-into-rdf.ttl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```turtle
@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章242
    a pack:生成単位 ;
    dcterms:title "242. Transcribe the Reference into RDF"@en ;
    pack:出力経路 "src/generated/242_242_transcribe_the_reference_into_rdf.rs" ;
    pack:順序 242 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **242. Transcribe the Reference into RDF**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-242` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-242 .

book:listing-242 a book:Listing ;
    book:sourcePath "listings/242-242-transcribe-the-reference-into-rdf.ttl" ;
    book:sourceText """@prefix pack: <https://example.org/ggen/pack#> .
@prefix dcterms: <http://purl.org/dc/terms/> .
@prefix prov: <http://www.w3.org/ns/prov#> .

pack:章242
    a pack:生成単位 ;
    dcterms:title "242. Transcribe the Reference into RDF"@en ;
    pack:出力経路 "src/generated/242_242_transcribe_the_reference_into_rdf.rs" ;
    pack:順序 242 ;
    pack:根拠 pack:独立参照 ;
    prov:wasDerivedFrom pack:参照実装_v26_7_19 .
""" .

# ── tcps-generation-243.ttl ─────────────────────────────────────
book:chapter-243 a book:Chapter ;
    book:number 243 ;
    book:title "243. Generate All 24 Modules" ;
    book:slug "generate-24-modules" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/243-generate-24-modules.md" ;
    book:sourceText """# 243. Generate All 24 Modules

> **Pattern 243 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **243. Generate All 24 Modules**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate All 24 Modules**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate All 24 Modules.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/243-243-generate-all-24-modules.tmpl`](../listings/243-243-generate-all-24-modules.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/243_243_generate_all_24_modules.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章243 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 243: 243. Generate All 24 Modules
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **243. Generate All 24 Modules**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-243` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-243 .

book:listing-243 a book:Listing ;
    book:sourcePath "listings/243-243-generate-all-24-modules.tmpl" ;
    book:sourceText """---
to: src/generated/243_243_generate_all_24_modules.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章243 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 243: 243. Generate All 24 Modules
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-244.ttl ─────────────────────────────────────
book:chapter-244 a book:Chapter ;
    book:number 244 ;
    book:title "244. Generate Japanese Rust Identifiers" ;
    book:slug "generate-japanese-identifiers" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/244-generate-japanese-identifiers.md" ;
    book:sourceText """# 244. Generate Japanese Rust Identifiers

> **Pattern 244 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **244. Generate Japanese Rust Identifiers**. Rust provides compile-time leverage: illegal production states can be removed from the vocabulary instead of detected after actuation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Japanese Rust Identifiers**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Japanese Rust Identifiers.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Transcribe twenty-four modules.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate the reference surface.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run 試験.rs unchanged.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/244-244-generate-japanese-rust-identifiers.tmpl`](../listings/244-244-generate-japanese-rust-identifiers.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/244_244_generate_japanese_rust_identifiers.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章244 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 244: 244. Generate Japanese Rust Identifiers
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **244. Generate Japanese Rust Identifiers**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-244` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-244 .

book:listing-244 a book:Listing ;
    book:sourcePath "listings/244-244-generate-japanese-rust-identifiers.tmpl" ;
    book:sourceText """---
to: src/generated/244_244_generate_japanese_rust_identifiers.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章244 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 244: 244. Generate Japanese Rust Identifiers
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-245.ttl ─────────────────────────────────────
book:chapter-245 a book:Chapter ;
    book:number 245 ;
    book:title "245. Generate Typestate Transitions" ;
    book:slug "generate-typestate" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/245-generate-typestate.md" ;
    book:sourceText """# 245. Generate Typestate Transitions

> **Pattern 245 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **245. Generate Typestate Transitions**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Typestate Transitions**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Typestate Transitions.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Transcribe twenty-four modules.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate the reference surface.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run 試験.rs unchanged.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/245-245-generate-typestate-transitions.tmpl`](../listings/245-245-generate-typestate-transitions.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/245_245_generate_typestate_transitions.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章245 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 245: 245. Generate Typestate Transitions
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **245. Generate Typestate Transitions**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-245` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-245 .

book:listing-245 a book:Listing ;
    book:sourcePath "listings/245-245-generate-typestate-transitions.tmpl" ;
    book:sourceText """---
to: src/generated/245_245_generate_typestate_transitions.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章245 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 245: 245. Generate Typestate Transitions
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-246.ttl ─────────────────────────────────────
book:chapter-246 a book:Chapter ;
    book:number 246 ;
    book:title "246. Generate the Original `試験.rs`" ;
    book:slug "generate-tests" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/246-generate-tests.md" ;
    book:sourceText """# 246. Generate the Original `試験.rs`

> **Pattern 246 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **246. Generate the Original `試験.rs`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate the Original `試験.rs`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate the Original `試験.rs`.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Transcribe twenty-four modules.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate the reference surface.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run 試験.rs unchanged.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/246-246-generate-the-original-rs.tmpl`](../listings/246-246-generate-the-original-rs.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/246_246_generate_the_original_rs.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章246 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 246: 246. Generate the Original `試験.rs`
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **246. Generate the Original `試験.rs`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-246` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-246 .

book:listing-246 a book:Listing ;
    book:sourcePath "listings/246-246-generate-the-original-rs.tmpl" ;
    book:sourceText """---
to: src/generated/246_246_generate_the_original_rs.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章246 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 246: 246. Generate the Original `試験.rs`
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-247.ttl ─────────────────────────────────────
book:chapter-247 a book:Chapter ;
    book:number 247 ;
    book:title "247. Run the Reference Tests Unmodified" ;
    book:slug "run-reference-tests" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/247-run-reference-tests.md" ;
    book:sourceText """# 247. Run the Reference Tests Unmodified

> **Pattern 247 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **247. Run the Reference Tests Unmodified**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Run the Reference Tests Unmodified**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Run the Reference Tests Unmodified.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/247-247-run-the-reference-tests-unmodified.tmpl`](../listings/247-247-run-the-reference-tests-unmodified.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/247_247_run_the_reference_tests_unmodified.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章247 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 247: 247. Run the Reference Tests Unmodified
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **247. Run the Reference Tests Unmodified**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-247` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-247 .

book:listing-247 a book:Listing ;
    book:sourcePath "listings/247-247-run-the-reference-tests-unmodified.tmpl" ;
    book:sourceText """---
to: src/generated/247_247_run_the_reference_tests_unmodified.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章247 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 247: 247. Run the Reference Tests Unmodified
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-248.ttl ─────────────────────────────────────
book:chapter-248 a book:Chapter ;
    book:number 248 ;
    book:title "248. The 14-of-14 Acceptance Gate" ;
    book:slug "fourteen-of-fourteen" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/248-fourteen-of-fourteen.md" ;
    book:sourceText """# 248. The 14-of-14 Acceptance Gate

> **Pattern 248 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **248. The 14-of-14 Acceptance Gate**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The 14-of-14 Acceptance Gate**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The 14-of-14 Acceptance Gate** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Transcribe twenty-four modules.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate the reference surface.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run 試験.rs unchanged.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/248-248-the-14-of-14-acceptance-gate.tmpl`](../listings/248-248-the-14-of-14-acceptance-gate.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/248_248_the_14_of_14_acceptance_gate.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章248 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 248: 248. The 14-of-14 Acceptance Gate
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **248. The 14-of-14 Acceptance Gate**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-248` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-248 .

book:listing-248 a book:Listing ;
    book:sourcePath "listings/248-248-the-14-of-14-acceptance-gate.tmpl" ;
    book:sourceText """---
to: src/generated/248_248_the_14_of_14_acceptance_gate.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章248 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 248: 248. The 14-of-14 Acceptance Gate
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-249.ttl ─────────────────────────────────────
book:chapter-249 a book:Chapter ;
    book:number 249 ;
    book:title "249. Fix the Module/Struct Shadowing Defect" ;
    book:slug "shadowing-defect" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/249-shadowing-defect.md" ;
    book:sourceText """# 249. Fix the Module/Struct Shadowing Defect

> **Pattern 249 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **249. Fix the Module/Struct Shadowing Defect**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Fix the Module/Struct Shadowing Defect**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Fix the Module/Struct Shadowing Defect** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Transcribe twenty-four modules.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate the reference surface.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run 試験.rs unchanged.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/249-249-fix-the-module-struct-shadowing-defect.tmpl`](../listings/249-249-fix-the-module-struct-shadowing-defect.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/249_249_fix_the_module_struct_shadowing_defect.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章249 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 249: 249. Fix the Module/Struct Shadowing Defect
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **249. Fix the Module/Struct Shadowing Defect**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-249` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-249 .

book:listing-249 a book:Listing ;
    book:sourcePath "listings/249-249-fix-the-module-struct-shadowing-defect.tmpl" ;
    book:sourceText """---
to: src/generated/249_249_fix_the_module_struct_shadowing_defect.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章249 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 249: 249. Fix the Module/Struct Shadowing Defect
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-250.ttl ─────────────────────────────────────
book:chapter-250 a book:Chapter ;
    book:number 250 ;
    book:title "250. Disclose the `_impl` Divergence" ;
    book:slug "impl-divergence" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/250-impl-divergence.md" ;
    book:sourceText """# 250. Disclose the `_impl` Divergence

> **Pattern 250 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **250. Disclose the `_impl` Divergence**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Disclose the `_impl` Divergence**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Disclose the `_impl` Divergence** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Transcribe twenty-four modules.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate the reference surface.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run 試験.rs unchanged.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/250-250-disclose-the-impl-divergence.tmpl`](../listings/250-250-disclose-the-impl-divergence.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/250_250_disclose_the_impl_divergence.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章250 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 250: 250. Disclose the `_impl` Divergence
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **250. Disclose the `_impl` Divergence**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-250` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-250 .

book:listing-250 a book:Listing ;
    book:sourcePath "listings/250-250-disclose-the-impl-divergence.tmpl" ;
    book:sourceText """---
to: src/generated/250_250_disclose_the_impl_divergence.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章250 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 250: 250. Disclose the `_impl` Divergence
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-251.ttl ─────────────────────────────────────
book:chapter-251 a book:Chapter ;
    book:number 251 ;
    book:title "251. Six-Case Conformance E2E" ;
    book:slug "six-case-e2e" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/251-six-case-e2e.md" ;
    book:sourceText """# 251. Six-Case Conformance E2E

> **Pattern 251 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **251. Six-Case Conformance E2E**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Six-Case Conformance E2E**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Six-Case Conformance E2E** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/251-251-six-case-conformance-e2e.tmpl`](../listings/251-251-six-case-conformance-e2e.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/251_251_six_case_conformance_e2e.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章251 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 251: 251. Six-Case Conformance E2E
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **251. Six-Case Conformance E2E**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-251` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-251 .

book:listing-251 a book:Listing ;
    book:sourcePath "listings/251-251-six-case-conformance-e2e.tmpl" ;
    book:sourceText """---
to: src/generated/251_251_six_case_conformance_e2e.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章251 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 251: 251. Six-Case Conformance E2E
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tcps-generation-252.ttl ─────────────────────────────────────
book:chapter-252 a book:Chapter ;
    book:number 252 ;
    book:title "252. Sync-Twice Idempotency" ;
    book:slug "sync-twice" ;
    book:part "tcps-generation" ;
    book:sourcePath "tcps-generation/252-sync-twice.md" ;
    book:sourceText """# 252. Sync-Twice Idempotency

> **Pattern 252 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

The TCPS core pack must regenerate the reference crate and satisfy its original tests without consumer-side repairs. The narrow problem in this chapter is **252. Sync-Twice Idempotency**. A second sync must be a no-op at the byte level unless an admitted source changed.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Sync-Twice Idempotency**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Sync-Twice Idempotency** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `praxis-graphlaw`
- `chicago-tdd-tools`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/252-252-sync-twice-idempotency.sh`](../listings/252-252-sync-twice-idempotency.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="252. Sync-Twice Idempotency"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

The pack reproduces the reference core and its tests from RDF without making generated Rust authoritative.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The original reference tests cannot run unchanged or a second sync changes the generated core.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The observed standing is a 24-module generated core with the unmodified reference tests passing 14 of 14 after disclosed shadowing repairs.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **252. Sync-Twice Idempotency**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-252` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `TCPS pack sync`
- `reference-test reuse`
- `idempotent core generation`

### Current repository evidence

- `packs/tcps-core-pack`
- `examples/tcps-generated`
- `book/scripts/check_level_five.py`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

Recorded prior runs do not substitute for the current commit's self-hosted verification result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-252 .

book:listing-252 a book:Listing ;
    book:sourcePath "listings/252-252-sync-twice-idempotency.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject="252. Sync-Twice Idempotency"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
""" .

# ── tcps-product-253.ttl ────────────────────────────────────────
book:chapter-253 a book:Chapter ;
    book:number 253 ;
    book:title "253. Move from One Crate to Five" ;
    book:slug "one-to-five-crates" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/253-one-to-five-crates.md" ;
    book:sourceText """# 253. Move from One Crate to Five

> **Pattern 253 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **253. Move from One Crate to Five**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Move from One Crate to Five**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Move from One Crate to Five** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/253-253-move-from-one-crate-to-five.rs`](../listings/253-253-move-from-one-crate-to-five.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 253: 253. Move from One Crate to Five
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **253. Move from One Crate to Five**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-253` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-253 .

book:listing-253 a book:Listing ;
    book:sourcePath "listings/253-253-move-from-one-crate-to-five.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 253: 253. Move from One Crate to Five
""" .

# ── tcps-product-254.ttl ────────────────────────────────────────
book:chapter-254 a book:Chapter ;
    book:number 254 ;
    book:title "254. `tcps-core`" ;
    book:slug "tcps-core" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/254-tcps-core.md" ;
    book:sourceText """# 254. `tcps-core`

> **Pattern 254 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **254. `tcps-core`**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`tcps-core`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`tcps-core`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/254-254-tcps-core.rs`](../listings/254-254-tcps-core.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 254: 254. `tcps-core`
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **254. `tcps-core`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-254` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-254 .

book:listing-254 a book:Listing ;
    book:sourcePath "listings/254-254-tcps-core.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 254: 254. `tcps-core`
""" .

# ── tcps-product-255.ttl ────────────────────────────────────────
book:chapter-255 a book:Chapter ;
    book:number 255 ;
    book:title "255. `tcps-std`" ;
    book:slug "tcps-std" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/255-tcps-std.md" ;
    book:sourceText """# 255. `tcps-std`

> **Pattern 255 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **255. `tcps-std`**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`tcps-std`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`tcps-std`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/255-255-tcps-std.rs`](../listings/255-255-tcps-std.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 255: 255. `tcps-std`
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **255. `tcps-std`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-255` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-255 .

book:listing-255 a book:Listing ;
    book:sourcePath "listings/255-255-tcps-std.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 255: 255. `tcps-std`
""" .

# ── tcps-product-256.ttl ────────────────────────────────────────
book:chapter-256 a book:Chapter ;
    book:number 256 ;
    book:title "256. `tcps-ffi`" ;
    book:slug "tcps-ffi" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/256-tcps-ffi.md" ;
    book:sourceText """# 256. `tcps-ffi`

> **Pattern 256 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **256. `tcps-ffi`**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`tcps-ffi`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`tcps-ffi`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/256-256-tcps-ffi.rs`](../listings/256-256-tcps-ffi.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 256: 256. `tcps-ffi`
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **256. `tcps-ffi`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-256` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-256 .

book:listing-256 a book:Listing ;
    book:sourcePath "listings/256-256-tcps-ffi.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 256: 256. `tcps-ffi`
""" .

# ── tcps-product-257.ttl ────────────────────────────────────────
book:chapter-257 a book:Chapter ;
    book:number 257 ;
    book:title "257. `tcps-wasm`" ;
    book:slug "tcps-wasm" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/257-tcps-wasm.md" ;
    book:sourceText """# 257. `tcps-wasm`

> **Pattern 257 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **257. `tcps-wasm`**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`tcps-wasm`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`tcps-wasm`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/257-257-tcps-wasm.rs`](../listings/257-257-tcps-wasm.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 257: 257. `tcps-wasm`
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **257. `tcps-wasm`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-257` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-257 .

book:listing-257 a book:Listing ;
    book:sourcePath "listings/257-257-tcps-wasm.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 257: 257. `tcps-wasm`
""" .

# ── tcps-product-258.ttl ────────────────────────────────────────
book:chapter-258 a book:Chapter ;
    book:number 258 ;
    book:title "258. `tcps-cli`" ;
    book:slug "tcps-cli" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/258-tcps-cli.md" ;
    book:sourceText """# 258. `tcps-cli`

> **Pattern 258 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **258. `tcps-cli`**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`tcps-cli`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`tcps-cli`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/258-258-tcps-cli.rs`](../listings/258-258-tcps-cli.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 258: 258. `tcps-cli`
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **258. `tcps-cli`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-258` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-258 .

book:listing-258 a book:Listing ;
    book:sourcePath "listings/258-258-tcps-cli.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 258: 258. `tcps-cli`
""" .

# ── tcps-product-259.ttl ────────────────────────────────────────
book:chapter-259 a book:Chapter ;
    book:number 259 ;
    book:title "259. Generate the Workspace `Cargo.toml`" ;
    book:slug "workspace-manifest" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/259-workspace-manifest.md" ;
    book:sourceText """# 259. Generate the Workspace `Cargo.toml`

> **Pattern 259 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **259. Generate the Workspace `Cargo.toml`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate the Workspace `Cargo.toml`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate the Workspace `Cargo.toml`.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/259-259-generate-the-workspace-cargo-toml.toml`](../listings/259-259-generate-the-workspace-cargo-toml.toml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```toml
[pack]
name = "chapter-259-259-generate-the-workspace-cargo-toml"
version = "26.7.19"
description = "259. Generate the Workspace `Cargo.toml`"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **259. Generate the Workspace `Cargo.toml`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-259` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-259 .

book:listing-259 a book:Listing ;
    book:sourcePath "listings/259-259-generate-the-workspace-cargo-toml.toml" ;
    book:sourceText """[pack]
name = "chapter-259-259-generate-the-workspace-cargo-toml"
version = "26.7.19"
description = "259. Generate the Workspace `Cargo.toml`"

[law]
ontology = "ontology.ttl"
gates = ["gates/010_admission.rq"] # SPARQL admission gates; a legacy shapes.ttl now refuses with FM-PACK-012

[verification]
independent_reference = "reference/"
sync_twice = true
refuse_unexplained_drift = true
""" .

# ── tcps-product-260.ttl ────────────────────────────────────────
book:chapter-260 a book:Chapter ;
    book:number 260 ;
    book:title "260. Generate Cross-Crate Dependencies" ;
    book:slug "cross-crate-dependencies" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/260-cross-crate-dependencies.md" ;
    book:sourceText """# 260. Generate Cross-Crate Dependencies

> **Pattern 260 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **260. Generate Cross-Crate Dependencies**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Cross-Crate Dependencies**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Cross-Crate Dependencies.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/260-260-generate-cross-crate-dependencies.yml`](../listings/260-260-generate-cross-crate-dependencies.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-260-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "260. Generate Cross-Crate Dependencies"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **260. Generate Cross-Crate Dependencies**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-260` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-260 .

book:listing-260 a book:Listing ;
    book:sourcePath "listings/260-260-generate-cross-crate-dependencies.yml" ;
    book:sourceText """name: chapter-260-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "260. Generate Cross-Crate Dependencies"
""" .

# ── tcps-product-261.ttl ────────────────────────────────────────
book:chapter-261 a book:Chapter ;
    book:number 261 ;
    book:title "261. Generate C Headers and Smoke Tests" ;
    book:slug "c-headers-smoke" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/261-c-headers-smoke.md" ;
    book:sourceText """# 261. Generate C Headers and Smoke Tests

> **Pattern 261 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **261. Generate C Headers and Smoke Tests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate C Headers and Smoke Tests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate C Headers and Smoke Tests.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/261-261-generate-c-headers-and-smoke-tests.rs`](../listings/261-261-generate-c-headers-and-smoke-tests.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 261: 261. Generate C Headers and Smoke Tests
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **261. Generate C Headers and Smoke Tests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-261` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-261 .

book:listing-261 a book:Listing ;
    book:sourcePath "listings/261-261-generate-c-headers-and-smoke-tests.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 261: 261. Generate C Headers and Smoke Tests
""" .

# ── tcps-product-262.ttl ────────────────────────────────────────
book:chapter-262 a book:Chapter ;
    book:number 262 ;
    book:title "262. Generate WASM Interfaces and Smoke Tests" ;
    book:slug "wasm-smoke" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/262-wasm-smoke.md" ;
    book:sourceText """# 262. Generate WASM Interfaces and Smoke Tests

> **Pattern 262 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **262. Generate WASM Interfaces and Smoke Tests**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate WASM Interfaces and Smoke Tests**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate WASM Interfaces and Smoke Tests.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/262-262-generate-wasm-interfaces-and-smoke-tests.rs`](../listings/262-262-generate-wasm-interfaces-and-smoke-tests.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 262: 262. Generate WASM Interfaces and Smoke Tests
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **262. Generate WASM Interfaces and Smoke Tests**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-262` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-262 .

book:listing-262 a book:Listing ;
    book:sourcePath "listings/262-262-generate-wasm-interfaces-and-smoke-tests.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 262: 262. Generate WASM Interfaces and Smoke Tests
""" .

# ── tcps-product-263.ttl ────────────────────────────────────────
book:chapter-263 a book:Chapter ;
    book:number 263 ;
    book:title "263. Generate CLI Acceptance Paths" ;
    book:slug "cli-acceptance" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/263-cli-acceptance.md" ;
    book:sourceText """# 263. Generate CLI Acceptance Paths

> **Pattern 263 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **263. Generate CLI Acceptance Paths**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate CLI Acceptance Paths**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate CLI Acceptance Paths.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Split crate responsibilities.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate interface products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Run cross-crate tests.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/263-263-generate-cli-acceptance-paths.rs`](../listings/263-263-generate-cli-acceptance-paths.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 263: 263. Generate CLI Acceptance Paths
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **263. Generate CLI Acceptance Paths**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-263` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-263 .

book:listing-263 a book:Listing ;
    book:sourcePath "listings/263-263-generate-cli-acceptance-paths.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 263: 263. Generate CLI Acceptance Paths
""" .

# ── tcps-product-264.ttl ────────────────────────────────────────
book:chapter-264 a book:Chapter ;
    book:number 264 ;
    book:title "264. Run the Richer Product Test Suite" ;
    book:slug "product-test-suite" ;
    book:part "tcps-product" ;
    book:sourcePath "tcps-product/264-product-test-suite.md" ;
    book:sourceText """# 264. Run the Richer Product Test Suite

> **Pattern 264 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Level Five depth extends from one module set to every crate and interface required by the product. The narrow problem in this chapter is **264. Run the Richer Product Test Suite**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Run the Richer Product Test Suite**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Run the Richer Product Test Suite.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/264-264-run-the-richer-product-test-suite.rs`](../listings/264-264-run-the-richer-product-test-suite.rs). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```rust
#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 264: 264. Run the Richer Product Test Suite
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Core, standard-library, FFI, WASM, CLI, and workspace surfaces are generated as one coherent product graph.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Any target requires manual cross-crate wiring or an independently maintained manifest.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The product includes no_std core logic, std adapters, stable C ABI, WASM boundary, and a CLI consumer.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **264. Run the Richer Product Test Suite**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-264` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `multi-crate product projection`
- `workspace manifests`
- `target surfaces`

### Current repository evidence

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `examples/tcps-generated`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`

### Bounded standing

`PARTIAL`

### Open gap

Every target family must be built and tested before complete-substitute standing.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-264 .

book:listing-264 a book:Listing ;
    book:sourcePath "listings/264-264-run-the-richer-product-test-suite.rs" ;
    book:sourceText """#![forbid(unsafe_code)]

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 稼働中;
#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub struct 停止中;

#[derive(Clone, Copy, Debug, PartialEq, Eq)]
pub enum 品質判定 { 正常, 異常 }

pub struct 生産線<状態> { 工程番号: u16, 状態: core::marker::PhantomData<状態> }

impl 生産線<稼働中> {
    pub const fn 新設(工程番号: u16) -> Self {
        Self { 工程番号, 状態: core::marker::PhantomData }
    }

    pub fn 作業する(self, 判定: 品質判定) -> Result<Self, 生産線<停止中>> {
        match 判定 {
            品質判定::正常 => Ok(self),
            品質判定::異常 => Err(生産線 {
                工程番号: self.工程番号,
                状態: core::marker::PhantomData,
            }),
        }
    }
}

// Chapter 264: 264. Run the Richer Product Test Suite
""" .

# ── tcps-release-265.ttl ────────────────────────────────────────
book:chapter-265 a book:Chapter ;
    book:number 265 ;
    book:title "265. Create `tcps-release-pack`" ;
    book:slug "create-release-pack" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/265-create-release-pack.md" ;
    book:sourceText """# 265. Create `tcps-release-pack`

> **Pattern 265 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **265. Create `tcps-release-pack`**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation. Release scaffolding is part of the product whenever downstream users require it to build, verify, package, or attest the subsystem.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Create `tcps-release-pack`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Create `tcps-release-pack`.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/265-265-create-tcps-release-pack.yml`](../listings/265-265-create-tcps-release-pack.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-265-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "265. Create `tcps-release-pack`"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **265. Create `tcps-release-pack`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-265` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-265 .

book:listing-265 a book:Listing ;
    book:sourcePath "listings/265-265-create-tcps-release-pack.yml" ;
    book:sourceText """name: chapter-265-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "265. Create `tcps-release-pack`"
""" .

# ── tcps-release-266.ttl ────────────────────────────────────────
book:chapter-266 a book:Chapter ;
    book:number 266 ;
    book:title "266. Inventory All 132 Product Files" ;
    book:slug "inventory-132-files" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/266-inventory-132-files.md" ;
    book:sourceText """# 266. Inventory All 132 Product Files

> **Pattern 266 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **266. Inventory All 132 Product Files**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Inventory All 132 Product Files**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Inventory All 132 Product Files.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/266-266-inventory-all-132-product-files.json`](../listings/266-266-inventory-all-132-product-files.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 266,
  "subject": "266. Inventory All 132 Product Files",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **266. Inventory All 132 Product Files**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-266` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-266 .

book:listing-266 a book:Listing ;
    book:sourcePath "listings/266-266-inventory-all-132-product-files.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 266,
  "subject": "266. Inventory All 132 Product Files",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

# ── tcps-release-267.ttl ────────────────────────────────────────
book:chapter-267 a book:Chapter ;
    book:number 267 ;
    book:title "267. Generate Seven GitHub Workflows" ;
    book:slug "github-workflows" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/267-github-workflows.md" ;
    book:sourceText """# 267. Generate Seven GitHub Workflows

> **Pattern 267 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **267. Generate Seven GitHub Workflows**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Seven GitHub Workflows**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Seven GitHub Workflows.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/267-267-generate-seven-github-workflows.yml`](../listings/267-267-generate-seven-github-workflows.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-267-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "267. Generate Seven GitHub Workflows"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **267. Generate Seven GitHub Workflows**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-267` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-267 .

book:listing-267 a book:Listing ;
    book:sourcePath "listings/267-267-generate-seven-github-workflows.yml" ;
    book:sourceText """name: chapter-267-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "267. Generate Seven GitHub Workflows"
""" .

# ── tcps-release-268.ttl ────────────────────────────────────────
book:chapter-268 a book:Chapter ;
    book:number 268 ;
    book:title "268. Generate GitLab CI" ;
    book:slug "gitlab-ci" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/268-gitlab-ci.md" ;
    book:sourceText """# 268. Generate GitLab CI

> **Pattern 268 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **268. Generate GitLab CI**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate GitLab CI**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate GitLab CI.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/268-268-generate-gitlab-ci.yml`](../listings/268-268-generate-gitlab-ci.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-268-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "268. Generate GitLab CI"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **268. Generate GitLab CI**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-268` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-268 .

book:listing-268 a book:Listing ;
    book:sourcePath "listings/268-268-generate-gitlab-ci.yml" ;
    book:sourceText """name: chapter-268-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "268. Generate GitLab CI"
""" .

# ── tcps-release-269.ttl ────────────────────────────────────────
book:chapter-269 a book:Chapter ;
    book:number 269 ;
    book:title "269. Generate Azure Pipelines" ;
    book:slug "azure-pipelines" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/269-azure-pipelines.md" ;
    book:sourceText """# 269. Generate Azure Pipelines

> **Pattern 269 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **269. Generate Azure Pipelines**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Azure Pipelines**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Azure Pipelines.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/269-269-generate-azure-pipelines.yml`](../listings/269-269-generate-azure-pipelines.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-269-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "269. Generate Azure Pipelines"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **269. Generate Azure Pipelines**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-269` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-269 .

book:listing-269 a book:Listing ;
    book:sourcePath "listings/269-269-generate-azure-pipelines.yml" ;
    book:sourceText """name: chapter-269-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "269. Generate Azure Pipelines"
""" .

# ── tcps-release-270.ttl ────────────────────────────────────────
book:chapter-270 a book:Chapter ;
    book:number 270 ;
    book:title "270. Generate Tier-One and Tier-Two Matrices" ;
    book:slug "tier-matrices" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/270-tier-matrices.md" ;
    book:sourceText """# 270. Generate Tier-One and Tier-Two Matrices

> **Pattern 270 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **270. Generate Tier-One and Tier-Two Matrices**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Tier-One and Tier-Two Matrices**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Tier-One and Tier-Two Matrices.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/270-270-generate-tier-one-and-tier-two-matrices.yml`](../listings/270-270-generate-tier-one-and-tier-two-matrices.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-270-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "270. Generate Tier-One and Tier-Two Matrices"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **270. Generate Tier-One and Tier-Two Matrices**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-270` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-270 .

book:listing-270 a book:Listing ;
    book:sourcePath "listings/270-270-generate-tier-one-and-tier-two-matrices.yml" ;
    book:sourceText """name: chapter-270-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "270. Generate Tier-One and Tier-Two Matrices"
""" .

# ── tcps-release-271.ttl ────────────────────────────────────────
book:chapter-271 a book:Chapter ;
    book:number 271 ;
    book:title "271. Discover Tier-Three Targets" ;
    book:slug "tier-three-discovery" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/271-tier-three-discovery.md" ;
    book:sourceText """# 271. Discover Tier-Three Targets

> **Pattern 271 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **271. Discover Tier-Three Targets**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Discover Tier-Three Targets**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Discover Tier-Three Targets** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/271-271-discover-tier-three-targets.yml`](../listings/271-271-discover-tier-three-targets.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-271-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "271. Discover Tier-Three Targets"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **271. Discover Tier-Three Targets**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-271` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-271 .

book:listing-271 a book:Listing ;
    book:sourcePath "listings/271-271-discover-tier-three-targets.yml" ;
    book:sourceText """name: chapter-271-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "271. Discover Tier-Three Targets"
""" .

# ── tcps-release-272.ttl ────────────────────────────────────────
book:chapter-272 a book:Chapter ;
    book:number 272 ;
    book:title "272. Generate Debian and RPM Packaging" ;
    book:slug "linux-packaging" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/272-linux-packaging.md" ;
    book:sourceText """# 272. Generate Debian and RPM Packaging

> **Pattern 272 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **272. Generate Debian and RPM Packaging**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Debian and RPM Packaging**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Debian and RPM Packaging.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/272-272-generate-debian-and-rpm-packaging.yml`](../listings/272-272-generate-debian-and-rpm-packaging.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-272-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "272. Generate Debian and RPM Packaging"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **272. Generate Debian and RPM Packaging**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-272` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-272 .

book:listing-272 a book:Listing ;
    book:sourcePath "listings/272-272-generate-debian-and-rpm-packaging.yml" ;
    book:sourceText """name: chapter-272-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "272. Generate Debian and RPM Packaging"
""" .

# ── tcps-release-273.ttl ────────────────────────────────────────
book:chapter-273 a book:Chapter ;
    book:number 273 ;
    book:title "273. Generate npm and NuGet Packaging" ;
    book:slug "npm-nuget" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/273-npm-nuget.md" ;
    book:sourceText """# 273. Generate npm and NuGet Packaging

> **Pattern 273 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **273. Generate npm and NuGet Packaging**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate npm and NuGet Packaging**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate npm and NuGet Packaging.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/273-273-generate-npm-and-nuget-packaging.yml`](../listings/273-273-generate-npm-and-nuget-packaging.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-273-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "273. Generate npm and NuGet Packaging"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **273. Generate npm and NuGet Packaging**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-273` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-273 .

book:listing-273 a book:Listing ;
    book:sourcePath "listings/273-273-generate-npm-and-nuget-packaging.yml" ;
    book:sourceText """name: chapter-273-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "273. Generate npm and NuGet Packaging"
""" .

# ── tcps-release-274.ttl ────────────────────────────────────────
book:chapter-274 a book:Chapter ;
    book:number 274 ;
    book:title "274. Generate Android Packaging" ;
    book:slug "android-packaging" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/274-android-packaging.md" ;
    book:sourceText """# 274. Generate Android Packaging

> **Pattern 274 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **274. Generate Android Packaging**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Android Packaging**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Android Packaging.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/274-274-generate-android-packaging.yml`](../listings/274-274-generate-android-packaging.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-274-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "274. Generate Android Packaging"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **274. Generate Android Packaging**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-274` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-274 .

book:listing-274 a book:Listing ;
    book:sourcePath "listings/274-274-generate-android-packaging.yml" ;
    book:sourceText """name: chapter-274-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "274. Generate Android Packaging"
""" .

# ── tcps-release-275.ttl ────────────────────────────────────────
book:chapter-275 a book:Chapter ;
    book:number 275 ;
    book:title "275. Generate Apple Packaging" ;
    book:slug "apple-packaging" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/275-apple-packaging.md" ;
    book:sourceText """# 275. Generate Apple Packaging

> **Pattern 275 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **275. Generate Apple Packaging**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Apple Packaging**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Apple Packaging.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/275-275-generate-apple-packaging.yml`](../listings/275-275-generate-apple-packaging.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-275-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "275. Generate Apple Packaging"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **275. Generate Apple Packaging**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-275` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-275 .

book:listing-275 a book:Listing ;
    book:sourcePath "listings/275-275-generate-apple-packaging.yml" ;
    book:sourceText """name: chapter-275-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "275. Generate Apple Packaging"
""" .

# ── tcps-release-276.ttl ────────────────────────────────────────
book:chapter-276 a book:Chapter ;
    book:number 276 ;
    book:title "276. Generate Docker and Compose Assets" ;
    book:slug "docker-compose" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/276-docker-compose.md" ;
    book:sourceText """# 276. Generate Docker and Compose Assets

> **Pattern 276 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **276. Generate Docker and Compose Assets**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Docker and Compose Assets**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Docker and Compose Assets.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/276-276-generate-docker-and-compose-assets.yml`](../listings/276-276-generate-docker-and-compose-assets.yml). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```yaml
name: chapter-276-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "276. Generate Docker and Compose Assets"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **276. Generate Docker and Compose Assets**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-276` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-276 .

book:listing-276 a book:Listing ;
    book:sourcePath "listings/276-276-generate-docker-and-compose-assets.yml" ;
    book:sourceText """name: chapter-276-verification
on:
  pull_request:
  push:
    branches: [main]
jobs:
  verify:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4
      - run: ggen sync run
      - run: cargo test --all-targets
      - run: python3 scripts/verify_idempotency.py
      - run: python3 scripts/write_receipt.py "276. Generate Docker and Compose Assets"
""" .

# ── tcps-release-277.ttl ────────────────────────────────────────
book:chapter-277 a book:Chapter ;
    book:number 277 ;
    book:title "277. Generate Lifecycle Scripts" ;
    book:slug "lifecycle-scripts" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/277-lifecycle-scripts.md" ;
    book:sourceText """# 277. Generate Lifecycle Scripts

> **Pattern 277 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **277. Generate Lifecycle Scripts**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate Lifecycle Scripts**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate Lifecycle Scripts.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Inventory 132 files.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Generate platform products.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Emit supply-chain evidence.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/277-277-generate-lifecycle-scripts.sh`](../listings/277-277-generate-lifecycle-scripts.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="277. Generate Lifecycle Scripts"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **277. Generate Lifecycle Scripts**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-277` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-277 .

book:listing-277 a book:Listing ;
    book:sourcePath "listings/277-277-generate-lifecycle-scripts.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject="277. Generate Lifecycle Scripts"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
""" .

# ── tcps-release-278.ttl ────────────────────────────────────────
book:chapter-278 a book:Chapter ;
    book:number 278 ;
    book:title "278. Generate SBOM, Provenance, and Signing Paths" ;
    book:slug "supply-chain-assets" ;
    book:part "tcps-release" ;
    book:sourcePath "tcps-release/278-supply-chain-assets.md" ;
    book:sourceText """# 278. Generate SBOM, Provenance, and Signing Paths

> **Pattern 278 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A complete substitute includes the release machinery that makes the product buildable and distributable across its declared platforms. The narrow problem in this chapter is **278. Generate SBOM, Provenance, and Signing Paths**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate SBOM, Provenance, and Signing Paths**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate SBOM, Provenance, and Signing Paths.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `cargo-cicd`
- `ggen-engine`
- `ggen-marketplace`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/278-278-generate-sbom-provenance-and-signing-paths.json`](../listings/278-278-generate-sbom-provenance-and-signing-paths.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 278,
  "subject": "278. Generate SBOM, Provenance, and Signing Paths",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Release workflows, packages, matrices, SBOMs, attestations, and signing paths remain projections of one admitted release model.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

A supported target exists in prose or code but is absent from the release inventory, generated assets, or provenance chain.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

The reference release contains 132 files spanning CI, platform matrices, packaging, containers, lifecycle scripts, SBOM, provenance, and signing paths.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **278. Generate SBOM, Provenance, and Signing Paths**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-278` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `release asset generation`
- `SBOM/provenance paths`
- `delivery matrices`

### Current repository evidence

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`
- `examples/tcps-generated/scripts/verify.sh`

### Pack witnesses

- `packs/tcps-release-pack`
- `packs/cargo-cicd-pack`

### Bounded standing

`PARTIAL`

### Open gap

Unavailable external SDKs or signers must remain explicit bounded outcomes, never false green.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-278 .

book:listing-278 a book:Listing ;
    book:sourcePath "listings/278-278-generate-sbom-provenance-and-signing-paths.json" ;
    book:sourceText """{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 278,
  "subject": "278. Generate SBOM, Provenance, and Signing Paths",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
""" .

# ── tcps-standing-291.ttl ───────────────────────────────────────
book:chapter-tcps-standing-291 a book:Chapter ;
    book:number 291 ;
    book:title "291. Generate the Inspection Receipt" ;
    book:slug "inspection-receipt" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/291-inspection-receipt.md" ;
    book:listing book:chapter-tcps-standing-291-listing ;
    book:sourceText """# 291. Generate the Inspection Receipt

> **Pattern 291 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **291. Generate the Inspection Receipt**. The receipt binds the exact inputs, tool identity, outputs, checks, and outcome without converting missing evidence into success.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generate the Inspection Receipt**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Generate the Inspection Receipt.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/291-291-generate-the-inspection-receipt.json`](../listings/291-291-generate-the-inspection-receipt.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 291,
  "subject": "291. Generate the Inspection Receipt",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **291. Generate the Inspection Receipt**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-291` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-291-listing a book:Listing ;
    book:sourcePath "listings/291-291-generate-the-inspection-receipt.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 291,
  \"subject\": \"291. Generate the Inspection Receipt\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-292.ttl ───────────────────────────────────────
book:chapter-tcps-standing-292 a book:Chapter ;
    book:number 292 ;
    book:title "292. Record `cargo fmt --check`" ;
    book:slug "format-receipt" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/292-format-receipt.md" ;
    book:listing book:chapter-tcps-standing-292-listing ;
    book:sourceText """# 292. Record `cargo fmt --check`

> **Pattern 292 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **292. Record `cargo fmt --check`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Record `cargo fmt --check`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Record `cargo fmt --check`.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Run real gates.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record pass or failure.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Close the evidence matrix.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/292-292-record-cargo-fmt-check.json`](../listings/292-292-record-cargo-fmt-check.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 292,
  "subject": "292. Record `cargo fmt --check`",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **292. Record `cargo fmt --check`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-292` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-292-listing a book:Listing ;
    book:sourcePath "listings/292-292-record-cargo-fmt-check.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 292,
  \"subject\": \"292. Record `cargo fmt --check`\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-293.ttl ───────────────────────────────────────
book:chapter-tcps-standing-293 a book:Chapter ;
    book:number 293 ;
    book:title "293. Record `cargo test --all-targets`" ;
    book:slug "test-receipt" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/293-test-receipt.md" ;
    book:listing book:chapter-tcps-standing-293-listing ;
    book:sourceText """# 293. Record `cargo test --all-targets`

> **Pattern 293 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **293. Record `cargo test --all-targets`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Record `cargo test --all-targets`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Record `cargo test --all-targets`.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Run real gates.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record pass or failure.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Close the evidence matrix.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/293-293-record-cargo-test-all-targets.json`](../listings/293-293-record-cargo-test-all-targets.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 293,
  "subject": "293. Record `cargo test --all-targets`",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **293. Record `cargo test --all-targets`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-293` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-293-listing a book:Listing ;
    book:sourcePath "listings/293-293-record-cargo-test-all-targets.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 293,
  \"subject\": \"293. Record `cargo test --all-targets`\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-294.ttl ───────────────────────────────────────
book:chapter-tcps-standing-294 a book:Chapter ;
    book:number 294 ;
    book:title "294. Record `cargo clippy -- -D warnings`" ;
    book:slug "clippy-receipt" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/294-clippy-receipt.md" ;
    book:listing book:chapter-tcps-standing-294-listing ;
    book:sourceText """# 294. Record `cargo clippy -- -D warnings`

> **Pattern 294 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **294. Record `cargo clippy -- -D warnings`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Record `cargo clippy -- -D warnings`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Record `cargo clippy -- -D warnings`.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Run real gates.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record pass or failure.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Close the evidence matrix.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/294-294-record-cargo-clippy-d-warnings.json`](../listings/294-294-record-cargo-clippy-d-warnings.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 294,
  "subject": "294. Record `cargo clippy -- -D warnings`",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **294. Record `cargo clippy -- -D warnings`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-294` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-294-listing a book:Listing ;
    book:sourcePath "listings/294-294-record-cargo-clippy-d-warnings.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 294,
  \"subject\": \"294. Record `cargo clippy -- -D warnings`\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-295.ttl ───────────────────────────────────────
book:chapter-tcps-standing-295 a book:Chapter ;
    book:number 295 ;
    book:title "295. Record Missing SDKs Honestly" ;
    book:slug "missing-sdks" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/295-missing-sdks.md" ;
    book:listing book:chapter-tcps-standing-295-listing ;
    book:sourceText """# 295. Record Missing SDKs Honestly

> **Pattern 295 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **295. Record Missing SDKs Honestly**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Record Missing SDKs Honestly**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Record Missing SDKs Honestly.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Run real gates.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record pass or failure.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Close the evidence matrix.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/295-295-record-missing-sdks-honestly.json`](../listings/295-295-record-missing-sdks-honestly.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 295,
  "subject": "295. Record Missing SDKs Honestly",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **295. Record Missing SDKs Honestly**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-295` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-295-listing a book:Listing ;
    book:sourcePath "listings/295-295-record-missing-sdks-honestly.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 295,
  \"subject\": \"295. Record Missing SDKs Honestly\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-296.ttl ───────────────────────────────────────
book:chapter-tcps-standing-296 a book:Chapter ;
    book:number 296 ;
    book:title "296. Regenerate `MANIFEST.256`" ;
    book:slug "regenerate-manifest" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/296-regenerate-manifest.md" ;
    book:listing book:chapter-tcps-standing-296-listing ;
    book:sourceText """# 296. Regenerate `MANIFEST.256`

> **Pattern 296 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **296. Regenerate `MANIFEST.256`**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Regenerate `MANIFEST.256`**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Regenerate `MANIFEST.256`** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Run real gates.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record pass or failure.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Close the evidence matrix.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/296-296-regenerate-manifest-256.json`](../listings/296-296-regenerate-manifest-256.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 296,
  "subject": "296. Regenerate `MANIFEST.256`",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **296. Regenerate `MANIFEST.256`**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-296` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-296-listing a book:Listing ;
    book:sourcePath "listings/296-296-regenerate-manifest-256.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 296,
  \"subject\": \"296. Regenerate `MANIFEST.256`\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-297.ttl ───────────────────────────────────────
book:chapter-tcps-standing-297 a book:Chapter ;
    book:number 297 ;
    book:title "297. Compare Generated and Reference Trees" ;
    book:slug "tree-comparison" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/297-tree-comparison.md" ;
    book:listing book:chapter-tcps-standing-297-listing ;
    book:sourceText """# 297. Compare Generated and Reference Trees

> **Pattern 297 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **297. Compare Generated and Reference Trees**. The reference must remain outside the generator's control; otherwise agreement can be manufactured without fidelity.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Compare Generated and Reference Trees**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Compare Generated and Reference Trees.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/297-297-compare-generated-and-reference-trees.json`](../listings/297-297-compare-generated-and-reference-trees.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 297,
  "subject": "297. Compare Generated and Reference Trees",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **297. Compare Generated and Reference Trees**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-297` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-297-listing a book:Listing ;
    book:sourcePath "listings/297-297-compare-generated-and-reference-trees.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 297,
  \"subject\": \"297. Compare Generated and Reference Trees\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-298.ttl ───────────────────────────────────────
book:chapter-tcps-standing-298 a book:Chapter ;
    book:number 298 ;
    book:title "298. Record Corrective Divergences" ;
    book:slug "corrective-divergences" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/298-corrective-divergences.md" ;
    book:listing book:chapter-tcps-standing-298-listing ;
    book:sourceText """# 298. Record Corrective Divergences

> **Pattern 298 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **298. Record Corrective Divergences**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Record Corrective Divergences**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Record Corrective Divergences.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Run real gates.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record pass or failure.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Close the evidence matrix.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/298-298-record-corrective-divergences.json`](../listings/298-298-record-corrective-divergences.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 298,
  "subject": "298. Record Corrective Divergences",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **298. Record Corrective Divergences**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-298` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-298-listing a book:Listing ;
    book:sourcePath "listings/298-298-record-corrective-divergences.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 298,
  \"subject\": \"298. Record Corrective Divergences\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-299.ttl ───────────────────────────────────────
book:chapter-tcps-standing-299 a book:Chapter ;
    book:number 299 ;
    book:title "299. Score Every Maturity Dimension" ;
    book:slug "score-every-dimension" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/299-score-every-dimension.md" ;
    book:listing book:chapter-tcps-standing-299-listing ;
    book:sourceText """# 299. Score Every Maturity Dimension

> **Pattern 299 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **299. Score Every Maturity Dimension**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Score Every Maturity Dimension**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Score Every Maturity Dimension.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Run real gates.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record pass or failure.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Close the evidence matrix.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/299-299-score-every-maturity-dimension.json`](../listings/299-299-score-every-maturity-dimension.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 299,
  "subject": "299. Score Every Maturity Dimension",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **299. Score Every Maturity Dimension**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-299` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-299-listing a book:Listing ;
    book:sourcePath "listings/299-299-score-every-maturity-dimension.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 299,
  \"subject\": \"299. Score Every Maturity Dimension\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-300.ttl ───────────────────────────────────────
book:chapter-tcps-standing-300 a book:Chapter ;
    book:number 300 ;
    book:title "300. Distinguish Prototype and Product Standing" ;
    book:slug "prototype-vs-product" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/300-prototype-vs-product.md" ;
    book:listing book:chapter-tcps-standing-300-listing ;
    book:sourceText """# 300. Distinguish Prototype and Product Standing

> **Pattern 300 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **300. Distinguish Prototype and Product Standing**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Distinguish Prototype and Product Standing**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Distinguish Prototype and Product Standing** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Run real gates.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Record pass or failure.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Close the evidence matrix.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/300-300-distinguish-prototype-and-product-standing.json`](../listings/300-300-distinguish-prototype-and-product-standing.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 300,
  "subject": "300. Distinguish Prototype and Product Standing",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **300. Distinguish Prototype and Product Standing**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-300` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-300-listing a book:Listing ;
    book:sourcePath "listings/300-300-distinguish-prototype-and-product-standing.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 300,
  \"subject\": \"300. Distinguish Prototype and Product Standing\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-301.ttl ───────────────────────────────────────
book:chapter-tcps-standing-301 a book:Chapter ;
    book:number 301 ;
    book:title "301. Close the Level Five Evidence Matrix" ;
    book:slug "close-evidence-matrix" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/301-close-evidence-matrix.md" ;
    book:listing book:chapter-tcps-standing-301-listing ;
    book:sourceText """# 301. Close the Level Five Evidence Matrix

> **Pattern 301 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **301. Close the Level Five Evidence Matrix**. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Close the Level Five Evidence Matrix**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

**Close the Level Five Evidence Matrix.** Perform the named operation at its owning layer, and preserve the evidence needed to replay both acceptance and refusal.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/301-301-close-the-level-five-evidence-matrix.json`](../listings/301-301-close-the-level-five-evidence-matrix.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 301,
  "subject": "301. Close the Level Five Evidence Matrix",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **301. Close the Level Five Evidence Matrix**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-301` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-301-listing a book:Listing ;
    book:sourcePath "listings/301-301-close-the-level-five-evidence-matrix.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 301,
  \"subject\": \"301. Close the Level Five Evidence Matrix\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tcps-standing-302.ttl ───────────────────────────────────────
book:chapter-tcps-standing-302 a book:Chapter ;
    book:number 302 ;
    book:title "302. The TCPS Level Five Acceptance Test" ;
    book:slug "tcps-l5-acceptance" ;
    book:part "tcps-standing" ;
    book:sourcePath "tcps-standing/302-tcps-l5-acceptance.md" ;
    book:listing book:chapter-tcps-standing-302-listing ;
    book:sourceText """# 302. The TCPS Level Five Acceptance Test

> **Pattern 302 · A Complete Pattern in Practice: TCPS**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

Standing is the conjunction of named evidence, not the confidence of the implementation agent. The narrow problem in this chapter is **302. The TCPS Level Five Acceptance Test**. The TCPS case study uses Japanese domain identities so translation cannot silently collapse 自働化 into generic automation. Level Five means a zero-knowledge consumer receives a finished, tested, receipted subsystem from pack plus ggen alone.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **The TCPS Level Five Acceptance Test**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[\\text{pack}+\\text{ggen}\\rightarrow\\text{complete subsystem}+\\text{independent proof}+\\text{safe regeneration}+\\text{receipts}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **The TCPS Level Five Acceptance Test** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `praxis-core`
- `ggen-engine`
- `cargo-cicd`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/302-302-the-tcps-level-five-acceptance-test.json`](../listings/302-302-the-tcps-level-five-acceptance-test.json). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```json
{
  "schema": "https://example.org/ggen/receipt/v1",
  "chapter": 302,
  "subject": "302. The TCPS Level Five Acceptance Test",
  "version": "v26.7.19",
  "inputs": {
    "ontology_sha256": "<record-at-run-time>",
    "template_sha256": "<record-at-run-time>"
  },
  "checks": [
    "shape-admission",
    "sync",
    "compile",
    "test",
    "sync-twice",
    "manifest-diff"
  ],
  "outcome": "must-be-recorded-not-assumed"
}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Each maturity claim names exact evidence, omissions, corrective divergences, and replay commands.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

The claimed standing survives after deleting or invalidating one of its named receipts.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

Prototype standing and full-product standing remain separate until the five-crate tree, 132-file release surface, and inspection receipts are all reproduced.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **302. The TCPS Level Five Acceptance Test**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-302` is created by the declared pack path.
- [ ] The result is compared with the TCPS v26.7.19 reference or an explicitly named derivative oracle.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **A Complete Pattern in Practice: TCPS** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `inspection receipts`
- `reference-tree comparison`
- `evidence matrix closure`

### Current repository evidence

- `book/scripts/check_level_five.py`
- `packs/tcps-core-pack/source-manifest.json`
- `packs/tcps-release-pack/source-manifest.json`

### Pack witnesses

- `packs/tcps-core-pack`
- `packs/tcps-release-pack`
- `packs/ggen-verify-pack`

### Bounded standing

`PARTIAL`

### Open gap

The standing ceiling is the weakest current-head evidence cell, not the strongest historical result.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:chapter-tcps-standing-302-listing a book:Listing ;
    book:sourcePath "listings/302-302-the-tcps-level-five-acceptance-test.json" ;
    book:sourceText """{
  \"schema\": \"https://example.org/ggen/receipt/v1\",
  \"chapter\": 302,
  \"subject\": \"302. The TCPS Level Five Acceptance Test\",
  \"version\": \"v26.7.19\",
  \"inputs\": {
    \"ontology_sha256\": \"<record-at-run-time>\",
    \"template_sha256\": \"<record-at-run-time>\"
  },
  \"checks\": [
    \"shape-admission\",
    \"sync\",
    \"compile\",
    \"test\",
    \"sync-twice\",
    \"manifest-diff\"
  ],
  \"outcome\": \"must-be-recorded-not-assumed\"
}
""" .

# ── tera-107.ttl ────────────────────────────────────────────────
book:chapter-107 a book:Chapter ;
    book:number "107"^^xsd:integer ;
    book:title "107. Templates Are Projection Law" ;
    book:slug "templates-projection-law" ;
    book:part "tera" ;
    book:sourcePath "tera/107-templates-projection-law.md" ;
    book:sourceText """# 107. Templates Are Projection Law

> **Pattern 107 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **107. Templates Are Projection Law**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Templates Are Projection Law**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Templates Are Projection Law** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/107-107-templates-are-projection-law.tmpl`](../listings/107-107-templates-are-projection-law.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/107_107_templates_are_projection_law.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章107 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 107: 107. Templates Are Projection Law
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **107. Templates Are Projection Law**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-107` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-107 a book:Listing ;
    book:forChapter book:chapter-107 ;
    book:sourcePath "listings/107-107-templates-are-projection-law.tmpl" ;
    book:sourceText """---
to: src/generated/107_107_templates_are_projection_law.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章107 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 107: 107. Templates Are Projection Law
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-108.ttl ────────────────────────────────────────────────
book:chapter-108 a book:Chapter ;
    book:number "108"^^xsd:integer ;
    book:title "108. Front Matter" ;
    book:slug "template-front-matter" ;
    book:part "tera" ;
    book:sourcePath "tera/108-template-front-matter.md" ;
    book:sourceText """# 108. Front Matter

> **Pattern 108 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **108. Front Matter**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Front Matter**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Front Matter** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/108-108-front-matter.tmpl`](../listings/108-108-front-matter.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/108_108_front_matter.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章108 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 108: 108. Front Matter
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **108. Front Matter**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-108` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-108 a book:Listing ;
    book:forChapter book:chapter-108 ;
    book:sourcePath "listings/108-108-front-matter.tmpl" ;
    book:sourceText """---
to: src/generated/108_108_front_matter.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章108 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 108: 108. Front Matter
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-109.ttl ────────────────────────────────────────────────
book:chapter-109 a book:Chapter ;
    book:number "109"^^xsd:integer ;
    book:title "109. Output Ownership" ;
    book:slug "output-ownership" ;
    book:part "tera" ;
    book:sourcePath "tera/109-output-ownership.md" ;
    book:sourceText """# 109. Output Ownership

> **Pattern 109 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **109. Output Ownership**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Output Ownership**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Output Ownership** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/109-109-output-ownership.tmpl`](../listings/109-109-output-ownership.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/109_109_output_ownership.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章109 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 109: 109. Output Ownership
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **109. Output Ownership**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-109` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-109 a book:Listing ;
    book:forChapter book:chapter-109 ;
    book:sourcePath "listings/109-109-output-ownership.tmpl" ;
    book:sourceText """---
to: src/generated/109_109_output_ownership.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章109 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 109: 109. Output Ownership
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-110.ttl ────────────────────────────────────────────────
book:chapter-110 a book:Chapter ;
    book:number "110"^^xsd:integer ;
    book:title "110. `force` and Regeneration" ;
    book:slug "force-regeneration" ;
    book:part "tera" ;
    book:sourcePath "tera/110-force-regeneration.md" ;
    book:sourceText """# 110. `force` and Regeneration

> **Pattern 110 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **110. `force` and Regeneration**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`force` and Regeneration**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`force` and Regeneration** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/110-110-force-and-regeneration.tmpl`](../listings/110-110-force-and-regeneration.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/110_110_force_and_regeneration.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章110 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 110: 110. `force` and Regeneration
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **110. `force` and Regeneration**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-110` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-110 a book:Listing ;
    book:forChapter book:chapter-110 ;
    book:sourcePath "listings/110-110-force-and-regeneration.tmpl" ;
    book:sourceText """---
to: src/generated/110_110_force_and_regeneration.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章110 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 110: 110. `force` and Regeneration
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-111.ttl ────────────────────────────────────────────────
book:chapter-111 a book:Chapter ;
    book:number "111"^^xsd:integer ;
    book:title "111. `skip_if` and Local Freeze" ;
    book:slug "skip-if-local-freeze" ;
    book:part "tera" ;
    book:sourcePath "tera/111-skip-if-local-freeze.md" ;
    book:sourceText """# 111. `skip_if` and Local Freeze

> **Pattern 111 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **111. `skip_if` and Local Freeze**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **`skip_if` and Local Freeze**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **`skip_if` and Local Freeze** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/111-111-skip-if-and-local-freeze.tmpl`](../listings/111-111-skip-if-and-local-freeze.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/111_111_skip_if_and_local_freeze.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章111 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 111: 111. `skip_if` and Local Freeze
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **111. `skip_if` and Local Freeze**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-111` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-111 a book:Listing ;
    book:forChapter book:chapter-111 ;
    book:sourcePath "listings/111-111-skip-if-and-local-freeze.tmpl" ;
    book:sourceText """---
to: src/generated/111_111_skip_if_and_local_freeze.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章111 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 111: 111. `skip_if` and Local Freeze
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-112.ttl ────────────────────────────────────────────────
book:chapter-112 a book:Chapter ;
    book:number "112"^^xsd:integer ;
    book:title "112. Injection Modes" ;
    book:slug "injection-modes" ;
    book:part "tera" ;
    book:sourcePath "tera/112-injection-modes.md" ;
    book:sourceText """# 112. Injection Modes

> **Pattern 112 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **112. Injection Modes**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Injection Modes**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Injection Modes** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/112-112-injection-modes.tmpl`](../listings/112-112-injection-modes.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/112_112_injection_modes.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章112 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 112: 112. Injection Modes
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **112. Injection Modes**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-112` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-112 a book:Listing ;
    book:forChapter book:chapter-112 ;
    book:sourcePath "listings/112-112-injection-modes.tmpl" ;
    book:sourceText """---
to: src/generated/112_112_injection_modes.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章112 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 112: 112. Injection Modes
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-113.ttl ────────────────────────────────────────────────
book:chapter-113 a book:Chapter ;
    book:number "113"^^xsd:integer ;
    book:title "113. One Writer per Output" ;
    book:slug "one-writer-per-output" ;
    book:part "tera" ;
    book:sourcePath "tera/113-one-writer-per-output.md" ;
    book:sourceText """# 113. One Writer per Output

> **Pattern 113 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **113. One Writer per Output**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **One Writer per Output**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **One Writer per Output** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/113-113-one-writer-per-output.tmpl`](../listings/113-113-one-writer-per-output.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/113_113_one_writer_per_output.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章113 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 113: 113. One Writer per Output
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **113. One Writer per Output**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-113` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-113 a book:Listing ;
    book:forChapter book:chapter-113 ;
    book:sourcePath "listings/113-113-one-writer-per-output.tmpl" ;
    book:sourceText """---
to: src/generated/113_113_one_writer_per_output.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章113 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 113: 113. One Writer per Output
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-114.ttl ────────────────────────────────────────────────
book:chapter-114 a book:Chapter ;
    book:number "114"^^xsd:integer ;
    book:title "114. FM-WRITE-008" ;
    book:slug "fm-write-008" ;
    book:part "tera" ;
    book:sourcePath "tera/114-fm-write-008.md" ;
    book:sourceText """# 114. FM-WRITE-008

> **Pattern 114 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **114. FM-WRITE-008**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **FM-WRITE-008**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **FM-WRITE-008** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Assign one writer.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Escape source safely.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Review rendered semantics.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/114-114-fm-write-008.tmpl`](../listings/114-114-fm-write-008.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/114_114_fm_write_008.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章114 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 114: 114. FM-WRITE-008
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **114. FM-WRITE-008**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-114` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-114 a book:Listing ;
    book:forChapter book:chapter-114 ;
    book:sourcePath "listings/114-114-fm-write-008.tmpl" ;
    book:sourceText """---
to: src/generated/114_114_fm_write_008.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章114 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 114: 114. FM-WRITE-008
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-115.ttl ────────────────────────────────────────────────
book:chapter-115 a book:Chapter ;
    book:number "115"^^xsd:integer ;
    book:title "115. Escaping Source Text" ;
    book:slug "escaping-source-text" ;
    book:part "tera" ;
    book:sourcePath "tera/115-escaping-source-text.md" ;
    book:sourceText """# 115. Escaping Source Text

> **Pattern 115 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **115. Escaping Source Text**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Escaping Source Text**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Escaping Source Text** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/115-115-escaping-source-text.tmpl`](../listings/115-115-escaping-source-text.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/115_115_escaping_source_text.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章115 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 115: 115. Escaping Source Text
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **115. Escaping Source Text**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-115` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-115 a book:Listing ;
    book:forChapter book:chapter-115 ;
    book:sourcePath "listings/115-115-escaping-source-text.tmpl" ;
    book:sourceText """---
to: src/generated/115_115_escaping_source_text.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章115 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 115: 115. Escaping Source Text
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-116.ttl ────────────────────────────────────────────────
book:chapter-116 a book:Chapter ;
    book:number "116"^^xsd:integer ;
    book:title "116. Template Self-Interpretation" ;
    book:slug "template-self-interpretation" ;
    book:part "tera" ;
    book:sourcePath "tera/116-template-self-interpretation.md" ;
    book:sourceText """# 116. Template Self-Interpretation

> **Pattern 116 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **116. Template Self-Interpretation**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Template Self-Interpretation**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Template Self-Interpretation** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Assign one writer.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Escape source safely.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Review rendered semantics.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/116-116-template-self-interpretation.tmpl`](../listings/116-116-template-self-interpretation.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/116_116_template_self_interpretation.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章116 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 116: 116. Template Self-Interpretation
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **116. Template Self-Interpretation**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-116` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-116 a book:Listing ;
    book:forChapter book:chapter-116 ;
    book:sourcePath "listings/116-116-template-self-interpretation.tmpl" ;
    book:sourceText """---
to: src/generated/116_116_template_self_interpretation.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章116 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 116: 116. Template Self-Interpretation
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-117.ttl ────────────────────────────────────────────────
book:chapter-117 a book:Chapter ;
    book:number "117"^^xsd:integer ;
    book:title "117. Unicode Identifiers" ;
    book:slug "unicode-identifiers" ;
    book:part "tera" ;
    book:sourcePath "tera/117-unicode-identifiers.md" ;
    book:sourceText """# 117. Unicode Identifiers

> **Pattern 117 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **117. Unicode Identifiers**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Unicode Identifiers**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Unicode Identifiers** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Assign one writer.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Escape source safely.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Review rendered semantics.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/117-117-unicode-identifiers.tmpl`](../listings/117-117-unicode-identifiers.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/117_117_unicode_identifiers.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章117 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 117: 117. Unicode Identifiers
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **117. Unicode Identifiers**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-117` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-117 a book:Listing ;
    book:forChapter book:chapter-117 ;
    book:sourcePath "listings/117-117-unicode-identifiers.tmpl" ;
    book:sourceText """---
to: src/generated/117_117_unicode_identifiers.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章117 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 117: 117. Unicode Identifiers
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-118.ttl ────────────────────────────────────────────────
book:chapter-118 a book:Chapter ;
    book:number "118"^^xsd:integer ;
    book:title "118. Generating Japanese Rust" ;
    book:slug "generating-japanese-rust" ;
    book:part "tera" ;
    book:sourcePath "tera/118-generating-japanese-rust.md" ;
    book:sourceText """# 118. Generating Japanese Rust

> **Pattern 118 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **118. Generating Japanese Rust**. Rust provides compile-time leverage: illegal production states can be removed from the vocabulary instead of detected after actuation.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Generating Japanese Rust**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Generating Japanese Rust** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. **Assign one writer.** Identify the authoritative source and the exact downstream observable that must survive generation.
2. **Escape source safely.** Encode the rule in the appropriate layer: ontology, gate, query, template, generated type, consumer, or engine.
3. **Review rendered semantics.** Execute a check that can produce a typed failure and preserve that result in the evidence chain.

The sequence deliberately separates semantic work from filesystem actuation. RDF identities describe the domain. SPARQL admission gates (`gates/*.rq`) decide whether the input may proceed. SPARQL projections select the construction facts. Tera renders a deterministic projection. The target compiler and test runner then judge the produced artifact from the consumer's perspective.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/118-118-generating-japanese-rust.tmpl`](../listings/118-118-generating-japanese-rust.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/118_118_generating_japanese_rust.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章118 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 118: 118. Generating Japanese Rust
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **118. Generating Japanese Rust**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-118` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-118 a book:Listing ;
    book:forChapter book:chapter-118 ;
    book:sourcePath "listings/118-118-generating-japanese-rust.tmpl" ;
    book:sourceText """---
to: src/generated/118_118_generating_japanese_rust.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章118 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 118: 118. Generating Japanese Rust
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── tera-119.ttl ────────────────────────────────────────────────
book:chapter-119 a book:Chapter ;
    book:number "119"^^xsd:integer ;
    book:title "119. Formatting without Semantic Drift" ;
    book:slug "formatting-without-drift" ;
    book:part "tera" ;
    book:sourcePath "tera/119-formatting-without-drift.md" ;
    book:sourceText """# 119. Formatting without Semantic Drift

> **Pattern 119 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **119. Formatting without Semantic Drift**. Drift is not merely reported. Unexplained drift must stop promotion and preserve both trees for diagnosis.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Formatting without Semantic Drift**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Formatting without Semantic Drift** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/119-119-formatting-without-semantic-drift.sh`](../listings/119-119-formatting-without-semantic-drift.sh). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```bash
#!/usr/bin/env bash
set -euo pipefail

subject="119. Formatting without Semantic Drift"
before="$(mktemp -d)"
after="$(mktemp -d)"

ggen sync run
cp -R src "$before/"
cargo test --all-targets
ggen sync run
cp -R src "$after/"

diff -ru "$before/src" "$after/src"
printf 'verified: %s\\n' "$subject"
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **119. Formatting without Semantic Drift**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-119` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-119 a book:Listing ;
    book:forChapter book:chapter-119 ;
    book:sourcePath "listings/119-119-formatting-without-semantic-drift.sh" ;
    book:sourceText """#!/usr/bin/env bash
set -euo pipefail

subject=\"119. Formatting without Semantic Drift\"
before=\"$(mktemp -d)\"
after=\"$(mktemp -d)\"

ggen sync run
cp -R src \"$before/\"
cargo test --all-targets
ggen sync run
cp -R src \"$after/\"

diff -ru \"$before/src\" \"$after/src\"
printf 'verified: %s\\n' \"$subject\"
""" .

# ── tera-120.ttl ────────────────────────────────────────────────
book:chapter-120 a book:Chapter ;
    book:number "120"^^xsd:integer ;
    book:title "120. Template Review as Compiler Review" ;
    book:slug "template-review" ;
    book:part "tera" ;
    book:sourcePath "tera/120-template-review.md" ;
    book:sourceText """# 120. Template Review as Compiler Review

> **Pattern 120 · Deterministic Projection**
>
> **Standing rule:** this pattern is `ALIVE` only when its consequence has been observed in a real consumer and bound to replayable evidence.

## Context

A template is executable projection law and therefore deserves the same ownership discipline as a compiler backend. The narrow problem in this chapter is **120. Template Review as Compiler Review**. The chapter converts a broad design claim into a named artifact, an executable check, and a refusal condition.

A weak implementation can usually demonstrate that a file rendered once. That is not the target. The target is a production rule whose input is admitted, whose output has one owner, whose behavior is observed by a real consumer, and whose standing can be replayed from durable evidence. Every claim in this chapter must therefore terminate in a path, command, test, fixture, digest, or receipt.

## Problem

Without a stable pattern for **Template Review as Compiler Review**, locally reasonable decisions accumulate into a pack that renders but does not manufacture a substitutable product. The defect normally appears later—as graph contamination, hidden handler work, path collision, drift, false proof, or an unverifiable release claim—when repair is more expensive and evidence is weaker.

## Forces

- The source of truth must remain distinguishable from every generated projection.
- A successful render is weaker than an admitted, consumed, independently verified consequence.
- Composition creates hidden coupling unless identity, ownership, and output boundaries are explicit.
- Fast regeneration is useful only when unchanged inputs reproduce unchanged bytes and changed inputs expose drift.
- Every actuation must terminate in a durable receipt or an explicit typed refusal.

## Governing law

\\[A=\\mu(O^{\\star})\\quad\\text{with standing carried by named evidence}\\]

The equation is read operationally. Observation is not accepted merely because it exists. It must enter the admitted set \\(O^\\star\\). Manufacturing \\(\\mu\\) must preserve the distinctions required by the downstream customer. The resulting artifact has standing only when the acceptance evidence names the exact source law, generator, output, and verification result.

## Therefore

Establish **Template Review as Compiler Review** as a named part of the pack contract rather than leaving it as convention or tacit knowledge.

Do this at the narrowest layer that owns the invariant. Keep graph-domain construction reversible until admission is complete. Route filesystem or external effects through the engine’s declared write path. Require the consumer and verifier to observe the intended consequence independently of the authoring convenience that produced it.

## Configuration

1. Admit the source.
2. Apply the pattern at its owning layer.
3. Actuate only through the declared generator path.
4. Verify in a clean consumer.
5. Preserve the receipt and refusal evidence.

### Crate alignment

The pattern is grounded in these live ownership surfaces:

- `ggen-engine`
- `ggen-config`

A pack may depend on these surfaces, but it must not silently absorb their responsibilities.

## Reference implementation

The companion listing is stored at [`src/listings/120-120-template-review-as-compiler-review.tmpl`](../listings/120-120-template-review-as-compiler-review.tmpl). It is intentionally small enough to inspect while retaining the chapter's load-bearing boundary.

```jinja
---
to: src/generated/120_120_template_review_as_compiler_review.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章120 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 120: 120. Template Review as Compiler Review
{% for row in rows %}
{{ row.body }}
{% endfor %}
```

## Verification procedure

Run the listing as part of the chapter laboratory rather than copying it into an unrelated scratch file. A valid verification cycle is:

```text
admit source → sync → build consumer → run independent proof → sync again → compare bytes → emit receipt
```

The proof must be non-vacuous. For code generation, changing a generated signature, dropping an ontology individual, widening an optional value, changing a Japanese identifier, or removing a refusal branch must cause a relevant check to fail. For release assets, removing a workflow, target family, package manifest, or attestation input must appear as manifest drift.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

Use the verification ladder in order:

```text
unit → integration → end-to-end → adversarial mutation → idempotency → receipt verification
```

A lower rung may establish `PARTIAL_ALIVE`; it cannot establish the crown claim of the higher rungs.

## Resulting context

Every template has one declared ownership domain, deterministic inputs, and a write mode compatible with local work.

The pattern also creates obligations. The new source law must remain inspectable. Generated outputs must retain one owner. Consumers must not acquire hidden setup. Receipts must be regenerated whenever an admitted input or verifier changes. Neighboring patterns can now rely on this consequence without reintroducing the resolved ambiguity.

## Failure modes

- **Inert truth:** the ontology is accurate but no real consumer reads any generated output.
- **Self-proof:** subject and expected value are derived through the same defective projection.
- **Hidden handler gap:** the pack emits catalogs while the consumer must write the real behavior.
- **Union leakage:** a shared RDF class causes one pack to select another pack's individuals.
- **Multiple writers:** two templates or packs claim the same path.
- **False green:** a missing compiler, SDK, runner, or signer is reported as success instead of an explicit bounded outcome.

## Falsifier

Two writers claim one path, a missing merge marker destroys local content, or a second sync changes bytes unexpectedly.

Execute that falsifier deliberately. A pattern with no plausible way to fail is a slogan, not production law.

## Continuous TCPS case study

TCPS generation preserved Japanese identifiers and disclosed five module/struct shadowing corrections rather than silently rewriting the domain.

The TCPS project is treated as a conformance oracle rather than a marketing example. The recorded prototype generated the twenty-four-module core and ran the original Japanese `試験.rs` unchanged, with 14 of 14 tests passing after five module/struct shadowing corrections were disclosed. The wider project record also captured 20 of 20 `receiptctl` tests, 130 of 130 engine library tests, and 11 of 11 framework-pack end-to-end tests. These are project-recorded results; the book source preserves them as case-study evidence and does not pretend they were independently rerun in this artifact environment. (A 2026-07-19 rerun in the host ggen repository is recorded in the book's `SOURCE_NOTES.md`.)

## Laboratory

Create a minimal mutation related to **120. Template Review as Compiler Review**. Record the expected refusal or proof failure before applying the mutation. Run the complete lifecycle, preserve the failing receipt, restore or improve the source law, and rerun until the consumer passes. The final commit should change the manufacturing definition, not hand-edit the generated product.

## Acceptance gate

- [ ] The output for `chapter-120` is created by the declared pack path.
- [ ] A clean consumer can mount the output without hand-editing generated files.
- [ ] At least one independent check fails when the generated subject is intentionally mutated.
- [ ] A second sync is byte-identical when admitted inputs are unchanged.
- [ ] The receipt records the exact outcome rather than assuming success.

## Connections

This pattern receives its context from **Deterministic Projection** and passes a narrower, better-admitted state to the patterns that follow it in `SUMMARY.md`. When a later pattern fails, trace backward through source identity, admission, projection, ownership, consumer observation, and receipt rather than patching the generated artifact.

## Standing statement

The pattern is complete only when every checked acceptance item resolves to a concrete repository artifact or command result. Missing execution is `UNKNOWN`; a missing admitted dependency is `BLOCKED`; an unreachable verifier caused by the build is `BUILD_BROKEN`; an intentionally absent capability is `UNSUPPORTED`. None of those states may be reported as `ALIVE`.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `template front matter`
- `per-row output routing`
- `checksum freeze`
- `single-writer ownership`

### Current repository evidence

- `crates/ggen-engine/src/pack.rs`
- `packs/level-five-book-pack/templates/chapter.md.tmpl`
- `packs/star-toml-pack/templates`

### Pack witnesses

- `packs/level-five-book-pack`
- `packs/star-toml-pack`
- `packs/lsp-max-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Template syntax alone is insufficient; a real second sync must prove output stability.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" .

book:listing-120 a book:Listing ;
    book:forChapter book:chapter-120 ;
    book:sourcePath "listings/120-120-template-review-as-compiler-review.tmpl" ;
    book:sourceText """---
to: src/generated/120_120_template_review_as_compiler_review.rs
force: true
sparql:
  rows: |
    PREFIX pack: <https://example.org/ggen/pack#>
    SELECT ?name ?body WHERE {
      pack:章120 pack:name ?name ; pack:sourceText ?body .
    }
---
//! Generated for chapter 120: 120. Template Review as Compiler Review
{% for row in rows %}
{{ row.body }}
{% endfor %}
""" .

# ── repository capability map ─────────────────────────────────────
book:chapter-capability-map a book:Chapter ;
    book:number 0 ;
    book:title "Repository Capability and Pack Map" ;
    book:slug "repository-capability-pack-map" ;
    book:part "front-matter" ;
    book:sourcePath "CAPABILITY_MAP.md" ;
    book:sourceText """# Repository Capability and Pack Map

This map binds the pack-writer pattern language to repository evidence. It is an observation ledger, not a roadmap and not a claim that every pattern already has a complete pack witness.

## Standing vocabulary

- **IMPLEMENTED** — a live crate or command owns the capability.
- **PACK_WITNESS** — at least one committed pack and consumer exercise the capability.
- **PARTIAL** — machinery exists, but the complete chapter claim is not demonstrated at the same scale.
- **TARGET** — the chapter is normative design guidance without a current repository witness.
- **ARCHIVE_ONLY** — evidence exists only in historical material and cannot establish current standing.

A chapter may cite multiple states. For example, deterministic RDF deltas can be **IMPLEMENTED**, while a claimed Level Five product based on them remains **PARTIAL**.

## Capability ownership

| Surface | Current responsibility | Primary evidence |
|---|---|---|
| `ggen-config` | Typed `ggen.toml` parsing, validation and deterministic manifest collections | `crates/ggen-config/src/manifest/types.rs` |
| `ggen-engine` | Schema dispatch, pack resolution, graph/law loading, projection, templates, bounded writes and receipts | `crates/ggen-engine/src/lib.rs`, `crates/ggen-engine/src/pack.rs`, `crates/ggen-engine/src/sync.rs` |
| `ggen-graph` | Deterministic RDF state, deltas, SHACL, SPARQL, hooks and transition receipts | `crates/ggen-graph/src/lib.rs` |
| `praxis-graphlaw` | Rule materialization and graph-law hooks | `crates/praxis-graphlaw/src/lib.rs` |
| `ggen-marketplace` | Discovery, composition, lifecycle, passports, isolation and substitution | `crates/ggen-marketplace/src/lib.rs`, `crates/ggen-marketplace/src/packs_registry/` |
| `ggen-lsp` | Live diagnostics, admissibility-pack emission, repair routes and OCEL evidence | `crates/ggen-lsp/src/lib.rs`, `crates/ggen-lsp/src/pack/mod.rs` |
| `ggen-cli-lib` | User-facing command routing into engine capabilities | `crates/ggen-cli/src/` |
| `chicago-tdd-tools` | Independent state-based test support | `crates/chicago-tdd-tools/` |
| `ggen-cheat-scanner` | Detection of vacuous or suspicious generated proof surfaces | `crates/ggen-cheat-scanner/` |
| `cargo-cicd` / `cargo-cicd-pack` | Release-law execution and generated delivery surfaces | `packs/cargo-cicd-pack/` |

## Pack witnesses

| Pack | Demonstrated capability | Evidence boundary | Standing |
|---|---|---|---|
| `packs/ggen-verify-pack` | Admits external build, test, clippy and byte-identity evidence as graph facts; refuses missing, red or stale evidence | Pack manifest, gates, bootstrap emitter and reasoner-independence test | **PACK_WITNESS** |
| `packs/star-toml-pack` | Generates typed TOML admission code, validation behavior, docs and two independent proof styles | `examples/star-toml-verify`, generated tests, drift mutation and idempotency record in `pack.toml` | **PACK_WITNESS** |
| `packs/lsp-max-pack` | Generates ontology-sourced lint-rule TOML, documentation and independent proof tests | `examples/lsp-max-verify`, SHACL validation, mutation and idempotency record in `pack.toml` | **PACK_WITNESS** |
| `packs/praxis-core-pack` | Projects Praxis refusal and core-law surfaces with generated verification | Pack templates, ontology and verification consumer | **PACK_WITNESS** |
| `packs/cargo-cicd-pack` | Projects CI/CD and release assets from ontology | Pack ontology/templates; broader release portability remains bounded by available SDKs | **PARTIAL** |
| `packs/wasm4pm-facts-pack` | Projects WASM4PM fact coverage and proof artifacts | Pack templates and proof surfaces | **PARTIAL** |
| `packs/affidavit-pack` | Generates affidavit/evidence-oriented artifacts with a consumer | `examples/affidavit-verify` | **PARTIAL** |
| `packs/chicago-tdd-tools-pack` | Makes Chicago-style test support consumable as a pack | Pack manifest and templates | **PARTIAL** |
| `packs/repo-as-found-pack` | Models admitted observations of an existing repository | `packs/repo-as-found-pack/pack.toml` | **PARTIAL** |
| `packs/repo-load-path-pack` | Models repository load-path decisions | `packs/repo-load-path-pack/pack.toml` | **PARTIAL** |
| `packs/repo-intervention-pack` | Models bounded repository interventions | `packs/repo-intervention-pack/pack.toml` | **PARTIAL** |
| `packs/repo-reconciliation-pack` | Models reconciliation of repository states | `packs/repo-reconciliation-pack/pack.toml` | **PARTIAL** |
| `packs/temporary-works-pack` | Models temporary construction required during change | `packs/temporary-works-pack/pack.toml` | **PARTIAL** |
| `packs/claude-code-pack` | Projects coding-agent configuration | Pack manifest and templates | **PARTIAL** |
| `packs/gh-terraform-pack` | Projects GitHub/Terraform infrastructure surfaces | Pack manifest and templates | **PARTIAL** |
| `packs/mfw-pcp-level5-pack` | Carries Multifractal Workflow / proof-carrying-plan Level Five material | Pack manifest and ontology | **PARTIAL** |
| `packs/ma-case-study-pack` | Case-study corpus pack | Pack ontology and design record | **PARTIAL** |

Marketplace descriptors under `marketplace/packs/` are discoverable package records. They are not automatically equivalent to executable local packs and must not be cited as a pack witness without a resolved package, generated output and consumer receipt.

## Field-to-capability alignment

| Book field and chapters | Live capability binding | Concrete pack witnesses | Current crown standing |
|---|---|---|---|
| **Using the Pattern Language** | Documentation law, standing vocabulary and executable acceptance | This map; `book/scripts/check_book.py` | **IMPLEMENTED** structurally; runtime claims remain bounded |
| **I. Whole Manufacturing System** — 1–12 | `ggen-engine`, `ggen-graph`, `praxis-graphlaw`, receipts and bounded writes | `ggen-verify-pack`, `star-toml-pack` | **PARTIAL** at whole-system scale |
| **II. Pack as a Living Part** — 13–24 | Pack parsing/resolution, manifests, hashes, policies, routes and provenance | `ggen-lsp` emitted admissibility pack; local pack manifests | **PARTIAL**; identity/passport work is implemented in marketplace |
| **III. Choose the Right Kind of Part** — 25–36 | Marketplace package types, codegen packs, hook packs and corpus packs | `star-toml-pack`, `ggen-verify-pack`, `ma-case-study-pack` | **PACK_WITNESS** for the taxonomy, not every subtype at L5 |
| **IV. Standing before Scale** — 37–52 | Verification ladders, cheat scanning, consumer tests, drift and receipts | `ggen-verify-pack`, `star-toml-pack`, `lsp-max-pack` | **PACK_WITNESS** for several maturity cells; universal L5 is **TARGET** |
| **V. Independent Reality** — 53–64 | Independent fixtures/tests and reference-tree comparison | `star-toml-pack`, `lsp-max-pack`, TCPS project record | **PARTIAL** |
| **VI. Ontology Neighborhood** — 65–80 | RDF parsing, stable IRIs, public vocabularies and graph-law materialization | Most ontology-bearing packs | **IMPLEMENTED** primitives; modeling quality is pack-specific |
| **VII. Admission Gates** — 81–94 | SHACL/SPARQL gate execution and typed refusal before writes | `ggen-verify-pack`, `star-toml-pack`, `lsp-max-pack` | **PACK_WITNESS** |
| **VIII. Deterministic Projection** — 95–120 | SPARQL extraction/CONSTRUCT, Tera rendering and write ownership | `star-toml-pack`, `lsp-max-pack`, `praxis-core-pack` | **PACK_WITNESS** |
| **IX. Complete Product Surfaces** — 121–134 | Generated Rust modules, manifests, C/WASM boundaries | TCPS book laboratory, `wasm4pm-facts-pack` | **PARTIAL**; full multi-target substitution is not universally verified |
| **X. Proof beside Product** — 135–148 | Generated tests, Chicago TDD, mutation sensitivity and cheat scanning | `star-toml-pack`, `lsp-max-pack`, `ggen-verify-pack` | **PACK_WITNESS** |
| **XI. Consumer Place** — 149–160 | Real `ggen.toml`, sync, generated mounts, build/test and replay | `examples/star-toml-verify`, `examples/lsp-max-verify`, `examples/affidavit-verify` | **PACK_WITNESS** |
| **XII. Pack Neighborhood** — 161–172 | Union graph, composition, non-interference, output ownership and passports | Marketplace composition code; multi-pack tests; repository lifecycle packs | **PARTIAL** |
| **XIII. Time, Change and Repair** — 173–186 | Locking, checksum freeze, idempotency, drift refusal, LSP repair routes | `star-toml-pack`, `lsp-max-pack`, emitted LSP admissibility packs | **PACK_WITNESS** for checksum/idempotency; longitudinal repair is **PARTIAL** |
| **XIV. Receipts and Standing** — 187–200 | Input/output/transition hashes, signatures, verification facts and provenance | `ggen-verify-pack`, graph transition receipts, marketplace passports | **PARTIAL** across the full supply chain |
| **XV. Change the Engine Only at the Boundary** — 201–212 | Shared schema dispatch, pack gate execution, write/error semantics | `ggen-engine` and reasoner-independence tests | **IMPLEMENTED** machinery; necessity claims remain case-specific |
| **XVI. Level Five Sequence** — 213–224 | Product/ontology/acceptance matrices and certification planning | `book/code/packs/canonical-level-five-pack`, TCPS design records | **PARTIAL** |
| **XVII. TCPS Complete Pattern** — 225–302, 337 | TCPS ontology, generated core/product/release artifacts and defect ledger | Book TCPS packs and project-record evidence | **PARTIAL** until current-head full toolchain replay |
| **XVIII. Make a New Language** — 303–320 | Practicum applying all pack-facing crates | No single repository pack can witness an arbitrary new domain | **TARGET** |
| **XIX. Certification Laboratories** — 321–336 | Adversarial mutation, consumer verification, composition, receipts and release checks | Canonical Level Five laboratory assets | **PARTIAL** until all laboratories execute on current head |
| **Pattern Reference** | Current manifests, layouts, examples and failure codes | Live pack manifests and crate schemas take precedence | **IMPLEMENTED** only where checked against current code |

## Chapter citation rule

Each chapter must identify:

1. the owning crate or command;
2. at least one exact repository path that can falsify the claim;
3. a real pack witness when one exists;
4. the current standing from this map;
5. an explicit gap when no witness exists.

A chapter may not cite a marketplace descriptor, archived document, generated listing or case-study statement as proof of a live capability unless a current consumer and verifier bind it to a reproducible run.

## Verification

```bash
python3 book/scripts/check_book.py
python3 book/scripts/check_level_five.py

# Representative live witnesses
cargo test -p ggen-engine --test reasoner_independence_e2e
cargo test -p ggen-graph
cargo test -p ggen-marketplace
```

The commands above are falsifiers, not claims that they passed in the environment reading this page. Record their exact outcome before promoting standing.

## Repository capability alignment

This section is generated from the repository capability ledger. It distinguishes live machinery from demonstrated pack witnesses and from target-state guidance.

### Owning capabilities

- `ggen-config manifest admission`
- `ggen-engine pack projection`
- `mdBook rendering`

### Current repository evidence

- `book/ggen.toml`
- `packs/level-five-book-pack/pack.toml`
- `book/scripts/check_book.py`

### Pack witnesses

- `packs/level-five-book-pack`

### Bounded standing

`PACK_WITNESS`

### Open gap

Reader comprehension is not runtime standing; the self-hosted book gate must execute on the current commit.

### Required falsifier

Run the narrow evidence path named above, then execute the chapter's consumer or mutation test. A missing tool, skipped consumer, stale lock, changed generated byte, or unverifiable receipt lowers standing; it never counts as success.
""" ;
    book:hasListing book:listing-capability-map .

book:listing-capability-map a book:Listing ;
    book:sourcePath "listings/capability-map.txt" ;
    book:sourceText """Generated witness for the repository capability and pack map.
""" .
