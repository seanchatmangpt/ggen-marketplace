[pack]
name = "evidence-capital-realization-control-pack"
version = "0.1.0"
description = "Production SELECT/VERIFY control over held-out evidence-capital realization."

[authority]
allowed = ["OBSERVE", "SELECT", "CONSTRUCT", "VERIFY"]
consequential_do = "BRCE_ONLY"

[manufacture]
ontology = "ontology.ttl"
ggen = "ggen.toml"
generated_outputs_are_consequences = true
