PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?receipt ?authority ?actuated WHERE {
  ?receipt a dfcm:SelectionReceipt ; dfcm:authority ?authority ; dfcm:actuationPerformed ?actuated .
  FILTER(?authority = "DO" || ?actuated = true)
}
