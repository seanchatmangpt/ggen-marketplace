PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?candidate ?required ?passed WHERE {
  ?candidate a dfcm:SelectionCandidate ; dfcm:closureRequired ?required ; dfcm:closurePassed ?passed .
  FILTER(?required <= 0 || ?passed < ?required)
}
