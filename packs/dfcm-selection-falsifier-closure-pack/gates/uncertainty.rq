PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?candidate ?uncertainty ?rollback WHERE {
  ?candidate a dfcm:SelectionCandidate ; dfcm:uncertaintyWidth ?uncertainty ; dfcm:rollbackCost ?rollback .
  FILTER(?uncertainty > 0.35 || ?rollback > 0.50)
}
