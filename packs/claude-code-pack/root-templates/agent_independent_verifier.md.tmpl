{#- .claude/agents/independent-verifier.md — root-compatible mirror of
    packs/claude-code-pack/templates/agent_independent_verifier.md.tmpl.
    Root ggen.toml is the "declarative-rules" schema (has [[generation.rules]]), which does
    NOT read a template's own frontmatter `sparql:` block -- only the "frontmatter" schema
    (ggen_engine::config::GgenConfig, proven in examples/claude-code-pack-verify/) does that.
    This template instead calls sparql() itself, mirroring
    packs/ggen-constitution-pack/root-templates/constitution.md.tmpl's working pattern. -#}
{%- set a = sparql(query='PREFIX ccw: <http://seanchatmangpt.github.io/packs/claude-code#> SELECT ?name ?description ?tools ?prompt WHERE { ccw:agent-independent-verifier ccw:name ?name ; ccw:description ?description ; ccw:tools ?tools ; ccw:prompt ?prompt . } ORDER BY ?name') -%}
---
name: {{ a[0].name }}
description: {{ a[0].description | json_encode | safe }}
tools: {{ a[0].tools }}
---

<!-- GENERATED by ggen sync from claude-code-pack/ontology.ttl. Edit the ontology, not this file. -->

{{ a[0].prompt | trim_end }}
