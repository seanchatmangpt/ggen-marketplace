# GENERATED consequence of evidence-capital-admission-pack.
SUBJECT = "{{ subject }}"
MIN_EFFECTIVE_CAPITAL = {{ min_effective_capital }}
MAX_FALSE_INDEPENDENT_RATE = {{ max_false_independent_rate }}
AUTHORITY = "SELECT"
ACTUATION_PERFORMED = False

def admit(effective_capital, false_independent_rate):
    assert effective_capital >= MIN_EFFECTIVE_CAPITAL, "PSEUDO_QUORUM"
    assert false_independent_rate <= MAX_FALSE_INDEPENDENT_RATE, "FALSE_INDEPENDENCE_UNCALIBRATED"
    return "PARTIAL_ALIVE"
