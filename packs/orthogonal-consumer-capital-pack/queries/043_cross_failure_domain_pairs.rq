PREFIX occ: <https://chatman.ai/ontology/orthogonal-consumer-capital#>
SELECT ?a ?b WHERE { ?a a occ:ConsumerCandidate ; occ:failureDomain ?da . ?b a occ:ConsumerCandidate ; occ:failureDomain ?db . FILTER(STR(?a) < STR(?b) && ?da != ?db) }
