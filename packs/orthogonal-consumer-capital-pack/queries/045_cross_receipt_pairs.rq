PREFIX occ: <https://chatman.ai/ontology/orthogonal-consumer-capital#>
SELECT ?a ?b WHERE { ?a a occ:ConsumerCandidate ; occ:receiptProtocol ?pa . ?b a occ:ConsumerCandidate ; occ:receiptProtocol ?pb . FILTER(STR(?a) < STR(?b) && ?pa != ?pb) }
