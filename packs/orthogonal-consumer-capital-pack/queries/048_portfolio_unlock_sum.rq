PREFIX occ: <https://chatman.ai/ontology/orthogonal-consumer-capital#>
SELECT (SUM(?u) AS ?totalUnlocks) WHERE { ?c a occ:ConsumerCandidate ; occ:current true ; occ:independent true ; occ:dependencyUnlocks ?u . }
