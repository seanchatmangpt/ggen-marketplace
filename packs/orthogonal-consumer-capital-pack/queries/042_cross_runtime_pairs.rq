PREFIX occ: <https://chatman.ai/ontology/orthogonal-consumer-capital#>
SELECT ?a ?b WHERE { ?a a occ:ConsumerCandidate ; occ:runtimeFamily ?ra . ?b a occ:ConsumerCandidate ; occ:runtimeFamily ?rb . FILTER(STR(?a) < STR(?b) && ?ra != ?rb) }
