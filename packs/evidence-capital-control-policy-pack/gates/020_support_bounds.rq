PREFIX ecp: <https://ggen.dev/ontology/evidence-capital-control-policy#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?p a ecp:PolicyCandidate ; ecp:support ?n ; ecp:falseCapitalRate ?f .
    FILTER(?n < 5 || ?f > 0.20)
  }
}
