{
  "schema": "ggen.evidence-capital-control-policy-consequence/1",
  "authority": "SELECT",
  "actuation_performed": false,
  "candidates": [
  {% for row in rows %}{"policy":"{{ row.p }}","realized_gain":"{{ row.gain }}","false_capital_rate":"{{ row.false }}","root_concentration":"{{ row.concentration }}","gamma":"{{ row.gamma }}"}{% if not loop.last %},{% endif %}
  {% endfor %}]
}
