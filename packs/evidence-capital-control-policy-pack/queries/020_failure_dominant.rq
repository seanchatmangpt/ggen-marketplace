PREFIX ecp: <https://ggen.dev/ontology/evidence-capital-control-policy#>
SELECT ?p ?standing WHERE {
  ?p a ecp:PolicyCandidate ; ecp:ownerStanding ?standing .
  FILTER(?standing IN ("BUILD_BROKEN","BLOCKED"))
}
ORDER BY ?p
