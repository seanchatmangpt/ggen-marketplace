PREFIX ecp: <https://ggen.dev/ontology/evidence-capital-control-policy#>
SELECT ?p ?g WHERE {
  ?p a ecp:PolicyCandidate ; ecp:generation ?g .
  FILTER NOT EXISTS { ?other a ecp:PolicyCandidate ; ecp:generation ?og . FILTER(?og > ?g) }
}
ORDER BY ?p
