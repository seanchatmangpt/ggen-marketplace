PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate (?capacity / ?cost AS ?ratio) WHERE {
  ?candidate a rmc:RevalidationCandidate ; rmc:futureManufacturingCapacity ?capacity ; rmc:authorityCost ?cost . FILTER(?cost > 0)
}
ORDER BY DESC(?ratio) ?candidate
