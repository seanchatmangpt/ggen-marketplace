PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?gain WHERE { ?candidate a rmc:RevalidationCandidate ; rmc:informationGain ?gain . FILTER(?gain >= 8) }
ORDER BY DESC(?gain) ?candidate
