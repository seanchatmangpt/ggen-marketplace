PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?repo WHERE {
  ?candidate a rmc:RevalidationCandidate ; rmc:standing "BUILD_BROKEN" ; rmc:targetRepo ?repo .
}
ORDER BY ?repo
