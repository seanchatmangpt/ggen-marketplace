PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?priority WHERE {
  ?candidate a rmc:RevalidationCandidate ; rmc:requiresRevalidation true ; rmc:priority ?priority .
  FILTER(?priority >= 80)
}
ORDER BY DESC(?priority) ?candidate
