PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate WHERE { ?candidate a rmc:RevalidationCandidate . FILTER NOT EXISTS { ?candidate rmc:targetRepo ?repo } }
ORDER BY ?candidate
