PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?n WHERE { ?candidate a rmc:RevalidationCandidate ; rmc:consumerFanout ?n . FILTER(?n >= 5) }
ORDER BY DESC(?n) ?candidate
