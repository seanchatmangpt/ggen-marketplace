PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?capacity WHERE { ?candidate a rmc:RevalidationCandidate ; rmc:futureManufacturingCapacity ?capacity . FILTER(?capacity >= 15) }
ORDER BY DESC(?capacity) ?candidate
