PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?cost WHERE { ?candidate a rmc:RevalidationCandidate ; rmc:authorityCost ?cost . FILTER(?cost <= 1) }
ORDER BY ?cost ?candidate
