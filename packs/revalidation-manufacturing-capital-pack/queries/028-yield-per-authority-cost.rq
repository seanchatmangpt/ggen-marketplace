PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate (?yield / ?cost AS ?ratio) WHERE {
  ?candidate a rmc:RevalidationCandidate ; rmc:expectedYield ?yield ; rmc:authorityCost ?cost . FILTER(?cost > 0)
}
ORDER BY DESC(?ratio) ?candidate
