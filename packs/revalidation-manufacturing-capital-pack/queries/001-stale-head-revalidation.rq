PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?observed ?live WHERE {
  ?candidate a rmc:RevalidationCandidate ; rmc:observedHead ?observed ; rmc:liveHead ?live .
  FILTER(?observed != ?live)
}
ORDER BY ?candidate
