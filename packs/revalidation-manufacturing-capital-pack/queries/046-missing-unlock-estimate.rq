PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate WHERE {
  ?candidate a rmc:RevalidationCandidate .
  FILTER(!EXISTS { ?candidate rmc:dependencyUnlockCount ?d } || !EXISTS { ?candidate rmc:compositionUnlockCount ?c })
}
ORDER BY ?candidate
