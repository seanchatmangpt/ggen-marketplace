PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?reason WHERE {
  ?candidate a rmc:RevalidationCandidate ; rmc:requiresRevalidation true ; rmc:revalidationReason ?reason .
}
ORDER BY DESC(?reason) ?candidate
