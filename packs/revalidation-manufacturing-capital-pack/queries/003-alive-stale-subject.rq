PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate WHERE {
  ?candidate a rmc:RevalidationCandidate ; rmc:standing "ALIVE" ; rmc:requiresRevalidation true .
}
ORDER BY ?candidate
