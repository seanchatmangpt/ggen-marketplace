PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate ?n WHERE { ?candidate a rmc:RevalidationCandidate ; rmc:dependencyUnlockCount ?n . FILTER(?n >= 8) }
ORDER BY DESC(?n) ?candidate
