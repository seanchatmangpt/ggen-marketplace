PREFIX rmc: <https://w3id.org/dfcm/revalidation-manufacturing#>
SELECT ?candidate WHERE {
  ?candidate a rmc:RevalidationCandidate .
  FILTER(!EXISTS { ?candidate rmc:receiptRequired true } || !EXISTS { ?candidate rmc:replayRequired true } || !EXISTS { ?candidate rmc:falsifierRequired true })
}
ORDER BY ?candidate
