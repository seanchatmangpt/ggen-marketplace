PREFIX errc: <https://ggen.dev/ontology/errc-ownership#>
SELECT ?fact ?ownership WHERE {
  ?fact a errc:FileOwnership ; errc:ownership ?ownership .
  FILTER (?ownership NOT IN (errc:handwritten, errc:generated, errc:reused, errc:materialized))
}
