PREFIX errc: <https://ggen.dev/ontology/errc-ownership#>
SELECT ?subject ?path (COUNT(?fact) AS ?count) WHERE {
  ?fact a errc:FileOwnership ;
    errc:forSubject ?subject ;
    errc:path ?path .
}
GROUP BY ?subject ?path
HAVING (COUNT(?fact) != 1)
