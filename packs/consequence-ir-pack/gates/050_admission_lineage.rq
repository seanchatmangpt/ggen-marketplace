# MESSAGE: every admitted observation must identify its raw observation lineage; any row is a refusal.
PREFIX cci: <https://ggen.dev/consequence-ir#>
SELECT ?observation WHERE {
  ?observation a cci:AdmittedObservation .
  FILTER NOT EXISTS { ?observation cci:admittedFrom ?raw }
}
ORDER BY ?observation
