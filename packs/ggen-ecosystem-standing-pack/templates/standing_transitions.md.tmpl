---
to: "generated/STANDING-TRANSITIONS.md"
shape:
  - shapes/standing.shacl.ttl
sparql:
  foundations: |
    PREFIX dct: <http://purl.org/dc/terms/>
    PREFIX eco: <https://ggen.dev/ecosystem#>
    SELECT ?id ?title ?description ?ceiling ?source WHERE {
      ?f a eco:StandingFoundationContract ; dct:identifier ?id ; dct:title ?title ;
         dct:description ?description ; eco:authorityCeiling ?ceiling ;
         <http://www.w3.org/ns/prov#hadPrimarySource> ?source .
    } ORDER BY ?id
  transitions: |
    PREFIX dct: <http://purl.org/dc/terms/>
    PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
    PREFIX eco: <https://ggen.dev/ecosystem#>
    SELECT ?id ?from ?to ?rule WHERE {
      ?t a eco:StandingTransition ; dct:identifier ?id ;
         eco:fromStanding/skos:prefLabel ?from ; eco:toStanding/skos:prefLabel ?to ;
         eco:promotionRule ?rule .
    } ORDER BY ?id
---
# Standing transition table

GENERATED by ggen from `ggen-ecosystem-standing-pack/ontology.ttl`. Do not edit.
Companion to `docs/STANDING.md`; both project the same catalog.

## Foundation contracts

| ID | Acceptance edge | Authority ceiling | Purpose | Source |
| --- | --- | --- | --- | --- |
{% for row in foundations %}| {{ row.id }} | `{{ row.title }}` | {{ row.ceiling }} | {{ row.description }} | `{{ row.source }}` |
{% endfor %}
## Transitions

Every transition carries authority effect `NONE`: standing never grants actuation.

| ID | From | To | Promotion rule |
| --- | --- | --- | --- |
{% for row in transitions %}| {{ row.id }} | `{{ row.from }}` | `{{ row.to }}` | {{ row.rule }} |
{% endfor %}
