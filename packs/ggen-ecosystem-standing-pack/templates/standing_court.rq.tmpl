---
to: "generated/standing-court.rq"
shape:
  - shapes/standing.shacl.ttl
sparql:
  transitions: |
    PREFIX dct: <http://purl.org/dc/terms/>
    PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
    PREFIX eco: <https://ggen.dev/ecosystem#>
    SELECT ?id ?from ?to WHERE {
      ?t a eco:StandingTransition ; dct:identifier ?id ;
         eco:fromStanding/skos:prefLabel ?from ; eco:toStanding/skos:prefLabel ?to .
    } ORDER BY ?id
---
# GENERATED by ggen from ggen-ecosystem-standing-pack/ontology.ttl. Do not edit.
# Falsifier court for the standing state machine. Any returned row is a court failure:
# a declared transition missing from the admitted graph, or a standing promotion that
# claims authority. One row per hand-written contract in contracts/standing-transitions/.
PREFIX dct: <http://purl.org/dc/terms/>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX eco: <https://ggen.dev/ecosystem#>
SELECT ?missing WHERE {
{% for row in transitions %}  { FILTER NOT EXISTS {
      ?t{{ loop.index }} a eco:StandingTransition ; dct:identifier "{{ row.id }}" ;
        eco:fromStanding/skos:prefLabel "{{ row.from }}" ;
        eco:toStanding/skos:prefLabel "{{ row.to }}" ;
        eco:authorityEffect "NONE" . }
    BIND("{{ row.id }}:{{ row.from }}->{{ row.to }}" AS ?missing) }
{% if not loop.last %}  UNION
{% endif %}{% endfor %}}
