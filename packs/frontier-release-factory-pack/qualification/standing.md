# Standing

`CANDIDATE` in source control until marketplace validation runs at this exact head.

`PARTIAL_ALIVE` may be claimed after marketplace structure/gates validate.

`ALIVE` requires a real consumer run with the matching ggen runtime, deterministic replay, negative-control refusals, and an earned-release projection whose claims are each bound to exact-subject evidence.
