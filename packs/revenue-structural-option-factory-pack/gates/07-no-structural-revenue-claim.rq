PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?claim WHERE { ?claim rsof:standing ?standing . FILTER(?standing = "OBSERVED_REVENUE" && NOT EXISTS { ?claim rsof:qualification ?q }) } ORDER BY ?claim
