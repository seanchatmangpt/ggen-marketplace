# How to realize an option in a consumer

Choose a non-dominated candidate only after its falsifier, consumer, and qualification edges are present. Render generated projections from the pack. Treat generated output as a projection, not the editing surface. Execute the consumer's exact qualification command. Feed observations upstream as provenance-bound evidence. Never promote a consumer-specific workaround into marketplace knowledge without a generalizability test.
