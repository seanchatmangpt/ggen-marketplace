PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate WHERE { ?candidate a rsof:CandidateStructure . FILTER NOT EXISTS { ?other a rsof:CandidateStructure ; rsof:dominates ?candidate . FILTER(?other != ?candidate) } } ORDER BY ?candidate
