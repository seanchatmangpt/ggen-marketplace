PREFIX rof: <https://ggen.dev/ontology/revenue-option-factory#>
SELECT ?candidate WHERE { ?candidate a rof:CandidateStructure . FILTER NOT EXISTS { ?other a rof:CandidateStructure ; rof:dominates ?candidate . FILTER(?other != ?candidate) } } ORDER BY ?candidate
