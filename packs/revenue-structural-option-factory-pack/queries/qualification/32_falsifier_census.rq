PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate (COUNT(?falsifier) AS ?falsifiers) WHERE { ?candidate rsof:falsifier ?falsifier . } GROUP BY ?candidate ORDER BY DESC(?falsifiers)
