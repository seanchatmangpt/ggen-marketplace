PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate (COUNT(?standing) AS ?count) WHERE { ?candidate a rsof:CandidateStructure ; rsof:standing ?standing . } GROUP BY ?candidate HAVING(COUNT(?standing) > 1)
