PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate (COUNT(?artifact) AS ?artifacts) WHERE { ?candidate rsof:generatedConsequence ?artifact . } GROUP BY ?candidate ORDER BY DESC(?artifacts)
