PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate WHERE { ?candidate a rsof:CandidateStructure . FILTER NOT EXISTS { ?candidate rsof:customerProcess ?process } }
