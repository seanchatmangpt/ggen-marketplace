PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?consumer (COUNT(?refinement) AS ?refinements) WHERE { ?consumer rsof:refinement ?refinement . } GROUP BY ?consumer ORDER BY DESC(?refinements)
