PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?consumer (COUNT(?qualification) AS ?qualifications) WHERE { ?consumer rsof:qualification ?qualification . } GROUP BY ?consumer ORDER BY DESC(?qualifications)
