PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate ?existing WHERE { ?candidate a rsof:CandidateStructure ; rsof:existingStructure ?existing . }
ORDER BY ?candidate ?existing
