PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate (COUNT(?option) AS ?options) WHERE { ?candidate rsof:packOption ?option . } GROUP BY ?candidate ORDER BY DESC(?options)
