PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate (COUNT(?consumer) AS ?consumers) WHERE { ?candidate rsof:consumer ?consumer . } GROUP BY ?candidate ORDER BY DESC(?consumers)
