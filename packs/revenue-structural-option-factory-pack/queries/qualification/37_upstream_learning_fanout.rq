PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?refinement (COUNT(?knowledge) AS ?knowledgeCount) WHERE { ?refinement rsof:upstreamLearning ?knowledge . } GROUP BY ?refinement ORDER BY DESC(?knowledgeCount)
