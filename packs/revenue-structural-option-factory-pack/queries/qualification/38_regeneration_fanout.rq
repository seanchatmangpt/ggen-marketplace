PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?knowledge (COUNT(?consumer) AS ?consumers) WHERE { ?knowledge rsof:regenerates ?consumer . } GROUP BY ?knowledge ORDER BY DESC(?consumers)
