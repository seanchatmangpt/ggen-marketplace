PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate (COUNT(?experiment) AS ?experiments) WHERE { ?candidate rsof:experiment ?experiment . } GROUP BY ?candidate ORDER BY DESC(?experiments)
