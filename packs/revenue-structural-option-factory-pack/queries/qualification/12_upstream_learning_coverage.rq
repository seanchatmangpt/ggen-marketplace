PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?refinement WHERE { ?consumer rsof:refinement ?refinement . FILTER NOT EXISTS { ?refinement rsof:upstreamLearning ?knowledge } }
