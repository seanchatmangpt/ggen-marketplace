PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate ?pack WHERE { ?candidate a rsof:CandidateStructure ; rsof:packOption ?pack . FILTER(CONTAINS(LCASE(STR(?pack)), "offer") || CONTAINS(LCASE(STR(?pack)), "package")) } ORDER BY ?candidate ?pack
