PREFIX rof: <https://ggen.dev/ontology/revenue-option-factory#>
SELECT ?candidate ?pack WHERE { ?candidate a rof:CandidateStructure ; rof:packOption ?pack . FILTER(CONTAINS(LCASE(STR(?pack)), "regen") || CONTAINS(LCASE(STR(?pack)), "consumer")) } ORDER BY ?candidate ?pack
