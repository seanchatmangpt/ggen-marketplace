PREFIX rof: <https://ggen.dev/ontology/revenue-option-factory#>
SELECT ?candidate ?pack WHERE { ?candidate a rof:CandidateStructure ; rof:packOption ?pack . FILTER(CONTAINS(LCASE(STR(?pack)), "resource")) } ORDER BY ?candidate ?pack
