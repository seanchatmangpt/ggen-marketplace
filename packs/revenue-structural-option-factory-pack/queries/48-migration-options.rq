PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?candidate ?pack WHERE { ?candidate a rsof:CandidateStructure ; rsof:packOption ?pack . FILTER(CONTAINS(LCASE(STR(?pack)), "migration")) } ORDER BY ?candidate ?pack
