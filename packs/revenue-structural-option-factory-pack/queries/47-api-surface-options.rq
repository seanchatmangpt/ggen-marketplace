PREFIX rof: <https://ggen.dev/ontology/revenue-option-factory#>
SELECT ?candidate ?pack WHERE { ?candidate a rof:CandidateStructure ; rof:packOption ?pack . FILTER(CONTAINS(LCASE(STR(?pack)), "api")) } ORDER BY ?candidate ?pack
