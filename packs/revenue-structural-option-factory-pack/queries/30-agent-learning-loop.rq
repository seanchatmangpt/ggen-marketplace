PREFIX rsof: <https://ggen.dev/ontology/revenue-structural-option-factory#>
SELECT ?baseline ?refinement ?knowledge ?consumer
WHERE {
  ?baseline rsof:refinement ?refinement .
  ?refinement rsof:upstreamLearning ?knowledge .
  ?knowledge rsof:regenerates ?consumer .
}
ORDER BY ?baseline ?refinement ?knowledge ?consumer
