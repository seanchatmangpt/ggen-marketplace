[project]
name = "wasm4pm-operator-applicability-pack"
version = "0.1.0"

[ontology]
files = ["ontology.ttl"]

# This pack is a canonical semantic fact carrier. It intentionally emits no
# runtime code and grants no execution authority. Downstream ggen consumers may
# project these admitted facts into language-specific eligibility tables.
