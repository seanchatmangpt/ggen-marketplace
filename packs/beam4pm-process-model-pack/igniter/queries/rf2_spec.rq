PREFIX rf2: <https://ggen.dev/beam4pm/rf2-conformance#>

SELECT ?oracle_name ?wire_op ?dataset_path ?module_name WHERE {
  ?stream a rf2:AdmittedOracleStream ;
    rf2:oracleName ?oracle_name ;
    rf2:wireOp ?wire_op ;
    rf2:datasetPath ?dataset_path ;
    rf2:moduleName ?module_name .
}
