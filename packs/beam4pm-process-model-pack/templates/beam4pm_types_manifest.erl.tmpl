---
to: src/beam4pm_types_manifest.erl
sparql:
  records: |
    PREFIX bpm: <https://ggen.dev/ontology/beam-process-model#>
    SELECT ?record_name ?record_doc WHERE {
      ?rt a bpm:RecordType ; bpm:recordName ?record_name ; bpm:recordDoc ?record_doc .
    } ORDER BY ?record_name
  fields: |
    PREFIX bpm: <https://ggen.dev/ontology/beam-process-model#>
    SELECT ?record_name ?field_name ?field_type ?field_doc ?field_required ?field_order WHERE {
      ?rt a bpm:RecordType ; bpm:recordName ?record_name ; bpm:hasField ?f .
      ?f bpm:fieldName ?field_name ; bpm:fieldType ?field_type ; bpm:fieldDoc ?field_doc ;
         bpm:fieldRequired ?field_required ; bpm:fieldOrder ?field_order .
    } ORDER BY ?record_name ?field_order
---
%% GENERATED by ggen from the admitted bpm:RecordType graph. Do not edit.
%%
%% Pure reflection module over the admitted bpm:RecordType graph: it returns
%% only atom lists describing the schema (every admitted record name, and
%% each record's ordered field-name list). It holds no reference to
%% beam4pm_types.erl's -record definitions, so it compiles and can be
%% generated safely independently of that module.

-module(beam4pm_types_manifest).

-export([record_names/0, fields/1]).

-spec record_names() -> [atom()].
record_names() -> [{% for r in records %}{{ r.record_name }}{% if not loop.last %}, {% endif %}{% endfor %}].

-spec fields(atom()) -> [atom()].
{% for r in records %}{% set rfields = fields | filter(attribute="record_name", value=r.record_name) %}fields({{ r.record_name }}) -> [{% for f in rfields %}{{ f.field_name }}{% if not loop.last %}, {% endif %}{% endfor %}];
{% endfor %}fields(_) -> [].
