---
to: test/test_helper.exs
sparql:
  records: |
    PREFIX bpm: <https://ggen.dev/ontology/beam-process-model#>
    SELECT ?record_name WHERE {
      ?rt a bpm:RecordType ; bpm:recordName ?record_name .
    } ORDER BY ?record_name
---
# GENERATED by ggen from the admitted bpm:RecordType graph. Do not edit.
# ExUnit bootstrap. test/beam4pm_types_test.exs iterates all {{ records | length }}
# admitted record type(s) at runtime inside 2 real tests (see that file's
# moduledoc for why this isn't one ExUnit test per record).
#
# :chicago is excluded by default -- real multi-node BEAM-distribution
# integration tests (test/chicago/) and any other test explicitly tagged
# :chicago for being a slow, full-integration-scope case. Run them via
# `mix test.chicago` (see mix.exs aliases) or `mix test --include chicago`.
ExUnit.start(exclude: [:chicago])
