PREFIX mem: <https://w3id.org/dfcm/memory#>
SELECT ?record WHERE { ?record a mem:MemoryRecord ; mem:current false . FILTER NOT EXISTS { ?record mem:opportunity ?op } FILTER NOT EXISTS { ?new mem:supersedes ?record } }
