PREFIX mem: <https://w3id.org/dfcm/memory#>
SELECT ?record ?subject WHERE { ?record a mem:MemoryRecord ; mem:exactSubject ?subject ; mem:current true . FILTER NOT EXISTS { ?record mem:receipt ?receipt } UNION { ?record mem:standing "UNKNOWN" } }
