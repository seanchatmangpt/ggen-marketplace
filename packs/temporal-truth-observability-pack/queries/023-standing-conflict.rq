PREFIX mem: <https://w3id.org/dfcm/memory#>
SELECT ?record (COUNT(DISTINCT ?standing) AS ?n) WHERE { ?record a mem:MemoryRecord ; mem:standing ?standing . } GROUP BY ?record HAVING(COUNT(DISTINCT ?standing) > 1)
