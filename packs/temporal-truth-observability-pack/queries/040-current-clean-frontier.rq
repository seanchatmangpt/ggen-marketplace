PREFIX mem: <https://w3id.org/dfcm/memory#>
SELECT ?record ?subject ?standing WHERE { ?record a mem:MemoryRecord ; mem:current true ; mem:exactSubject ?subject ; mem:standing ?standing ; mem:receipt ?receipt ; mem:source ?source . FILTER NOT EXISTS { ?c a mem:Contradiction ; mem:contradicts ?record } }
