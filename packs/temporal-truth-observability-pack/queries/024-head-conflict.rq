PREFIX mem: <https://w3id.org/dfcm/memory#>
SELECT ?record (COUNT(DISTINCT ?sha) AS ?n) WHERE { ?record a mem:MemoryRecord ; mem:headSha ?sha ; mem:current true . } GROUP BY ?record HAVING(COUNT(DISTINCT ?sha) > 1)
