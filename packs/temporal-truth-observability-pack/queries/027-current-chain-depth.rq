PREFIX mem: <https://w3id.org/dfcm/memory#>
SELECT ?record (COUNT(?ancestor) AS ?depth) WHERE { ?record mem:supersedes+ ?ancestor ; mem:current true . } GROUP BY ?record HAVING(COUNT(?ancestor) > 10)
