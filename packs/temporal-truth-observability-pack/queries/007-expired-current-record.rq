PREFIX mem: <https://w3id.org/dfcm/memory#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?record ?expiry WHERE { ?record a mem:MemoryRecord ; mem:current true ; mem:expiresAt ?expiry . FILTER(xsd:dateTime(?expiry) < NOW()) }
