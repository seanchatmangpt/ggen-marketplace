PREFIX dfcm: <https://ggen.dev/ontology/dfcm#>
SELECT ?order ?code ?text ?falsifier WHERE {
  ?inv a dfcm:Invariant ; dfcm:order ?order ; dfcm:ruleCode ?code ; dfcm:ruleText ?text ; dfcm:falsifiedBy ?f .
  FILTER(?order >= 110)
  ?f dfcm:slug ?falsifier .
}
ORDER BY ?order
