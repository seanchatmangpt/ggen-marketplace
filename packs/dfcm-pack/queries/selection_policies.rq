PREFIX dfcm: <https://ggen.dev/ontology/dfcm#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
SELECT ?policy ?label WHERE {
  ?p a dfcm:SelectionPolicy ; skos:prefLabel ?label .
  BIND(STRAFTER(STR(?p), "#") AS ?policy)
}
ORDER BY ?policy
