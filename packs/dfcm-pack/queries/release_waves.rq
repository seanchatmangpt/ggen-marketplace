PREFIX dfcm: <https://ggen.dev/ontology/dfcm#>
SELECT ?order ?slug ?percent ?prior WHERE {
  ?wave a dfcm:ReleaseWave ; dfcm:order ?order ; dfcm:slug ?slug ; dfcm:releasePercent ?percent .
  OPTIONAL { ?wave dfcm:requiresPriorWave ?priorNode . ?priorNode dfcm:slug ?prior . }
}
ORDER BY ?order
