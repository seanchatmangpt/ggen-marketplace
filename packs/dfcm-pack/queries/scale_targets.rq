PREFIX dfcm: <https://ggen.dev/ontology/dfcm#>
SELECT ?slug ?value ?unit WHERE {
  ?target a dfcm:ScaleTarget ; dfcm:slug ?slug ; dfcm:targetOnly true ; dfcm:targetValue ?value ; dfcm:targetUnit ?unit .
}
ORDER BY ?slug
