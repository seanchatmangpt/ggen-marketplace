PREFIX dfcm: <https://ggen.dev/ontology/dfcm#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
SELECT ?order ?slug ?label WHERE { ?x a dfcm:Phase ; dfcm:order ?order ; dfcm:slug ?slug ; skos:prefLabel ?label . } ORDER BY ?order
