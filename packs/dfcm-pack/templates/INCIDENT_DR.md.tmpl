# Fortune 5 Incident + Disaster Recovery Contract

{% for row in results %}Profile: `{{ row["slug"] }}`  
Standing: **UNKNOWN**  
Target availability: `{{ row["availability"] }}`  
Target RTO: `{{ row["rto"] }}s`  
Target RPO: `{{ row["rpo"] }}s`  
Maximum admitted blast radius: `{{ row["blast"] }}%`
{% endfor %}
## Incident rail

`detect → fence blast radius → preserve evidence → diagnose → construct options → SELECT → BRCE DO → receipt → replay → standing`

Incident pressure does not create ambient authority. Break-glass accelerates selection while retaining independent authority domains, BRCE, receipt, and replay.

## DR rail

Recovery is not ALIVE because a runbook exists. A recovery exercise must execute against the exact admitted subject and preserve a receipt that binds the recovery consequence and replay identity.
