{
  "schema": "https://ggen.dev/dfcm/enterprise-controls/v1",
  "standing": "UNKNOWN",
  "controls": [
{% for row in results %}    {"order": {{ row["order"] }}, "code": "{{ row["code"] }}", "slug": "{{ row["slug"] }}", "title": "{{ row["title"] }}", "domain": "{{ row["domain"] }}", "criticality": "{{ row["criticality"] }}", "automated": {{ row["automated"] }}, "evidence": "{{ row["evidence"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
