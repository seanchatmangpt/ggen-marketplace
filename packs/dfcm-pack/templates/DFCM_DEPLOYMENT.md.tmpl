# Full Chatman Ecosystem — DfCM Deployment Blueprint

**Initial standing:** `UNKNOWN` — generated declarations are not execution receipts.

## Deployment subjects

| # | Component | Layer | Required |
|---:|---|---|---|
{% for row in results %}| {{ row["order"] }} | `{{ row["slug"] }}` | {{ row["layer"] }} | {{ row["requiredJson"] }} |
{% endfor %}

## Governing construction law

Preserve → Fence/Chesterton → Calculus → Exclusions → Falsifier → Extension → Operationalization.

The graph is the deployment source. A failed edge is topology: retain all remaining admitted reversible paths. Irreversible selection requires its own admission evidence. `DO` is not inferred from construction; it is a separate BRCE-authorized transition with a receipt route.

The existing `chatman-ecosystem-release-pack` remains a release/publication boundary. This deployment blueprint cannot turn release metadata, generated code, planner output, proof terms, or hooks into ambient execution authority.
