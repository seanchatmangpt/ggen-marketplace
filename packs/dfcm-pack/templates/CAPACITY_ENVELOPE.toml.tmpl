# GENERATED design targets. Benchmark receipts are required before any ALIVE claim.
standing = "UNKNOWN"
target_only = true
overload_semantics = "refuse-or-shed-before-authority-receipt-replay-degrade"

{% for row in results %}[[target]]
slug = "{{ row["slug"] }}"
value = {{ row["value"] }}
unit = "{{ row["unit"] }}"
standing = "UNKNOWN"

{% endfor %}