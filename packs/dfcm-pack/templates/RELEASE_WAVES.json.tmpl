{
  "schema": "https://ggen.dev/dfcm/release-waves/v1",
  "standing": "UNKNOWN",
  "rule": "prior-wave-observed-and-admitted-before-expansion",
  "waves": [
{% for row in results %}    {"order": {{ row["order"] }}, "slug": "{{ row["slug"] }}", "population_percent": {{ row["percent"] }}}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
