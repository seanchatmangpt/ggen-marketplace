{
  "schema": "https://ggen.dev/dfcm/mfact-manifest/v1",
  "calculus": "ggen renders; Lean admits; mfact certifies",
  "formal_source": "consumer/dfcm/formal/DfcmAdmission.lean",
  "required_surfaces": ["ontology", "admission", "runtime_receipt", "replay", "standing"],
  "invariants": [
{% for row in results %}    {"code": "{{ row["code"] }}", "falsifier": "{{ row["falsifier"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
