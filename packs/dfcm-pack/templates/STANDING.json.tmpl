{
  "schema": "https://ggen.dev/dfcm/standing/v1",
  "status": "UNKNOWN",
  "scope": "generated-deployment-contract",
  "reason": "Manufacture is not execution; no external consequence has been observed.",
  "falsifiers": [
{% for row in results %}    {"id": "{{ row["slug"] }}", "condition": "{{ row["label"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
