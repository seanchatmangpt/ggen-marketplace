# GENERATED target envelope. It is not observed production standing.
{% for row in results %}[resilience]
profile = "{{ row["slug"] }}"
standing = "UNKNOWN"
target_only = true
minimum_regions = {{ row["regions"] }}
zones_per_region = {{ row["zones"] }}
minimum_providers = {{ row["providers"] }}
availability_target = {{ row["availability"] }}
rto_seconds = {{ row["rto"] }}
rpo_seconds = {{ row["rpo"] }}
max_blast_radius_percent = {{ row["blast"] }}
cell_isolation_required = true
active_active_required = true
single_provider_failure_must_preserve_lawful_continuation = true
{% endfor %}