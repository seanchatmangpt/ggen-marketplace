{
  "schema": "https://ggen.dev/dfcm/brce-policy/v1",
  "exclusive_do_route": "BRCE",
  "zero_unreceipted_actuation": true,
  "operations": [
{% for row in results %}    {"operation": "{{ row["slug"] }}", "reversibility": "{{ row["reversibility"] }}", "may_actuate": {{ row["mayActuateJson"] }}, "authority": "{{ row["authority"] }}", "receipt_route": "{{ row["receipt"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ],
  "hook_policy": "manufacture-intent-only"
}
