{
  "schema": "https://ggen.dev/dfcm/option-graph/v1",
  "subject": "chatman-ecosystem-full",
  "standing": "UNKNOWN",
  "failure_semantics": "edge-prunes-topology-not-graph",
  "dependency_edges": [
{% for row in results %}    {"from": "{{ row["from"] }}", "requires": "{{ row["to"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
