{
  "schema": "https://ggen.dev/dfcm/change-control/v1",
  "standing": "UNKNOWN",
  "authority": "BRCE-only",
  "human_approval_required": false,
  "independent-authority-domains-required": true,
  "classes": [
{% for row in results %}    {"order": {{ row["order"] }}, "slug": "{{ row["slug"] }}", "approval_quorum": {{ row["quorum"] }}, "independent_authority_domains": {{ row["domains"] }}, "machine_eligible": {{ row["machine"] }}, "canary_required": {{ row["canary"] }}, "rollback_required": {{ row["rollback"] }}}{% if not loop.last %},{% endif %}
{% endfor %}  ],
  "break_glass": "expedited-selection-still-BRCE-receipt-replay"
}
