{
  "schema": "https://ggen.dev/dfcm/data-boundaries/v1",
  "standing": "UNKNOWN",
  "residency": "policy-bound-no-jurisdiction-invented",
  "encryption_at_rest_required": true,
  "encryption_in_transit_required": true,
  "key_authority_isolation_required": true,
  "tenant_isolation_required": true,
  "classes": [
{% for row in results %}    {"order": {{ row["order"] }}, "slug": "{{ row["slug"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
