{% for row in results %}{
  "schema": "https://ggen.dev/dfcm/post-agi-readiness/v1",
  "profile": "{{ row["slug"] }}",
  "standing": "UNKNOWN",
  "target_only": true,
  "minimum_controls": {{ row["minimumControls"] }},
  "agent_population_ceiling": {{ row["populationCeiling"] }},
  "interruptibility_latency_ms": {{ row["interruptLatency"] }},
  "max_blast_radius_percent": {{ row["blast"] }},
  "human_authority_floor_required": true,
  "law": "targets-do-not-equal-observations",
  "note": "This profile does not claim any agent in this ecosystem has reached AGI capability. It is an admission envelope for a deployment operating agents whose capability, corrigibility, or population scale exceeds Fortune5Baseline's human-paced assumptions."
}
{% endfor %}