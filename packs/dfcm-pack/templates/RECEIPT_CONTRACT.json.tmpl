{
  "schema": "https://ggen.dev/dfcm/receipt-contract/v1",
  "receipt_dag_fields": ["source_identity", "base_identity", "admitted_subject", "authority", "intent", "consequence", "verifier", "toolchain", "environment", "replay_identity", "standing"],
  "morphisms": [
{% for row in results %}    {"order": {{ row["order"] }}, "id": "{{ row["slug"] }}", "from": "{{ row["from"] }}", "to": "{{ row["to"] }}", "reversibility": "{{ row["reversibility"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
