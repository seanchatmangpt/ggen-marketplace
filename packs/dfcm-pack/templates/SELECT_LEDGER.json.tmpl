{
  "schema": "https://ggen.dev/dfcm/select-ledger/v1",
  "standing": "UNKNOWN",
  "selection": null,
  "admitted": false,
  "irreversible_selection_requires_evidence": true,
  "invariants": [
{% for row in results %}    {"code": "{{ row["code"] }}", "rule": "{{ row["rule"] }}", "falsifier": "{{ row["falsifier"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
