{
  "schema": "https://ggen.dev/dfcm/enterprise-invariants/v1",
  "standing": "UNKNOWN",
  "invariants": [
{% for row in results %}    {"order": {{ row["order"] }}, "code": "{{ row["code"] }}", "rule": "{{ row["text"] }}", "falsifier": "{{ row["falsifier"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
