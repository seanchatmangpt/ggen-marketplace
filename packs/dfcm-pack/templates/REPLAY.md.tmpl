# DfCM Replay Contract

A replay may reuse prior verifier standing only when source, validator, toolchain, configuration, and environment identities match. Subject standing remains separately proven.

## Stage rail

{% for row in results %}{{ row["order"] }}. `{{ row["slug"] }}`
{% endfor %}

Replay the same admitted capsule twice and compare consequences. Any unexplained difference is `REFUSED:NONDETERMINISTIC_REPLAY`. Replaying manufacture is not permission to replay external `DO`; consequential replay must re-enter BRCE with current authority.
