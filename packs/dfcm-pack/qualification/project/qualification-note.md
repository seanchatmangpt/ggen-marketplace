# Qualification overlay

This project-profile pack requires no external authority. Marketplace qualification copies this source and runs `ggen sync` twice. The resulting consumer remains `UNKNOWN`; deterministic manufacture is not deployment `ALIVE`.
