PREFIX dfcm: <https://ggen.dev/ontology/dfcm#>
SELECT ?problem WHERE {
  { FILTER NOT EXISTS { dfcm:Fortune5Baseline a dfcm:EnterpriseReadinessProfile ; dfcm:targetOnly true ; dfcm:minimumControls ?m . FILTER(?m >= 24) } BIND("fortune5-profile-missing-or-underfilled" AS ?problem) }
}
