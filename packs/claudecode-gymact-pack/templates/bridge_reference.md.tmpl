---
to: "generated/reference.md"
sparql:
  ops: |
    PREFIX dct: <http://purl.org/dc/terms/>
    PREFIX sosa: <http://www.w3.org/ns/sosa/>
    SELECT ?id ?class WHERE { ?cap a sosa:Procedure ; dct:title ?id ; dct:type ?iri . BIND(IF(CONTAINS(STR(?iri), "consequence:do"), "Do", "Read") AS ?class) } ORDER BY ?id
---
# ClaudeCodeGym GymAct Bridge

Generated from the admitted ABox. Generated files carry no execution authority.

| id | consequence |
|---|---|
{% for row in ops -%}
| `{{ row.id }}` | {{ row.class }} |
{% endfor %}
