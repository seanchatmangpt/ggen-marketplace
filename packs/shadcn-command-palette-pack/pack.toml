[pack]
name = "shadcn-command-palette-pack"
version = "0.1.0"
description = "Cmd+K / Ctrl+K global command palette using shadcn Dialog + Input/Label, filtering an entity list by name/ID as-you-type with Enter to jump/select. Fills the confirmed-absent command-palette gap in the marketplace (no cmdk/kbar/Cmd+K pack existed prior to this)."
