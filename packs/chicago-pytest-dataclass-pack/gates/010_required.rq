# GATE: every cpd:DataclassProof must carry a module and an axiom.
# Non-empty result = violation.
PREFIX cpd: <http://seanchatmangpt.github.io/packs/chicago-pytest-dataclass#>
SELECT ?p WHERE {
  ?p a cpd:DataclassProof .
  FILTER NOT EXISTS { ?p cpd:module ?m }
}
