# Expand option capital without premature collapse

1. Admit fresh changed capabilities, existing capabilities, public ontologies, marketplace packs, repositories, runtime surfaces, and verification surfaces as graph nodes.
2. Emit pairwise and higher-order `dmc:Composition` nodes for lawful combinations.
3. Link every independently buildable result with `dmc:producesCandidate`; preserve multiple candidates when they coexist.
4. Attach `dmc:marketplaceSupport`, `dmc:missingPrimitive`, `dmc:qualification`, `dmc:reversibility`, `dmc:expectedReuse`, and `dmc:expectedCapabilitySpaceDelta` before ranking.
5. Run R12 courts 09-18 as refusal/falsifier gates, then courts 05-08 and 25-32 to inspect the surviving frontier.
6. Do not convert CONSTRUCT evidence into DO authority. A candidate with `dmc:actuationPerformed true` is rejected by the R12 actuation-drift court.
