PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?candidate ?experimentCost WHERE {
  ?candidate a dfcm:Candidate ; dfcm:uncertainty ?u ; dfcm:experimentCost ?experimentCost .
  FILTER(?u > 0.25 && ?experimentCost <= 0.30)
}
ORDER BY ?experimentCost DESC(?u) ?candidate
