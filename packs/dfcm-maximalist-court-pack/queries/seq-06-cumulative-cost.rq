PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence (SUM(?cost) AS ?cumulativeCost) WHERE {
  ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:stepCost ?cost .
} GROUP BY ?sequence ORDER BY ?sequence
