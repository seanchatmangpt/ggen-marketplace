PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?authority WHERE {
  ?r a dmc:ControllerGenerationRealization ; dmc:realizationAuthority ?authority .
  FILTER(?authority != "OBSERVE|VERIFY")
}
