PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?candidate ?rollback WHERE { ?candidate a dmc:RemediationCandidate ; dmc:rollbackCost ?rollback . FILTER(xsd:decimal(?rollback) <= 2) } ORDER BY xsd:decimal(?rollback) ?candidate
