PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?name (COUNT(DISTINCT ?rule) AS ?ruleCount) WHERE {
  ?rule a dmc:GenerationRule ; dmc:ruleName ?name .
}
GROUP BY ?name
HAVING (COUNT(DISTINCT ?rule) > 1)
ORDER BY DESC(?ruleCount) ?name
