PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?subject WHERE {
  ?r a dmc:ControllerGenerationRealization ; dmc:exactSubject ?subject .
  FILTER(!regex(str(?subject), "^[0-9a-f]{40}$"))
}
