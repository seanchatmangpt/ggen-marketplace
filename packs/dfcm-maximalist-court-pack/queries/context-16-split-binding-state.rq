PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule (COUNT(DISTINCT ?state) AS ?states) WHERE {
  ?rule a dmc:GenerationRule ; dmc:bindingState ?state .
} GROUP BY ?rule HAVING(COUNT(DISTINCT ?state) > 1) ORDER BY ?rule
