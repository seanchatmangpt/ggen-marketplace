PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?o ?receipt WHERE {
  ?o a dmc:GenerationCapabilityObservation ; dmc:capabilityReceiptDigest ?receipt .
  FILTER(!regex(str(?receipt), "^[0-9a-f]{64}$"))
} ORDER BY ?o
