PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?owner WHERE {
  ?r a dmc:ControllerGenerationRealization ; dmc:realizationOwnerStanding ?owner .
  FILTER(?owner IN ("FAIL", "BUILD_BROKEN", "BLOCKED"))
}
