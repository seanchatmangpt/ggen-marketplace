PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?obs ?value WHERE {
  ?obs a dmc:PolicyCalibrationObservation ; dmc:realizedDecision "STOP" ; dmc:observedNetValue ?value ; dmc:currentGeneration true .
  FILTER(?value > 0)
} ORDER BY DESC(?value) ?obs
