PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r WHERE {
  ?r a dmc:GenerationRealization ; dmc:realizationState "PASS" .
  FILTER NOT EXISTS { ?r dmc:receiptDigest ?receipt }
} ORDER BY ?r
