PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?skipEmpty (COUNT(DISTINCT ?rule) AS ?ruleCount) WHERE {
  ?rule a dmc:GenerationRule ; dmc:skipEmpty ?skipEmpty .
}
GROUP BY ?skipEmpty
ORDER BY DESC(?ruleCount)
