PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?novelty ?evidence ?rollback WHERE {
  ?candidate a dmc:Candidate ; dmc:ctqNovelty ?novelty ; dmc:ctqEvidence ?evidence ; dmc:rollbackCost ?rollback .
} ORDER BY DESC(?novelty) DESC(?evidence) ?rollback
