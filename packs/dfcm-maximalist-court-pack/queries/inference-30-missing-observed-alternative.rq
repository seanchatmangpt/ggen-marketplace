PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment WHERE {
  ?experiment a dmc:ExperimentRealization .
  FILTER NOT EXISTS { ?experiment dmc:observedAlternativeGain ?alternative }
} ORDER BY ?experiment
