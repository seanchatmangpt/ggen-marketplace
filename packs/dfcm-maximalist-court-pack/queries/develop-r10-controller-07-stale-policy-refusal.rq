PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?digest (COUNT(?obs) AS ?staleSupport) WHERE {
 ?obs a dmc:PolicyCalibrationObservation ; dmc:policyDigest ?digest ; dmc:currentGeneration false .
} GROUP BY ?digest HAVING(COUNT(?obs) > 0) ORDER BY DESC(?staleSupport) ?digest
