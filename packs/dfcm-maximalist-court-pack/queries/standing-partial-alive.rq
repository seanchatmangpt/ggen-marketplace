PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?family ?evidence ?falsifier WHERE {
  ?candidate a dmc:Candidate ; dmc:family ?family ; dmc:standing "PARTIAL_ALIVE" ; dmc:ctqEvidence ?evidence ; dmc:falsifier ?falsifier .
}
ORDER BY DESC(?evidence) ?family
