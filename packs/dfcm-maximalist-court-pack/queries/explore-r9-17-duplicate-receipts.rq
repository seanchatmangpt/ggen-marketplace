PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?receipt (COUNT(DISTINCT ?obs) AS ?uses) WHERE {
  ?obs a dmc:PolicyCalibrationObservation ; dmc:receiptDigest ?receipt .
} GROUP BY ?receipt HAVING(COUNT(DISTINCT ?obs) > 1) ORDER BY DESC(?uses) ?receipt
