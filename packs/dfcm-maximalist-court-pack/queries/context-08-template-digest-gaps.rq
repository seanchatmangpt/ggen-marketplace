PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule ?template WHERE {
  ?rule a dmc:GenerationRule ; dmc:templateFile ?template .
  FILTER NOT EXISTS { ?rule dmc:templateDigest ?digest }
} ORDER BY ?rule
