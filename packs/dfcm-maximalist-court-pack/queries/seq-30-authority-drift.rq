PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence ?index ?step ?authority WHERE {
  ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:stepIndex ?index ; dmc:authority ?authority .
  FILTER(?authority != "OBSERVE|VERIFY")
} ORDER BY ?sequence ?index
