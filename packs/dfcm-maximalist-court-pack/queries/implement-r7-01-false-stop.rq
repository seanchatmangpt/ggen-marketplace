PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?obs ?loss WHERE {
  ?obs a dmc:PolicyCalibrationObservation ; dmc:realizedDecision "STOP" ; dmc:decisionLoss ?loss .
  FILTER(xsd:decimal(?loss) > 0)
}
