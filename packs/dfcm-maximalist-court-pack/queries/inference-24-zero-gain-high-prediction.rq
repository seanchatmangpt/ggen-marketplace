PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment ?predicted WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:predictedInformationGain ?predicted ; dmc:realizedInformationGain 0 .
  FILTER(?predicted > 0)
} ORDER BY DESC(?predicted)
