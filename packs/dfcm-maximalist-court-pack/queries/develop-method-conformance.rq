PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?candidate ?family WHERE { ?candidate a dfcm:Candidate ; dfcm:family ?family . FILTER(REGEX(LCASE(STR(?family)),"conformance|process")) }
ORDER BY ?candidate
