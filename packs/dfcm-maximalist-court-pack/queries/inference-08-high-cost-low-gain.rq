PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment ?gain ?cost WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:realizedInformationGain ?gain ; dmc:experimentCost ?cost .
  FILTER(?cost > ?gain)
} ORDER BY DESC(?cost)
