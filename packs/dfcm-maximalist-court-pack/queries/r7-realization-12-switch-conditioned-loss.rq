PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT (AVG(xsd:decimal(?loss)) AS ?switchLoss) WHERE {
 ?o a dmc:PolicyCalibrationObservation ; dmc:policySwitch true ; dmc:decisionLoss ?loss ; dmc:currentGeneration true .
}
