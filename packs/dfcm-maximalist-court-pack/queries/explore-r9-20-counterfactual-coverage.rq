PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (COUNT(?obs) AS ?total) (COUNT(?cf) AS ?withCounterfactual) WHERE {
  ?obs a dmc:PolicyCalibrationObservation .
  OPTIONAL { ?obs dmc:counterfactualObserved ?cf }
}
