PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule ?variable WHERE {
  ?rule a dmc:GenerationRule ; dmc:projectedVariable ?variable .
  FILTER NOT EXISTS { ?rule dmc:requiredVariable ?variable }
} ORDER BY ?rule ?variable
