PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?evidence ?rollback ?assumption WHERE {
  ?candidate a dmc:Candidate ; dmc:family "fixed-point" ; dmc:ctqEvidence ?evidence ; dmc:rollbackCost ?rollback ; dmc:assumption ?assumption .
}
