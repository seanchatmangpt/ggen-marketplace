PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment ?predicted ?cost WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:currentGeneration true ; dmc:realizedInformationGain 0 ; dmc:predictedInformationGain ?predicted ; dmc:experimentCost ?cost .
} ORDER BY DESC(?predicted) DESC(?cost) ?experiment
