PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment ?authority WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:authority ?authority ; dmc:actuationPerformed false .
  FILTER(?authority IN ("OBSERVE","VERIFY","SELECT","CONSTRUCT"))
} ORDER BY ?experiment
