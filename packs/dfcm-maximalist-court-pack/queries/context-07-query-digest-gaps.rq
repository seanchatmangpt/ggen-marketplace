PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule ?query WHERE {
  ?rule a dmc:GenerationRule ; dmc:queryFile ?query .
  FILTER NOT EXISTS { ?rule dmc:queryDigest ?digest }
} ORDER BY ?rule
