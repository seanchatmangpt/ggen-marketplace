PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence (COUNT(?step) AS ?currentSteps) WHERE {
  ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:currentGeneration true .
} GROUP BY ?sequence ORDER BY ?sequence
