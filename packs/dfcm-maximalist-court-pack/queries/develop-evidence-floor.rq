PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?candidate ?evidence WHERE {
  ?candidate a dfcm:Candidate ; dfcm:evidenceScore ?evidence .
  FILTER(?evidence >= 0.70)
}
ORDER BY DESC(?evidence) ?candidate
