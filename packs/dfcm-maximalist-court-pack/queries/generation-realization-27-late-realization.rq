PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule ?r ?time WHERE {
  ?r a dmc:GenerationRealization ; dmc:realizesRule ?rule ; dmc:realizedAt ?time .
} ORDER BY ?rule DESC(?time)
