PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?receipt WHERE {
  ?r a dmc:ControllerGenerationRealization ; dmc:realizationReceiptDigest ?receipt .
  FILTER(!regex(str(?receipt), "^[0-9a-f]{64}$"))
}
