PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule ?name (COUNT(DISTINCT ?state) AS ?stateCount) WHERE {
  ?rule a dmc:GenerationRule ; dmc:ruleName ?name ; dmc:bindingState ?state .
}
GROUP BY ?rule ?name
HAVING (COUNT(DISTINCT ?state) > 1)
ORDER BY ?name
