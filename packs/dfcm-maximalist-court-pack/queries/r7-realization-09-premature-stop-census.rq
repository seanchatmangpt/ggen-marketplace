PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (COUNT(?o) AS ?prematureStops) WHERE {
 ?o a dmc:PolicyCalibrationObservation ; dmc:realizedDecision "STOP" ; dmc:sequenceTerminal false ; dmc:observedNetValue ?value ; dmc:currentGeneration true .
 FILTER(?value > 0)
}
