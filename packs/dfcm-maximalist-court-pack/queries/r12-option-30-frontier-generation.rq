PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?generation (COUNT(DISTINCT ?candidate) AS ?frontierSize) WHERE { ?candidate a dmc:ExecutableCandidate ; dmc:nonDominated true ; dmc:frontierGeneration ?generation . } GROUP BY ?generation ORDER BY ?generation
