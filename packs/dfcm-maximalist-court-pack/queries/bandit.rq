PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?assumption ?falsifier ?novelty WHERE {
  ?candidate a dmc:Candidate ; dmc:family "bandit" ; dmc:assumption ?assumption ; dmc:falsifier ?falsifier ; dmc:ctqNovelty ?novelty .
}
