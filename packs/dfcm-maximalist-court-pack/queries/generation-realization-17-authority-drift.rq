PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?authority WHERE {
  ?r a dmc:GenerationRealization ; dmc:realizationAuthority ?authority .
  FILTER(str(?authority) != "OBSERVE|VERIFY")
} ORDER BY ?r
