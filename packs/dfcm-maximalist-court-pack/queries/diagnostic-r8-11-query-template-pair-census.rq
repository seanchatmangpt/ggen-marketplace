PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?query ?template (COUNT(DISTINCT ?rule) AS ?ruleCount) WHERE {
  ?rule a dmc:GenerationRule ; dmc:queryFile ?query ; dmc:templateFile ?template .
}
GROUP BY ?query ?template
ORDER BY DESC(?ruleCount) ?query ?template
