PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment ?predicted ?realized ((?predicted - ?realized) AS ?overprediction) WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:currentGeneration true ; dmc:predictedInformationGain ?predicted ; dmc:realizedInformationGain ?realized .
  FILTER(?predicted > ?realized)
} ORDER BY DESC(?overprediction) ?experiment
