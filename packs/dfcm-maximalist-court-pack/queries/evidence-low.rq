PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?family ?evidence ?falsifier WHERE {
  ?candidate a dmc:Candidate ; dmc:family ?family ; dmc:ctqEvidence ?evidence ; dmc:falsifier ?falsifier .
  FILTER(?evidence <= 4)
}
ORDER BY ?evidence ?family
