PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT (AVG(xsd:decimal(?loss)) AS ?meanStopLoss) WHERE {
  ?o a dmc:PolicyCalibrationObservation ; dmc:realizedDecision "STOP" ; dmc:decisionLoss ?loss ; dmc:currentGeneration true .
}
