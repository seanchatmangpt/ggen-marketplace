PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (SUM(?cost) AS ?totalCost) WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:currentGeneration true ; dmc:experimentCost ?cost .
}
