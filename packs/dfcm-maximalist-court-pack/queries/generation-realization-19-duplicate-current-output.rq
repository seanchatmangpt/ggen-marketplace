PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?output (COUNT(?r) AS ?count) WHERE {
  ?r a dmc:GenerationRealization ; dmc:realizedOutput ?output ; dmc:outputCurrent true .
} GROUP BY ?output HAVING(COUNT(?r) > 1) ORDER BY DESC(?count) ?output
