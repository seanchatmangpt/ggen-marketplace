PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?pack (COUNT(DISTINCT ?candidate) AS ?candidates) WHERE { ?candidate a dmc:ExecutableCandidate ; dmc:marketplacePack ?pack . } GROUP BY ?pack ORDER BY DESC(?candidates) ?pack
