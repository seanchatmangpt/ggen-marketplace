PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (AVG(?predicted - ?realized) AS ?staleBias) WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:currentGeneration false ; dmc:predictedInformationGain ?predicted ; dmc:realizedInformationGain ?realized .
}
