PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?left ?right WHERE {
  ?left a dmc:RemediationCandidate ; dmc:compatibleWith ?right .
  ?right a dmc:RemediationCandidate ; dmc:compatibleWith ?left .
  FILTER(STR(?left) < STR(?right))
}
ORDER BY ?left ?right
