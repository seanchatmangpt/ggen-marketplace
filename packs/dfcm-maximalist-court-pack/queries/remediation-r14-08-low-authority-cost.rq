PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?candidate ?cost WHERE { ?candidate a dmc:RemediationCandidate ; dmc:authorityCost ?cost . FILTER(xsd:decimal(?cost) <= 1) } ORDER BY xsd:decimal(?cost) ?candidate
