PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?diagnostic WHERE {
  ?diagnostic a dmc:GenerationDiagnostic .
  FILTER NOT EXISTS { ?candidate a dmc:RemediationCandidate ; dmc:targetsRule ?diagnostic . }
}
ORDER BY ?diagnostic
