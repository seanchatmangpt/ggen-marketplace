PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (COUNT(?o) AS ?deadlineViolations) WHERE { ?o a dmc:PolicyCalibrationObservation ; dmc:deadlineMs ?deadline ; dmc:stepLatencyMs ?latency . FILTER(?latency > ?deadline) }
