PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?candidate ?uncertainty WHERE { ?candidate a dfcm:Candidate ; dfcm:uncertainty ?uncertainty . FILTER(?uncertainty <= 0.25) }
ORDER BY ?uncertainty ?candidate
