PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?timeout (COUNT(DISTINCT ?rule) AS ?ruleCount) WHERE {
  ?rule a dmc:GenerationRule ; dmc:qualificationTimeoutSeconds ?timeout .
}
GROUP BY ?timeout
ORDER BY ?timeout
