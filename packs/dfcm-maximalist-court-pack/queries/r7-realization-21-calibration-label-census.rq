PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?label (COUNT(?o) AS ?support) WHERE {
 ?o a dmc:PolicyCalibrationObservation ; dmc:calibrationLabel ?label ; dmc:currentGeneration true .
} GROUP BY ?label ORDER BY DESC(?support) ?label
