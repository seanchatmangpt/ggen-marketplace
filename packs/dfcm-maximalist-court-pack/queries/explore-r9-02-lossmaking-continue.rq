PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?obs ?value WHERE {
  ?obs a dmc:PolicyCalibrationObservation ; dmc:realizedDecision "CONTINUE" ; dmc:observedNetValue ?value ; dmc:currentGeneration true .
  FILTER(?value <= 0)
} ORDER BY ?value ?obs
