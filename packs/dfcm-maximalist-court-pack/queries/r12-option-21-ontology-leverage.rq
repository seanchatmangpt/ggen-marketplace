PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?ontology (COUNT(DISTINCT ?candidate) AS ?candidates) WHERE { ?candidate a dmc:ExecutableCandidate ; dmc:publicOntology ?ontology . } GROUP BY ?ontology ORDER BY DESC(?candidates) ?ontology
