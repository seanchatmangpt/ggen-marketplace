PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?candidate ?family WHERE { ?candidate a dfcm:Candidate ; dfcm:family ?family . FILTER(REGEX(LCASE(STR(?family)),"intervention|process")) FILTER NOT EXISTS { ?candidate dfcm:authority "DO" } }
ORDER BY ?candidate
