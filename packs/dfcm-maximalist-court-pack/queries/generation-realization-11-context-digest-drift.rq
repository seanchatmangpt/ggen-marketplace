PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?declared ?realized WHERE {
  ?r a dmc:GenerationRealization ; dmc:realizesRule ?rule ; dmc:realizedContextDigest ?realized .
  ?rule dmc:contextDigest ?declared . FILTER(str(?declared) != str(?realized))
} ORDER BY ?r
