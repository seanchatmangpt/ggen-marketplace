PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule (COUNT(?r) AS ?realizations) WHERE {
  ?rule a dmc:GenerationRule .
  OPTIONAL { ?r a dmc:GenerationRealization ; dmc:realizesRule ?rule . }
} GROUP BY ?rule ORDER BY ?rule
