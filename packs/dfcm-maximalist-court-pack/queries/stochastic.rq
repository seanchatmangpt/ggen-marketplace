PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?family ?rollback ?falsifier WHERE {
  ?candidate a dmc:Candidate ; dmc:family ?family ; dmc:deterministic false ; dmc:rollbackCost ?rollback ; dmc:falsifier ?falsifier .
} ORDER BY ?rollback ?family
