PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate (COUNT(DISTINCT ?variant) AS ?variants) WHERE { ?candidate a dmc:ExecutableCandidate ; dmc:algorithmVariant ?variant . } GROUP BY ?candidate ORDER BY DESC(?variants) ?candidate
