PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment ?stratum WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:currentGeneration true ; dmc:stratum ?stratum .
} ORDER BY ?stratum ?experiment
