PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?runtime (COUNT(DISTINCT ?candidate) AS ?candidates) WHERE { ?candidate a dmc:ExecutableCandidate ; dmc:runtimeSurface ?runtime . } GROUP BY ?runtime ORDER BY DESC(?candidates) ?runtime
