PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?gain WHERE { ?r a dmc:ExperimentRealization ; dmc:frontierBefore ?f ; dmc:frontierAfter ?f ; dmc:realizedInformationGain ?gain . FILTER(?gain > 0) } ORDER BY DESC(?gain)
