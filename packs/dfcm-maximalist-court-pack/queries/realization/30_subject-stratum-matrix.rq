PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sha ?stratum (AVG(?gain) AS ?meanGain) WHERE { ?r a dmc:ExperimentRealization ; dmc:subjectSha ?sha ; dmc:stratum ?stratum ; dmc:realizedInformationGain ?gain . } GROUP BY ?sha ?stratum ORDER BY ?sha ?stratum
