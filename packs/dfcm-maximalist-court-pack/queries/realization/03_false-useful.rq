PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?predicted ?realized WHERE { ?r a dmc:ExperimentRealization ; dmc:predictedInformationGain ?predicted ; dmc:realizedInformationGain ?realized . FILTER(?predicted > 0 && ?realized = 0) } ORDER BY DESC(?predicted)
