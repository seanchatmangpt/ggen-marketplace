PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?stratum (AVG(?gain-?cost) AS ?net) WHERE { ?r a dmc:ExperimentRealization ; dmc:stratum ?stratum ; dmc:realizedInformationGain ?gain ; dmc:experimentCost ?cost . } GROUP BY ?stratum ORDER BY ?net LIMIT 1
