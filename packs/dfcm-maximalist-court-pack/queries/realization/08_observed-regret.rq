PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?gain ?alt (?alt-?gain AS ?regret) WHERE { ?r a dmc:ExperimentRealization ; dmc:realizedInformationGain ?gain ; dmc:observedAlternativeGain ?alt . FILTER(?alt >= ?gain) } ORDER BY DESC(?regret)
