PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?latency ?gain WHERE { ?r a dmc:ExperimentRealization ; dmc:latencyMs ?latency ; dmc:realizedInformationGain ?gain . FILTER(?latency > 0 && ?gain = 0) } ORDER BY DESC(?latency)
