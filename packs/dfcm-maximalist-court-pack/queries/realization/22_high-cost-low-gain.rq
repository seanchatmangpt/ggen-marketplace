PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?cost ?gain WHERE { ?r a dmc:ExperimentRealization ; dmc:experimentCost ?cost ; dmc:realizedInformationGain ?gain . FILTER(?cost > ?gain) } ORDER BY DESC(?cost)
