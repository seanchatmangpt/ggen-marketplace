PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?before ?after ?gain WHERE { ?r a dmc:ExperimentRealization ; dmc:frontierBefore ?before ; dmc:frontierAfter ?after ; dmc:realizedInformationGain ?gain . FILTER(?before != ?after && ?gain > 0) } ORDER BY DESC(?gain)
