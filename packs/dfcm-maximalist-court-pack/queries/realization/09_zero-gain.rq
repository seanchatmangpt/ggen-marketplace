PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?predicted WHERE { ?r a dmc:ExperimentRealization ; dmc:predictedInformationGain ?predicted ; dmc:realizedInformationGain 0 . } ORDER BY DESC(?predicted)
