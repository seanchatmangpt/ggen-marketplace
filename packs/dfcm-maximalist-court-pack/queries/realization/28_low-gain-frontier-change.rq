PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?gain ?before ?after WHERE { ?r a dmc:ExperimentRealization ; dmc:realizedInformationGain ?gain ; dmc:frontierBefore ?before ; dmc:frontierAfter ?after . FILTER(?gain = 0 && ?before != ?after) } ORDER BY ?r
