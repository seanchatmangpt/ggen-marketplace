PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?gain WHERE { ?r a dmc:ExperimentRealization ; dmc:realizedInformationGain ?gain . FILTER NOT EXISTS { ?r dmc:observedAlternativeGain ?alt } } ORDER BY DESC(?gain)
