PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?before ?after (?before-?after AS ?shrinkage) WHERE { ?r a dmc:ExperimentRealization ; dmc:frontierBefore ?before ; dmc:frontierAfter ?after . FILTER(?after <= ?before) } ORDER BY DESC(?shrinkage)
