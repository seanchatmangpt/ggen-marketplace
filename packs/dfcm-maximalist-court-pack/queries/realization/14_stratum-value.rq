PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?stratum (AVG(?gain) AS ?meanGain) WHERE { ?r a dmc:ExperimentRealization ; dmc:stratum ?stratum ; dmc:realizedInformationGain ?gain . } GROUP BY ?stratum ORDER BY ?meanGain
