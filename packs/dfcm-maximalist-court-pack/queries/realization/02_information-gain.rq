PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?predicted ?realized (?realized-?predicted AS ?error) WHERE { ?r a dmc:ExperimentRealization ; dmc:predictedInformationGain ?predicted ; dmc:realizedInformationGain ?realized . } ORDER BY DESC(?realized)
