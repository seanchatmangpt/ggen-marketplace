PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?gain ?cost (?gain-?cost AS ?net) WHERE { ?r a dmc:ExperimentRealization ; dmc:realizedInformationGain ?gain ; dmc:experimentCost ?cost . } ORDER BY DESC(?net)
