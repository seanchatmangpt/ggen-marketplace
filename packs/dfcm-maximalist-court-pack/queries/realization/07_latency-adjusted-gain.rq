PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?gain ?latency (?gain/(1+?latency) AS ?rate) WHERE { ?r a dmc:ExperimentRealization ; dmc:realizedInformationGain ?gain ; dmc:latencyMs ?latency . } ORDER BY DESC(?rate)
