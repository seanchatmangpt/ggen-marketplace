PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?gain ?cost WHERE { ?r a dmc:ExperimentRealization ; dmc:frontierBefore ?f ; dmc:frontierAfter ?f ; dmc:realizedInformationGain ?gain ; dmc:experimentCost ?cost . } ORDER BY DESC(?cost)
