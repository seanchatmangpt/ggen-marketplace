PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?sha ?digest WHERE { ?r a dmc:ExperimentRealization ; dmc:subjectSha ?sha ; dmc:semanticDigest ?digest . FILTER regex(str(?sha), "^[0-9a-f]{40}$") FILTER regex(str(?digest), "^[0-9a-f]{64}$") } ORDER BY ?r
