PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment ?authority WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:authority ?authority .
  FILTER(?authority != "OBSERVE|VERIFY")
} ORDER BY ?experiment
