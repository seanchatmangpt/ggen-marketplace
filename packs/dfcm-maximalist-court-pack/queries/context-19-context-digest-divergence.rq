PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule (COUNT(DISTINCT ?digest) AS ?digests) WHERE {
  ?rule a dmc:GenerationRule ; dmc:contextDigest ?digest .
} GROUP BY ?rule HAVING(COUNT(DISTINCT ?digest) > 1) ORDER BY ?rule
