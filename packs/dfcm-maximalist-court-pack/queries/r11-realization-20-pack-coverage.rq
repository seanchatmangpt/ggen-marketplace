PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?pack (COUNT(?r) AS ?realizations) WHERE {
  ?r a dmc:ControllerGenerationRealization ; dmc:realizationCapabilityPack ?pack .
} GROUP BY ?pack ORDER BY DESC(?realizations) ?pack
