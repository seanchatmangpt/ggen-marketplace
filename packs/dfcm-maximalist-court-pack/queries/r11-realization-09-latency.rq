PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (AVG(?latency) AS ?meanLatencyMs) (MAX(?latency) AS ?maxLatencyMs) WHERE {
  ?r a dmc:ControllerGenerationRealization ; dmc:realizationLatencyMs ?latency .
}
