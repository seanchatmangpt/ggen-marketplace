PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?seconds (COUNT(?rule) AS ?rules) WHERE {
  ?rule a dmc:GenerationRule ; dmc:qualificationTimeoutSeconds ?seconds .
} GROUP BY ?seconds ORDER BY ?seconds
