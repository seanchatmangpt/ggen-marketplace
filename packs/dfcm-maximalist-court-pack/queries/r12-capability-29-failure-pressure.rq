PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?failure (COUNT(?r) AS ?count) WHERE { ?r a dmc:ControllerGenerationRealization ; dmc:realizationFailureClass ?failure . FILTER(?failure != "NONE") } GROUP BY ?failure ORDER BY DESC(?count) ?failure
