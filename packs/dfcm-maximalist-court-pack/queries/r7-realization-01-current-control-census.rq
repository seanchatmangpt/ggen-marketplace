PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?policyDigest (COUNT(?o) AS ?support) WHERE {
  ?o a dmc:PolicyCalibrationObservation ; dmc:currentGeneration true ; dmc:policyDigest ?policyDigest .
} GROUP BY ?policyDigest ORDER BY DESC(?support) ?policyDigest
