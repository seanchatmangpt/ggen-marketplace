PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence (SUM(?gain) AS ?cumulativeGain) WHERE {
  ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:realizedGain ?gain .
} GROUP BY ?sequence ORDER BY DESC(?cumulativeGain)
