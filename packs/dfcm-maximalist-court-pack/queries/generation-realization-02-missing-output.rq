PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?declared WHERE {
  ?r a dmc:GenerationRealization ; dmc:declaredOutput ?declared .
  FILTER NOT EXISTS { ?r dmc:realizedOutput ?actual }
} ORDER BY ?r
