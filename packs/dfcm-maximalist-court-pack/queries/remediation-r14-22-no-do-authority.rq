PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?authority WHERE {
  ?candidate a dmc:RemediationCandidate ; dmc:authority ?authority .
  FILTER(STR(?authority) = "DO")
}
ORDER BY ?candidate
