PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (SUM(IF(?deterministic,1,0)) AS ?deterministicCount) (COUNT(?r) AS ?total) WHERE {
  ?r a dmc:GenerationRealization ; dmc:deterministicReplay ?deterministic .
}
