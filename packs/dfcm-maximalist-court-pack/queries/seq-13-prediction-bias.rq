PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence (AVG(?predicted - ?realized) AS ?meanBias) WHERE {
  ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:predictedGain ?predicted ; dmc:realizedGain ?realized .
} GROUP BY ?sequence ORDER BY DESC(?meanBias)
