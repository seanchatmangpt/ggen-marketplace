PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?stratum (COUNT(?experiment) AS ?support) WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:stratum ?stratum ; dmc:currentGeneration true .
} GROUP BY ?stratum ORDER BY ASC(?support)
