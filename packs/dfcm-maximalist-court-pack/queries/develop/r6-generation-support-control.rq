PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?generation (COUNT(?o) AS ?support)
WHERE { ?o a dmc:PolicyCalibrationObservation ; dmc:policyGeneration ?generation ; dmc:currentGeneration true . }
GROUP BY ?generation
ORDER BY DESC(?support) ?generation
