PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence ?decision (COUNT(?step) AS ?count) WHERE { ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:policyDecision ?decision . } GROUP BY ?sequence ?decision ORDER BY ?sequence DESC(?count) ?decision
