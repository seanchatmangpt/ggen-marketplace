PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence (COUNT(?step) AS ?stops) WHERE { ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:policyDecision ?decision . FILTER(UCASE(STR(?decision)) = "STOP") } GROUP BY ?sequence ORDER BY DESC(?stops) ?sequence
