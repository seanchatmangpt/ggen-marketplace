PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence (SUM(?gain - ?cost) AS ?cumulativeNet) WHERE { ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:realizedGain ?gain ; dmc:stepCost ?cost . } GROUP BY ?sequence ORDER BY DESC(?cumulativeNet) ?sequence
