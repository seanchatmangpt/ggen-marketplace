PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate (COUNT(?assumption) AS ?assumptions) WHERE { ?candidate a dmc:Candidate ; dmc:assumption ?assumption . } GROUP BY ?candidate HAVING(COUNT(?assumption) > 0) ORDER BY DESC(?assumptions) ?candidate
