PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?utility WHERE {
  ?candidate a dmc:Candidate ; dmc:ctqEvidence ?e ; dmc:ctqNovelty ?n ; dmc:rollbackCost ?r .
  BIND((2 * ?e + ?n) - (2 * ?r) AS ?utility)
}
ORDER BY DESC(?utility) ?candidate
