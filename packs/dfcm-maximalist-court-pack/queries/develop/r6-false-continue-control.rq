PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (COUNT(?o) AS ?falseContinues)
WHERE { ?o a dmc:PolicyCalibrationObservation ; dmc:currentGeneration true ; dmc:realizedDecision "CONTINUE" ; dmc:observedNetValue ?v . FILTER(?v <= 0) }
