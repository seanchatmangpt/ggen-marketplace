PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (COUNT(DISTINCT ?digest) AS ?currentDigests)
WHERE { ?o a dmc:PolicyCalibrationObservation ; dmc:currentGeneration true ; dmc:policyDigest ?digest . }
HAVING(COUNT(DISTINCT ?digest) > 1)
