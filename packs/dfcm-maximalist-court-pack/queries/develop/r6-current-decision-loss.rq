PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (AVG(?loss) AS ?meanCurrentLoss) (MAX(?loss) AS ?worstCurrentLoss)
WHERE { ?o a dmc:PolicyCalibrationObservation ; dmc:currentGeneration true ; dmc:decisionLoss ?loss . }
