PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (AVG(IF(?switch, 1.0, 0.0)) AS ?currentSwitchRate)
WHERE { ?o a dmc:PolicyCalibrationObservation ; dmc:currentGeneration true ; dmc:policySwitch ?switch . }
