PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?mode (COUNT(DISTINCT ?rule) AS ?ruleCount) WHERE {
  ?rule a dmc:GenerationRule ; dmc:generationMode ?mode .
}
GROUP BY ?mode
ORDER BY DESC(?ruleCount) ?mode
