PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?assumption ?falsifier ?rollback WHERE {
  ?candidate a dmc:Candidate ; dmc:family "distributed" ; dmc:assumption ?assumption ; dmc:falsifier ?falsifier ; dmc:rollbackCost ?rollback .
}
