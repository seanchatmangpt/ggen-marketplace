PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?evidence ?rollback ?falsifier WHERE {
  ?candidate a dmc:Candidate ; dmc:family "process-intelligence" ; dmc:ctqEvidence ?evidence ; dmc:rollbackCost ?rollback ; dmc:falsifier ?falsifier .
}
