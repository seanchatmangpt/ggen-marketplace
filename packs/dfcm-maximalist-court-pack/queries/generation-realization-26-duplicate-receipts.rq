PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?receipt (COUNT(DISTINCT ?r) AS ?realizations) WHERE {
  ?r a dmc:GenerationRealization ; dmc:receiptDigest ?receipt .
} GROUP BY ?receipt HAVING(COUNT(DISTINCT ?r) > 1) ORDER BY DESC(?realizations) ?receipt
