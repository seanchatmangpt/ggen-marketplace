PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?mode (COUNT(?rule) AS ?count) WHERE {
  ?rule a dmc:GenerationRule ; dmc:generationMode ?mode .
} GROUP BY ?mode ORDER BY DESC(?count) ?mode
