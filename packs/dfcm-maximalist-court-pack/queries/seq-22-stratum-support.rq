PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?stratum (COUNT(?step) AS ?support) WHERE {
  ?step a dmc:ExperimentStep ; dmc:stratum ?stratum .
} GROUP BY ?stratum ORDER BY ?stratum
