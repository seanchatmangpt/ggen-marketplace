PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?link ?authority WHERE {
 ?link a dmc:ControllerGenerationCorrespondence ; dmc:correspondenceAuthority ?authority .
 FILTER(?authority NOT IN ("OBSERVE", "SELECT", "CONSTRUCT", "VERIFY"))
} ORDER BY ?link
