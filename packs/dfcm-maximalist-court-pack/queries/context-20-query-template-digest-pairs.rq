PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?pack ?rule ?queryDigest ?templateDigest WHERE {
  ?rule a dmc:GenerationRule ; dmc:sourcePack ?pack ; dmc:queryDigest ?queryDigest ; dmc:templateDigest ?templateDigest .
} ORDER BY ?pack ?rule
