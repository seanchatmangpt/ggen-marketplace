PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (COUNT(?r) AS ?mismatches) WHERE {
  ?r a dmc:ControllerGenerationRealization ;
     dmc:realizationExpectedSemanticDigest ?expected ;
     dmc:realizationActualSemanticDigest ?actual .
  FILTER(?expected != ?actual)
}
