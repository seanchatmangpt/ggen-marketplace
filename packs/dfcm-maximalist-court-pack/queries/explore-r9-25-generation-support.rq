PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?generation ?current (COUNT(?obs) AS ?support) WHERE {
  ?obs a dmc:PolicyCalibrationObservation ; dmc:policyGeneration ?generation ; dmc:currentGeneration ?current .
} GROUP BY ?generation ?current ORDER BY DESC(?generation) DESC(?current)
