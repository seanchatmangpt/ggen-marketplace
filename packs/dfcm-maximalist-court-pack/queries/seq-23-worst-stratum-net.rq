PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?stratum (MIN(?net) AS ?worstNetValue) WHERE {
  ?step a dmc:ExperimentStep ; dmc:stratum ?stratum ; dmc:realizedGain ?gain ; dmc:stepCost ?cost .
  BIND(?gain - ?cost AS ?net)
} GROUP BY ?stratum ORDER BY ?worstNetValue
