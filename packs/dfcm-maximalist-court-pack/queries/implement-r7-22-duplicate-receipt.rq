PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?receipt (COUNT(?obs) AS ?uses) WHERE { ?obs a dmc:PolicyCalibrationObservation ; dmc:receiptDigest ?receipt . } GROUP BY ?receipt HAVING(COUNT(?obs) > 1)
