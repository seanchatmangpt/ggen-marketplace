PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule (COUNT(?required) AS ?requiredCount) (COUNT(?projected) AS ?projectedCount) WHERE {
  ?rule a dmc:GenerationRule .
  OPTIONAL { ?rule dmc:requiredVariable ?required }
  OPTIONAL { ?rule dmc:projectedVariable ?projected }
} GROUP BY ?rule ORDER BY ?rule
