PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (AVG(IF(?decision = "STOP" && ?value > 0, 1.0, 0.0)) AS ?falseStopRate) WHERE {
  ?obs a dmc:PolicyCalibrationObservation ; dmc:realizedDecision ?decision ; dmc:observedNetValue ?value ; dmc:currentGeneration true .
}
