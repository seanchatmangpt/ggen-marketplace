PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule (SUM(IF(?current,1,0)) AS ?currentCount) (COUNT(?r) AS ?total) WHERE {
  ?r a dmc:GenerationRealization ; dmc:realizesRule ?rule ; dmc:outputCurrent ?current .
} GROUP BY ?rule ORDER BY ?rule
