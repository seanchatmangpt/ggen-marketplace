PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule WHERE {
  ?rule a dmc:GenerationRule .
  FILTER NOT EXISTS { ?r a dmc:GenerationRealization ; dmc:realizesRule ?rule . }
} ORDER BY ?rule
