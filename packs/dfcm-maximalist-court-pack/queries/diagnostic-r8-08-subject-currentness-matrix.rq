PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule ?name ?subject ?current WHERE {
  ?rule a dmc:GenerationRule ; dmc:ruleName ?name .
  OPTIONAL { ?rule dmc:exactSubject ?subject }
  OPTIONAL { ?rule dmc:currentGeneration ?current }
}
ORDER BY ?name
