PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence (SUM(?latency) AS ?cumulativeLatencyMs) WHERE {
  ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:stepLatencyMs ?latency .
} GROUP BY ?sequence ORDER BY ?sequence
