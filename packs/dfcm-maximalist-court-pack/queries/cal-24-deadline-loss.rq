PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (AVG(?loss) AS ?lateMeanLoss) (COUNT(?o) AS ?lateSupport) WHERE { ?o a dmc:PolicyCalibrationObservation ; dmc:deadlineMs ?deadline ; dmc:stepLatencyMs ?latency ; dmc:decisionLoss ?loss . FILTER(?latency > ?deadline) }
