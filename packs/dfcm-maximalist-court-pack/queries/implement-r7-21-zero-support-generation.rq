PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?generation (COUNT(?obs) AS ?support) WHERE { ?obs a dmc:PolicyCalibrationObservation ; dmc:currentGeneration true ; dmc:policyGeneration ?generation . } GROUP BY ?generation HAVING(COUNT(?obs) < 2)
