PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?variable (COUNT(?rule) AS ?affectedRules) WHERE {
  ?rule a dmc:GenerationRule ; dmc:missingVariable ?variable .
} GROUP BY ?variable ORDER BY DESC(?affectedRules) ?variable
