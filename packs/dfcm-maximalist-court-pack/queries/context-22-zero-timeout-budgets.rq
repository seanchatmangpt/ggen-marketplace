PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?rule ?seconds WHERE {
  ?rule a dmc:GenerationRule ; dmc:qualificationTimeoutSeconds ?seconds .
  FILTER(xsd:integer(?seconds) <= 0)
} ORDER BY ?rule
