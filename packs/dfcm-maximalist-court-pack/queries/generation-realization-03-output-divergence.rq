PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?declared ?actual WHERE {
  ?r a dmc:GenerationRealization ; dmc:declaredOutput ?declared ; dmc:realizedOutput ?actual .
  FILTER(str(?declared) != str(?actual))
} ORDER BY ?r
