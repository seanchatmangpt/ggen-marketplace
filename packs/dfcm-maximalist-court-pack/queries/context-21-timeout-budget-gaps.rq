PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rule WHERE {
  ?rule a dmc:GenerationRule .
  FILTER NOT EXISTS { ?rule dmc:qualificationTimeoutSeconds ?seconds }
} ORDER BY ?rule
