PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?c (COUNT(DISTINCT ?part) AS ?arity) WHERE { ?c a dmc:Composition ; dmc:composes ?part . } GROUP BY ?c HAVING(COUNT(DISTINCT ?part)=2) ORDER BY ?c
