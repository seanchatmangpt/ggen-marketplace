PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?receipt (COUNT(?r) AS ?count) WHERE {
  ?r a dmc:GenerationRealization ; dmc:receiptDigest ?receipt .
} GROUP BY ?receipt ORDER BY DESC(?count) ?receipt
