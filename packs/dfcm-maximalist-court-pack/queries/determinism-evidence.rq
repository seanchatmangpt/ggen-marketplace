PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?family ?evidence ?rollback WHERE {
  ?candidate a dmc:Candidate ; dmc:family ?family ; dmc:deterministic true ; dmc:ctqEvidence ?evidence ; dmc:rollbackCost ?rollback .
}
ORDER BY DESC(?evidence) ?rollback ?family
