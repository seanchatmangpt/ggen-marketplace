PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?state (COUNT(?r) AS ?count) WHERE {
  ?r a dmc:GenerationRealization ; dmc:realizationState ?state .
} GROUP BY ?state ORDER BY ?state
