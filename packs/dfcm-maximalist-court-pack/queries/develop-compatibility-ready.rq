PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT DISTINCT ?candidate WHERE {
  ?candidate a dfcm:Candidate .
  OPTIONAL { ?candidate dfcm:compatibleWith ?peer . }
  FILTER(BOUND(?peer))
}
ORDER BY ?candidate
