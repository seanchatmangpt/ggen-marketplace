PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?experiment ?before ?after WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:realizedInformationGain 0 ; dmc:frontierBefore ?before ; dmc:frontierAfter ?after .
  FILTER(?after < ?before)
} ORDER BY DESC(?before-?after)
