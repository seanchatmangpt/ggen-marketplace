PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?generation (COUNT(?obs) AS ?support) WHERE {
  ?obs a dmc:PolicyCalibrationObservation ; dmc:policyGeneration ?generation ; dmc:currentGeneration true .
} GROUP BY ?generation HAVING(COUNT(?obs) < 5) ORDER BY ?generation
