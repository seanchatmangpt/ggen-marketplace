PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (SUM(IF(?decision="STOP" && ?value>0,1,0))/COUNT(?o) AS ?falseStopRate) WHERE {
 ?o a dmc:PolicyCalibrationObservation ; dmc:realizedDecision ?decision ; dmc:observedNetValue ?value ; dmc:currentGeneration true .
}
