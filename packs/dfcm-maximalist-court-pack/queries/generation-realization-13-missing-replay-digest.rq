PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r WHERE {
  ?r a dmc:GenerationRealization ; dmc:deterministicReplay true .
  FILTER NOT EXISTS { ?r dmc:replayOutputDigest ?digest }
} ORDER BY ?r
