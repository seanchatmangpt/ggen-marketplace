PREFIX dfcm: <https://chatman.ai/dfcm#>
SELECT ?candidate ?family WHERE {
  ?candidate a dfcm:Candidate ; dfcm:family ?family ; dfcm:requiresReceipt true .
  FILTER(REGEX(LCASE(STR(?family)),"process|powl|event|object|runtime|search"))
}
ORDER BY LCASE(STR(?family)) ?candidate
