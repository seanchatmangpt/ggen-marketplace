PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?controller (COUNT(?r) AS ?realizations) WHERE {
  ?r a dmc:ControllerGenerationRealization ; dmc:realizationControllerDigest ?controller .
} GROUP BY ?controller ORDER BY DESC(?realizations) ?controller
