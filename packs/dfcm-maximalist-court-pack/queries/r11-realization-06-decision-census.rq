PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?decision (COUNT(?r) AS ?count) WHERE {
  ?r a dmc:ControllerGenerationRealization ; dmc:realizationControllerDecision ?decision .
} GROUP BY ?decision ORDER BY ?decision
