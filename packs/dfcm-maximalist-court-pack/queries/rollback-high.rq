PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?family ?rollback ?falsifier WHERE {
  ?candidate a dmc:Candidate ; dmc:family ?family ; dmc:rollbackCost ?rollback ; dmc:falsifier ?falsifier .
  FILTER(?rollback >= 2)
}
ORDER BY DESC(?rollback) ?family
