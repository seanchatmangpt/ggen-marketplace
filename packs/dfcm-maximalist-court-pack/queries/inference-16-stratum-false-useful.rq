PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?stratum (COUNT(?experiment) AS ?falseUsefulCount) WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:stratum ?stratum ; dmc:falseUseful true .
} GROUP BY ?stratum ORDER BY DESC(?falseUsefulCount)
