PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate WHERE {
  ?candidate a dmc:RemediationCandidate .
  FILTER NOT EXISTS { ?candidate dmc:dependsOnCandidate ?dependency . FILTER NOT EXISTS { ?dependency a dmc:RemediationCandidate . } }
}
ORDER BY ?candidate
