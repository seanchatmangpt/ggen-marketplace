PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT (SUM(?gain) AS ?totalRealizedGain) WHERE {
  ?experiment a dmc:ExperimentRealization ; dmc:currentGeneration true ; dmc:realizedInformationGain ?gain .
}
