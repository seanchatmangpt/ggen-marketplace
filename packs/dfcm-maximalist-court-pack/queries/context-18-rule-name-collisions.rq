PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?pack ?name (COUNT(?rule) AS ?count) WHERE {
  ?rule a dmc:GenerationRule ; dmc:sourcePack ?pack ; dmc:ruleName ?name .
} GROUP BY ?pack ?name HAVING(COUNT(?rule) > 1) ORDER BY ?pack ?name
