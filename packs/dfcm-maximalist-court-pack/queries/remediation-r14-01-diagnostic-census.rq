PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?code (COUNT(?d) AS ?diagnostics) WHERE {
  ?d a dmc:GenerationDiagnostic ; dmc:diagnosticCode ?code .
}
GROUP BY ?code
ORDER BY DESC(?diagnostics) ?code
