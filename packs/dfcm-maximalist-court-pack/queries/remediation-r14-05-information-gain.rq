PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?candidate ?gain WHERE { ?candidate a dmc:RemediationCandidate ; dmc:informationGain ?gain . FILTER(xsd:decimal(?gain) >= 0) } ORDER BY DESC(xsd:decimal(?gain)) ?candidate
