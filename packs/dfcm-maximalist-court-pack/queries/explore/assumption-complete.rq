PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?assumption WHERE { ?candidate a dmc:Candidate ; dmc:assumption ?assumption . FILTER(STRLEN(STR(?assumption)) > 0) } ORDER BY STR(?candidate)
