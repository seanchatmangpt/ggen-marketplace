PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?falsifier WHERE { ?candidate a dmc:Candidate ; dmc:falsifier ?falsifier . FILTER(STRLEN(STR(?falsifier)) > 0) } ORDER BY STR(?candidate)
