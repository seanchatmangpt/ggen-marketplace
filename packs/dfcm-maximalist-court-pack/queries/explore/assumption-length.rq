PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?assumption (STRLEN(STR(?assumption)) AS ?length) WHERE { ?candidate a dmc:Candidate ; dmc:assumption ?assumption . } ORDER BY DESC(?length) STR(?candidate)
