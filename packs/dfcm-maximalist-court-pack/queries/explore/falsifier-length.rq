PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?falsifier (STRLEN(STR(?falsifier)) AS ?length) WHERE { ?candidate a dmc:Candidate ; dmc:falsifier ?falsifier . } ORDER BY DESC(?length) STR(?candidate)
