PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?novelty (COUNT(?candidate) AS ?count) WHERE { ?candidate a dmc:Candidate ; dmc:ctqNovelty ?novelty . } GROUP BY ?novelty ORDER BY DESC(?novelty)