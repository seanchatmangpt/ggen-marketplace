PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?evidence ?novelty WHERE { ?candidate a dmc:Candidate ; dmc:ctqEvidence ?evidence ; dmc:ctqNovelty ?novelty . FILTER NOT EXISTS { ?candidate dmc:dependency ?dependency . } } ORDER BY DESC(?evidence) DESC(?novelty)