PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?family ?evidence ?rollback WHERE { ?candidate a dmc:Candidate ; dmc:deterministic false ; dmc:family ?family ; dmc:ctqEvidence ?evidence ; dmc:rollbackCost ?rollback . } ORDER BY ASC(?rollback) DESC(?evidence)