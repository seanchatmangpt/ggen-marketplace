PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?deterministic (AVG(?evidence) AS ?avgEvidence) (COUNT(?candidate) AS ?count) WHERE { ?candidate a dmc:Candidate ; dmc:deterministic ?deterministic ; dmc:ctqEvidence ?evidence . } GROUP BY ?deterministic ORDER BY DESC(?avgEvidence)