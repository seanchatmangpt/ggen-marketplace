PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?novelty ?rollback WHERE { ?candidate a dmc:Candidate ; dmc:ctqNovelty ?novelty ; dmc:rollbackCost ?rollback . FILTER(?novelty >= 8 && ?rollback <= 1) } ORDER BY DESC(?novelty)