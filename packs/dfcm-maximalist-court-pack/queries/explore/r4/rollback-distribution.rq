PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?rollback (COUNT(?candidate) AS ?count) WHERE { ?candidate a dmc:Candidate ; dmc:rollbackCost ?rollback . } GROUP BY ?rollback ORDER BY ASC(?rollback)