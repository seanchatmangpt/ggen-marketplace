PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?candidate ?score WHERE { ?candidate a dmc:Candidate ; dmc:ctqEvidence ?e ; dmc:rollbackCost ?r . BIND((?e * 1.0) / (?r + 1) AS ?score) } ORDER BY DESC(?score)