PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?family (MAX(?novelty) AS ?maxNovelty) WHERE { ?candidate a dmc:Candidate ; dmc:family ?family ; dmc:ctqNovelty ?novelty . } GROUP BY ?family ORDER BY DESC(?maxNovelty) ?family
