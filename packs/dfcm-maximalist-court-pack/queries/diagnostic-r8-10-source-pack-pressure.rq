PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?pack (COUNT(DISTINCT ?rule) AS ?ruleCount) WHERE {
  ?rule a dmc:GenerationRule ; dmc:sourcePack ?pack .
}
GROUP BY ?pack
ORDER BY DESC(?ruleCount) ?pack
