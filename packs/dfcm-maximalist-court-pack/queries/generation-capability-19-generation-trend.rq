PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?generation (AVG(IF(?ok,1.0,0.0)) AS ?successRate) WHERE {
  ?o a dmc:GenerationCapabilityObservation ; dmc:capabilityGeneration ?generation ; dmc:capabilitySuccess ?ok .
} GROUP BY ?generation ORDER BY ?generation
