PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?output (COUNT(?rule) AS ?writers) WHERE {
  ?rule a dmc:GenerationRule ; dmc:outputFile ?output .
} GROUP BY ?output HAVING(COUNT(?rule) > 1) ORDER BY ?output
