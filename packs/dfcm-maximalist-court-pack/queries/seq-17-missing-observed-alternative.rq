PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?sequence ?index ?step WHERE {
  ?step a dmc:ExperimentStep ; dmc:sequenceId ?sequence ; dmc:stepIndex ?index .
  FILTER NOT EXISTS { ?step dmc:observedAlternative ?alternative }
} ORDER BY ?sequence ?index
