PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?stratum (AVG(?loss) AS ?meanLoss) (MAX(?loss) AS ?worstLoss) WHERE { ?o a dmc:PolicyCalibrationObservation ; dmc:stratum ?stratum ; dmc:decisionLoss ?loss . } GROUP BY ?stratum ORDER BY DESC(?worstLoss)
