PREFIX dmc: <https://ggen.dev/ontology/dfcm-maximalist-court#>
SELECT ?r ?actual ?replay WHERE {
  ?r a dmc:GenerationRealization ; dmc:realizedOutputDigest ?actual ; dmc:replayOutputDigest ?replay .
  FILTER(str(?actual) != str(?replay))
} ORDER BY ?r
