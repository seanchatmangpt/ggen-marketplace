PREFIX sfe: <https://seanchatmangpt.dev/ontology/selection-frontier-execution#>
SELECT ?run ?authority ?actuation WHERE {
  ?run a sfe:SelectionExecution ; sfe:authority ?authority ; sfe:actuationPerformed ?actuation .
  FILTER(?authority NOT IN ("SELECT", "CONSTRUCT", "VERIFY") || ?actuation = true)
}
