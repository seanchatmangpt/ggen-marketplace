PREFIX sfe: <https://seanchatmangpt.dev/ontology/selection-frontier-execution#>
SELECT ?candidate ?falsifier ?uncertainty WHERE {
  ?candidate a sfe:Candidate ; sfe:uncertainty ?uncertainty ; sfe:falsifier ?falsifier .
  FILTER(?uncertainty > 0.0)
}
ORDER BY DESC(?uncertainty) ?candidate ?falsifier
