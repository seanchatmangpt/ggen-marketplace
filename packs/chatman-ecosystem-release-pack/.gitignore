# `ggen sync run` consequences of the qualification consumers are never committed
# (repository CLAUDE.md: generated consumer files are not a second source of truth).
# Mutant M6 re-includes its single hand-edited rendered file in its own .gitignore.
qualification/consumer-v26.9.23/out/
qualification/mutants/*/out/*
