# MESSAGE: exactly one cnv-policies:Config individual must be admitted; any row (count != 1) is a refusal.
PREFIX cnv-policies: <https://clap-noun-verb.dev/ontology/policies#>
SELECT (COUNT(?subject) AS ?count) WHERE {
  ?subject a cnv-policies:Config .
}
HAVING (COUNT(?subject) != 1)
