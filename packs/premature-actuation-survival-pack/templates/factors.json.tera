{
  "schema": "ggen.survival-factor-catalog/1",
  "factors": [
{% for row in rows %}
    {
      "name": "{{ row.factorName }}",
      "level": "{{ row.levelId }}"
    }{% if not loop.last %},{% endif %}
{% endfor %}
  ]
}
