{
  "schema": "ggen.survival-fault-catalog/1",
  "authority": "none",
  "faults": [
{% for row in rows %}
    {
      "kind": "{{ row.faultKindId }}",
      "actuationPerformed": false
    }{% if not loop.last %},{% endif %}
{% endfor %}
  ]
}
