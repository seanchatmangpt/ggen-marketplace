PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?composition WHERE { ?composition a cpc:Composition . FILTER NOT EXISTS { ?composition cpc:causedBy ?consumer . ?consumer a cpc:Consumer ; cpc:independent true } }
