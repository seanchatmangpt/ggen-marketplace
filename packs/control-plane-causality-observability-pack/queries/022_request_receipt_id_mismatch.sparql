PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?request ?receipt WHERE { ?request a cpc:Request ; cpc:requestId ?rid . ?receipt a cpc:Receipt ; cpc:requestId ?oid . FILTER(?rid != ?oid) }
