PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?x WHERE { ?x a ?type . FILTER(?type IN (cpc:Request,cpc:WorkflowRun,cpc:Receipt)) FILTER NOT EXISTS { ?x cpc:headSha ?sha } }
