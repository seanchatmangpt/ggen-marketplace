PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?id (COUNT(?run) AS ?fanout) WHERE { ?request a cpc:Request ; cpc:requestId ?id . ?run a cpc:WorkflowRun ; cpc:requestId ?id } GROUP BY ?id HAVING(COUNT(?run) > 1)
