PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?request ?receipt WHERE { ?request a cpc:Request ; cpc:requestId ?id ; cpc:current false . ?receipt a cpc:Receipt ; cpc:requestId ?id ; cpc:current true . }
