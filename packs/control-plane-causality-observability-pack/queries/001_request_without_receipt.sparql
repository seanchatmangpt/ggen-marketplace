PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?request WHERE { ?request a cpc:Request . FILTER NOT EXISTS { ?receipt a cpc:Receipt ; cpc:requestId ?id . ?request cpc:requestId ?id } }
