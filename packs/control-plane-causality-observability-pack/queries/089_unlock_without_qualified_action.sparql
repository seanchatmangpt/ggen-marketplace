PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?unlock WHERE { ?unlock a cpc:DependencyUnlock . FILTER NOT EXISTS { ?unlock cpc:causedBy ?action . ?action a cpc:QualifiedManufacturingAction } }
