PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?yield ?unlocks WHERE { ?yield a cpc:YieldObservation ; cpc:memoryUnlocks ?unlocks . FILTER(?unlocks > 0) FILTER NOT EXISTS { ?yield cpc:memoryRecordsConsumed ?records } }
