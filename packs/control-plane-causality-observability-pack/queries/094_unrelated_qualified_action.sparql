PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?action WHERE { ?action a cpc:QualifiedManufacturingAction . FILTER NOT EXISTS { ?action cpc:causedBy ?consumer . ?consumer a cpc:Consumer } }
