PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?consumer ?independent ?standing WHERE { ?consumer a cpc:Consumer ; cpc:independent ?independent ; cpc:standing ?standing . } ORDER BY ?consumer
