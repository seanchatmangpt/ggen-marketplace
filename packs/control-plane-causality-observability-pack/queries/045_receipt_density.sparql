PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT (COUNT(?receipt) AS ?receipts) (COUNT(?request) AS ?requests) WHERE { OPTIONAL { ?receipt a cpc:Receipt } OPTIONAL { ?request a cpc:Request } }
