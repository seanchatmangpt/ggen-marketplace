PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
PREFIX prov: <http://www.w3.org/ns/prov#>
SELECT ?receipt WHERE { ?receipt a cpc:Receipt . FILTER NOT EXISTS { ?receipt prov:wasGeneratedBy ?run . ?run a cpc:WorkflowRun } }
