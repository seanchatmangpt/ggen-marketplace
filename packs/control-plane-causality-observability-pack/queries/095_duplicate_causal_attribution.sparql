PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?action (COUNT(?cause) AS ?causeCount) WHERE { ?action a cpc:QualifiedManufacturingAction ; cpc:causedBy ?cause . } GROUP BY ?action HAVING(COUNT(?cause) > 1)
