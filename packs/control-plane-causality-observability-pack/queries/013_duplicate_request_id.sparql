PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?id (COUNT(?request) AS ?count) WHERE { ?request a cpc:Request ; cpc:requestId ?id } GROUP BY ?id HAVING(COUNT(?request) > 1)
