PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?yield ?records WHERE { ?yield a cpc:YieldObservation ; cpc:memoryRecordsConsumed ?records ; cpc:memoryUnlocks 0 . FILTER(?records > 0) }
