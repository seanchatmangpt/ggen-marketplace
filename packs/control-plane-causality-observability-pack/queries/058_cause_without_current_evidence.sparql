PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?action ?cause WHERE { ?action a cpc:QualifiedManufacturingAction ; cpc:causedBy ?cause . FILTER NOT EXISTS { ?action cpc:currentEvidence true } }
