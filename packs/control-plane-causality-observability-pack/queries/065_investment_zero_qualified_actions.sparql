PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?yield ?units WHERE { ?yield a cpc:YieldObservation ; cpc:investmentUnits ?units ; cpc:qualifiedActionsUnlocked 0 . FILTER(?units > 0) }
