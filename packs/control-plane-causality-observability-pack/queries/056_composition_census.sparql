PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?composition ?independent ?standing WHERE { ?composition a cpc:Composition ; cpc:independent ?independent ; cpc:standing ?standing . }
