PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?key (COUNT(?m) AS ?count) WHERE { ?m a cpc:MemoryRecord ; cpc:memoryKey ?key ; cpc:archived false } GROUP BY ?key HAVING(COUNT(?m) > 1)
