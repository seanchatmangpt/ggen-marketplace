PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?consumer WHERE { ?consumer a cpc:Consumer . FILTER NOT EXISTS { ?consumer cpc:causedBy ?primitive . ?primitive a cpc:ReusablePrimitive } }
