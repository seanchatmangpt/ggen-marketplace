PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?yield (?reuse * ?consumer * ?composition AS ?factorProduct) WHERE { ?yield a cpc:YieldObservation ; cpc:reuseFactor ?reuse ; cpc:consumerFactor ?consumer ; cpc:compositionFactor ?composition ; cpc:currentEvidence true . }
