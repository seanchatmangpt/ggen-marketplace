PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
PREFIX prov: <http://www.w3.org/ns/prov#>
SELECT ?run WHERE { ?run a cpc:WorkflowRun . FILTER NOT EXISTS { ?run prov:used ?request . ?request a cpc:Request } }
