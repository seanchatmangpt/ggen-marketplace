PREFIX cpc: <https://chatman.ai/ontology/control-plane-causality#>
SELECT ?receipt WHERE { ?receipt a cpc:Receipt ; cpc:requestId ?id . FILTER NOT EXISTS { ?request a cpc:Request ; cpc:requestId ?id } }
