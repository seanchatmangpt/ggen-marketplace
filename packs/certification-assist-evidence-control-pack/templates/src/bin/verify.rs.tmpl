fn main() -> Result<(), Box<dyn std::error::Error>> {
    let report = certification_assist_evidence_control_consumer::verify()?;
    let json = serde_json::to_string_pretty(&report)?;
    println!("{json}");
    Ok(())
}
