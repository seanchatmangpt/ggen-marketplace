PREFIX dfcm: <https://seanchatman.dev/ontology/dfcm#>
SELECT ?left ?right WHERE { ?edge a dfcm:CompatibilityEdge ; dfcm:left ?left ; dfcm:right ?right . FILTER(STR(?left) < STR(?right)) } ORDER BY ?left ?right
