PREFIX dfcm: <https://seanchatman.dev/ontology/dfcm#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?candidate dfcm:subject ?subject .
    FILTER (!REGEX(STR(?subject), "^[A-Za-z0-9_.-]+/[A-Za-z0-9_.-]+@[0-9a-f]{40}$"))
  }
}
