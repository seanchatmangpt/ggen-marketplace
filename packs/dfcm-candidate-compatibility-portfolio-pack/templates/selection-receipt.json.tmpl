{
  "schema": "dfcm.selection-portfolio/1",
  "authority": "SELECT",
  "actuation_performed": false,
  "compatible_pairs": [
  {% for row in results %}{"left":"{{ row.left }}","right":"{{ row.right }}"}{% if not loop.last %},{% endif %}
  {% endfor %}]
}
