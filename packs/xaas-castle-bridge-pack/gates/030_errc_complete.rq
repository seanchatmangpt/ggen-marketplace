PREFIX xcb: <https://ggen.dev/ontology/xaas-castle-bridge#>
SELECT ?required WHERE {
  VALUES ?required { "ELIMINATE" "REDUCE" "RAISE" "CREATE" }
  FILTER NOT EXISTS { ?d a xcb:ERRCDecision ; xcb:category ?required . }
}
