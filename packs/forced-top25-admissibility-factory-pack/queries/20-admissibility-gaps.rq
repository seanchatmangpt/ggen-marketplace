PREFIX fta: <https://ggen.dev/ontology/forced-top25-admissibility#>
SELECT ?rank ?repo ?head WHERE {
  ?target a fta:FanoutTarget ; fta:rank ?rank ; fta:repo ?repo ; fta:exactHead ?head .
  FILTER NOT EXISTS { ?target fta:admittedBy ?receipt }
}
ORDER BY ?rank ?repo
