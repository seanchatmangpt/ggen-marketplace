[project]
name = "forced-top25-admissibility-factory-pack"

[ontology]
source = "ontology.ttl"

[templates]
dir = "templates"

[generation]
output_dir = "."

[[generation.rules]]
name = "forced-top25-consumer-subjects"
query = { file = "queries/10-ready-set.rq" }
template = { file = "templates/consumer-subject.json.tera" }
output_file = "generated/forced-top25/{{ rank }}.json"
mode = "Overwrite"
skip_empty = true

[law]
rules = []
gates = []
reflexive = false
