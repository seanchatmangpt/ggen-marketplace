---
# Renders infra/gcp/packer/README.md for the beam4pm_pro image build.
# First body line is an HTML comment (not '#') because '#' opens an H1 in
# Markdown; the probe string "GENERATED by ggen" is preserved verbatim.
to: infra/gcp/packer/README.md
force: true
sparql:
  image: |
    PREFIX b4pi: <http://seanchatmangpt.github.io/packs/beam4pm-pro-infra#>
    SELECT ?img ?imageName ?imageFamily ?gcpProjectDefault ?buildZone
           ?buildMachineType ?buildDiskGb ?sourceImageFamily
           ?sourceImageProject ?sshUsername ?containerImage ?containerName
           ?servicePort
    WHERE {
      ?img a b4pi:PackerImage ;
           b4pi:imageName ?imageName ;
           b4pi:imageFamily ?imageFamily ;
           b4pi:gcpProjectDefault ?gcpProjectDefault ;
           b4pi:buildZone ?buildZone ;
           b4pi:buildMachineType ?buildMachineType ;
           b4pi:buildDiskGb ?buildDiskGb ;
           b4pi:sourceImageFamily ?sourceImageFamily ;
           b4pi:sourceImageProject ?sourceImageProject ;
           b4pi:sshUsername ?sshUsername ;
           b4pi:packerContainerImage ?containerImage ;
           b4pi:containerName ?containerName ;
           b4pi:servicePort ?servicePort .
    }
    ORDER BY ?imageName
---
<!-- GENERATED by ggen (beam4pm-pro-infra-pack/templates/packer-readme.md.tmpl). Do not edit. -->
{%- set img = image[0] %}
# beam4pm_pro GCE Image Build

Packer template building a Google Compute Engine custom image for
beam4pm_pro: `{{ img.sourceImageFamily }}` base, Docker installed, the
product container `{{ img.containerImage }}` pre-pulled, and a systemd unit
(`{{ img.containerName }}.service`) that runs it on port
{{ img.servicePort }} at boot.

Both `.pkr.hcl` files here are generated consequences of the consumer RDF
graph (`b4pi:PackerImage` facts) via `ggen sync run` -- edit the graph and
regenerate; never hand-edit these files.

## Files

| File | Source template | Content |
|---|---|---|
| `beam4pm-image.pkr.hcl` | `packer-beam4pm-image.pkr.hcl.tmpl` | `packer`/`source`/`build` blocks. Fact-driven provisioning content (the systemd unit body, the `docker pull` line, the provenance-facts write) stays inline here as HCL heredoc content. |
| `setup.sh` | `packer-setup.sh.tmpl` | Static OS-provisioning script (package install, Docker daemon enablement), uploaded to the build VM by its own `file` provisioner. Carries zero `b4pi:*` fact interpolation, so it renders byte-identically for any consumer's `b4pi:PackerImage` individual. |
| `variables.pkr.hcl` | `packer-variables.pkr.hcl.tmpl` | Variable defaults projected from the graph. |
| `README.md` | `packer-readme.md.tmpl` | This document. |

Packer provisioning is split across two files by what each provisioner
content depends on. The fully static half -- installing Docker and enabling
its daemon, with no per-consumer fact or build-time variable in it -- lives
in `setup.sh` and is uploaded verbatim by a `file` provisioner. The
fact-driven half -- the systemd unit (keyed on `b4pi:containerName` /
`b4pi:servicePort`), the `docker pull ${var.container_image}` line, and the
provenance-facts write -- stays inline inside `beam4pm-image.pkr.hcl`'s own
heredoc content, because Packer's `file` provisioner copies externally
sourced bytes verbatim and never HCL-interpolates `${var.*}` inside them:
anything needing a build-time `${var.container_image}` or a per-render
templated fact value has to stay where Tera and Packer both still evaluate
it.

## Build

Requires real GCP credentials (`gcloud auth application-default login`),
the Compute Engine API enabled, and image-create IAM. None of that is
exercised by the manufacturing pipeline -- local validation stops at
`packer validate`.

```bash
packer init  infra/gcp/packer
packer validate infra/gcp/packer
packer build -var "project_id=YOUR_PROJECT" infra/gcp/packer
```

## Variables (defaults projected from the RDF graph)

| Variable | Default | Source fact |
|---|---|---|
| `project_id` | `{{ img.gcpProjectDefault }}` (non-production default -- always override) | `b4pi:gcpProjectDefault` |
| `zone` | `{{ img.buildZone }}` | `b4pi:buildZone` |
| `machine_type` | `{{ img.buildMachineType }}` | `b4pi:buildMachineType` |
| `source_image_family` | `{{ img.sourceImageFamily }}` | `b4pi:sourceImageFamily` |
| `source_image_project` | `{{ img.sourceImageProject }}` | `b4pi:sourceImageProject` |
| `image_name` | `{{ img.imageName }}` | `b4pi:imageName` |
| `image_family` | `{{ img.imageFamily }}` | `b4pi:imageFamily` |
| `disk_size_gb` | `{{ img.buildDiskGb }}` | `b4pi:buildDiskGb` |
| `ssh_username` | `{{ img.sshUsername }}` | `b4pi:sshUsername` |
| `container_image` | `{{ img.containerImage }}` | `b4pi:packerContainerImage` |

## Standing

Local `packer validate` evidence is manufacturing substrate only. An actual
image build, a GCP Marketplace VM listing, purchase, and entitlement all
remain BLOCKED on external authorities (GCP project, Producer Portal,
billing) -- this directory claims none of them.
