# How to run the R43 Better-Eyes court

1. Resolve the repository/ref to an exact SHA before collecting facts.
2. Populate an RDF observation graph without importing standing from a different subject.
3. Run `python3 tests/test_contract.py` to verify the pack's immutable sensor surface and authority invariants.
4. Execute the SPARQL sensors against the admitted graph with a standards-compliant SPARQL 1.1 engine.
5. Preserve UNKNOWN, PARTIAL_ALIVE, ALIVE, BUILD_BROKEN, UNSUPPORTED, and typed REFUSED as distinct values.
6. Compute E and M only from observed seed/opportunity counts. Do not promote fixture multipliers to portfolio evidence.
7. Require sensor 049 to return evidence before claiming 1000X; otherwise use sensor 050 to quantify the shortfall.
8. Any consequential repair remains outside this pack and must traverse the authorized BRCE path with its own receipt.
