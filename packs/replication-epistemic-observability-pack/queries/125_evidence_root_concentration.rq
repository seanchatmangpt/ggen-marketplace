PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?root (COUNT(DISTINCT ?subject) AS ?subjects) WHERE { ?subject reo:evidenceRoot ?root . } GROUP BY ?root ORDER BY DESC(?subjects) ?root
