PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?repo (COUNT(DISTINCT ?ci) AS ?executions) WHERE { ?ci a reo:CIExecution ; reo:observedInRepo ?repo . } GROUP BY ?repo ORDER BY DESC(?executions) ?repo
