PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?input) AS ?inputs) WHERE { ?candidate a reo:ReplicationCandidate ; reo:consumes ?input . } GROUP BY ?candidate ORDER BY DESC(?inputs) ?candidate
