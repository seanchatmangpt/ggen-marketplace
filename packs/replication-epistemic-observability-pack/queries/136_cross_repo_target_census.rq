PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?repo (COUNT(DISTINCT ?candidate) AS ?candidates) WHERE { ?candidate a reo:ReplicationCandidate ; reo:targets ?target . ?target reo:observedInRepo ?repo . } GROUP BY ?repo ORDER BY DESC(?candidates) ?repo
