PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?seed (COUNT(DISTINCT ?opportunity) AS ?opportunities) WHERE {
  ?opportunity a reo:Opportunity ; reo:derivedFromSeed ?seed .
}
GROUP BY ?seed
ORDER BY DESC(?opportunities) ?seed
