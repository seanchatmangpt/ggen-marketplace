PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?blocked) AS ?relieved) WHERE { ?candidate a reo:ReplicationCandidate ; reo:relieves ?blocked . } GROUP BY ?candidate ORDER BY DESC(?relieved) ?candidate
