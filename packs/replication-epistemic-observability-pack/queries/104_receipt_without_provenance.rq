PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?receipt WHERE {
  ?receipt a reo:ReplicationReceipt .
  FILTER NOT EXISTS { ?receipt reo:provenanceReceipt ?source . }
  FILTER NOT EXISTS { ?receipt reo:evidenceRoot ?source . }
}
ORDER BY ?receipt
