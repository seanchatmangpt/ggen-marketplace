PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT DISTINCT ?artifact WHERE { ?artifact a reo:ReusableArtifact . FILTER NOT EXISTS { ?candidate a reo:ReplicationCandidate ; reo:consumes ?artifact . } } ORDER BY ?artifact
