PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?artifact WHERE {
  ?artifact a reo:ReusableArtifact .
  FILTER NOT EXISTS { ?candidate reo:targets ?artifact . }
  FILTER NOT EXISTS { ?artifact reo:derivedFromSeed ?seed . }
}
ORDER BY ?artifact
