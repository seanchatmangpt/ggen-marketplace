PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?target) AS ?targets) WHERE { ?candidate a reo:ReplicationCandidate ; reo:targets ?target . } GROUP BY ?candidate ORDER BY DESC(?targets)
