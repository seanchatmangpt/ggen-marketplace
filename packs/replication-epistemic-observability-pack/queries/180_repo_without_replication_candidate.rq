PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT DISTINCT ?repo WHERE { ?target reo:observedInRepo ?repo . FILTER NOT EXISTS { ?candidate a reo:ReplicationCandidate ; reo:targets ?target . } } ORDER BY ?repo
