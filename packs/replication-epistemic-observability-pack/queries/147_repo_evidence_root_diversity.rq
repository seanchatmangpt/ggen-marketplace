PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?repo (COUNT(DISTINCT ?root) AS ?roots) WHERE { ?candidate a reo:ReplicationCandidate ; reo:targets ?target ; reo:evidenceRoot ?root . ?target reo:observedInRepo ?repo . } GROUP BY ?repo ORDER BY DESC(?roots) ?repo
