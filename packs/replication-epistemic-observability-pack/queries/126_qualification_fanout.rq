PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?court (COUNT(DISTINCT ?candidate) AS ?candidates) WHERE { ?court a reo:QualificationCourt ; reo:qualifies ?candidate . } GROUP BY ?court ORDER BY DESC(?candidates)
