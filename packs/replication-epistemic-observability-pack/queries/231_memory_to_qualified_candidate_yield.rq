PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?memory (COUNT(DISTINCT ?candidate) AS ?qualified) WHERE { ?candidate a reo:ReplicationCandidate ; reo:derivedFromMemory ?memory . ?court a reo:QualificationCourt ; reo:qualifies ?candidate . } GROUP BY ?memory ORDER BY DESC(?qualified) ?memory
