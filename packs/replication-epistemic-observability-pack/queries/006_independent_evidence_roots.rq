PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?root) AS ?roots) WHERE { ?candidate a reo:ReplicationCandidate . OPTIONAL { ?candidate reo:evidenceRoot ?root . } } GROUP BY ?candidate HAVING (?roots < 2)
