PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?subject WHERE {
  ?subject a reo:CurrentObservation .
  FILTER NOT EXISTS { ?subject reo:subjectSha ?sha . }
}
ORDER BY ?subject
