PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT (COUNT(DISTINCT ?candidate) AS ?candidates) (COUNT(DISTINCT ?court) AS ?courts) WHERE { ?candidate a reo:ReplicationCandidate . OPTIONAL { ?court a reo:QualificationCourt ; reo:qualifies ?candidate . } }
