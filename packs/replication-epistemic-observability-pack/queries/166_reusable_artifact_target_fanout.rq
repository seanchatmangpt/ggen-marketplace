PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?artifact (COUNT(DISTINCT ?target) AS ?targets) WHERE { ?artifact a reo:ReusableArtifact . ?candidate a reo:ReplicationCandidate ; reo:consumes ?artifact ; reo:targets ?target . } GROUP BY ?artifact ORDER BY DESC(?targets) ?artifact
