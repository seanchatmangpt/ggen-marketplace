PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT DISTINCT ?candidate WHERE { ?candidate a reo:ReplicationCandidate . FILTER NOT EXISTS { ?candidate reo:exercised ?execution . } } ORDER BY ?candidate
