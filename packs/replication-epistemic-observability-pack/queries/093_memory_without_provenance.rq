PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?memory WHERE {
  ?memory a reo:MemoryRecord .
  FILTER NOT EXISTS { ?memory reo:provenanceReceipt ?receipt . }
}
ORDER BY ?memory
