PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT (COUNT(DISTINCT ?candidate) AS ?candidates) (COUNT(DISTINCT ?receipt) AS ?receipts) WHERE { ?candidate a reo:ReplicationCandidate . OPTIONAL { ?receipt a reo:ReplicationReceipt ; reo:forCandidate ?candidate . } }
