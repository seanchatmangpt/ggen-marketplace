PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate WHERE { ?candidate a reo:ReplicationCandidate . { SELECT ?candidate (COUNT(DISTINCT ?repo) AS ?n) WHERE { ?candidate reo:observedInRepo ?repo . } GROUP BY ?candidate } FILTER(?n = 1) }
