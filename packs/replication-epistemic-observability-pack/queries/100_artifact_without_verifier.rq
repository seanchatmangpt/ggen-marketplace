PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?artifact WHERE {
  ?artifact a reo:ReusableArtifact .
  FILTER NOT EXISTS { ?verifier reo:verifies ?artifact . }
}
ORDER BY ?artifact
