PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?run (COUNT(DISTINCT ?edge) AS ?receiptedEdges) WHERE { ?run reo:producedEdge ?edge . ?edge reo:provenanceReceipt ?receipt . } GROUP BY ?run ORDER BY DESC(?receiptedEdges)
