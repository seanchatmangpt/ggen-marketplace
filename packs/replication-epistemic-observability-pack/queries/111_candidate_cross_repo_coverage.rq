PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?repo) AS ?repos) WHERE {
  ?candidate a reo:ReplicationCandidate ; reo:observedInRepo ?repo .
}
GROUP BY ?candidate
HAVING(COUNT(DISTINCT ?repo) > 1)
ORDER BY DESC(?repos) ?candidate
