PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate WHERE { ?candidate a reo:ReplicationCandidate . FILTER NOT EXISTS { ?candidate reo:targets ?target . } FILTER NOT EXISTS { ?candidate reo:observedInRepo ?repo . } } ORDER BY ?candidate
