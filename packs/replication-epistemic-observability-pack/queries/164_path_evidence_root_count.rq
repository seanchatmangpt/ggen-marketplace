PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?path (COUNT(DISTINCT ?root) AS ?roots) WHERE { ?path a reo:ReplicationPath ; reo:evidenceRoot ?root . } GROUP BY ?path ORDER BY DESC(?roots) ?path
