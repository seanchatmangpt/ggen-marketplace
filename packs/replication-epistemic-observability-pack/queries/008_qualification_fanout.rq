PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?court) AS ?courts) WHERE { ?candidate a reo:ReplicationCandidate . OPTIONAL { ?court a reo:QualificationCourt ; reo:qualifies ?candidate . } } GROUP BY ?candidate ORDER BY DESC(?courts)
