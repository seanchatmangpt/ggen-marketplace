PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?seed (COUNT(DISTINCT ?candidate) AS ?candidates) WHERE { ?candidate a reo:ReplicationCandidate ; reo:derivedFromSeed ?seed . } GROUP BY ?seed ORDER BY DESC(?candidates) ?seed
