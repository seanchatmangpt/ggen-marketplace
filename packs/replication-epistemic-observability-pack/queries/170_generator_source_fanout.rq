PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?source (COUNT(DISTINCT ?candidate) AS ?candidates) WHERE { ?candidate a reo:ReplicationCandidate ; reo:generatorSource ?source . } GROUP BY ?source ORDER BY DESC(?candidates) ?source
