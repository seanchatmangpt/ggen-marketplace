PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT (COUNT(DISTINCT ?repo) AS ?repos) (COUNT(DISTINCT ?artifact) AS ?artifacts) WHERE { ?artifact a reo:ReusableArtifact ; reo:observedInRepo ?repo . }
