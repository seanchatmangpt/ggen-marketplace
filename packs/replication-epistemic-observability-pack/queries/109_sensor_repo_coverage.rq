PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?sensor (COUNT(DISTINCT ?repo) AS ?repos) WHERE {
  ?observation a reo:SensorObservation ; reo:bySensor ?sensor ; reo:observedInRepo ?repo .
}
GROUP BY ?sensor
ORDER BY DESC(?repos) ?sensor
