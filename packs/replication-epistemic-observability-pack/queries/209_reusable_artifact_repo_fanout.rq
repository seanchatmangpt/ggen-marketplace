PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?artifact (COUNT(DISTINCT ?repo) AS ?repos) WHERE { ?artifact a reo:ReusableArtifact ; reo:observedInRepo ?repo . } GROUP BY ?artifact ORDER BY DESC(?repos) ?artifact
