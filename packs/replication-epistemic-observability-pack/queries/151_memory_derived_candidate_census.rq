PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?memory (COUNT(DISTINCT ?candidate) AS ?candidates) WHERE { ?candidate a reo:ReplicationCandidate ; reo:derivedFromMemory ?memory . } GROUP BY ?memory ORDER BY DESC(?candidates) ?memory
