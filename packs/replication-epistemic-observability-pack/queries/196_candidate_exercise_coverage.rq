PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate ?execution WHERE { ?candidate a reo:ReplicationCandidate ; reo:exercised ?execution . }
ORDER BY ?candidate ?execution
