PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate WHERE { ?candidate a reo:ReplicationCandidate . FILTER NOT EXISTS { ?ci a reo:CIExecution ; reo:verifies ?candidate . } }
ORDER BY ?candidate
