PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?standing (COUNT(DISTINCT ?candidate) AS ?count) WHERE { ?candidate a reo:ReplicationCandidate ; reo:standing ?standing . } GROUP BY ?standing ORDER BY DESC(?count)
