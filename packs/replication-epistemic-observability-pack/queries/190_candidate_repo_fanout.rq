PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?repo) AS ?repos) WHERE { ?candidate a reo:ReplicationCandidate ; reo:observedInRepo ?repo . } GROUP BY ?candidate ORDER BY DESC(?repos) ?candidate
