PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?sensor WHERE {
  ?sensor a reo:Sensor .
  FILTER NOT EXISTS { ?observation a reo:SensorObservation ; reo:bySensor ?sensor . }
}
ORDER BY ?sensor
