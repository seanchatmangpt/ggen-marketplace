PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?memory) AS ?memoryRoots) WHERE {
  ?candidate a reo:ReplicationCandidate .
  OPTIONAL { ?candidate reo:derivedFromMemory ?memory . }
}
GROUP BY ?candidate
ORDER BY DESC(?memoryRoots) ?candidate
