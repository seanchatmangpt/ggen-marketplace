PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT (COUNT(?memory) AS ?memories) (COUNT(?duplicate) AS ?duplicates) WHERE {
  ?memory a reo:MemoryRecord .
  OPTIONAL { ?memory reo:duplicatesMemory ?duplicate . }
}
