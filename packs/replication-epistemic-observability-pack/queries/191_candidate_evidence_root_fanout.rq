PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?root) AS ?roots) WHERE { ?candidate a reo:ReplicationCandidate ; reo:evidenceRoot ?root . } GROUP BY ?candidate ORDER BY DESC(?roots) ?candidate
