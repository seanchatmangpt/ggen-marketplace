PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?source (COUNT(DISTINCT ?artifact) AS ?artifacts) WHERE { ?artifact a reo:ReusableArtifact ; reo:generatorSource ?source . } GROUP BY ?source ORDER BY DESC(?artifacts)
