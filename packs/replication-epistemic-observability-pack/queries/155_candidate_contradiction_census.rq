PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?claim) AS ?contradictions) WHERE { ?candidate a reo:ReplicationCandidate ; reo:contradicts ?claim . } GROUP BY ?candidate ORDER BY DESC(?contradictions) ?candidate
