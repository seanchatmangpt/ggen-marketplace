PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?authority (COUNT(DISTINCT ?candidate) AS ?count) WHERE { ?candidate a reo:ReplicationCandidate ; reo:authority ?authority . } GROUP BY ?authority ORDER BY DESC(?count)
