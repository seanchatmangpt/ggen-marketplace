PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?opportunity WHERE {
  ?opportunity a reo:Opportunity .
  FILTER NOT EXISTS { ?subject reo:consumes ?opportunity . }
}
ORDER BY ?opportunity
