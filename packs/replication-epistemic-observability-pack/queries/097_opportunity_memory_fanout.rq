PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?memory (COUNT(DISTINCT ?opportunity) AS ?opportunities) WHERE {
  ?opportunity a reo:Opportunity ; reo:derivedFromMemory ?memory .
}
GROUP BY ?memory
ORDER BY DESC(?opportunities) ?memory
