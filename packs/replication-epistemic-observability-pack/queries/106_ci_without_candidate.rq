PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?execution WHERE {
  ?execution a reo:CIExecution .
  FILTER NOT EXISTS { ?execution reo:qualifies ?candidate . }
  FILTER NOT EXISTS { ?execution reo:targets ?candidate . }
}
ORDER BY ?execution
