PREFIX reo: <https://ggen.dev/ontology/replication-epistemic#>
SELECT ?candidate (COUNT(DISTINCT ?dependency) AS ?relieved) WHERE { ?candidate a reo:ReplicationCandidate . OPTIONAL { ?candidate reo:relieves ?dependency . } } GROUP BY ?candidate ORDER BY DESC(?relieved)
