# MESSAGE: paired evidence must compare two distinct capability resources.
PREFIX ecap: <https://seanchatmangpt.github.io/packs/evolvable-capability#>
SELECT ?evidence ?capability WHERE {
  ?evidence a ecap:PairedEvidence ;
            ecap:champion ?capability ;
            ecap:candidate ?capability .
}
ORDER BY ?evidence ?capability
