---
to: "templates/backend-service/v1/skeleton/README.md"
sparql:
  entities: |
    PREFIX gc: <https://ggen.io/ontology/ggen-create#>
    SELECT ?name ?upper ?lower ?capitalized ?pascal ?camel ?snake ?upper_snake ?kebab ?title
    WHERE {
      gc:subject a gc:GenerationSubject ;
          gc:name ?name ;
          gc:upper ?upper ;
          gc:lower ?lower ;
          gc:capitalized ?capitalized ;
          gc:pascal ?pascal ;
          gc:camel ?camel ;
          gc:snake ?snake ;
          gc:upper_snake ?upper_snake ;
          gc:kebab ?kebab ;
          gc:title ?title .
    }
for_each: entities
---
{% raw %}# ${{ values.serviceName }}

${{ values.description }}

## Getting Started

```bash
npm install
npm run dev
```

## Development

| Command | Description |
|---------|-------------|
| `npm run dev` | Start local development server |
| `npm test` | Run tests |
| `npm run build` | Build for production |
| `npm run lint` | Run linter |

## Deployment

This service is deployed via the platform CI/CD pipeline. Push to `main` to trigger a deployment.

## Team

Owned by **${{ values.team }}**

---
Generated by Platform Starter Kit (${{ values.year }})
{% endraw %}