{
  "schema": "dfcm.select/1",
  "authority": "SELECT",
  "actuation_performed": false,
  "capabilities": [{% for row in results %}{"id":"{{ row.capability }}","family":"{{ row.family }}","falsifier":"{{ row.falsifier }}"}{% if not loop.last %},{% endif %}{% endfor %}]
}
