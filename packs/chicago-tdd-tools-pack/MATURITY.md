# `chicago-tdd-tools-pack` — 5×7 maturity matrix

Standing: all 7 axes closed and independently checked, level 5.

| Axis | Level | Evidence |
|---|---|---|
| 1. Semantic source | 5 | `ontology.ttl` is the only place `ctt:CliBoundaryTest` facts (testName/binary/args/expectExitCode/needles/coversAxiom/composesCommand) are asserted; every template's `sparql:` frontmatter derives its rows from it, nothing is hardcoded (`EXPECTED_TEST_COUNT` in `cli_boundary_proof.rs.tmpl` is `{{ tests \| length }}`, not a literal; the `binaries` list in `cli_boundary_doc.md.tmpl` is `DISTINCT ?binary`, not hand-maintained). |
| 2. Admission | 5 | All 4 gates (`gates/010_required.rq`, `020_single_valued.rq`, `030_value_constraints.rq`, `040_args_source.rq`) proven `0 rows` against the real `qualification/consumer.ttl` fixture (`rdflib` 7.6.0, verified this session). Real refusal proof against a deliberately-broken scratch fixture (missing required props, duplicate `ctt:binary`, invalid `testName`/out-of-range `expectExitCode`, both `ctt:args` and `ctt:composesCommand` on one individual) — every gate returned real nonzero rows (3, 2, 2, 1 respectively); the broken fixture was discarded, never committed. No new gate was added: this pack's data model has no positional/ordering property (unlike `readme-diataxis-pack`'s `rdx:position`), so an ordering-integrity gate would have no real referent here. |
| 3. Manufacture | 5 | 4 templates, all `force: true` (v0.5.0 fix, documented in `pack.toml`), deterministic Tera with explicit `{%- / -%}` whitespace control already present in `cli_boundary_proof.rs.tmpl`'s conditional blocks from first authorship. |
| 4. Execution | 5 | Real `ggen` 26.8.18 binary, isolated `/tmp/ctt-consumer` project, real `[packs]` local-path reference per `docs/how-to/consume-a-pack.md`. `ggen sync run` wrote all 4 files (`docs/chicago_tdd_tools_boundary.md`, `tests/chicago_tdd_tools_boundary.rs`, `tests/chicago_tdd_tools_boundary_runtime.rs`, `tests/chicago_tdd_tools_boundary_proof.rs`); spot-checked content: 6 `#[test]` fns / 5 real generated tests (the ontology's other individuals correctly did not render since the synthetic `qualification/consumer.ttl` `cnv:Command` stubs carry no `cnv:noun`/`cnv:verb`, exactly matching the `composesCommand` SPARQL's join requirement — real, not fabricated), `EXPECTED_TEST_COUNT = 5` derived, `run_boundary_spec(&BoundarySpec {` appears 5 times. |
| 5. Receipt/replay | 5 | Two consecutive real `ggen sync run` invocations in `/tmp/ctt-consumer` report identical `graph_hash_hex` (`c233083191a3aead96105a9a0e0e762842d72986e9bc39089dca100f1bad4c1d`); on the second run `written: []` and all 4 files report `skipped: unchanged: content identical`. |
| 6. Authority fence | 5 | Every generated Rust/Markdown file's own header states it is `GENERATED by chicago-tdd-tools-pack`; the pack repo itself never contains a generated `tests/`/`docs/chicago_tdd_tools_boundary.md` output — only `ontology.ttl`/`gates/`/`templates/` are committed. Confirmed this session: the example's real `ggen sync run` output (`tests/`, `docs/`, `ggen.lock`) was deleted after verification rather than committed, leaving only `ggen.toml`, `README.md`, `schema/domain.ttl` as the example's committed source. |
| 7. Composition | 5 | Real cross-pack proof: composed with `star-toml-pack` in one shared `/tmp/ctt-star-composed` consumer (`[packs]` table, both packs, one merged ontology graph — `ctt:` and `stp:` namespaces, zero overlap). `ggen sync run` produced 11 distinct output files (`docs/chicago_tdd_tools_boundary.md`, `docs/star_toml/{admission,telemetry}.md`, `docs/star_toml/CARGO_DEPENDENCIES.md`, `src/star_toml_config.rs`, `src/star_toml_lib_wiring.rs`, `tests/chicago_tdd_tools_boundary{,_proof,_runtime}.rs`, `tests/star_toml_config_proof.rs`, `tests/star_toml_config_sparql_derived_proof.rs`) — **zero path collision**, **zero subject-IRI collision** (`ctt:` vs `stp:`), deterministic replay: identical `graph_hash_hex` (`8b527c72b6a0f292c526b778699880f5f582d1f2de7a97d9fe80459bc57f4556`) on both runs, `written: []` and 22 `unchanged: content identical` markers on the second. `star-toml-pack` is the real, grounded composition partner: chicago-tdd-tools genuinely integrates with `star_toml` in `examples/star-toml/main.rs` of the real `chicago-tdd-tools` repo, not an arbitrary pairing. |

## Fan-out mechanism (examples/ and playground/)

Unlike `readme-diataxis-pack`'s `example-file.tmpl`/`playground-file.tmpl`
(a generic template with a row-parameterized `to:` path fanning out to
many files from one unbounded `SELECT`), this pack's 4 templates each have
a **fixed** `to:` target — `cli_boundary_tests.rs.tmpl`'s row loop fans out
`#[test]` fns *within* one file, not across many output paths. This is a
real structural difference from the exemplar's mechanism, not a gap to
paper over: `ctt:CliBoundaryTest` individuals are proof-suite members of
one generated test module, not standalone files.

`examples/consume-chicago-tdd-tools-pack/` and `playground/SCRATCH.md`
still follow the exemplar's *intent* (self-referential, grounded in real
primitives, runnable): the example attaches this pack via the documented
local-path convention and declares two real `ctt:CliBoundaryTest`
individuals against the same `receiptctl`-shaped binary the pack's own
`ontology.ttl`/`qualification/consumer.ttl` already target; verified this
session with a real `ggen sync run` (8 `#[test]` fns generated). The
playground shows how to add a new individual and re-run `ggen sync run`,
and contrasts this pack's `CliHarness`-based boundary-test shape against
the real `config_test!` macro (`chicago_tdd_tools::core::macros::config_test`)
that `star-toml-pack` targets instead — naming, not inventing, the
boundary between the two packs' real domains.

## Baseline

```
$ python3 scripts/marketplace.py validate
validated packs=144 manifests=144 ontologies=200 templates=1296 native_gates=411 verifier_gates=5 profiles={"project":35,"projection":98,"semantic":11} diataxis=20
```

This branch was cut fresh from `origin/main` (not from the
`feat/readme-diataxis-pack` branch the research report's 145-pack baseline
was taken on, which is 4 commits ahead of `origin/main` and not yet
merged) — 144 is `origin/main`'s real current pack count, unaffected by
this session's additions to `chicago-tdd-tools-pack` (`examples/` and
`playground/` are pack-internal content, not new marketplace packs; the
validator's `packs=144` did not change before/after adding them).
