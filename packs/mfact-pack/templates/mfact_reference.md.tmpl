---
to: docs/mfact_reference.md
sparql:
  stages: |
    PREFIX mfa: <http://seanchatmangpt.github.io/packs/mfact#>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?order ?actor ?label ?doc WHERE {
      ?s a mfa:PipelineStage ; mfa:order ?order ; mfa:actor ?actor ; rdfs:label ?label ; rdfs:comment ?doc .
    } ORDER BY ?order
  dirs: |
    PREFIX mfa: <http://seanchatmangpt.github.io/packs/mfact#>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?path ?doc WHERE {
      ?d a mfa:AuthorityDir ; mfa:path ?path ; rdfs:comment ?doc .
    } ORDER BY ?path
---
# mfact reference

GENERATED by `ggen sync` from `packs/mfact-pack/ontology.ttl`.

## Pipeline

| # | Actor | Stage | Doc |
|---|---|---|---|
{% for row in stages %}| {{ row.order }} | {{ row.actor }} | {{ row.label }} | {{ row.doc }} |
{% endfor %}
## Authority directories

| Path | Doc |
|---|---|
{% for row in dirs %}| `{{ row.path }}` | {{ row.doc }} |
{% endfor %}
