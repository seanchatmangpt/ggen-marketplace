PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target ?since WHERE { ?target a alloc:FanoutTarget ; alloc:standing "COMPATIBLE_NOT_READY" ; <http://www.w3.org/ns/prov#generatedAtTime> ?since . }
