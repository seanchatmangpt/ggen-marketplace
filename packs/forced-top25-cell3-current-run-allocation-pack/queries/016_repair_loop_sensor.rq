PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target (COUNT(?event) AS ?repairs) WHERE { ?event a alloc:ManufacturingEvent ; alloc:standing ?s ; alloc:dependsOn ?target . FILTER(CONTAINS(STR(?s), "BUILD_BROKEN")) } GROUP BY ?target HAVING(COUNT(?event) > 1)
