PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?event WHERE { ?event a alloc:ManufacturingEvent ; alloc:standing "SELECTED" . FILTER NOT EXISTS { ?event alloc:dependsOn ?primitive . ?primitive a alloc:UpstreamPrimitive ; alloc:exactHead ?head } }
