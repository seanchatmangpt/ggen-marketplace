PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target (COUNT(?dependent) AS ?unlocks) WHERE { ?dependent alloc:dependsOn ?target . ?target a alloc:FanoutTarget . } GROUP BY ?target
