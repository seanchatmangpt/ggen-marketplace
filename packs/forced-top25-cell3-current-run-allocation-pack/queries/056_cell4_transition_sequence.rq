PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target WHERE { ?target a alloc:FanoutTarget ; alloc:standing "COMPATIBLE_READY" . FILTER NOT EXISTS { ?admit a alloc:ManufacturingEvent ; alloc:dependsOn ?target . ?qualify a alloc:ManufacturingEvent ; alloc:dependsOn ?admit . } }
