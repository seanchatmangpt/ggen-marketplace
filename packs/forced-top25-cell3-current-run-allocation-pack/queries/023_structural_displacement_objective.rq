PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?primitive (COUNT(DISTINCT ?target) AS ?displacement) WHERE { ?target a alloc:FanoutTarget ; alloc:dependsOn ?primitive . ?primitive a alloc:UpstreamPrimitive . } GROUP BY ?primitive
