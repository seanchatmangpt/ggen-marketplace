PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target WHERE { ?target a alloc:FanoutTarget ; alloc:standing ?s . FILTER(CONTAINS(STR(?s), "REPAIRED")) FILTER NOT EXISTS { ?event a alloc:ManufacturingEvent ; alloc:dependsOn ?target ; alloc:standing "ALIVE" } }
