PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target WHERE { ?target a alloc:FanoutTarget ; alloc:standing "UNKNOWN_WITH_REASON" . FILTER NOT EXISTS { ?target <http://www.w3.org/2004/02/skos/core#note> ?reason } }
