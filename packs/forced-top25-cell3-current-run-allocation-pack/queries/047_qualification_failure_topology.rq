PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target ?standing WHERE { ?target a alloc:FanoutTarget ; alloc:standing ?standing . FILTER(CONTAINS(STR(?standing), "BUILD_BROKEN") || CONTAINS(STR(?standing), "PARTIAL_ALIVE")) }
