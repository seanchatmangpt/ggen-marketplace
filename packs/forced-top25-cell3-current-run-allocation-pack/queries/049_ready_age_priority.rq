PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
PREFIX prov: <http://www.w3.org/ns/prov#>
SELECT ?target ?since WHERE { ?target a alloc:FanoutTarget ; alloc:standing "COMPATIBLE_READY" ; prov:generatedAtTime ?since . }
