PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?standing (COUNT(?target) AS ?targets) WHERE { ?target a alloc:FanoutTarget ; alloc:standing ?standing . FILTER(CONTAINS(STR(?standing), "BUILD_BROKEN")) } GROUP BY ?standing HAVING(COUNT(?target) >= 2)
