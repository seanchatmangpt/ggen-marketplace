PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?run WHERE { ?run alloc:uCount ?u ; alloc:gCount ?g ; alloc:oCount ?o ; alloc:qCount ?q ; alloc:mCount ?m . FILTER(?g > ?u || ?o > ?g || ?q > ?o || ?m > ?q) }
