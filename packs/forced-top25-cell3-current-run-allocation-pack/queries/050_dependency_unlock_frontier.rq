PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target (COUNT(?dependent) AS ?blockedDependents) WHERE { ?dependent alloc:dependsOn ?target . ?target a alloc:FanoutTarget ; alloc:standing "COMPATIBLE_NOT_READY" . } GROUP BY ?target ORDER BY DESC(?blockedDependents)
