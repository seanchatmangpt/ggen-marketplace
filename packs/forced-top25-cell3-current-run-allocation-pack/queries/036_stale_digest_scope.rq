PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?run ?digest WHERE { ?run a alloc:AllocationRun ; alloc:ocelDigest ?digest ; alloc:standing ?s . FILTER(CONTAINS(STR(?s), "UNCHANGED_PRIOR")) }
