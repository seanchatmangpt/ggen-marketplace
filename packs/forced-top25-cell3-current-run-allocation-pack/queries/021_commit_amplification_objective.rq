PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target (COUNT(?event) AS ?manufacturedEvents) WHERE { ?event a alloc:ManufacturingEvent ; alloc:dependsOn ?target . ?target a alloc:FanoutTarget . } GROUP BY ?target
