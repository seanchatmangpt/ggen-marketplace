PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?primitive (COUNT(?target) AS ?futureCapacity) WHERE { ?primitive a alloc:UpstreamPrimitive . ?target a alloc:FanoutTarget ; alloc:dependsOn ?primitive . } GROUP BY ?primitive
