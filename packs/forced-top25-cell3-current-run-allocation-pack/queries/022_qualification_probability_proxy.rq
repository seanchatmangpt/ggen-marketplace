PREFIX alloc: <https://ggen.dev/ontology/cell3-allocation#>
SELECT ?target (COUNT(?ok)/COUNT(?event) AS ?qualificationProxy) WHERE { ?event a alloc:ManufacturingEvent ; alloc:dependsOn ?target ; alloc:standing ?s . OPTIONAL { FILTER(?s = "ALIVE") BIND(?event AS ?ok) } } GROUP BY ?target
