PREFIX sel: <https://seanchatman.github.io/ggen/selection#>
SELECT ?candidate ?pack ?reuse WHERE {
  ?candidate a sel:Candidate ; sel:manufacturableBy ?pack ; sel:marketplaceReuse ?reuse .
}
ORDER BY DESC(?reuse) ?candidate ?pack
