PREFIX sel: <https://seanchatman.github.io/ggen/selection#>
SELECT ?candidate ?rollbackCost ?irreversibleEdges WHERE {
  ?candidate a sel:Candidate ; sel:rollbackCost ?rollbackCost ; sel:irreversibleEdgeCount ?irreversibleEdges .
}
ORDER BY ?irreversibleEdges ?rollbackCost ?candidate
