PREFIX sel: <https://seanchatman.github.io/ggen/selection#>
SELECT ?candidate ?falsifier ?severity WHERE {
  ?candidate a sel:Candidate ; sel:challengedBy ?falsifier .
  ?falsifier sel:severity ?severity .
}
ORDER BY DESC(?severity) ?candidate ?falsifier
