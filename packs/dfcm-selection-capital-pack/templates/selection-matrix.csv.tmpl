candidate,new_meaningful_commit_yield,independent_merge_yield,dependency_relief,qualified_capability
{% for row in results %}{{ row.candidate }},{{ row.commitYield }},{{ row.mergeYield }},{{ row.dependencyRelief }},{{ row.qualifiedCapability }}
{% endfor %}