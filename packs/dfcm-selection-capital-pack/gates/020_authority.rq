PREFIX sel: <https://seanchatman.github.io/ggen/selection#>
SELECT ?selection ?authority ?actuated WHERE {
  ?selection a sel:Selection ; sel:authority ?authority ; sel:actuationPerformed ?actuated .
  FILTER(?authority NOT IN ("OBSERVE","SELECT","CONSTRUCT","VERIFY") || ?actuated = true)
}
