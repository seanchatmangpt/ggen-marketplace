PREFIX sel: <https://seanchatman.github.io/ggen/selection#>
SELECT ?candidate ?uncertainty ?limit WHERE {
  ?candidate a sel:Candidate ; sel:uncertainty ?uncertainty ; sel:maxUncertainty ?limit .
  FILTER(?uncertainty > ?limit)
}
