PREFIX dspc: <https://seanchatmangpt.github.io/ontology/dfcm-selection-portfolio-consensus#>
SELECT ?candidate ?sha WHERE {
  ?candidate a dspc:Candidate ; dspc:subjectSha ?sha .
  FILTER(!regex(str(?sha), "^[0-9a-f]{40}$"))
}
