PREFIX dspc: <https://seanchatmangpt.github.io/ontology/dfcm-selection-portfolio-consensus#>
SELECT ?decision ?authority ?actuated WHERE {
  ?decision a dspc:Decision ; dspc:authority ?authority ; dspc:actuationPerformed ?actuated .
  FILTER(str(?authority) = "DO" || ?actuated = true)
}
