PREFIX fta: <https://ggen.dev/ontology/forced-top25-admissibility#>
SELECT ?rank ?repo ?blocker WHERE {
  ?t a fta:FanoutTarget ; fta:rank ?rank ; fta:repo ?repo ; fta:compatibilityState "UNKNOWN" ; fta:blocker ?blocker .
}
ORDER BY ?rank ?repo
