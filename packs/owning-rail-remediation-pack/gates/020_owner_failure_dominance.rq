PREFIX rem: <https://seanchatman.github.io/ggen/remediation#>
SELECT ?rail ?owner ?railState ?ownerState WHERE {
  ?rail a rem:Rail ; rem:owner ?owner ; rem:state ?railState .
  ?owner rem:state ?ownerState .
  FILTER(?railState = "PASS" && ?ownerState IN ("FAIL","BUILD_BROKEN","BLOCKED"))
}
