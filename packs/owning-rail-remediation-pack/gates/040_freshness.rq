PREFIX rem: <https://seanchatman.github.io/ggen/remediation#>
SELECT ?rail ?observedAt ?expiresAt WHERE {
  ?rail a rem:Rail ; rem:observedAt ?observedAt ; rem:expiresAt ?expiresAt .
  BIND(NOW() AS ?now)
  FILTER(?expiresAt < ?now)
}
