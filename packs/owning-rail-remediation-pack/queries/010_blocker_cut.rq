PREFIX rem: <https://seanchatman.github.io/ggen/remediation#>
SELECT ?rail ?blocker ?severity WHERE {
  ?rail a rem:Rail ; rem:blockedBy ?blocker .
  ?blocker rem:severity ?severity .
}
ORDER BY DESC(?severity) ?rail ?blocker
