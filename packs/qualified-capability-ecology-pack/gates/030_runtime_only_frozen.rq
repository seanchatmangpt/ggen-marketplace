PREFIX qce: <https://ggen.dev/ontology/qualified-capability-ecology#>
SELECT ?episode ?capability WHERE {
  ?episode a qce:RuntimeEpisode ; qce:executedCapability ?capability .
  FILTER NOT EXISTS { ?capability a qce:FrozenCapability ; qce:frozen true }
}
