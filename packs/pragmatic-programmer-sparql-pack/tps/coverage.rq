# TPS profile completeness: every PragProg TPS tip must map to at least one TPS mechanism.
PREFIX pp: <https://ggen.dev/pragprog/>

SELECT ?tip WHERE {
  pp:PragProgTPS pp:includesTip ?tip .
  FILTER NOT EXISTS { ?tip pp:tpsMechanism ?mechanism . }
}
ORDER BY ?tip
