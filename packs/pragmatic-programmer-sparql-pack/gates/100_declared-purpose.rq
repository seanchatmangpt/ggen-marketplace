# MESSAGE: PP-100 production objective requires declared beneficiary and purpose
PREFIX pp: <https://ggen.dev/pragprog/>
PREFIX ggen: <https://ggen.dev/ontology/>
PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>

SELECT DISTINCT ?subject WHERE {
  ?subject pp:governedBy pp:tip100 .
  FILTER (NOT EXISTS { ?subject pp:beneficiary ?a . } || NOT EXISTS { ?subject pp:purpose ?b . })
}
ORDER BY ?subject
