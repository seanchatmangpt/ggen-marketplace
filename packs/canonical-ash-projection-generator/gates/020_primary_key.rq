PREFIX apg: <https://ggen.dev/ash-projection#>
SELECT ?resource ?reason WHERE {
  ?resource a apg:Resource .
  FILTER NOT EXISTS {
    ?resource apg:attribute ?a .
    ?a apg:primaryKey true .
  }
  BIND("resource-missing-explicit-primary-key" AS ?reason)
}
ORDER BY ?resource
