PREFIX rea: <https://chatman.ai/ontology/runtime-evidence-authenticity#>
ASK { ?s a rea:ExactSubject ; rea:subjectSha ?sha ; rea:semanticDigest ?digest .
FILTER regex(str(?sha), "^[0-9a-f]{40}$") FILTER regex(str(?digest), "^[0-9a-f]{64}$") }
