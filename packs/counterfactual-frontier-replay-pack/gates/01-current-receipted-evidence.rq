PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?c a cfr:Candidate .
    FILTER NOT EXISTS { ?c cfr:current true ; cfr:receiptDigest ?r . FILTER(STRLEN(STR(?r)) = 64) }
  }
}
