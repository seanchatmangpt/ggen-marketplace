PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?composition a cfr:Composition .
    FILTER NOT EXISTS { ?composition cfr:qualified true . }
  }
}
