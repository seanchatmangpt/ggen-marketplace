PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?candidate a cfr:Candidate ; cfr:reversibility ?r ; cfr:expectedReuse ?u ; cfr:expectedCapabilitySpaceDelta ?d .
    FILTER(?r <= 0 || ?u <= 0 || ?d <= 0)
  }
}
