PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?composition a cfr:Composition ; cfr:order ?order .
    FILTER(?order < 2 || ?order > 8)
  }
}
