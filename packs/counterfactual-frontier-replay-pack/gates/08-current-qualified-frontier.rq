PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?candidate a cfr:Candidate ; cfr:qualified true .
    FILTER NOT EXISTS { ?candidate cfr:current true ; cfr:standing ?standing . }
  }
}
