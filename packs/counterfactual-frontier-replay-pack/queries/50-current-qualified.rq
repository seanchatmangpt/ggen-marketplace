PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
SELECT ?candidate ?standing WHERE {
  ?candidate a cfr:Candidate ; cfr:qualified true ; cfr:current true ; cfr:standing ?standing ; cfr:actuationPerformed false .
  FILTER(?standing = "ALIVE" || ?standing = "PARTIAL_ALIVE")
}
ORDER BY ?candidate
