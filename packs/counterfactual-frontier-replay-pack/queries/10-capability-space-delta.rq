PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
SELECT ?candidate ?delta ?reuse WHERE {
  ?candidate a cfr:Candidate ; cfr:expectedCapabilitySpaceDelta ?delta ; cfr:expectedReuse ?reuse .
}
ORDER BY DESC(?delta) DESC(?reuse) ?candidate
