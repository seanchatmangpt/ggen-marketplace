PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
SELECT ?candidate ?authority WHERE {
  ?candidate a cfr:Candidate ; cfr:authority ?authority ; cfr:actuationPerformed false .
  FILTER(?authority != "DO")
}
ORDER BY ?candidate
