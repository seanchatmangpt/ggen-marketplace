PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
SELECT ?candidate ?reuse ?delta (?reuse * ?delta AS ?optionCapital) WHERE {
  ?candidate a cfr:Candidate ; cfr:expectedReuse ?reuse ; cfr:expectedCapabilitySpaceDelta ?delta .
}
ORDER BY DESC(?optionCapital) ?candidate
