PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
SELECT ?candidate ?reversibility ?delta WHERE {
  ?candidate a cfr:Candidate ; cfr:reversibility ?reversibility ; cfr:expectedCapabilitySpaceDelta ?delta .
}
ORDER BY DESC(?reversibility) DESC(?delta) ?candidate
