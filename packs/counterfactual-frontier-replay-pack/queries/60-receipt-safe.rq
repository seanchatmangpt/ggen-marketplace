PREFIX cfr: <https://ggen.dev/ontology/counterfactual-frontier-replay#>
SELECT ?candidate ?receipt WHERE {
  ?candidate a cfr:Candidate ; cfr:receiptDigest ?receipt ; cfr:actuationPerformed false .
  FILTER(STRLEN(STR(?receipt)) = 64)
}
ORDER BY ?candidate
