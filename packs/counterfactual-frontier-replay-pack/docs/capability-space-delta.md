# Capability-space delta

Capability-space delta estimates how many additional lawful composition edges become reachable if a candidate is preserved and qualified. Record observed deltas separately from inferred deltas. Do not promote an inferred multiplier to evidence until the generated consumer surfaces, verifier courts, or cross-repository integrations actually exist and can be replayed from receipts.
