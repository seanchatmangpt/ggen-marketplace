# @generated by ggen — source is marketplace ontology/query/template.
CANDIDATES = [
{% for row in results %}    {"candidate": "{{ row.candidate }}", "receipt": "{{ row.receipt }}", "authority": "{{ row.authority }}"},{% endfor %}
]
assert CANDIDATES, "REFUSED[EMPTY_REPLAYABLE_FRONTIER]"
assert all(len(item["receipt"]) == 64 for item in CANDIDATES)
assert all(item["authority"] != "DO" for item in CANDIDATES)
