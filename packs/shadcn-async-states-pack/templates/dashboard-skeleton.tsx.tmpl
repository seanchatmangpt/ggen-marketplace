---
to: "components/states/DashboardSkeleton.tsx"
---
{% raw %}
import { Skeleton } from "@/components/ui/skeleton";

/**
 * Content-shaped skeleton matching the eventual dashboard layout:
 * canvas + legend + side panel + table rows. Never a bare spinner.
 */
export function DashboardSkeleton() {
  return (
    <div className="grid grid-cols-[1fr_320px] gap-4">
      <div className="space-y-4">
        <Skeleton className="h-[480px] w-full rounded-lg" /> {/* canvas */}
        <Skeleton className="h-24 w-64 rounded-lg" /> {/* legend */}
        <div className="space-y-2">
          {Array.from({ length: 5 }).map((_, i) => (
            <Skeleton key={i} className="h-8 w-full rounded" /> // table rows
          ))}
        </div>
      </div>
      <Skeleton className="h-[560px] w-80 rounded-lg" /> {/* side panel */}
    </div>
  );
}
{% endraw %}
