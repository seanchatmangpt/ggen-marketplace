PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?binding WHERE { ?binding a gra:RuntimeAdapterBinding . FILTER NOT EXISTS { ?binding gra:adapterId ?id ; gra:adapterVersion ?version . FILTER(STRLEN(STR(?id)) > 0 && STRLEN(STR(?version)) > 0) } }
