PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?receipt WHERE { ?invocation a gra:Invocation ; gra:usesBinding ?binding ; gra:hasReceipt ?receipt . ?binding gra:policyDigest ?policy . ?receipt gra:policyDigest ?other . FILTER(STR(?policy) != STR(?other)) }
