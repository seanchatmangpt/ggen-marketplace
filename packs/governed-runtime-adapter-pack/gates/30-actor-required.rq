PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?invocation WHERE { ?invocation a gra:Invocation ; gra:usesBinding ?binding . ?binding gra:actorRequired true . FILTER NOT EXISTS { ?invocation gra:actorIdentity ?actor . FILTER(STRLEN(STR(?actor)) > 0) } }
