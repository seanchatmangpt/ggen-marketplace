PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?binding WHERE { ?binding a gra:RuntimeAdapterBinding ; gra:executionMode gra:AuthorizedDo . FILTER NOT EXISTS { ?binding gra:hostCapabilityDigest ?digest . FILTER(STRLEN(STR(?digest)) > 0) } }
