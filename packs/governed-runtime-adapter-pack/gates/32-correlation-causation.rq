PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?invocation WHERE { ?invocation a gra:Invocation . FILTER NOT EXISTS { ?invocation gra:correlationId ?correlation ; gra:causationId ?causation . FILTER(STRLEN(STR(?correlation)) > 0 && STRLEN(STR(?causation)) > 0) } }
