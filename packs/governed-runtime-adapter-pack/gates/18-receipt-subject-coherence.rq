PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?receipt WHERE { ?invocation a gra:Invocation ; gra:usesBinding ?binding ; gra:hasReceipt ?receipt . ?binding gra:exactSubject ?subject . ?receipt gra:exactSubject ?other . FILTER(STR(?subject) != STR(?other)) }
