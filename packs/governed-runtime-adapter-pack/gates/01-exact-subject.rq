PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?binding WHERE { ?binding a gra:RuntimeAdapterBinding . FILTER NOT EXISTS { ?binding gra:exactSubject ?subject . FILTER(STRLEN(STR(?subject)) > 0) } }
