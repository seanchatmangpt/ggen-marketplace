PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?invocation WHERE { ?invocation a gra:Invocation . FILTER NOT EXISTS { ?invocation gra:inputDigest ?in ; gra:outputDigest ?out . FILTER(STRLEN(STR(?in)) > 0 && STRLEN(STR(?out)) > 0) } }
