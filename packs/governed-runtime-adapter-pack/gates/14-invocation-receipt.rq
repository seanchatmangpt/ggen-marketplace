PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?invocation WHERE { ?invocation a gra:Invocation . FILTER NOT EXISTS { ?invocation gra:usesBinding ?binding ; gra:hasReceipt ?receipt . ?binding a gra:RuntimeAdapterBinding . ?receipt a gra:Receipt . } }
