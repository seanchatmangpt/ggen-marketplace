PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?receipt WHERE { ?receipt a gra:Receipt . FILTER NOT EXISTS { ?receipt gra:policyDigest ?digest . FILTER(STRLEN(STR(?digest)) > 0) } }
