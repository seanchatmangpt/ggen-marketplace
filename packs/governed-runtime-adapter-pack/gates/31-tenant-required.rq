PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?invocation WHERE { ?invocation a gra:Invocation ; gra:usesBinding ?binding . ?binding gra:tenantRequired true . FILTER NOT EXISTS { ?invocation gra:tenantIdentity ?tenant . FILTER(STRLEN(STR(?tenant)) > 0) } }
