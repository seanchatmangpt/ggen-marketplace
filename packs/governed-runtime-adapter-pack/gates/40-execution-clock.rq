PREFIX gra: <https://ggen.dev/ontology/governed-runtime-adapter#>
SELECT ?invocation WHERE { ?invocation a gra:Invocation . FILTER NOT EXISTS { ?invocation gra:clockIdentity ?clock . FILTER(STRLEN(STR(?clock)) > 0) } }
