PREFIX ecr: <https://chatman.ai/ontology/evidence-capital-realization#>
SELECT ?capital (AVG(?gain) AS ?meanGain) WHERE {
  ?t a ecr:RealizationTrial ; ecr:claimedCapital ?capital ; ecr:realizedGain ?gain .
} GROUP BY ?capital ORDER BY ?capital
