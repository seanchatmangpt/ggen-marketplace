PREFIX ecr: <https://chatman.ai/ontology/evidence-capital-realization#>
SELECT ?t ?baseline ?augmented ((?baseline - ?augmented) AS ?delta) WHERE {
  ?t a ecr:RealizationTrial ; ecr:baselineLoss ?baseline ; ecr:augmentedLoss ?augmented .
} ORDER BY DESC(?delta) ?t
