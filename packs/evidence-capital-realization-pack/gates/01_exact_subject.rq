PREFIX ecr: <https://chatman.ai/ontology/evidence-capital-realization#>
ASK {
  ?s ecr:subjectSha ?sha ; ecr:semanticDigest ?digest .
  FILTER regex(str(?sha), "^[0-9a-f]{40}$")
  FILTER regex(str(?digest), "^[0-9a-f]{64}$")
}
