PREFIX ecr: <https://chatman.ai/ontology/evidence-capital-realization#>
ASK {
  ?t a ecr:RealizationTrial ; ecr:realizedGain ?gain ; ecr:informationGain ?information .
  FILTER(?gain >= 0 && ?information >= 0)
}
