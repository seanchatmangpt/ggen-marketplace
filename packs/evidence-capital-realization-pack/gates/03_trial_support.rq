PREFIX ecr: <https://chatman.ai/ontology/evidence-capital-realization#>
ASK {
  ?t a ecr:RealizationTrial ; ecr:claimedCapital ?capital ; ecr:prediction ?p ; ecr:observedTruth ?truth .
  FILTER(?capital >= 0 && ?p >= 0 && ?p <= 1 && ?truth >= 0 && ?truth <= 1)
}
