# Weaver DfCM Coverage Matrix

Generated from the pack's admitted coverage ontology. Coverage means the surface is represented and bounded; it does not imply runtime execution.

| Dimension | Surface | Standing | Reversible |
|---|---|---|---|
{% for row in results %}| {{ row["dimension"] }} | `{{ row["surface"] }}` | `{{ row["status"] }}` | {{ row["reversible"] }} |
{% endfor %}

## Crown condition

The pack remains `PARTIAL_ALIVE` until the exact consumer executes its real instrumentation path against `weaver registry live-check` and the resulting report contains no configured refusal-level findings. Static registry checks, generated files, packaged manifests, MCP access, and inferred schemas are not substitutes for observed subject execution.
