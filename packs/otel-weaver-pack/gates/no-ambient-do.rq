PREFIX otelw: <https://chatmangpt.com/ontology/otel-weaver#>
ASK {
  ?item a otelw:CoverageItem ;
        otelw:surface ?surface ;
        otelw:reversible false ;
        otelw:status ?status .
  FILTER (?status != "bounded-test-DO" && ?surface != "registry update-markdown")
}
