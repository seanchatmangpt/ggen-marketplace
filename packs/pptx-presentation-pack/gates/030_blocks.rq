PREFIX pres: <https://ggen.dev/ns/presentation#>
SELECT ?slide_id ?order ?role ?title ?body ?tone
WHERE {
  ?block a pres:Block ;
         pres:blockOf ?slide ;
         pres:order ?order ;
         pres:role ?role .
  ?slide pres:id ?slide_id .
  OPTIONAL { ?block pres:title ?title . }
  OPTIONAL { ?block pres:body ?body . }
  OPTIONAL { ?block pres:tone ?tone . }
}
