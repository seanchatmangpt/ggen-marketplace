# ggen-pack-spec-pack — executable specification corpus for the GGEN
# Semantic Pack Protocol (RFC-GPACK-001), per RFC §94.
#
# Scaffold note (ticket T02): the full gp: vocabulary, admission gates, and
# qualification-corpus contract are owned by ticket T01
# (branch gpack/mkt-01-specpack-scaffold). This minimal strict-§7.1 root
# exists so the T02 corpus fixtures under qualification/positive/ ship
# inside an admissible marketplace pack (admission requires a manifest and
# at least one top-level ontology.ttl). On merge, T01's files supersede
# these as a semantic superset; the T02 fixture subtree needs no change.
[pack]
name = "ggen-pack-spec-pack"
version = "0.1.0"
description = "Executable specification corpus for the GGEN Semantic Pack Protocol (RFC-GPACK-001)."
