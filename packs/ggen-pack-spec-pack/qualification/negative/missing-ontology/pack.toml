# T03 invalid corpus — attack: missing semantic graph
# RFC-GPACK-001 v26.9.17 §6: "Only pack.toml and ontology.ttl are REQUIRED by
# GGEN-PACK-CORE." This fixture ships a fully lawful §7.1 manifest and NO
# ontology.ttl, so a conforming engine must refuse with
# REFUSED:PACK_GRAPH_MISSING at graph-admission time. The manifest must not
# be blamed: it is valid on purpose, isolating the missing-graph law.
[pack]
name = "mkt03-missing-ontology"
version = "1.0.0"
description = "T03 negative fixture: pack.toml present, ontology.ttl absent."
