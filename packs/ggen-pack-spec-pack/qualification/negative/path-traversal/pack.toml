# T03 invalid corpus — attack: frontmatter target escapes the admitted root
# RFC-GPACK-001 v26.9.17 §71: a conforming implementation MUST prevent
# pack-controlled paths from escaping admitted roots; `..` traversal is the
# named §96 "Path safety" falsifier ("Target ../../outside"). The template
# below is the only attacked surface; manifest and graph stay lawful so the
# engine must reach the path-safety gate and refuse with
# REFUSED:PACK_PATH_ESCAPE.
[pack]
name = "mkt03-path-traversal"
version = "1.0.0"
description = "T03 negative fixture: projection frontmatter targets ../../outside the admitted root."
