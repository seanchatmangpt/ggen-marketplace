# T03 invalid corpus — target-collision, entry pack one
# RFC-GPACK-001 v26.9.17 §43 (Collision Law): two independent projections
# resolving to the same target with no explicit merge contract MUST be
# refused with REFUSED:TARGET_OWNERSHIP_CONFLICT — "The fact that both
# happened to render identical bytes MUST NOT silently legitimize duplicated
# ownership." pack-one enters composition via {{PACK}} and carries the
# explicit PROJECTION-scope dependency on pack-two (§29) so both templates
# participate in one sync.
[pack]
name = "mkt03-target-pack-one"
version = "1.0.0"
description = "T03 negative fixture: claims shared-collision-target.txt while depending on pack-two."
