# T03 invalid corpus — ambiguous-capability, consumer pack (entry)
# RFC-GPACK-001 v26.9.17 §31: a pack MAY depend on a capability; resolution
# MUST be deterministic and receipted. This consumer requires the capability
# <urn:capability:mkt03-rdf-canonicalization> (declared in ontology.ttl),
# which BOTH sibling providers supply with no selection rule — the §96
# "capability ambiguity refusal" court (Court 14, §78).
[pack]
name = "mkt03-capability-consumer"
version = "1.0.0"
description = "T03 negative fixture: requires one capability that two providers supply."
