# T03 invalid corpus — query-positive-select
# Valid, identity-consistent, NON-EMPTY: the pack self-description triples
# match the D1 SELECT (?s ?p ?o), so the query genuinely returns rows —
# including this pack IRI, which assert/run.sh requires as positive evidence
# that rows were processed without a refusal (§77 RequiredOutcomeObserved).
@prefix gp: <https://ggen.dev/ns/pack#> .

<urn:ggen:pack:mkt03-query-positive-select>
    a gp:Pack ;
    gp:name "mkt03-query-positive-select" ;
    gp:version "1.0.0" ;
    gp:profile gp:Portable1 ;
    gp:authorityCeiling gp:Construct .
