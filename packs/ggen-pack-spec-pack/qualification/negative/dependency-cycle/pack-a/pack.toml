# T03 invalid corpus — dependency-cycle, entry pack A of the A->B->A cycle
# RFC-GPACK-001 v26.9.17 §34: the resolution result is DAG(P); cycles are
# refused by default with REFUSED:DEPENDENCY_CYCLE. This manifest is lawful;
# the cycle is declared pack-owned in ontology.ttl (§7.2: dependencies live
# in RDF; §26-27: gp:requiresPack + gp:dependencyScope gp:Semantics).
[pack]
name = "mkt03-cycle-a-pack"
version = "1.0.0"
description = "T03 negative fixture: cycle member A; requires B, which requires A."
