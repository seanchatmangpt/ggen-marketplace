PREFIX crf: <https://ggen.dev/ontology/consumer-realization-frontier#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
SELECT ?verifier ?receipt WHERE { ?verifier skos:inScheme crf:VerifierStrategy . ?receipt skos:inScheme crf:ReceiptStrategy . } ORDER BY ?verifier ?receipt
