PREFIX crf: <https://ggen.dev/ontology/consumer-realization-frontier#>
SELECT (COUNT(DISTINCT ?s) AS ?subjects) (COUNT(DISTINCT ?covered) AS ?covered) WHERE { ?s a crf:ExactSubject . OPTIONAL { ?r a crf:ConsumerReceipt ; crf:receiptSubject ?covered } }