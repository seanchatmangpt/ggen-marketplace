PREFIX crf: <https://ggen.dev/ontology/consumer-realization-frontier#>
SELECT ?s ?repo ?sha WHERE { ?s a crf:ExactSubject ; crf:repository ?repo ; crf:sha ?sha . FILTER(STRLEN(?sha)=40) } ORDER BY ?repo