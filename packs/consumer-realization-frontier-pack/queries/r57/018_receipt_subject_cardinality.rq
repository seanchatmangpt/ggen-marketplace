PREFIX crf: <https://ggen.dev/ontology/consumer-realization-frontier#>
SELECT ?s (COUNT(?r) AS ?receipts) WHERE { ?r a crf:ConsumerReceipt ; crf:receiptSubject ?s } GROUP BY ?s ORDER BY DESC(?receipts)