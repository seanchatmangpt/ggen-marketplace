PREFIX crf: <https://ggen.dev/ontology/consumer-realization-frontier#>
SELECT (COUNT(?r) AS ?receipts) (SUM(IF(?fresh=true,1,0)) AS ?freshReceipts) WHERE { ?r a crf:ConsumerReceipt . OPTIONAL { ?r crf:receiptFresh ?fresh } }