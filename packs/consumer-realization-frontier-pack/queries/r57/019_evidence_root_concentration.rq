PREFIX crf: <https://ggen.dev/ontology/consumer-realization-frontier#>
SELECT ?root (COUNT(?r) AS ?uses) WHERE { ?r a crf:ConsumerReceipt ; crf:evidenceRoot ?root } GROUP BY ?root HAVING(COUNT(?r) > 1) ORDER BY DESC(?uses)