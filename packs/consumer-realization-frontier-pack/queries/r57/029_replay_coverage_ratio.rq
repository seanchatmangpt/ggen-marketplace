PREFIX crf: <https://ggen.dev/ontology/consumer-realization-frontier#>
SELECT (COUNT(?r) AS ?receipts) (SUM(IF(?replay=true,1,0)) AS ?replayable) WHERE { ?r a crf:ConsumerReceipt . OPTIONAL { ?r crf:replayable ?replay } }