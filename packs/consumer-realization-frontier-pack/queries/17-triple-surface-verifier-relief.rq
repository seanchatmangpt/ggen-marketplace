PREFIX crf: <https://ggen.dev/ontology/consumer-realization-frontier#>
PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
SELECT ?surface ?verifier ?relief WHERE { ?surface skos:inScheme crf:RealizationSurface . ?verifier skos:inScheme crf:VerifierStrategy . ?relief skos:inScheme crf:DependencyRelief . } ORDER BY ?surface ?verifier ?relief
