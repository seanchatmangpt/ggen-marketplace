---
to: src/integration/progress.rs
---
//! Re-export shim, GENERATED by `osx-clnr-pack`, existing solely so a
//! consumer crate named (and Cargo-packaged as) `osx_clnr` exposes
//! `osx_clnr::integration::progress::human_bytes` at the SAME module path
//! as the real reference `~/osx-clnr/src/integration/progress.rs`.
//! Source-verified (2026-07-20): that file's own `human_bytes` is exactly
//! `pub use crate::domain::time::human_bytes;` -- the same re-export
//! pattern as `domain::tool_roots::human_bytes` (see
//! `osx_clnr_domain_tool_roots.rs.tmpl`), kept at this path "so existing
//! `integration::progress::human_bytes` call sites resolve unchanged" (its
//! own doc comment).
//!
//! This is what lets `reference/verbatim_size_roundtrip_test.rs` (copied
//! byte-identical from `~/osx-clnr/tests/integration_tests.rs`,
//! checksum-verified) compile and pass UNMODIFIED against this generated
//! code -- its `use osx_clnr::integration::progress::human_bytes as
//! hb_integration` line resolves to this file without any import-path
//! edit.

pub use crate::osx_clnr_plan::human_bytes;
