---
to: src/domain/time.rs
---
//! Re-export shim, GENERATED by `osx-clnr-pack`, existing solely so a
//! consumer crate named (and Cargo-packaged as) `osx_clnr` exposes
//! `osx_clnr::domain::time::*` at the SAME module path as the real
//! reference `~/osx-clnr/src/domain/time.rs`. This is what lets
//! `reference/verbatim_time_tests.rs` (copied byte-identical from
//! `~/osx-clnr/tests/integration_tests.rs`, checksum-verified) compile and
//! pass UNMODIFIED against this generated code -- its `use
//! osx_clnr::domain::time::{...}` lines resolve to this file without any
//! import-path edit.
//!
//! All real logic lives in `osx_clnr_plan.rs` (SPARQL-derived unit table +
//! date-suffix-length policy); this file only re-exports it under the
//! reference's module path.

pub use crate::osx_clnr_plan::*;
