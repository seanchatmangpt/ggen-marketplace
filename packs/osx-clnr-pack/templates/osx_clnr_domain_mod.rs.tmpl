---
to: src/domain/mod.rs
---
// GENERATED by `osx-clnr-pack`. Mounts the `time`/`tool_roots` shims so the
// module path matches `~/osx-clnr/src/domain/mod.rs` -- see
// `osx_clnr_domain_time.rs.tmpl` / `osx_clnr_domain_tool_roots.rs.tmpl` for
// why this path match matters (verbatim reference test compilation).
pub mod time;
pub mod tool_roots;
