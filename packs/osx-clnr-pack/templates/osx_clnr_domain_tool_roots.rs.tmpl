---
to: src/domain/tool_roots.rs
---
//! Re-export shim, GENERATED by `osx-clnr-pack`, existing solely so a
//! consumer crate named (and Cargo-packaged as) `osx_clnr` exposes
//! `osx_clnr::domain::tool_roots::human_bytes` at the SAME module path as
//! the real reference `~/osx-clnr/src/domain/tool_roots.rs`. Source-verified
//! (2026-07-20): that file's own `human_bytes` is exactly
//! `pub use crate::domain::time::human_bytes;` -- a pure re-export of the
//! one canonical definition, kept at this path "so existing
//! `domain::tool_roots::human_bytes` call sites resolve unchanged" (its own
//! doc comment). This shim mirrors that same re-export relationship,
//! pointing at this pack's generated `osx_clnr_plan::human_bytes` instead
//! of a hand-copy, so the two paths cannot drift from each other here
//! either.
//!
//! This is what lets `reference/verbatim_size_roundtrip_test.rs` (copied
//! byte-identical from `~/osx-clnr/tests/integration_tests.rs`,
//! checksum-verified) compile and pass UNMODIFIED against this generated
//! code -- its `use osx_clnr::domain::tool_roots::human_bytes as hb_domain`
//! line resolves to this file without any import-path edit.

pub use crate::osx_clnr_plan::human_bytes;
