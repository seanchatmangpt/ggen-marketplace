# MESSAGE: VALUE_INNOVATION_FINDING_ACTION_REQUIRED -- every finding must bind a valid ERRC action
PREFIX vi: <http://seanchatmangpt.github.io/packs/value-innovation-errc#>
SELECT ?finding WHERE {
  ?finding a vi:Finding .
  FILTER NOT EXISTS { ?finding vi:action ?action . ?action a vi:Action }
}