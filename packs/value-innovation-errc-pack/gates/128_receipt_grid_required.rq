# MESSAGE: VALUE_INNOVATION_RECEIPT_GRID_REQUIRED
PREFIX vi: <http://seanchatmangpt.github.io/packs/value-innovation-errc#>
SELECT ?receipt WHERE { ?receipt a vi:DecisionReceipt . FILTER NOT EXISTS { ?receipt vi:forGrid ?grid . ?grid a vi:StrategyGrid } }