# MESSAGE: VALUE_INNOVATION_FINDING_GRID_REQUIRED -- every finding must bind exactly to a strategy grid
PREFIX vi: <http://seanchatmangpt.github.io/packs/value-innovation-errc#>
SELECT ?finding WHERE {
  ?finding a vi:Finding .
  FILTER NOT EXISTS { ?finding vi:grid ?grid . ?grid a vi:StrategyGrid }
}