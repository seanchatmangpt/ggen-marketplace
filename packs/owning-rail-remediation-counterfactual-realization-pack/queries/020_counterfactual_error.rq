PREFIX rr: <https://chatman.ai/measure/remediation-realization#>
SELECT ?plan ?predicted ?realized (ABS(?predicted-?realized) AS ?absoluteError) WHERE {
  ?o a rr:OutcomeObservation ; rr:planId ?plan ; rr:predictedRelief ?predicted ; rr:realizedRelief ?realized .
}
