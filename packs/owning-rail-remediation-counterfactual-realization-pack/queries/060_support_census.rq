PREFIX rr: <https://chatman.ai/measure/remediation-realization#>
SELECT ?plan (COUNT(?o) AS ?support) WHERE {
  ?o a rr:OutcomeObservation ; rr:planId ?plan .
} GROUP BY ?plan ORDER BY DESC(?support)
