PREFIX rr: <https://chatman.ai/measure/remediation-realization#>
SELECT ?plan ?rollbackCost ?realized WHERE {
  ?o a rr:OutcomeObservation ; rr:planId ?plan ; rr:rollbackCost ?rollbackCost ; rr:realizedRelief ?realized .
} ORDER BY DESC(?rollbackCost)
