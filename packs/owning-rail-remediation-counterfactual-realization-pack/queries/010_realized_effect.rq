PREFIX rr: <https://chatman.ai/measure/remediation-realization#>
SELECT ?plan (AVG(?realized) AS ?meanRealizedRelief) WHERE {
  ?o a rr:OutcomeObservation ; rr:planId ?plan ; rr:realizedRelief ?realized .
} GROUP BY ?plan
