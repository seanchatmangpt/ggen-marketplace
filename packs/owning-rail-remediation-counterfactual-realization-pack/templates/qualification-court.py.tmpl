# Generated consequence: owning-rail remediation counterfactual realization court.
assert exact_subject_sha == "{{ subject_sha }}"
assert authority == "OBSERVE|VERIFY"
assert actuation_performed is False
assert calibration_state in {"CALIBRATED", "UNRELIABLE", "INSUFFICIENT"}
assert standing != "ALIVE"
