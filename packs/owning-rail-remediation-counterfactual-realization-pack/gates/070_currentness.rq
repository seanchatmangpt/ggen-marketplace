PREFIX rr: <https://chatman.ai/measure/remediation-realization#>
SELECT ?model ?generation WHERE {
  ?model a rr:CalibrationModel ; rr:generation ?generation .
  FILTER EXISTS {
    ?other a rr:CalibrationModel ; rr:generation ?otherGeneration .
    FILTER(?otherGeneration > ?generation)
  }
}
