PREFIX rr: <https://chatman.ai/measure/remediation-realization#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?o ?p WHERE {
  ?o a rr:OutcomeObservation ; rr:propensity ?p .
  FILTER(xsd:decimal(?p) <= 0 || xsd:decimal(?p) > 1)
}
