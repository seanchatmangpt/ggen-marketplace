---
to: docs/SAFE_EA_SELF_PLAY_USE_CASES.md
force: true
sparql:
  scenarios: |
    PREFIX sp: <https://ggen.dev/ontology/self-play#>
    SELECT ?scenarioId (GROUP_CONCAT(DISTINCT ?useCase; separator=", ") AS ?useCases)
           (COUNT(DISTINCT ?policy) AS ?actors)
           (COUNT(DISTINCT ?action) AS ?actions)
    WHERE {
      ?scenario a sp:Scenario ; sp:scenarioId ?scenarioId ;
                sp:coversUseCase ?useCaseNode ; sp:hasPolicy ?policy ; sp:hasAction ?action .
      ?useCaseNode sp:code ?useCase .
    }
    GROUP BY ?scenarioId
    ORDER BY ?scenarioId
---
# SAFe and Enterprise Architecture as Strategy Self-Play

GENERATED by `ggen sync run` from the admitted ontology. Do not hand-edit.

The framework performs deterministic rule self-play. It makes zero LLM calls. Each role chooses the highest-utility legal move under explicit guards, authority tokens, choice groups, invariants, and round ceilings. Model transitions may manufacture BRCE-addressed intent, but `actuation_performed` remains false.

| Scenario | Use cases | Policies | Actions |
|---|---|---:|---:|
{% for row in scenarios %}| `{{ row.scenarioId }}` | {{ row.useCases }} | {{ row.actors }} | {{ row.actions }} |
{% endfor %}

## Proof surface

1. The real ggen compiler manufactures the Rust runner, independent verifier, tests, and manifest.
2. The runner and verifier execute as separate operating-system processes.
3. Every move produces a BLAKE3 state transition and predecessor-linked receipt.
4. A second process execution must be byte-identical.
5. Tampering with a real receipt must be refused independently.
6. All twelve SAFe and Enterprise Architecture as Strategy use cases must reach `ALIVE` without direct actuation.
