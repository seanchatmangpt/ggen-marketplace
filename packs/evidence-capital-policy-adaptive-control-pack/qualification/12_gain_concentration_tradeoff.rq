PREFIX ec: <https://chatman.ai/evidence-capital#>
SELECT ?outcome ?gain ?concentration WHERE { ?outcome a ec:PolicyOutcome ; ec:realizedGain ?gain ; ec:rootConcentration ?concentration . }
ORDER BY DESC(?gain) ?concentration ?outcome
