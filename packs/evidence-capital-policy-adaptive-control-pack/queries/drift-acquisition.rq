PREFIX ec: <https://chatman.ai/evidence-capital#>
SELECT ?decision ?drift ?regret WHERE {
  ?decision a ec:PolicyDecision ; ec:driftScore ?drift ; ec:regret ?regret .
  FILTER(?drift >= 1.0 || ?regret > 0.20)
}
ORDER BY DESC(?drift) DESC(?regret) ?decision
