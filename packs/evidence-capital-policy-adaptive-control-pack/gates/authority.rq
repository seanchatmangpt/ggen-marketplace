PREFIX ec: <https://chatman.ai/evidence-capital#>
SELECT ?receipt ?authority ?actuated WHERE {
  ?receipt a ec:ControlReceipt ; ec:authority ?authority ; ec:actuationPerformed ?actuated .
  FILTER(?authority = "DO" || ?actuated = true)
}
