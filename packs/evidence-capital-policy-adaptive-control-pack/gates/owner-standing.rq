PREFIX ec: <https://chatman.ai/evidence-capital#>
SELECT ?decision ?standing WHERE {
  ?decision a ec:PolicyDecision ; ec:ownerStanding ?standing .
  FILTER(?standing = "BUILD_BROKEN" || ?standing = "BLOCKED")
}
