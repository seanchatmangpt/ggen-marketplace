PREFIX ec: <https://chatman.ai/evidence-capital#>
SELECT ?generation (COUNT(?frontier) AS ?count) WHERE {
  ?frontier a ec:ControlFrontier ; ec:generation ?generation .
}
GROUP BY ?generation
HAVING(COUNT(?frontier) > 1)
