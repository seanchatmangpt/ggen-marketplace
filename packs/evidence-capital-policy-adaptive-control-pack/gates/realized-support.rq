PREFIX ec: <https://chatman.ai/evidence-capital#>
SELECT ?decision (COUNT(?outcome) AS ?support) WHERE {
  ?decision a ec:PolicyDecision .
  ?outcome a ec:PolicyOutcome .
}
GROUP BY ?decision
HAVING(COUNT(?outcome) < 5)
