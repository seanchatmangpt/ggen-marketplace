PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reuse ?fanout (?reuse * ?fanout AS ?score) WHERE { ?candidate a clm:Candidate ; clm:reusability ?reuse ; clm:consumerFanout ?fanout ; clm:actuationPerformed false . }
ORDER BY DESC(?score) ?candidate
