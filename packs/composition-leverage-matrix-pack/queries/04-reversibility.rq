PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reversibility WHERE { ?candidate a clm:Candidate ; clm:reversibility ?reversibility ; clm:actuationPerformed false . }
ORDER BY DESC(?reversibility) ?candidate
