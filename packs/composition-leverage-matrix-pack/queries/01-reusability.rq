PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reuse WHERE { ?candidate a clm:Candidate ; clm:reusability ?reuse ; clm:actuationPerformed false . }
ORDER BY DESC(?reuse) ?candidate
