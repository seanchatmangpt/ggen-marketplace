PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?composition ?order ?leverage WHERE { ?composition a clm:Composition ; clm:order ?order ; clm:expectedLeverage ?leverage ; clm:qualified true . FILTER(?order > 2) }
ORDER BY DESC(?order) DESC(?leverage) ?composition
