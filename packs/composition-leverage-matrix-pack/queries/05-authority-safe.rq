PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?authority WHERE { ?candidate a clm:Candidate ; clm:authority ?authority ; clm:actuationPerformed false . FILTER(?authority != "DO") }
ORDER BY ?authority ?candidate
