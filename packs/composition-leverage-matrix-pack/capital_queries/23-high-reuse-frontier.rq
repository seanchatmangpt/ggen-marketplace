PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reuse WHERE { ?candidate a clm:Candidate ; clm:reusability ?reuse ; clm:actuationPerformed false . FILTER(?reuse >= 0.8) }
ORDER BY DESC(?reuse) ?candidate
