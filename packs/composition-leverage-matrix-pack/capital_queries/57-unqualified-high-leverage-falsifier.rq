PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?composition ?leverage WHERE { ?composition a clm:Composition ; clm:expectedLeverage ?leverage . FILTER(?leverage >= 10) FILTER NOT EXISTS { ?composition clm:qualified true } }
ORDER BY DESC(?leverage) ?composition
