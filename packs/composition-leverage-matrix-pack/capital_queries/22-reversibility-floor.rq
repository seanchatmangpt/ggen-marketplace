PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reversibility WHERE { ?candidate a clm:Candidate ; clm:reversibility ?reversibility ; clm:actuationPerformed false . FILTER(?reversibility >= 0.8) }
ORDER BY DESC(?reversibility) ?candidate
