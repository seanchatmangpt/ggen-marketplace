PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?authority (COUNT(?candidate) AS ?count) WHERE { ?candidate a clm:Candidate ; clm:authority ?authority ; clm:actuationPerformed false . FILTER(?authority != "DO") }
GROUP BY ?authority
ORDER BY DESC(?count) ?authority
