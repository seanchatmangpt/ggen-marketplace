PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?composition (COUNT(?member) AS ?members) WHERE { ?composition a clm:Composition ; clm:member ?member ; clm:qualified true . }
GROUP BY ?composition
ORDER BY DESC(?members) ?composition
