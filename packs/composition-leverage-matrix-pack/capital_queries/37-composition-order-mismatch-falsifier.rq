PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?composition ?order (COUNT(?member) AS ?members) WHERE { ?composition a clm:Composition ; clm:order ?order ; clm:member ?member ; clm:qualified true . }
GROUP BY ?composition ?order
HAVING(COUNT(?member) != ?order)
ORDER BY ?composition
