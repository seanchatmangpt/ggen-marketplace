PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?composition ?order ?leverage WHERE { ?composition a clm:Composition ; clm:order ?order ; clm:expectedLeverage ?leverage ; clm:qualified true . FILTER(?leverage >= 10) }
ORDER BY DESC(?leverage) DESC(?order) ?composition
