# Reference

Candidate dimensions are `reusability`, `consumerFanout`, `qualificationFanout`, and `reversibility`. Composition dimensions are `order` and `expectedLeverage`. Authority is descriptive admission evidence only; this pack never actuates. Total leverage is a projection for exploration, not a grant of standing or DO authority.
