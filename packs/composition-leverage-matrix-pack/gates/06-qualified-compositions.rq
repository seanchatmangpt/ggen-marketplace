PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
ASK WHERE { FILTER NOT EXISTS { ?composition a clm:Composition . FILTER NOT EXISTS { ?composition clm:qualified true ; clm:expectedLeverage ?leverage . FILTER(?leverage > 0) } } }
