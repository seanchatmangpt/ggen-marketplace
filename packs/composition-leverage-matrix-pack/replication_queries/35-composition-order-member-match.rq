PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?composition ?order (COUNT(?member) AS ?memberCount) WHERE { ?composition a clm:Composition ; clm:order ?order ; clm:member ?member . }
GROUP BY ?composition ?order
HAVING(COUNT(?member) = ?order)
ORDER BY DESC(?order) ?composition
