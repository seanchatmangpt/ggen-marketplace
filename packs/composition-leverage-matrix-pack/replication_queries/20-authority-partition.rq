PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?authority (COUNT(?candidate) AS ?candidateCount) WHERE { ?candidate a clm:Candidate ; clm:authority ?authority ; clm:actuationPerformed false . }
GROUP BY ?authority
ORDER BY DESC(?candidateCount) ?authority
