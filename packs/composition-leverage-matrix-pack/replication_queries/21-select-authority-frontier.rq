PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate WHERE { ?candidate a clm:Candidate ; clm:authority "SELECT" ; clm:actuationPerformed false . }
ORDER BY ?candidate
