PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reversibility WHERE { ?candidate a clm:Candidate ; clm:reversibility ?reversibility . FILTER(?reversibility < 0.0) }
ORDER BY ?reversibility ?candidate
