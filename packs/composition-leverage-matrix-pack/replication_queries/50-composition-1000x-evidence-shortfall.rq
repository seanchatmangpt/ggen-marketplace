PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?composition ?expectedLeverage WHERE { ?composition a clm:Composition ; clm:expectedLeverage ?expectedLeverage ; clm:qualified true . FILTER(?expectedLeverage < 1000) }
ORDER BY DESC(?expectedLeverage) ?composition
