PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate WHERE { ?candidate a clm:Candidate ; clm:actuationPerformed false . FILTER NOT EXISTS { ?composition a clm:Composition ; clm:member ?candidate . } }
ORDER BY ?candidate
