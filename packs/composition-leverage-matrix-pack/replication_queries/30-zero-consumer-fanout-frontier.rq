PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate WHERE { ?candidate a clm:Candidate ; clm:consumerFanout 0 ; clm:actuationPerformed false . }
ORDER BY ?candidate
