PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?capability (COUNT(?candidate) AS ?candidateCount) WHERE { ?candidate a clm:Candidate ; clm:capability ?capability ; clm:actuationPerformed false . }
GROUP BY ?capability
ORDER BY DESC(?candidateCount) ?capability
