PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reuse WHERE { ?candidate a clm:Candidate ; clm:reusability ?reuse . FILTER(?reuse > 1.0) }
ORDER BY DESC(?reuse) ?candidate
