PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reversibility WHERE { ?candidate a clm:Candidate ; clm:reversibility ?reversibility . FILTER(?reversibility > 1.0) }
ORDER BY DESC(?reversibility) ?candidate
