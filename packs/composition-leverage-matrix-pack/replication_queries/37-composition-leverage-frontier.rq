PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?composition ?leverage WHERE { ?composition a clm:Composition ; clm:expectedLeverage ?leverage ; clm:qualified true . }
ORDER BY DESC(?leverage) ?composition
