PREFIX clm: <https://ggen.dev/ontology/composition-leverage-matrix#>
SELECT ?candidate ?reuse WHERE { ?candidate a clm:Candidate ; clm:reusability ?reuse ; clm:actuationPerformed false . FILTER(?reuse < 0.5) }
ORDER BY ?reuse ?candidate
