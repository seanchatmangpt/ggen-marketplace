# generated by ggen; deterministic qualification projection
COMPOSITIONS = [
{% for row in rows %}    ("{{ row.composition }}", {{ row.order }}, {{ row.leverage }}),
{% endfor %}]
assert all(order >= 2 and leverage > 0 for _, order, leverage in COMPOSITIONS)
