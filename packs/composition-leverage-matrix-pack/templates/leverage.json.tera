{
  "schema": "ggen.composition-leverage/1",
  "candidates": [
  {% for row in results %}{"candidate":"{{ row.candidate }}","leverage":{{ row.leverage }}}{% if not loop.last %},{% endif %}{% endfor %}
  ],
  "actuation_performed": false
}
