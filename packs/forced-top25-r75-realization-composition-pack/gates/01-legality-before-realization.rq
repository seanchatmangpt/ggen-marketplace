PREFIX fta: <https://ggen.dev/ontology/forced-top25-admissibility#>
SELECT ?repo WHERE {
  ?target a fta:FanoutTarget ; fta:repo ?repo .
  FILTER(
    NOT EXISTS { ?target fta:compatibilityState "COMPATIBLE_READY" } ||
    EXISTS { ?target fta:blocker ?blocker }
  )
}
ORDER BY ?repo
