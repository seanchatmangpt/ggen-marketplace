# GENERATED by ggen from the canonical DfCM ontology. Do not hand-edit.
from __future__ import annotations

from dataclasses import dataclass, replace
from enum import Enum
from hashlib import blake2b
from typing import Protocol, Sequence

COMPONENTS = (
{% for row in results %}    {"order": {{ row["order"] }}, "slug": "{{ row["slug"] }}", "name": "{{ row["name"] }}", "kind": "{{ row["kind"] }}"},
{% endfor %})

class Standing(str, Enum):
    UNKNOWN = "UNKNOWN"
    PARTIAL_ALIVE = "PARTIAL_ALIVE"
    ALIVE = "ALIVE"
    BLOCKED = "BLOCKED"
    BUILD_BROKEN = "BUILD_BROKEN"
    UNSUPPORTED = "UNSUPPORTED"
    REFUSED = "REFUSED"

class DfcmRefusal(RuntimeError):
    def __init__(self, code: str, detail: str):
        super().__init__(f"{code}: {detail}")
        self.code = code
        self.detail = detail

@dataclass(frozen=True)
class Evidence:
    observed: bool = False
    admitted: bool = False
    executed: bool = False
    changed: bool = False
    verified: bool = False
    inferred: bool = False
    refused: bool = False
    blocked: bool = False
    unsupported: bool = False

@dataclass(frozen=True)
class Intent:
    intent_id: str
    subject: str
    operation: str
    payload: bytes
    policy_id: str

@dataclass(frozen=True)
class Artifact:
    subject: str
    operation: str
    payload: bytes
    digest: str

@dataclass(frozen=True)
class Grant:
    grant_id: str
    subject: str
    operation: str
    artifact_digest: str
    policy_id: str

@dataclass(frozen=True)
class Receipt:
    receipt_id: str
    subject: str
    operation: str
    policy_id: str
    artifact_digest: str
    consequence_digest: str
    predecessor_digest: str
    digest: str
    changed: bool

@dataclass(frozen=True)
class Edge:
    source: str
    target: str
    lawful: bool = True
    available: bool = True

class Actuator(Protocol):
    def actuate(self, artifact: Artifact) -> tuple[bytes, bool]: ...

class OptionGraph:
    def __init__(self, edges: Sequence[Edge]):
        self._edges = list(edges)

    def available_from(self, source: str) -> tuple[Edge, ...]:
        return tuple(e for e in self._edges if e.source == source and e.lawful and e.available)

    def mark_failed(self, source: str, target: str) -> None:
        for i, edge in enumerate(self._edges):
            if edge.source == source and edge.target == target:
                self._edges[i] = replace(edge, available=False)
                return
        raise DfcmRefusal("REFUSED_UNKNOWN_EDGE", f"{source}->{target}")

def digest_bytes(*parts: bytes) -> str:
    h = blake2b(digest_size=32)
    for part in parts:
        h.update(len(part).to_bytes(8, "big"))
        h.update(part)
    return h.hexdigest()

def select(candidates: Sequence[Intent], intent_id: str) -> Intent:
    for candidate in candidates:
        if candidate.intent_id == intent_id:
            return candidate
    raise DfcmRefusal("REFUSED_SELECTION", intent_id)

def construct(intent: Intent) -> Artifact:
    # Construction has no actuator and therefore cannot perform DO.
    payload = intent.payload
    return Artifact(intent.subject, intent.operation, payload, digest_bytes(payload))

class AdmissionBoundary:
    def __init__(self, allowed_operations: Sequence[str], policy_id: str):
        self.allowed_operations = frozenset(allowed_operations)
        self.policy_id = policy_id

    def admit(self, intent: Intent, artifact: Artifact) -> Grant:
        if intent.policy_id != self.policy_id:
            raise DfcmRefusal("REFUSED_POLICY", intent.policy_id)
        if intent.operation not in self.allowed_operations:
            raise DfcmRefusal("REFUSED_OPERATION", intent.operation)
        if intent.subject != artifact.subject or intent.operation != artifact.operation:
            raise DfcmRefusal("REFUSED_SUBJECT_MISMATCH", intent.subject)
        return Grant(
            grant_id=digest_bytes(intent.intent_id.encode(), artifact.digest.encode())[:24],
            subject=intent.subject,
            operation=intent.operation,
            artifact_digest=artifact.digest,
            policy_id=self.policy_id,
        )

class ReceiptLedger:
    def __init__(self):
        self._receipts: list[Receipt] = []

    @property
    def receipts(self) -> tuple[Receipt, ...]:
        return tuple(self._receipts)

    @property
    def head(self) -> str:
        return self._receipts[-1].digest if self._receipts else "GENESIS"

    def append(self, receipt: Receipt) -> None:
        if receipt.predecessor_digest != self.head:
            raise DfcmRefusal("REFUSED_RECEIPT_PREDECESSOR", receipt.predecessor_digest)
        if not self.verify(receipt):
            raise DfcmRefusal("REFUSED_RECEIPT_DIGEST", receipt.receipt_id)
        self._receipts.append(receipt)

    @staticmethod
    def verify(receipt: Receipt) -> bool:
        expected = digest_bytes(
            receipt.receipt_id.encode(), receipt.subject.encode(), receipt.operation.encode(),
            receipt.policy_id.encode(), receipt.artifact_digest.encode(),
            receipt.consequence_digest.encode(), receipt.predecessor_digest.encode(),
            b"1" if receipt.changed else b"0",
        )
        return expected == receipt.digest

    def replay(self) -> bool:
        predecessor = "GENESIS"
        for receipt in self._receipts:
            if receipt.predecessor_digest != predecessor or not self.verify(receipt):
                return False
            predecessor = receipt.digest
        return True

class BRCE:
    def __init__(self, ledger: ReceiptLedger):
        self.ledger = ledger

    def do(self, grant: Grant, artifact: Artifact, actuator: Actuator) -> tuple[Receipt, Evidence]:
        if grant.subject != artifact.subject or grant.operation != artifact.operation:
            raise DfcmRefusal("REFUSED_SUBJECT_MISMATCH", artifact.subject)
        if grant.artifact_digest != artifact.digest:
            raise DfcmRefusal("REFUSED_ARTIFACT_DIGEST", artifact.digest)
        consequence, changed = actuator.actuate(artifact)
        consequence_digest = digest_bytes(consequence)
        predecessor = self.ledger.head
        receipt_id = digest_bytes(grant.grant_id.encode(), predecessor.encode())[:24]
        receipt_digest = digest_bytes(
            receipt_id.encode(), artifact.subject.encode(), artifact.operation.encode(),
            grant.policy_id.encode(), artifact.digest.encode(), consequence_digest.encode(),
            predecessor.encode(), b"1" if changed else b"0",
        )
        receipt = Receipt(receipt_id, artifact.subject, artifact.operation, grant.policy_id,
                          artifact.digest, consequence_digest, predecessor, receipt_digest, changed)
        self.ledger.append(receipt)
        evidence = Evidence(observed=True, admitted=True, executed=True, changed=changed, verified=True)
        return receipt, evidence

class HookBoundary:
    @staticmethod
    def on_receipt(receipt: Receipt) -> Intent:
        # Hooks can manufacture a new intent, never invoke an actuator.
        return Intent(
            intent_id=f"hook-{receipt.receipt_id}", subject=receipt.subject,
            operation="replay", payload=receipt.digest.encode(), policy_id=receipt.policy_id,
        )

def standing(evidence: Evidence, exact_subject: bool) -> Standing:
    if evidence.refused:
        return Standing.REFUSED
    if evidence.blocked:
        return Standing.BLOCKED
    if evidence.unsupported:
        return Standing.UNSUPPORTED
    if exact_subject and evidence.observed and evidence.admitted and evidence.executed and evidence.verified:
        return Standing.ALIVE
    if evidence.observed or evidence.admitted or evidence.executed or evidence.verified:
        return Standing.PARTIAL_ALIVE
    return Standing.UNKNOWN
