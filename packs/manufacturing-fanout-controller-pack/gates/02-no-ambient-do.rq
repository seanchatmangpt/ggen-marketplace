PREFIX mfc: <https://ggen.dev/ontology/manufacturing-fanout#>
ASK {
  mfc:controller mfc:authority "SELECT|CONSTRUCT|VERIFY" ; mfc:actuationPerformed false .
  FILTER NOT EXISTS { ?s mfc:authority "DO" }
  FILTER NOT EXISTS { ?s mfc:actuationPerformed true }
}
