import json
from pathlib import Path

def test_fanout_plan():
    data=json.loads(Path('generated/fanout/plan.json').read_text())
    assert data['schema']=='ggen.manufacturing-fanout/1'
    assert data['actuation_performed'] is False
    assert all(plan['yield'] > 1 for plan in data['plans'])
