# Composition contract

Validated before publication against the pack graph plus unrelated foreign `prov:Agent`, `prov:Entity`, and SKOS subjects.

Expected result from `gates/010_admission.rq`: zero rows for both the standalone pack and the composed graph.

The gate is intentionally profile/namespace scoped. Public ontology types do not imply pack ownership.

This evidence establishes the static semantic composition contract only. It does **not** claim a current `ggen sync` execution receipt; the exact ggen toolchain must execute this pack separately before manufacture standing can become ALIVE.
