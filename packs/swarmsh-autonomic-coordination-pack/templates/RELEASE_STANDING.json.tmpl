{
{% for row in results %}  "identifier": "{{ row["identifier"] }}",
  "title": "{{ row["title"] }}",
  "version": "{{ row["version"] }}",
  "authority": "{{ row["authority"] }}",
  "qualification": "{{ row["qualification"] }}",
  "standing": "UNKNOWN",
  "standing_rule": "Promotion to ALIVE requires consumer exact-head evidence; generation alone never crowns runtime standing."
{% endfor %}}
