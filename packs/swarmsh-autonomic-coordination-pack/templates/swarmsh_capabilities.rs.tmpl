// GENERATED by swarmsh-autonomic-coordination-pack v26.8.25. DO NOT EDIT.
// Facts only: this module intentionally exposes no execution primitive.

pub const RELEASE: &str = "26.8.25";
pub const PACK: &str = "swarmsh-autonomic-coordination-pack";
pub const GENERATED_AUTHORITY: &str = "construct-only";

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Capability {
    pub order: u16,
    pub slug: &'static str,
    pub category: &'static str,
    pub authority: &'static str,
    pub proof_surface: &'static str,
}

pub const CAPABILITIES: &[Capability] = &[
{% for row in results %}    Capability { order: {{ row["order"] }}, slug: "{{ row["slug"] }}", category: "{{ row["category"] }}", authority: "{{ row["authority"] }}", proof_surface: "{{ row["proofSurface"] }}" },
{% endfor %}];

pub fn capability(slug: &str) -> Option<&'static Capability> {
    CAPABILITIES.iter().find(|capability| capability.slug == slug)
}
