---
to: test/beam4pm_ai_contracts_codec_test.exs
sparql:
  records: |
    PREFIX aic: <https://ggen.dev/ontology/beam4pm-ai#>
    SELECT ?record_name ?record_doc WHERE {
      ?rt a aic:ContractType ;
          aic:contractName ?record_name ;
          aic:contractDoc ?record_doc .
    } ORDER BY ?record_name
  fields: |
    PREFIX aic: <https://ggen.dev/ontology/beam4pm-ai#>
    SELECT ?record_name ?field_name ?field_type ?field_doc ?field_required ?field_order WHERE {
      ?rt a aic:ContractType ;
          aic:contractName ?record_name ;
          aic:hasField ?f .
      ?f aic:fieldName ?field_name ;
         aic:fieldType ?field_type ;
         aic:fieldDoc ?field_doc ;
         aic:fieldRequired ?field_required ;
         aic:fieldOrder ?field_order .
    } ORDER BY ?record_name ?field_order
---
# GENERATED by ggen from the admitted aic:ContractType graph. Do not edit.
defmodule BeamPM.AI.Contracts.Codec.GeneratedTest do
  use ExUnit.Case, async: true

  test "from_map returns unknown_record for an unadmitted record name" do
    assert {:error, {:unknown_record, :not_a_real_record}} =
             BeamPM.AI.Contracts.Codec.from_map(:not_a_real_record, %{})
  end

  test "decode returns unknown_record for an unadmitted record name" do
    assert {:error, {:unknown_record, :not_a_real_record}} =
             BeamPM.AI.Contracts.Codec.decode(:not_a_real_record, "{}")
  end

  test "decode wraps invalid JSON input in an error tuple" do
    assert {:error, _} = BeamPM.AI.Contracts.Codec.decode(:not_a_real_record, "this is not json")
  end

  test "decode rejects JSON that is not an object" do
    assert {:error, {:not_a_json_object, [1, 2, 3]}} =
             BeamPM.AI.Contracts.Codec.decode(:not_a_real_record, "[1,2,3]")
  end
{% for r in records %}{% set rfields = fields | filter(attribute="record_name", value=r.record_name) %}{% set req_str = rfields | filter(attribute="field_required", value="true") %}{% set req_bool = rfields | filter(attribute="field_required", value=true) %}{% set req = req_str | concat(with=req_bool) %}{% set opt_str = rfields | filter(attribute="field_required", value="false") %}{% set opt_bool = rfields | filter(attribute="field_required", value=false) %}{% set opt = opt_str | concat(with=opt_bool) %}{% set mod_name = r.record_name | replace(from="_", to=" ") | title | replace(from=" ", to="") %}
  test "{{ r.record_name }} to_map/from_map roundtrip (full variant) with exact map values" do
    attrs = %{
{% for f in rfields %}      {{ f.field_name }}: {% if f.field_type == "string" %}"sample_{{ f.field_name }}"{% elif f.field_type == "datetime" %}"2026-08-29T12:00:00Z"{% elif f.field_type == "integer" %}42{% elif f.field_type == "float" %}3.5{% elif f.field_type == "boolean" %}true{% elif f.field_type == "atom" %}:sample_atom{% elif f.field_type == "list_string" %}["alpha", "beta"]{% elif f.field_type == "map" %}%{"k" => "v"}{% endif %}{% if not loop.last %},{% endif %}
{% endfor %}    }

    assert {:ok, rec} = BeamPM.AI.Contracts.{{ mod_name }}.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
{% for f in rfields %}    assert m["{{ f.field_name }}"] == {% if f.field_type == "string" %}"sample_{{ f.field_name }}"{% elif f.field_type == "datetime" %}"2026-08-29T12:00:00Z"{% elif f.field_type == "integer" %}42{% elif f.field_type == "float" %}3.5{% elif f.field_type == "boolean" %}true{% elif f.field_type == "atom" %}"sample_atom"{% elif f.field_type == "list_string" %}["alpha", "beta"]{% elif f.field_type == "map" %}%{"k" => "v"}{% endif %}
{% endfor %}    assert map_size(m) == {{ rfields | length }}
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:{{ r.record_name }}, m)
    assert {:ok, ^rec} =
             BeamPM.AI.Contracts.Codec.from_map(:{{ r.record_name }}, Map.put(m, "definitely_unknown_key", "x"))
  end

  test "{{ r.record_name }} encode/decode JSON roundtrip (full variant)" do
    attrs = %{
{% for f in rfields %}      {{ f.field_name }}: {% if f.field_type == "string" %}"sample_{{ f.field_name }}"{% elif f.field_type == "datetime" %}"2026-08-29T12:00:00Z"{% elif f.field_type == "integer" %}42{% elif f.field_type == "float" %}3.5{% elif f.field_type == "boolean" %}true{% elif f.field_type == "atom" %}:sample_atom{% elif f.field_type == "list_string" %}["alpha", "beta"]{% elif f.field_type == "map" %}%{"k" => "v"}{% endif %}{% if not loop.last %},{% endif %}
{% endfor %}    }

    assert {:ok, rec} = BeamPM.AI.Contracts.{{ mod_name }}.new(attrs)
    json = BeamPM.AI.Contracts.Codec.encode(rec)
    assert is_binary(json)
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.decode(:{{ r.record_name }}, json)
  end

{% if req | length > 0 %}{% set first_req = req | first %}  test "{{ r.record_name }} from_map reports the first missing required field" do
    assert {:error, {:missing_field, :{{ first_req.field_name }}}} =
             BeamPM.AI.Contracts.Codec.from_map(:{{ r.record_name }}, %{})
  end
{% else %}  test "{{ r.record_name }} from_map succeeds on an empty map since no fields are required" do
    assert {:ok, _} = BeamPM.AI.Contracts.Codec.from_map(:{{ r.record_name }}, %{})
  end
{% endif %}
{% if opt | length > 0 %}  test "{{ r.record_name }} to_map omits nil optional fields (minimal variant roundtrip)" do
    attrs = %{
{% for f in req %}      {{ f.field_name }}: {% if f.field_type == "string" %}"sample_{{ f.field_name }}"{% elif f.field_type == "datetime" %}"2026-08-29T12:00:00Z"{% elif f.field_type == "integer" %}42{% elif f.field_type == "float" %}3.5{% elif f.field_type == "boolean" %}true{% elif f.field_type == "atom" %}:sample_atom{% elif f.field_type == "list_string" %}["alpha", "beta"]{% elif f.field_type == "map" %}%{"k" => "v"}{% endif %}{% if not loop.last %},{% endif %}
{% endfor %}    }

    assert {:ok, rec} = BeamPM.AI.Contracts.{{ mod_name }}.new(attrs)
    m = BeamPM.AI.Contracts.Codec.to_map(rec)
{% for f in opt %}    refute Map.has_key?(m, "{{ f.field_name }}")
{% endfor %}    assert map_size(m) == {{ req | length }}
    assert {:ok, ^rec} = BeamPM.AI.Contracts.Codec.from_map(:{{ r.record_name }}, m)
  end
{% endif %}{% endfor %}end
