---
to: "docs/DRIFT_STATUS.md"
force: true
sparql:
  drift: |
    PREFIX cnv-ocel: <https://clap-noun-verb.dev/ontology/ocel#>
    SELECT ?coverage_ratio ?min_coverage_ratio WHERE {
      ?report a cnv-ocel:DriftReport ;
              cnv-ocel:coverageRatio ?coverage_ratio ;
              cnv-ocel:minCoverageRatio ?min_coverage_ratio .
    }
---
# Drift Status

Real admitted-vs-exercised coverage from the merged OCEL corpus.
`gates/010_coverage_floor.rq` refuses generation when coverage falls
below the configured floor.

- Coverage ratio: {{ drift[0].coverage_ratio }}
- Minimum required: {{ drift[0].min_coverage_ratio }}
