[consumer]
# The product bodies are deliberately separate semantic authority. Real
# consumers must union them into the pack graph; qualification uses the same
# documented ggen PackRef contract instead of duplicating the bodies.
extra_ontologies = ["bodies.ttl"]
