# GENERATED by ggen-marketplace/otel-weaver-ocel-pack from ontology.ttl. Do not hand-edit.
#
# wasm4pm-compat requires nightly Rust (generic_const_exprs and related unstable
# features are enabled unconditionally at its crate root, not feature-gated) --
# build this crate with `cargo +nightly check`.
[package]
name = "otel-weaver-ocel"
version = "0.1.0"
edition = "2021"
description = "Generated OTEL span to OCEL v2 event transformer, admitting via wasm4pm-compat's typed Evidence carrier."

[dependencies]
# Sibling checkout on disk, following chicago-tdd-tools' own "Use As a
# Dependency" path-dependency precedent (README.md: "From a sibling checkout
# on disk ... chicago-tdd-tools = { path = \"../chicago-tdd-tools\" }").
wasm4pm-compat = { path = "../../../../wasm4pm-compat" }
chrono = { version = "0.4", features = ["serde"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# ocel_accumulator's OTLP-HTTP ingest server. Sync/blocking, no async
# runtime -- this repo carries no axum/actix/hyper precedent yet, and the
# accumulator's real traffic (a handful of spans/10s) doesn't need one.
tiny_http = "0.12"

[[bin]]
name = "ocel_accumulator"
path = "src/bin/ocel_accumulator.rs"
