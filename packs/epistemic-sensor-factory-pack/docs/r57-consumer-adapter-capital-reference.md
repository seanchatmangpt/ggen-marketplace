# Reference: R57 consumer adapter capital

## Subject

R57 courts are numbered 751–800 and execute over `r55-independent-consumer-fanout.ttl` plus the canonical `ontology.ttl`.

## Dimensions

The court family observes adapter demand, admission standing, exact-head parity, producer target binding, qualification paths, consumer family, receipt return, deterministic ggen manufacturability, dependency closure, replay, generated projection, public ontology alignment, bounded authority, court reuse, and zero ambient DO.

## Standing law

`adapterRequired` is not a failure and does not imply ALIVE. `ADMITTED` is not equivalent to independently qualified. Full adapter readiness requires the complete conjunction used by court 754/782. The 10x shortfall is `10 - full_adapter_readiness_count`; the 1000X crown remains refused until independently returned consumer receipts and downstream causal leverage satisfy their own courts.
