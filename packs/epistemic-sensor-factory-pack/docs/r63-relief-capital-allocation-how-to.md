# How to qualify an R63 capital-allocation candidate

1. Admit an exact consumer subject and preserve repository/ref/SHA identity.
2. Require replay, qualification receipt, falsifier, generated projection, and positive `reliefDelta`.
3. Require non-zero `actionableOpportunities` before treating relief as manufacturing-capital yield.
4. Inspect consumer and evidence-root concentration separately; repeated receipts are not independent breadth.
5. Verify request→execution→receipt→relief temporal ordering.
6. Measure memory-caused opportunities per memory record consumed without granting memory ambient authority.
7. Run `python3 packs/epistemic-sensor-factory-pack/tests/run_r63_relief_capital_allocation.py` and marketplace validation before publication.
