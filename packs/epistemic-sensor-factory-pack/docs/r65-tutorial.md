# Tutorial: close the epistemic reinvestment loop

1. Bind observations to exact repository heads.
2. Load `ontology.ttl`, `ontology.r65-epistemic-reinvestment.ttl`, and the R65 fixture or equivalent live projection.
3. Execute queries 1401–1450 to measure realized yield, dependency relief, reuse, calibration, redundancy, complementarity, replay and authority.
4. Execute query 1451 through ggen to manufacture the deterministic reinvestment plan.
5. Treat the plan as CONSTRUCT output only. It has no ambient DO authority.
6. Re-qualify the exact resulting subject and bind receipts before any later BRCE-governed actuation.
