# R57 how-to: admit multiplicative evidence

1. Resolve every repository subject to an exact current SHA.
2. Bind each factor to receipts and at least two independent evidence roots when the factor is used for an admitted multiplier.
3. Record `observationFactor`, `reuseFactor`, and `opportunityFactor` separately; never infer missing factors from the product.
4. Record `frontierBefore`, `frontierAfter`, and `frontierDelta` so throughput without frontier expansion is observable.
5. Execute sensors 751–800 and the R57 court.
6. Treat sensor 800 as the clean frontier: each factor >=10, product >=1000, positive frontier delta, multiple roots, and explicit multiplier admission.
7. Keep consequential DO false; R57 observes and derives only.
