# How-to: Manufacture the next target-local adapter

1. Run the R57 court and record the exact prioritized consumer repository and current default head.
2. Revalidate that head immediately before construction; stale-head evidence is a refusal, not a repair hint.
3. Preserve the producer target token, qualification path, public ontology alignment, bounded authority, replay semantics, and zero-ambient-DO invariant.
4. Prefer an existing ggen-marketplace generator or adapter family. Handwrite only the irreducible runtime/bootstrap boundary and record the reason.
5. Manufacture on a purpose branch from the exact consumer default SHA.
6. Execute the producer-owned court against the exact consumer subject, return a machine-readable receipt, and independently verify replay.
7. Promote consumer breadth only after the returned exact-subject receipt is ALIVE. Admission metadata alone is insufficient.
