# Tutorial: turn a consumer receipt into propagation capital

Start from a qualified consumer receipt bound to an exact repository head. Project the receipt into the R51 fixture or equivalent admitted RDF, execute sensors `500–549`, and use the `transitive-propagation-plan` ggen rule to derive the next exact targets. A returned consumer standing is evidence, never ambient authority. Consequential execution remains prohibited; downstream actions require their own admission and receipt boundary.
