# Tutorial — compile a consumer replication frontier

1. Admit exact repository default-head subjects as `esf:ReplicationTarget` facts.
2. Bind an independent evidence root for each target.
3. Run ggen with `ggen.toml`; the `replication-plan` rule projects deterministic consumer work.
4. Execute sensors 186–235 against the admitted graph.
5. Qualify each consumer independently; never import producer standing.
6. Record receipts and feed missing/failed targets back to EXPLORE.

The compiler has SELECT/CONSTRUCT/VERIFY authority only. Consequential DO remains outside this pack.
