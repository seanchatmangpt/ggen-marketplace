# R58 tutorial: observe the independent consumer factor

Start from exact live default-head identities for each consumer. Materialize those identities as `esf:ExactSubject` evidence, bind each admitted candidate to a cross-repository replication target, and preserve its observed standing rather than inheriting producer standing. Execute sensors 851–900 against that graph. Sensor 896 reports the independently ALIVE consumer factor; sensor 897 exposes the shortfall to factor 10; sensor 900 admits the 10x consumer factor only when ten authority-bounded, no-ambient-DO, public-ontology-aligned cross-repository consumers are independently ALIVE.

The reference fixture is evidence for the court, not a fleet-wide claim. Generated `consumer-factor-report.json` is a deterministic consequence and must not become an editing surface.
