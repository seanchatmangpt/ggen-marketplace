# Tutorial: observe independent consumer evidence

Start with an exact consumer subject for each candidate repository. Record runtime, language, execution kernel, failure domain, authority domain, receipt protocol, qualification family, ontology profile, and hosting plane in `fixtures/r54-consumer-independence-current.ttl`.

Run `python3 packs/epistemic-sensor-factory-pack/tests/run_r54_consumer_independence.py`. The court executes sensors 651–700 and refuses to treat raw repository count as independent epistemic evidence. A consumer adds multiplicative confidence only when its evidence-generating mechanism is sufficiently orthogonal to already admitted consumers.

Use sensor 699 to expose the next non-admitted candidate and sensor 700 to inspect the clean exact-subject frontier. Revalidate candidate heads before changing standing; fixture observations are evidence snapshots, not ambient current truth.
