PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?head (COUNT(?consumer) AS ?count) WHERE { ?consumer a esf:ExactSubject ; esf:consumerHead ?head . } GROUP BY ?head HAVING(COUNT(?consumer) > 1)
