PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?execution ?cell WHERE { ?execution a esf:CellExecution ; esf:cell ?cell . FILTER(?cell != "MEMORY_COMPLETE") FILTER NOT EXISTS { ?execution esf:nextCell ?next } } ORDER BY ?cell