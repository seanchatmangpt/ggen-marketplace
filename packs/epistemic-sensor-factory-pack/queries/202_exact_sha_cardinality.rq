PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?subject (COUNT(DISTINCT ?sha) AS ?shaCount) WHERE { ?subject a esf:ExactSubject ; esf:subjectSha ?sha . } GROUP BY ?subject HAVING(COUNT(DISTINCT ?sha) != 1)
