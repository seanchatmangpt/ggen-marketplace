PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?court (COUNT(DISTINCT ?head) AS ?consumer_heads) WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:producerCourtHead ?court ; esf:consumerHead ?head . } GROUP BY ?court ORDER BY DESC(?consumer_heads)
