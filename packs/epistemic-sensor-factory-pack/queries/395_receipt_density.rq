PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?receipt) AS ?receipts) (COUNT(DISTINCT ?consumer) AS ?consumers) WHERE { ?consumer a esf:ExactSubject ; esf:consumerHead ?head . OPTIONAL { ?receipt a esf:ConsumerReceiptAssertion ; esf:consumerHead ?head . } }
