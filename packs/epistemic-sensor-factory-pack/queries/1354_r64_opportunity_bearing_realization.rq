PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?obs) AS ?opportunityBearing) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:downstreamOpportunityCount ?count . FILTER(?count > 0) }
