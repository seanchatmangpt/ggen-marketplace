PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?head (COUNT(?plan) AS ?count) WHERE { ?plan a esf:ConsumerRealizationPlan ; esf:exactTargetHead ?head . } GROUP BY ?head HAVING(COUNT(?plan) > 1)
