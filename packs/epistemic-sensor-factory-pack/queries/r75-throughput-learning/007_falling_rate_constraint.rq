PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?run ?current ?previous WHERE { ?run a esf:ScheduleRun ; esf:commitsPerHour ?current ; esf:previousHourRate ?previous . FILTER(?current < ?previous) }
