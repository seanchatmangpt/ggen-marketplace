PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?commit (COUNT(DISTINCT ?run) AS ?runCount) WHERE { ?commit a esf:SemanticCommit ; esf:attributedToRun ?run . } GROUP BY ?commit HAVING(COUNT(DISTINCT ?run) > 1)
