PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
PREFIX prov: <http://www.w3.org/ns/prov#>
SELECT ?a WHERE { ?a a esf:ProducerAssimilation . FILTER NOT EXISTS { ?a prov:used ?receipt . ?receipt a esf:ConsumerReceiptAssertion . } }
