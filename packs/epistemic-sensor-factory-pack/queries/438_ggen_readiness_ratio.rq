PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?ready)/COUNT(?c) AS ?ggen_readiness_ratio) WHERE { ?c a esf:ConsumerAdmissionCandidate . OPTIONAL { ?c esf:ggenManufacturable ?ready . FILTER(?ready = true) } }
