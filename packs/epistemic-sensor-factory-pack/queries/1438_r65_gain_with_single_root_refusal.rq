PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs ?delta ?roots WHERE { ?obs a esf:ProductionFunctionObservation ; esf:frontierDelta ?delta ; esf:factorIndependentRoots ?roots . FILTER(?delta > 0 && ?roots < 2) }
