PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?sharedQualificationPairs) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:qualificationPath ?path .
  ?b a esf:ConsumerAdmissionCandidate ; esf:qualificationPath ?path .
  FILTER(STR(?a) < STR(?b))
}
