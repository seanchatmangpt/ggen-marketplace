PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?ready)/COUNT(?c) AS ?receipt_return_ratio) WHERE { ?c a esf:ConsumerAdmissionCandidate . OPTIONAL { ?c esf:receiptReturnCapable ?ready . FILTER(?ready = true) } }
