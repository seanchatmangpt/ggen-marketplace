PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs ?opps WHERE { ?obs a esf:ProductionFunctionObservation ; esf:downstreamOpportunityCount ?opps . FILTER(?opps > 0) } ORDER BY DESC(?opps) LIMIT 25
