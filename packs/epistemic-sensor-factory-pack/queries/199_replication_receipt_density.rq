PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?receipt) AS ?receipts) (COUNT(DISTINCT ?sensor) AS ?sensors) WHERE { ?sensor a esf:ManufacturedSensor . OPTIONAL { ?sensor esf:receipt ?receipt } }
