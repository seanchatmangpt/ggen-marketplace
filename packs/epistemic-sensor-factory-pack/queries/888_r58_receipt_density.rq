PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(DISTINCT ?r) AS ?receipts) (COUNT(DISTINCT ?s) AS ?subjects) WHERE { ?s a esf:ExactSubject . OPTIONAL { ?s esf:receipt ?r } }
