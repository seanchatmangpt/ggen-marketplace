PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?token (COUNT(?c) AS ?candidates) WHERE { ?c a esf:ConsumerAdmissionCandidate ; esf:producerTargetToken ?token . } GROUP BY ?token ORDER BY DESC(?candidates)
