PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?edge ?consumer ?root WHERE {
  ?edge a esf:PropagationEdge ; esf:sourceConsumer ?consumer ; esf:independentTargetRoot ?root .
}
ORDER BY ?consumer ?root ?edge
