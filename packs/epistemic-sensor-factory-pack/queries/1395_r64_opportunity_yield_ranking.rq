PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs ?opps WHERE { ?obs a esf:ProductionFunctionObservation ; esf:downstreamOpportunityCount ?opps . } ORDER BY DESC(?opps) LIMIT 25
