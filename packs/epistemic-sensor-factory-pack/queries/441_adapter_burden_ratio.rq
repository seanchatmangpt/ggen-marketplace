PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?needed)/COUNT(?c) AS ?adapter_burden_ratio) WHERE { ?c a esf:ConsumerAdmissionCandidate . OPTIONAL { ?c esf:adapterRequired ?needed . FILTER(?needed = true) } }
