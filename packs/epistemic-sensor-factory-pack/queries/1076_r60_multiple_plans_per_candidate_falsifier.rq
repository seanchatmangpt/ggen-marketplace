PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate (COUNT(?plan) AS ?count) WHERE { ?plan a esf:ConsumerRealizationPlan ; esf:realizedFor ?candidate . } GROUP BY ?candidate HAVING(COUNT(?plan) > 1)
