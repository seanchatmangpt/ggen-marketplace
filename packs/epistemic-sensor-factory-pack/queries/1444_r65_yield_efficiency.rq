PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs ((?opps + ?reuse) / ?delta AS ?yieldEfficiency) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:downstreamOpportunityCount ?opps ; esf:reusedCapabilityCount ?reuse ; esf:frontierDelta ?delta . FILTER(?delta > 0) }
