PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?obs) AS ?reuseBearing) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:reusedCapabilityCount ?count . FILTER(?count > 0) }
