PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?score WHERE { ?candidate a esf:EpistemicInvestmentCandidate ; esf:complementarityScore ?score . FILTER(?score >= 0.7) }
