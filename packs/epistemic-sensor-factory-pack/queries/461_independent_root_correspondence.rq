PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?r ?root ?repo WHERE { ?r a esf:QualifiedConsumerReceipt ; esf:independentEvidenceRoot ?root ; esf:observedInRepo ?repo . FILTER(STR(?root) = STR(?repo)) }
