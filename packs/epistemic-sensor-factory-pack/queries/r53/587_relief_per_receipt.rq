PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?receipt (COUNT(?edge) AS ?reliefCount) WHERE { ?edge a esf:PropagationEdge ; esf:sourceReceipt ?receipt ; esf:reliefRealized true . } GROUP BY ?receipt ORDER BY DESC(?reliefCount)
