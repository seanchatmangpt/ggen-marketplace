PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?consumer (COUNT(DISTINCT ?opportunity) AS ?opportunityCount) WHERE { ?edge a esf:PropagationEdge ; esf:sourceConsumer ?consumer ; esf:causedOpportunity ?opportunity . } GROUP BY ?consumer ORDER BY DESC(?opportunityCount)
