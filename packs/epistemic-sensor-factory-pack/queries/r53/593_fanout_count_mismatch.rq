PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?consumer ?declared (COUNT(?edge) AS ?observed) WHERE { ?edge a esf:PropagationEdge ; esf:sourceConsumer ?consumer ; esf:fanoutCount ?declared . } GROUP BY ?consumer ?declared HAVING(COUNT(?edge) != ?declared)
