PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?edge ?repo WHERE { ?edge a esf:PropagationEdge ; esf:causedOpportunity ?opportunity ; esf:sourceConsumer ?consumer . ?consumer esf:observedInRepo ?sourceRepo . ?opportunity esf:targetRepo ?repo . FILTER(?sourceRepo != ?repo) }
