PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?opportunity WHERE { ?opportunity a esf:DownstreamOpportunity . FILTER NOT EXISTS { ?edge a esf:PropagationEdge ; esf:causedOpportunity ?opportunity . } }
