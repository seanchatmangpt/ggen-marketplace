PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?receipt (COUNT(DISTINCT ?opportunity) AS ?opportunityCount) WHERE { ?edge a esf:PropagationEdge ; esf:sourceReceipt ?receipt ; esf:causedOpportunity ?opportunity . } GROUP BY ?receipt ORDER BY DESC(?opportunityCount)
