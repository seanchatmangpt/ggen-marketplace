PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?consumer (COUNT(?edge) AS ?edgeCount) WHERE { ?edge a esf:PropagationEdge ; esf:sourceConsumer ?consumer . } GROUP BY ?consumer ORDER BY DESC(?edgeCount)
