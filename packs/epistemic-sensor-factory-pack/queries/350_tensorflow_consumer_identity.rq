PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target ?repository WHERE { ?target a esf:ReplicationTarget ; esf:repository ?repository . FILTER(CONTAINS(LCASE(STR(?repository)), "tensorflow")) }
