PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target (COUNT(DISTINCT ?opportunity) AS ?opportunities) WHERE {
  ?opportunity a esf:DownstreamOpportunity ; esf:targetRepo ?target .
}
GROUP BY ?target
ORDER BY DESC(?opportunities) ?target
