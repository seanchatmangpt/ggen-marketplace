PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate (?yield * ?synergy AS ?score) WHERE { ?candidate a esf:EpistemicInvestmentCandidate ; esf:realizedYield ?yield ; esf:complementarityScore ?synergy . }
