PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(DISTINCT ?target) AS ?crossRepoCount) WHERE { ?target a esf:ReplicationTarget ; esf:targetRepo ?repo . FILTER(?repo != <https://github.com/seanchatmangpt/ggen-marketplace>) }
