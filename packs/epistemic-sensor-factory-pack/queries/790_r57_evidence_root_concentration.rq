PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?root (COUNT(?s) AS ?uses) WHERE { ?s esf:independentEvidenceRoot ?root . } GROUP BY ?root HAVING(COUNT(?s) > 1) ORDER BY DESC(?uses)
