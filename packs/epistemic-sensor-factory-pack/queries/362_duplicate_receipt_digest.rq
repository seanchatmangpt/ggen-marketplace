PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?digest (COUNT(?r) AS ?count) WHERE { ?r a esf:ConsumerReceiptAssertion ; esf:receiptDigest ?digest . } GROUP BY ?digest HAVING(COUNT(?r) > 1)
