PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?expected ?realized WHERE { ?candidate a esf:EpistemicInvestmentCandidate ; esf:expectedInformationGain ?expected ; esf:realizedInformationGain ?realized . FILTER(?expected > ?realized + 0.2) }
