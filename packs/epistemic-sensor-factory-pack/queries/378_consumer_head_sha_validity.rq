PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?consumer ?head WHERE { ?consumer a esf:ExactSubject ; esf:consumerHead ?head . FILTER(!REGEX(STR(?head), "^[0-9a-f]{40}$")) }
