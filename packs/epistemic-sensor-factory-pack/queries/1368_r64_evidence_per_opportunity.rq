PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs (?evidence / ?opps AS ?evidencePerOpportunity) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:factorEvidenceCount ?evidence ; esf:downstreamOpportunityCount ?opps . FILTER(?opps > 0) }
