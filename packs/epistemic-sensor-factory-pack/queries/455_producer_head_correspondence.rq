PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?r ?producer WHERE { ?r a esf:QualifiedConsumerReceipt ; esf:producerCourtHead ?producer . FILTER(STRLEN(STR(?producer)) != 40) }
