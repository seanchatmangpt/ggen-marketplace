PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?return WHERE {
  ?return a esf:EvidenceReturn .
  FILTER NOT EXISTS { ?return esf:sourceReceipt ?receipt }
}
ORDER BY ?return
