PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?replayPairs) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:replayCapable true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:replayCapable true .
  FILTER(STR(?a) < STR(?b))
}
