PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?digest (COUNT(?r) AS ?uses) WHERE { ?r a esf:ConsumerEvidenceReturn ; esf:receiptDigestR66 ?digest . } GROUP BY ?digest HAVING(COUNT(?r) > 1)
