PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?ready)/COUNT(?c) AS ?dependency_closure_ratio) WHERE { ?c a esf:ConsumerAdmissionCandidate . OPTIONAL { ?c esf:dependencyClosed ?ready . FILTER(?ready = true) } }
