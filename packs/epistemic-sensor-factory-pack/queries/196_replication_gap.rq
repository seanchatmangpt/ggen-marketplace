PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?subject WHERE { ?subject a esf:ExactSubject . FILTER NOT EXISTS { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?subject } }
