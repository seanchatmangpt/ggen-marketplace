PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?manufacturableTriples) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:ggenManufacturable true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:ggenManufacturable true .
  ?c a esf:ConsumerAdmissionCandidate ; esf:ggenManufacturable true .
  FILTER(STR(?a) < STR(?b) && STR(?b) < STR(?c))
}
