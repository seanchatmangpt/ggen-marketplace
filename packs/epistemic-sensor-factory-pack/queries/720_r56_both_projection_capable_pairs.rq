PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?projectionPairs) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:generatedProjectionCapable true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:generatedProjectionCapable true .
  FILTER(STR(?a) < STR(?b))
}
