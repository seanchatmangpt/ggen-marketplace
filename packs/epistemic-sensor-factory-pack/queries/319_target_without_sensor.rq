PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target WHERE { ?target a esf:ReplicationTarget . FILTER NOT EXISTS { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?target } }
