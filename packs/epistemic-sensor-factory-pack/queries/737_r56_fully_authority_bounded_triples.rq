PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?boundedTriples) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:authorityBounded true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:authorityBounded true .
  ?c a esf:ConsumerAdmissionCandidate ; esf:authorityBounded true .
  FILTER(STR(?a) < STR(?b) && STR(?b) < STR(?c))
}
