PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?novel ?fp WHERE { ?candidate a esf:EpistemicInvestmentCandidate ; esf:novelOpportunityCount ?novel ; esf:falsePositiveCount ?fp . FILTER(?novel > ?fp) }
