PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?provider (COUNT(?root) AS ?n) WHERE { ?r a esf:ConsumerExecutionReceipt ; esf:evidenceRoot ?root . ?root esf:evidenceRootProvider ?provider . } GROUP BY ?provider ORDER BY DESC(?n)
