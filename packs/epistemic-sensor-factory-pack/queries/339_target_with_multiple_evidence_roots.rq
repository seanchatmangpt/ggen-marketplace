PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target (COUNT(DISTINCT ?root) AS ?rootCount) WHERE { ?target a esf:ReplicationTarget ; esf:independentEvidenceRoot ?root . } GROUP BY ?target HAVING(COUNT(DISTINCT ?root) > 1)
