PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?receipt ?source WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:returnSource ?source . FILTER(!CONTAINS(STR(?source), "/")) }
