PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?command (COUNT(?plan) AS ?count) WHERE { ?plan a esf:ConsumerRealizationPlan ; esf:qualificationCommand ?command . } GROUP BY ?command ORDER BY DESC(?count) ?command
