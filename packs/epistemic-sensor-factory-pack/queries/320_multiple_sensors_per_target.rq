PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target (COUNT(DISTINCT ?sensor) AS ?sensorCount) WHERE { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?target . ?target a esf:ReplicationTarget . } GROUP BY ?target HAVING(COUNT(DISTINCT ?sensor) > 1)
