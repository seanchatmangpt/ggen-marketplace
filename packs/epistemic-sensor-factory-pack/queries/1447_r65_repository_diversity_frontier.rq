PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?repo (COUNT(?obs) AS ?observations) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:observedInRepo ?repo . } GROUP BY ?repo ORDER BY ASC(?observations)
