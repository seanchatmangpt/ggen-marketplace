PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?path (COUNT(?plan) AS ?count) WHERE { ?plan a esf:ConsumerRealizationPlan ; esf:replayPath ?path . } GROUP BY ?path ORDER BY DESC(?count) ?path
