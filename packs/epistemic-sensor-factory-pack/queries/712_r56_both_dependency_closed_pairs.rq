PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?dependencyClosedPairs) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:dependencyClosed true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:dependencyClosed true .
  FILTER(STR(?a) < STR(?b))
}
