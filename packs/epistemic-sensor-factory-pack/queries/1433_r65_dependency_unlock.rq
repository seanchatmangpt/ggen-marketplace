PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?count WHERE { ?candidate a esf:EpistemicInvestmentCandidate ; esf:dependencyUnlockCount ?count . FILTER(?count > 0) }
