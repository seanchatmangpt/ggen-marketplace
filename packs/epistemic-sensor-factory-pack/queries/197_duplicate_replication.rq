PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?subject (COUNT(?sensor) AS ?count) WHERE { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?subject ; esf:derivedFromSpec ?spec . } GROUP BY ?subject ?spec HAVING(COUNT(?sensor)>1)
