PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?digest (COUNT(?e) AS ?count) WHERE { ?e a esf:DependencyReliefEvidence ; esf:receiptDigest ?digest } GROUP BY ?digest HAVING(COUNT(?e) > 1)
