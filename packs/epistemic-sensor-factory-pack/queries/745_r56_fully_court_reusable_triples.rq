PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?reusableCourtTriples) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:courtReusable true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:courtReusable true .
  ?c a esf:ConsumerAdmissionCandidate ; esf:courtReusable true .
  FILTER(STR(?a) < STR(?b) && STR(?b) < STR(?c))
}
