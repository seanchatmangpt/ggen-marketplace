PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?sensor (COUNT(DISTINCT ?repo) AS ?repoCount) WHERE { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?repo . } GROUP BY ?sensor HAVING(COUNT(DISTINCT ?repo) > 1)
