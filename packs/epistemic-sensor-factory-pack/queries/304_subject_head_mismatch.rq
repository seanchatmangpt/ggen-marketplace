PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target ?subject ?head WHERE { ?target a esf:ReplicationTarget ; esf:subjectSha ?subject ; esf:defaultHead ?head . FILTER(?subject != ?head) }
