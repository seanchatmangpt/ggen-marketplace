PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?consumer ?observed ?current WHERE {
  ?consumer a esf:ConsumerAdmissionCandidate ; esf:consumerHead ?observed ; esf:currentDefaultHead ?current .
  FILTER(?observed != ?current)
}
ORDER BY ?consumer
