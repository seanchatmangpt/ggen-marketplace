PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?score WHERE { ?candidate a esf:EpistemicInvestmentCandidate ; esf:interfaceCompatibility ?score . FILTER(?score < 1.0) }
