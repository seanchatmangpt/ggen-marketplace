PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?target WHERE {
  ?candidate a esf:ConsumerAdmissionCandidate ; esf:admittedTarget ?target .
}
ORDER BY ?candidate ?target
