PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?verifiedTriples) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:identityVerified true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:identityVerified true .
  ?c a esf:ConsumerAdmissionCandidate ; esf:identityVerified true .
  FILTER(STR(?a) < STR(?b) && STR(?b) < STR(?c))
}
