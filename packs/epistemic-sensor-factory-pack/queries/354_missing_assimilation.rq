PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
PREFIX prov: <http://www.w3.org/ns/prov#>
SELECT ?receipt WHERE { ?receipt a esf:ConsumerReceiptAssertion . FILTER NOT EXISTS { ?a a esf:ProducerAssimilation ; prov:used ?receipt . } }
