PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?pairCount) WHERE {
  ?a a esf:ConsumerAdmissionCandidate .
  ?b a esf:ConsumerAdmissionCandidate .
  FILTER(STR(?a) < STR(?b))
}
