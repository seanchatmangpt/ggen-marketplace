PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?adapterRequiredPairs) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:adapterRequired true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:adapterRequired true .
  FILTER(STR(?a) < STR(?b))
}
