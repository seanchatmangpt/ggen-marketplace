PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?run ?reads ?reuse WHERE { ?run a esf:DfCMRun ; esf:memoryReadCount ?reads ; esf:memoryReuseCount ?reuse . } ORDER BY DESC(?reuse)