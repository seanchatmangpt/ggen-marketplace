PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?repo (COUNT(DISTINCT ?target) AS ?count) WHERE { ?target a esf:ReplicationTarget ; esf:targetRepo ?repo . } GROUP BY ?repo HAVING(COUNT(DISTINCT ?target) > 1)
