PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs (?opps / ?delta AS ?opportunityPerDelta) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:downstreamOpportunityCount ?opps ; esf:frontierDelta ?delta . FILTER(?delta > 0) }
