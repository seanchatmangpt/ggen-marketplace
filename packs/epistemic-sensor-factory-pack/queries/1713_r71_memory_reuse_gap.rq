PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?run ?reads ?reuse WHERE { ?run a esf:DfCMRun ; esf:memoryReadCount ?reads ; esf:memoryReuseCount ?reuse . FILTER(?reads > 0 && ?reuse = 0) }