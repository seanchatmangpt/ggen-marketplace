PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?consumer ?head WHERE { ?consumer a esf:ExactSubject ; esf:consumerHead ?head . FILTER NOT EXISTS { ?receipt a esf:ConsumerReceiptAssertion ; esf:consumerHead ?head . } }
