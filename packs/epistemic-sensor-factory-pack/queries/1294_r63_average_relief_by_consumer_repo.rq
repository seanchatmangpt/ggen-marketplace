PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?repo (AVG(?delta) AS ?averageRelief) WHERE { ?e a esf:DependencyReliefEvidence ; esf:consumerRepository ?repo ; esf:reliefDelta ?delta } GROUP BY ?repo ORDER BY DESC(?averageRelief)
