PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?nonActuatingPairs) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:noAmbientDo true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:noAmbientDo true .
  FILTER(STR(?a) < STR(?b))
}
