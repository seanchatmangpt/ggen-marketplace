PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?edge ?confidence WHERE {
  ?edge a esf:PropagationEdge ; esf:causalConfidence ?confidence .
  FILTER(?confidence < 0 || ?confidence > 1)
}
ORDER BY ?edge
