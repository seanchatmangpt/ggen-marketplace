PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(DISTINCT ?target) AS ?gapCount) WHERE { ?target a esf:ReplicationTarget . FILTER NOT EXISTS { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?target ; esf:standing "ALIVE" } }
