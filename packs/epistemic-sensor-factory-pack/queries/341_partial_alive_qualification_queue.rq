PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target ?repo ?sensor WHERE { ?target a esf:ReplicationTarget ; esf:targetRepo ?repo . ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?target ; esf:standing "PARTIAL_ALIVE" . } ORDER BY ?repo
