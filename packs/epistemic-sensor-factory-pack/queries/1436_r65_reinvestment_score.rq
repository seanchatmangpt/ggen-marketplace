PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?score WHERE { ?candidate a esf:EpistemicInvestmentCandidate ; esf:reinvestmentScore ?score . } ORDER BY DESC(?score)
