PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?consumer WHERE {
  ?consumer a esf:ConsumerAdmissionCandidate .
  FILTER NOT EXISTS { ?edge a esf:PropagationEdge ; esf:sourceConsumer ?consumer ; esf:causedOpportunity ?opportunity }
}
ORDER BY ?consumer
