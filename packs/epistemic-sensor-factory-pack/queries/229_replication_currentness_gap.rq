PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?sensor ?subject WHERE { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?subject . FILTER NOT EXISTS { ?subject esf:subjectSha ?sha } }
