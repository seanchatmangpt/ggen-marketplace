PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate WHERE { ?candidate a esf:ConsumerAdmissionCandidate . FILTER NOT EXISTS { ?plan a esf:ConsumerRealizationPlan ; esf:realizedFor ?candidate . } }
