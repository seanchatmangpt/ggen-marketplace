PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?root (COUNT(DISTINCT ?target) AS ?count) WHERE { ?target a esf:ReplicationTarget ; esf:independentEvidenceRoot ?root . } GROUP BY ?root HAVING(COUNT(DISTINCT ?target) > 1)
