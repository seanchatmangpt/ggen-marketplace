PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?s ?consumer ?current WHERE { ?s a esf:ExactSubject ; esf:consumerHead ?consumer ; esf:currentDefaultHead ?current . FILTER(?consumer != ?current) }
