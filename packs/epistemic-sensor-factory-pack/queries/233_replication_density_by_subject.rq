PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?subject (COUNT(DISTINCT ?sensor) AS ?sensorCount) WHERE { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?subject . } GROUP BY ?subject ORDER BY DESC(?sensorCount)
