PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?receipt WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:consumerHead ?head . FILTER NOT EXISTS { ?target a esf:ReplicationTarget ; esf:consumerHead ?head . } }
