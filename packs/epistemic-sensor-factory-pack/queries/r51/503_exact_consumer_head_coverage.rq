PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?target) AS ?targets) (COUNT(?head) AS ?heads) WHERE { ?target a esf:ReplicationTarget . OPTIONAL { ?target esf:consumerHead ?head . } }
