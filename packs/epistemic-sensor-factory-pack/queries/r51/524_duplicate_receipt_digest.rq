PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?digest (COUNT(?receipt) AS ?count) WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:receiptDigest ?digest . } GROUP BY ?digest HAVING(COUNT(?receipt) > 1)
