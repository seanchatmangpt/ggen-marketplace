PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?standing (COUNT(?receipt) AS ?count) WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:producerStandingValue ?standing . } GROUP BY ?standing ORDER BY DESC(?count)
