PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?receipt WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:consumerStandingValue ?standing . FILTER(UCASE(STR(?standing)) = "BUILD_BROKEN") }
