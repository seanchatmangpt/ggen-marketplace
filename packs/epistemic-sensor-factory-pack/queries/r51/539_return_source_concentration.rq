PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?source (COUNT(?return) AS ?returns) WHERE { ?return a esf:EvidenceReturn ; esf:returnSource ?source . } GROUP BY ?source ORDER BY DESC(?returns)
