PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?head (COUNT(?receipt) AS ?receipts) WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:consumerHead ?head . } GROUP BY ?head ORDER BY DESC(?receipts)
