PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target WHERE { ?target a esf:ReplicationTarget ; esf:consumerHead ?head . FILTER NOT EXISTS { ?receipt a esf:ConsumerReceiptAssertion ; esf:consumerHead ?head . } }
