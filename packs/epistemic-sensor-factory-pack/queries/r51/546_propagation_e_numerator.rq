PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(DISTINCT ?target) AS ?actionable) WHERE { ?target a esf:ReplicationTarget ; esf:eligible true . FILTER NOT EXISTS { ?receipt a esf:ConsumerReceiptAssertion ; esf:consumerHead ?head . ?target esf:consumerHead ?head . } }
