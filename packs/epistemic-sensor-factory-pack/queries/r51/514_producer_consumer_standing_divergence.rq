PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?receipt ?consumer ?producer WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:consumerStandingValue ?consumer ; esf:producerStandingValue ?producer . FILTER(STR(?consumer) != STR(?producer)) }
