PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?receipt ?roots WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:evidenceRootCount ?roots . FILTER(xsd:integer(?roots) >= 2) }
