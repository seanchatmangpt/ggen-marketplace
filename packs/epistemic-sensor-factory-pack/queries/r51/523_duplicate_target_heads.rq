PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?repo ?head (COUNT(?target) AS ?count) WHERE { ?target a esf:ReplicationTarget ; esf:targetRepo ?repo ; esf:consumerHead ?head . } GROUP BY ?repo ?head HAVING(COUNT(?target) > 1)
