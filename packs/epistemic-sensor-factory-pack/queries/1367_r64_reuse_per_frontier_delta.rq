PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs (?reuse / ?delta AS ?reusePerDelta) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:reusedCapabilityCount ?reuse ; esf:frontierDelta ?delta . FILTER(?delta > 0) }
