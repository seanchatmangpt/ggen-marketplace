PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target ?repo ?head ?root WHERE { ?target a esf:ReplicationTarget ; esf:targetRepo ?repo ; esf:defaultHead ?head ; esf:eligible true ; esf:independentEvidenceRoot ?root . } ORDER BY ?repo
