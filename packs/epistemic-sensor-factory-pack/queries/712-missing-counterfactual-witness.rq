PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?edge WHERE {
  ?edge a esf:PropagationEdge .
  FILTER NOT EXISTS { ?edge esf:counterfactualWitness ?witness }
}
ORDER BY ?edge
