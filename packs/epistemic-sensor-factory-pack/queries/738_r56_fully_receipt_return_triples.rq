PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?receiptTriples) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:receiptReturnCapable true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:receiptReturnCapable true .
  ?c a esf:ConsumerAdmissionCandidate ; esf:receiptReturnCapable true .
  FILTER(STR(?a) < STR(?b) && STR(?b) < STR(?c))
}
