PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate WHERE { ?candidate a esf:CapitalCandidate ; esf:requiresPublicOntology true . FILTER NOT EXISTS { ?candidate esf:canonicalPrimitive ?primitive } }
