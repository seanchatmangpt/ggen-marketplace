PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?generator WHERE { ?candidate a esf:CapitalCandidate ; esf:generatorPlan ?generator . FILTER NOT EXISTS { ?candidate esf:consumerPopulation ?population } }
