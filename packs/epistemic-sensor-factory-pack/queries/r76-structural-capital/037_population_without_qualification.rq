PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?population WHERE { ?candidate a esf:CapitalCandidate ; esf:consumerPopulation ?population . FILTER NOT EXISTS { ?candidate esf:qualificationPlan ?qualification } }
