PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?primitive WHERE { ?candidate a esf:CapitalCandidate ; esf:canonicalPrimitive ?primitive . FILTER NOT EXISTS { ?candidate esf:packPlan ?pack } }
