PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?candidate ?unlocks ?records WHERE { ?candidate a esf:CapitalCandidate ; esf:memoryUnlockCount ?unlocks ; esf:memoryConsumedCount ?records . FILTER(xsd:decimal(?records) > 0) }
