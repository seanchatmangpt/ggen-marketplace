PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?pack WHERE { ?candidate a esf:CapitalCandidate ; esf:packPlan ?pack . FILTER NOT EXISTS { ?candidate esf:generatorPlan ?generator } }
