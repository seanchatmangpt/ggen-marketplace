PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?ontologyTriples) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:publicOntologyAligned true .
  ?b a esf:ConsumerAdmissionCandidate ; esf:publicOntologyAligned true .
  ?c a esf:ConsumerAdmissionCandidate ; esf:publicOntologyAligned true .
  FILTER(STR(?a) < STR(?b) && STR(?b) < STR(?c))
}
