PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?digest (COUNT(DISTINCT ?e) AS ?subjects) WHERE { ?e a esf:DependencyReliefEvidence ; esf:receiptDigest ?digest ; esf:actionableOpportunities ?n . FILTER(?n > 0) } GROUP BY ?digest HAVING(COUNT(DISTINCT ?e) > 1)
