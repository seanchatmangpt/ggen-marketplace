PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
PREFIX prov: <http://www.w3.org/ns/prov#>
SELECT ?r (COUNT(?c) AS ?fanout) WHERE { ?a a esf:ProducerAssimilation ; prov:used ?r ; esf:nextWave ?wave . ?c a esf:WaveCandidate ; esf:admittedTarget ?wave . } GROUP BY ?r
