PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?s ?repo ?freshness WHERE { ?s esf:consumerRepositoryR66 ?repo ; esf:standingFreshness ?freshness . FILTER(?freshness < 1.0) }
