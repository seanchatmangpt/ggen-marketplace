PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target ?head WHERE { ?target a esf:ReplicationTarget ; esf:defaultHead ?head . FILTER(!REGEX(STR(?head), "^[0-9a-f]{40}$")) }
