PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?consumer (COUNT(DISTINCT ?head) AS ?heads) WHERE { ?consumer a esf:ExactSubject ; esf:consumerHead ?head . } GROUP BY ?consumer
