PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate ?count WHERE { ?candidate a esf:EpistemicInvestmentCandidate ; esf:evidenceRootCountR65 ?count . FILTER(?count < 2) }
