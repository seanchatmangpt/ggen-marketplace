PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?receipt ?digest WHERE { ?receipt a esf:ConsumerReceiptAssertion ; esf:receiptDigest ?digest . FILTER(!STRSTARTS(STR(?digest), "sha256:")) }
