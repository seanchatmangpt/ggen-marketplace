PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?edge ?consumer WHERE {
  ?edge a esf:PropagationEdge ; esf:sourceConsumer ?consumer ; esf:observedAfterReceipt true .
}
ORDER BY ?consumer ?edge
