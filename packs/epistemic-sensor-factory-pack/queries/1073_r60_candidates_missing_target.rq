PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?candidate WHERE { ?candidate a esf:ConsumerAdmissionCandidate . FILTER NOT EXISTS { ?candidate esf:admittedTarget ?target } }
