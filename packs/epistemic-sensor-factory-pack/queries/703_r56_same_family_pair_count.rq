PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?sameFamilyPairs) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:consumerFamily ?family .
  ?b a esf:ConsumerAdmissionCandidate ; esf:consumerFamily ?family .
  FILTER(STR(?a) < STR(?b))
}
