PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs (?product * ?delta AS ?priority) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:multiplicativeProduct ?product ; esf:frontierDelta ?delta . FILTER(?product > 1 && ?delta > 0) } ORDER BY DESC(?priority)
