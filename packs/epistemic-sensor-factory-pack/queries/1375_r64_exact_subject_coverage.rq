PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(?obs) AS ?exactSubjectBound) WHERE { ?obs a esf:ProductionFunctionObservation ; esf:subjectSha ?sha . FILTER(STRLEN(STR(?sha)) = 40) }
