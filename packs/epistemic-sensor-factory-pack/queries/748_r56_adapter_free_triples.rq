PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT (COUNT(*) AS ?adapterFreeTriples) WHERE {
  ?a a esf:ConsumerAdmissionCandidate ; esf:adapterRequired false .
  ?b a esf:ConsumerAdmissionCandidate ; esf:adapterRequired false .
  ?c a esf:ConsumerAdmissionCandidate ; esf:adapterRequired false .
  FILTER(STR(?a) < STR(?b) && STR(?b) < STR(?c))
}
