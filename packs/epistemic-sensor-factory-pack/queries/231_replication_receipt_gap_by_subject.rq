PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?subject WHERE { ?sensor a esf:ManufacturedSensor ; esf:observedInRepo ?subject . FILTER NOT EXISTS { ?sensor esf:receipt ?receipt } }
