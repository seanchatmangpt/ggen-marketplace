PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?obs ?reuse WHERE { ?obs a esf:ProductionFunctionObservation ; esf:reusedCapabilityCount ?reuse . FILTER(?reuse > 0) } ORDER BY DESC(?reuse) LIMIT 25
