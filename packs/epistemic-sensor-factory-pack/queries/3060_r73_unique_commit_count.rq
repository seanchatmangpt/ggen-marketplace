PREFIX r73:<https://ggen.dev/measure/r73#>
SELECT ?run (COUNT(DISTINCT ?sha) AS ?uniqueCommits) WHERE { ?run a r73:ScheduleRun ; r73:observedCommit ?commit . ?commit r73:commitSha ?sha . } GROUP BY ?run
