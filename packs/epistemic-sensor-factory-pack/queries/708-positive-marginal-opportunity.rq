PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?edge ?count WHERE {
  ?edge a esf:PropagationEdge ; esf:marginalOpportunityCount ?count .
  FILTER(?count > 0)
}
ORDER BY DESC(?count) ?edge
