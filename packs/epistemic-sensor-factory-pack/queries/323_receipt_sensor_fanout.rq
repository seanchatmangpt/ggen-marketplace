PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?receipt (COUNT(DISTINCT ?sensor) AS ?sensorCount) WHERE { ?sensor a esf:ManufacturedSensor ; esf:receipt ?receipt . } GROUP BY ?receipt HAVING(COUNT(DISTINCT ?sensor) > 1)
