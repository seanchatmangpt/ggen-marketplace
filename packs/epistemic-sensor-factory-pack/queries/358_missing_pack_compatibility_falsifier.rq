PREFIX esf: <https://ggen.dev/ontology/epistemic-sensor-factory#>
SELECT ?target WHERE { ?target a esf:ReplicationTarget ; esf:eligible true . FILTER NOT EXISTS { ?target esf:compatiblePack ?pack } }
