{
  "schema": "ggen.epistemic-replication-plan/1",
  "authority": "SELECT|CONSTRUCT|VERIFY",
  "consequential_do": false,
  "targets": [
  {% for row in results %}{"repo":"{{ row.repo }}","head":"{{ row.head }}","evidence_root":"{{ row.root }}"}{% if not loop.last %},{% endif %}{% endfor %}
  ]
}
