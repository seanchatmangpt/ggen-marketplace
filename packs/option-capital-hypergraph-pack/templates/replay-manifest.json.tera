{
  "schema": "ggen.option-capital.replay.v1",
  "authority": "VERIFY",
  "actuationPerformed": false,
  "orderedCandidates": [
  {%- for row in results %}
    {"composition":"{{ row.composition }}","candidate":"{{ row.candidate }}","repository":"{{ row.repository }}"}{% if not loop.last %},{% endif %}
  {%- endfor %}
  ]
}
