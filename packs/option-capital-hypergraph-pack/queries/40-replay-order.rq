PREFIX oc: <https://ggen.dev/ontology/option-capital#>
SELECT ?composition ?candidate ?repository
WHERE {
  ?candidate a oc:Candidate ;
    oc:composition ?composition ;
    oc:repository ?repository ;
    oc:executable true .
}
ORDER BY ?composition ?candidate ?repository
