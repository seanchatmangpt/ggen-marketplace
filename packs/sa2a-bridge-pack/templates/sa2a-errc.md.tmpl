---
to: "docs/claude/diataxis/reference/generated-sa2a-bridge-errc.md"
skip_empty: false
sparql:
  results: |
    PREFIX s2b: <https://ggen.dev/ontology/sa2a-bridge#>
    SELECT ?category ?priority ?decision ?rationale ?expectedLeverage WHERE {
      ?d a s2b:ERRCDecision ; s2b:category ?category ; s2b:priority ?priority ; s2b:decision ?decision ;
        s2b:rationale ?rationale ; s2b:expectedLeverage ?expectedLeverage .
    } ORDER BY ?category ?priority
---
# Generated autofde-lab SA2A ↔ XaaS ERRC

> Generated from `ggen-marketplace/sa2a-bridge-pack`; do not hand-edit.

| ERRC | Priority | Decision | Why | Leverage |
|---|---:|---|---|---:|
{% for row in results %}| {{ row["category"] }} | {{ row["priority"] }} | {{ row["decision"] }} | {{ row["rationale"] }} | {{ row["expectedLeverage"] }} |
{% endfor %}

The bridge composes `xaas-castle-bridge-pack`'s edge-chain / proof-obligation /
SHA-pinning shape against autofde-lab's real stdio JSON-lines BEAM port
bridge (`src/autofde_lab/beam/beam_port_bridge.py`) instead of a Rust BRCE
binary. Public semantic projection is descriptive and never grants DO
authority; only the `sa2a_execute` edge is consequential.
