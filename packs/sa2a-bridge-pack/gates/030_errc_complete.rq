PREFIX s2b: <https://ggen.dev/ontology/sa2a-bridge#>
SELECT ?required WHERE {
  VALUES ?required { "ELIMINATE" "REDUCE" "RAISE" "CREATE" }
  FILTER NOT EXISTS { ?d a s2b:ERRCDecision ; s2b:category ?required . }
}
