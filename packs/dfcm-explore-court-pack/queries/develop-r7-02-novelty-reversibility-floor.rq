PREFIX ex: <https://ggen.dev/ontology/dfcm-explore#>
SELECT ?candidate ?novelty ?reversibility (IF(?novelty < ?reversibility, ?novelty, ?reversibility) AS ?floor) WHERE {
  ?candidate a ex:Candidate ; ex:ctqNovelty ?novelty ; ex:ctqReversibility ?reversibility .
} ORDER BY DESC(?floor) ?candidate
