PREFIX ex: <https://ggen.dev/ontology/dfcm-explore#>
SELECT ?candidate ?novelty ?evidence ?rollback WHERE {
  ?candidate a ex:AlgorithmicCandidate ; ex:ctqNovelty ?novelty ; ex:ctqEvidence ?evidence ; ex:rollbackCost ?rollback .
} ORDER BY ?rollback DESC(?evidence) DESC(?novelty) ?candidate
