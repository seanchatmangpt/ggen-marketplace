PREFIX ex: <https://ggen.dev/ontology/dfcm-explore#>
SELECT ?candidate ?novelty ?evidence ?reversibility WHERE {
  ?candidate a ex:SemanticCandidate ; ex:ctqNovelty ?novelty ; ex:ctqEvidence ?evidence ; ex:ctqReversibility ?reversibility .
} ORDER BY DESC(?evidence) DESC(?novelty) ?candidate
