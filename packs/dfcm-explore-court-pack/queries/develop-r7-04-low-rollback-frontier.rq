PREFIX ex: <https://ggen.dev/ontology/dfcm-explore#>
SELECT ?candidate ?rollback ?evidence WHERE {
  ?candidate a ex:Candidate ; ex:rollbackCost ?rollback ; ex:ctqEvidence ?evidence .
  FILTER(?rollback <= 1 && ?evidence >= 8)
} ORDER BY DESC(?evidence) ?candidate
