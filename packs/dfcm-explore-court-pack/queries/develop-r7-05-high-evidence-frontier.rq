PREFIX ex: <https://ggen.dev/ontology/dfcm-explore#>
SELECT ?candidate ?evidence ?novelty ?rollback WHERE {
  ?candidate a ex:Candidate ; ex:ctqEvidence ?evidence ; ex:ctqNovelty ?novelty ; ex:rollbackCost ?rollback .
  FILTER(?evidence >= 9)
} ORDER BY ?rollback DESC(?novelty) ?candidate
