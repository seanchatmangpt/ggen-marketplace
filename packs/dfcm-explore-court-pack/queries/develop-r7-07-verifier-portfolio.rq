PREFIX ex: <https://ggen.dev/ontology/dfcm-explore#>
SELECT ?candidate ?evidence ?reversibility ?rollback WHERE {
  ?candidate a ex:VerifierCandidate ; ex:ctqEvidence ?evidence ; ex:ctqReversibility ?reversibility ; ex:rollbackCost ?rollback .
} ORDER BY DESC(?evidence) DESC(?reversibility) ?rollback ?candidate
