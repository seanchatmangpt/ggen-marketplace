PREFIX clp: <https://ggen.dev/ontology/capability-lineage-propagation#>
SELECT ?plan ?qualified ?observed WHERE {
  ?plan a clp:PropagationPlan ;
        clp:qualifiedTargetHead ?qualified ;
        clp:observedTargetHead ?observed .
  FILTER(STR(?qualified) != STR(?observed))
}
