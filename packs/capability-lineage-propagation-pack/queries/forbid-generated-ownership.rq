PREFIX clp: <https://ggen.dev/ontology/capability-lineage-propagation#>
SELECT ?plan WHERE {
  ?plan a clp:PropagationPlan ; clp:generatedOwnership ?owner .
  FILTER(LCASE(STR(?owner)) != "projection-only")
}
