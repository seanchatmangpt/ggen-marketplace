PREFIX clp: <https://ggen.dev/ontology/capability-lineage-propagation#>
SELECT ?edge ?from ?to WHERE {
  ?edge a clp:LineageEdge ; clp:fromPr ?from ; clp:toPr ?to ; clp:semanticDelta 0 ; clp:preservesCapability true .
}
ORDER BY ?from ?to
