PREFIX clp: <https://ggen.dev/ontology/capability-lineage-propagation#>
SELECT ?plan WHERE {
  ?plan a clp:PropagationPlan .
  FILTER NOT EXISTS {
    ?plan clp:qualificationWitness ?witness .
    FILTER(STRLEN(STR(?witness)) > 0)
  }
}
