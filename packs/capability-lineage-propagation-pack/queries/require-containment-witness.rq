PREFIX clp: <https://ggen.dev/ontology/capability-lineage-propagation#>
SELECT ?plan WHERE {
  ?plan a clp:PropagationPlan .
  FILTER NOT EXISTS {
    ?plan clp:containmentWitness ?witness .
    ?witness a clp:ContainmentWitness .
  }
}
