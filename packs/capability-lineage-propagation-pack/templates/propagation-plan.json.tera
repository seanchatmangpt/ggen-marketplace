{
  "schema": "ggen.capability-lineage-propagation/1",
  "authority": "CONSTRUCT",
  "actuation_performed": false,
  "force_push_allowed": false,
  "plans": [
  {% for row in results %}{"plan":"{{ row.plan }}","capability":"{{ row.cap }}","edges":{{ row.edges }}}{% if not loop.last %},{% endif %}{% endfor %}
  ]
}
