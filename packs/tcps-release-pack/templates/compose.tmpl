---
to: infra/compose.yaml
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:ComposeArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
