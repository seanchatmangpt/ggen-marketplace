---
to: infra/ci/security.yml
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:SecurityWorkflow tcps:workflowBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
