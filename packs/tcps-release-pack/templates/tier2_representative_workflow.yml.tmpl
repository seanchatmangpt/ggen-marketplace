---
to: infra/ci/tier2-representative.yml
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:Tier2RepresentativeWorkflow tcps:workflowBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
