---
to: infra/evidence/source/DIST_SHA256SUMS
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:DistSha256SumsArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
