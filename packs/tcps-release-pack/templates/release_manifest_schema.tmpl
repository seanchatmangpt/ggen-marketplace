---
to: infra/schemas/release-manifest.schema.json
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:ReleaseManifestSchemaArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
