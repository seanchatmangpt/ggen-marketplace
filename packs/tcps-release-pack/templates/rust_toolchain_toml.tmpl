---
to: infra/rust-toolchain.toml
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:RustToolchainTomlArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
