---
to: infra/schemas/build-receipt.schema.json
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:BuildReceiptSchemaArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
