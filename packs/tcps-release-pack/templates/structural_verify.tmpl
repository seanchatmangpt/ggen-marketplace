---
to: infra/evidence/source/structural-verify.json
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:StructuralVerifyArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
