---
to: infra/scripts/lifecycle.sh
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:LifecycleShScript tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}