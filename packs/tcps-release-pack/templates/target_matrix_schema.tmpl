---
to: infra/schemas/target-matrix.schema.json
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:TargetMatrixSchemaArtifact tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
