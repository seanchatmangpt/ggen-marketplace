---
to: infra/scripts/platform/uefi.sh
sparql:
  row: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-release#>
    SELECT ?body WHERE {
      tcps:UefiPlatformShScript tcps:artifactBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}