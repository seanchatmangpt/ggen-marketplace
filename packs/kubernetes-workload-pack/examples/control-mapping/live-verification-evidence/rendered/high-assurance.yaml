# GENERATED by ggen (kubernetes-workload-pack/templates/deployment_and_service.yaml.tmpl). Do not edit.
# Source of truth: this consumer's own admitted k8s:Workload/k8s:Container facts.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger-reconciler
  namespace: regulated-workloads
  labels:
    app: ledger-reconciler
spec:
  replicas: 3
  selector:
    matchLabels:
      app: ledger-reconciler
  template:
    metadata:
      labels:
        app: ledger-reconciler
    spec:
      serviceAccountName: ledger-reconciler
      automountServiceAccountToken: false
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        runAsGroup: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: ledger-reconciler
          image: registry.internal.example.com/ledger-reconciler@sha256:d1a6e2c4f9b3a7e0c8f2b4d6a1e3c5f7091b2d4e6f8a0c2e4b6d8f0a2c4e6f8a0
          imagePullPolicy: Always
          securityContext:
            allowPrivilegeEscalation: false
            privileged: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]

          ports:
            - containerPort: 8443
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
              ephemeral-storage: 1Gi

            limits:
              cpu: 1
              memory: 512Mi
              ephemeral-storage: 2Gi

          envFrom:
            - configMapRef:
                name: ledger-reconciler-config
            - secretRef:
                name: ledger-reconciler-secrets
          readinessProbe:
            httpGet:
              path: /healthz/ready
              port: 8443
              scheme: HTTPS
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 3

          livenessProbe:
            httpGet:
              path: /healthz/live
              port: 8443
              scheme: HTTPS
            initialDelaySeconds: 15
            periodSeconds: 20
            failureThreshold: 3

---
apiVersion: v1
kind: Service
metadata:
  name: ledger-reconciler
  namespace: regulated-workloads
spec:
  selector:
    app: ledger-reconciler
  ports:
    - port: 443
      targetPort: 8443
  type: ClusterIP
