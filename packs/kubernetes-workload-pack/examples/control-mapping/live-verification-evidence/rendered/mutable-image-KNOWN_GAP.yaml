# GENERATED by ggen (kubernetes-workload-pack/templates/deployment_and_service.yaml.tmpl). Do not edit.
# Source of truth: this consumer's own admitted k8s:Workload/k8s:Container facts.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mutable-image-demo
  namespace: default
  labels:
    app: mutable-image-demo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mutable-image-demo
  template:
    metadata:
      labels:
        app: mutable-image-demo
    spec:
      securityContext:
        runAsNonRoot: true

      containers:
        - name: mutable-image-demo
          image: example/app:latest
          resources:
            requests:
              cpu: 100m
              memory: 128Mi

            limits:
              cpu: 250m
              memory: 256Mi

