# GENERATED by ggen (kubernetes-workload-pack/templates/deployment_and_service.yaml.tmpl). Do not edit.
# Source of truth: this consumer's own admitted k8s:Workload/k8s:Container facts.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: net-diag-sidecar
  namespace: default
  labels:
    app: net-diag-sidecar
spec:
  replicas: 1
  selector:
    matchLabels:
      app: net-diag-sidecar
  template:
    metadata:
      labels:
        app: net-diag-sidecar
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault

      containers:
        - name: net-diag
          image: example/net-diag@sha256:1111111111111111111111111111111111111111111111111111111111111a
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
              add: ["NET_RAW"]

          resources:
            requests:
              cpu: 25m
              memory: 32Mi

            limits:
              cpu: 100m
              memory: 64Mi

