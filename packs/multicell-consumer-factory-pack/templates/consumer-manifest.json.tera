{
  "schema": "ggen.multicell-consumer/1",
  "authority": "SELECT|CONSTRUCT|VERIFY",
  "actuation_performed": false,
  "qualification": "independent",
  "cells": [
  {% for row in results %}{"plan":"{{ row.plan }}","count":{{ row.count }}}{% if not loop.last %},{% endif %}{% endfor %}
  ]
}
