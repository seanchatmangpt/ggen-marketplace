PREFIX mcf: <https://ggen.dev/ontology/multicell-consumer#>
SELECT ?plan ?count WHERE {
  ?plan a mcf:CompositionPlan ; mcf:selectedCellCount ?count ; mcf:authority "SELECT" ; mcf:actuationPerformed false ; mcf:preservedUnselectedOptions true .
  FILTER(?count >= 2)
}
ORDER BY DESC(?count)
