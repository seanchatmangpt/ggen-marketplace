PREFIX mcf: <https://ggen.dev/ontology/multicell-consumer#>
SELECT ?plan (COUNT(DISTINCT ?cell) AS ?cells) WHERE {
  ?plan a mcf:CompositionPlan ; mcf:cell ?cell ; mcf:actuationPerformed false .
  ?cell a mcf:ConsumerCell .
}
GROUP BY ?plan
HAVING(COUNT(DISTINCT ?cell) >= 2)
