PREFIX mcf: <https://ggen.dev/ontology/multicell-consumer#>
ASK {
  mcf:factory mcf:authority "SELECT|CONSTRUCT|VERIFY" ; mcf:actuationPerformed false .
  FILTER NOT EXISTS { ?s mcf:authority "DO" }
  FILTER NOT EXISTS { ?s mcf:actuationPerformed true }
}
