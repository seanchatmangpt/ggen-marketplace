[pack]
name = "shadcn-dark-theme-tokens-pack"
version = "0.1.0"
description = "Standalone, reusable shadcn CSS-variable dark theme: globals.css tokens, class=\"dark\" default on <html>, and a next-themes ThemeToggle. Extracted as its own pack rather than the inline per-app theme code found in every existing shadcn pack."
