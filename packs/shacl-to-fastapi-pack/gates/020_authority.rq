# MESSAGE: a consequential FastAPI projection may never declare authority unnecessary.
PREFIX sfa: <https://ggen.dev/packs/shacl-to-fastapi#>
SELECT ?projection WHERE {
  ?projection a sfa:FastAPIProjection ;
              sfa:consequential true ;
              sfa:requiresAuthority false .
}
ORDER BY ?projection
