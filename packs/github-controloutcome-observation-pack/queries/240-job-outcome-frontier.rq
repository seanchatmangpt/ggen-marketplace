PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?name ?conclusion (COUNT(?job) AS ?count) WHERE {
  ?job a gco:GitHubJobObservation ; gco:name ?name ; gco:conclusion ?conclusion ; gco:current true .
}
GROUP BY ?name ?conclusion
ORDER BY ?name ?conclusion
