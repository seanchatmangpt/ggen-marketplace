PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?root (COUNT(?job) AS ?jobs) WHERE {
  ?job a gco:GitHubJobObservation ; gco:evidenceRoot ?root ; gco:current true .
}
GROUP BY ?root
ORDER BY DESC(?jobs) ?root
