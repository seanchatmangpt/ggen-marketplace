PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ((1 + SUM(IF(?c = "success", 1, 0))) AS ?alpha) ((1 + SUM(IF(?c != "success", 1, 0))) AS ?beta) WHERE {
  ?job a gco:GitHubJobObservation ; gco:conclusion ?c ; gco:current true .
}
