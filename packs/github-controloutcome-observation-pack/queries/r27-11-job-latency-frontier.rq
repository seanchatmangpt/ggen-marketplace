PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?job ?name ?duration WHERE { ?job a gco:GitHubJobObservation ; gco:name ?name ; gco:durationMs ?duration ; gco:current true . } ORDER BY DESC(?duration) ?name ?job
