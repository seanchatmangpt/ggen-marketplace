PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?conclusion (COUNT(?job) AS ?count) WHERE { ?job a gco:GitHubJobObservation ; gco:conclusion ?conclusion . } GROUP BY ?conclusion ORDER BY ?conclusion
