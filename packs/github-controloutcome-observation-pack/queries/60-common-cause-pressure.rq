PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?run (COUNT(?job) AS ?jobs) (COUNT(DISTINCT ?root) AS ?roots) WHERE {
  ?job a gco:GitHubJobObservation ; gco:run ?run ; gco:evidenceRoot ?root ; gco:current true .
} GROUP BY ?run ORDER BY DESC(?jobs) ?run
