PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?job ?duration ?conclusion WHERE {
  ?job a gco:GitHubJobObservation ; gco:durationMs ?duration ; gco:conclusion ?conclusion ; gco:current true .
} ORDER BY ?duration ?job
