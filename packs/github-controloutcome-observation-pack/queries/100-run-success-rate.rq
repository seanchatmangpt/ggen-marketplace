PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT (COUNT(?run) AS ?runs) (SUM(IF(?conclusion = "success", 1, 0)) AS ?successes) WHERE {
  ?run a gco:GitHubRunObservation ; gco:conclusion ?conclusion ; gco:current true .
}
ORDER BY ?runs
