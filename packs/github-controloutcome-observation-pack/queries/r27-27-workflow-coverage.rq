PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT (COUNT(DISTINCT ?workflow) AS ?workflowKinds) WHERE { ?run a gco:GitHubRunObservation ; gco:workflow ?workflow ; gco:current true . }
