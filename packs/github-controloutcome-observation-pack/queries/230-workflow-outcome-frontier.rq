PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?workflow ?conclusion (COUNT(?run) AS ?count) WHERE {
  ?run a gco:GitHubRunObservation ; gco:workflow ?workflow ; gco:conclusion ?conclusion ; gco:current true .
}
GROUP BY ?workflow ?conclusion
ORDER BY ?workflow ?conclusion
