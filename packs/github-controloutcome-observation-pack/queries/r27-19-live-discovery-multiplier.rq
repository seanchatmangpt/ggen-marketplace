PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?bundle ?seeds ?opportunities (?opportunities / ?seeds AS ?E) WHERE { ?bundle a gco:ObservationBundle ; gco:seedObservations ?seeds ; gco:actionableOpportunities ?opportunities ; gco:current true . FILTER(?seeds > 0) } ORDER BY DESC(?E) ?bundle
