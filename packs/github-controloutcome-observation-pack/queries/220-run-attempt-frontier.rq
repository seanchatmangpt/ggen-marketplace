PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?run ?runId ?attempt WHERE {
  ?run a gco:GitHubRunObservation ; gco:runId ?runId ; gco:runAttempt ?attempt ; gco:current true .
}
ORDER BY ?runId DESC(?attempt) ?run
