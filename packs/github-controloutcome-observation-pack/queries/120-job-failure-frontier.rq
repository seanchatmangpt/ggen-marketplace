PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?job ?name ?conclusion ?root WHERE {
  ?job a gco:GitHubJobObservation ; gco:name ?name ; gco:conclusion ?conclusion ; gco:evidenceRoot ?root ; gco:current true .
  FILTER(?conclusion != "success")
}
ORDER BY ?name ?job
