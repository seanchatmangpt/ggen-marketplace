PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?run ?head WHERE {
  ?run a prov:Activity ; dcterms:type "workflow-run" ; dcterms:status "SUCCESS" ; prov:used ?head ; dcterms:relation "OWNING_COURT" .
}
ORDER BY ?head ?run
