PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?root (COUNT(?job) AS ?jobs) WHERE { ?job a gco:GitHubJobObservation ; gco:evidenceRoot ?root ; gco:current true . } GROUP BY ?root HAVING(COUNT(?job) > 1) ORDER BY DESC(?jobs) ?root
