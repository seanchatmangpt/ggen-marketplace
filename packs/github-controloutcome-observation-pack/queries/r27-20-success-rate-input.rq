PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT (SUM(IF(?conclusion = "success",1,0)) AS ?successes) (COUNT(?run) AS ?support) WHERE { ?run a gco:GitHubRunObservation ; gco:conclusion ?conclusion ; gco:current true . }
