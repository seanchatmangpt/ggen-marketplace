PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?pr ?head ?base WHERE {
  ?pr a gco:GitHubPullRequestObservation ; gco:headSha ?head ; gco:baseSha ?base ; gco:current true .
  FILTER(?head != ?base)
}
ORDER BY ?pr ?head ?base
