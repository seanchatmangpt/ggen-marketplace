PREFIX gco: <https://ggen.dev/ontology/github-controloutcome#>
SELECT ?name (COUNT(?job) AS ?failures) WHERE { ?job a gco:GitHubJobObservation ; gco:name ?name ; gco:conclusion "failure" . } GROUP BY ?name ORDER BY DESC(?failures) ?name
