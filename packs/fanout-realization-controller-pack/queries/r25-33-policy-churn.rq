PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?subject (COUNT(DISTINCT ?digest) AS ?policyDigests) WHERE { ?d a frc:ControlDecision ; frc:exactSubject ?subject ; frc:policyDigest ?digest . } GROUP BY ?subject HAVING(COUNT(DISTINCT ?digest) > 1)
