PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?r ?relief ?realized WHERE {
  ?r a frc:FanoutRealization ; frc:dependencyRelief ?relief ; frc:realizedQualifiedActions ?realized .
}
ORDER BY DESC(?relief) DESC(?realized) ?r
