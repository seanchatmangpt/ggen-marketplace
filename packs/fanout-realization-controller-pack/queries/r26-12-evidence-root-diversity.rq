PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?decision (COUNT(DISTINCT ?root) AS ?independentRoots) WHERE {
  ?outcome a frc:ControlOutcome ; frc:decision ?decision ; frc:evidenceRoot ?root ; frc:current true .
} GROUP BY ?decision ORDER BY ?decision
