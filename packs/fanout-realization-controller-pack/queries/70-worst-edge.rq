PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?r ?edge ?realized ?relief WHERE {
  ?r a frc:FanoutRealization ; frc:edge ?edge ; frc:realizedQualifiedActions ?realized ; frc:dependencyRelief ?relief .
}
ORDER BY ?realized ?relief ?r
LIMIT 1
