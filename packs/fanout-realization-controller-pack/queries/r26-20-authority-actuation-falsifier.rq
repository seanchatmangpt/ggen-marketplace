PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?entity ?authority ?actuation WHERE {
  ?entity frc:authority ?authority ; frc:actuationPerformed ?actuation .
  FILTER(?actuation = true || ?authority = "DO")
} ORDER BY ?entity
