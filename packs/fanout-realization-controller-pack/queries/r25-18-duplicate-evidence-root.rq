PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?root (COUNT(?o) AS ?uses) WHERE { ?o a frc:ControlOutcome ; frc:evidenceRoot ?root . } GROUP BY ?root HAVING(COUNT(?o) > 1)
