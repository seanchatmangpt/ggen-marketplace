PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT (COUNT(?outcome) AS ?outcomes) (SUM(IF(STRLEN(STR(?receipt)) = 64, 1, 0)) AS ?receipted) WHERE {
  ?outcome a frc:ControlOutcome ; frc:selected true ; frc:receiptDigest ?receipt ; frc:current true .
}
