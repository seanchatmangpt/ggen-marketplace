PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?root (COUNT(DISTINCT ?outcome) AS ?uses) WHERE {
  ?outcome a frc:ControlOutcome ; frc:evidenceRoot ?root ; frc:current true .
} GROUP BY ?root HAVING(COUNT(DISTINCT ?outcome) > 1) ORDER BY DESC(?uses) ?root
