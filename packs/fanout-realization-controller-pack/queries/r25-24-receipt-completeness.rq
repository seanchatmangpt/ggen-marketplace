PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?x WHERE { ?x a ?type . FILTER(?type IN (frc:ControlDecision,frc:ControlOutcome)) FILTER NOT EXISTS { ?x frc:receiptDigest ?receipt . FILTER(regex(str(?receipt),"^[0-9a-f]{64}$")) } }
