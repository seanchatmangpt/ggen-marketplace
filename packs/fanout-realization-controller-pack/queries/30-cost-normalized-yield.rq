PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?r ?realized ?cost (?realized / ?cost AS ?costYield) WHERE {
  ?r a frc:FanoutRealization ; frc:realizedQualifiedActions ?realized ; frc:costUnits ?cost .
  FILTER(?cost > 0)
}
ORDER BY DESC(?costYield) ?r
