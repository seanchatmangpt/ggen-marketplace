PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
SELECT ?outcome ?decision ?subject WHERE {
  ?outcome a frc:ControlOutcome ; frc:decision ?decision ; frc:exactSubject ?subject ; frc:current true .
  FILTER NOT EXISTS { ?decision a frc:ControlDecision . }
} ORDER BY ?subject ?outcome
