# @generated by ggen — source is marketplace ontology/query/template.
FRONTIER = [
{% for row in results %}    {"realization": "{{ row.r }}", "edge": "{{ row.edge }}", "realized_actions": {{ row.realized }}, "investment": {{ row.investment }}, "dependency_relief": {{ row.relief }}, "realized_y": {{ row.realizedY }}},{% endfor %}
]
assert FRONTIER, "REFUSED[EMPTY_REALIZATION_CONTROL_FRONTIER]"
assert all(item["realized_actions"] > 0 for item in FRONTIER)
assert all(item["investment"] > 0 for item in FRONTIER)
assert all(item["dependency_relief"] > 0 for item in FRONTIER)
assert all(item["realized_y"] > 0 for item in FRONTIER)
