PREFIX frc: <https://ggen.dev/ontology/fanout-realization-control#>
ASK WHERE {
  FILTER NOT EXISTS {
    ?r a frc:FanoutRealization .
    FILTER NOT EXISTS { ?r frc:current true ; frc:exactSubject ?subject ; frc:receiptDigest ?receipt . FILTER(STRLEN(STR(?receipt)) = 64) }
  }
}
