PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle ?admitted ?terminal ?spillover WHERE {
  ?cycle a tps:Cycle ; tps:admittedCount ?admitted ; tps:terminalCount ?terminal ; tps:spilloverCount ?spillover .
  FILTER(?spillover != (?admitted - ?terminal))
}
