# Cell5 R103: qualification evidence must identify the validator used for the exact subject.
ASK WHERE {
  ?receipt <https://ggen.dev/tps#validatorIdentity> ?validator .
  FILTER(STRLEN(STR(?validator)) > 0)
}
