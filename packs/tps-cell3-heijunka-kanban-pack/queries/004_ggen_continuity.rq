# Current-cycle READY requires an exact upstream GGEN primitive identity.
ASK WHERE {
  ?card <https://ggen.dev/tps#upstreamPrimitive> ?primitive .
  ?primitive <https://ggen.dev/tps#exactHead> ?sha .
  FILTER(STRLEN(STR(?sha)) >= 40)
}