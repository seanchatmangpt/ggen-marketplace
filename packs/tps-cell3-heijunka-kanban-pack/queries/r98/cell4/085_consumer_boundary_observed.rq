PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card tps:consumerBoundary ?boundary ; tps:consumerObservation ?observation . FILTER(STRLEN(STR(?boundary)) > 0 && STRLEN(STR(?observation)) > 0) }