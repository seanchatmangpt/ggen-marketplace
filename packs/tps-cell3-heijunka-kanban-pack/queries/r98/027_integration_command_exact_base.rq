PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card a tps:KanbanCard ; tps:base ?base ; tps:integration ?i . ?i tps:command ?command ; tps:targetBase ?base . FILTER(STRLEN(?command) > 0) }
