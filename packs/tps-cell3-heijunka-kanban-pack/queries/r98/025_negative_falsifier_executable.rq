PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card a tps:KanbanCard ; tps:negativeFalsifier ?f . ?f a tps:Falsifier ; tps:command ?command ; tps:failureMeans ?meaning . FILTER(STRLEN(?command) > 0 && STRLEN(?meaning) > 0) }
