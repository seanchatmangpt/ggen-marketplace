PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
ASK { ?card a tps:KanbanCard ; tps:qualificationCost ?cost ; tps:remainingTakt ?takt . FILTER(xsd:decimal(?cost) >= 0 && xsd:decimal(?cost) <= xsd:decimal(?takt)) }
