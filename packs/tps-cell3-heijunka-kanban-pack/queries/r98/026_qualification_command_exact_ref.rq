PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card a tps:KanbanCard ; tps:head ?head ; tps:qualification ?q . ?q tps:command ?command ; tps:subjectRef ?head . FILTER(STRLEN(?command) > 0) }
