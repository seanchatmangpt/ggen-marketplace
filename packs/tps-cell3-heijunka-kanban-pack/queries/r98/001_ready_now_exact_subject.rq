PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card a tps:KanbanCard ; tps:readiness tps:READY_NOW ; tps:base ?base ; tps:currentBase ?base ; tps:head ?head . FILTER(STRLEN(STR(?head)) > 0) }
