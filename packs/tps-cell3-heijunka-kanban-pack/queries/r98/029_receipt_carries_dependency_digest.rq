PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?receipt a tps:SelectionReceipt ; tps:dependencyDigest ?digest ; tps:card ?card . ?card tps:dependencyClosure ?closure . ?closure tps:digest ?digest . FILTER(STRLEN(?digest) > 0) }
