PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?receipt a tps:SelectionReceipt ; tps:repo ?repo ; tps:base ?base ; tps:head ?head ; tps:card ?card . ?card a tps:KanbanCard . FILTER(STRLEN(?repo) > 0 && STRLEN(?base) > 0 && STRLEN(?head) > 0) }
