PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card a tps:KanbanCard ; tps:readiness tps:REFUSE_NOW ; tps:refusal ?refusal . ?refusal a tps:TypedRefusal ; tps:type ?type ; tps:subject ?subject . FILTER(STRLEN(STR(?type)) > 0 && STRLEN(STR(?subject)) > 0) }
