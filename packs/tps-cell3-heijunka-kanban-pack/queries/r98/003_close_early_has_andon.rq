PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card a tps:KanbanCard ; tps:readiness tps:CLOSE_EARLY ; tps:andon ?andon . ?andon a tps:Andon ; tps:type ?type ; tps:disposition ?disposition . FILTER(STRLEN(STR(?type)) > 0 && STRLEN(STR(?disposition)) > 0) }
