PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?cycle a tps:Cycle ; tps:currentBase ?base ; tps:activeCard ?card . ?card tps:base ?base ; tps:head ?head ; tps:qualificationWitness ?q ; tps:expectedTerminalState ?state ; tps:replayReceipt ?receipt . }
