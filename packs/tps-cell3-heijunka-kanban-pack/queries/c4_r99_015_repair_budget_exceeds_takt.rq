PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?cycle WHERE { ?cycle a tps:Cycle ; tps:repairBudgetMinutes ?r ; tps:remainingTaktMinutes ?t . FILTER(xsd:integer(?r) > xsd:integer(?t)) }