PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:readiness "READY_NOW" ; tps:starvationAgeMinutes ?age ; tps:maxStarvationMinutes ?max . FILTER(?age > ?max) } }
