# R102 Cell5 court: terminal receipt must carry an observed timestamp no later than the cycle due timestamp.
ASK WHERE { ?cycle <https://ggen.dev/tps#cycleId> "R102" ; <https://ggen.dev/tps#dueTimestamp> ?due ; <https://ggen.dev/tps#terminalTimestamp> ?done . FILTER(?done <= ?due) }
