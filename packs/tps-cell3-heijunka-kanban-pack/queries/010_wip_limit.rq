# Active cards must remain at or below the declared Cell4/5 closable WIP limit.
ASK WHERE { ?cycle <https://ggen.dev/tps#activeWip> ?w ; <https://ggen.dev/tps#wipLimit> ?limit . FILTER(?w <= ?limit) }