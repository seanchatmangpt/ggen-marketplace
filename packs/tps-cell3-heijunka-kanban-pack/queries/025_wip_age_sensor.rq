# Aging active WIP gets Jidoka attention before new work is added to the same constrained path.
SELECT ?card ?age WHERE { ?card <https://ggen.dev/tps#active> true ; <https://ggen.dev/tps#wipAgeSeconds> ?age . } ORDER BY DESC(?age)