# READY requires explicit current-cycle authority for the bounded workstream.
ASK WHERE {
  ?card <https://ggen.dev/tps#authority> ?authority .
  FILTER(?authority != <https://ggen.dev/tps#NoAuthority>)
}