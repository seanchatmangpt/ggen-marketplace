# Cell5 R103: observed execution evidence must bind the environment capsule identity.
ASK WHERE {
  ?receipt <https://ggen.dev/tps#environmentIdentity> ?environment .
  FILTER(STRLEN(STR(?environment)) > 0)
}
