# Cell5 R103: a repeated Andon must carry a permanent upstream-return action.
ASK WHERE {
  ?andon <https://ggen.dev/tps#repeatedCause> true ;
         <https://ggen.dev/tps#upstreamReturn> ?return .
  FILTER(STRLEN(STR(?return)) > 0)
}
