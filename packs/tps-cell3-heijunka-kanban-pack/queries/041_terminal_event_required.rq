# Every admitted card must have exactly one terminal event before cycle close.
SELECT ?card (COUNT(?event) AS ?n) WHERE { ?event <https://ggen.dev/tps#terminalFor> ?card . } GROUP BY ?card HAVING(?n = 1)