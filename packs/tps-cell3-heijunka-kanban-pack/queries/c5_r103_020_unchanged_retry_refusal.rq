# Cell5 R103: a failed subject may be retried only after the hypothesis or subject changes.
ASK WHERE {
  ?retry <https://ggen.dev/tps#priorHead> ?prior ;
         <https://ggen.dev/tps#head> ?current ;
         <https://ggen.dev/tps#newHypothesis> true .
  FILTER(?prior = ?current)
}
