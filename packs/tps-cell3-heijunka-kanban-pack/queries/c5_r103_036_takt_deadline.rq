# Cell5 R103: terminalization must be observed no later than the cycle takt deadline.
ASK WHERE {
  ?terminal <https://ggen.dev/tps#terminalTimestamp> ?at ;
            <https://ggen.dev/tps#taktDeadline> ?deadline .
  FILTER(?at <= ?deadline)
}
