PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:readiness "CLOSE_EARLY" . FILTER NOT EXISTS { ?card tps:reason ?reason . FILTER(STRLEN(?reason) > 0) } } }
