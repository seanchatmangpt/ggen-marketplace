PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card (COUNT(?witness) AS ?witnesses) WHERE {
  ?card a tps:KanbanCard ; tps:qualificationWitness ?witness .
}
GROUP BY ?card
HAVING(COUNT(?witness) != 1)
