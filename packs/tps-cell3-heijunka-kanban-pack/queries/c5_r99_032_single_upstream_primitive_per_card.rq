PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card (COUNT(?primitive) AS ?primitives) WHERE {
  ?card a tps:KanbanCard ; tps:upstreamPrimitive ?primitive .
}
GROUP BY ?card
HAVING(COUNT(?primitive) != 1)
