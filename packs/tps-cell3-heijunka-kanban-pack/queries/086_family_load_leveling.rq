PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?cycle a tps:Cycle ; tps:maxFamilyWip ?max . FILTER NOT EXISTS { ?family a tps:ConsumerFamily ; tps:activeWip ?wip . FILTER(?wip > ?max) } }
