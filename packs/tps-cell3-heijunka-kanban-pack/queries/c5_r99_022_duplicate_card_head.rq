PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?left ?right ?head WHERE { ?left a tps:KanbanCard ; tps:head ?head . ?right a tps:KanbanCard ; tps:head ?head . FILTER(STR(?left) < STR(?right)) }
