PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?cycle a tps:Cycle ; tps:cycleId ?id . FILTER NOT EXISTS { ?cycle tps:activeCard ?card . ?card tps:cycleId ?other . FILTER(?other != ?id) } }
