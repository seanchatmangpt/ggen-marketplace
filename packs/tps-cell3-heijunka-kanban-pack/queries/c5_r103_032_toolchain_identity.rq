# Cell5 R103: execution receipts must identify the toolchain capsule used for qualification.
ASK WHERE {
  ?receipt <https://ggen.dev/tps#toolchainIdentity> ?toolchain .
  FILTER(STRLEN(STR(?toolchain)) > 0)
}
