# R102 Cell5 court: terminal evidence must bind an exact immutable consumer head.
ASK WHERE { ?card <https://ggen.dev/tps#cycleId> "R102" ; <https://ggen.dev/tps#consumerHead> ?sha . FILTER(strlen(str(?sha)) = 40) }
