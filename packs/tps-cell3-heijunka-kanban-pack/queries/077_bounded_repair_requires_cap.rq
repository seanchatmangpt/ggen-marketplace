PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:readiness "READY_AFTER_BOUNDED_REPAIR" . FILTER NOT EXISTS { ?card tps:repairBudgetMinutes ?m . FILTER(?m > 0) } } }
