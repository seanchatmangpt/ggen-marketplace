# Repairs without a bounded estimate are removed from same-hour WIP and typed TAKT_NOT_CLOSABLE.
ASK WHERE { ?card <https://ggen.dev/tps#repairRequired> true . FILTER NOT EXISTS { ?card <https://ggen.dev/tps#repairSeconds> ?seconds } }