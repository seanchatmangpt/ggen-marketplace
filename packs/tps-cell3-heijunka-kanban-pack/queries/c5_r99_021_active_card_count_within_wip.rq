PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle (COUNT(?card) AS ?cards) ?wip WHERE {
  ?cycle a tps:Cycle ; tps:activeCard ?card ; tps:wipCount ?wip .
}
GROUP BY ?cycle ?wip
HAVING(COUNT(?card) != ?wip)
