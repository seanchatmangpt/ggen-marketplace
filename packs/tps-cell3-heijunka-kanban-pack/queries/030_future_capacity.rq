# Favor current-cycle work that increases future independently closable manufacturing capacity.
SELECT ?card ?capacity WHERE { ?card <https://ggen.dev/tps#eligible> true ; <https://ggen.dev/tps#futureCapacityGain> ?capacity . } ORDER BY DESC(?capacity)