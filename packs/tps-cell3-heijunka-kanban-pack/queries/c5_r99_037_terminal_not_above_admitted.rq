PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle ?admitted ?terminal WHERE {
  ?cycle a tps:Cycle ; tps:admittedCount ?admitted ; tps:terminalCount ?terminal .
  FILTER(?terminal > ?admitted)
}
