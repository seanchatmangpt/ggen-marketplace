# Prefer distributing active cards across distinct repositories when independent alternatives exist.
SELECT ?repo (COUNT(?card) AS ?load) WHERE { ?card <https://ggen.dev/tps#repository> ?repo ; <https://ggen.dev/tps#active> true . } GROUP BY ?repo ORDER BY ?load