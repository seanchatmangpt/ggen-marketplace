PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:readiness "REFUSE_NOW" . FILTER NOT EXISTS { ?card tps:typedRefusal ?r . ?r tps:code ?code . FILTER(STRLEN(?code)>0) } } }
