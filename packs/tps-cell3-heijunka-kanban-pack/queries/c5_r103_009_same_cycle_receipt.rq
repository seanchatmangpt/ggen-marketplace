# Cell5 R103: terminal evidence and its receipt must share a cycle identity.
ASK WHERE {
  ?terminal <https://ggen.dev/tps#cycleId> ?cycle ;
            <https://ggen.dev/tps#receipt> ?receipt .
  ?receipt <https://ggen.dev/tps#cycleId> ?cycle .
}
