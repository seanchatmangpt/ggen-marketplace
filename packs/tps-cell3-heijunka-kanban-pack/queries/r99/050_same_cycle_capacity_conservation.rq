PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?cycle a tps:Cycle ; tps:admittedCount ?admitted ; tps:terminalCount ?terminal ; tps:spilloverCount 0 ; tps:wipCount ?wip ; tps:wipLimit ?limit ; tps:capacityConserved true . FILTER(?terminal <= ?admitted && ?wip <= ?limit) }
