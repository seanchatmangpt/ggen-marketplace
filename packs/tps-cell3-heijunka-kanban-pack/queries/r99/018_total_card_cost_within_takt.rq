PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card a tps:KanbanCard ; tps:repairBudgetMinutes ?repair ; tps:qualificationCostMinutes ?qual ; tps:integrationCostMinutes ?integrate ; tps:remainingTaktMinutes ?takt . FILTER((?repair + ?qual + ?integrate) <= ?takt) }
