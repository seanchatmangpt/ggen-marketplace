PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?replacement a tps:KanbanCard ; tps:replaces ?old ; tps:dependencyDigest ?newDigest . ?old tps:dependencyDigest ?oldDigest . FILTER(?newDigest != ?oldDigest || EXISTS { ?replacement tps:dependenciesRevalidated true }) }
