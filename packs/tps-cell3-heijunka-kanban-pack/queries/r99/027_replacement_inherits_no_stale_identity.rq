PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?replacement a tps:KanbanCard ; tps:replaces ?old ; tps:base ?newBase . ?old tps:base ?oldBase . FILTER(?newBase != ?oldBase || EXISTS { ?replacement tps:revalidatedBase ?newBase }) }
