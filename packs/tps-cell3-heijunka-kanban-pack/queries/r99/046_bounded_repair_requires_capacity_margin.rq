PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { ?card a tps:KanbanCard ; tps:disposition tps:READY_AFTER_BOUNDED_REPAIR ; tps:repairBudgetMinutes ?repair ; tps:capacityMarginMinutes ?margin . FILTER(?margin >= ?repair) }
