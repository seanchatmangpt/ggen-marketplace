# Starvation sensor: candidate age without recent allocation raises priority but does not bypass admission.
SELECT ?card ?age WHERE { ?card <https://ggen.dev/tps#candidate> true ; <https://ggen.dev/tps#starvationSeconds> ?age . FILTER(?age > 0) } ORDER BY DESC(?age)