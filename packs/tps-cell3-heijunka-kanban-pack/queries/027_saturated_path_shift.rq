# When one repo/CI path is saturated, expose independent alternatives instead of idling the line.
SELECT ?alternative WHERE { ?path <https://ggen.dev/tps#saturated> true . ?alternative <https://ggen.dev/tps#eligible> true ; <https://ggen.dev/tps#executionPath> ?other . FILTER(?other != ?path) }