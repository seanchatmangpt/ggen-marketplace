# TAKT_NOT_CLOSABLE and authority failures must become explicit typed refusal events.
ASK WHERE { ?event <https://ggen.dev/tps#eventType> "REFUSE" ; <https://ggen.dev/tps#reason> ?reason . FILTER(STRSTARTS(?reason,"REFUSED_TYPED")) }