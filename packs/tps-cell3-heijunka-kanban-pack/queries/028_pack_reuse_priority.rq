# Prefer cards that reuse an already-qualified pack before extending or creating new machinery.
SELECT ?card ?reuse WHERE { ?card <https://ggen.dev/tps#eligible> true ; <https://ggen.dev/tps#packReuseClass> ?reuse . } ORDER BY ?reuse