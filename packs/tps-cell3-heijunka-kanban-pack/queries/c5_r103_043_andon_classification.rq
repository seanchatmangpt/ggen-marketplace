# Cell5 R103: any end-of-line defect must be classified as a typed Andon, never generic BLOCKED.
ASK WHERE {
  ?andon <https://ggen.dev/tps#andonCode> ?code .
  FILTER(STRLEN(STR(?code)) > 0 && STR(?code) != "BLOCKED")
}
