# Cell5 R103: unresolved merge conflict may terminate only through a typed conflict refusal.
ASK WHERE {
  ?card <https://ggen.dev/tps#mergeConflict> true ;
        <https://ggen.dev/tps#terminalDisposition> "REFUSED_TYPED[MERGE_CONFLICT]" .
}
