# Cell5 R103: consequential repository mutation requires an explicit bounded authority receipt.
ASK WHERE {
  ?card <https://ggen.dev/tps#authorityReceipt> ?receipt .
  ?receipt <https://ggen.dev/tps#scope> ?scope .
  FILTER(STRLEN(STR(?scope)) > 0)
}
