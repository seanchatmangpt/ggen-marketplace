PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card ?base ?current WHERE { ?cycle a tps:Cycle ; tps:activeCard ?card . ?card tps:base ?base ; tps:currentBase ?current . FILTER(?base != ?current) }
