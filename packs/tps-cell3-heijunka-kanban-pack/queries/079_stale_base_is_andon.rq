PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:base ?base ; tps:currentBase ?current . FILTER(?base != ?current) FILTER NOT EXISTS { ?card tps:andonCode "STALE_BASE" } } }
