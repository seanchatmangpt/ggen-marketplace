PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card (COUNT(?authority) AS ?authorities) WHERE {
  ?card a tps:KanbanCard ; tps:authority ?authority .
}
GROUP BY ?card
HAVING(COUNT(?authority) != 1)
