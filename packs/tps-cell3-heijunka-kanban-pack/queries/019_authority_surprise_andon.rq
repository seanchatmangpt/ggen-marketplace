# Authority changed after admission is an Andon requiring re-admission or refusal.
ASK WHERE { ?card <https://ggen.dev/tps#admittedAuthority> ?a ; <https://ggen.dev/tps#currentAuthority> ?b . FILTER(?a != ?b) }