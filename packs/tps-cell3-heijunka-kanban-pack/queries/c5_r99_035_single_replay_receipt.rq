PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card (COUNT(?receipt) AS ?receipts) WHERE {
  ?card a tps:KanbanCard ; tps:replayReceipt ?receipt .
}
GROUP BY ?card
HAVING(COUNT(?receipt) != 1)
