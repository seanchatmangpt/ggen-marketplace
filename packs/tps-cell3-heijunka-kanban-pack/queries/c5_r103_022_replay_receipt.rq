# Cell5 R103: terminal evidence must include a replay receipt bound to the same exact head.
ASK WHERE {
  ?card <https://ggen.dev/tps#head> ?head ;
        <https://ggen.dev/tps#replayReceipt> ?receipt .
  ?receipt <https://ggen.dev/tps#subjectHead> ?head .
}
