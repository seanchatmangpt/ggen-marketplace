PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card ?admitted ?current WHERE { ?card a tps:ReadyCard ; tps:admittedBase ?admitted ; tps:currentBase ?current . FILTER(?admitted != ?current) }
