PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card WHERE { ?card a tps:ReadyCard ; tps:remainingTakt ?takt ; tps:closureCost ?cost . FILTER(?cost > ?takt) FILTER NOT EXISTS { ?card tps:readinessDisposition tps:CLOSE_EARLY } }
