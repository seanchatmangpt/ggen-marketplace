PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:qualificationCostMinutes ?cost ; tps:remainingTaktMinutes ?takt . FILTER(?cost > ?takt) } }
