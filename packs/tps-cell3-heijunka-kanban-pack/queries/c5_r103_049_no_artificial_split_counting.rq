# Cell5 R103: counted semantic commits must map one-to-one to independently falsifiable invariants.
ASK WHERE {
  ?receipt <https://ggen.dev/tps#countedSemanticCommit> ?commit ;
           <https://ggen.dev/tps#independentFalsifier> ?falsifier .
  FILTER(?commit != ?falsifier)
}
