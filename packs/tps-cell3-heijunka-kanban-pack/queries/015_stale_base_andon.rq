# Stale base is an Andon whenever the admitted base differs from current default head.
ASK WHERE { ?card <https://ggen.dev/tps#admittedBase> ?base ; <https://ggen.dev/tps#currentDefaultHead> ?current . FILTER(?base != ?current) }