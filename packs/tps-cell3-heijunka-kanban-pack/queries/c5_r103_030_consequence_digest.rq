# Cell5 R103: observed generated or runtime consequences must be digest-bound before closure.
ASK WHERE {
  ?receipt <https://ggen.dev/tps#consequenceDigest> ?digest .
  FILTER(STRLEN(STR(?digest)) > 0)
}
