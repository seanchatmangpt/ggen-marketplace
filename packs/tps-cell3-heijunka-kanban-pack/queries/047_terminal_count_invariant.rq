# Same-hour line closure requires every admitted card to terminate exactly once.
ASK WHERE { ?cycle <https://ggen.dev/tps#admittedCount> ?a ; <https://ggen.dev/tps#terminalCount> ?t ; <https://ggen.dev/tps#spillover> 0 . FILTER(?a = ?t) }