PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?cycle a tps:Cycle ; tps:wipCount ?count ; tps:wipLimit ?limit . FILTER NOT EXISTS { ?cycle tps:pullEvent ?e . ?e tps:beforeWip ?count ; tps:afterWip ?after . FILTER(?count >= ?limit && ?after > ?count) } }
