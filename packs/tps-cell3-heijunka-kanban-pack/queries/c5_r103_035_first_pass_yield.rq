# Cell5 R103: first-pass yield may be claimed only with explicit final-subject attempt accounting.
ASK WHERE {
  ?cycle <https://ggen.dev/tps#finalSubjectAttempts> 1 ;
         <https://ggen.dev/tps#finalSubjectQualified> true .
}
