# Cell5 R103: closure capital must identify the canonical GGEN primitive that manufactured it.
ASK WHERE {
  ?card <https://ggen.dev/tps#upstreamPrimitive> ?primitive .
  FILTER(STRLEN(STR(?primitive)) > 0)
}
