PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?cycle a tps:Cycle ; tps:maxRepositoryWip ?max . FILTER NOT EXISTS { ?repo a tps:Repository ; tps:activeWip ?wip . FILTER(?wip > ?max) } }
