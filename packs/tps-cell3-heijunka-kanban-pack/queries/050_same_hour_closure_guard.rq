# Cell3 must not hand downstream a card whose expected terminal time exceeds the cycle deadline.
ASK WHERE { ?card <https://ggen.dev/tps#expectedTerminalEpoch> ?terminal ; <https://ggen.dev/tps#cycleDeadlineEpoch> ?deadline . FILTER(?terminal <= ?deadline) }