# Cell5 R103: every admitted closure card must carry an observed qualification witness.
ASK WHERE {
  ?card <https://ggen.dev/tps#qualificationWitness> ?witness .
  FILTER(STRLEN(STR(?witness)) > 0)
}
