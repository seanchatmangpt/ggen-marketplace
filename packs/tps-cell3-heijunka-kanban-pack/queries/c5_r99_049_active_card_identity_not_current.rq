PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle ?card WHERE { ?cycle a tps:Cycle ; tps:activeCard ?card . ?card a tps:KanbanCard ; tps:base ?base ; tps:currentBase ?current ; tps:head ?head . FILTER(?base != ?current || ?base = ?head) }
