# R102 Cell5 court: closure evidence must bind the admitted exact base.
ASK WHERE { ?card <https://ggen.dev/tps#cycleId> "R102" ; <https://ggen.dev/tps#baseSha> ?sha . FILTER(strlen(str(?sha)) = 40) }
