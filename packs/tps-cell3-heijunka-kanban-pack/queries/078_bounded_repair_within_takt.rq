PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:readiness "READY_AFTER_BOUNDED_REPAIR" ; tps:repairBudgetMinutes ?repair ; tps:remainingTaktMinutes ?takt . FILTER(?repair > ?takt) } }
