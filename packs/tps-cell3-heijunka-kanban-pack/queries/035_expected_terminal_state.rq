# Each Kanban card declares its intended same-cycle terminal state.
ASK WHERE { ?card <https://ggen.dev/tps#expectedTerminalState> ?state . FILTER(?state IN ("ALIVE","MERGED","REFUSED_TYPED","BUILD_BROKEN")) }