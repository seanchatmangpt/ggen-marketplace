# Cell5 R103: terminal_count must equal admitted_count at line closure.
ASK WHERE {
  ?cycle <https://ggen.dev/tps#admittedCount> ?admitted ;
         <https://ggen.dev/tps#terminalCount> ?terminal .
  FILTER(?terminal = ?admitted)
}
