# Detect whether admitted independent inventory can lawfully support at least 250 semantic commits this hour.
ASK WHERE { { SELECT (SUM(?n) AS ?total) WHERE { ?card <https://ggen.dev/tps#eligible> true ; <https://ggen.dev/tps#expectedSemanticCommits> ?n . } } FILTER(?total >= 250) }