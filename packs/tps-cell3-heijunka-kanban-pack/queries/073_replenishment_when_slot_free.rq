PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?cycle a tps:Cycle ; tps:wipCount ?count ; tps:wipLimit ?limit ; tps:replenishmentQueue ?queue . FILTER(?count < ?limit) FILTER EXISTS { ?queue tps:nextReadyCard ?card . ?card tps:readiness "READY_NOW" } }
