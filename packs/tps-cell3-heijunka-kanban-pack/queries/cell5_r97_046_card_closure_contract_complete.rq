PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?cycle tps:activeCard ?card . ?card tps:base ?base ; tps:head ?head ; tps:upstreamPrimitive ?primitive ; tps:authority ?authority ; tps:qualificationCommand ?q ; tps:integrationCommand ?i ; tps:expectedTerminalState ?state . }
