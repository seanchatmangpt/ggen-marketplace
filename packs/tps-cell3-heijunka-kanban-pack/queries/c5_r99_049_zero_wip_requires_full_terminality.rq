PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle WHERE {
  ?cycle a tps:Cycle ; tps:wipCount 0 ; tps:admittedCount ?admitted ; tps:terminalCount ?terminal .
  FILTER(?terminal != ?admitted)
}
