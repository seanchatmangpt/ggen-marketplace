# A card is READY only when every declared current-cycle dependency is terminal or admitted-ready.
ASK WHERE {
  ?card <https://ggen.dev/tps#dependency> ?dep .
  ?dep <https://ggen.dev/tps#standing> ?s .
  FILTER(?s IN ("ALIVE", "READY_NOW"))
}