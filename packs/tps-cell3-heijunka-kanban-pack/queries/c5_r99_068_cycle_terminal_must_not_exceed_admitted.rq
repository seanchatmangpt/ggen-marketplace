PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?cycle WHERE { ?cycle a tps:Cycle ; tps:terminalCount ?terminal ; tps:admittedCount ?admitted . FILTER(xsd:integer(?terminal) > xsd:integer(?admitted)) }
