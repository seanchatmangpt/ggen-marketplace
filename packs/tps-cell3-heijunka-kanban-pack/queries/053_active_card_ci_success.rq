PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE {
  ?cycle a tps:Cycle ; tps:activeCard ?card .
  ?card tps:qualificationWitness ?w .
  ?w tps:checkName "CI" ; tps:conclusion "SUCCESS" ; tps:subjectHead ?head .
  ?card tps:head ?head .
}
