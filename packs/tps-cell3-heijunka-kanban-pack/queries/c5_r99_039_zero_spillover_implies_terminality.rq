PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle WHERE {
  ?cycle a tps:Cycle ; tps:admittedCount ?admitted ; tps:terminalCount ?terminal ; tps:spilloverCount 0 .
  FILTER(?terminal != ?admitted)
}
