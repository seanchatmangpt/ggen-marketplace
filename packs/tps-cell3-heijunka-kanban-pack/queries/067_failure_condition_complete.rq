PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?card a tps:KanbanCard ; tps:failureCondition ?f . ?f tps:condition ?condition ; tps:standing ?standing . FILTER(STRLEN(?condition)>0 && STRLEN(?standing)>0) }
