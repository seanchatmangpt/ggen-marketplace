# Every selected card emits a bounded allocation event object; transport is evidence-only.
ASK WHERE { ?event <https://ggen.dev/tps#eventType> "ALLOCATE_READY" ; <https://ggen.dev/tps#card> ?card ; <https://ggen.dev/tps#cycle> ?cycle . }