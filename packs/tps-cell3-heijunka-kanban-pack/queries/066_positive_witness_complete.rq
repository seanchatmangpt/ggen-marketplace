PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { ?card a tps:KanbanCard ; tps:positiveWitness ?w . ?w tps:subjectHead ?h ; tps:command ?c ; tps:exitCode 0 . FILTER(STRLEN(?h)>0 && STRLEN(?c)>0) }
