# Replaying the same cycle/card/head selector tuple must not create duplicate active allocation.
ASK WHERE { ?a <https://ggen.dev/tps#replayKey> ?key ; <https://ggen.dev/tps#active> true . ?b <https://ggen.dev/tps#replayKey> ?key ; <https://ggen.dev/tps#active> true . FILTER(?a != ?b) }