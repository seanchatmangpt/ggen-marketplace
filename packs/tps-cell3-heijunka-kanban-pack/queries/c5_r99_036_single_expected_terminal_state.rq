PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card (COUNT(?state) AS ?states) WHERE {
  ?card a tps:KanbanCard ; tps:expectedTerminalState ?state .
}
GROUP BY ?card
HAVING(COUNT(?state) != 1)
