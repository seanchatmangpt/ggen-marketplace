PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?cycle ?left ?right WHERE { ?cycle a tps:Cycle ; tps:wipLimit ?limit ; tps:activeCard ?left, ?right . FILTER(xsd:integer(?limit) = 1 && STR(?left) < STR(?right)) }
