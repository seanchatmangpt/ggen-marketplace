PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?cycle a tps:Cycle ; tps:refusedCard ?old . FILTER NOT EXISTS { ?cycle tps:replacementCard ?new . ?new tps:readiness "READY_NOW" } } }
