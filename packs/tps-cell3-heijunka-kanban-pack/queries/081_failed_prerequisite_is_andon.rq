PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:dependency ?d . ?d tps:standing "BUILD_BROKEN" . FILTER NOT EXISTS { ?card tps:andonCode "FAILED_PREREQUISITE" } } }
