PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card WHERE { ?card a tps:KanbanCard ; tps:readinessDisposition tps:READY_NOW . FILTER NOT EXISTS { ?card tps:diversityEvidence ?evidence } }
