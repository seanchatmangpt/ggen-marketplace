PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle WHERE { ?cycle a tps:Cycle ; tps:terminalCount ?terminal ; tps:wipCount ?wip ; tps:admittedCount ?admitted . FILTER((?terminal + ?wip) > ?admitted) }
