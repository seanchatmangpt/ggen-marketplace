PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle ?terminal ?admitted WHERE { ?cycle tps:closureComplete true ; tps:terminalCount ?terminal ; tps:admittedCount ?admitted . FILTER(?terminal != ?admitted) }
