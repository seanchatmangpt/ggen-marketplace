PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card WHERE { ?card a tps:ReadyCard ; tps:readinessDisposition tps:READY_AFTER_BOUNDED_REPAIR . FILTER NOT EXISTS { ?card tps:repairBudget ?budget } }
