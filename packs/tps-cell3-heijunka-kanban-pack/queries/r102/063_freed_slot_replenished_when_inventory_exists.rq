PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle ?slot WHERE { ?cycle tps:freedSlot ?slot ; tps:readyInventoryCount ?count . FILTER(?count > 0) FILTER NOT EXISTS { ?event a tps:ReplenishmentEvent ; tps:freedSlot ?slot } }
