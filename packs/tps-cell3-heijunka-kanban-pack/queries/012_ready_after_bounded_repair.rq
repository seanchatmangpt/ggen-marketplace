# READY_AFTER_BOUNDED_REPAIR requires a typed Andon with a repair estimate inside remaining takt.
ASK WHERE { ?card <https://ggen.dev/tps#andon> ?andon ; <https://ggen.dev/tps#repairSeconds> ?repair ; <https://ggen.dev/tps#remainingSeconds> ?remaining . FILTER(?repair <= ?remaining) }