PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?left ?right ?base ?head WHERE { ?left a tps:KanbanCard ; tps:base ?base ; tps:head ?head . ?right a tps:KanbanCard ; tps:base ?base ; tps:head ?head . FILTER(STR(?left) < STR(?right)) }