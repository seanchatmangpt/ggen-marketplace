# GGEN-first work without a reusable primitive/generator is an Andon, not an invitation to handwrite.
ASK WHERE { ?card <https://ggen.dev/tps#ggenFirst> true . FILTER NOT EXISTS { ?card <https://ggen.dev/tps#upstreamPrimitive> ?p } }