PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:manufactureMode ?mode . FILTER(?mode = "NEW_PACK" && EXISTS { ?card tps:reusablePrimitive ?existing }) } }
