# Surface candidates that replace repeated handwritten/duplicated allocation logic with reusable GGEN capital.
SELECT ?card ?displacement WHERE { ?card <https://ggen.dev/tps#eligible> true ; <https://ggen.dev/tps#structuralDisplacement> ?displacement . } ORDER BY DESC(?displacement)