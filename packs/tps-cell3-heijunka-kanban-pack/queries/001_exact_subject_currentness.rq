# Refuse READY when a current-cycle Kanban card is not bound to the repository's exact current subject.
ASK WHERE {
  ?card <https://ggen.dev/tps#cycle> ?cycle ;
        <https://ggen.dev/tps#consumerHead> ?head ;
        <https://ggen.dev/tps#currentHead> ?head .
}