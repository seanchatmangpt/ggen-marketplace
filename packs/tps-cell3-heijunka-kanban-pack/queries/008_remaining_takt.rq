# Admit only cards whose bounded qualification estimate fits the remaining same-hour takt.
ASK WHERE { ?card <https://ggen.dev/tps#estimatedSeconds> ?estimate ; <https://ggen.dev/tps#remainingSeconds> ?remaining . FILTER(?estimate <= ?remaining) }