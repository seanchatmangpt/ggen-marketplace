PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK { FILTER NOT EXISTS { ?cycle a tps:Cycle ; tps:wipCount ?w ; tps:wipLimit ?l . ?card a tps:KanbanCard ; tps:readyClass tps:READY_NOW ; tps:starvationAgeMinutes ?age . FILTER(?w < ?l && ?age > 0) } }