# A failed prerequisite blocks a card from READY and triggers replacement search.
ASK WHERE { ?card <https://ggen.dev/tps#prerequisite> ?p . ?p <https://ggen.dev/tps#standing> ?s . FILTER(STRSTARTS(?s,"BUILD_BROKEN") || STRSTARTS(?s,"REFUSED")) }