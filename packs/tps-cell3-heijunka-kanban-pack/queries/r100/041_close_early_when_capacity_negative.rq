PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card WHERE { ?card a tps:ReadyCard ; tps:capacityMargin ?margin . FILTER(?margin < 0) FILTER NOT EXISTS { ?card tps:disposition tps:CLOSE_EARLY } }