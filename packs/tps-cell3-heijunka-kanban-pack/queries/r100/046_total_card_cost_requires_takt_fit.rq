PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card WHERE { ?card a tps:ReadyCard ; tps:qualificationCost ?q ; tps:integrationCost ?i ; tps:closureCost ?c ; tps:remainingTakt ?takt . FILTER((?q + ?i + ?c) > ?takt) }