PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?event WHERE { ?event a tps:AllocationEvent . { FILTER NOT EXISTS { ?event tps:baseSha ?base } } UNION { FILTER NOT EXISTS { ?event tps:headSha ?head } } }