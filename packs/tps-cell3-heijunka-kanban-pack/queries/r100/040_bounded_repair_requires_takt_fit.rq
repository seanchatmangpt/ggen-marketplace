PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card WHERE { ?card a tps:ReadyCard ; tps:disposition tps:READY_AFTER_BOUNDED_REPAIR ; tps:repairCost ?cost ; tps:remainingTakt ?takt . FILTER(?cost > ?takt) }