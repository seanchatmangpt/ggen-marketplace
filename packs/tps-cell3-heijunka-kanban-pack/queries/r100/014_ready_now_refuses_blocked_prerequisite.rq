PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?card ?dependency WHERE { ?card a tps:ReadyCard ; tps:disposition tps:READY_NOW ; tps:dependsOn ?dependency . ?dependency tps:standing tps:BLOCKED . }