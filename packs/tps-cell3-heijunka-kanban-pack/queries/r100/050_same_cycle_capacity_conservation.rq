PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
SELECT ?cycle WHERE { ?cycle a tps:Cycle ; tps:admittedCount ?admitted ; tps:terminalCount ?terminal ; tps:wipCount ?wip ; tps:spillover ?spillover . FILTER((?terminal + ?wip + ?spillover) != ?admitted) }