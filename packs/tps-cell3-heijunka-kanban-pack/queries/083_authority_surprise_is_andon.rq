PREFIX tps: <https://ggen.dev/ontology/tps-cell3#>
ASK WHERE { FILTER NOT EXISTS { ?card a tps:KanbanCard ; tps:requiredAuthority ?required ; tps:admittedAuthority ?admitted . FILTER(?required != ?admitted) FILTER NOT EXISTS { ?card tps:andonCode "AUTHORITY_SURPRISE" } } }
