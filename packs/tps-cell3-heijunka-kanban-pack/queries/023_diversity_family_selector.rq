# Keep the ready set broad across consumer families instead of saturating one family.
SELECT ?family (COUNT(?card) AS ?n) WHERE { ?card <https://ggen.dev/tps#consumerFamily> ?family ; <https://ggen.dev/tps#eligible> true . } GROUP BY ?family ORDER BY ?n