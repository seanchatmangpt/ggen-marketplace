PREFIX ex: <https://seanchatmangpt.github.io/ggen-marketplace/dfcm/explore#>
SELECT ?candidate ?rollback WHERE {
  ?candidate ex:standing ex:retained .
  OPTIONAL { ?candidate ex:rollbackCost ?rollback }
  FILTER(!BOUND(?rollback))
} ORDER BY ?candidate
