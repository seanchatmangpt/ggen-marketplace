PREFIX ex: <https://seanchatmangpt.github.io/ggen-marketplace/dfcm/explore#>
SELECT ?candidate ?assumption WHERE {
  ?candidate ex:standing ex:retained ; ex:assumption ?assumption .
  FILTER(STRLEN(STR(?assumption)) > 0)
} ORDER BY ?candidate
