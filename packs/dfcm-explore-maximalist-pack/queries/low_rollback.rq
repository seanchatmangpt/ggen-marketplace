PREFIX ex: <https://ggen.dev/ontology/dfcm-explore#>
PREFIX dct: <http://purl.org/dc/terms/>
SELECT ?candidate ?title ?family ?rollback WHERE {
  ?candidate a ex:Candidate ; dct:title ?title ; ex:family ?family ; ex:rollbackCost ?rollback .
  FILTER(?rollback <= 1)
}
ORDER BY ?family ?candidate
