PREFIX ex: <https://seanchatmangpt.github.io/ggen-marketplace/dfcm/explore#>
SELECT ?assumption (COUNT(?candidate) AS ?candidateCount) WHERE {
  ?candidate ex:standing ex:retained ; ex:assumption ?assumption .
} GROUP BY ?assumption HAVING(COUNT(?candidate) > 1) ORDER BY DESC(?candidateCount) ?assumption
