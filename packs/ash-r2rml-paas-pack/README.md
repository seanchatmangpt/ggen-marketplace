# Ash / AshR2RML PaaS pack

Marketplace-canonical manufacturer for a provider-neutral PaaS control-plane kernel.

`ontology.ttl → admission gate → ggen → Ash resources/domain → AshR2RML semantic projection → Reactor → injected BRCE → receipt/replay`.

The platform profile composes the marketplace Challenger value contract through `cv:ChallengerProjectionPolicy`. ERRC remains SELECT/CONSTRUCT-only strategic input; it cannot actuate a provider. Generated runtime artifacts are projections and must not become a second semantic authority.

Public semantics: Schema.org, PROV-O, DCAT, DCTERMS, ODRL, QUDT, SKOS, SHACL, R2RML, plus the marketplace Challenger/ERRC contract.

## Standing

Marketplace qualification proves deterministic construction through the admitted ggen runtime. Generated Elixir execution is a separate subject court; a consumer may claim `ALIVE` only after compiling/running that exact generated subject and receiving a BRCE-bound receipt.
