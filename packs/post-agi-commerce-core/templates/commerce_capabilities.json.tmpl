---
to: "generated/commerce-capabilities.json"
sparql:
  capabilities: |
    PREFIX dcterms: <http://purl.org/dc/terms/>
    PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
    PREFIX c: <urn:ggen:pack:post-agi-commerce:>
    SELECT ?id ?operation ?external
    WHERE {
      ?cap a skos:Concept ;
        skos:inScheme c:capabilities ;
        dcterms:identifier ?id ;
        dcterms:type ?operation ;
        skos:note ?externalNote .
      FILTER(STRSTARTS(STR(?externalNote), "external-authority-required:"))
      BIND(STRAFTER(STR(?externalNote), "external-authority-required:") AS ?external)
    }
    ORDER BY ?id
---
{
  "generated": true,
  "authority": "NONE",
  "gymact_subject": "e339c3fbbddd116f86f672aeba5dc82d11ec6b14",
  "capability_digest": "c9a429501b79b7f799b7b6e8fc12d065bb1452fdd8021761d34242f257cd9032",
  "capabilities": [
{% for row in capabilities -%}
    {"id":"{{ row.id }}","operation":"{{ row.operation }}","external_authority_required":{{ row.external }}}{% if not loop.last %},{% endif %}
{% endfor -%}
  ]
}
