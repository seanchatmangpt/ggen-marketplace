PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX c: <urn:ggen:pack:post-agi-commerce:>
SELECT ?violation WHERE {
  c:pack skos:note ?note .
  FILTER(CONTAINS(UCASE(STR(?note)), "EXTERNAL-STANDING:ALIVE"))
  BIND("pack must not claim external ALIVE" AS ?violation)
}
