# Manufacture an option-capital frontier

Use the admitted ggen binary for this marketplace revision, then run `ggen sync run` inside `packs/ggen-project-boundary-pack`. R19B reads the canonical manufacturing-capital period and deterministically emits `generated/project-boundary/capital-calibrated-frontier.json` plus `capital_calibrated_frontier_court.py`. Execute the court to qualify arithmetic invariants. Delete generated consequences afterward; source remains ontology, query, template, and generation rule.
