# Add a calibration variant without collapsing the frontier

Create a distinct SPARQL query with its own formula and typed denominator guards, pair it with a dedicated template, register a new generation rule, and qualify it beside—not instead of—the existing R19B heuristic. Preserve all variants whose domains differ or whose tradeoffs remain non-dominated. A failing variant receives a typed topology result; it does not invalidate adjacent variants.
