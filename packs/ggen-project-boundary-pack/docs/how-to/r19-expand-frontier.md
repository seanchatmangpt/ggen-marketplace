# Expand the R19 option frontier

Add a new composition as RDF facts, bind it to its calibration, declare reversibility, confidence lower bound, capability delta, authority cost, qualification status, reuse, and marketplace support, then evaluate all `r19-xcal-*` queries. Never delete a candidate because a neighboring edge fails; encode the failure as BLOCKED, UNSUPPORTED, or another typed standing and preserve reversible alternatives.
