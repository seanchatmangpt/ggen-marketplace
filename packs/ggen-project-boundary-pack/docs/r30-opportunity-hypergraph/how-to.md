# How to run R30 as an EXPLORE cell

- Resolve the previous merged default-head SHA and bind it as the observation subject.
- Ingest newly ALIVE capabilities and their provenance into the Innovation Capital Ledger.
- Enumerate pairwise compositions, then higher-order compositions whose inputs coexist lawfully.
- For each composition, emit technically distinct candidates across runtime, verifier, generator, ontology, compatibility, recovery, process-intelligence, and integration surfaces when grounded.
- Record marketplace support as existing, extend, new, or missing. Missing support is topology; it does not delete the candidate.
- Preserve every non-dominated reversible candidate on the frontier until an authorized SELECT boundary exists.
- Rank by expected reuse and capability-space delta without collapsing to one winner.
- Run the R30 permanent court and authority gate; reject any candidate fixture that claims consequential DO.
- Append realization changes after merge so the next cell can distinguish newly reachable adjacency from stale OPEN state.
