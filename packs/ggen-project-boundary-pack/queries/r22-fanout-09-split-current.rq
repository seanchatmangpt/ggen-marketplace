PREFIX mfc: <https://ggen.dev/ontology/manufacturing-fanout#>
SELECT ?edge (COUNT(DISTINCT ?digest) AS ?digests) WHERE { ?run mfc:realizes ?edge ; mfc:current true ; mfc:semanticDigest ?digest . } GROUP BY ?edge HAVING(COUNT(DISTINCT ?digest)>1)