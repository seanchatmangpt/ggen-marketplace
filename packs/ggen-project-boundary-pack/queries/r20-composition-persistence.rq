PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?period ?persistence ?growth
WHERE { ?period a gpb:CapitalCompoundingPeriod ; gpb:compositionPersistence ?persistence ; gpb:netCapitalGrowth ?growth . }
ORDER BY DESC(?persistence) DESC(?growth) ?period