PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?opportunities WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:novelCount 0 ; gpb:opportunityCount ?opportunities .
  FILTER(?opportunities > 0)
} ORDER BY DESC(?opportunities)
