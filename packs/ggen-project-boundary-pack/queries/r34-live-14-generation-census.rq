PREFIX r33: <https://ggen.dev/ontology/live-candidate-realization-r33#>
SELECT ?generation (COUNT(DISTINCT ?realization) AS ?realizations) WHERE { ?realization a r33:CandidateRealization ; r33:currentGeneration ?generation . } GROUP BY ?generation ORDER BY DESC(?realizations) ?generation