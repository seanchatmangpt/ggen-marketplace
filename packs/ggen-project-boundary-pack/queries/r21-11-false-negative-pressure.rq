PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?fn ?tp WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:falseNegativeCount ?fn ; gpb:truePositiveCount ?tp .
  FILTER(?fn > 0)
} ORDER BY DESC(?fn)
