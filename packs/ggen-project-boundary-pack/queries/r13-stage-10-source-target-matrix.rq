PREFIX isc: <https://ggen.dev/ontology/isolated-staging-control#>
SELECT ?source ?target (COUNT(?plan) AS ?plans)
WHERE {
  ?plan a isc:StagePlan ; isc:sourceSubject ?source ; isc:targetRepository ?target .
}
GROUP BY ?source ?target
ORDER BY DESC(?plans) ?source ?target
