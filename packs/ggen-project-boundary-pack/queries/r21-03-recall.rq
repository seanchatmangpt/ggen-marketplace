PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?tp ?fn (?tp / (?tp + ?fn) AS ?recall) WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:truePositiveCount ?tp ; gpb:falseNegativeCount ?fn .
  FILTER((?tp + ?fn) > 0)
} ORDER BY ?run
