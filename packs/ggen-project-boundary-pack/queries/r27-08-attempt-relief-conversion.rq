PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT (COUNT(DISTINCT ?attempt) AS ?attempts) (COUNT(DISTINCT ?relief) AS ?relieved) WHERE {
  ?attempt a gpb:RemediationAttempt . OPTIONAL { ?relief a gpb:DependencyReliefObservation ; gpb:reliefFromAttempt ?attempt . }
}
