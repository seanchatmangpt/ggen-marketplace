PREFIX obc: <https://ggen.dev/ontology/option-boundary-capital#>
SELECT ?op ?unlocked ?investment (?unlocked / ?investment AS ?yield)
WHERE {
  ?op a obc:Opportunity ; obc:unlockedActions ?unlocked ; obc:investment ?investment .
  FILTER(?investment > 0)
}
ORDER BY DESC(?yield) ?op
