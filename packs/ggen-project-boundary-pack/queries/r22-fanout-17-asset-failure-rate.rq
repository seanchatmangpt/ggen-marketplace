PREFIX mfc: <https://ggen.dev/ontology/manufacturing-fanout#>
SELECT ?asset (COUNT(?run) AS ?runs) (SUM(IF(?standing="BUILD_BROKEN",1,0)) AS ?failures) WHERE { ?run mfc:asset ?asset ; mfc:standing ?standing . } GROUP BY ?asset