PREFIX mfc: <https://ggen.dev/ontology/manufacturing-fanout#>
SELECT ?asset ?standing (COUNT(?run) AS ?runs) WHERE { ?run mfc:asset ?asset ; mfc:standing ?standing . FILTER(?standing != "ALIVE") } GROUP BY ?asset ?standing ORDER BY DESC(?runs)