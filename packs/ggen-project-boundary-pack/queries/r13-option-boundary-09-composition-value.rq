PREFIX obc: <https://ggen.dev/ontology/option-boundary-capital#>
SELECT ?composition ?compositionValue ?capacity
WHERE {
  ?composition a obc:Composition ; obc:compositionValue ?compositionValue ; obc:futureManufacturingCapacity ?capacity .
}
ORDER BY DESC(?compositionValue) DESC(?capacity) ?composition
