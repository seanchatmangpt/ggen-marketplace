PREFIX obc: <https://ggen.dev/ontology/option-boundary-capital#>
SELECT ?primitive (COUNT(DISTINCT ?op) AS ?consumers)
WHERE {
  ?op a obc:Opportunity ; obc:requiresPrimitive ?primitive .
}
GROUP BY ?primitive
HAVING(COUNT(DISTINCT ?op) >= 2)
ORDER BY DESC(?consumers) ?primitive
