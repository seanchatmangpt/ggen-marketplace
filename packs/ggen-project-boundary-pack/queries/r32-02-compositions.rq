PREFIX cap: <https://ggen.dev/ontology/option-capital#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?candidate ?id ?neighbor WHERE {
  ?candidate dcterms:identifier ?id ; cap:composesWith ?neighbor .
} ORDER BY ?id ?neighbor
