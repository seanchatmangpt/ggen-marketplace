PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?seeds ?actionable (?actionable / ?seeds AS ?e) WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:seedCount ?seeds ; gpb:actionableCount ?actionable .
  FILTER(?seeds > 0)
  FILTER((?actionable / ?seeds) < 2)
} ORDER BY ?e
