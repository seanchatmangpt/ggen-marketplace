PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?entry ?gain WHERE { ?entry a gp:CapitalLedgerEntry ; gp:informationGain ?gain . FILTER(xsd:decimal(?gain) >= 0) } ORDER BY DESC(xsd:decimal(?gain)) ?entry
