PREFIX obc: <https://ggen.dev/ontology/option-boundary-capital#>
SELECT ?op ?capacity
WHERE {
  ?op a obc:Opportunity ; obc:futureManufacturingCapacity ?capacity ; obc:projectContained true .
}
ORDER BY DESC(?capacity) ?op
