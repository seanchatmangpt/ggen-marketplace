PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?shard (COUNT(?obs) AS ?packs) (SUM(?latency) AS ?seconds) WHERE {
  ?obs a gpb:PackQualificationObservation ; gpb:shardIndex ?shard ; gpb:observedLatencySeconds ?latency .
}
GROUP BY ?shard
ORDER BY DESC(?seconds) ?shard
