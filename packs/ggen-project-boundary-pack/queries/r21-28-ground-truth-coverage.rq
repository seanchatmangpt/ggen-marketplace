PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT (COUNT(?opportunity) AS ?grounded) (SUM(IF(?discovered,1,0)) AS ?discoveredGrounded) WHERE {
  ?opportunity a gpb:SensorOpportunity ; gpb:groundTruth true ; gpb:discovered ?discovered .
}
