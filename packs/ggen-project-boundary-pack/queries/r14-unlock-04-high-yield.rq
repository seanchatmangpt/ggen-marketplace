PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?entry ?yield WHERE { ?entry a gp:CapitalLedgerEntry ; gp:capitalYield ?yield . FILTER(xsd:decimal(?yield) > 1) } ORDER BY DESC(xsd:decimal(?yield)) ?entry
