PREFIX cap: <https://ggen.dev/ontology/option-capital#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?id ?repository WHERE {
  ?candidate dcterms:identifier ?id ; cap:repository ?repository .
} ORDER BY ?repository ?id
