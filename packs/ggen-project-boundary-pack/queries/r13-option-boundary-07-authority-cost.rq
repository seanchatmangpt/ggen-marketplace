PREFIX obc: <https://ggen.dev/ontology/option-boundary-capital#>
SELECT ?op ?authorityCost ?reversibility
WHERE {
  ?op a obc:Opportunity ; obc:authorityCost ?authorityCost ; obc:reversibility ?reversibility .
}
ORDER BY ?authorityCost DESC(?reversibility) ?op
