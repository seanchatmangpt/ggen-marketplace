PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?novel ?latency (?novel / ?latency AS ?novelPerMillisecond) WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:novelCount ?novel ; gpb:latencyMillis ?latency .
  FILTER(?latency > 0)
} ORDER BY DESC(?novelPerMillisecond)
