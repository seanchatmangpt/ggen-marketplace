PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?authority WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:authority ?authority .
  FILTER(?authority != "OBSERVE|VERIFY")
} ORDER BY ?run
