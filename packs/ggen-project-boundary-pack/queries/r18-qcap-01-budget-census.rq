PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?budget ?packSeconds ?workflowMinutes WHERE {
  ?budget a gpb:QualificationBudget ;
          gpb:qualificationTimeoutSeconds ?packSeconds ;
          gpb:workflowTimeoutMinutes ?workflowMinutes .
}
ORDER BY ?budget
