PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?tp ?fp (?tp / (?tp + ?fp) AS ?precision) WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:truePositiveCount ?tp ; gpb:falsePositiveCount ?fp .
  FILTER((?tp + ?fp) > 0)
} ORDER BY ?run
