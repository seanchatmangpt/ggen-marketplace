PREFIX cap: <https://ggen.dev/ontology/option-capital#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?candidate ?id WHERE {
  ?candidate dcterms:identifier ?id ; cap:reversibility cap:Reversible .
} ORDER BY ?id
