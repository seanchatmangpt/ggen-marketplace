PREFIX gt: <https://ggen.dev/ontology/ground-truth-r33#>
SELECT ?obs ?generation WHERE {
  ?obs a gt:CalibrationObservation ; gt:generation ?generation .
  FILTER NOT EXISTS {
    ?other a gt:CalibrationObservation ; gt:generation ?newer .
    FILTER(?newer > ?generation)
  }
}
ORDER BY ?obs
