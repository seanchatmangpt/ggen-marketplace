PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?workflow WHERE { ?workflow a gpb:WorkflowBound . FILTER NOT EXISTS { ?workflow gpb:workflowTimeoutMinutes ?minutes } } ORDER BY ?workflow
