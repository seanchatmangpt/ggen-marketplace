PREFIX r33: <https://ggen.dev/ontology/live-candidate-realization-r33#>
SELECT ?realization (COUNT(DISTINCT ?root) AS ?roots) WHERE { ?realization a r33:CandidateRealization ; r33:evidenceRoot ?root . } GROUP BY ?realization HAVING(COUNT(DISTINCT ?root) > 1) ORDER BY DESC(?roots) ?realization