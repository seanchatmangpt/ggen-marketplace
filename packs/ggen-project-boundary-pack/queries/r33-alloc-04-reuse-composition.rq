PREFIX alloc: <https://ggen.dev/ontology/relief-allocation-r33#>
SELECT ?candidate ?reuse ?composition ((?reuse + ?composition) AS ?multiplier) WHERE {
  ?candidate a alloc:AllocationCandidate ; alloc:reuseValue ?reuse ; alloc:compositionValue ?composition .
}
ORDER BY DESC(?multiplier) ?candidate
