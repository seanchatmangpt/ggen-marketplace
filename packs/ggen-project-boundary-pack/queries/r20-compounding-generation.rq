PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?generation (COUNT(?period) AS ?periods)
WHERE { ?period a gpb:CapitalCompoundingPeriod ; gpb:compoundingGeneration ?generation ; gpb:current true . }
GROUP BY ?generation
HAVING(COUNT(?period) > 1)