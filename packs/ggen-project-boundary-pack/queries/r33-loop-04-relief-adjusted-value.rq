PREFIX loop: <https://ggen.dev/ontology/calibrated-capital-allocation-r33#>
SELECT ?decision ?value WHERE {
  ?decision a loop:CalibratedAllocationDecision ; loop:reliefAdjustedValue ?value .
}
ORDER BY DESC(?value) ?decision
