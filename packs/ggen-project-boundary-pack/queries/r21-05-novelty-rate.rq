PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?novel ?opportunities (?novel / ?opportunities AS ?noveltyRate) WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:novelCount ?novel ; gpb:opportunityCount ?opportunities .
  FILTER(?opportunities > 0)
} ORDER BY DESC(?noveltyRate)
