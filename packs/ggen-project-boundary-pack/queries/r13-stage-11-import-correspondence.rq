PREFIX isc: <https://ggen.dev/ontology/isolated-staging-control#>
SELECT ?plan ?artifactDigest ?importDigest
WHERE {
  ?plan a isc:StagePlan ; isc:artifactDigest ?artifactDigest ; isc:importDigest ?importDigest .
  FILTER(?artifactDigest != ?importDigest)
}
ORDER BY ?plan
