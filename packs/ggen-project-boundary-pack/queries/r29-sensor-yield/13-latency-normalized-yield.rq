PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ((?novel / ?latency) AS ?yieldPerSecond) WHERE { ?run a prov:Activity ; r29:novelCount ?novel ; r29:latencySeconds ?latency . FILTER(?latency > 0) } ORDER BY DESC(?yieldPerSecond) ?run
