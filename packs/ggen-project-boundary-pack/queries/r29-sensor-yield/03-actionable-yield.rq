PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ?actionable WHERE { ?run a prov:Activity ; r29:actionableCount ?actionable . } ORDER BY DESC(?actionable) ?run
