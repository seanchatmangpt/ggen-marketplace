PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ((?realized / ?seeds) AS ?closureAdjustedE) WHERE { ?run a prov:Activity ; r29:seedCount ?seeds ; r29:realizedCount ?realized . FILTER(?seeds > 0) } ORDER BY DESC(?closureAdjustedE) ?run
