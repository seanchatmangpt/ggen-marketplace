PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ((?actionable / ?seeds) AS ?E) WHERE { ?run a prov:Activity ; r29:seedCount ?seeds ; r29:actionableCount ?actionable . FILTER(?seeds > 0) } ORDER BY DESC(?E) ?run
