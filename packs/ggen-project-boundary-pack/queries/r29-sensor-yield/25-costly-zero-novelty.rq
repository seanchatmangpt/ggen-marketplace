PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ?cost WHERE { ?run a prov:Activity ; r29:novelCount 0 ; r29:costUnits ?cost . FILTER(?cost > 0) } ORDER BY DESC(?cost) ?run
