PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ?repositories WHERE { ?run a prov:Activity ; r29:repositoryReach ?repositories . } ORDER BY DESC(?repositories) ?run
