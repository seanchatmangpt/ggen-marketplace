PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ?latency ?novel WHERE { ?run a prov:Activity ; r29:latencySeconds ?latency ; r29:novelCount ?novel . FILTER(?latency >= 60 && ?novel <= 1) } ORDER BY DESC(?latency) ?run
