PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ((?novel / ?actionable) AS ?noveltyRate) WHERE { ?run a prov:Activity ; r29:novelCount ?novel ; r29:actionableCount ?actionable . FILTER(?actionable > 0) } ORDER BY DESC(?noveltyRate) ?run
