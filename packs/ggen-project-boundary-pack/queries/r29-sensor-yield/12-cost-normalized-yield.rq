PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ((?novel / ?cost) AS ?yieldPerCost) WHERE { ?run a prov:Activity ; r29:novelCount ?novel ; r29:costUnits ?cost . FILTER(?cost > 0) } ORDER BY DESC(?yieldPerCost) ?run
