PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ?actionable WHERE { ?run a prov:Activity ; r29:actionableCount ?actionable ; r29:dependencyReliefCount 0 . FILTER(?actionable > 0) } ORDER BY DESC(?actionable) ?run
