PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run ((?relief / ?actionable) AS ?reliefRate) WHERE { ?run a prov:Activity ; r29:dependencyReliefCount ?relief ; r29:actionableCount ?actionable . FILTER(?actionable > 0) } ORDER BY DESC(?reliefRate) ?run
