PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX r29: <https://chatman.ai/ledger/r29/>
SELECT ?run WHERE { ?run a prov:Activity ; r29:seedCount ?seeds . FILTER NOT EXISTS { ?run r29:receiptDigest ?digest } } ORDER BY ?run
