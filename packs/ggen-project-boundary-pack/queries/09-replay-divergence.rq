PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?o ?output ?replay WHERE { ?o a gpb:ReplayObservation ; gpb:outputDigest ?output ; gpb:replayDigest ?replay . FILTER(?output != ?replay) }
