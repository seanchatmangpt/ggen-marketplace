PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?fp ?tp WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:falsePositiveCount ?fp ; gpb:truePositiveCount ?tp .
  FILTER(?fp > 0)
} ORDER BY DESC(?fp)
