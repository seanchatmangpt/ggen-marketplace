PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?entry ?relief WHERE { ?entry a gp:CapitalLedgerEntry ; gp:dependencyRelief ?relief . FILTER(xsd:decimal(?relief) >= 0) } ORDER BY DESC(xsd:decimal(?relief)) ?entry
