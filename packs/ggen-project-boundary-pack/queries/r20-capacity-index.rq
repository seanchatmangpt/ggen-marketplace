PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?period ?capacity ?growth
WHERE { ?period a gpb:CapitalCompoundingPeriod ; gpb:capacityIndex ?capacity ; gpb:netCapitalGrowth ?growth . }
ORDER BY DESC(?capacity) DESC(?growth) ?period