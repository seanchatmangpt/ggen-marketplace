PREFIX r33: <https://ggen.dev/ontology/live-candidate-realization-r33#>
SELECT ?realization ?investment WHERE {
  ?realization a r33:CandidateRealization ; r33:investmentUnits ?investment .
  FILTER(?investment < 0)
}
ORDER BY ?investment ?realization