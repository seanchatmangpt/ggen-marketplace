PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?entry ?cost WHERE { ?entry a gp:CapitalLedgerEntry ; gp:authorityCost ?cost . FILTER(xsd:decimal(?cost) <= 1) } ORDER BY xsd:decimal(?cost) ?entry
