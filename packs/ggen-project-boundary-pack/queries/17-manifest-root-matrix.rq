PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?manifest ?root (COUNT(?b) AS ?bindings) WHERE { ?b a gpb:ProjectBoundary ; gpb:manifestPath ?manifest ; gpb:projectRoot ?root . } GROUP BY ?manifest ?root ORDER BY ?manifest ?root
