PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?relief ?actionable (?relief / ?actionable AS ?reliefRate) WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:realizedReliefCount ?relief ; gpb:actionableCount ?actionable .
  FILTER(?actionable > 0)
} ORDER BY DESC(?reliefRate)
