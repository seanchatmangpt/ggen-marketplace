PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT (COUNT(DISTINCT ?option) AS ?options) (COUNT(DISTINCT ?attempt) AS ?attempted) WHERE {
  ?option a gpb:PortfolioOption . OPTIONAL { ?attempt a gpb:RemediationAttempt ; gpb:attemptForOption ?option . }
}
