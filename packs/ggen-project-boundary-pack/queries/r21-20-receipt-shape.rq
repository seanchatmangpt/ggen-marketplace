PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?digest WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:receiptDigest ?digest .
  FILTER(!regex(str(?digest), "^[0-9a-f]{64}$"))
} ORDER BY ?run
