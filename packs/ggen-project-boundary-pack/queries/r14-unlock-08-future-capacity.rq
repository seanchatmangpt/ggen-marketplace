PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?entry ?capacity WHERE { ?entry a gp:CapitalLedgerEntry ; gp:futureCapacity ?capacity . FILTER(xsd:decimal(?capacity) > 0) } ORDER BY DESC(xsd:decimal(?capacity)) ?entry
