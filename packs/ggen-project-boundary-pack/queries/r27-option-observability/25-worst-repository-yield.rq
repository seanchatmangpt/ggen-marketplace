PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?repo (COUNT(?opportunity) AS ?actionable) WHERE {
  ?opportunity a prov:Entity ; dcterms:type "actionable-opportunity" ; dcterms:isPartOf ?repo .
}
GROUP BY ?repo
ORDER BY ASC(?actionable) ?repo
LIMIT 1
