PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?subject (COUNT(?obs) AS ?seedObservations) WHERE {
  ?obs a prov:Entity ; dcterms:type "seed-observation" ; prov:specializationOf ?subject .
}
GROUP BY ?subject
ORDER BY DESC(?seedObservations) ?subject
