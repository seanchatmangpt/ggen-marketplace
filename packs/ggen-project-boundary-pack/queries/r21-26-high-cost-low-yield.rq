PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?novel ?cost WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:novelCount ?novel ; gpb:costMillis ?cost .
  FILTER(?cost > 1000 && ?novel < 2)
} ORDER BY DESC(?cost)
