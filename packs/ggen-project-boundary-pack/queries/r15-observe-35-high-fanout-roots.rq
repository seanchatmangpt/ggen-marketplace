PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
SELECT ?root (COUNT(DISTINCT ?opp) AS ?fanout) WHERE { ?opp a gp:Opportunity ; gp:failureRoot ?root . } GROUP BY ?root HAVING (COUNT(DISTINCT ?opp) >= 2) ORDER BY DESC(?fanout)
