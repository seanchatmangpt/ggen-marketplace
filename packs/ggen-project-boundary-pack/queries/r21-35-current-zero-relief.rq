PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?run ?actionable WHERE {
  ?run a gpb:SensorRealizationRun ; gpb:current true ; gpb:actionableCount ?actionable ; gpb:realizedReliefCount 0 .
  FILTER(?actionable > 0)
} ORDER BY DESC(?actionable)
