PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?period ?information ?capacity
WHERE { ?period a gpb:CapitalCompoundingPeriod ; gpb:informationIndex ?information ; gpb:capacityIndex ?capacity . }
ORDER BY DESC(?information) DESC(?capacity) ?period