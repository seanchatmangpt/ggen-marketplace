PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?candidate ?unlock ?capacity
WHERE { ?candidate a gpb:ReinvestmentCandidate ; gpb:dependencyUnlock ?unlock ; gpb:futureManufacturingCapacity ?capacity . FILTER(?unlock > 0) }
ORDER BY DESC(?unlock) DESC(?capacity) ?candidate