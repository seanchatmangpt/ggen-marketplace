PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?class (COUNT(?opportunity) AS ?count) WHERE {
  ?opportunity a gpb:SensorOpportunity ; gpb:opportunityClass ?class .
} GROUP BY ?class ORDER BY DESC(?count) ?class
