PREFIX r30: <https://chatman.ai/ledger/r30/>
SELECT ?candidate ?reuse ?delta WHERE { ?candidate a r30:Candidate, r30:FrontierMember ; r30:expectedReuse ?reuse ; r30:capabilitySpaceDelta ?delta . } ORDER BY DESC(?delta) DESC(?reuse) ?candidate
