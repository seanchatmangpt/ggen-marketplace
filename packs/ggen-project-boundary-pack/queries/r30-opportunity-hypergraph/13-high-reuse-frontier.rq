PREFIX r30: <https://chatman.ai/ledger/r30/>
SELECT ?candidate ?reuse WHERE { ?candidate a r30:Candidate, r30:FrontierMember ; r30:expectedReuse ?reuse . FILTER(?reuse >= 15) } ORDER BY DESC(?reuse) ?candidate
