PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
SELECT ?composition (COUNT(?capability) AS ?arity) WHERE { ?composition a gp:Composition ; gp:usesCapability ?capability . } GROUP BY ?composition HAVING(COUNT(?capability) >= 3) ORDER BY DESC(?arity) ?composition