PREFIX obc: <https://ggen.dev/ontology/option-boundary-capital#>
SELECT ?primitive (COUNT(DISTINCT ?consumer) AS ?unlocked)
WHERE {
  ?primitive a obc:ManufacturingPrimitive ; obc:unlocksConsumer ?consumer .
}
GROUP BY ?primitive
ORDER BY DESC(?unlocked) ?primitive
