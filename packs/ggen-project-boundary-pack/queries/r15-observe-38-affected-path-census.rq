PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
SELECT ?root (COUNT(DISTINCT ?path) AS ?paths) WHERE { ?opp a gp:Opportunity ; gp:failureRoot ?root ; gp:affectedPath ?path . } GROUP BY ?root ORDER BY DESC(?paths)
