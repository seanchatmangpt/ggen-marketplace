PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
SELECT ?sensor (COUNT(DISTINCT ?opp) AS ?opportunities) (COUNT(DISTINCT ?seed) AS ?seeds) WHERE { ?opp a gp:Opportunity ; gp:detectedBy ?sensor ; gp:seedObservation ?seed . } GROUP BY ?sensor ORDER BY DESC(?opportunities)
