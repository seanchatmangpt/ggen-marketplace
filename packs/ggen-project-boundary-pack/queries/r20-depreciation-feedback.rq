PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?decision ?depreciation ?regret
WHERE { ?decision a gpb:AdaptiveCapitalDecision ; gpb:depreciationRate ?depreciation ; gpb:capitalRegret ?regret . FILTER(?depreciation > 0) }
ORDER BY DESC(?depreciation) DESC(?regret) ?decision