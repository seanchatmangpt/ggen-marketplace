PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?period ?drawdown ?opening (?drawdown / ?opening AS ?drawdownRate)
WHERE { ?period a gpb:CapitalCompoundingPeriod ; gpb:drawdown ?drawdown ; gpb:openingStock ?opening . FILTER(?opening > 0) }
ORDER BY DESC(?drawdownRate) ?period