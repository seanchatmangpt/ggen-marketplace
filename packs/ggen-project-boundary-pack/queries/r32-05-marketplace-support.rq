PREFIX cap: <https://ggen.dev/ontology/option-capital#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?id ?support WHERE {
  ?candidate dcterms:identifier ?id ; cap:marketplaceSupport ?support .
} ORDER BY ?id ?support
