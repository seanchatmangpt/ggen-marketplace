PREFIX cap: <https://ggen.dev/ontology/option-capital#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?id ?evidence WHERE {
  ?candidate dcterms:identifier ?id ; cap:evidence ?evidence .
} ORDER BY ?id ?evidence
