PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT (COUNT(DISTINCT ?opportunity) AS ?opportunities) (COUNT(DISTINCT ?option) AS ?optionized) WHERE {
  ?opportunity a gpb:SensorOpportunity . OPTIONAL { ?option a gpb:PortfolioOption ; gpb:optionForOpportunity ?opportunity . }
}
