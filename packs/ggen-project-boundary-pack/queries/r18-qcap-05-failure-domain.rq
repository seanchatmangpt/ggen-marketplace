PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?result (COUNT(?obs) AS ?count) WHERE {
  ?obs a gpb:PackQualificationObservation ; gpb:qualificationResult ?result .
}
GROUP BY ?result
ORDER BY DESC(?count) ?result
