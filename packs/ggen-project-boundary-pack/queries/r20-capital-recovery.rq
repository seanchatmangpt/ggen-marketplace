PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?candidate ?recovery ?relief (?recovery + ?relief AS ?recoverableCapital)
WHERE { ?candidate a gpb:ReinvestmentCandidate ; gpb:expectedCapitalRecovery ?recovery ; gpb:depreciationRelief ?relief . }
ORDER BY DESC(?recoverableCapital) ?candidate