PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?qualified ?merge WHERE {
  ?merge a prov:Activity ; dcterms:type "merge-containment" ; prov:used ?qualified ; dcterms:status "CONTAINED" .
}
ORDER BY ?qualified ?merge
