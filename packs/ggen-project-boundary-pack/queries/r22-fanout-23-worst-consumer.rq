PREFIX mfc: <https://ggen.dev/ontology/manufacturing-fanout#>
SELECT ?consumer ?standing (COUNT(?run) AS ?runs) WHERE { ?run mfc:consumer ?consumer ; mfc:standing ?standing . FILTER(?standing != "ALIVE") } GROUP BY ?consumer ?standing ORDER BY DESC(?runs)