PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
SELECT ?allocation ?yield WHERE {
  ?allocation a gp:CapitalAllocation ; gp:capitalYield ?yield .
  FILTER(?yield > 1)
}
ORDER BY DESC(?yield) ?allocation
