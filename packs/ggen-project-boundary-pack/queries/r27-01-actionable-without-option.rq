PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
SELECT ?opportunity WHERE {
  ?opportunity a gpb:SensorOpportunity ; gpb:actionable true .
  FILTER NOT EXISTS { ?option a gpb:PortfolioOption ; gpb:optionForOpportunity ?opportunity . }
} ORDER BY ?opportunity
