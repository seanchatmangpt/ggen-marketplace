PREFIX gmc: <https://ggen.dev/ontology/manifest-compatibility#>
SELECT ?manifest WHERE { ?manifest a gmc:ManifestSubject . FILTER NOT EXISTS { ?manifest gmc:projectVersion ?version } } ORDER BY ?manifest
