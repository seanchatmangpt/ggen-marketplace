PREFIX gp: <https://ggen.dev/ontology/project-boundary#>
SELECT ?candidate (COUNT(DISTINCT ?repo) AS ?repositories) WHERE { ?candidate a gp:ExecutableCandidate ; gp:targetsRepository ?repo . } GROUP BY ?candidate ORDER BY DESC(?repositories) ?candidate