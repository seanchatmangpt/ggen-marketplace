PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
ASK { ?t a gpb:GenerationTarget ; gpb:sourceRepository ?source ; gpb:targetRepository ?target . FILTER(strlen(str(?source)) > 0 && strlen(str(?target)) > 0) }
