PREFIX gpb: <https://ggen.dev/ontology/project-boundary#>
ASK { ?b a gpb:ProjectBoundary ; gpb:projectRoot ?root ; gpb:manifestPath ?manifest . FILTER(strlen(str(?root)) > 0 && strlen(str(?manifest)) > 0) }
