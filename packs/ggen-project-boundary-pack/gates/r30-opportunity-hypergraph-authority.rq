PREFIX r30: <https://chatman.ai/ledger/r30/>
ASK {
  FILTER NOT EXISTS {
    ?candidate a r30:Candidate .
    { ?candidate r30:authority ?authority . FILTER(?authority != "OBSERVE|VERIFY") }
    UNION
    { ?candidate r30:actuationPerformed true }
  }
}
