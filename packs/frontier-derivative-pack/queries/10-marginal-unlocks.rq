PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?candidate (COUNT(DISTINCT ?unlocked) AS ?unlockCount)
WHERE { ?candidate a fd:Candidate . OPTIONAL { ?candidate fd:unlocks ?unlocked . } }
GROUP BY ?candidate
ORDER BY DESC(?unlockCount) ?candidate
