PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?candidate ?standing
WHERE {
 ?candidate a fd:Candidate ; fd:reversible true ; fd:standing ?standing .
 FILTER NOT EXISTS { ?candidate fd:authority "DO" }
}
ORDER BY ?standing ?candidate
