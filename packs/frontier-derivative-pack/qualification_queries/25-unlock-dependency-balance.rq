PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?candidate (COUNT(DISTINCT ?target) AS ?unlocks) (COUNT(DISTINCT ?dependency) AS ?requires) WHERE { ?candidate a fd:Candidate . OPTIONAL { ?candidate fd:unlocks ?target } OPTIONAL { ?candidate fd:requires ?dependency } } GROUP BY ?candidate
