PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?reversible (COUNT(DISTINCT ?candidate) AS ?count) WHERE { ?candidate a fd:Candidate ; fd:reversible ?reversible . } GROUP BY ?reversible
