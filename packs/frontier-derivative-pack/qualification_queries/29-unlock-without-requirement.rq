PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT DISTINCT ?candidate WHERE { ?candidate a fd:Candidate ; fd:unlocks ?target . FILTER NOT EXISTS { ?candidate fd:requires ?dependency } }
