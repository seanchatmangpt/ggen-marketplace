PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT (COUNT(DISTINCT ?reversibleCandidate) AS ?reversible_count) (COUNT(DISTINCT ?candidate) AS ?candidate_count) WHERE { ?candidate a fd:Candidate . OPTIONAL { ?candidate fd:reversible true . BIND(?candidate AS ?reversibleCandidate) } }
