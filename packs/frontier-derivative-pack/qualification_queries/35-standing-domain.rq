PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?standing (COUNT(DISTINCT ?candidate) AS ?count) WHERE { ?candidate a fd:Candidate ; fd:standing ?standing . } GROUP BY ?standing ORDER BY DESC(?count)
