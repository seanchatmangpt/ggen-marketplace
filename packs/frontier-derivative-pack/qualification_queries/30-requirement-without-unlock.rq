PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT DISTINCT ?candidate WHERE { ?candidate a fd:Candidate ; fd:requires ?dependency . FILTER NOT EXISTS { ?candidate fd:unlocks ?target } }
