PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?candidate WHERE { ?candidate a fd:Candidate ; fd:standing "ALIVE" ; fd:authority "SELECT" ; fd:reversible true . FILTER NOT EXISTS { ?candidate fd:unlocks ?target } }
