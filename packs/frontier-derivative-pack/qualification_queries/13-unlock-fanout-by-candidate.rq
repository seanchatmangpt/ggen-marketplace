PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?candidate (COUNT(DISTINCT ?target) AS ?unlock_count) WHERE { ?candidate a fd:Candidate ; fd:unlocks ?target . } GROUP BY ?candidate ORDER BY DESC(?unlock_count)
