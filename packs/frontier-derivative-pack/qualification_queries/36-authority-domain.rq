PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?authority (COUNT(DISTINCT ?candidate) AS ?count) WHERE { ?candidate a fd:Candidate ; fd:authority ?authority . } GROUP BY ?authority ORDER BY DESC(?count)
