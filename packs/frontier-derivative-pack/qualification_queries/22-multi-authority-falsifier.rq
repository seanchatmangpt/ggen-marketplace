PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?candidate (COUNT(DISTINCT ?authority) AS ?authority_count) WHERE { ?candidate a fd:Candidate ; fd:authority ?authority . } GROUP BY ?candidate HAVING(COUNT(DISTINCT ?authority) > 1)
