PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT ?candidate (COUNT(DISTINCT ?dependency) AS ?dependency_count) WHERE { ?candidate a fd:Candidate ; fd:requires ?dependency . } GROUP BY ?candidate ORDER BY DESC(?dependency_count)
