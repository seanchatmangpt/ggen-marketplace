PREFIX fd: <https://ggen.dev/ontology/frontier-derivative#>
SELECT DISTINCT ?source ?middle ?dependency WHERE { ?source fd:requires ?middle . ?middle fd:requires ?dependency . FILTER(?source != ?dependency) }
