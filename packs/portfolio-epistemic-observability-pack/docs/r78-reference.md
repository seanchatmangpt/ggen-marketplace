# Reference — R78 TCPS ready-set capital

R78 defines `CapitalCandidate`, `AdmissibleSet`, `ReadySet`, `PartialOrderEdge`, `DownstreamCustomer`, `DemandShape`, `AuthorityBroker`, `ExecutionReceipt`, `UpstreamReturn`, and `StandardWorkPromotion` over PROV-O/DQV-aligned RDF. Fifty SPARQL courts cover legality, readiness, priority, partial order, parallelism, downstream pull, capital dimensions, authority, receipts, exact subject identity, upstream return, standard-work promotion, and the clean allocation crown. Authority is SELECT|CONSTRUCT; consequential DO is not granted.
