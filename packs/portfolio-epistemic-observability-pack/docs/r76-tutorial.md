# Tutorial — build a structural census observation

Start with exact repository snapshots bound to repository, default branch, head SHA, and observation time. Add an `ExistingStructure -> DesiredStructure` displacement candidate, bind a public ontology and marketplace pack/composition, then bind generator, generated consequence, consumer, qualification receipt, and standing. Run the R76 court to execute all 50 sensors before treating the observation as admitted measurement capital.
