{
  "schema": "ggen-r78-tcps-ready-set-capital/v1",
  "authority": "SELECT|CONSTRUCT",
  "consequential_do": false,
  "allocations": [
  {%- for row in rows %}
    {"candidate":"{{ row.candidate }}","score":"{{ row.score }}"}{% if not loop.last %},{% endif %}
  {%- endfor %}
  ]
}
