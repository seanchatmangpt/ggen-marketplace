PREFIX dcat: <http://www.w3.org/ns/dcat#>
PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT (COUNT(DISTINCT ?repo) AS ?repositories) WHERE { ?repo a peo:Repository . }
