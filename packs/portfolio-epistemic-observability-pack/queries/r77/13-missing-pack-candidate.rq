PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo WHERE { ?repo a peo:RepositorySnapshot ; peo:desiredStructure ?d . FILTER NOT EXISTS { ?repo peo:packCandidate ?p } }
