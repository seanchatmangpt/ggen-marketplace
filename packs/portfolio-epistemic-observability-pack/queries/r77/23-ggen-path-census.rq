PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?path (COUNT(?repo) AS ?repos) WHERE { ?repo a peo:RepositorySnapshot ; peo:ggenPath ?path } GROUP BY ?path ORDER BY DESC(?repos)
