PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo ?standing WHERE { ?repo a peo:RepositorySnapshot ; peo:standing ?standing . FILTER(STRSTARTS(STR(?standing), "REFUSED")) }
