PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?declared (COUNT(DISTINCT ?repo) AS ?observed) WHERE { ?u a peo:RepositoryUniverse ; peo:repositoryCount ?declared . ?repo a peo:RepositorySnapshot } GROUP BY ?declared HAVING(?declared != COUNT(DISTINCT ?repo))
