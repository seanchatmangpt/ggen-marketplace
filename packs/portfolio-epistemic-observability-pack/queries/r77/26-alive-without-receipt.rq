PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo WHERE { ?repo a peo:RepositorySnapshot ; peo:standing "ALIVE" . FILTER NOT EXISTS { ?repo peo:qualificationReceipt ?r } }
