PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?run ?rate ?mean WHERE { ?run a peo:ScheduleRun ; peo:commitsPerHour ?rate ; peo:mean24hRate ?mean . FILTER(?rate < ?mean) }
