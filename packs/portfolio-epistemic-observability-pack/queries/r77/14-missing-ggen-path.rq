PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo WHERE { ?repo a peo:RepositorySnapshot ; peo:packCandidate ?p . FILTER NOT EXISTS { ?repo peo:ggenPath ?path } }
