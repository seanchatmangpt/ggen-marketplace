PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo (COUNT(DISTINCT ?sha) AS ?heads) WHERE { ?repo a peo:RepositorySnapshot ; peo:exactHead ?sha } GROUP BY ?repo HAVING(COUNT(DISTINCT ?sha) > 1)
