PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo WHERE { ?repo a peo:RepositorySnapshot ; peo:existingStructure ?s . FILTER NOT EXISTS { ?repo peo:desiredStructure ?d } }
