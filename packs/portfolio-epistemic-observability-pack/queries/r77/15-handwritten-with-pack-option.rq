PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo ?path WHERE { ?repo a peo:RepositorySnapshot ; peo:ggenPath ?path ; peo:packCandidate ?p . FILTER(CONTAINS(LCASE(STR(?path)), "handwritten")) }
