PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?visibility (COUNT(?repo) AS ?count) WHERE { ?repo a peo:RepositorySnapshot ; peo:visibility ?visibility } GROUP BY ?visibility
