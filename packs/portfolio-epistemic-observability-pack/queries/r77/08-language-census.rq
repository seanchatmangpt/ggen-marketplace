PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?language (COUNT(?repo) AS ?count) WHERE { ?repo a peo:RepositorySnapshot ; peo:language ?language } GROUP BY ?language ORDER BY DESC(?count)
