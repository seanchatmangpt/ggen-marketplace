PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?pack (COUNT(?repo) AS ?repos) WHERE { ?repo a peo:RepositorySnapshot ; peo:packCandidate ?pack } GROUP BY ?pack ORDER BY DESC(?repos)
