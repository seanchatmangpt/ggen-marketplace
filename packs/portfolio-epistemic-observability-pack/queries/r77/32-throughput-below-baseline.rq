PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?run ?rate ?baseline WHERE { ?run a peo:ScheduleRun ; peo:commitsPerHour ?rate ; peo:baseline ?baseline . FILTER(?rate < ?baseline) }
