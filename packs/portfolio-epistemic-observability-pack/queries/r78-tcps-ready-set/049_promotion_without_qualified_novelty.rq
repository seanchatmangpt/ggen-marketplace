PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?promotion WHERE { ?promotion a peo:StandardWorkPromotion . FILTER NOT EXISTS { ?promotion peo:qualifiedNovelty true } }
