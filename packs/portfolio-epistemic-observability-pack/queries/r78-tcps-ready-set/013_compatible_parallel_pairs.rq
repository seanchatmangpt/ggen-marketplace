PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?a ?b WHERE { ?a a peo:CapitalCandidate ; peo:parallelClass ?class . ?b a peo:CapitalCandidate ; peo:parallelClass ?class . FILTER(STR(?a) < STR(?b)) }
