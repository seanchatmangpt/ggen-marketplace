PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?selected (COUNT(DISTINCT ?alternative) AS ?preservedCount) WHERE { ?selected a peo:CapitalCandidate ; peo:selected true ; peo:preservedAlternative ?alternative . } GROUP BY ?selected
