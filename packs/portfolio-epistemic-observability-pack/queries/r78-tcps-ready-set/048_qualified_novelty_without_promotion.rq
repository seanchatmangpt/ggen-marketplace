PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?return WHERE { ?return a peo:UpstreamReturn ; peo:qualifiedNovelty true . FILTER NOT EXISTS { ?promotion a peo:StandardWorkPromotion ; peo:qualifiedNovelty true } }
