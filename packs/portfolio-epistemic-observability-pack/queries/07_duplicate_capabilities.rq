PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?concept (COUNT(DISTINCT ?cap) AS ?copies) WHERE { ?cap a peo:Capability ; skos:exactMatch ?concept . } GROUP BY ?concept HAVING(COUNT(DISTINCT ?cap) > 1)
