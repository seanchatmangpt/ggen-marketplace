PREFIX dct: <http://purl.org/dc/terms/>
PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?doc ?source WHERE { ?doc peo:documents ?source ; dct:modified ?docAt . ?source dct:modified ?sourceAt . FILTER(?sourceAt > ?docAt) }
