PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?method (COUNT(?observation) AS ?observations) WHERE { ?observation a peo:Observation ; peo:observationMethod ?method . } GROUP BY ?method ORDER BY DESC(?observations)
