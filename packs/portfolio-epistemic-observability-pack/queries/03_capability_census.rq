PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo (COUNT(DISTINCT ?cap) AS ?capabilities) WHERE { ?cap a peo:Capability ; peo:repository ?repo . } GROUP BY ?repo ORDER BY DESC(?capabilities)
