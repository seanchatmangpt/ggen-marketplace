PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?artifact ?generatedFrom ?currentSource WHERE { ?artifact peo:generatedFromDigest ?generatedFrom ; peo:currentSourceDigest ?currentSource . FILTER(?generatedFrom != ?currentSource) }
