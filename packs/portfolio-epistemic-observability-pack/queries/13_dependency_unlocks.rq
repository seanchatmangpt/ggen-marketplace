PREFIX prov: <http://www.w3.org/ns/prov#>
PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?subject ?dependency WHERE { ?subject peo:blockedBy ?dependency . ?evidence prov:specializationOf ?dependency ; peo:standing "ALIVE" . }
