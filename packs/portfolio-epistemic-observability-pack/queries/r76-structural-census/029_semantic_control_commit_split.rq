PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT (SUM(IF(?meaningful = true,1,0)) AS ?meaningfulCount) (SUM(IF(?control = true,1,0)) AS ?controlCount) WHERE { ?commit a peo:SemanticCommit ; peo:meaningful ?meaningful ; peo:controlCommit ?control . }
