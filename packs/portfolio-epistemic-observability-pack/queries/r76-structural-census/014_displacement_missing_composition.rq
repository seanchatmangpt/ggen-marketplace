PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate WHERE { ?candidate a peo:DisplacementCandidate ; peo:pack ?pack . FILTER NOT EXISTS { ?candidate peo:composition ?composition } }
