PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?run (COUNT(DISTINCT ?commit) AS ?meaningfulCommits) WHERE { ?commit a peo:SemanticCommit ; peo:meaningful true ; peo:attributedToRun ?run . } GROUP BY ?run
