PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate WHERE { ?candidate a peo:DisplacementCandidate . FILTER NOT EXISTS { ?candidate peo:desiredStructure ?desired } }
