PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?run (COUNT(?commit) AS ?meaningful) (SUM(IF(?ggen = true,1,0)) AS ?ggenCommits) WHERE { ?commit a peo:SemanticCommit ; peo:meaningful true ; peo:ggenInvolved ?ggen ; peo:attributedToRun ?run . } GROUP BY ?run
