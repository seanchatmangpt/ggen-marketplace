PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate ?desired WHERE { ?candidate a peo:DisplacementCandidate ; peo:desiredStructure ?desired . FILTER NOT EXISTS { ?candidate peo:ontologySource ?ontology } }
