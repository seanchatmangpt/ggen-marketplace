PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?commit WHERE { ?commit a peo:SemanticCommit ; peo:meaningful true . FILTER NOT EXISTS { ?commit peo:attributedToRun ?run } }
