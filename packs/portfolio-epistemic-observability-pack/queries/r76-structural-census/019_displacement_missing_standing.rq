PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate WHERE { ?candidate a peo:DisplacementCandidate ; peo:qualificationReceipt ?receipt . FILTER NOT EXISTS { ?candidate peo:standing ?standing } }
