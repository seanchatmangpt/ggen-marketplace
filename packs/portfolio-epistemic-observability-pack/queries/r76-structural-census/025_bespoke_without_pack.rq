PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate ?existing WHERE { ?candidate a peo:DisplacementCandidate ; peo:existingStructure ?existing . FILTER NOT EXISTS { ?candidate peo:pack ?pack } }
