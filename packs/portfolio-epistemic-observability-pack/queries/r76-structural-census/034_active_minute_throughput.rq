PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?run ?rate ?minutes WHERE { ?run a peo:ScheduleRun ; peo:commitsPerHour ?rate ; peo:activeMinutes ?minutes . FILTER(?minutes > 0) }
