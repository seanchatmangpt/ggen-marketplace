PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate WHERE { ?candidate a peo:DisplacementCandidate ; peo:generator ?generator . FILTER NOT EXISTS { ?candidate peo:generatedArtifact ?artifact } }
