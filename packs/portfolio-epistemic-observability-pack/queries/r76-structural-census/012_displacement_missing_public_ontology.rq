PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate WHERE { ?candidate a peo:DisplacementCandidate ; peo:desiredStructure ?desired . FILTER NOT EXISTS { ?candidate peo:ontologySource ?ontology } }
