PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?snapshot WHERE { ?snapshot a peo:RepositorySnapshot ; peo:exactHead ?head . FILTER NOT EXISTS { ?candidate peo:consumer ?snapshot ; peo:qualificationReceipt ?receipt } }
