PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?generator (COUNT(DISTINCT ?consumer) AS ?consumers) WHERE { ?candidate a peo:DisplacementCandidate ; peo:generator ?generator ; peo:consumer ?consumer . } GROUP BY ?generator HAVING(COUNT(DISTINCT ?consumer) > 1)
