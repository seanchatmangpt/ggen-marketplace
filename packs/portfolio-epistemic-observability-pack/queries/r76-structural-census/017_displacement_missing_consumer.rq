PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate WHERE { ?candidate a peo:DisplacementCandidate ; peo:generatedArtifact ?artifact . FILTER NOT EXISTS { ?candidate peo:consumer ?consumer } }
