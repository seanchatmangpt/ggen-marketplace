PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repository (COUNT(DISTINCT ?head) AS ?heads) WHERE { ?snapshot a peo:RepositorySnapshot ; peo:repository ?repository ; peo:exactHead ?head . } GROUP BY ?repository HAVING(COUNT(DISTINCT ?head) > 1)
