PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?pack (COUNT(DISTINCT ?consumer) AS ?consumers) WHERE { ?candidate a peo:DisplacementCandidate ; peo:pack ?pack ; peo:consumer ?consumer . } GROUP BY ?pack HAVING(COUNT(DISTINCT ?consumer) = 1)
