PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate ?consumer WHERE { ?candidate a peo:DisplacementCandidate ; peo:consumer ?consumer . FILTER NOT EXISTS { ?candidate peo:qualificationReceipt ?receipt } }
