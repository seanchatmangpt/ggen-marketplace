PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?observation WHERE { ?observation a peo:StructureObservation . FILTER NOT EXISTS { ?observation peo:existingStructure ?existing } }
