PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?candidate (COUNT(DISTINCT ?consumer) AS ?consumerCount) WHERE { ?candidate a peo:DisplacementCandidate ; peo:consumer ?consumer . } GROUP BY ?candidate
