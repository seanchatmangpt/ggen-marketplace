PREFIX dct: <http://purl.org/dc/terms/>
PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?subject ?observed ?current WHERE { ?subject peo:observedHead ?observed ; peo:currentHead ?current . FILTER(?observed != ?current) }
