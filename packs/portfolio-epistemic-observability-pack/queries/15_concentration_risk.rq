PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?repo (COUNT(?obs) AS ?observations) WHERE { ?obs a peo:Observation ; peo:repository ?repo . } GROUP BY ?repo ORDER BY DESC(?observations)
