PREFIX peo: <https://ggen.dev/ontology/portfolio-epistemic-observability#>
SELECT ?reason (COUNT(?subject) AS ?refusals) WHERE { ?subject peo:standing ?standing ; peo:refusalReason ?reason . FILTER(STRSTARTS(STR(?standing), "REFUSED")) } GROUP BY ?reason ORDER BY DESC(?refusals)
