PREFIX ocp: <https://ggen.dev/ontology/opportunity-currentness#>
SELECT ?authority ?actuation ?network WHERE {
  ocp:evaluator ocp:authority ?authority ; ocp:actuationPerformed ?actuation ; ocp:networkAllowed ?network .
}
