# How to qualify opportunity currentness

Run the three runtime evaluators only on captured immutable inputs. Execute the authority, append-only, and composition fences plus the test suite. A default-head mismatch is not failure when the qualified head is proven contained by the newer default ancestry; otherwise retain PARTIAL_ALIVE. Negative realization latency is a typed refusal. Ledger refresh emits a new fact and never edits prior facts.
