# {{ name }}

{{ description }}

Generated by `ggen pack new {{ name }}` from `ggen-self-pack`. This pack's
structure — `pack.toml`, `ontology.ttl`, `gates/010_required.rq` — was
constructed by the canonical self-pack constructor, not hand-assembled;
edit `ontology.ttl` to add real classes/properties/individuals, and
`gates/010_required.rq` to state what "a valid {{ name }} fact" means
before wiring templates that consume it.
