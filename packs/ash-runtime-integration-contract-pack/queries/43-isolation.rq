PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?isolation_module ?isolation_level WHERE {
  ?integration a rt:Integration ; rt:isolationModule ?isolation_module ; rt:isolationLevel ?isolation_level .
}
ORDER BY ?integration ?isolation_module
