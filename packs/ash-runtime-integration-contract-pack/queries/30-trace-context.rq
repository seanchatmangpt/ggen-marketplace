PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?traceSource ?spanSource WHERE {
  ?integration a rt:Integration ; rt:traceIdSource ?traceSource ; rt:spanIdSource ?spanSource .
}
