PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?event WHERE {
  ?integration a rt:Integration ; rt:telemetryEvent ?event .
  FILTER NOT EXISTS { ?integration rt:correlationIdSource ?correlation }
}
ORDER BY ?integration
