PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?flag WHERE {
  ?integration a rt:Integration ; rt:featureFlag ?flag .
  FILTER NOT EXISTS { ?integration rt:policyModule ?policy }
}
ORDER BY ?integration
