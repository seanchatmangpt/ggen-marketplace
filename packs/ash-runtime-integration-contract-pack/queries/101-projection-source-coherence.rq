PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?projection WHERE {
  ?integration a rt:Integration ; rt:projectionPath ?projection .
  FILTER NOT EXISTS { ?integration rt:sourcePath ?source }
}
ORDER BY ?integration
