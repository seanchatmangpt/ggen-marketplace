PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?key WHERE {
  ?integration a rt:Integration ; rt:idempotencyKey ?key .
  FILTER(STRLEN(STR(?key)) > 0)
}
ORDER BY ?integration ?key
