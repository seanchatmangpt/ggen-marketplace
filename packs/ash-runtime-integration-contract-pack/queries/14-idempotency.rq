PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?idempotency_module WHERE {
  ?integration a rt:Integration ; rt:idempotencyModule ?idempotency_module ; rt:idempotencyKey ?key .
  FILTER(STRLEN(STR(?key)) > 0)
}
ORDER BY ?integration ?idempotency_module
