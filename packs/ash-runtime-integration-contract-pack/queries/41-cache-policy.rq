PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?ttlMs ?scope WHERE {
  ?integration a rt:Integration ; rt:cacheTtlMs ?ttlMs ; rt:cacheScope ?scope .
}
ORDER BY ?integration ?ttlMs ?scope
