PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?code WHERE {
  ?integration a rt:Integration ; rt:refusalCode ?code .
  FILTER NOT EXISTS { ?integration rt:refusalReason ?reason }
}
ORDER BY ?integration
