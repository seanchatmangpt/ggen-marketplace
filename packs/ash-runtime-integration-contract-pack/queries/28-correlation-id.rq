PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?correlation_module ?correlation_source WHERE {
  ?integration a rt:Integration ; rt:correlationModule ?correlation_module ; rt:correlationIdSource ?correlation_source .
}
ORDER BY ?integration ?correlation_module
