PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?header ?required WHERE {
  ?integration a rt:Integration ; rt:correlationIdSource ?header ; rt:correlationIdRequired ?required .
}
ORDER BY ?integration ?header ?required
