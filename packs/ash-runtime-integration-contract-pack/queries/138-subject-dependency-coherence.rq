PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?dependency WHERE {
  ?integration a rt:Integration ; rt:hasDependencyDigest ?dependency .
  FILTER NOT EXISTS { ?integration rt:hasSubject ?subject }
}
ORDER BY ?integration
