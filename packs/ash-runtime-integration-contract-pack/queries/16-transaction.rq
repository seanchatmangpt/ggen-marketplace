PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?transaction ?boundary WHERE {
  ?integration a rt:Integration ; rt:transactionStrategy ?transaction ; rt:transactionBoundary ?boundary .
}
