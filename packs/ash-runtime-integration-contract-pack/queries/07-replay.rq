PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?replay ?key WHERE {
  ?integration a rt:Integration ; rt:replayActivity ?replay ; rt:hasReplayKey ?key .
  FILTER(STRLEN(STR(?key)) > 0)
}
ORDER BY ?integration ?replay ?key
