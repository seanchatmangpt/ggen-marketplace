PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?replay_module WHERE {
  ?integration a rt:Integration ; rt:replayModule ?replay_module ; rt:hasReplayKey ?key .
  FILTER(STRLEN(STR(?key)) > 0)
}
ORDER BY ?integration ?replay_module
