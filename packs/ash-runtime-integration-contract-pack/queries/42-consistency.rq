PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?readMode ?writeMode WHERE {
  ?integration a rt:Integration ; rt:readConsistency ?readMode ; rt:writeConsistency ?writeMode .
}
ORDER BY ?integration ?readMode ?writeMode
