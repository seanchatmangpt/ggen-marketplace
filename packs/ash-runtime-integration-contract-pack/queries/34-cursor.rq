PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?field ?direction WHERE {
  ?integration a rt:Integration ; rt:cursorField ?field ; rt:cursorDirection ?direction .
}
ORDER BY ?integration ?field ?direction
