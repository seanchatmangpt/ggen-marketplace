PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?policy WHERE {
  ?integration a rt:Integration ; rt:policyModule ?policy ; rt:tenantRequired true .
  FILTER NOT EXISTS { ?integration rt:tenantSource ?tenant }
}
ORDER BY ?integration
