PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?batch ?max WHERE {
  ?integration a rt:Integration ; rt:maxBatchSize ?batch ; rt:maxLimit ?max .
  FILTER(?batch < 1 || ?batch > ?max)
}
ORDER BY ?integration
