PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?capacity ?keySource WHERE {
  ?integration a rt:Integration ; rt:rateLimitCapacity ?capacity ; rt:rateLimitKeySource ?keySource .
  FILTER(?capacity > 0)
  FILTER NOT EXISTS { ?integration rt:actorSource ?actorSource }
}
ORDER BY ?integration
