PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?deadline WHERE {
  ?integration a rt:Integration ; rt:deadlineSource ?deadline .
  FILTER NOT EXISTS { ?integration rt:cancellationTokenSource ?token }
}
ORDER BY ?integration
