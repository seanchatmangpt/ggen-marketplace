PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?normalizer WHERE {
  ?integration a rt:Integration ; rt:inputNormalizer ?normalizer .
  FILTER NOT EXISTS { ?integration rt:inputSchema ?schema }
}
ORDER BY ?integration
