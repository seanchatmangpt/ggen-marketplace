PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?eventType WHERE {
  ?integration a rt:Integration ; rt:eventTypeField ?eventType .
  FILTER NOT EXISTS { ?integration rt:eventTimeField ?eventTime }
}
ORDER BY ?integration
