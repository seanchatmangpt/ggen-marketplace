PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?reactor WHERE {
  ?integration a rt:Integration ; rt:reactorModule ?reactor ; rt:transactionBoundary ?boundary .
  FILTER NOT EXISTS { ?integration rt:compensationAction ?compensation }
}
ORDER BY ?integration
