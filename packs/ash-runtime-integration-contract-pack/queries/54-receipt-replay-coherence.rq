PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?receipt WHERE {
  ?integration a rt:Integration ; rt:hasReceipt ?receipt .
  FILTER NOT EXISTS { ?integration rt:hasReplayKey ?replay }
}
ORDER BY ?integration
