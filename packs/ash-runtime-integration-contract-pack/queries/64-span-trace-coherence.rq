PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?span WHERE {
  ?integration a rt:Integration ; rt:spanIdSource ?span .
  FILTER NOT EXISTS { ?integration rt:traceIdSource ?trace }
}
ORDER BY ?integration
