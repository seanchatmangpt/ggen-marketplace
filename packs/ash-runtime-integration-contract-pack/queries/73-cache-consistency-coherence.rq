PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?ttl WHERE {
  ?integration a rt:Integration ; rt:cacheTtlMs ?ttl ; rt:readConsistency ?consistency .
  FILTER(?ttl > 0 && LCASE(STR(?consistency)) = "strong")
}
ORDER BY ?integration
