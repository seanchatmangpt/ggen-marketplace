PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?action WHERE {
  ?integration a rt:Integration ; rt:actionName ?action ; rt:maxAttempts ?attempts .
  FILTER(?attempts > 1)
  FILTER NOT EXISTS { ?integration rt:idempotencyKey ?key }
}
ORDER BY ?integration
