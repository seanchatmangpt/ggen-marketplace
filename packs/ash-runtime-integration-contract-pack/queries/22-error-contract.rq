PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?errorModule ?errorCode WHERE {
  ?integration a rt:Integration ; rt:errorModule ?errorModule ; rt:errorCode ?errorCode .
  FILTER(STRLEN(STR(?errorCode)) > 0)
}
