PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?cli WHERE {
  ?integration a rt:Integration ; rt:cliModule ?cli .
  FILTER NOT EXISTS { ?integration rt:refusalCode ?code }
}
ORDER BY ?integration
