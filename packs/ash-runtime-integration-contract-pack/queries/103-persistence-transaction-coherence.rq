PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?repository WHERE {
  ?integration a rt:Integration ; rt:repositoryModule ?repository .
  FILTER NOT EXISTS { ?integration rt:transactionBoundary ?boundary }
}
ORDER BY ?integration
