PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?resource ?action WHERE {
  ?integration a rt:Integration ; rt:resourceModule ?resource ; rt:actionName ?action .
  FILTER(STRLEN(STR(?resource)) > 0 && STRLEN(STR(?action)) > 0)
}
