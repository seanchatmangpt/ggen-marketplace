PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration WHERE {
  ?integration a rt:Integration ; rt:correlationIdRequired true .
  FILTER NOT EXISTS { ?integration rt:correlationIdSource ?source }
}
ORDER BY ?integration
