PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?chunkSize ?backpressure WHERE {
  ?integration a rt:Integration ; rt:streamChunkSize ?chunkSize ; rt:backpressureMode ?backpressure .
}
ORDER BY ?integration ?chunkSize ?backpressure
