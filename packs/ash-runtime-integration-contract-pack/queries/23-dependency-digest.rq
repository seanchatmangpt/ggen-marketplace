PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?digest WHERE {
  ?integration a rt:Integration ; rt:hasDependencyDigest ?digest .
  FILTER(STRLEN(STR(?digest)) >= 7)
}

ORDER BY ?integration ?digest
