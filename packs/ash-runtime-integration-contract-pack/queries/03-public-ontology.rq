PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
PREFIX dct: <http://purl.org/dc/terms/>
SELECT ?integration ?ontology WHERE {
  ?integration a rt:Integration ; dct:conformsTo ?ontology .
  FILTER(STRSTARTS(STR(?ontology), "http"))
}

ORDER BY ?integration ?ontology
