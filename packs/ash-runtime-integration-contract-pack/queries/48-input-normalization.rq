PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?normalizer ?mode WHERE {
  ?integration a rt:Integration ; rt:inputNormalizer ?normalizer ; rt:normalizationMode ?mode .
}

ORDER BY ?integration ?normalizer ?mode
