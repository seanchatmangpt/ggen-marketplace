PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?policyModule ?contextSource WHERE {
  ?integration a rt:Integration ; rt:policyModule ?policyModule ; rt:policyContextSource ?contextSource .
}

ORDER BY ?integration ?policyModule ?contextSource
