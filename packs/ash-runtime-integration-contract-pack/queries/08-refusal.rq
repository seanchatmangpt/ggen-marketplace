PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?code ?reason WHERE {
  ?integration a rt:Integration ; rt:refusalCode ?code ; rt:refusalReason ?reason .
  FILTER(STRLEN(STR(?code)) > 0 && STRLEN(STR(?reason)) > 0)
}
ORDER BY ?integration ?code ?reason
