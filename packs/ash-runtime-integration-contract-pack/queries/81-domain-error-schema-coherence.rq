PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?errorCode WHERE {
  ?integration a rt:Integration ; rt:domainErrorCodeField ?errorCode .
  FILTER NOT EXISTS { ?integration rt:errorModule ?errorModule }
}
ORDER BY ?integration
