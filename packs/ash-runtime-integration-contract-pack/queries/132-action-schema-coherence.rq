PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?action WHERE {
  ?integration a rt:Integration ; rt:actionName ?action .
  FILTER NOT EXISTS { ?integration rt:inputSchema ?input }
  FILTER NOT EXISTS { ?integration rt:outputSchema ?output }
}
ORDER BY ?integration
