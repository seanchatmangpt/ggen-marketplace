PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?concurrent ?queue WHERE {
  ?integration a rt:Integration ; rt:maxConcurrent ?concurrent ; rt:queueLimit ?queue .
  FILTER(?concurrent < 1 || ?queue < 0)
}
ORDER BY ?integration
