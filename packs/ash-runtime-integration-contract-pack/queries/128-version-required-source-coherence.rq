PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration WHERE {
  ?integration a rt:Integration ; rt:versionRequired true .
  FILTER NOT EXISTS { ?integration rt:versionSource ?source }
}
ORDER BY ?integration
