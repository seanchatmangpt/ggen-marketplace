PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?createdAt WHERE {
  ?integration a rt:Integration ; rt:createdAtField ?createdAt .
  FILTER NOT EXISTS { ?integration rt:updatedAtField ?updatedAt }
}
ORDER BY ?integration
