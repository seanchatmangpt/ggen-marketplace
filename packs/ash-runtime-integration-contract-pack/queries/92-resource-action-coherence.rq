PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?resource WHERE {
  ?integration a rt:Integration ; rt:resourceModule ?resource .
  FILTER NOT EXISTS { ?integration rt:actionName ?action }
}
ORDER BY ?integration
