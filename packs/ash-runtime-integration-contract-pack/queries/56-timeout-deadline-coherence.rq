PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?timeout ?deadline WHERE {
  ?integration a rt:Integration ; rt:timeoutMs ?timeout ; rt:deadlineSource ?deadline .
  FILTER(?timeout <= 0 || STRLEN(STR(?deadline)) = 0)
}
ORDER BY ?integration
