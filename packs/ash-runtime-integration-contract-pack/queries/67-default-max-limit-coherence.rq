PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?default ?max WHERE {
  ?integration a rt:Integration ; rt:defaultLimit ?default ; rt:maxLimit ?max .
  FILTER(?default < 1 || ?max < ?default)
}
ORDER BY ?integration
