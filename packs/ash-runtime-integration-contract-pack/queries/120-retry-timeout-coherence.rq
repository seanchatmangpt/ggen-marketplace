PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?attempts WHERE {
  ?integration a rt:Integration ; rt:maxAttempts ?attempts .
  FILTER(?attempts > 1)
  FILTER NOT EXISTS { ?integration rt:timeoutMs ?timeout }
}
ORDER BY ?integration
