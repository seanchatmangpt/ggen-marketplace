PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?source ?projection WHERE {
  ?integration a rt:Integration ; rt:sourcePath ?source ; rt:projectionPath ?projection .
  FILTER(?source != ?projection)
}
