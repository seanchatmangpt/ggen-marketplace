PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?tenantSource WHERE {
  ?integration a rt:Integration ; rt:tenantRequired true ; rt:tenantSource ?tenantSource .
  FILTER NOT EXISTS { ?integration rt:policyContextSource ?policyContext }
}
ORDER BY ?integration
