PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration WHERE {
  ?integration a rt:Integration ; rt:tenantRequired true .
  FILTER NOT EXISTS { ?integration rt:tenantSource ?source }
}
ORDER BY ?integration
