PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?lockField WHERE {
  ?integration a rt:Integration ; rt:optimisticLockField ?lockField .
  FILTER NOT EXISTS { ?integration rt:versionSource ?versionSource }
}
ORDER BY ?integration
