PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?route WHERE {
  ?integration a rt:Integration ; rt:route ?route .
  FILTER NOT EXISTS { ?integration rt:httpMethod ?method }
}
ORDER BY ?integration
