PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?cancellation_module ?cancellation_source WHERE {
  ?integration a rt:Integration ; rt:cancellationModule ?cancellation_module ; rt:cancellationTokenSource ?cancellation_source .
}
ORDER BY ?integration ?cancellation_module
