PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?tokenSource WHERE {
  ?integration a rt:Integration ; rt:cancellationTokenSource ?tokenSource .
}
ORDER BY ?integration ?tokenSource
