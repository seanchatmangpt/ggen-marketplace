PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?since WHERE {
  ?integration a rt:Integration ; rt:deprecatedSince ?since .
  FILTER NOT EXISTS { ?integration rt:versionSource ?version }
}
ORDER BY ?integration
