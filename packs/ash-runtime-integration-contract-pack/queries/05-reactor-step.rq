PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?reactor ?step ?stepModule WHERE {
  ?integration a rt:Integration ; rt:reactorModule ?reactor ; rt:hasStep ?step .
  ?step rt:stepModule ?stepModule .
}
