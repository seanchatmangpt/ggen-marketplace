PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?capacity ?window WHERE {
  ?integration a rt:Integration ; rt:rateLimitCapacity ?capacity ; rt:rateLimitWindowMs ?window .
  FILTER(?capacity < 1 || ?window < 1)
}
ORDER BY ?integration
