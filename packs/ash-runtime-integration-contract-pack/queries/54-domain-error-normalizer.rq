PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?domainErrorNormalizerModule WHERE {
  ?integration a rt:Integration ; rt:domainErrorNormalizerModule ?domainErrorNormalizerModule .
}
ORDER BY ?integration ?domainErrorNormalizerModule
