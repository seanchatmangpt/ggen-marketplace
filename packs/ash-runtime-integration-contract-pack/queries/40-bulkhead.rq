PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?maxConcurrent ?queueLimit WHERE {
  ?integration a rt:Integration ; rt:maxConcurrent ?maxConcurrent ; rt:queueLimit ?queueLimit .
}
