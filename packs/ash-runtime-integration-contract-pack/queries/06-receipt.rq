PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
PREFIX prov: <http://www.w3.org/ns/prov#>
SELECT ?integration ?receipt ?activity WHERE {
  ?integration a rt:Integration ; rt:hasReceipt ?receipt .
  ?receipt a rt:Receipt ; prov:wasGeneratedBy ?activity .
}
