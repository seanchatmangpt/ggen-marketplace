PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?threshold ?attempts WHERE {
  ?integration a rt:Integration ; rt:circuitFailureThreshold ?threshold ; rt:maxAttempts ?attempts .
  FILTER(?threshold < 1 || ?attempts < 1)
}
ORDER BY ?integration
