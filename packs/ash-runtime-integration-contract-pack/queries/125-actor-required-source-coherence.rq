PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration WHERE {
  ?integration a rt:Integration ; rt:actorRequired true .
  FILTER NOT EXISTS { ?integration rt:actorSource ?source }
}
ORDER BY ?integration
