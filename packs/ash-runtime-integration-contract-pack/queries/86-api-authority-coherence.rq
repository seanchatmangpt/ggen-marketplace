PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?api WHERE {
  ?integration a rt:Integration ; rt:apiModule ?api .
  FILTER NOT EXISTS { ?integration rt:hasAuthorityPolicy ?authority }
}
ORDER BY ?integration
