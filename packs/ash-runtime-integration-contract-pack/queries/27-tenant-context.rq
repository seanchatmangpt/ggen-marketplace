PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?tenantSource ?required WHERE {
  ?integration a rt:Integration ; rt:tenantSource ?tenantSource ; rt:tenantRequired ?required .
}
