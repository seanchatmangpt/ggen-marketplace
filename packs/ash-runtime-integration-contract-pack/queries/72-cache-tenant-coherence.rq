PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?scope WHERE {
  ?integration a rt:Integration ; rt:cacheScope ?scope ; rt:tenantRequired true .
  FILTER(LCASE(STR(?scope)) = "global")
}
ORDER BY ?integration
