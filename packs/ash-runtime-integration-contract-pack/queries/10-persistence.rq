PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?repoModule ?dataLayer WHERE {
  ?integration a rt:Integration ; rt:repositoryModule ?repoModule ; rt:dataLayer ?dataLayer .
}
ORDER BY ?integration ?repoModule ?dataLayer
