PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?action WHERE {
  ?integration a rt:Integration ; rt:actionName ?action ; rt:hasReceipt ?receipt .
  FILTER NOT EXISTS { ?integration rt:hasReplayKey ?replayKey }
}
ORDER BY ?integration
