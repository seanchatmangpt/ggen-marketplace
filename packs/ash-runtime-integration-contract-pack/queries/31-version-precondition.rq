PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?source ?required WHERE {
  ?integration a rt:Integration ; rt:versionSource ?source ; rt:versionRequired ?required .
}
ORDER BY ?integration ?source ?required
