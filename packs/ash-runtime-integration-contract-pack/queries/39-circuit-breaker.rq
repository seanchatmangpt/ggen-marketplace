PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?threshold ?resetMs WHERE {
  ?integration a rt:Integration ; rt:circuitFailureThreshold ?threshold ; rt:circuitResetMs ?resetMs .
}
ORDER BY ?integration ?threshold ?resetMs
