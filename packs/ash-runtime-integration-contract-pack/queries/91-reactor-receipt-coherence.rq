PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?reactor WHERE {
  ?integration a rt:Integration ; rt:reactorModule ?reactor .
  FILTER NOT EXISTS { ?integration rt:hasReceipt ?receipt }
}
ORDER BY ?integration
