PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?integration ?timeout WHERE {
  ?integration a rt:Integration ; rt:timeoutMs ?timeout .
  FILTER(xsd:integer(?timeout) > 0)
}

ORDER BY ?integration ?timeout
