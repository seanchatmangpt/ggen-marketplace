PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?write WHERE {
  ?integration a rt:Integration ; rt:writeConsistency ?write .
  FILTER NOT EXISTS { ?integration rt:isolationLevel ?isolation }
}
ORDER BY ?integration
