PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?maxLimit WHERE {
  ?integration a rt:Integration ; rt:maxLimit ?maxLimit .
  FILTER(?maxLimit > 0)
  FILTER NOT EXISTS { ?integration rt:cursorField ?cursor }
}
ORDER BY ?integration
