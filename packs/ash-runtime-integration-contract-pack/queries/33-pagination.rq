PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?defaultLimit ?maxLimit WHERE {
  ?integration a rt:Integration ; rt:defaultLimit ?defaultLimit ; rt:maxLimit ?maxLimit .
}
ORDER BY ?integration ?defaultLimit ?maxLimit
