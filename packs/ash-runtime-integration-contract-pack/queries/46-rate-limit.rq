PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?capacity ?windowMs ?keySource WHERE {
  ?integration a rt:Integration ; rt:rateLimitCapacity ?capacity ; rt:rateLimitWindowMs ?windowMs ; rt:rateLimitKeySource ?keySource .
}

ORDER BY ?integration ?capacity ?windowMs ?keySource
