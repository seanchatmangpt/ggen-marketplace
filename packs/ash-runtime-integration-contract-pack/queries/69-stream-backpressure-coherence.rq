PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?chunk WHERE {
  ?integration a rt:Integration ; rt:streamChunkSize ?chunk .
  FILTER(?chunk > 0)
  FILTER NOT EXISTS { ?integration rt:backpressureMode ?mode }
}
ORDER BY ?integration
