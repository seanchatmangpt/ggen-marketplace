PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?outputSchema WHERE {
  ?integration a rt:Integration ; rt:outputSchema ?outputSchema .
}

ORDER BY ?integration ?outputSchema
