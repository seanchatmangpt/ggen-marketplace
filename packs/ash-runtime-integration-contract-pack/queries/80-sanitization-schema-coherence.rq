PREFIX rt: <https://ggen.dev/ontology/runtime-integration#>
SELECT ?integration ?sanitizer WHERE {
  ?integration a rt:Integration ; rt:outputSanitizer ?sanitizer .
  FILTER NOT EXISTS { ?integration rt:outputSchema ?schema }
}
ORDER BY ?integration
