defmodule {{ idempotency_module }} do
  @moduledoc false

  def key(subject, action, payload) do
    :crypto.hash(:sha256, :erlang.term_to_binary({subject, action, payload}))
    |> Base.encode16(case: :lower)
  end
end
