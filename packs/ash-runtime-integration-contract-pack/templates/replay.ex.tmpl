defmodule {{ replay_module }} do
  @moduledoc false

  def replay(receipt, fun) when is_function(fun, 1) do
    case Map.fetch(receipt, :replay_key) do
      {:ok, key} when not is_nil(key) -> fun.(key)
      _ -> {:error, {:refused, :missing_replay_key}}
    end
  end
end
