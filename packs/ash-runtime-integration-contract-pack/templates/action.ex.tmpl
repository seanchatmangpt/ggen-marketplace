defmodule {{ action_module }} do
  @moduledoc false

  def run(resource, action, input, opts \\ []) do
    resource
    |> Ash.Changeset.for_action(action, input, opts)
    |> Ash.run_action()
  end
end
