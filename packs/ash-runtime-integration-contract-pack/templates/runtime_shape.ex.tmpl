defmodule {{ module }} do
  @moduledoc "Generated projection of one admitted runtime semantic shape."

  alias GgenIgniter.RuntimeShape

  @spec shape() :: RuntimeShape.t()
  def shape do
    RuntimeShape.new!(%{
      subject_id: "{{ subject_id }}",
      source_digest: "{{ source_digest }}",
      graph_digest: "{{ graph_digest }}",
      admission: %{"status" => "{{ admission_status }}"}
    })
  end
end
