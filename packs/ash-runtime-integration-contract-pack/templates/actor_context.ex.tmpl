defmodule {{ actor_context_module }} do
  @source {{ actor_source | json_encode }}
  @required {{ actor_required }}

  def resolve(context) do
    case Map.get(context, @source) do
      nil when @required -> {:error, :REFUSED_ACTOR_CONTEXT_MISSING}
      actor -> {:ok, actor}
    end
  end
end
