defmodule {{ rate_limit_module }} do
  @capacity {{ rate_limit_capacity }}
  @window_ms {{ rate_limit_window_ms }}
  @key_source {{ rate_limit_key_source | json_encode }}

  def contract, do: %{capacity: @capacity, window_ms: @window_ms, key_source: @key_source}
end
