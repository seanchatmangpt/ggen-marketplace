defmodule {{ input_normalization_module }} do
  @mode {{ normalization_mode | json_encode }}
  @normalizer {{ input_normalizer | json_encode }}

  def contract, do: %{mode: @mode, normalizer: @normalizer}
end
