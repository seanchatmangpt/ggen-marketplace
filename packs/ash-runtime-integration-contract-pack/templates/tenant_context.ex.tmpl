defmodule {{ tenant_context_module }} do
  @source {{ tenant_source | json_encode }}
  @required {{ tenant_required }}

  def resolve(context) do
    case Map.get(context, @source) do
      nil when @required -> {:error, :REFUSED_TENANT_CONTEXT_MISSING}
      tenant -> {:ok, tenant}
    end
  end
end
