defmodule {{ pagination_module }} do
  @default {{ default_limit }}
  @max {{ max_limit }}

  def normalize(nil), do: @default
  def normalize(limit) when is_integer(limit) and limit > 0, do: min(limit, @max)
  def normalize(_), do: {:error, :REFUSED_INVALID_PAGE_LIMIT}
end
