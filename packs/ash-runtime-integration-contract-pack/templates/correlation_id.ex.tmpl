defmodule {{ correlation_module }} do
  @source {{ correlation_source | json_encode }}

  def resolve(context) do
    case Map.get(context, @source) do
      nil -> {:error, :REFUSED_CORRELATION_ID_MISSING}
      value -> {:ok, value}
    end
  end
end
