defmodule {{ repository_module }} do
  @moduledoc false

  def get(resource, id), do: Ash.get(resource, id)
  def read(query), do: Ash.read(query)
  def transact(fun) when is_function(fun, 0), do: Ash.DataLayer.transaction([], fun)
end
