defmodule {{ config_module }} do
  @moduledoc false
  @digest {{ config_digest | json_encode }}
  @config {{ runtime_config | json_encode }}

  def admitted, do: %{digest: @digest, config: @config}
  def matches?(digest), do: digest == @digest
end
