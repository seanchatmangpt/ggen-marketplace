PREFIX r90: <https://ggen.dev/ontology/r90#>
ASK {
  FILTER NOT EXISTS {
    ?t a r90:FanoutTarget ; r90:compatibilityState ?s .
    FILTER(?s = "COMPATIBLE_READY" || ?s = "COMPATIBLE_ADMITTED")
    FILTER NOT EXISTS { ?t r90:exactConsumerHead ?h . FILTER(STRLEN(?h)=40) }
  }
}
