PREFIX r90: <https://ggen.dev/ontology/r90#>
SELECT ?family (COUNT(?t) AS ?targets) (SUM(IF(?state="DOWNSTREAM_REALIZED",1,0)) AS ?realized)
WHERE {
  ?t a r90:FanoutTarget ; r90:consumerFamily ?family ; r90:fanoutState ?state .
}
GROUP BY ?family
ORDER BY ?family
