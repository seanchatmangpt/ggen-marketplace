PREFIX r90: <https://ggen.dev/ontology/r90#>
SELECT ?family WHERE {
  ?t a r90:FanoutTarget ; r90:consumerFamily ?family .
  FILTER NOT EXISTS { ?x a r90:FanoutTarget ; r90:consumerFamily ?family ; r90:fanoutState "DOWNSTREAM_REALIZED" }
}
GROUP BY ?family
ORDER BY ?family
