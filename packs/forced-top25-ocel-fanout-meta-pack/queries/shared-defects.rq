PREFIX r90: <https://ggen.dev/ontology/r90#>
SELECT ?blocker (COUNT(?t) AS ?affected) WHERE {
  ?t a r90:FanoutTarget ; r90:blocker ?blocker .
  FILTER(STRLEN(?blocker)>0)
}
GROUP BY ?blocker
HAVING(COUNT(?t)>1)
ORDER BY DESC(?affected)
