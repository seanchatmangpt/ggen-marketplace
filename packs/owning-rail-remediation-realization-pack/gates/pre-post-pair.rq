PREFIX orr: <https://ggen.io/ontology/owning-rail-remediation-realization#>
ASK WHERE { ?o a orr:Observation ; orr:preState ?before ; orr:postState ?after ; orr:observedAt ?t . FILTER(str(?before) != "" && str(?after) != "") }
