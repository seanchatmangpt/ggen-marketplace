PREFIX rr: <https://seanchatmangpt.dev/ontology/remediation-realization#>
SELECT ?observation WHERE {
  ?observation a rr:PostObservation ; rr:claimedStanding "ALIVE" ; rr:blockerCount ?count .
  FILTER(?count > 0)
}
