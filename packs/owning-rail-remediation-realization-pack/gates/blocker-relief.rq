PREFIX orr: <https://ggen.io/ontology/owning-rail-remediation-realization#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
ASK WHERE { ?r a orr:Realization ; orr:blockerBefore ?before ; orr:blockerAfter ?after . FILTER(xsd:integer(?after) <= xsd:integer(?before)) }
