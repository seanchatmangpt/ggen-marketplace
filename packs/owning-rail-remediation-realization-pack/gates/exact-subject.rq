PREFIX orr: <https://ggen.io/ontology/owning-rail-remediation-realization#>
ASK WHERE {
  ?subject orr:subjectSha ?sha ; orr:semanticDigest ?digest .
  FILTER(regex(str(?sha), "^[0-9a-f]{40}$") && regex(str(?digest), "^[0-9a-f]{64}$"))
}
