PREFIX orr: <https://ggen.io/ontology/owning-rail-remediation-realization#>
SELECT ?realization ?before ?after WHERE { ?realization a orr:Realization ; orr:blockerBefore ?before ; orr:blockerAfter ?after . } ORDER BY ?realization
