PREFIX orr: <https://ggen.io/ontology/owning-rail-remediation-realization#>
SELECT ?realization ?state WHERE { ?realization a orr:Realization ; orr:realizationState ?state . FILTER(?state != "REALIZED") } ORDER BY ?state ?realization
