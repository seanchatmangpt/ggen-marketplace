# Generated ERRC Innovation Contract

Generated from `ontology.ttl`; do not hand-edit this projection.

{% for row in results %}## {{ row["order"] }}. {{ row["label"] }}

{{ row["description"] }}

Action: `{{ row["action"] }}`{% if row["requiresExperiment"] %} · experiment required{% endif %}{% if row["requiresFalsifier"] %} · falsifier required{% endif %}

{% endfor %}
Every admitted grid must contain all four actions, assign each factor once, preserve alternatives, bind observed/verified metrics to sources, and keep actuation false. CREATE remains a hypothesis until its experiment and falsifier are executed against an exact subject.
