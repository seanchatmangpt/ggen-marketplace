PREFIX cv: <https://chatmangpt.com/ontology/challenger-value#>
PREFIX dcterms: <http://purl.org/dc/terms/>
SELECT ?buyer ?tension ?diagnostic
WHERE {
  ?p a cv:BuyerProjection ;
     dcterms:identifier ?buyer ;
     cv:tension ?tension ;
     cv:diagnostic ?diagnostic .
}
ORDER BY ?buyer
