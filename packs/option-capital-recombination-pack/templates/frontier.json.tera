{
  "schema": "ggen.option-capital-frontier/v1",
  "authority": "SELECT",
  "candidates": [{% for row in rows %}{"id": {{ row.candidate | json_encode }}, "reversibility": {{ row.reversibility | json_encode }}}{% if not loop.last %},{% endif %}{% endfor %}]
}
