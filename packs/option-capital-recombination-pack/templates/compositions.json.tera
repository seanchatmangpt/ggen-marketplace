{
  "schema": "ggen.option-capital-compositions/v1",
  "authority": "CONSTRUCT",
  "compositions": [{% for row in rows %}{"id": {{ row.composition | json_encode }}, "source": {{ row.source | json_encode }}, "target": {{ row.target | json_encode }}}{% if not loop.last %},{% endif %}{% endfor %}]
}
