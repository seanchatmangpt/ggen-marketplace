PREFIX ocr: <https://ggen.dev/ontology/option-capital-recombination#>
SELECT ?candidate WHERE { ?candidate a ocr:Candidate . FILTER NOT EXISTS { ?other a ocr:Candidate ; ocr:dominates ?candidate . FILTER(?other != ?candidate) } } ORDER BY STR(?candidate)
