PREFIX ocr: <https://ggen.dev/ontology/option-capital-recombination#>
SELECT ?candidate (COUNT(DISTINCT ?repo) AS ?repositories) WHERE { ?candidate a ocr:Candidate ; ocr:targetsRepository ?repo . } GROUP BY ?candidate ORDER BY DESC(?repositories) STR(?candidate)
