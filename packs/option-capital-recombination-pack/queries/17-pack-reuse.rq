PREFIX ocr: <https://ggen.dev/ontology/option-capital-recombination#>
SELECT ?pack (COUNT(DISTINCT ?candidate) AS ?reuse) WHERE { ?candidate a ocr:Candidate ; ocr:usesPack ?pack . } GROUP BY ?pack ORDER BY DESC(?reuse) STR(?pack)
