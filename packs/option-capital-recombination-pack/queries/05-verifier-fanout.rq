PREFIX ocr: <https://ggen.dev/ontology/option-capital-recombination#>
SELECT ?candidate (COUNT(DISTINCT ?verifier) AS ?verifierCount) WHERE { ?candidate a ocr:Candidate ; ocr:verifiedBy ?verifier . } GROUP BY ?candidate ORDER BY DESC(?verifierCount) STR(?candidate)
