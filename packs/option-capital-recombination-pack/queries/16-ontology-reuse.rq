PREFIX ocr: <https://ggen.dev/ontology/option-capital-recombination#>
SELECT ?ontology (COUNT(DISTINCT ?candidate) AS ?reuse) WHERE { ?candidate a ocr:Candidate ; ocr:usesOntology ?ontology . } GROUP BY ?ontology ORDER BY DESC(?reuse) STR(?ontology)
