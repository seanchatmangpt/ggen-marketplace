PREFIX ocr: <https://ggen.dev/ontology/option-capital-recombination#>
PREFIX prov: <http://www.w3.org/ns/prov#>
SELECT ?candidate ?memory WHERE { ?candidate a ocr:Candidate ; prov:wasDerivedFrom ?memory . FILTER(CONTAINS(STR(?memory), "dfcm/")) } ORDER BY STR(?memory) STR(?candidate)
