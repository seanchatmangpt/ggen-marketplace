PREFIX ocr: <https://ggen.dev/ontology/option-capital-recombination#>
SELECT ?candidate (COUNT(DISTINCT ?unlocked) AS ?delta) WHERE { ?candidate a ocr:Candidate ; ocr:unlocks ?unlocked . } GROUP BY ?candidate ORDER BY DESC(?delta) STR(?candidate)
