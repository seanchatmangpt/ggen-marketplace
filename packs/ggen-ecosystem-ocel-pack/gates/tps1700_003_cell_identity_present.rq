# MESSAGE: production evidence without cell identity cannot be attributed to the TPS line.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE {
  ?run a geocel:ManufacturingRun .
  FILTER NOT EXISTS { ?run geocel:cell ?cell }
}
