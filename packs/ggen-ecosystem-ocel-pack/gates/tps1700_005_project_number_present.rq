# MESSAGE: canonical Project state must name the project number used by the run.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE {
  ?run a geocel:ManufacturingRun .
  FILTER NOT EXISTS { ?run geocel:projectNumber ?number }
}
