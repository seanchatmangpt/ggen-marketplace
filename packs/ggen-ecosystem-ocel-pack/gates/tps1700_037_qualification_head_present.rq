# MESSAGE: qualification standing must bind the exact consumer head actually exercised.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:QualificationObject .
  FILTER NOT EXISTS { ?object geocel:exactHead ?head }
}
