PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?run WHERE { ?run a geocel:ManufacturingRun ; geocel:admittedCount ?admitted ; geocel:wipLimit ?limit . FILTER(xsd:integer(?admitted) > xsd:integer(?limit)) } LIMIT 1
