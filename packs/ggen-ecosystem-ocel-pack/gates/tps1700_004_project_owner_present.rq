# MESSAGE: Project2-backed manufacturing must bind the owning account explicitly.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE {
  ?run a geocel:ManufacturingRun .
  FILTER NOT EXISTS { ?run geocel:projectOwner ?owner }
}
