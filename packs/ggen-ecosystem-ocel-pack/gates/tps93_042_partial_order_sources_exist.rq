# TPS93: dependency sources must resolve to manufacturing objects.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?a ?b WHERE { ?a geocel:precedes ?b . FILTER NOT EXISTS { ?a a geocel:ManufacturingObject . } }