# TPS93: Project/control objects cannot masquerade as downstream repository production.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?o WHERE { ?o a geocel:ProjectMemoryObject , geocel:RepositoryObject . }