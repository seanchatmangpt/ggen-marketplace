# TPS93: every CELL1 run requires explicit standing.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE { ?run a geocel:ManufacturingRun ; geocel:cell "CELL1" . FILTER NOT EXISTS { ?run geocel:standing ?s . FILTER(STRLEN(STR(?s)) > 0) } }