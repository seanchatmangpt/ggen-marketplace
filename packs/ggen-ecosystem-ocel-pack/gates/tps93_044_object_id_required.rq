# TPS93: every manufacturing object requires stable objectId.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?o WHERE { ?o a geocel:ManufacturingObject . FILTER NOT EXISTS { ?o geocel:objectId ?id . FILTER(STRLEN(STR(?id)) > 0) } }