# TPS93: every repository object requires explicit standing.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?r WHERE { ?r a geocel:RepositoryObject . FILTER NOT EXISTS { ?r geocel:standing ?s . FILTER(STRLEN(STR(?s)) > 0) } }