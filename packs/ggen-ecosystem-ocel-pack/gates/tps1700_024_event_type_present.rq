# MESSAGE: manufacturing events without a typed transition cannot enter OCEL replay.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?event WHERE {
  ?event a geocel:ManufacturingEvent .
  FILTER NOT EXISTS { ?event geocel:eventType ?type }
}
