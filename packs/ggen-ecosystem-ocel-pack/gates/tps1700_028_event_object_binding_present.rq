# MESSAGE: each manufacturing event must identify the object whose state changed or was observed.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?event WHERE {
  ?event a geocel:ManufacturingEvent .
  FILTER NOT EXISTS { ?event geocel:relatesTo ?object }
}
