PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?run ?o WHERE {
  ?run a geocel:ManufacturingRun ; geocel:oCount ?o .
  FILTER(xsd:integer(?o) < 0)
}
