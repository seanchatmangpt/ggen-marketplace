# MESSAGE: digest bindings must name the algorithm used to bind replay identity.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:DigestBindingObject .
  FILTER NOT EXISTS { ?object geocel:digestAlgorithm ?algorithm }
}
