# MESSAGE: each manufacturing event requires an explicit sequence position within its run.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?event WHERE {
  ?event a geocel:ManufacturingEvent .
  FILTER NOT EXISTS { ?event geocel:sequence ?sequence }
}
