# MESSAGE: typed refusal without a reason is not an admissible terminal disposition.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:RefusalObject .
  FILTER NOT EXISTS { ?object geocel:reason ?reason }
}
