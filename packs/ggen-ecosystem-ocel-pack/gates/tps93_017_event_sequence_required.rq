# TPS93: every manufacturing event requires sequence identity for replay.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?e WHERE { ?e a geocel:ManufacturingEvent . FILTER NOT EXISTS { ?e geocel:sequence ?n . } }