# TPS93: COMPLETE requires an admitted OCEL digest.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE { ?run a geocel:ManufacturingRun ; geocel:standing "COMPLETE" . FILTER NOT EXISTS { ?run geocel:ocelDigest ?d . FILTER(STRLEN(STR(?d)) > 0) } }