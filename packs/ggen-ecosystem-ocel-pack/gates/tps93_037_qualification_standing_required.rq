# TPS93: qualification objects require explicit standing.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?q WHERE { ?q a geocel:QualificationObject . FILTER NOT EXISTS { ?q geocel:standing ?s . FILTER(STRLEN(STR(?s)) > 0) } }