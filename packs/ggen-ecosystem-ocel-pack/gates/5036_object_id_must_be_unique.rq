PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?id (COUNT(?object) AS ?count) WHERE {
  ?object a geocel:ManufacturingObject ; geocel:objectId ?id .
}
GROUP BY ?id
HAVING(COUNT(?object) > 1)
