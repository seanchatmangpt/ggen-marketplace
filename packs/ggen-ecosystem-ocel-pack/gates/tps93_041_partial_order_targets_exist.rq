# TPS93: dependency targets must resolve to manufacturing objects.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?a ?b WHERE { ?a geocel:precedes ?b . FILTER NOT EXISTS { ?b a geocel:ManufacturingObject . } }