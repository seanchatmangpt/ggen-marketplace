# MESSAGE: every manufacturing run must expose an explicit standing before it can be terminally accounted.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE {
  ?run a geocel:ManufacturingRun .
  FILTER NOT EXISTS { ?run geocel:standing ?standing }
}
