# TPS93: merge events are closure bookkeeping and cannot be generation events.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?event WHERE { ?event a geocel:ManufacturingEvent ; geocel:eventType "merge" , "generate" . }