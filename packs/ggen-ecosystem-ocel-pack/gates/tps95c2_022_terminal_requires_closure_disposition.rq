PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?pull WHERE { ?pull a geocel:TPSPullObject ; geocel:expectedTerminalState ?state . FILTER NOT EXISTS { ?pull geocel:closureDisposition ?disposition } } LIMIT 1
