PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?run ?m WHERE {
  ?run a geocel:ManufacturingRun ; geocel:mCount ?m .
  FILTER(xsd:integer(?m) < 0)
}
