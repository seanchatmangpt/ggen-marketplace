# TPS93: every falsifier must carry an explicit reason for replay and repair routing.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?f WHERE { ?f a geocel:FalsifierObject . FILTER NOT EXISTS { ?f geocel:reason ?reason . FILTER(STRLEN(STR(?reason)) > 0) } }