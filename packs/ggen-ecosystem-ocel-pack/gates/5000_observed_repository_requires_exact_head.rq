PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?repo WHERE {
  ?repo a geocel:RepositoryObject ; geocel:standing "OBSERVED" .
  FILTER NOT EXISTS { ?repo geocel:exactHead ?head }
}
