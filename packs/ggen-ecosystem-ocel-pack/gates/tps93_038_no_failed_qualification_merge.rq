# TPS93: failed qualification cannot coexist with M standing for the same repository.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?r WHERE { ?r a geocel:RepositoryObject ; geocel:standing "M" . ?q a geocel:QualificationObject ; geocel:repository ?r ; geocel:standing "FAILURE" . }