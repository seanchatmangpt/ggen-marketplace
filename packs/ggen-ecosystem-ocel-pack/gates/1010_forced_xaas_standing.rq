# MESSAGE: CELL1 must state xaas standing explicitly; exact identity alone is not lifecycle evidence.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run ?repo WHERE { ?run a geocel:ManufacturingRun ; geocel:cell "CELL1" . ?repo a geocel:RepositoryObject ; geocel:repository "seanchatmangpt/xaas" . FILTER NOT EXISTS { ?repo geocel:standing ?standing . } }
