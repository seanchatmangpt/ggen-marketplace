# MESSAGE: Project-memory objects must identify the stable canonical memory key they represent.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:ProjectMemoryObject .
  FILTER NOT EXISTS { ?object geocel:objectId ?key }
}
