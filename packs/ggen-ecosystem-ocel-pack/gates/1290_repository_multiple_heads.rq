PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?repo (COUNT(DISTINCT ?head) AS ?heads) WHERE { ?repo a geocel:RepositoryObject ; geocel:exactHead ?head } GROUP BY ?repo HAVING(COUNT(DISTINCT ?head) > 1)
