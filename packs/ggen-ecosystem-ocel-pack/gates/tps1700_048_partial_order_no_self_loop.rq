# MESSAGE: a manufacturing dependency cannot point an object to itself.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?edge ?object WHERE {
  ?edge a geocel:PartialOrderEdge ; geocel:sourceObject ?object ; geocel:targetObject ?object .
}
