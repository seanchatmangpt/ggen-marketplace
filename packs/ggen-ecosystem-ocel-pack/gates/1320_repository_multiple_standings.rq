PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?repo (COUNT(DISTINCT ?standing) AS ?standings) WHERE { ?repo a geocel:RepositoryObject ; geocel:standing ?standing } GROUP BY ?repo HAVING(COUNT(DISTINCT ?standing) > 1)
