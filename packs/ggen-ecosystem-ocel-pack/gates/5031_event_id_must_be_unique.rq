PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?id (COUNT(?event) AS ?count) WHERE {
  ?event a geocel:ManufacturingEvent ; geocel:eventId ?id .
}
GROUP BY ?id
HAVING(COUNT(?event) > 1)
