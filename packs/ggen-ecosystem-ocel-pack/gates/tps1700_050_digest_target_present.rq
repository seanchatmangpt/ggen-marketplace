# MESSAGE: digest bindings must identify the exact artifact or evidence target they secure.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:DigestBindingObject .
  FILTER NOT EXISTS { ?object geocel:digestTarget ?target }
}
