# TPS93: a repository object cannot carry conflicting lifecycle standings.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?r ?a ?b WHERE { ?r a geocel:RepositoryObject ; geocel:standing ?a , ?b . FILTER(?a != ?b) }