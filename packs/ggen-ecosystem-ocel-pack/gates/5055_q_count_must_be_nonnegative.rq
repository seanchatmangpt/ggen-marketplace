PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?run ?q WHERE {
  ?run a geocel:ManufacturingRun ; geocel:qCount ?q .
  FILTER(xsd:integer(?q) < 0)
}
