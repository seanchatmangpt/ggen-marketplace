# TPS93: canonical control-plane identity requires Project number.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE { ?run a geocel:ManufacturingRun ; geocel:cell "CELL1" . FILTER NOT EXISTS { ?run geocel:projectNumber ?n . FILTER(?n > 0) } }