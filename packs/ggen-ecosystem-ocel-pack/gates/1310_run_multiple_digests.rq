PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run (COUNT(DISTINCT ?digest) AS ?digests) WHERE { ?run a geocel:ManufacturingRun ; geocel:ocelDigest ?digest } GROUP BY ?run HAVING(COUNT(DISTINCT ?digest) > 1)
