PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
ASK WHERE {
  ?a a geocel:ManufacturingEvent ; geocel:inRun ?run ; geocel:sequence ?seq .
  ?b a geocel:ManufacturingEvent ; geocel:inRun ?run ; geocel:sequence ?seq .
  FILTER(?a != ?b)
}
