PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run ?sequence (COUNT(?event) AS ?events) WHERE { ?event a geocel:ManufacturingEvent ; geocel:inRun ?run ; geocel:sequence ?sequence } GROUP BY ?run ?sequence HAVING(COUNT(?event) > 1)
