# MESSAGE: qualification objects require explicit exact-subject standing.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:QualificationObject .
  FILTER NOT EXISTS { ?object geocel:standing ?standing }
}
