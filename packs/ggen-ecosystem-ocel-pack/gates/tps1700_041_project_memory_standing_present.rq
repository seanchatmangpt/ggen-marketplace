# MESSAGE: Project-memory evidence without standing cannot control pull decisions.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:ProjectMemoryObject .
  FILTER NOT EXISTS { ?object geocel:standing ?standing }
}
