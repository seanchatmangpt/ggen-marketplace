PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?repo WHERE {
  ?repo a geocel:RepositoryObject ; geocel:standing "G" .
  FILTER NOT EXISTS { ?repo geocel:primitive ?primitive }
}
