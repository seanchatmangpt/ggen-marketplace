# MESSAGE: GGEN-realized consumer count cannot be negative.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?run ?g WHERE {
  ?run a geocel:ManufacturingRun ; geocel:gCount ?g .
  FILTER(xsd:decimal(?g) < 0)
}
