# MESSAGE: CELL1 must bind fdegym to an exact Git subject.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE { ?run a geocel:ManufacturingRun; geocel:cell "CELL1". FILTER NOT EXISTS { ?r a geocel:RepositoryObject; geocel:repository "seanchatmangpt/fdegym"; geocel:exactHead ?h. FILTER(STRLEN(STR(?h))>0) } }
