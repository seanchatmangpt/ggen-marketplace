# MESSAGE: partial-order edges without a target object cannot define downstream dependency.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?edge WHERE {
  ?edge a geocel:PartialOrderEdge .
  FILTER NOT EXISTS { ?edge geocel:targetObject ?target }
}
