# TPS93: one repository identity cannot carry two current exact heads in one run.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?name ?a ?b WHERE { ?r geocel:repository ?name ; geocel:exactHead ?a , ?b . FILTER(?a != ?b) }