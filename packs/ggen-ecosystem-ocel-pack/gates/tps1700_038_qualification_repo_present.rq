# MESSAGE: qualification evidence without repository identity cannot prove consumer closure.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:QualificationObject .
  FILTER NOT EXISTS { ?object geocel:repository ?repository }
}
