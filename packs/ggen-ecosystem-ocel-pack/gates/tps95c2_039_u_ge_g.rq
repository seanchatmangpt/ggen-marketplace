PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?run WHERE { ?run a geocel:ManufacturingRun ; geocel:uCount ?u ; geocel:gCount ?g . FILTER(xsd:integer(?u) < xsd:integer(?g)) } LIMIT 1
