# MESSAGE: repository subjects without explicit standing cannot be terminally reconciled.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:RepositoryObject .
  FILTER NOT EXISTS { ?object geocel:standing ?standing }
}
