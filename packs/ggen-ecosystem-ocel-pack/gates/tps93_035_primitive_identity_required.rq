# TPS93: every GGEN primitive requires immutable identity.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?p WHERE { ?p a geocel:GgenPrimitiveObject . FILTER NOT EXISTS { ?p geocel:objectId ?id . FILTER(STRLEN(STR(?id)) > 0) } }