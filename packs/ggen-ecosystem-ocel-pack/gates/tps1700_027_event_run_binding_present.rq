# MESSAGE: orphan events cannot be attributed to a manufacturing run.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?event WHERE {
  ?event a geocel:ManufacturingEvent .
  FILTER NOT EXISTS { ?event geocel:inRun ?run }
}
