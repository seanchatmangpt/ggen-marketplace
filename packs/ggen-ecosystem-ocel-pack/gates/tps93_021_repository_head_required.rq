# TPS93: repository objects require immutable 40-hex exact heads.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?r WHERE { ?r a geocel:RepositoryObject . FILTER NOT EXISTS { ?r geocel:exactHead ?h . FILTER(REGEX(STR(?h), "^[0-9a-f]{40}$")) } }