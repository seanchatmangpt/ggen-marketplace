PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE { ?object a geocel:DigestBindingObject ; geocel:digestAlgorithm ?algorithm . FILTER(STRLEN(STR(?algorithm)) = 0) } LIMIT 1
