# TPS93: every manufacturing event requires stable eventId.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?e WHERE { ?e a geocel:ManufacturingEvent . FILTER NOT EXISTS { ?e geocel:eventId ?id . FILTER(STRLEN(STR(?id)) > 0) } }