# TPS93: every manufacturing event requires an observed timestamp.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?e WHERE { ?e a geocel:ManufacturingEvent . FILTER NOT EXISTS { ?e geocel:timestamp ?t . } }