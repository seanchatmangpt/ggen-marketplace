# MESSAGE: falsifier evidence must state the invariant or hypothesis it disproves.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:FalsifierObject .
  FILTER NOT EXISTS { ?object geocel:reason ?reason }
}
