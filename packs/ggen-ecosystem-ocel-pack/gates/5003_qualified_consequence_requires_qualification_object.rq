PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?repo WHERE {
  ?repo a geocel:RepositoryObject ; geocel:standing "Q" .
  FILTER NOT EXISTS { ?repo geocel:qualification ?qualification }
}
