# MESSAGE: falsifier evidence requires explicit standing before it changes the option graph.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:FalsifierObject .
  FILTER NOT EXISTS { ?object geocel:standing ?standing }
}
