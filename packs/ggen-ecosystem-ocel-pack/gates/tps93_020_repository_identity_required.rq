# TPS93: repository objects require owner/name identity.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?r WHERE { ?r a geocel:RepositoryObject . FILTER NOT EXISTS { ?r geocel:repository ?name . FILTER(CONTAINS(STR(?name), "/")) } }