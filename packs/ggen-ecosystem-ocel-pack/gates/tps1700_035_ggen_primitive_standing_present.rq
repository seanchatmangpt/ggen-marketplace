# MESSAGE: GGEN primitive evidence requires explicit standing before downstream use.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:GgenPrimitiveObject .
  FILTER NOT EXISTS { ?object geocel:standing ?standing }
}
