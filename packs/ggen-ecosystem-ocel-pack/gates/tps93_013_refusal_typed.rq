# TPS93: refusal is terminal only when its reason is explicit.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?refusal WHERE { ?refusal a geocel:RefusalObject . FILTER NOT EXISTS { ?refusal geocel:reason ?reason . FILTER(STRLEN(STR(?reason)) > 0) } }