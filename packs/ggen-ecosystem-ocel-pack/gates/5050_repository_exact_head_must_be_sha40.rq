PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?repo ?head WHERE {
  ?repo a geocel:RepositoryObject ; geocel:exactHead ?head .
  FILTER(!REGEX(STR(?head), "^[0-9a-f]{40}$"))
}
