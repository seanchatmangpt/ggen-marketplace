PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
PREFIX xsd: <http://www.w3.org/2001/XMLSchema#>
SELECT ?run WHERE { ?run a geocel:ManufacturingRun ; geocel:terminalCount ?terminal ; geocel:admittedCount ?admitted . FILTER(xsd:integer(?terminal) > xsd:integer(?admitted)) } LIMIT 1
