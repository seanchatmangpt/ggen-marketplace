# MESSAGE: a run using Project memory must bind the canonical current-state key.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?run WHERE {
  ?run a geocel:ManufacturingRun .
  FILTER NOT EXISTS { ?run geocel:projectMemoryKey ?key }
}
