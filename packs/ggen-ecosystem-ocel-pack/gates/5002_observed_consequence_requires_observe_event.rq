PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?repo WHERE {
  ?repo a geocel:RepositoryObject ; geocel:standing "O" .
  FILTER NOT EXISTS {
    ?event a geocel:ManufacturingEvent ; geocel:eventType geocel:Observe ; geocel:relatesTo ?repo .
  }
}
