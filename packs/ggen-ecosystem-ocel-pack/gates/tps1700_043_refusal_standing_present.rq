# MESSAGE: refusal objects require explicit standing so refusal is not confused with unknown.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?object WHERE {
  ?object a geocel:RefusalObject .
  FILTER NOT EXISTS { ?object geocel:standing ?standing }
}
