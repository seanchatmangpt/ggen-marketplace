# TPS93: eventId is unique across distinct manufacturing events.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?id WHERE { ?a geocel:eventId ?id . ?b geocel:eventId ?id . FILTER(?a != ?b) }