# TPS93: qualification objects require exact subject head.
PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?q WHERE { ?q a geocel:QualificationObject . FILTER NOT EXISTS { ?q geocel:exactHead ?h . FILTER(REGEX(STR(?h), "^[0-9a-f]{40}$")) } }