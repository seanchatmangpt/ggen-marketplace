PREFIX geocel: <https://ggen.dev/ontology/ggen-ecosystem-ocel#>
SELECT ?repoObject ?head ?terminal WHERE {
  ?repoObject a geocel:RepositoryObject ; geocel:repository "seanchatmangpt/mfact-command-center" ; geocel:exactHead ?head .
  ?terminal a geocel:ManufacturingEvent ; geocel:eventType ?terminalType ; geocel:relatesTo ?repoObject ; geocel:inRun ?run .
  FILTER(?terminalType IN (geocel:Merge, geocel:Refuse))
  FILTER(!REGEX(STR(?head), "^[0-9a-fA-F]{40}$"))
}
