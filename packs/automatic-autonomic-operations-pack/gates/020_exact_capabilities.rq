PREFIX aa: <https://ggen.dev/ontology/automatic-autonomic#>
SELECT ?actual
WHERE {
  {
    SELECT (COUNT(?capability) AS ?actual)
    WHERE { ?capability a aa:Capability }
  }
  FILTER (?actual != 15)
}
