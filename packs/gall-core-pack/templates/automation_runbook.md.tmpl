---
to: docs/GALL_AUTOMATION_RUNBOOK.md
force: true
sparql:
  profiles: |
    PREFIX gall: <http://seanchatmangpt.github.io/packs/gall-core#>
    SELECT ?programId ?releaseIdentity ?profileId ?provider ?executionMode ?agentMode
           ?maxParallelism ?branchPattern ?runtimeDirectory ?receiptDirectory WHERE {
      ?program a gall:GallProgram ;
               gall:programId ?programId ;
               gall:releaseIdentity ?releaseIdentity ;
               gall:hasAutomationProfile ?profile .
      ?profile a gall:AutomationProfile ;
               gall:automationProfileId ?profileId ;
               gall:trackerProvider ?providerNode ;
               gall:executionMode ?executionModeNode ;
               gall:agentMode ?agentModeNode ;
               gall:maxParallelism ?maxParallelism ;
               gall:branchPattern ?branchPattern ;
               gall:runtimeDirectory ?runtimeDirectory ;
               gall:receiptDirectory ?receiptDirectory .
      ?providerNode gall:providerName ?provider .
      ?executionModeNode gall:modeName ?executionMode .
      ?agentModeNode gall:agentModeName ?agentMode .
    } ORDER BY ?programId
---
# Gall Automation Runbook

GENERATED by `ggen sync run` from the admitted Gall automation profile. Do not hand-edit.

{% for profile in profiles %}## Program

| Field | Value |
|---|---|
| Program | `{{ profile.programId }}` |
| Release | `{{ profile.releaseIdentity }}` |
| Automation profile | `{{ profile.profileId }}` |
| Tracker provider | `{{ profile.provider }}` |
| Execution mode | `{{ profile.executionMode }}` |
| Agent mode | `{{ profile.agentMode }}` |
| Maximum parallel work | {{ profile.maxParallelism }} |
| Branch pattern | `{{ profile.branchPattern }}` |
| Runtime directory | `{{ profile.runtimeDirectory }}` |
| Receipt directory | `{{ profile.receiptDirectory }}` |
{% endfor %}

## Authority model

The ontology is the sole authority. The automation manifest, tracker payloads,
agent bundles, workflow, runtime state, and receipts are projections or
observations. Never repair a generated artifact by hand. Change the graph and
regenerate.

Every external mutation follows:

```text
admitted graph
→ deterministic plan
→ intent receipt
→ explicit apply boundary
→ external response
→ result receipt
→ independent receipt verification
```

Plan-only commands never require tracker or agent credentials.

## Standard lifecycle

```bash
ggen sync run
bash scripts/gall/gall automation validate
bash scripts/gall/gall work status
bash scripts/gall/gall work next
bash scripts/gall/gall work dispatch-ready
```

After implementation produces its declared evidence:

```bash
bash scripts/gall/gall work advance
bash scripts/gall/gall tracker plan
bash scripts/gall/gall receipt verify
```

To run checkpoint evidence and test total closure:

```bash
bash scripts/gall/gall checkpoint run
ggen sync run
bash scripts/gall/gall crown
```

`crown` reruns every work-item verifier and content snapshot in proof order at
the exact sealed revision before evaluating checkpoint and receipt closure.

## External tracker actuation

External tracker mutation is refused unless the profile is `apply-allowed` and
the operator explicitly runs:

```bash
bash scripts/gall/gall tracker apply
```

### Jira Cloud environment

```text
JIRA_BASE_URL=https://your-domain.atlassian.net
JIRA_EMAIL=operator@example.com
JIRA_API_TOKEN=secret
JIRA_DEPENDENCY_LINK_TYPE=Blocks
GALL_JIRA_USE_COMPONENTS=0
GALL_JIRA_STATUS_MAP={"blocked":"Blocked","ready":"To Do","complete":"Done"}
```

`GALL_JIRA_STATUS_MAP` is optional because Jira workflow transition names are
deployment-specific.

### GitHub Issues environment

```text
GITHUB_TOKEN=secret
GITHUB_REPOSITORY=owner/repository
```

The synchronizer provisions deterministic labels before issue upsert and closes
an issue only after its Gall completion receipt exists.

## Coding-agent actuation

Handoff bundles are always available:

```bash
bash scripts/gall/gall work dispatch WORK_ITEM_ID
```

External invocation additionally requires an `apply-allowed` profile,
`command-agent` mode, and:

```text
GALL_AGENT_COMMAND=/path/to/agent-command --fixed-arguments
```

Then execute:

```bash
bash scripts/gall/gall work dispatch WORK_ITEM_ID --apply
```

The executor creates the declared Git branch in an isolated worktree, copies the
work order into that boundary, and appends its path as the final command
argument. After execution it calculates staged, unstaged, and untracked paths;
refuses forbidden or unauthorized paths; runs the declared verifiers inside the
worktree; and records standard output, standard error, path classification,
verifier results, intent, and result receipts. It does not merge or push.

Review and cleanup:

```bash
bash scripts/gall/gall work inspect WORK_ITEM_ID
bash scripts/gall/gall work remove-worktree WORK_ITEM_ID
```

Cleanup refuses a worktree with uncommitted changes.

## Evidence snapshots

`work verify`, `work advance`, and `crown` create a content-addressed snapshot for
every declared evidence path. Files are SHA-256 hashed directly. Directories are
hashed from the ordered sequence of relative path, file digest, and size. Crown
verification recomputes those observations and refuses drift.

## GitHub Actions automation

The generated `.github/workflows/gall-control-plane.yml` performs plan and replay
on pull requests. Manual dispatch can independently request:

- advancement of ready work;
- tracker application;
- one agent handoff or isolated invocation;
- crown verification.

Tracker and agent actuation use the `gall-external-actuation` environment so
repository approval rules can guard secrets and execution.

## Refusal conditions

Automation must stop instead of improvising when:

- a dependency is incomplete;
- the work-item DAG is cyclic or violates proof order;
- an automation profile has the wrong type, duplicate identity, ambiguous owner,
  unsupported mode, unsafe path, or invalid branch pattern;
- a context path is missing;
- a write escapes the repository or the ticket's allowed paths;
- apply mode is not admitted;
- required credentials are missing;
- a verification command fails;
- an evidence artifact is absent or changes after snapshot;
- a receipt chain is invalid or stale;
- a checkpoint is not `ALIVE` at crown time.

## Independent replay and receipts

```bash
bash scripts/gall/gall replay
bash scripts/gall/gall receipt verify
```

The receipt verifier recomputes every digest and predecessor link, verifies
materialized receipts belong to the chain, checks freshness against the current
automation manifest, and at crown time requires exact-revision verification,
evidence snapshot, and completion for every work item.
