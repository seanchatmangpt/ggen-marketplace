# MESSAGE: consequence-bearing DO must remain outside semantic manufacture
PREFIX sm: <http://seanchatmangpt.github.io/packs/semantic-manufacture-epoch#>
SELECT ?epoch WHERE {
  ?epoch a sm:Epoch .
  FILTER NOT EXISTS { ?epoch sm:consequenceDoAuthority "external" . }
}
ORDER BY ?epoch