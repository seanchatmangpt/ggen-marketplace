# MESSAGE: generated projections may not become a handwritten source of truth
PREFIX sm: <http://seanchatmangpt.github.io/packs/semantic-manufacture-epoch#>
SELECT ?epoch WHERE {
  ?epoch a sm:Epoch .
  FILTER NOT EXISTS { ?epoch sm:handwrittenGeneratedEdits "refused" . }
}
ORDER BY ?epoch