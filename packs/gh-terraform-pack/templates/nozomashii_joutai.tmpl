---
to: docs/gh-terraform/TCPS望ましい状態.md
sparql:
  rows: |
    PREFIX ghtf: <http://seanchatmangpt.github.io/packs/gh-terraform#>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?label ?family (SAMPLE(?tier) AS ?risk) WHERE {
      ?s a ghtf:RepoManagementInstance ; rdfs:label ?label ; ghtf:family ?family .
      OPTIONAL { ?s ghtf:riskTier ?tier }
    } GROUP BY ?label ?family ORDER BY ?family ?label
---
# TCPS望ましい状態

GENERATED by `ggen sync` from `packs/gh-terraform-pack/ontology.ttl` (法則一: 意味を原本とする).

GitHub画面は観測であり、原本ではない。原本は本表の意味個体である。

| 望ましい状態 | 責任工程(.tfファイル) | 変更危険度 |
|---|---|---|
{% for row in rows %}| {{ row.label }} | {{ row.family }}.tf | {% if row.risk %}第{{ row.risk }}段階{% else %}—{% endif %} |
{% endfor %}
