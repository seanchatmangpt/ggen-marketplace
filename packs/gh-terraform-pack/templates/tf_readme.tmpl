---
to: infra/terraform/github/README.md
sparql:
  version: |
    PREFIX ghtf: <http://seanchatmangpt.github.io/packs/gh-terraform#>
    SELECT ?pinned ?latest WHERE {
      ghtf:ProviderVersionFacts ghtf:pinnedProviderVersion ?pinned ; ghtf:latestProviderVersion ?latest .
    } ORDER BY ?pinned
---
# GitHub desired-state module (GENERATED)

GENERATED by `ggen sync` from `packs/gh-terraform-pack/ontology.ttl`. Do not edit; edit the ontology.

Provider pin: integrations/github v{{ version[0].pinned }} (latest observed upstream: v{{ version[0].latest }}).

- `terraform validate` — syntax/config inspection (safe, no API)
- `terraform plan` — drift observation (read-only against GitHub)
- `terraform apply` — external mutation. NEVER run without explicit approval (法則四).

File split per 第十六章: one file per responsibility family. Empty files are declared
placeholders, not omissions.
