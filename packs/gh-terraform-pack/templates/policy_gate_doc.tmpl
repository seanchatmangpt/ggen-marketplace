---
to: docs/gh-terraform/policy-gates.md
sparql:
  rows: |
    PREFIX ghtf: <http://seanchatmangpt.github.io/packs/gh-terraform#>
    PREFIX rdfs: <http://www.w3.org/2000/01/rdf-schema#>
    SELECT ?rule ?label WHERE {
      ?rule a rdfs:Individual ; rdfs:label ?label .
      FILTER(STRSTARTS(STR(?rule), STR(ghtf:Policy)))
    } ORDER BY ?rule
---
# gh-terraform-pack policy-as-code gates

GENERATED by `ggen sync` from `packs/gh-terraform-pack/ontology.ttl` and
`packs/gh-terraform-pack/gates/030_policy.rq`. This document echoes the live
gate for humans — it is generated, not hand-written, so the doc and the gate
can never drift apart.

`gates/030_policy.rq` is a cross-individual *policy* refusal gate, distinct
from `gates/010_required.rq` (structural completeness) and
`gates/020_fleet.rq` (the ⊕ composition law). A non-empty result from
`030_policy.rq` is a refusal: sync stops, naming the offending subject, the
rule it broke, and a diagnostic detail.

## Rules

{% for row in rows %}### `{{ row.rule }}`

{{ row.label }}

{% endfor %}
