---
to: docs/gh-terraform/資源一覧.md
sparql:
  rows: |
    PREFIX ghtf: <http://seanchatmangpt.github.io/packs/gh-terraform#>
    SELECT ?type ?scope ?purpose (SAMPLE(?dep) AS ?deprecated) WHERE {
      ?s a ghtf:TerraformResource ; ghtf:resourceType ?type ; ghtf:scope ?scope ; ghtf:purpose ?purpose .
      OPTIONAL { ?s ghtf:deprecated ?dep }
    } GROUP BY ?type ?scope ?purpose ORDER BY ?type
---
# 資源一覧 (Terraform GitHub提供者 全資源面)

GENERATED by `ggen sync` from `packs/gh-terraform-pack/ontology.ttl` (第十四章 組合せ最大化).

新しい資源を管理下に追加する手順は [資源追加手順.md](資源追加手順.md) を参照。

| 資源型 | 対象範囲 | 目的 | 廃止 |
|---|---|---|---|
{% for row in rows %}| `{{ row.type }}` | {{ row.scope }} | {{ row.purpose }} | {% if row.deprecated %}廃止{% endif %} |
{% endfor %}
