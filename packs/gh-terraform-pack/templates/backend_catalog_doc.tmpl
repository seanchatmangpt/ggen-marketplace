---
to: "docs/gh-terraform/backends/{{ kind }}.md"
sparql:
  rows: |
    PREFIX ghtf: <http://seanchatmangpt.github.io/packs/gh-terraform#>
    SELECT ?kind (SAMPLE(?body) AS ?example) WHERE {
      ?s a ghtf:BackendConfig ; ghtf:backendKind ?kind ; ghtf:hclBody ?body .
    } GROUP BY ?kind ORDER BY ?kind
---
# {{ kind }}

GENERATED by `ggen sync` from `packs/gh-terraform-pack/ontology.ttl`.

## Example (real, captured — not fabricated)

```hcl
{{ example }}```
