---
to: "docs/gh-terraform/org-security.md"
sparql:
  rows: |
    PREFIX ghtf: <http://seanchatmangpt.github.io/packs/gh-terraform#>
    SELECT ?control ?verified (SAMPLE(?reason) AS ?flag) WHERE {
      ?s a ghtf:OrgSecurityPolicy ; ghtf:controlName ?control ; ghtf:verifiedProviderResource ?verified .
      OPTIONAL { ?s ghtf:flagReason ?reason }
    } GROUP BY ?control ?verified ORDER BY ?control
---
# Org security & compliance

GENERATED by `ggen sync` from `packs/gh-terraform-pack/ontology.ttl` (class: `ghtf:OrgSecurityPolicy`).

| Control | Verified against provider schema | Notes |
|---------|-----------------------------------|-------|
{% for row in rows %}| {{ row.control }} | {{ row.verified }} | {% if not row.verified %}⚠ FLAGGED: {{ row.flag }}{% endif %} |
{% endfor %}
