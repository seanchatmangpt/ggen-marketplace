---
to: docs/gh-terraform/廃止資源一覧.md
sparql:
  rows: |
    PREFIX ghtf: <http://seanchatmangpt.github.io/packs/gh-terraform#>
    SELECT ?type ?purpose (SAMPLE(?succ) AS ?successor) (SAMPLE(?dead) AS ?deadapi) WHERE {
      ?s a ghtf:TerraformResource ; ghtf:resourceType ?type ; ghtf:purpose ?purpose ; ghtf:deprecated true .
      OPTIONAL { ?s ghtf:successor ?succ }
      OPTIONAL { ?s ghtf:deadApi ?dead }
    } GROUP BY ?type ?purpose ORDER BY ?type
---
# 廃止資源一覧

GENERATED by `ggen sync` from `packs/gh-terraform-pack/ontology.ttl` (第十四章: 廃止資源を削除せず、廃止として表示する).

| 資源型 | 目的 | 後継 | API状態 |
|---|---|---|---|
{% for row in rows %}| `{{ row.type }}` | {{ row.purpose }} | {% if row.successor %}`{{ row.successor }}`{% else %}—{% endif %} | {% if row.deadapi %}死API{% else %}稼働{% endif %} |
{% endfor %}
