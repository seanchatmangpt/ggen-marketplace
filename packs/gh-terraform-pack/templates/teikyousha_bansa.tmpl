---
to: docs/gh-terraform/提供者版差.md
sparql:
  v: |
    PREFIX ghtf: <http://seanchatmangpt.github.io/packs/gh-terraform#>
    SELECT ?pinned ?latest ?note WHERE {
      ghtf:ProviderVersionFacts ghtf:pinnedProviderVersion ?pinned ;
        ghtf:latestProviderVersion ?latest ; ghtf:breakingChangeNote ?note .
    } ORDER BY ?pinned
---
# 提供者版差 (第十五章)

GENERATED by `ggen sync` from `packs/gh-terraform-pack/ontology.ttl`.

| 項目 | 版 |
|---|---|
| 検証済み固定版 | {{ v[0].pinned }} |
| 比較最新版 | {{ v[0].latest }} |

破壊的差異: {{ v[0].note }}

版更新は通常の文字列変更として扱わない。九工程 (第十五章): 版差記録 → 廃止確認 →
必須引数差確認 → 再製造 → validate → plan比較 → 破壊的差異受領 → 実地受入れ試験 →
standing上限更新。
