---
to: "generated/innovation/ERRC.md"
force: true
determinism: true
sparql:
  decisions: |
    PREFIX errc: <https://ggen.dev/ontology/innovation/errc#>
    PREFIX dcterms: <http://purl.org/dc/terms/>
    PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
    SELECT ?profileTitle ?factorLabel ?actionLabel ?rationale ?order WHERE {
      ?profile a errc:InnovationProfile ; dcterms:title ?profileTitle ; errc:hasDecision ?decision .
      ?decision errc:decisionFactor ?factor ; errc:decisionAction ?action ; errc:rationale ?rationale .
      ?factor skos:prefLabel ?factorLabel .
      ?action skos:prefLabel ?actionLabel .
      OPTIONAL { ?factor errc:factorOrder ?order }
    } ORDER BY ?profileTitle ?order ?factorLabel
---
# ERRC Value Innovation Matrix

> GENERATED by `errc-innovation-contract-pack`. Authority: **CONSTRUCT**. No actuation is performed by this projection.

| Profile | Factor | Action | Rationale |
|---|---|---|---|
{% for row in decisions %}| {{ row.profileTitle }} | {{ row.factorLabel }} | **{{ row.actionLabel }}** | {{ row.rationale }} |
{% endfor %}

The RDF graph is authoritative. This document is a deterministic projection and does not confer execution authority.
