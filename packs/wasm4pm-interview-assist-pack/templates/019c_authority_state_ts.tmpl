---
to: "lib/domain/authority-state.ts"
sparql:
  fields: |
    PREFIX schema: <https://schema.org/>
    SELECT ?c ?name WHERE {
      ?c a schema:Action ; schema:name ?name .
      FILTER(CONTAINS(STR(?c), "/authority-action/"))
    }
    ORDER BY ?c
---
// GENERATED by ggen sync from packs/wasm4pm-interview-assist-pack/ontology/50-policy.ttl
// via queries/authority-actions.rq (TICKET-019). Do not hand-edit -- edit the ontology.

export interface AuthorityState {
{% for f in fields %}  "{{ f.c | replace(from="https://github.com/seanchatmangpt/ggen/blob/main/packs/wasm4pm-interview-assist-pack/authority-action/", to="") }}"?: boolean; // {{ f.name }}
{% endfor %}}

export const AUTHORITY_STATE_FIELD_COUNT = {{ fields | length }};
