---
to: "lib/domain/event-routing.ts"
sparql:
  families: |
    PREFIX skos: <http://www.w3.org/2004/02/skos/core#>
    SELECT ?family ?label WHERE {
      ?family a skos:Concept ;
              skos:inScheme <https://github.com/seanchatmangpt/ggen/blob/main/packs/wasm4pm-interview-assist-pack/event-family-scheme> ;
              skos:prefLabel ?label .
    }
    ORDER BY ?family
---
// GENERATED by ggen sync from packs/wasm4pm-interview-assist-pack/ontology/40-events-workflow.ttl
// via queries/event-families.rq (TICKET-016 reuse, TICKET-022). Do not hand-edit -- edit the ontology.
//
// One routing entry per event-family/* concept ({{ families | length }} total), mapping each
// EventFamily member to a handler-name slot. The reducer (TICKET-023) dispatches
// through this table -- it must never switch on a family literal directly.
import type { EventFamily } from "./event-family";

export const EVENT_ROUTING: Record<EventFamily, string> = {
{% for f in families %}  "{{ f.label }}": "handle{{ f.label }}",
{% endfor %}};

export const EVENT_ROUTING_ENTRY_COUNT = {{ families | length }};
