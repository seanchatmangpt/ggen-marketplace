---
to: "lib/domain/problem-state.ts"
sparql:
  fields: |
    PREFIX schema: <https://schema.org/>
    PREFIX dcterms: <http://purl.org/dc/terms/>
    SELECT ?c ?name WHERE {
      ?c a schema:Action ; schema:name ?name ;
         dcterms:isPartOf <https://github.com/seanchatmangpt/ggen/blob/main/packs/wasm4pm-interview-assist-pack/capability-category/problem> .
    }
    ORDER BY ?c
---
// GENERATED by ggen sync from packs/wasm4pm-interview-assist-pack/ontology/30-capabilities.ttl
// via queries/problem-capabilities.rq (TICKET-018). Do not hand-edit -- edit the ontology.
//
// One optional field per capability/problem/* resource, keyed by its relative IRI path
// (quoted string key -- avoids inventing a camelCase-derivation heuristic not present in the
// admitted graph). Field VALUE is unknown -- populated at runtime by the capability's handler.

export interface ProblemState {
{% for f in fields %}  "{{ f.c | replace(from="https://github.com/seanchatmangpt/ggen/blob/main/packs/wasm4pm-interview-assist-pack/capability/", to="") }}"?: unknown; // {{ f.name }}
{% endfor %}}

export const PROBLEM_STATE_FIELD_COUNT = {{ fields | length }};
