---
to: "lib/domain/refusal.ts"
sparql:
  codes: |
    PREFIX schema: <https://schema.org/>
    PREFIX rdf: <http://www.w3.org/1999/02/22-rdf-syntax-ns#>
    SELECT ?refusal ?code WHERE {
      ?refusal a schema:DigitalDocument ;
               rdf:value ?code .
      FILTER(CONTAINS(STR(?refusal), "/req/ard-refusal-") && !CONTAINS(STR(?refusal), "no-inferred-truth"))
    }
    ORDER BY ?refusal
---
// GENERATED by ggen sync from packs/wasm4pm-interview-assist-pack/ontology/20-requirements.ttl
// via queries/refusal-codes.rq (TICKET-017). Do not hand-edit -- edit the ontology.

/** {{ codes | length }}-member union of ARD Section 11's refusal taxonomy codes. */
export type RefusalCode =
{% for c in codes %}  | "{{ c.code }}"
{% endfor %};

export const REFUSAL_CODES: readonly RefusalCode[] = [
{% for c in codes %}  "{{ c.code }}",
{% endfor %}] as const;

export const REFUSAL_CODE_COUNT = {{ codes | length }};

/** Refusal is a first-class outcome (Architecture Decision 13) -- never a thrown exception. */
export type AdmissionResult<T> =
  | { status: "admitted"; value: T }
  | { status: "refused"; code: RefusalCode; reason?: string };
