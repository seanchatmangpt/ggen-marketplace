---
to: "docs/process-mining-proof/pipelines.md"
sparql:
  rows: |
    PREFIX pmp: <http://seanchatmangpt.github.io/packs/process-mining-proof#>
    PREFIX dcterms: <http://purl.org/dc/terms/>
    SELECT ?pipelineName ?planner ?domain ?powlProjector ?ocelReplayEngine ?conformanceChecker ?description WHERE {
      ?pipeline a pmp:ProofPipeline ;
          pmp:pipelineName ?pipelineName ;
          pmp:planner ?planner ;
          pmp:domain ?domain ;
          pmp:powlProjector ?powlProjector ;
          pmp:ocelReplayEngine ?ocelReplayEngine ;
          pmp:conformanceChecker ?conformanceChecker ;
          dcterms:description ?description .
    } ORDER BY ?pipelineName
---
# Process-mining proof pipelines (generated, not hand-maintained)

Every row below echoes literals already stored in this consumer's own facts against
`process-mining-proof-pack`'s real `pmp:ProofPipeline` vocabulary — nothing here is
synthesized. A pipeline missing any required stage cannot reach this document at all:
`gates/010_pipeline_grounded_or_refused.rq` refuses it first.

{% for row in rows %}## `{{ row.pipelineName }}`

{{ row.description }}

| Stage | Value |
|---|---|
| Planner | `{{ row.planner }}` |
| Domain | `{{ row.domain }}` |
| POWL projector | `{{ row.powlProjector }}` |
| OCEL replay engine | `{{ row.ocelReplayEngine }}` |
| Conformance checker | `{{ row.conformanceChecker }}` |

{% endfor %}
