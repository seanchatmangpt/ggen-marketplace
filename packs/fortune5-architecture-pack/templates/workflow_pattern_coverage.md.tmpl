---
to: docs/FORTUNE5_WORKFLOW_PATTERN_COVERAGE.md
force: true
sparql:
  coverage: |
    PREFIX f5: <http://seanchatmangpt.github.io/packs/fortune5-architecture#>
    SELECT ?patternNumber ?patternName ?implementation ?positive ?negative ?receipt WHERE {
      ?program a f5:EnterpriseArchitectureProgram ;
               f5:hasWorkflowPatternEvidence ?evidence .
      ?evidence a f5:WorkflowPatternEvidence ;
                f5:workflowPattern ?pattern ;
                f5:implementationEvidence ?implementation ;
                f5:positiveWitness ?positive ;
                f5:negativeFalsifier ?negative ;
                f5:receiptVerifier ?receipt .
      ?pattern a f5:WorkflowPattern ;
               f5:patternNumber ?patternNumber ;
               f5:patternName ?patternName .
    }
    ORDER BY ?patternNumber
---
# Fortune 5 Workflow Pattern Coverage

GENERATED by `ggen sync run`. Each row is a graph-declared evidence contract. It does not promote an implementation merely because a name exists; the referenced implementation, positive witness, negative falsifier, and receipt verifier must remain independently executable.

| WCP | Pattern | Implementation evidence | Positive witness | Negative falsifier | Receipt verifier |
|---:|---|---|---|---|---|
{% for item in coverage %}| {{ item.patternNumber }} | {{ item.patternName }} | `{{ item.implementation }}` | `{{ item.positive }}` | `{{ item.negative }}` | `{{ item.receipt }}` |
{% endfor %}

## Crown law

The revised control-flow catalogue contains 43 patterns. Crown standing requires exactly one complete evidence contract for every `WCP01` through `WCP43`; gate `060_workflow_pattern_evidence.rq` refuses any missing evidence dimension.
