{
  "schema": "https://chatmangpt.com/ggen/dd-ui/grammar/v1",
  "grammarVersion": "dd-ui/2",
  "runtimeAiRenderAuthority": false,
  "renderActuationAuthority": false,
  "irreversibleUiSelections": 0,
  "doBoundary": "BRCE",
  "vocabulary": [
{% for row in results %}    {"order": {{ row["order"] }}, "kind": "{{ row["kind"] }}", "id": "{{ row["id"] }}", "label": "{{ row["label"] }}"}{% if not loop.last %},{% endif %}
{% endfor %}  ]
}
