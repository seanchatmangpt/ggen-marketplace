flowchart TB
  O[Process / repository observations] --> OStar[Admitted world O*]
  OStar --> G[Shared world G_t]
  Avatar[Avatar alpha] --> P[Deterministic projection P]
  Context[Context kappa] --> P
  Authority[Authority rho] --> P
  Grammar[Grammar dd-ui/2] --> P
  G --> P
  P --> Frontier[DfCM reversible presentation frontier]
  Frontier --> Screen[Deterministic Screen IR]
  Screen --> Intent[Unselected intent]
  Intent --> Select[SELECT]
  Intent --> Construct[CONSTRUCT]
  Intent --> BRCE[BRCE gate]
  BRCE --> DO[DO]
  Screen -. no direct actuation .-> DO
  Screen --> Receipt[Screen receipt]
  Receipt --> Replay[Replay]
  LLM[LLM / AGI] -. no runtime render authority .-> P
{% for row in results %}  Grammar --> V{{ loop.index }}["{{ row["kind"] }} · {{ row["id"] }}"]
{% endfor %}
