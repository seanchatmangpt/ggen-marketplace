# Superseded InterviewAssist site prototype

## Standing

`SUPERSEDED`

This pack is retained as reviewable history of the first generated Next.js interview-sandbox experiment. It derives application capabilities from a private `site:` vocabulary.

That vocabulary is not admitted. The canonical specification source is:

```text
packs/wasm4pm-interview-assist-pack/
```

The downstream implementation contract is:

```text
docs/jira/v26.7.23/
```

## Allowed use

- Review generic Next.js/Tera/SPARQL projection techniques.
- Recover framework wiring that can be made domain-independent.
- Compare the prototype against later Chicago-TDD ticket implementations.

## Excluded use

- Do not treat `site:InterviewCapability` or any `site:` predicate as InterviewAssist domain truth.
- Do not extend this private vocabulary.
- Do not use this pack as the source for TICKET-001 through TICKET-057.
- Do not copy InterviewAssist-specific literals from this pack into reusable templates.

## Disposition

Archived in place rather than deleted because the templates and browser experiment remain useful implementation evidence. Any reusable mechanism must be re-derived against the admitted public-ontology corpus before promotion.
