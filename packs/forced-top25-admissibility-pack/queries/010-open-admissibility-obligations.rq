PREFIX fta: <https://ggen.dev/ontology/forced-top25-admissibility#>
SELECT ?rank ?repo ?head ?blocker WHERE {
  ?t a fta:FanoutTarget ; fta:rank ?rank ; fta:repo ?repo ; fta:exactCurrentHead ?head ; fta:compatibilityState "UNKNOWN_PENDING_RECEIPT" ; fta:blocker ?blocker .
} ORDER BY ?rank ?repo
