PREFIX fta: <https://ggen.dev/ontology/forced-top25-admissibility#>
SELECT ?repo ?head WHERE { ?t a fta:FanoutTarget ; fta:repo ?repo ; fta:exactCurrentHead ?head . FILTER(STRLEN(?head) != 40 || !REGEX(?head,"^[0-9a-f]{40}$")) } ORDER BY ?repo
