//! GENERATED by ggen from the RWR Level-5 ontology. Do not hand-edit.

/// One required proof surface in the full maturity matrix.
#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub struct Requirement {
    pub order: u8,
    pub dimension: &'static str,
    pub name: &'static str,
    pub domain: &'static str,
    pub surface: &'static str,
    pub outcome: &'static str,
    pub artifact: &'static str,
    pub verifier: &'static str,
    pub falsifier: &'static str,
}

/// All 63 proof obligations: 21 dimensions × 3 independent surfaces.
pub const REQUIREMENTS: &[Requirement] = &[
{% for row in results %}    Requirement {
        order: {{ row["order"] }},
        dimension: "{{ row["dimension"] }}",
        name: "{{ row["name"] }}",
        domain: "{{ row["domain"] }}",
        surface: "{{ row["surface"] }}",
        outcome: "{{ row["outcome"] }}",
        artifact: "{{ row["artifact"] }}",
        verifier: "{{ row["verifier"] }}",
        falsifier: "{{ row["falsifier"] }}",
    },
{% endfor %}];
