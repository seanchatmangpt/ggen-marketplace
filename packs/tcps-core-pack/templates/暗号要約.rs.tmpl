---
to: src/暗号要約.rs
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:暗号要約Module tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source | trim_end }}
