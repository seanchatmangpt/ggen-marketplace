---
to: src/安全.rs
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:安全Module tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source | trim_end }}
