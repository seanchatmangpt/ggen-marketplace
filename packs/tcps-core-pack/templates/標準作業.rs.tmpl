---
to: src/標準作業.rs
sparql:
  rows: |
    PREFIX tcps: <http://seanchatmangpt.github.io/packs/tcps-core#>
    SELECT ?source WHERE { tcps:標準作業Module tcps:sourceText ?source } ORDER BY ?source
---
{{ rows[0].source | trim_end }}
