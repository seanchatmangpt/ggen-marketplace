# CertificationAssist Pack v26.7.30

`certification-assist-pack` manufactures the bounded capability-acquisition cell shared by CertificationAssist and InterviewAssist: Systems.

## Constitutional sequence

```text
credential objective
→ admitted competency
→ capability-standing diagnosis
→ minimum sufficient horizon
→ deterministic practice manufacture
→ bounded self-play
→ evidence admission
→ standing decision
→ receipt and replay
```

The pack separates two independent rails:

- **external standing** — a credential issuer awarded a certificate or badge;
- **internal standing** — the subject can explain, decide, draw, execute, break, transfer, generate, and verify the capability.

A certificate never self-promotes internal standing. An autonomic learning cycle may manufacture practice intents and fixtures, but it cannot promote standing without admitted evidence.

## Crown

The first bounded crown contains 12 capabilities and three proof surfaces per capability:

1. Credential Horizon ingestion
2. Capability Standing Graph
3. Triple Darkness detection
4. Minimum Sufficient Horizon selection
5. Whiteboard projection
6. Practice manufacturing
7. Deterministic self-play
8. MCP resource/tool boundary
9. A2A agent/task boundary
10. Bounded automation
11. Bounded autonomics
12. Standing receipt and replay

```text
12 capabilities × 3 proof surfaces = 36 obligations
```

## Protocol boundary

- MCP exposes deterministic resources and bounded tools.
- MCP task augmentation is deliberately not declared because the 2025-11-25 task facility is experimental.
- A2A owns stateful multi-agent tasks, messages, and artifacts.
- Self-play is deterministic and uses Candidate Twin, Examiner Twin, and Domain Adjudicator roles.
- Every sensitive standing change is human-authorized and evidence-gated.

## Automatic and autonomic boundary

The generated consumer reuses the repository automatic/autonomic operating rail for receipted execution. The CertificationAssist autonomic controller is intent-only:

```text
monitor standing
→ analyze highest-leverage gap
→ plan practice intent
→ execute bounded simulation
→ record evidence candidate
→ stop before promotion
```

There is no unreceipted emergency promotion path.

## Authored and manufactured surfaces

Authored:

- `ontology.ttl`
- SPARQL refusal gates
- deterministic queries
- Tera templates

Manufactured:

- Rust capability-standing verifier
- self-play and autonomic tests
- MCP server contract
- A2A Agent Card
- whiteboard projections
- 80/20 speedrun plan
- verifier report and receipts

## Standing

`UNKNOWN` until the dedicated exact-head workflow executes ontology gates, two byte-identical generations, the generated Rust crown, receipt verification, protocol-schema checks, sabotage fixtures, and replay.
