PREFIX gcd: <https://ggen.dev/ontology/github-cloud-operating-doctrine#>

SELECT ?violation
WHERE {
  {
    SELECT (COUNT(?route) AS ?count)
    WHERE { ?route a gcd:DependencyRoute . }
  }
  FILTER (?count != 7)
  BIND(gcd:DependencyRouteCardinality AS ?violation)
}
