PREFIX gcd: <https://ggen.dev/ontology/github-cloud-operating-doctrine#>

SELECT ?violation
WHERE {
  {
    SELECT (COUNT(?plane) AS ?count)
    WHERE { ?plane a gcd:TransportPlane . }
  }
  FILTER (?count != 5)
  BIND(gcd:TransportPlaneCardinality AS ?violation)
}
