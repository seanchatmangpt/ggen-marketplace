# github-cloud-operating-doctrine-pack

Ontology-first GitHub cloud operating doctrine for ChatGPT cloud sessions.

## What this pack manufactures

A consumer that references this pack through ggen receives three deterministic projections:

- `docs/github-cloud-operating-doctrine.md` — the human-readable 28-section constitution;
- `policy/github-cloud-operating-doctrine.json` — machine-readable transport, authority, dependency, standing, and safety policy;
- `scripts/verify_github_cloud_operating_doctrine.py` — an executable regression receipt proving that a missing local CLI and a failed default Actions token are route failures, not crown blockers while another lawful authorized route remains.

The canonical authority is `ontology.ttl`. Templates query that graph; they do not maintain a shadow copy of the doctrine.

## Constitutional transport order

```text
GitHub Connector
  -> GitHub Actions
  -> Artifacts
  -> Local verification capsule
  -> CLI fallback
```

This is a preference over available lawful edges, not a rigid requirement to execute every edge.

## Crown blocker rule

```text
BLOCKED iff no lawful authorized path from the current state to the requested acceptance state remains.
```

Consequently:

- `gh` unavailable is a local transport observation, not `GH_FIX_CI=BLOCKED`;
- `git push` unavailable does not forbid connector-native blob/tree/commit/ref publication;
- a repository-scoped `GITHUB_TOKEN` that cannot clone a private dependency establishes only that principal-route failure;
- `NEXT_ROUTE_IDENTIFIED` is mandatory while exact packages, artifacts, existing App authority, dependency-side manufacture, lawful materialization, or other compatible execution routes remain.

## Qualification

The pack is a marketplace **projection** profile: it has canonical Turtle plus Tera templates and no project-local `ggen.toml`. The all-pack qualification court therefore loads it through a real ggen pack reference, syncs twice within the repository's bounded qualification contract, requires deterministic replay, and checks that canonical marketplace source remains byte-identical.

The SPARQL gates additionally protect:

- exactly five transport planes;
- exactly seven cross-private dependency routes;
- exactly 28 constitutional rules;
- distinct authority-principal inventory;
- the exact-head and no-false-blocker laws;
- the definition of `BLOCKED` as terminal route exhaustion.

## Evidence boundary

Marketplace qualification proves ggen load/manufacture/replay behavior for this pack. It does not itself prove that a consumer's GitHub App, Actions token, runner, package registry, or external repository has a particular authority. Those identities must be reobserved at the exact task boundary.
