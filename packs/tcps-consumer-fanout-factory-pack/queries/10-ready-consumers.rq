PREFIX tcf: <https://ggen.dev/ontology/tcps-consumer-fanout#>
SELECT ?consumer ?head WHERE {
  ?consumer a tcf:ConsumerFamily ; tcf:consumerHead ?head .
  FILTER NOT EXISTS { ?consumer tcf:standing "BLOCKED" }
}
ORDER BY ?consumer
