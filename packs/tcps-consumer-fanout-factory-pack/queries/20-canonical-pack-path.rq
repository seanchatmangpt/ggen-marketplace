PREFIX tcf: <https://ggen.dev/ontology/tcps-consumer-fanout#>
SELECT ?candidate ?consumer ?packPath WHERE {
  ?candidate a tcf:FanoutCandidate ; tcf:consumer ?consumer ; tcf:packPath ?packPath .
}
ORDER BY ?consumer ?candidate ?packPath
