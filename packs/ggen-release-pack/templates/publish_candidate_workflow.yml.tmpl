---
to: .github/workflows/publish-candidate.yml
sparql:
  row: |
    PREFIX grp: <http://seanchatmangpt.github.io/packs/ggen-release#>
    SELECT ?body WHERE {
      grp:PublishCandidateWorkflow grp:workflowBody ?body .
    } ORDER BY ?body
---
{{ row[0].body }}
