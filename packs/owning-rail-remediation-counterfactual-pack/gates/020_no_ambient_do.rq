PREFIX orc: <https://seanchatmangpt.github.io/ontology/owning-rail-remediation-counterfactual#>
SELECT ?r ?authority ?actuated WHERE {
  ?r a orc:Remediation ; orc:authority ?authority ; orc:actuationPerformed ?actuated .
  FILTER(str(?authority) = "DO" || ?actuated = true)
}
