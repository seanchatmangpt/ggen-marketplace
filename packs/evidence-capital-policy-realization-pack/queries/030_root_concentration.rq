PREFIX ecpr: <https://chatman.ai/ontology/evidence-capital-policy-realization#>
SELECT ?strategy (MAX(?r) AS ?worstConcentration) WHERE {
 ?d a ecpr:PolicyDecision ; ecpr:strategy ?strategy .
 ?o a ecpr:PolicyOutcome ; ecpr:realizedRootConcentration ?r .
} GROUP BY ?strategy ORDER BY ?strategy
