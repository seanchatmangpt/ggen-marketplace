PREFIX ecpr: <https://chatman.ai/ontology/evidence-capital-policy-realization#>
SELECT ?s WHERE {
 ?s a ecpr:PolicySubject ; ecpr:subjectSha ?sha ; ecpr:semanticDigest ?digest .
 FILTER(!regex(str(?sha), "^[0-9a-f]{40}$") || !regex(str(?digest), "^[0-9a-f]{64}$"))
}
