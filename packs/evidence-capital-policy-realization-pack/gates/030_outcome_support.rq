PREFIX ecpr: <https://chatman.ai/ontology/evidence-capital-policy-realization#>
SELECT ?o WHERE {
 ?o a ecpr:PolicyOutcome ; ecpr:realizedUtility ?u ; ecpr:realizedFalseCapital ?f ; ecpr:realizedRootConcentration ?r .
 FILTER(?f < 0 || ?f > 1 || ?r < 0 || ?r > 1)
}
