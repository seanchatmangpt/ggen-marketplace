PREFIX ar: <https://ggen.dev/ontology/ash-revops-structural#>
SELECT ?consumer ?qualification WHERE {
  ?consumer a ar:Consumer ; ar:hasQualification ?qualification .
  ?qualification a ar:Qualification .
} ORDER BY ?consumer ?qualification
