PREFIX ar: <https://ggen.dev/ontology/ash-revops-structural#>
SELECT ?candidate ?pack WHERE {
  ?candidate a ar:CandidateStructure ; ar:hasPackOption ?pack .
  ?pack a ar:PackOption .
} ORDER BY ?candidate ?pack
