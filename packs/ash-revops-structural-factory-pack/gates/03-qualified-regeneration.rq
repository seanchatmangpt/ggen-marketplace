PREFIX ar: <https://ggen.dev/ontology/ash-revops-structural#>
ASK {
  FILTER NOT EXISTS {
    ?knowledge a ar:GeneralizableKnowledge ; ar:regenerates ?consumer .
    FILTER NOT EXISTS { ?consumer ar:hasQualification ?qualification . ?qualification a ar:Qualification . }
  }
}
