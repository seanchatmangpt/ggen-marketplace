defmodule {{ module_name | default(value="Generated.RevOpsWorkspace") }} do
  @revenue_from {{ revenue_from | default(value=[]) | json_encode() }}
  @revenue_for {{ revenue_for | default(value=[]) | json_encode() }}

  def revenue_from, do: @revenue_from
  def revenue_for, do: @revenue_for
end
