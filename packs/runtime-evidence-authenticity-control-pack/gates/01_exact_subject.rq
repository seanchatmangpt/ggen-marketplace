PREFIX reac: <https://chatman.ai/ontology/runtime-evidence-authenticity-control#>
ASK { ?s reac:subjectSha ?sha ; reac:semanticDigest ?digest ; reac:generation ?g . FILTER(strlen(str(?sha))=40 && strlen(str(?digest))=64 && ?g>=0) }
