def test_generated_runtime_evidence_control_boundary():
    decision = {{ decision_json }}
    assert decision["authority"] == "SELECT"
    assert decision["actuation_performed"] is False
    assert decision["standing"] in {"UNKNOWN","PARTIAL_ALIVE","BUILD_BROKEN","REFUSED"}
