[pack]
name = "runtime-evidence-authenticity-control-pack"
version = "0.1.0"
description = "Production admission over measured runtime-evidence authenticity with owner failure dominance and BRCE-only consequential DO."

[authority]
allowed = ["OBSERVE", "VERIFY", "SELECT", "CONSTRUCT"]
consequential_do = "BRCE_ONLY"

[manufacture]
ontology = "ontology.ttl"
ggen = "ggen.toml"
generated_outputs_are_consequences = true
