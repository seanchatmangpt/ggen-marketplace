def test_generated_selection_evidence_acquisition_boundary():
    plan = {{ plan_json }}
    assert plan["authority"] == "SELECT"
    assert plan["actuation_performed"] is False
    assert plan["experiment"]
