name: DEVELOP Adaptive Capital R20 Exact Head
on:
  pull_request:
permissions:
  contents: read
concurrency:
  group: develop-adaptive-capital-r20-${{ github.event.pull_request.number }}
  cancel-in-progress: true
jobs:
  qualify:
    if: github.event.pull_request.head.ref == 'develop/adaptive-capital-controller-r20-20260824'
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.sha }}
          persist-credentials: false
      - name: Assert exact head
        run: test "$(git rev-parse HEAD)" = "${{ github.event.pull_request.head.sha }}"
      - name: Validate marketplace
        run: python3 scripts/marketplace.py validate
      - name: Verify adaptive capital surface
        run: |
          test -f packs/ggen-project-boundary-pack/ontology-r20-adaptive-capital.ttl
          test "$(find packs/ggen-project-boundary-pack/queries -maxdepth 1 -name 'r20-*capital*.rq' -o -name 'r20-*feedback.rq' | wc -l)" -ge 6
          python3 -c "import json; p=json.load(open('receipts/select/2026-08-24-adaptive-capital-controller-r20.json')); assert p['authority']=='SELECT' and p['actuation_performed'] is False and len(p['strategies'])==3"
