# WAVE-RECEIPT — agent G1, ggen maximization wave v26.9.18 (ticket g1-cli-bridge-pack)

Date: 2026-09-18/19 (session). Operator did NOT write: every pack file below, every
rendered wrapper line, the gate queries, the fixture, and all rendered Elixir in the
proof tree. Operator keystrokes this wave: 0 on 産面 hand-authored code.

## Standing

**PARTIAL_ALIVE** — the bpm:CliBridge family renders real, compiling, green-running
wrappers from facts (observed this session, final artifact); retirement of the
hand-written BeamPM.Dfcm lines awaits the consumer cutover commit (this wave never
writes to beam4pm). Scope disclosure: `json_stdin` / `temp_file` template arms are
render+compile observed on the synthetic qualification fixture only (no real lab
command exercises those modes today — none of the lab's one-shot commands needs
them yet); ok_map / batch / typed_refusal + positional/flags are exercised against
the REAL lab CLI.

## 階 decision

EXTEND `beam4pm-process-model-pack` (0.1.19 → **0.1.20**), not INVENT. REUSE rung:
`protocol-integration-pack`'s `pr:PriorArtAdapter` cli-subprocess adapter was
evaluated and REJECTED (raw System.cmd passthrough: no JSON envelope decode, no
typed refusal shapes, no resolution ladder, one fixed subcommand) — failed edge
recorded in the pack ontology.ttl header. The CLI bridge is the sibling of the
pack's bpm:Engine facade family (same consumer, same counted-debt ledger, same
gates/qualification conventions).

## SHAs / trees

| tree | branch | head |
|---|---|---|
| /Users/sac/ggen-marketplace-wt/g1 (pack authorship) | `pack/cli-bridge` | 6f63cdd69 (0.1.20 facts) + 855cee5c2 (cherry-picked reviewed ceiling raise f2ae5382e) + follow-up commit (Tera/f fixes, this receipt) |
| /Users/sac/beam4pm-worktrees/wt-g1-proof (proof scratch) | `scratch/g1-proof` @ 27ff280 | all edits UNCOMMITTED by design (ggen.toml repoint, ontology.ttl admissions, rendered lib/beam4pm_cli_autofde.ex, priv/bin/autofde trampoline copy) |

Rendered artifact: `lib/beam4pm_cli_autofde.ex` (208 lines), sha256
`ee0fa5cdd75ac072ac40e13d15e641c5c0967010c2cbe607375f645bb1479bee`, marker
`# GENERATED by ggen from the admitted bpm:CliBridge graph (autofde). Do not edit.`

## Pack changes (all in packs/beam4pm-process-model-pack/)

- `ontology.ttl` — bpm:CliBridge / bpm:CliCommand / bpm:CliArg / bpm:CliWitness +
  CLOSED vocabularies: bpm:CliResultShape (ok_map | batch | typed_refusal),
  bpm:CliArgMode (positional | flags | json_stdin | temp_file), bpm:CliArgEncoding
  (raw | json), bpm:CliArgType (string | map; Elixir leg only — Erlang/Gleam legs
  and an optional-keyword-flags mode are recorded failed edges).
- `templates/beam4pm_cli_bridge.ex.tmpl` — for_each: bridges; frontmatter SPARQL
  (bridges/commands/args), per-shape collapse, resolution ladder, Task.yield +
  Task.shutdown(:brutal_kill) timeout.
- `gates/100_cli_command_required.rq` (080 pattern) and
  `gates/110_cli_vocab_witnessed.rq` (090 pattern + the WITNESSING FENCE: a
  rendered command must be witnessed by a real observed run).
- `qualification/consumer.ttl` — synthetic `frobnicate` bridge exercising EVERY
  closed-vocabulary member (incl. json_stdin + temp_file arms) with
  structurally-complete fixture witnesses (declared synthetic in-file).
- `pack.toml` — version 0.1.20 + dated description increment (v0.1.20 2026-09-18).
- Cherry-picked 855cee5c2 = upstream reviewed raise (hand_authored_qualification
  35→50, native_engine_facade 6→7, integration commit f2ae5382e) — required for
  ANY full sync against the current consumer tree; replicated, not re-invented.

## Commands + exits (observed)

| # | command (tree) | exit | evidence |
|---|---|---|---|
| 1 | `mix deps.get` (wt-g1-proof) | 0 | deps resolved |
| 2 | witness runs (see below) | 0 | tmp_probe/witness/*.stdout.json + sha256s |
| 3 | gates vs merged fixture graph (pyoxigraph, oxigraph engine) | 0 rows | 100: 0 rows, 110: 0 rows |
| 4 | in-memory plant of fabricated command (pack fixture graph) | refused | 1 row `cli_frob_time_travel \| unwitnessed_command` |
| 5 | `ggen sync run --dry-run` / `ggen sync run` (consumer, ggen.toml → wt-g1 pack) | 0 / 0 | FM-PACK-008 → prescribed re-lock; FM-WRITE-005 → one drifted doc cleared (vendored rsync churn, scratch) |
| 6 | `elixirc lib/beam4pm_cli_autofde.ex` | 0 | **0 warnings** |
| 7 | `mix run -e` green suite (final artifact) | 0 | ALL GREEN (details below) |
| 8 | planted falsifier on merged CONSUMER graph copy | refused | `cli_ocel_time_travel \| unwitnessed_command` + `cliwitness_vacuous \| vacuous_witness_missing_observed_evidence`; clean graph: 0+0 rows |
| 9 | qualification fixture render + `elixirc` (qual_render scratch project) | 0 / 0 | **0 warnings**; json_stdin stdin_data plumbing, temp_file try/after + do_/defp split, flags-json all render-observed |

Witness runs (real lab CLI via the trampoline, captured under tmp_probe/witness/):
- `priv/bin/autofde fabric cache-stats` → exit 0, envelope sha256 `ac18fd9f…36d`
- `priv/bin/autofde ocel validate tmp_probe/witness/ocel-validate-witness.json` → exit 0, sha256 `1d17c323…fd7`
- `priv/bin/autofde sa2a replay '<manifest>' --expected-hash 097126b4…d09` → exit 0, sha256 `91c44e7e…bcb`
  (computed_hash discovered by a real refused probe run first: exit 1 with the lab's verdict envelope — the exact case `typed_refusal` exists for)

Green suite (mix run -e, FINAL rendered artifact, exit 0):
1. `fabric_cache_stats/0` → `{:ok, %{"entries" => 8, "hits" => 40, "hit_rate" => 0.714…, "schema" => "autofde_lab.fabric.errc-cache/2", …}}` (real lab counters)
2. `ocel_validate/1` doctest body → `{true, 1, 1, nil}` (byte-equal semantics to hand-written)
3. `sa2a_replay/2` → bad hash relayed typed `{:error, {:replay_refused, %{"ok" => false, "computed_hash" => <64 hex>, …}}}`; correct hash → `{:ok, %{"ok" => true, "verified" => true}}`
4. `run_cli(args, 1)` → `{:error, {:cli_timeout, 1}}` (budget enforced, no hang)
5. `AUTOFDE_CLI_PATH=/nonexistent/...` → env rung wins → `{:error, {:binary_missing, path}}`; default rung restored → `cli_available? == true`

## Render-diff table (semantic diff vs hand-written wt-integration/lib/beam4pm_dfcm.ex)

| function | hand lines | rendered lines | deltas (all documented, none silent) |
|---|---|---|---|
| `ocel_validate/1` | 39 (Dfcm:602–641) | 43 (cli_autofde:31–73) | (a) module BeamPM.Dfcm→BeamPM.DfcmCli (doctest in fact updated to match); (b) `run_cli(argv, 30000)` — Task-timeout the hand runner lacked; (c) `when is_binary(log_path)` guard added; (d) inline comment naming bpm:CliResultShape_ok_map; (e) projection identical (5 atom fields, `ok: ok` bound var, Map.fetch!/2 rest); error arm `{:ocel_validate_error, err, resp}` identical; passthrough arm identical. Doc: "+ timeout" in the error clause. |
| `sa2a_replay` | 40 (Dfcm:808–848) | 38 (cli_autofde:76–113) | (a) SIGNATURE: hand takes one map `%{manifest_json:, expected_hash:}`, rendered takes two `String.t()` positionals — same is_binary guards on both values, same argv, same refusal relay; (b) timeout (as above); (c) refusal arm comments name the shape fact. Success/refusal/passthrough arms semantically identical. Failed edge: the map-keyed signature is presentation, not behavior — a `map_field` arg mode would be vocabulary for ONE observed case, so it was not invented. |
| `fabric_cache_stats/0` | 0 (did not exist) | 17 (cli_autofde:116–132) | net-new, facts only: group `fabric`, subcommand `cache-stats`, bpm:CliResultShape_batch (observed bare counter payload, no "ok" envelope), 10s budget. Run GREEN vs real lab CLI. |
| `cli_path/0`, `cli_available?/0`, `run_cli/2,3` | ≈26 (Dfcm:540–576, no timeout) | 56 | resolution ladder identical (app env → ENV → Path.expand default); runner adds Task timeout + `{:cli_timeout, ms}` + `{:cli_task_exit, reason}` totality arm. These retire from Dfcm only at FULL cutover (6 other wrappers still call them). |

Unified diffs: `tmp_probe/diff_ocel_validate.unified.txt`, `tmp_probe/diff_sa2a_replay.unified.txt`
(in wt-g1-proof; 54 + 48 lines, every hunk accounted for above).

## 比 (honest)

- Retirable NOW (parity proven, green vs real CLI): **79 hand-written lines**
  (ocel_validate 39 + sa2a_replay 40) rendered from facts as 81 rendered lines.
  Their deletion from BeamPM.Dfcm is the consumer cutover commit — REMAINING (this
  wave never commits to beam4pm).
- Net-new manufactured: fabric_cache_stats/0, 17 lines, 0 hand-written
  counterparts (prevented future hand-write).
- Bridge plumbing (56 lines) retires the hand runner (≈26 lines) only when the
  remaining 6 wrappers (catalog, ocel_conformance, graphlaw_hash/validate/hooks,
  sa2a_validate_card, sa2a_admit, fabric_match, fabric_solve — per wrapper count
  ≈9 in the wave context) land as facts. Not counted as retired here.
- Whole rendered file 208 lines; every line attributable to the template +
  admitted facts + reconciliation manifest from `ggen sync run` (exit 0).
- Rendered-line attribution: manufactured 208/208 of the new file; hand-written
  delta on 産面 this wave: 0 (proof tree only; all consumer edits uncommitted and
  reproducible from facts).

## Falsifiers attempted (all observed, none papered over)

1. Gate negative control (pack fixture graph): fabricated unwitnessed command →
   refused `unwitnessed_command`. OBSERVED.
2. Gate negative control (merged consumer graph copy): planted
   `cli_ocel_time_travel` (complete, plausible, NO witness) → refused
   `unwitnessed_command`; planted evidence-free witness → refused
   `vacuous_witness_missing_observed_evidence`; clean graph → 0 rows both gates. OBSERVED.
3. Render defect caught by real render, not review: `sa2a_replay` first rendered
   with ONE parameter while argv referenced `expected_hash` (the SPARQL `inline`
   flag conflated function-parameter with argv-position). Fixed in template facts
   (every mode takes a parameter; modes differ only in wire routing). OBSERVED + FIXED.
4. Compiler-as-falsifier: first render tripped Elixir outdented-heredoc warnings →
   rule established: cmdDoc facts carry full 2-space indentation including their
   first line (fact-side formatting, matching hand-written style); final
   `elixirc` = 0 warnings on both consumer and fixture renders. OBSERVED + FIXED.
5. Timeout enforcement executed (1 ms budget vs real CLI) → `{:cli_timeout, 1}`. OBSERVED.
6. Resolution ladder executed (env rung override → binary_missing typed; default
   rung → available). OBSERVED.
7. Probe-script misassumption (expecting `{:ok, v1}` for a deliberately wrong
   hash) actually DEMONSTRATED the typed-refusal contract: the wrapper relayed the
   lab's refusal instead of crashing or fabricating. Documented, not hidden.

Integration frictions hit and resolved (all with prescribed remediations):
FM-PACK-003 (TOML `" escaping in the dated increment`), FM-PACK-008 (lock hash
mismatch → prescribed `rm ggen.lock` re-lock), FM-WRITE-005 (one drifted
docs/reference/beam4pm_hand_authored_source.md from repopulating the empty
vendored submodule at marketplace 800b8c6c5 — cleared, scratch), hand-authored
ceiling 36 > 35 (cherry-picked the integration branch's reviewed f2ae5382e raise).

## Remaining

1. Consumer cutover commit (beam4pm side): admit the CliBridge facts + witnesses,
   delete the two retired wrappers (+ eventually runner) from BeamPM.Dfcm, wire
   the doctest swap — unlocks the 79-line retirement this wave proved.
2. Facts for the remaining ~6–7 autofde wrappers (catalog, ocel_conformance,
   graphlaw_*, sa2a_validate_card, sa2a_admit, fabric_match, fabric_solve). Two
   need vocabulary members first: an optional-keyword-flags mode
   (graphlaw_validate/2's `:shacl` opt) and — if the map-keyed signature must be
   preserved — a map_field arg mode (recorded failed edge, single observation).
3. ExUnit test template for the family (the rendered doctests are live text but
   no test module runs `doctest BeamPM.DfcmCli` yet; green proof was direct
   execution, observed in this receipt).
4. Erlang/Gleam legs (engine-family symmetry) — recorded failed edge, no consumer
   demand yet.
5. Ticket frontmatter `standing:` update + ledger deltas are the coordinator's
   (this agent never writes /Users/sac/beam4pm).
